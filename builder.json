{
  "name": "aws-sdk-swift",
  "needs_compiler": false,
  "packages": [],
  "variables": {
    "gradlew": "{source_dir}/gradlew -p {source_dir}"
  },
  "pre_build_steps": [
    "swift package edit --path {deps_dir}/smithy-swift/ ClientRuntime"
  ],
  "build_steps": [
    "{gradlew} assemble",
    "{gradlew} testAllProtocols",
    "swift build"
  ],
  "test_steps": [
    "swift test"
  ],
  "build_dir": "target/build",
  "hosts": {
    "ubuntu": {
      "packages": [
        "openjdk-8-jdk-headless"
      ]
    },
    "debian": {
      "packages": [
        "openjdk-8-jdk-headless"
      ]
    }
  },
  "upstream": [
    {
      "name": "smithy-swift"
    }
  ]
}