//
// Copyright Amazon.com Inc. or its affiliates.
// All Rights Reserved.
//
// SPDX-License-Identifier: Apache-2.0
//

// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime
import Foundation
@testable import SerdeBenchmarkAWSRestJSON
import Smithy
import SmithyHTTPAPI
import SmithyTestUtil
import XCTest
import func SmithyTestUtil.dummyIdentityResolver
import struct SmithyHTTPAPI.Endpoint


class CopyObjectRequestTest: HttpRequestTestBase {
    class SerializationBenchmarkInterceptor<InputType, OutputType>: Interceptor {
        typealias RequestType = HTTPRequest
        typealias ResponseType = HTTPResponse

        let serializationTime: BoxedDouble

        public init(
            _ serializationTime: BoxedDouble
        ) {
            self.serializationTime = serializationTime
        }

        func readAfterSerialization(context: some AfterSerialization<InputType, RequestType>) async throws {
            let serializeDuration = context.getAttributes().get(key: AttributeKey<Double>(name: "SerializeDuration"))
            serializationTime.value = serializeDuration ?? 0
        }
    }

    class SerializationBenchmarkInterceptorProvider: HttpInterceptorProvider {
        let serializationTime: BoxedDouble

        public init(
            _ serializationTime: BoxedDouble
        ) {
            self.serializationTime = serializationTime
        }
      func create<InputType, OutputType>() -> any Interceptor<InputType, OutputType, HTTPRequest, HTTPResponse> {
        return SerializationBenchmarkInterceptor(serializationTime)
      }
    }
    func logSerdeBenchmarkResult(_ message: String) {
        let fileManager = FileManager.default
        let path = fileManager.currentDirectoryPath + "/serde_benchmark_log.txt"

        let line = message + "\n"

        if fileManager.fileExists(atPath: path) {
            if let handle = FileHandle(forWritingAtPath: path) {
                handle.seekToEndOfFile()
                handle.write(Data(line.utf8))
                handle.closeFile()
            }
        } else {
            fileManager.createFile(atPath: path, contents: Data(line.utf8))
        }
    }

    func calculatePercentiles(_ measurements: [Double]) -> (p50: Double, p90: Double, p95: Double, p99: Double) {
        let sorted = measurements.sorted()
        let count = sorted.count
        let nsPerSecond = 1_000_000_000.0

        func percentile(_ p: Double) -> Double {
            let index = p / 100.0 * Double(count - 1)
            let lower = Int(floor(index))
            let upper = Int(ceil(index))
            if lower == upper {
                return sorted[lower] * nsPerSecond
            }
            let fraction = index - Double(lower)
            return (sorted[lower] * (1 - fraction) + sorted[upper] * fraction) * nsPerSecond
        }

        return (
            p50: percentile(50),
            p90: percentile(90),
            p95: percentile(95),
            p99: percentile(99)
        )
    }

    func calculateAndFormatMetrics(from measurements: [Double], testID: String) -> String {
        let runCount = 10000

        // Calcluate mean
        var sum = 0.0
        for num in measurements {
            sum += num
        }
        let mean = sum / Double(runCount)

        // Calculate standard deviation
        var diffSquaredSum = 0.0
        for num in measurements {
            diffSquaredSum += (num - mean) * (num - mean)
        }
        let sd = sqrt(diffSquaredSum / Double(runCount - 1))
        let percentiles = calculatePercentiles(measurements)
        return """
        {
            "id": "\(testID)",
            "n": \(runCount),
            "mean": \(mean * 1_000_000_000) ns,
            "p50": \(percentiles.p50) ns,
            "p90": \(percentiles.p90) ns,
            "p95": \(percentiles.p95) ns,
            "p99": \(percentiles.p99) ns,
            "std_dev": \(sd * 1_000_000_000) ns
        }
        """
    }

    class BoxedDouble {
        var value: Double
        init(_ value: Double) {
            self.value = value
        }
    }
    ///
    func testrestJson1_CopyObjectRequest_Baseline() async throws {
        let expected = buildExpectedHttpRequest(
            method: .put,
            path: "/test-bucket/test-key?x-id=CopyObject",
            body: nil,
            host: "example.com",
            resolvedHost: "example.com"
        )

        var config = try await AwsRestJsonDataPlaneClient.AwsRestJsonDataPlaneClientConfig(
            awsCredentialIdentityResolver: try SmithyTestUtil.dummyIdentityResolver(),
            region: "us-west-2",
            signingRegion: "us-west-2",
            endpointResolver: StaticEndpointResolver(endpoint: try SmithyHTTPAPI.Endpoint(urlString: "https://example.com")),
            idempotencyTokenGenerator: ProtocolTestIdempotencyTokenGenerator(),
            httpClientEngine: ProtocolTestClient()
        )
        let serializationTime: BoxedDouble = BoxedDouble(-1)
        config.addInterceptorProvider(SerializationBenchmarkInterceptorProvider(serializationTime))
        let client = AwsRestJsonDataPlaneClient(config: config)
        var measurements: [Double] = []

        let input = CopyObjectInput(
            bucket: "test-bucket",
            copySource: "/source-bucket/source-key",
            key: "test-key"
        )
        for i in 0..<20000 {
            do {
                _ = try await client.copyObject(input: input)
            } catch TestCheckError.actual(let actual) {
                if i >= 10000 {
                    measurements.append(serializationTime.value)
                }
            }
        }

        logSerdeBenchmarkResult(calculateAndFormatMetrics(from: measurements, testID: "restJson1_CopyObjectRequest_Baseline"))
    }
    /// Serialization of a large set of headers.
    func testrestJson1_CopyObjectRequest_M() async throws {
        let expected = buildExpectedHttpRequest(
            method: .put,
            path: "/dest-bucket/dest-key?x-id=CopyObject",
            body: nil,
            host: "example.com",
            resolvedHost: "example.com"
        )

        var config = try await AwsRestJsonDataPlaneClient.AwsRestJsonDataPlaneClientConfig(
            awsCredentialIdentityResolver: try SmithyTestUtil.dummyIdentityResolver(),
            region: "us-west-2",
            signingRegion: "us-west-2",
            endpointResolver: StaticEndpointResolver(endpoint: try SmithyHTTPAPI.Endpoint(urlString: "https://example.com")),
            idempotencyTokenGenerator: ProtocolTestIdempotencyTokenGenerator(),
            httpClientEngine: ProtocolTestClient()
        )
        let serializationTime: BoxedDouble = BoxedDouble(-1)
        config.addInterceptorProvider(SerializationBenchmarkInterceptorProvider(serializationTime))
        let client = AwsRestJsonDataPlaneClient(config: config)
        var measurements: [Double] = []

        let input = CopyObjectInput(
            acl: "private",
            bucket: "dest-bucket",
            bucketKeyEnabled: true,
            cacheControl: "max-age=3600",
            checksumAlgorithm: "SHA256",
            contentDisposition: "attachment; filename=\"example.txt\"",
            contentEncoding: "gzip",
            contentLanguage: "en-US",
            contentType: "text/plain",
            copySource: "/source-bucket/source-key",
            copySourceIfMatch: "\"9bb58f26192e4ba00f01e2e7b136bbd8\"",
            copySourceIfModifiedSince: Date(timeIntervalSince1970: 1609459200),
            copySourceIfNoneMatch: "\"different-etag\"",
            copySourceIfUnmodifiedSince: Date(timeIntervalSince1970: 1640995199),
            copySourceSSECustomerAlgorithm: "AES256",
            copySourceSSECustomerKey: "source-customer-key-base64",
            copySourceSSECustomerKeyMD5: "source-customer-key-md5-hash",
            expectedBucketOwner: "123456789012",
            expectedSourceBucketOwner: "123456789012",
            expires: Date(timeIntervalSince1970: 1641024000),
            grantFullControl: "id=canonical-user-id",
            grantRead: "id=read-user-id",
            grantReadACP: "id=read-acp-user-id",
            grantWriteACP: "id=write-acp-user-id",
            ifMatch: "\"target-etag\"",
            ifNoneMatch: "\"different-target-etag\"",
            key: "dest-key",
            metadataDirective: "REPLACE",
            objectLockLegalHoldStatus: "ON",
            objectLockMode: "GOVERNANCE",
            objectLockRetainUntilDate: Date(timeIntervalSince1970: 1641024000),
            requestPayer: "BucketOwner",
            serverSideEncryption: "AES256",
            sseCustomerAlgorithm: "AES256",
            sseCustomerKey: "customer-key-base64",
            sseCustomerKeyMD5: "customer-key-md5-hash",
            ssekmsEncryptionContext: "encryption-context",
            ssekmsKeyId: "arn:aws:kms:us-east-1:123456789012:key/12345678-1234-1234-1234-123456789012",
            storageClass: "STANDARD_IA",
            tagging: "key1=value1&key2=value2",
            taggingDirective: "REPLACE",
            websiteRedirectLocation: "https://example.com/redirect"
        )
        for i in 0..<20000 {
            do {
                _ = try await client.copyObject(input: input)
            } catch TestCheckError.actual(let actual) {
                if i >= 10000 {
                    measurements.append(serializationTime.value)
                }
            }
        }

        logSerdeBenchmarkResult(calculateAndFormatMetrics(from: measurements, testID: "restJson1_CopyObjectRequest_M"))
    }
}
