//
// Copyright Amazon.com Inc. or its affiliates.
// All Rights Reserved.
//
// SPDX-License-Identifier: Apache-2.0
//

// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime
import Foundation
@testable import SerdeBenchmarkAWSRestJSON
import Smithy
import SmithyHTTPAPI
import SmithyTestUtil
import XCTest
import func SmithyTestUtil.dummyIdentityResolver
import struct SmithyHTTPAPI.Endpoint


class CopyObjectResponseTest: HttpResponseTestBase {
    class DeserializationBenchmarkInterceptor<InputType, OutputType>: Interceptor {
        typealias RequestType = HTTPRequest
        typealias ResponseType = HTTPResponse

        let deserializationTime: BoxedDouble

        public init(
            _ deserializationTime: BoxedDouble
        ) {
            self.deserializationTime = deserializationTime
        }

        func readAfterDeserialization(
            context: some AfterDeserialization<InputType, OutputType, RequestType, ResponseType>
        ) async throws {
            let deserializeDuration = context.getAttributes().get(key: AttributeKey<Double>(name: "DeserializeDuration"))
            deserializationTime.value = deserializeDuration ?? 0
        }
    }

    class DeserializationBenchmarkInterceptorProvider: HttpInterceptorProvider {
        let deserializationTime: BoxedDouble

        public init(
            _ deserializationTime: BoxedDouble
        ) {
            self.deserializationTime = deserializationTime
        }
      func create<InputType, OutputType>() -> any Interceptor<InputType, OutputType, HTTPRequest, HTTPResponse> {
        return DeserializationBenchmarkInterceptor(deserializationTime)
      }
    }
    func logSerdeBenchmarkResult(_ message: String) {
        let fileManager = FileManager.default
        let path = fileManager.currentDirectoryPath + "/serde_benchmark_log.txt"

        let line = message + "\n"

        if fileManager.fileExists(atPath: path) {
            if let handle = FileHandle(forWritingAtPath: path) {
                handle.seekToEndOfFile()
                handle.write(Data(line.utf8))
                handle.closeFile()
            }
        } else {
            fileManager.createFile(atPath: path, contents: Data(line.utf8))
        }
    }

    func calculatePercentiles(_ measurements: [Double]) -> (p50: Double, p90: Double, p95: Double, p99: Double) {
        let sorted = measurements.sorted()
        let count = sorted.count
        let nsPerSecond = 1_000_000_000.0

        func percentile(_ p: Double) -> Double {
            let index = p / 100.0 * Double(count - 1)
            let lower = Int(floor(index))
            let upper = Int(ceil(index))
            if lower == upper {
                return sorted[lower] * nsPerSecond
            }
            let fraction = index - Double(lower)
            return (sorted[lower] * (1 - fraction) + sorted[upper] * fraction) * nsPerSecond
        }

        return (
            p50: percentile(50),
            p90: percentile(90),
            p95: percentile(95),
            p99: percentile(99)
        )
    }

    func calculateAndFormatMetrics(from measurements: [Double], testID: String) -> String {
        let runCount = 10000

        // Calcluate mean
        var sum = 0.0
        for num in measurements {
            sum += num
        }
        let mean = sum / Double(runCount)

        // Calculate standard deviation
        var diffSquaredSum = 0.0
        for num in measurements {
            diffSquaredSum += (num - mean) * (num - mean)
        }
        let sd = sqrt(diffSquaredSum / Double(runCount - 1))
        let percentiles = calculatePercentiles(measurements)
        return """
        {
            "id": "\(testID)",
            "n": \(runCount),
            "mean": \(mean * 1_000_000_000) ns,
            "p50": \(percentiles.p50) ns,
            "p90": \(percentiles.p90) ns,
            "p95": \(percentiles.p95) ns,
            "p99": \(percentiles.p99) ns,
            "std_dev": \(sd * 1_000_000_000) ns
        }
        """
    }

    class BoxedDouble {
        var value: Double
        init(_ value: Double) {
            self.value = value
        }
    }
    ///
    func testrestJson1_CopyObjectOutput_Baseline() async throws {
        guard let httpResponse = buildHttpResponse(
            code: 200,
            headers: nil,
            content: nil
        ) else {
            XCTFail("Something is wrong with the created http response")
            return
        }

        var config = try await AwsRestJsonDataPlaneClient.AwsRestJsonDataPlaneClientConfig(
            awsCredentialIdentityResolver: try SmithyTestUtil.dummyIdentityResolver(),
            region: "us-west-2",
            signingRegion: "us-west-2",
            endpointResolver: StaticEndpointResolver(endpoint: try SmithyHTTPAPI.Endpoint(
                urlString: "https://example.com"
            )),
            httpClientEngine: ProtocolResponseTestClient(httpResponse: httpResponse)
        )

        let deserializationTime: BoxedDouble = BoxedDouble(-1)
        config.addInterceptorProvider(DeserializationBenchmarkInterceptorProvider(deserializationTime))
        var measurements: [Double] = []
        let client = AwsRestJsonDataPlaneClient(config: config)

        let input = CopyObjectInput(bucket: "test", key: "test")

        for i in 0..<20000 {
            let actual = try await client.copyObject(input: input)
            if i >= 10000 {
                measurements.append(deserializationTime.value)
            }
        }

        logSerdeBenchmarkResult(calculateAndFormatMetrics(from: measurements, testID: "restJson1_CopyObjectOutput_Baseline"))

        let expected = CopyObjectOutput(
        )

    }
    /// Deserialization of headers and JSON body.
    func testrestJson1_CopyObjectOutput_M() async throws {
        guard let httpResponse = buildHttpResponse(
            code: 200,
            headers: [
                "x-amz-copy-source-version-id": "source-version-id-12345",
                "x-amz-expiration": "expiry-date=\"Fri, 01 Jan 2022 00:00:00 GMT\", rule-id=\"rule1\"",
                "x-amz-request-charged": "requester",
                "x-amz-server-side-encryption": "AES256",
                "x-amz-server-side-encryption-aws-kms-key-id": "arn:aws:kms:us-east-1:123456789012:key/12345678-1234-1234-1234-123456789012",
                "x-amz-server-side-encryption-bucket-key-enabled": "true",
                "x-amz-server-side-encryption-context": "encryption-context",
                "x-amz-server-side-encryption-customer-algorithm": "AES256",
                "x-amz-server-side-encryption-customer-key-MD5": "customer-key-md5-hash",
                "x-amz-version-id": "dest-version-id-67890"
            ],
            content: .data(Data("""
            {
                "ETag": "\"9bb58f26192e4ba00f01e2e7b136bbd8\"",
                "LastModified": "2021-01-01T00:00:00.000Z",
                "ChecksumType": "SHA256",
                "ChecksumCRC32": "checksum-crc32",
                "ChecksumCRC32C": "checksum-crc32c",
                "ChecksumCRC64NVME": "checksum-crc64nvme",
                "ChecksumSHA1": "checksum-sha1",
                "ChecksumSHA256": "checksum-sha256"
            }

            """.utf8))
        ) else {
            XCTFail("Something is wrong with the created http response")
            return
        }

        var config = try await AwsRestJsonDataPlaneClient.AwsRestJsonDataPlaneClientConfig(
            awsCredentialIdentityResolver: try SmithyTestUtil.dummyIdentityResolver(),
            region: "us-west-2",
            signingRegion: "us-west-2",
            endpointResolver: StaticEndpointResolver(endpoint: try SmithyHTTPAPI.Endpoint(
                urlString: "https://example.com"
            )),
            httpClientEngine: ProtocolResponseTestClient(httpResponse: httpResponse)
        )

        let deserializationTime: BoxedDouble = BoxedDouble(-1)
        config.addInterceptorProvider(DeserializationBenchmarkInterceptorProvider(deserializationTime))
        var measurements: [Double] = []
        let client = AwsRestJsonDataPlaneClient(config: config)

        let input = CopyObjectInput(bucket: "test", key: "test")

        for i in 0..<20000 {
            let actual = try await client.copyObject(input: input)
            if i >= 10000 {
                measurements.append(deserializationTime.value)
            }
        }

        logSerdeBenchmarkResult(calculateAndFormatMetrics(from: measurements, testID: "restJson1_CopyObjectOutput_M"))

        let expected = CopyObjectOutput(
        )

    }
}
