// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

#if swift(>=5.5)
@available(macOS 12.0, iOS 15.0, tvOS 15.0, watchOS 8.0, macCatalyst 15.0, *)
public extension CostAndUsageReportClient {
    /// <p>Deletes the specified report.</p>
    func deleteReportDefinition(input: DeleteReportDefinitionInput) async throws -> DeleteReportDefinitionOutputResponse
    {
        typealias deleteReportDefinitionContinuation = CheckedContinuation<DeleteReportDefinitionOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: deleteReportDefinitionContinuation) in
            deleteReportDefinition(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// <p>Lists the AWS Cost and Usage reports available to this account.</p>
    func describeReportDefinitions(input: DescribeReportDefinitionsInput) async throws -> DescribeReportDefinitionsOutputResponse
    {
        typealias describeReportDefinitionsContinuation = CheckedContinuation<DescribeReportDefinitionsOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: describeReportDefinitionsContinuation) in
            describeReportDefinitions(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// <p>Allows you to programatically update your report preferences.</p>
    func modifyReportDefinition(input: ModifyReportDefinitionInput) async throws -> ModifyReportDefinitionOutputResponse
    {
        typealias modifyReportDefinitionContinuation = CheckedContinuation<ModifyReportDefinitionOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: modifyReportDefinitionContinuation) in
            modifyReportDefinition(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// <p>Creates a new report using the description that you provide.</p>
    func putReportDefinition(input: PutReportDefinitionInput) async throws -> PutReportDefinitionOutputResponse
    {
        typealias putReportDefinitionContinuation = CheckedContinuation<PutReportDefinitionOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: putReportDefinitionContinuation) in
            putReportDefinition(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

}
#endif
