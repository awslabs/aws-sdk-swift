// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

#if swift(>=5.5) && canImport(_Concurrency)
@available(macOS 12.0, iOS 15.0, tvOS 15.0, watchOS 8.0, macCatalyst 15.0, *)
public extension ResourceGroupsTaggingApiClient {
    /// Describes the status of the StartReportCreation operation.
    ///         You can call this operation only from the organization's
    ///     management account and from the us-east-1 Region.
    func describeReportCreation(input: DescribeReportCreationInput) async throws -> DescribeReportCreationOutputResponse
    {
        typealias describeReportCreationContinuation = CheckedContinuation<DescribeReportCreationOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: describeReportCreationContinuation) in
            describeReportCreation(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Returns a table that shows counts of resources that are noncompliant with their tag
    ///             policies.
    ///         For more information on tag policies, see <a href="https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_tag-policies.html">Tag Policies in
    ///             the AWS Organizations User Guide.
    ///
    ///         You can call this operation only from the organization's
    ///     management account and from the us-east-1 Region.
    ///          This operation supports pagination, where the response can be sent in
    ///     multiple pages. You should check the PaginationToken response parameter to determine
    ///     if there are additional results available to return. Repeat the query, passing the
    ///     PaginationToken response parameter value as an input to the next request until you
    ///     recieve a null value. A null value for PaginationToken indicates that
    ///     there are no more results waiting to be returned.
    func getComplianceSummary(input: GetComplianceSummaryInput) async throws -> GetComplianceSummaryOutputResponse
    {
        typealias getComplianceSummaryContinuation = CheckedContinuation<GetComplianceSummaryOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: getComplianceSummaryContinuation) in
            getComplianceSummary(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Returns all the tagged or previously tagged resources that are located in the
    ///             specified Region for the AWS account.
    ///         Depending on what information you want returned, you can also specify the
    ///             following:
    ///
    ///
    ///
    ///                   Filters that specify what tags and resource types you
    ///                     want returned. The response includes all tags that are associated with the
    ///                     requested resources.
    ///
    ///
    ///                 Information about compliance with the account's effective tag policy. For more
    ///                     information on tag policies, see <a href="https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_tag-policies.html">Tag
    ///                         Policies in the AWS Organizations User Guide.
    ///
    ///
    ///
    ///          This operation supports pagination, where the response can be sent in
    ///     multiple pages. You should check the PaginationToken response parameter to determine
    ///     if there are additional results available to return. Repeat the query, passing the
    ///     PaginationToken response parameter value as an input to the next request until you
    ///     recieve a null value. A null value for PaginationToken indicates that
    ///     there are no more results waiting to be returned.
    func getResources(input: GetResourcesInput) async throws -> GetResourcesOutputResponse
    {
        typealias getResourcesContinuation = CheckedContinuation<GetResourcesOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: getResourcesContinuation) in
            getResources(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Returns all tag keys currently in use in the specified Region for the calling AWS
    ///             account.
    ///          This operation supports pagination, where the response can be sent in
    ///     multiple pages. You should check the PaginationToken response parameter to determine
    ///     if there are additional results available to return. Repeat the query, passing the
    ///     PaginationToken response parameter value as an input to the next request until you
    ///     recieve a null value. A null value for PaginationToken indicates that
    ///     there are no more results waiting to be returned.
    func getTagKeys(input: GetTagKeysInput) async throws -> GetTagKeysOutputResponse
    {
        typealias getTagKeysContinuation = CheckedContinuation<GetTagKeysOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: getTagKeysContinuation) in
            getTagKeys(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Returns all tag values for the specified key that are used in the specified AWS
    ///             Region for the calling AWS account.
    ///          This operation supports pagination, where the response can be sent in
    ///     multiple pages. You should check the PaginationToken response parameter to determine
    ///     if there are additional results available to return. Repeat the query, passing the
    ///     PaginationToken response parameter value as an input to the next request until you
    ///     recieve a null value. A null value for PaginationToken indicates that
    ///     there are no more results waiting to be returned.
    func getTagValues(input: GetTagValuesInput) async throws -> GetTagValuesOutputResponse
    {
        typealias getTagValuesContinuation = CheckedContinuation<GetTagValuesOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: getTagValuesContinuation) in
            getTagValues(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Generates a report that lists all tagged resources in the accounts across your
    ///             organization and tells whether each resource is compliant with the effective tag policy.
    ///             Compliance data is refreshed daily. The report is generated asynchronously.
    ///         The generated report is saved to the following location:
    ///
    ///             s3://example-bucket/AwsTagPolicies/o-exampleorgid/YYYY-MM-ddTHH:mm:ssZ/report.csv
    ///
    ///         You can call this operation only from the organization's
    ///     management account and from the us-east-1 Region.
    func startReportCreation(input: StartReportCreationInput) async throws -> StartReportCreationOutputResponse
    {
        typealias startReportCreationContinuation = CheckedContinuation<StartReportCreationOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: startReportCreationContinuation) in
            startReportCreation(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Applies one or more tags to the specified resources. Note the following:
    ///
    ///
    ///                 Not all resources can have tags. For a list of services with resources that
    ///                     support tagging using this operation, see <a href="https://docs.aws.amazon.com/resourcegroupstagging/latest/APIReference/supported-services.html">Services that support the
    ///                         Resource Groups Tagging API.
    ///
    ///
    ///                 Each resource can have up to 50 tags. For other limits, see <a href="https://docs.aws.amazon.com/general/latest/gr/aws_tagging.html#tag-conventions">Tag Naming and Usage Conventions in the AWS General
    ///                         Reference.
    ///
    ///
    ///
    ///                 You can only tag resources that are located in the specified AWS Region for
    ///                     the AWS account.
    ///
    ///
    ///                 To add tags to a resource, you need the necessary permissions for the service
    ///                     that the resource belongs to as well as permissions for adding tags. For more
    ///                     information, see the documentation for each service.
    ///
    ///
    ///
    ///             Do not store personally identifiable information (PII) or other confidential or
    ///                 sensitive information in tags. We use tags to provide you with billing and
    ///                 administration services. Tags are not intended to be used for private or sensitive
    ///                 data.
    ///
    func tagResources(input: TagResourcesInput) async throws -> TagResourcesOutputResponse
    {
        typealias tagResourcesContinuation = CheckedContinuation<TagResourcesOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: tagResourcesContinuation) in
            tagResources(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

    /// Removes the specified tags from the specified resources. When you specify a tag key,
    ///             the action removes both that key and its associated value. The operation succeeds even
    ///             if you attempt to remove tags from a resource that were already removed. Note the
    ///             following:
    ///
    ///
    ///                 To remove tags from a resource, you need the necessary permissions for the
    ///                     service that the resource belongs to as well as permissions for removing tags.
    ///                     For more information, see the documentation for the service whose resource you
    ///                     want to untag.
    ///
    ///
    ///                 You can only tag resources that are located in the specified AWS Region for
    ///                     the calling AWS account.
    ///
    ///
    func untagResources(input: UntagResourcesInput) async throws -> UntagResourcesOutputResponse
    {
        typealias untagResourcesContinuation = CheckedContinuation<UntagResourcesOutputResponse, Swift.Error>
        return try await withCheckedThrowingContinuation { (continuation: untagResourcesContinuation) in
            untagResources(input: input) { result in
                switch result {
                    case .success(let output):
                        continuation.resume(returning: output)
                    case .failure(let error):
                        continuation.resume(throwing: error)
                }
            }
        }
    }

}
#endif
