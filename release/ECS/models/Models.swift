// Code generated by smithy-swift-codegen. DO NOT EDIT!
import AWSClientRuntime
import ClientRuntime

extension AccessDeniedException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "AccessDeniedException(message: \(String(describing: message)))"}
}

extension AccessDeniedException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: AccessDeniedExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>You do not have authorization to perform the requested action.</p>
public struct AccessDeniedException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct AccessDeniedExceptionBody: Equatable {
    public let message: String?
}

extension AccessDeniedExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public enum AgentUpdateStatus {
    case failed
    case pending
    case staged
    case staging
    case updated
    case updating
    case sdkUnknown(String)
}

extension AgentUpdateStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [AgentUpdateStatus] {
        return [
            .failed,
            .pending,
            .staged,
            .staging,
            .updated,
            .updating,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .failed: return "FAILED"
        case .pending: return "PENDING"
        case .staged: return "STAGED"
        case .staging: return "STAGING"
        case .updated: return "UPDATED"
        case .updating: return "UPDATING"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = AgentUpdateStatus(rawValue: rawValue) ?? AgentUpdateStatus.sdkUnknown(rawValue)
    }
}

public enum AssignPublicIp {
    case disabled
    case enabled
    case sdkUnknown(String)
}

extension AssignPublicIp : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [AssignPublicIp] {
        return [
            .disabled,
            .enabled,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .disabled: return "DISABLED"
        case .enabled: return "ENABLED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = AssignPublicIp(rawValue: rawValue) ?? AssignPublicIp.sdkUnknown(rawValue)
    }
}

extension Attachment: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case details
        case id
        case status
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let details = details {
            var detailsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .details)
            for attachmentdetails0 in details {
                try detailsContainer.encode(attachmentdetails0)
            }
        }
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let type = type {
            try encodeContainer.encode(type, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
        let typeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .type)
        type = typeDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let detailsContainer = try containerValues.decodeIfPresent([KeyValuePair?].self, forKey: .details)
        var detailsDecoded0:[KeyValuePair]? = nil
        if let detailsContainer = detailsContainer {
            detailsDecoded0 = [KeyValuePair]()
            for structure0 in detailsContainer {
                if let structure0 = structure0 {
                    detailsDecoded0?.append(structure0)
                }
            }
        }
        details = detailsDecoded0
    }
}

extension Attachment: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Attachment(details: \(String(describing: details)), id: \(String(describing: id)), status: \(String(describing: status)), type: \(String(describing: type)))"}
}

/// <p>An object representing a container instance or task attachment.</p>
public struct Attachment: Equatable {
    /// <p>Details of the attachment. For elastic network interfaces, this includes the network
    /// 			interface ID, the MAC address, the subnet ID, and the private IPv4 address.</p>
    public let details: [KeyValuePair]?
    /// <p>The unique identifier for the attachment.</p>
    public let id: String?
    /// <p> The status of the attachment. Valid values are <code>PRECREATED</code>,
    /// 				<code>CREATED</code>, <code>ATTACHING</code>, <code>ATTACHED</code>,
    /// 				<code>DETACHING</code>, <code>DETACHED</code>, and <code>DELETED</code>.</p>
    public let status: String?
    /// <p>The type of the attachment, such as <code>ElasticNetworkInterface</code>.</p>
    public let type: String?

    public init (
        details: [KeyValuePair]? = nil,
        id: String? = nil,
        status: String? = nil,
        type: String? = nil
    )
    {
        self.details = details
        self.id = id
        self.status = status
        self.type = type
    }
}

extension AttachmentStateChange: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attachmentArn
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attachmentArn = attachmentArn {
            try encodeContainer.encode(attachmentArn, forKey: .attachmentArn)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let attachmentArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .attachmentArn)
        attachmentArn = attachmentArnDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
    }
}

extension AttachmentStateChange: CustomDebugStringConvertible {
    public var debugDescription: String {
        "AttachmentStateChange(attachmentArn: \(String(describing: attachmentArn)), status: \(String(describing: status)))"}
}

/// <p>An object representing a change in state for a task attachment.</p>
public struct AttachmentStateChange: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the attachment.</p>
    public let attachmentArn: String?
    /// <p>The status of the attachment.</p>
    public let status: String?

    public init (
        attachmentArn: String? = nil,
        status: String? = nil
    )
    {
        self.attachmentArn = attachmentArn
        self.status = status
    }
}

extension Attribute: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case targetId
        case targetType
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let targetId = targetId {
            try encodeContainer.encode(targetId, forKey: .targetId)
        }
        if let targetType = targetType {
            try encodeContainer.encode(targetType.rawValue, forKey: .targetType)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let targetTypeDecoded = try containerValues.decodeIfPresent(TargetType.self, forKey: .targetType)
        targetType = targetTypeDecoded
        let targetIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .targetId)
        targetId = targetIdDecoded
    }
}

extension Attribute: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Attribute(name: \(String(describing: name)), targetId: \(String(describing: targetId)), targetType: \(String(describing: targetType)), value: \(String(describing: value)))"}
}

/// <p>An attribute is a name-value pair associated with an Amazon ECS object. Attributes enable
/// 			you to extend the Amazon ECS data model by adding custom metadata to your resources. For more
/// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-placement-constraints.html#attributes">Attributes</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct Attribute: Equatable {
    /// <p>The name of the attribute. The <code>name</code> must contain between 1 and 128
    /// 			characters and name may contain letters (uppercase and lowercase), numbers, hyphens,
    /// 			underscores, forward slashes, back slashes, or periods.</p>
    public let name: String?
    /// <p>The ID of the target. You can specify the short form ID for a resource or the full
    /// 			Amazon Resource Name (ARN).</p>
    public let targetId: String?
    /// <p>The type of the target with which to attach the attribute. This parameter is required
    /// 			if you use the short form ID for a resource instead of the full ARN.</p>
    public let targetType: TargetType?
    /// <p>The value of the attribute. The <code>value</code> must contain between 1 and 128
    /// 			characters and may contain letters (uppercase and lowercase), numbers, hyphens,
    /// 			underscores, periods, at signs (@), forward slashes, back slashes, colons, or spaces.
    /// 			The value cannot contain any leading or trailing whitespace.</p>
    public let value: String?

    public init (
        name: String? = nil,
        targetId: String? = nil,
        targetType: TargetType? = nil,
        value: String? = nil
    )
    {
        self.name = name
        self.targetId = targetId
        self.targetType = targetType
        self.value = value
    }
}

extension AttributeLimitExceededException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "AttributeLimitExceededException(message: \(String(describing: message)))"}
}

extension AttributeLimitExceededException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: AttributeLimitExceededExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>You can apply up to 10 custom attributes per resource. You can view the attributes of
/// 			a resource with <a>ListAttributes</a>. You can remove existing attributes on
/// 			a resource with <a>DeleteAttributes</a>.</p>
public struct AttributeLimitExceededException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct AttributeLimitExceededExceptionBody: Equatable {
    public let message: String?
}

extension AttributeLimitExceededExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension AutoScalingGroupProvider: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case autoScalingGroupArn
        case managedScaling
        case managedTerminationProtection
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let autoScalingGroupArn = autoScalingGroupArn {
            try encodeContainer.encode(autoScalingGroupArn, forKey: .autoScalingGroupArn)
        }
        if let managedScaling = managedScaling {
            try encodeContainer.encode(managedScaling, forKey: .managedScaling)
        }
        if let managedTerminationProtection = managedTerminationProtection {
            try encodeContainer.encode(managedTerminationProtection.rawValue, forKey: .managedTerminationProtection)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let autoScalingGroupArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .autoScalingGroupArn)
        autoScalingGroupArn = autoScalingGroupArnDecoded
        let managedScalingDecoded = try containerValues.decodeIfPresent(ManagedScaling.self, forKey: .managedScaling)
        managedScaling = managedScalingDecoded
        let managedTerminationProtectionDecoded = try containerValues.decodeIfPresent(ManagedTerminationProtection.self, forKey: .managedTerminationProtection)
        managedTerminationProtection = managedTerminationProtectionDecoded
    }
}

extension AutoScalingGroupProvider: CustomDebugStringConvertible {
    public var debugDescription: String {
        "AutoScalingGroupProvider(autoScalingGroupArn: \(String(describing: autoScalingGroupArn)), managedScaling: \(String(describing: managedScaling)), managedTerminationProtection: \(String(describing: managedTerminationProtection)))"}
}

/// <p>The details of the Auto Scaling group for the capacity provider.</p>
public struct AutoScalingGroupProvider: Equatable {
    /// <p>The Amazon Resource Name (ARN) that identifies the Auto Scaling group.</p>
    public let autoScalingGroupArn: String?
    /// <p>The managed scaling settings for the Auto Scaling group capacity provider.</p>
    public let managedScaling: ManagedScaling?
    /// <p>The managed termination protection setting to use for the Auto Scaling group capacity
    /// 			provider. This determines whether the Auto Scaling group has managed termination
    /// 			protection.</p>
    /// 		       <important>
    /// 			         <p>When using managed termination protection, managed scaling must also be used
    /// 				otherwise managed termination protection will not work.</p>
    /// 		       </important>
    /// 		       <p>When managed termination protection is enabled, Amazon ECS prevents the Amazon EC2 instances in
    /// 			an Auto Scaling group that contain tasks from being terminated during a scale-in action.
    /// 			The Auto Scaling group and each instance in the Auto Scaling group must have instance
    /// 			protection from scale-in actions enabled as well. For more information, see <a href="https://docs.aws.amazon.com/autoscaling/ec2/userguide/as-instance-termination.html#instance-protection">Instance Protection</a> in the <i>AWS Auto Scaling User Guide</i>.</p>
    /// 		       <p>When managed termination protection is disabled, your Amazon EC2 instances are not
    /// 			protected from termination when the Auto Scaling group scales in.</p>
    public let managedTerminationProtection: ManagedTerminationProtection?

    public init (
        autoScalingGroupArn: String? = nil,
        managedScaling: ManagedScaling? = nil,
        managedTerminationProtection: ManagedTerminationProtection? = nil
    )
    {
        self.autoScalingGroupArn = autoScalingGroupArn
        self.managedScaling = managedScaling
        self.managedTerminationProtection = managedTerminationProtection
    }
}

extension AutoScalingGroupProviderUpdate: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case managedScaling
        case managedTerminationProtection
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let managedScaling = managedScaling {
            try encodeContainer.encode(managedScaling, forKey: .managedScaling)
        }
        if let managedTerminationProtection = managedTerminationProtection {
            try encodeContainer.encode(managedTerminationProtection.rawValue, forKey: .managedTerminationProtection)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let managedScalingDecoded = try containerValues.decodeIfPresent(ManagedScaling.self, forKey: .managedScaling)
        managedScaling = managedScalingDecoded
        let managedTerminationProtectionDecoded = try containerValues.decodeIfPresent(ManagedTerminationProtection.self, forKey: .managedTerminationProtection)
        managedTerminationProtection = managedTerminationProtectionDecoded
    }
}

extension AutoScalingGroupProviderUpdate: CustomDebugStringConvertible {
    public var debugDescription: String {
        "AutoScalingGroupProviderUpdate(managedScaling: \(String(describing: managedScaling)), managedTerminationProtection: \(String(describing: managedTerminationProtection)))"}
}

/// <p>The details of the Auto Scaling group capacity provider to update.</p>
public struct AutoScalingGroupProviderUpdate: Equatable {
    /// <p>The managed scaling settings for the Auto Scaling group capacity provider.</p>
    public let managedScaling: ManagedScaling?
    /// <p>The managed termination protection setting to use for the Auto Scaling group capacity
    /// 			provider. This determines whether the Auto Scaling group has managed termination
    /// 			protection.</p>
    /// 		       <important>
    /// 			         <p>When using managed termination protection, managed scaling must also be used
    /// 				otherwise managed termination protection will not work.</p>
    /// 		       </important>
    /// 		       <p>When managed termination protection is enabled, Amazon ECS prevents the Amazon EC2 instances in
    /// 			an Auto Scaling group that contain tasks from being terminated during a scale-in action.
    /// 			The Auto Scaling group and each instance in the Auto Scaling group must have instance
    /// 			protection from scale-in actions enabled as well. For more information, see <a href="https://docs.aws.amazon.com/autoscaling/ec2/userguide/as-instance-termination.html#instance-protection">Instance Protection</a> in the <i>AWS Auto Scaling User Guide</i>.</p>
    /// 		       <p>When managed termination protection is disabled, your Amazon EC2 instances are not
    /// 			protected from termination when the Auto Scaling group scales in.</p>
    public let managedTerminationProtection: ManagedTerminationProtection?

    public init (
        managedScaling: ManagedScaling? = nil,
        managedTerminationProtection: ManagedTerminationProtection? = nil
    )
    {
        self.managedScaling = managedScaling
        self.managedTerminationProtection = managedTerminationProtection
    }
}

extension AwsVpcConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case assignPublicIp
        case securityGroups
        case subnets
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let assignPublicIp = assignPublicIp {
            try encodeContainer.encode(assignPublicIp.rawValue, forKey: .assignPublicIp)
        }
        if let securityGroups = securityGroups {
            var securityGroupsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .securityGroups)
            for stringlist0 in securityGroups {
                try securityGroupsContainer.encode(stringlist0)
            }
        }
        if let subnets = subnets {
            var subnetsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .subnets)
            for stringlist0 in subnets {
                try subnetsContainer.encode(stringlist0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let subnetsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .subnets)
        var subnetsDecoded0:[String]? = nil
        if let subnetsContainer = subnetsContainer {
            subnetsDecoded0 = [String]()
            for string0 in subnetsContainer {
                if let string0 = string0 {
                    subnetsDecoded0?.append(string0)
                }
            }
        }
        subnets = subnetsDecoded0
        let securityGroupsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .securityGroups)
        var securityGroupsDecoded0:[String]? = nil
        if let securityGroupsContainer = securityGroupsContainer {
            securityGroupsDecoded0 = [String]()
            for string0 in securityGroupsContainer {
                if let string0 = string0 {
                    securityGroupsDecoded0?.append(string0)
                }
            }
        }
        securityGroups = securityGroupsDecoded0
        let assignPublicIpDecoded = try containerValues.decodeIfPresent(AssignPublicIp.self, forKey: .assignPublicIp)
        assignPublicIp = assignPublicIpDecoded
    }
}

extension AwsVpcConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "AwsVpcConfiguration(assignPublicIp: \(String(describing: assignPublicIp)), securityGroups: \(String(describing: securityGroups)), subnets: \(String(describing: subnets)))"}
}

/// <p>An object representing the networking details for a task or service.</p>
public struct AwsVpcConfiguration: Equatable {
    /// <p>Whether the task's elastic network interface receives a public IP address. The default
    /// 			value is <code>DISABLED</code>.</p>
    public let assignPublicIp: AssignPublicIp?
    /// <p>The IDs of the security groups associated with the task or service. If you do not
    /// 			specify a security group, the default security group for the VPC is used. There is a
    /// 			limit of 5 security groups that can be specified per
    /// 			<code>AwsVpcConfiguration</code>.</p>
    /// 		
    /// 		       <note>
    /// 			         <p>All specified security groups must be from the same VPC.</p>
    /// 		       </note>
    public let securityGroups: [String]?
    /// <p>The IDs of the subnets associated with the task or service. There is a limit of 16
    /// 			subnets that can be specified per <code>AwsVpcConfiguration</code>.</p>
    /// 		
    /// 		       <note>
    /// 			         <p>All specified subnets must be from the same VPC.</p>
    /// 		       </note>
    public let subnets: [String]?

    public init (
        assignPublicIp: AssignPublicIp? = nil,
        securityGroups: [String]? = nil,
        subnets: [String]? = nil
    )
    {
        self.assignPublicIp = assignPublicIp
        self.securityGroups = securityGroups
        self.subnets = subnets
    }
}

extension BlockedException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "BlockedException(message: \(String(describing: message)))"}
}

extension BlockedException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: BlockedExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>Your AWS account has been blocked. For more information, contact <a href="http://aws.amazon.com/contact-us/">AWS Support</a>.</p>
public struct BlockedException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct BlockedExceptionBody: Equatable {
    public let message: String?
}

extension BlockedExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension CapacityProvider: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case autoScalingGroupProvider
        case capacityProviderArn
        case name
        case status
        case tags
        case updateStatus
        case updateStatusReason
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let autoScalingGroupProvider = autoScalingGroupProvider {
            try encodeContainer.encode(autoScalingGroupProvider, forKey: .autoScalingGroupProvider)
        }
        if let capacityProviderArn = capacityProviderArn {
            try encodeContainer.encode(capacityProviderArn, forKey: .capacityProviderArn)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let updateStatus = updateStatus {
            try encodeContainer.encode(updateStatus.rawValue, forKey: .updateStatus)
        }
        if let updateStatusReason = updateStatusReason {
            try encodeContainer.encode(updateStatusReason, forKey: .updateStatusReason)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .capacityProviderArn)
        capacityProviderArn = capacityProviderArnDecoded
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let statusDecoded = try containerValues.decodeIfPresent(CapacityProviderStatus.self, forKey: .status)
        status = statusDecoded
        let autoScalingGroupProviderDecoded = try containerValues.decodeIfPresent(AutoScalingGroupProvider.self, forKey: .autoScalingGroupProvider)
        autoScalingGroupProvider = autoScalingGroupProviderDecoded
        let updateStatusDecoded = try containerValues.decodeIfPresent(CapacityProviderUpdateStatus.self, forKey: .updateStatus)
        updateStatus = updateStatusDecoded
        let updateStatusReasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .updateStatusReason)
        updateStatusReason = updateStatusReasonDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension CapacityProvider: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CapacityProvider(autoScalingGroupProvider: \(String(describing: autoScalingGroupProvider)), capacityProviderArn: \(String(describing: capacityProviderArn)), name: \(String(describing: name)), status: \(String(describing: status)), tags: \(String(describing: tags)), updateStatus: \(String(describing: updateStatus)), updateStatusReason: \(String(describing: updateStatusReason)))"}
}

/// <p>The details of a capacity provider.</p>
public struct CapacityProvider: Equatable {
    /// <p>The Auto Scaling group settings for the capacity provider.</p>
    public let autoScalingGroupProvider: AutoScalingGroupProvider?
    /// <p>The Amazon Resource Name (ARN) that identifies the capacity provider.</p>
    public let capacityProviderArn: String?
    /// <p>The name of the capacity provider.</p>
    public let name: String?
    /// <p>The current status of the capacity provider. Only capacity providers in an
    /// 				<code>ACTIVE</code> state can be used in a cluster. When a capacity provider is
    /// 			successfully deleted, it will have an <code>INACTIVE</code> status.</p>
    public let status: CapacityProviderStatus?
    /// <p>The metadata that you apply to the capacity provider to help you categorize and
    /// 			organize it. Each tag consists of a key and an optional value, both of which you
    /// 			define.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The update status of the capacity provider. The following are the possible states that
    /// 			will be returned.</p>
    /// 		       <dl>
    ///             <dt>DELETE_IN_PROGRESS</dt>
    ///             <dd>
    /// 					          <p>The capacity provider is in the process of being deleted.</p>
    /// 				        </dd>
    ///             <dt>DELETE_COMPLETE</dt>
    ///             <dd>
    /// 					          <p>The capacity provider has been successfully deleted and will have an
    /// 							<code>INACTIVE</code> status.</p>
    /// 				        </dd>
    ///             <dt>DELETE_FAILED</dt>
    ///             <dd>
    /// 					          <p>The capacity provider was unable to be deleted. The update status reason
    /// 						will provide further details about why the delete failed.</p>
    /// 				        </dd>
    ///          </dl>
    public let updateStatus: CapacityProviderUpdateStatus?
    /// <p>The update status reason. This provides further details about the update status for
    /// 			the capacity provider.</p>
    public let updateStatusReason: String?

    public init (
        autoScalingGroupProvider: AutoScalingGroupProvider? = nil,
        capacityProviderArn: String? = nil,
        name: String? = nil,
        status: CapacityProviderStatus? = nil,
        tags: [Tag]? = nil,
        updateStatus: CapacityProviderUpdateStatus? = nil,
        updateStatusReason: String? = nil
    )
    {
        self.autoScalingGroupProvider = autoScalingGroupProvider
        self.capacityProviderArn = capacityProviderArn
        self.name = name
        self.status = status
        self.tags = tags
        self.updateStatus = updateStatus
        self.updateStatusReason = updateStatusReason
    }
}

public enum CapacityProviderField {
    case tags
    case sdkUnknown(String)
}

extension CapacityProviderField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [CapacityProviderField] {
        return [
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = CapacityProviderField(rawValue: rawValue) ?? CapacityProviderField.sdkUnknown(rawValue)
    }
}

public enum CapacityProviderStatus {
    case active
    case inactive
    case sdkUnknown(String)
}

extension CapacityProviderStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [CapacityProviderStatus] {
        return [
            .active,
            .inactive,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .active: return "ACTIVE"
        case .inactive: return "INACTIVE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = CapacityProviderStatus(rawValue: rawValue) ?? CapacityProviderStatus.sdkUnknown(rawValue)
    }
}

extension CapacityProviderStrategyItem: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case base
        case capacityProvider
        case weight
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if base != 0 {
            try encodeContainer.encode(base, forKey: .base)
        }
        if let capacityProvider = capacityProvider {
            try encodeContainer.encode(capacityProvider, forKey: .capacityProvider)
        }
        if weight != 0 {
            try encodeContainer.encode(weight, forKey: .weight)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderDecoded = try containerValues.decodeIfPresent(String.self, forKey: .capacityProvider)
        capacityProvider = capacityProviderDecoded
        let weightDecoded = try containerValues.decode(Int.self, forKey: .weight)
        weight = weightDecoded
        let baseDecoded = try containerValues.decode(Int.self, forKey: .base)
        base = baseDecoded
    }
}

extension CapacityProviderStrategyItem: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CapacityProviderStrategyItem(base: \(String(describing: base)), capacityProvider: \(String(describing: capacityProvider)), weight: \(String(describing: weight)))"}
}

/// <p>The details of a capacity provider strategy. A capacity provider strategy can be set
/// 			when using the <a>RunTask</a> or <a>CreateCluster</a> APIs or as
/// 			the default capacity provider strategy for a cluster with the <a>CreateCluster</a> API.</p>
/// 		       <p>Only capacity providers that are already associated with a cluster and have an
/// 				<code>ACTIVE</code> or <code>UPDATING</code> status can be used in a capacity
/// 			provider strategy. The <a>PutClusterCapacityProviders</a> API is used to
/// 			associate a capacity provider with a cluster.</p>
/// 		       <p>If specifying a capacity provider that uses an Auto Scaling group, the capacity
/// 			provider must already be created. New Auto Scaling group capacity providers can be
/// 			created with the <a>CreateCapacityProvider</a> API operation.</p>
/// 		       <p>To use a AWS Fargate capacity provider, specify either the <code>FARGATE</code> or
/// 				<code>FARGATE_SPOT</code> capacity providers. The AWS Fargate capacity providers are
/// 			available to all accounts and only need to be associated with a cluster to be used in a
/// 			capacity provider strategy.</p>
public struct CapacityProviderStrategyItem: Equatable {
    /// <p>The <i>base</i> value designates how many tasks, at a minimum, to run on
    /// 			the specified capacity provider. Only one capacity provider in a capacity provider
    /// 			strategy can have a <i>base</i> defined. If no value is specified, the
    /// 			default value of <code>0</code> is used.</p>
    public let base: Int
    /// <p>The short name of the capacity provider.</p>
    public let capacityProvider: String?
    /// <p>The <i>weight</i> value designates the relative percentage of the total
    /// 			number of tasks launched that should use the specified capacity provider. The
    /// 				<code>weight</code> value is taken into consideration after the <code>base</code>
    /// 			value, if defined, is satisfied.</p>
    /// 		       <p>If no <code>weight</code> value is specified, the default value of <code>0</code> is
    /// 			used. When multiple capacity providers are specified within a capacity provider
    /// 			strategy, at least one of the capacity providers must have a weight value greater than
    /// 			zero and any capacity providers with a weight of <code>0</code> will not be used to
    /// 			place tasks. If you specify multiple capacity providers in a strategy that all have a
    /// 			weight of <code>0</code>, any <code>RunTask</code> or <code>CreateService</code> actions
    /// 			using the capacity provider strategy will fail.</p>
    /// 		       <p>An example scenario for using weights is defining a strategy that contains two
    /// 			capacity providers and both have a weight of <code>1</code>, then when the
    /// 				<code>base</code> is satisfied, the tasks will be split evenly across the two
    /// 			capacity providers. Using that same logic, if you specify a weight of <code>1</code> for
    /// 				<i>capacityProviderA</i> and a weight of <code>4</code> for
    /// 				<i>capacityProviderB</i>, then for every one task that is run using
    /// 				<i>capacityProviderA</i>, four tasks would use
    /// 				<i>capacityProviderB</i>.</p>
    public let weight: Int

    public init (
        base: Int = 0,
        capacityProvider: String? = nil,
        weight: Int = 0
    )
    {
        self.base = base
        self.capacityProvider = capacityProvider
        self.weight = weight
    }
}

public enum CapacityProviderUpdateStatus {
    case deleteComplete
    case deleteFailed
    case deleteInProgress
    case updateComplete
    case updateFailed
    case updateInProgress
    case sdkUnknown(String)
}

extension CapacityProviderUpdateStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [CapacityProviderUpdateStatus] {
        return [
            .deleteComplete,
            .deleteFailed,
            .deleteInProgress,
            .updateComplete,
            .updateFailed,
            .updateInProgress,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .deleteComplete: return "DELETE_COMPLETE"
        case .deleteFailed: return "DELETE_FAILED"
        case .deleteInProgress: return "DELETE_IN_PROGRESS"
        case .updateComplete: return "UPDATE_COMPLETE"
        case .updateFailed: return "UPDATE_FAILED"
        case .updateInProgress: return "UPDATE_IN_PROGRESS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = CapacityProviderUpdateStatus(rawValue: rawValue) ?? CapacityProviderUpdateStatus.sdkUnknown(rawValue)
    }
}

extension ClientException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClientException(message: \(String(describing: message)))"}
}

extension ClientException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ClientExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>These errors are usually caused by a client action, such as using an action or
/// 			resource on behalf of a user that doesn't have permissions to use the action or
/// 			resource, or specifying an identifier that is not valid.</p>
public struct ClientException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ClientExceptionBody: Equatable {
    public let message: String?
}

extension ClientExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension Cluster: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case activeServicesCount
        case attachments
        case attachmentsStatus
        case capacityProviders
        case clusterArn
        case clusterName
        case configuration
        case defaultCapacityProviderStrategy
        case pendingTasksCount
        case registeredContainerInstancesCount
        case runningTasksCount
        case settings
        case statistics
        case status
        case tags
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if activeServicesCount != 0 {
            try encodeContainer.encode(activeServicesCount, forKey: .activeServicesCount)
        }
        if let attachments = attachments {
            var attachmentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attachments)
            for attachments0 in attachments {
                try attachmentsContainer.encode(attachments0)
            }
        }
        if let attachmentsStatus = attachmentsStatus {
            try encodeContainer.encode(attachmentsStatus, forKey: .attachmentsStatus)
        }
        if let capacityProviders = capacityProviders {
            var capacityProvidersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviders)
            for stringlist0 in capacityProviders {
                try capacityProvidersContainer.encode(stringlist0)
            }
        }
        if let clusterArn = clusterArn {
            try encodeContainer.encode(clusterArn, forKey: .clusterArn)
        }
        if let clusterName = clusterName {
            try encodeContainer.encode(clusterName, forKey: .clusterName)
        }
        if let configuration = configuration {
            try encodeContainer.encode(configuration, forKey: .configuration)
        }
        if let defaultCapacityProviderStrategy = defaultCapacityProviderStrategy {
            var defaultCapacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .defaultCapacityProviderStrategy)
            for capacityproviderstrategy0 in defaultCapacityProviderStrategy {
                try defaultCapacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if pendingTasksCount != 0 {
            try encodeContainer.encode(pendingTasksCount, forKey: .pendingTasksCount)
        }
        if registeredContainerInstancesCount != 0 {
            try encodeContainer.encode(registeredContainerInstancesCount, forKey: .registeredContainerInstancesCount)
        }
        if runningTasksCount != 0 {
            try encodeContainer.encode(runningTasksCount, forKey: .runningTasksCount)
        }
        if let settings = settings {
            var settingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .settings)
            for clustersettings0 in settings {
                try settingsContainer.encode(clustersettings0)
            }
        }
        if let statistics = statistics {
            var statisticsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .statistics)
            for statistics0 in statistics {
                try statisticsContainer.encode(statistics0)
            }
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterArn)
        clusterArn = clusterArnDecoded
        let clusterNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterName)
        clusterName = clusterNameDecoded
        let configurationDecoded = try containerValues.decodeIfPresent(ClusterConfiguration.self, forKey: .configuration)
        configuration = configurationDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let registeredContainerInstancesCountDecoded = try containerValues.decode(Int.self, forKey: .registeredContainerInstancesCount)
        registeredContainerInstancesCount = registeredContainerInstancesCountDecoded
        let runningTasksCountDecoded = try containerValues.decode(Int.self, forKey: .runningTasksCount)
        runningTasksCount = runningTasksCountDecoded
        let pendingTasksCountDecoded = try containerValues.decode(Int.self, forKey: .pendingTasksCount)
        pendingTasksCount = pendingTasksCountDecoded
        let activeServicesCountDecoded = try containerValues.decode(Int.self, forKey: .activeServicesCount)
        activeServicesCount = activeServicesCountDecoded
        let statisticsContainer = try containerValues.decodeIfPresent([KeyValuePair?].self, forKey: .statistics)
        var statisticsDecoded0:[KeyValuePair]? = nil
        if let statisticsContainer = statisticsContainer {
            statisticsDecoded0 = [KeyValuePair]()
            for structure0 in statisticsContainer {
                if let structure0 = structure0 {
                    statisticsDecoded0?.append(structure0)
                }
            }
        }
        statistics = statisticsDecoded0
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let settingsContainer = try containerValues.decodeIfPresent([ClusterSetting?].self, forKey: .settings)
        var settingsDecoded0:[ClusterSetting]? = nil
        if let settingsContainer = settingsContainer {
            settingsDecoded0 = [ClusterSetting]()
            for structure0 in settingsContainer {
                if let structure0 = structure0 {
                    settingsDecoded0?.append(structure0)
                }
            }
        }
        settings = settingsDecoded0
        let capacityProvidersContainer = try containerValues.decodeIfPresent([String?].self, forKey: .capacityProviders)
        var capacityProvidersDecoded0:[String]? = nil
        if let capacityProvidersContainer = capacityProvidersContainer {
            capacityProvidersDecoded0 = [String]()
            for string0 in capacityProvidersContainer {
                if let string0 = string0 {
                    capacityProvidersDecoded0?.append(string0)
                }
            }
        }
        capacityProviders = capacityProvidersDecoded0
        let defaultCapacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .defaultCapacityProviderStrategy)
        var defaultCapacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let defaultCapacityProviderStrategyContainer = defaultCapacityProviderStrategyContainer {
            defaultCapacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in defaultCapacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    defaultCapacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        defaultCapacityProviderStrategy = defaultCapacityProviderStrategyDecoded0
        let attachmentsContainer = try containerValues.decodeIfPresent([Attachment?].self, forKey: .attachments)
        var attachmentsDecoded0:[Attachment]? = nil
        if let attachmentsContainer = attachmentsContainer {
            attachmentsDecoded0 = [Attachment]()
            for structure0 in attachmentsContainer {
                if let structure0 = structure0 {
                    attachmentsDecoded0?.append(structure0)
                }
            }
        }
        attachments = attachmentsDecoded0
        let attachmentsStatusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .attachmentsStatus)
        attachmentsStatus = attachmentsStatusDecoded
    }
}

extension Cluster: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Cluster(activeServicesCount: \(String(describing: activeServicesCount)), attachments: \(String(describing: attachments)), attachmentsStatus: \(String(describing: attachmentsStatus)), capacityProviders: \(String(describing: capacityProviders)), clusterArn: \(String(describing: clusterArn)), clusterName: \(String(describing: clusterName)), configuration: \(String(describing: configuration)), defaultCapacityProviderStrategy: \(String(describing: defaultCapacityProviderStrategy)), pendingTasksCount: \(String(describing: pendingTasksCount)), registeredContainerInstancesCount: \(String(describing: registeredContainerInstancesCount)), runningTasksCount: \(String(describing: runningTasksCount)), settings: \(String(describing: settings)), statistics: \(String(describing: statistics)), status: \(String(describing: status)), tags: \(String(describing: tags)))"}
}

/// <p>A regional grouping of one or more container instances on which you can run task
/// 			requests. Each account receives a default cluster the first time you use the Amazon ECS
/// 			service, but you may also create other clusters. Clusters may contain more than one
/// 			instance type simultaneously.</p>
public struct Cluster: Equatable {
    /// <p>The number of services that are running on the cluster in an <code>ACTIVE</code>
    /// 			state. You can view these services with <a>ListServices</a>.</p>
    public let activeServicesCount: Int
    /// <p>The resources attached to a cluster. When using a capacity provider with a cluster,
    /// 			the Auto Scaling plan that is created will be returned as a cluster attachment.</p>
    public let attachments: [Attachment]?
    /// <p>The status of the capacity providers associated with the cluster. The following are
    /// 			the states that will be returned:</p>
    /// 		       <dl>
    ///             <dt>UPDATE_IN_PROGRESS</dt>
    ///             <dd>
    /// 					          <p>The available capacity providers for the cluster are updating. This occurs
    /// 						when the Auto Scaling plan is provisioning or deprovisioning.</p>
    /// 				        </dd>
    ///             <dt>UPDATE_COMPLETE</dt>
    ///             <dd>
    /// 					          <p>The capacity providers have successfully updated.</p>
    /// 				        </dd>
    ///             <dt>UPDATE_FAILED</dt>
    ///             <dd>
    /// 					          <p>The capacity provider updates failed.</p>
    /// 				        </dd>
    ///          </dl>
    public let attachmentsStatus: String?
    /// <p>The capacity providers associated with the cluster.</p>
    public let capacityProviders: [String]?
    /// <p>The Amazon Resource Name (ARN) that identifies the cluster. The ARN contains the <code>arn:aws:ecs</code> namespace, followed by the Region of the cluster, the AWS account ID of the cluster owner, the <code>cluster</code> namespace, and then the cluster name. For example, <code>arn:aws:ecs:region:012345678910:cluster/test</code>.</p>
    public let clusterArn: String?
    /// <p>A user-generated string that you use to identify your cluster.</p>
    public let clusterName: String?
    /// <p>The execute command configuration for the cluster.</p>
    public let configuration: ClusterConfiguration?
    /// <p>The default capacity provider strategy for the cluster. When services or tasks are run
    /// 			in the cluster with no launch type or capacity provider strategy specified, the default
    /// 			capacity provider strategy is used.</p>
    public let defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The number of tasks in the cluster that are in the <code>PENDING</code> state.</p>
    public let pendingTasksCount: Int
    /// <p>The number of container instances registered into the cluster. This includes container
    /// 			instances in both <code>ACTIVE</code> and <code>DRAINING</code> status.</p>
    public let registeredContainerInstancesCount: Int
    /// <p>The number of tasks in the cluster that are in the <code>RUNNING</code> state.</p>
    public let runningTasksCount: Int
    /// <p>The settings for the cluster. This parameter indicates whether CloudWatch Container Insights
    /// 			is enabled or disabled for a cluster.</p>
    public let settings: [ClusterSetting]?
    /// <p>Additional information about your clusters that are separated by launch type,
    /// 			including:</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>runningEC2TasksCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>RunningFargateTasksCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>pendingEC2TasksCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>pendingFargateTasksCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>activeEC2ServiceCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>activeFargateServiceCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>drainingEC2ServiceCount</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>drainingFargateServiceCount</p>
    /// 			         </li>
    ///          </ul>
    public let statistics: [KeyValuePair]?
    /// <p>The status of the cluster. The following are the possible states that will be
    /// 			returned.</p>
    /// 		       <dl>
    ///             <dt>ACTIVE</dt>
    ///             <dd>
    /// 					          <p>The cluster is ready to accept tasks and if applicable you can register
    /// 						container instances with the cluster.</p>
    /// 				        </dd>
    ///             <dt>PROVISIONING</dt>
    ///             <dd>
    /// 					          <p>The cluster has capacity providers associated with it and the resources
    /// 						needed for the capacity provider are being created.</p>
    /// 				        </dd>
    ///             <dt>DEPROVISIONING</dt>
    ///             <dd>
    /// 					          <p>The cluster has capacity providers associated with it and the resources
    /// 						needed for the capacity provider are being deleted.</p>
    /// 				        </dd>
    ///             <dt>FAILED</dt>
    ///             <dd>
    /// 					          <p>The cluster has capacity providers associated with it and the resources
    /// 						needed for the capacity provider have failed to create.</p>
    /// 				        </dd>
    ///             <dt>INACTIVE</dt>
    ///             <dd>
    /// 					          <p>The cluster has been deleted. Clusters with an <code>INACTIVE</code>
    /// 						status may remain discoverable in your account for a period of time.
    /// 						However, this behavior is subject to change in the future, so you should not
    /// 						rely on <code>INACTIVE</code> clusters persisting.</p>
    /// 				        </dd>
    ///          </dl>
    public let status: String?
    /// <p>The metadata that you apply to the cluster to help you categorize and organize them.
    /// 			Each tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?

    public init (
        activeServicesCount: Int = 0,
        attachments: [Attachment]? = nil,
        attachmentsStatus: String? = nil,
        capacityProviders: [String]? = nil,
        clusterArn: String? = nil,
        clusterName: String? = nil,
        configuration: ClusterConfiguration? = nil,
        defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        pendingTasksCount: Int = 0,
        registeredContainerInstancesCount: Int = 0,
        runningTasksCount: Int = 0,
        settings: [ClusterSetting]? = nil,
        statistics: [KeyValuePair]? = nil,
        status: String? = nil,
        tags: [Tag]? = nil
    )
    {
        self.activeServicesCount = activeServicesCount
        self.attachments = attachments
        self.attachmentsStatus = attachmentsStatus
        self.capacityProviders = capacityProviders
        self.clusterArn = clusterArn
        self.clusterName = clusterName
        self.configuration = configuration
        self.defaultCapacityProviderStrategy = defaultCapacityProviderStrategy
        self.pendingTasksCount = pendingTasksCount
        self.registeredContainerInstancesCount = registeredContainerInstancesCount
        self.runningTasksCount = runningTasksCount
        self.settings = settings
        self.statistics = statistics
        self.status = status
        self.tags = tags
    }
}

extension ClusterConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case executeCommandConfiguration
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let executeCommandConfiguration = executeCommandConfiguration {
            try encodeContainer.encode(executeCommandConfiguration, forKey: .executeCommandConfiguration)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let executeCommandConfigurationDecoded = try containerValues.decodeIfPresent(ExecuteCommandConfiguration.self, forKey: .executeCommandConfiguration)
        executeCommandConfiguration = executeCommandConfigurationDecoded
    }
}

extension ClusterConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClusterConfiguration(executeCommandConfiguration: \(String(describing: executeCommandConfiguration)))"}
}

/// <p>The execute command configuration for the cluster.</p>
public struct ClusterConfiguration: Equatable {
    /// <p>The details of the execute command configuration.</p>
    public let executeCommandConfiguration: ExecuteCommandConfiguration?

    public init (
        executeCommandConfiguration: ExecuteCommandConfiguration? = nil
    )
    {
        self.executeCommandConfiguration = executeCommandConfiguration
    }
}

extension ClusterContainsContainerInstancesException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClusterContainsContainerInstancesException(message: \(String(describing: message)))"}
}

extension ClusterContainsContainerInstancesException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ClusterContainsContainerInstancesExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>You cannot delete a cluster that has registered container instances. First, deregister
/// 			the container instances before you can delete the cluster. For more information, see
/// 				<a>DeregisterContainerInstance</a>.</p>
public struct ClusterContainsContainerInstancesException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ClusterContainsContainerInstancesExceptionBody: Equatable {
    public let message: String?
}

extension ClusterContainsContainerInstancesExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ClusterContainsServicesException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClusterContainsServicesException(message: \(String(describing: message)))"}
}

extension ClusterContainsServicesException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ClusterContainsServicesExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>You cannot delete a cluster that contains services. First, update the service to
/// 			reduce its desired task count to 0 and then delete the service. For more information,
/// 			see <a>UpdateService</a> and <a>DeleteService</a>.</p>
public struct ClusterContainsServicesException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ClusterContainsServicesExceptionBody: Equatable {
    public let message: String?
}

extension ClusterContainsServicesExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ClusterContainsTasksException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClusterContainsTasksException(message: \(String(describing: message)))"}
}

extension ClusterContainsTasksException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ClusterContainsTasksExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>You cannot delete a cluster that has active tasks.</p>
public struct ClusterContainsTasksException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ClusterContainsTasksExceptionBody: Equatable {
    public let message: String?
}

extension ClusterContainsTasksExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public enum ClusterField {
    case attachments
    case configurations
    case settings
    case statistics
    case tags
    case sdkUnknown(String)
}

extension ClusterField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ClusterField] {
        return [
            .attachments,
            .configurations,
            .settings,
            .statistics,
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .attachments: return "ATTACHMENTS"
        case .configurations: return "CONFIGURATIONS"
        case .settings: return "SETTINGS"
        case .statistics: return "STATISTICS"
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ClusterField(rawValue: rawValue) ?? ClusterField.sdkUnknown(rawValue)
    }
}

extension ClusterNotFoundException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClusterNotFoundException(message: \(String(describing: message)))"}
}

extension ClusterNotFoundException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ClusterNotFoundExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified cluster could not be found. You can view your available clusters with
/// 				<a>ListClusters</a>. Amazon ECS clusters are Region-specific.</p>
public struct ClusterNotFoundException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ClusterNotFoundExceptionBody: Equatable {
    public let message: String?
}

extension ClusterNotFoundExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ClusterSetting: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(ClusterSettingName.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
    }
}

extension ClusterSetting: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ClusterSetting(name: \(String(describing: name)), value: \(String(describing: value)))"}
}

/// <p>The settings to use when creating a cluster. This parameter is used to enable CloudWatch
/// 			Container Insights for a cluster.</p>
public struct ClusterSetting: Equatable {
    /// <p>The name of the cluster setting. The only supported value is
    /// 				<code>containerInsights</code>.</p>
    public let name: ClusterSettingName?
    /// <p>The value to set for the cluster setting. The supported values are
    /// 				<code>enabled</code> and <code>disabled</code>. If <code>enabled</code> is
    /// 			specified, CloudWatch Container Insights will be enabled for the cluster, otherwise it will be
    /// 			disabled unless the <code>containerInsights</code> account setting is enabled. If a
    /// 			cluster value is specified, it will override the <code>containerInsights</code> value
    /// 			set with <a>PutAccountSetting</a> or <a>PutAccountSettingDefault</a>.</p>
    public let value: String?

    public init (
        name: ClusterSettingName? = nil,
        value: String? = nil
    )
    {
        self.name = name
        self.value = value
    }
}

public enum ClusterSettingName {
    case containerInsights
    case sdkUnknown(String)
}

extension ClusterSettingName : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ClusterSettingName] {
        return [
            .containerInsights,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .containerInsights: return "containerInsights"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ClusterSettingName(rawValue: rawValue) ?? ClusterSettingName.sdkUnknown(rawValue)
    }
}

public enum Compatibility {
    case ec2
    case external
    case fargate
    case sdkUnknown(String)
}

extension Compatibility : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [Compatibility] {
        return [
            .ec2,
            .external,
            .fargate,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .ec2: return "EC2"
        case .external: return "EXTERNAL"
        case .fargate: return "FARGATE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = Compatibility(rawValue: rawValue) ?? Compatibility.sdkUnknown(rawValue)
    }
}

public enum Connectivity {
    case connected
    case disconnected
    case sdkUnknown(String)
}

extension Connectivity : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [Connectivity] {
        return [
            .connected,
            .disconnected,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .connected: return "CONNECTED"
        case .disconnected: return "DISCONNECTED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = Connectivity(rawValue: rawValue) ?? Connectivity.sdkUnknown(rawValue)
    }
}

extension Container: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerArn
        case cpu
        case exitCode
        case gpuIds
        case healthStatus
        case image
        case imageDigest
        case lastStatus
        case managedAgents
        case memory
        case memoryReservation
        case name
        case networkBindings
        case networkInterfaces
        case reason
        case runtimeId
        case taskArn
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerArn = containerArn {
            try encodeContainer.encode(containerArn, forKey: .containerArn)
        }
        if let cpu = cpu {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let exitCode = exitCode {
            try encodeContainer.encode(exitCode, forKey: .exitCode)
        }
        if let gpuIds = gpuIds {
            var gpuIdsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .gpuIds)
            for gpuids0 in gpuIds {
                try gpuIdsContainer.encode(gpuids0)
            }
        }
        if let healthStatus = healthStatus {
            try encodeContainer.encode(healthStatus.rawValue, forKey: .healthStatus)
        }
        if let image = image {
            try encodeContainer.encode(image, forKey: .image)
        }
        if let imageDigest = imageDigest {
            try encodeContainer.encode(imageDigest, forKey: .imageDigest)
        }
        if let lastStatus = lastStatus {
            try encodeContainer.encode(lastStatus, forKey: .lastStatus)
        }
        if let managedAgents = managedAgents {
            var managedAgentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .managedAgents)
            for managedagents0 in managedAgents {
                try managedAgentsContainer.encode(managedagents0)
            }
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let memoryReservation = memoryReservation {
            try encodeContainer.encode(memoryReservation, forKey: .memoryReservation)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let networkBindings = networkBindings {
            var networkBindingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .networkBindings)
            for networkbindings0 in networkBindings {
                try networkBindingsContainer.encode(networkbindings0)
            }
        }
        if let networkInterfaces = networkInterfaces {
            var networkInterfacesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .networkInterfaces)
            for networkinterfaces0 in networkInterfaces {
                try networkInterfacesContainer.encode(networkinterfaces0)
            }
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
        if let runtimeId = runtimeId {
            try encodeContainer.encode(runtimeId, forKey: .runtimeId)
        }
        if let taskArn = taskArn {
            try encodeContainer.encode(taskArn, forKey: .taskArn)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerArn)
        containerArn = containerArnDecoded
        let taskArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskArn)
        taskArn = taskArnDecoded
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let imageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .image)
        image = imageDecoded
        let imageDigestDecoded = try containerValues.decodeIfPresent(String.self, forKey: .imageDigest)
        imageDigest = imageDigestDecoded
        let runtimeIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .runtimeId)
        runtimeId = runtimeIdDecoded
        let lastStatusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .lastStatus)
        lastStatus = lastStatusDecoded
        let exitCodeDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .exitCode)
        exitCode = exitCodeDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
        let networkBindingsContainer = try containerValues.decodeIfPresent([NetworkBinding?].self, forKey: .networkBindings)
        var networkBindingsDecoded0:[NetworkBinding]? = nil
        if let networkBindingsContainer = networkBindingsContainer {
            networkBindingsDecoded0 = [NetworkBinding]()
            for structure0 in networkBindingsContainer {
                if let structure0 = structure0 {
                    networkBindingsDecoded0?.append(structure0)
                }
            }
        }
        networkBindings = networkBindingsDecoded0
        let networkInterfacesContainer = try containerValues.decodeIfPresent([NetworkInterface?].self, forKey: .networkInterfaces)
        var networkInterfacesDecoded0:[NetworkInterface]? = nil
        if let networkInterfacesContainer = networkInterfacesContainer {
            networkInterfacesDecoded0 = [NetworkInterface]()
            for structure0 in networkInterfacesContainer {
                if let structure0 = structure0 {
                    networkInterfacesDecoded0?.append(structure0)
                }
            }
        }
        networkInterfaces = networkInterfacesDecoded0
        let healthStatusDecoded = try containerValues.decodeIfPresent(HealthStatus.self, forKey: .healthStatus)
        healthStatus = healthStatusDecoded
        let managedAgentsContainer = try containerValues.decodeIfPresent([ManagedAgent?].self, forKey: .managedAgents)
        var managedAgentsDecoded0:[ManagedAgent]? = nil
        if let managedAgentsContainer = managedAgentsContainer {
            managedAgentsDecoded0 = [ManagedAgent]()
            for structure0 in managedAgentsContainer {
                if let structure0 = structure0 {
                    managedAgentsDecoded0?.append(structure0)
                }
            }
        }
        managedAgents = managedAgentsDecoded0
        let cpuDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cpu)
        cpu = cpuDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .memory)
        memory = memoryDecoded
        let memoryReservationDecoded = try containerValues.decodeIfPresent(String.self, forKey: .memoryReservation)
        memoryReservation = memoryReservationDecoded
        let gpuIdsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .gpuIds)
        var gpuIdsDecoded0:[String]? = nil
        if let gpuIdsContainer = gpuIdsContainer {
            gpuIdsDecoded0 = [String]()
            for string0 in gpuIdsContainer {
                if let string0 = string0 {
                    gpuIdsDecoded0?.append(string0)
                }
            }
        }
        gpuIds = gpuIdsDecoded0
    }
}

extension Container: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Container(containerArn: \(String(describing: containerArn)), cpu: \(String(describing: cpu)), exitCode: \(String(describing: exitCode)), gpuIds: \(String(describing: gpuIds)), healthStatus: \(String(describing: healthStatus)), image: \(String(describing: image)), imageDigest: \(String(describing: imageDigest)), lastStatus: \(String(describing: lastStatus)), managedAgents: \(String(describing: managedAgents)), memory: \(String(describing: memory)), memoryReservation: \(String(describing: memoryReservation)), name: \(String(describing: name)), networkBindings: \(String(describing: networkBindings)), networkInterfaces: \(String(describing: networkInterfaces)), reason: \(String(describing: reason)), runtimeId: \(String(describing: runtimeId)), taskArn: \(String(describing: taskArn)))"}
}

/// <p>A Docker container that is part of a task.</p>
public struct Container: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the container.</p>
    public let containerArn: String?
    /// <p>The number of CPU units set for the container. The value will be <code>0</code> if no
    /// 			value was specified in the container definition when the task definition was
    /// 			registered.</p>
    public let cpu: String?
    /// <p>The exit code returned from the container.</p>
    public let exitCode: Int?
    /// <p>The IDs of each GPU assigned to the container.</p>
    public let gpuIds: [String]?
    /// <p>The health status of the container. If health checks are not configured for this
    /// 			container in its task definition, then it reports the health status as
    /// 				<code>UNKNOWN</code>.</p>
    public let healthStatus: HealthStatus?
    /// <p>The image used for the container.</p>
    public let image: String?
    /// <p>The container image manifest digest.</p>
    /// 		       <note>
    /// 			         <p>The <code>imageDigest</code> is only returned if the container is using an image
    /// 				hosted in Amazon ECR, otherwise it is omitted.</p>
    /// 		       </note>
    public let imageDigest: String?
    /// <p>The last known status of the container.</p>
    public let lastStatus: String?
    /// <p>The details of any Amazon ECS managed agents associated with the container.</p>
    public let managedAgents: [ManagedAgent]?
    /// <p>The hard limit (in MiB) of memory set for the container.</p>
    public let memory: String?
    /// <p>The soft limit (in MiB) of memory set for the container.</p>
    public let memoryReservation: String?
    /// <p>The name of the container.</p>
    public let name: String?
    /// <p>The network bindings associated with the container.</p>
    public let networkBindings: [NetworkBinding]?
    /// <p>The network interfaces associated with the container.</p>
    public let networkInterfaces: [NetworkInterface]?
    /// <p>A short (255 max characters) human-readable string to provide additional details about
    /// 			a running or stopped container.</p>
    public let reason: String?
    /// <p>The ID of the Docker container.</p>
    public let runtimeId: String?
    /// <p>The ARN of the task.</p>
    public let taskArn: String?

    public init (
        containerArn: String? = nil,
        cpu: String? = nil,
        exitCode: Int? = nil,
        gpuIds: [String]? = nil,
        healthStatus: HealthStatus? = nil,
        image: String? = nil,
        imageDigest: String? = nil,
        lastStatus: String? = nil,
        managedAgents: [ManagedAgent]? = nil,
        memory: String? = nil,
        memoryReservation: String? = nil,
        name: String? = nil,
        networkBindings: [NetworkBinding]? = nil,
        networkInterfaces: [NetworkInterface]? = nil,
        reason: String? = nil,
        runtimeId: String? = nil,
        taskArn: String? = nil
    )
    {
        self.containerArn = containerArn
        self.cpu = cpu
        self.exitCode = exitCode
        self.gpuIds = gpuIds
        self.healthStatus = healthStatus
        self.image = image
        self.imageDigest = imageDigest
        self.lastStatus = lastStatus
        self.managedAgents = managedAgents
        self.memory = memory
        self.memoryReservation = memoryReservation
        self.name = name
        self.networkBindings = networkBindings
        self.networkInterfaces = networkInterfaces
        self.reason = reason
        self.runtimeId = runtimeId
        self.taskArn = taskArn
    }
}

public enum ContainerCondition {
    case complete
    case healthy
    case start
    case success
    case sdkUnknown(String)
}

extension ContainerCondition : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ContainerCondition] {
        return [
            .complete,
            .healthy,
            .start,
            .success,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .complete: return "COMPLETE"
        case .healthy: return "HEALTHY"
        case .start: return "START"
        case .success: return "SUCCESS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ContainerCondition(rawValue: rawValue) ?? ContainerCondition.sdkUnknown(rawValue)
    }
}

extension ContainerDefinition: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case command
        case cpu
        case dependsOn
        case disableNetworking
        case dnsSearchDomains
        case dnsServers
        case dockerLabels
        case dockerSecurityOptions
        case entryPoint
        case environment
        case environmentFiles
        case essential
        case extraHosts
        case firelensConfiguration
        case healthCheck
        case hostname
        case image
        case interactive
        case links
        case linuxParameters
        case logConfiguration
        case memory
        case memoryReservation
        case mountPoints
        case name
        case portMappings
        case privileged
        case pseudoTerminal
        case readonlyRootFilesystem
        case repositoryCredentials
        case resourceRequirements
        case secrets
        case startTimeout
        case stopTimeout
        case systemControls
        case ulimits
        case user
        case volumesFrom
        case workingDirectory
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let command = command {
            var commandContainer = encodeContainer.nestedUnkeyedContainer(forKey: .command)
            for stringlist0 in command {
                try commandContainer.encode(stringlist0)
            }
        }
        if cpu != 0 {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let dependsOn = dependsOn {
            var dependsOnContainer = encodeContainer.nestedUnkeyedContainer(forKey: .dependsOn)
            for containerdependencies0 in dependsOn {
                try dependsOnContainer.encode(containerdependencies0)
            }
        }
        if let disableNetworking = disableNetworking {
            try encodeContainer.encode(disableNetworking, forKey: .disableNetworking)
        }
        if let dnsSearchDomains = dnsSearchDomains {
            var dnsSearchDomainsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .dnsSearchDomains)
            for stringlist0 in dnsSearchDomains {
                try dnsSearchDomainsContainer.encode(stringlist0)
            }
        }
        if let dnsServers = dnsServers {
            var dnsServersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .dnsServers)
            for stringlist0 in dnsServers {
                try dnsServersContainer.encode(stringlist0)
            }
        }
        if let dockerLabels = dockerLabels {
            var dockerLabelsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .dockerLabels)
            for (dictKey0, dockerlabelsmap0) in dockerLabels {
                try dockerLabelsContainer.encode(dockerlabelsmap0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let dockerSecurityOptions = dockerSecurityOptions {
            var dockerSecurityOptionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .dockerSecurityOptions)
            for stringlist0 in dockerSecurityOptions {
                try dockerSecurityOptionsContainer.encode(stringlist0)
            }
        }
        if let entryPoint = entryPoint {
            var entryPointContainer = encodeContainer.nestedUnkeyedContainer(forKey: .entryPoint)
            for stringlist0 in entryPoint {
                try entryPointContainer.encode(stringlist0)
            }
        }
        if let environment = environment {
            var environmentContainer = encodeContainer.nestedUnkeyedContainer(forKey: .environment)
            for environmentvariables0 in environment {
                try environmentContainer.encode(environmentvariables0)
            }
        }
        if let environmentFiles = environmentFiles {
            var environmentFilesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .environmentFiles)
            for environmentfiles0 in environmentFiles {
                try environmentFilesContainer.encode(environmentfiles0)
            }
        }
        if let essential = essential {
            try encodeContainer.encode(essential, forKey: .essential)
        }
        if let extraHosts = extraHosts {
            var extraHostsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .extraHosts)
            for hostentrylist0 in extraHosts {
                try extraHostsContainer.encode(hostentrylist0)
            }
        }
        if let firelensConfiguration = firelensConfiguration {
            try encodeContainer.encode(firelensConfiguration, forKey: .firelensConfiguration)
        }
        if let healthCheck = healthCheck {
            try encodeContainer.encode(healthCheck, forKey: .healthCheck)
        }
        if let hostname = hostname {
            try encodeContainer.encode(hostname, forKey: .hostname)
        }
        if let image = image {
            try encodeContainer.encode(image, forKey: .image)
        }
        if let interactive = interactive {
            try encodeContainer.encode(interactive, forKey: .interactive)
        }
        if let links = links {
            var linksContainer = encodeContainer.nestedUnkeyedContainer(forKey: .links)
            for stringlist0 in links {
                try linksContainer.encode(stringlist0)
            }
        }
        if let linuxParameters = linuxParameters {
            try encodeContainer.encode(linuxParameters, forKey: .linuxParameters)
        }
        if let logConfiguration = logConfiguration {
            try encodeContainer.encode(logConfiguration, forKey: .logConfiguration)
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let memoryReservation = memoryReservation {
            try encodeContainer.encode(memoryReservation, forKey: .memoryReservation)
        }
        if let mountPoints = mountPoints {
            var mountPointsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .mountPoints)
            for mountpointlist0 in mountPoints {
                try mountPointsContainer.encode(mountpointlist0)
            }
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let portMappings = portMappings {
            var portMappingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .portMappings)
            for portmappinglist0 in portMappings {
                try portMappingsContainer.encode(portmappinglist0)
            }
        }
        if let privileged = privileged {
            try encodeContainer.encode(privileged, forKey: .privileged)
        }
        if let pseudoTerminal = pseudoTerminal {
            try encodeContainer.encode(pseudoTerminal, forKey: .pseudoTerminal)
        }
        if let readonlyRootFilesystem = readonlyRootFilesystem {
            try encodeContainer.encode(readonlyRootFilesystem, forKey: .readonlyRootFilesystem)
        }
        if let repositoryCredentials = repositoryCredentials {
            try encodeContainer.encode(repositoryCredentials, forKey: .repositoryCredentials)
        }
        if let resourceRequirements = resourceRequirements {
            var resourceRequirementsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .resourceRequirements)
            for resourcerequirements0 in resourceRequirements {
                try resourceRequirementsContainer.encode(resourcerequirements0)
            }
        }
        if let secrets = secrets {
            var secretsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .secrets)
            for secretlist0 in secrets {
                try secretsContainer.encode(secretlist0)
            }
        }
        if let startTimeout = startTimeout {
            try encodeContainer.encode(startTimeout, forKey: .startTimeout)
        }
        if let stopTimeout = stopTimeout {
            try encodeContainer.encode(stopTimeout, forKey: .stopTimeout)
        }
        if let systemControls = systemControls {
            var systemControlsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .systemControls)
            for systemcontrols0 in systemControls {
                try systemControlsContainer.encode(systemcontrols0)
            }
        }
        if let ulimits = ulimits {
            var ulimitsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .ulimits)
            for ulimitlist0 in ulimits {
                try ulimitsContainer.encode(ulimitlist0)
            }
        }
        if let user = user {
            try encodeContainer.encode(user, forKey: .user)
        }
        if let volumesFrom = volumesFrom {
            var volumesFromContainer = encodeContainer.nestedUnkeyedContainer(forKey: .volumesFrom)
            for volumefromlist0 in volumesFrom {
                try volumesFromContainer.encode(volumefromlist0)
            }
        }
        if let workingDirectory = workingDirectory {
            try encodeContainer.encode(workingDirectory, forKey: .workingDirectory)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let imageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .image)
        image = imageDecoded
        let repositoryCredentialsDecoded = try containerValues.decodeIfPresent(RepositoryCredentials.self, forKey: .repositoryCredentials)
        repositoryCredentials = repositoryCredentialsDecoded
        let cpuDecoded = try containerValues.decode(Int.self, forKey: .cpu)
        cpu = cpuDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .memory)
        memory = memoryDecoded
        let memoryReservationDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .memoryReservation)
        memoryReservation = memoryReservationDecoded
        let linksContainer = try containerValues.decodeIfPresent([String?].self, forKey: .links)
        var linksDecoded0:[String]? = nil
        if let linksContainer = linksContainer {
            linksDecoded0 = [String]()
            for string0 in linksContainer {
                if let string0 = string0 {
                    linksDecoded0?.append(string0)
                }
            }
        }
        links = linksDecoded0
        let portMappingsContainer = try containerValues.decodeIfPresent([PortMapping?].self, forKey: .portMappings)
        var portMappingsDecoded0:[PortMapping]? = nil
        if let portMappingsContainer = portMappingsContainer {
            portMappingsDecoded0 = [PortMapping]()
            for structure0 in portMappingsContainer {
                if let structure0 = structure0 {
                    portMappingsDecoded0?.append(structure0)
                }
            }
        }
        portMappings = portMappingsDecoded0
        let essentialDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .essential)
        essential = essentialDecoded
        let entryPointContainer = try containerValues.decodeIfPresent([String?].self, forKey: .entryPoint)
        var entryPointDecoded0:[String]? = nil
        if let entryPointContainer = entryPointContainer {
            entryPointDecoded0 = [String]()
            for string0 in entryPointContainer {
                if let string0 = string0 {
                    entryPointDecoded0?.append(string0)
                }
            }
        }
        entryPoint = entryPointDecoded0
        let commandContainer = try containerValues.decodeIfPresent([String?].self, forKey: .command)
        var commandDecoded0:[String]? = nil
        if let commandContainer = commandContainer {
            commandDecoded0 = [String]()
            for string0 in commandContainer {
                if let string0 = string0 {
                    commandDecoded0?.append(string0)
                }
            }
        }
        command = commandDecoded0
        let environmentContainer = try containerValues.decodeIfPresent([KeyValuePair?].self, forKey: .environment)
        var environmentDecoded0:[KeyValuePair]? = nil
        if let environmentContainer = environmentContainer {
            environmentDecoded0 = [KeyValuePair]()
            for structure0 in environmentContainer {
                if let structure0 = structure0 {
                    environmentDecoded0?.append(structure0)
                }
            }
        }
        environment = environmentDecoded0
        let environmentFilesContainer = try containerValues.decodeIfPresent([EnvironmentFile?].self, forKey: .environmentFiles)
        var environmentFilesDecoded0:[EnvironmentFile]? = nil
        if let environmentFilesContainer = environmentFilesContainer {
            environmentFilesDecoded0 = [EnvironmentFile]()
            for structure0 in environmentFilesContainer {
                if let structure0 = structure0 {
                    environmentFilesDecoded0?.append(structure0)
                }
            }
        }
        environmentFiles = environmentFilesDecoded0
        let mountPointsContainer = try containerValues.decodeIfPresent([MountPoint?].self, forKey: .mountPoints)
        var mountPointsDecoded0:[MountPoint]? = nil
        if let mountPointsContainer = mountPointsContainer {
            mountPointsDecoded0 = [MountPoint]()
            for structure0 in mountPointsContainer {
                if let structure0 = structure0 {
                    mountPointsDecoded0?.append(structure0)
                }
            }
        }
        mountPoints = mountPointsDecoded0
        let volumesFromContainer = try containerValues.decodeIfPresent([VolumeFrom?].self, forKey: .volumesFrom)
        var volumesFromDecoded0:[VolumeFrom]? = nil
        if let volumesFromContainer = volumesFromContainer {
            volumesFromDecoded0 = [VolumeFrom]()
            for structure0 in volumesFromContainer {
                if let structure0 = structure0 {
                    volumesFromDecoded0?.append(structure0)
                }
            }
        }
        volumesFrom = volumesFromDecoded0
        let linuxParametersDecoded = try containerValues.decodeIfPresent(LinuxParameters.self, forKey: .linuxParameters)
        linuxParameters = linuxParametersDecoded
        let secretsContainer = try containerValues.decodeIfPresent([Secret?].self, forKey: .secrets)
        var secretsDecoded0:[Secret]? = nil
        if let secretsContainer = secretsContainer {
            secretsDecoded0 = [Secret]()
            for structure0 in secretsContainer {
                if let structure0 = structure0 {
                    secretsDecoded0?.append(structure0)
                }
            }
        }
        secrets = secretsDecoded0
        let dependsOnContainer = try containerValues.decodeIfPresent([ContainerDependency?].self, forKey: .dependsOn)
        var dependsOnDecoded0:[ContainerDependency]? = nil
        if let dependsOnContainer = dependsOnContainer {
            dependsOnDecoded0 = [ContainerDependency]()
            for structure0 in dependsOnContainer {
                if let structure0 = structure0 {
                    dependsOnDecoded0?.append(structure0)
                }
            }
        }
        dependsOn = dependsOnDecoded0
        let startTimeoutDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .startTimeout)
        startTimeout = startTimeoutDecoded
        let stopTimeoutDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .stopTimeout)
        stopTimeout = stopTimeoutDecoded
        let hostnameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .hostname)
        hostname = hostnameDecoded
        let userDecoded = try containerValues.decodeIfPresent(String.self, forKey: .user)
        user = userDecoded
        let workingDirectoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .workingDirectory)
        workingDirectory = workingDirectoryDecoded
        let disableNetworkingDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .disableNetworking)
        disableNetworking = disableNetworkingDecoded
        let privilegedDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .privileged)
        privileged = privilegedDecoded
        let readonlyRootFilesystemDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .readonlyRootFilesystem)
        readonlyRootFilesystem = readonlyRootFilesystemDecoded
        let dnsServersContainer = try containerValues.decodeIfPresent([String?].self, forKey: .dnsServers)
        var dnsServersDecoded0:[String]? = nil
        if let dnsServersContainer = dnsServersContainer {
            dnsServersDecoded0 = [String]()
            for string0 in dnsServersContainer {
                if let string0 = string0 {
                    dnsServersDecoded0?.append(string0)
                }
            }
        }
        dnsServers = dnsServersDecoded0
        let dnsSearchDomainsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .dnsSearchDomains)
        var dnsSearchDomainsDecoded0:[String]? = nil
        if let dnsSearchDomainsContainer = dnsSearchDomainsContainer {
            dnsSearchDomainsDecoded0 = [String]()
            for string0 in dnsSearchDomainsContainer {
                if let string0 = string0 {
                    dnsSearchDomainsDecoded0?.append(string0)
                }
            }
        }
        dnsSearchDomains = dnsSearchDomainsDecoded0
        let extraHostsContainer = try containerValues.decodeIfPresent([HostEntry?].self, forKey: .extraHosts)
        var extraHostsDecoded0:[HostEntry]? = nil
        if let extraHostsContainer = extraHostsContainer {
            extraHostsDecoded0 = [HostEntry]()
            for structure0 in extraHostsContainer {
                if let structure0 = structure0 {
                    extraHostsDecoded0?.append(structure0)
                }
            }
        }
        extraHosts = extraHostsDecoded0
        let dockerSecurityOptionsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .dockerSecurityOptions)
        var dockerSecurityOptionsDecoded0:[String]? = nil
        if let dockerSecurityOptionsContainer = dockerSecurityOptionsContainer {
            dockerSecurityOptionsDecoded0 = [String]()
            for string0 in dockerSecurityOptionsContainer {
                if let string0 = string0 {
                    dockerSecurityOptionsDecoded0?.append(string0)
                }
            }
        }
        dockerSecurityOptions = dockerSecurityOptionsDecoded0
        let interactiveDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .interactive)
        interactive = interactiveDecoded
        let pseudoTerminalDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .pseudoTerminal)
        pseudoTerminal = pseudoTerminalDecoded
        let dockerLabelsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .dockerLabels)
        var dockerLabelsDecoded0: [String:String]? = nil
        if let dockerLabelsContainer = dockerLabelsContainer {
            dockerLabelsDecoded0 = [String:String]()
            for (key0, string0) in dockerLabelsContainer {
                if let string0 = string0 {
                    dockerLabelsDecoded0?[key0] = string0
                }
            }
        }
        dockerLabels = dockerLabelsDecoded0
        let ulimitsContainer = try containerValues.decodeIfPresent([Ulimit?].self, forKey: .ulimits)
        var ulimitsDecoded0:[Ulimit]? = nil
        if let ulimitsContainer = ulimitsContainer {
            ulimitsDecoded0 = [Ulimit]()
            for structure0 in ulimitsContainer {
                if let structure0 = structure0 {
                    ulimitsDecoded0?.append(structure0)
                }
            }
        }
        ulimits = ulimitsDecoded0
        let logConfigurationDecoded = try containerValues.decodeIfPresent(LogConfiguration.self, forKey: .logConfiguration)
        logConfiguration = logConfigurationDecoded
        let healthCheckDecoded = try containerValues.decodeIfPresent(HealthCheck.self, forKey: .healthCheck)
        healthCheck = healthCheckDecoded
        let systemControlsContainer = try containerValues.decodeIfPresent([SystemControl?].self, forKey: .systemControls)
        var systemControlsDecoded0:[SystemControl]? = nil
        if let systemControlsContainer = systemControlsContainer {
            systemControlsDecoded0 = [SystemControl]()
            for structure0 in systemControlsContainer {
                if let structure0 = structure0 {
                    systemControlsDecoded0?.append(structure0)
                }
            }
        }
        systemControls = systemControlsDecoded0
        let resourceRequirementsContainer = try containerValues.decodeIfPresent([ResourceRequirement?].self, forKey: .resourceRequirements)
        var resourceRequirementsDecoded0:[ResourceRequirement]? = nil
        if let resourceRequirementsContainer = resourceRequirementsContainer {
            resourceRequirementsDecoded0 = [ResourceRequirement]()
            for structure0 in resourceRequirementsContainer {
                if let structure0 = structure0 {
                    resourceRequirementsDecoded0?.append(structure0)
                }
            }
        }
        resourceRequirements = resourceRequirementsDecoded0
        let firelensConfigurationDecoded = try containerValues.decodeIfPresent(FirelensConfiguration.self, forKey: .firelensConfiguration)
        firelensConfiguration = firelensConfigurationDecoded
    }
}

extension ContainerDefinition: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ContainerDefinition(command: \(String(describing: command)), cpu: \(String(describing: cpu)), dependsOn: \(String(describing: dependsOn)), disableNetworking: \(String(describing: disableNetworking)), dnsSearchDomains: \(String(describing: dnsSearchDomains)), dnsServers: \(String(describing: dnsServers)), dockerLabels: \(String(describing: dockerLabels)), dockerSecurityOptions: \(String(describing: dockerSecurityOptions)), entryPoint: \(String(describing: entryPoint)), environment: \(String(describing: environment)), environmentFiles: \(String(describing: environmentFiles)), essential: \(String(describing: essential)), extraHosts: \(String(describing: extraHosts)), firelensConfiguration: \(String(describing: firelensConfiguration)), healthCheck: \(String(describing: healthCheck)), hostname: \(String(describing: hostname)), image: \(String(describing: image)), interactive: \(String(describing: interactive)), links: \(String(describing: links)), linuxParameters: \(String(describing: linuxParameters)), logConfiguration: \(String(describing: logConfiguration)), memory: \(String(describing: memory)), memoryReservation: \(String(describing: memoryReservation)), mountPoints: \(String(describing: mountPoints)), name: \(String(describing: name)), portMappings: \(String(describing: portMappings)), privileged: \(String(describing: privileged)), pseudoTerminal: \(String(describing: pseudoTerminal)), readonlyRootFilesystem: \(String(describing: readonlyRootFilesystem)), repositoryCredentials: \(String(describing: repositoryCredentials)), resourceRequirements: \(String(describing: resourceRequirements)), secrets: \(String(describing: secrets)), startTimeout: \(String(describing: startTimeout)), stopTimeout: \(String(describing: stopTimeout)), systemControls: \(String(describing: systemControls)), ulimits: \(String(describing: ulimits)), user: \(String(describing: user)), volumesFrom: \(String(describing: volumesFrom)), workingDirectory: \(String(describing: workingDirectory)))"}
}

/// <p>Container definitions are used in task definitions to describe the different
/// 			containers that are launched as part of a task.</p>
public struct ContainerDefinition: Equatable {
    /// <p>The command that is passed to the container. This parameter maps to <code>Cmd</code>
    /// 			in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>COMMAND</code> parameter to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>. For more information, see <a href="https://docs.docker.com/engine/reference/builder/#cmd">https://docs.docker.com/engine/reference/builder/#cmd</a>. If there are multiple arguments, each
    /// 			argument should be a separated string in the array.</p>
    public let command: [String]?
    /// <p>The number of <code>cpu</code> units reserved for the container. This parameter maps
    /// 			to <code>CpuShares</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--cpu-shares</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <p>This field is optional for tasks using the Fargate launch type, and the
    /// 			only requirement is that the total amount of CPU reserved for all containers within a
    /// 			task be lower than the task-level <code>cpu</code> value.</p>
    /// 		       <note>
    /// 			         <p>You can determine the number of CPU units that are available per EC2 instance type
    /// 				by multiplying the vCPUs listed for that instance type on the <a href="http://aws.amazon.com/ec2/instance-types/">Amazon EC2 Instances</a> detail page
    /// 				by 1,024.</p>
    /// 		       </note>
    /// 		       <p>Linux containers share unallocated CPU units with other containers on the container
    /// 			instance with the same ratio as their allocated amount. For example, if you run a
    /// 			single-container task on a single-core instance type with 512 CPU units specified for
    /// 			that container, and that is the only task running on the container instance, that
    /// 			container could use the full 1,024 CPU unit share at any given time. However, if you
    /// 			launched another copy of the same task on that container instance, each task would be
    /// 			guaranteed a minimum of 512 CPU units when needed, and each container could float to
    /// 			higher CPU usage if the other container was not using it, but if both tasks were 100%
    /// 			active all of the time, they would be limited to 512 CPU units.</p>
    /// 		       <p>On Linux container instances, the Docker daemon on the container instance uses the CPU
    /// 			value to calculate the relative CPU share ratios for running containers. For more
    /// 			information, see <a href="https://docs.docker.com/engine/reference/run/#cpu-share-constraint">CPU share
    /// 				constraint</a> in the Docker documentation. The minimum valid CPU share value
    /// 			that the Linux kernel allows is 2. However, the CPU parameter is not required, and you
    /// 			can use CPU values below 2 in your container definitions. For CPU values below 2
    /// 			(including null), the behavior varies based on your Amazon ECS container agent
    /// 			version:</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    /// 					             <b>Agent versions less than or equal to 1.1.0:</b>
    /// 					Null and zero CPU values are passed to Docker as 0, which Docker then converts
    /// 					to 1,024 CPU shares. CPU values of 1 are passed to Docker as 1, which the Linux
    /// 					kernel converts to two CPU shares.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    /// 					             <b>Agent versions greater than or equal to 1.2.0:</b>
    /// 					Null, zero, and CPU values of 1 are passed to Docker as 2.</p>
    /// 			         </li>
    ///          </ul>
    /// 		       <p>On Windows container instances, the CPU limit is enforced as an absolute limit, or a
    /// 			quota. Windows containers only have access to the specified amount of CPU that is
    /// 			described in the task definition. A null or zero CPU value is passed to Docker as
    /// 				<code>0</code>, which Windows interprets as 1% of one CPU.</p>
    public let cpu: Int
    /// <p>The dependencies defined for container startup and shutdown. A container can contain
    /// 			multiple dependencies. When a dependency is defined for container startup, for container
    /// 			shutdown it is reversed.</p>
    /// 		       <p>For tasks using the EC2 launch type, the container instances require at
    /// 			least version 1.26.0 of the container agent to enable container dependencies. However,
    /// 			we recommend using the latest container agent version. For information about checking
    /// 			your agent version and updating to the latest version, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-update.html">Updating the Amazon ECS
    /// 				Container Agent</a> in the <i>Amazon Elastic Container Service Developer Guide</i>. If you are
    /// 			using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the
    /// 				<code>ecs-init</code> package. If your container instances are launched from version
    /// 				<code>20190301</code> or later, then they contain the required versions of the
    /// 			container agent and <code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-optimized_AMI.html">Amazon ECS-optimized Linux AMI</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>For tasks using the Fargate launch type, the task or service requires
    /// 			platform version <code>1.3.0</code> or later.</p>
    public let dependsOn: [ContainerDependency]?
    /// <p>When this parameter is true, networking is disabled within the container. This
    /// 			parameter maps to <code>NetworkDisabled</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a>
    /// 			section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a>.</p>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    public let disableNetworking: Bool?
    /// <p>A list of DNS search domains that are presented to the container. This parameter maps
    /// 			to <code>DnsSearch</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--dns-search</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    public let dnsSearchDomains: [String]?
    /// <p>A list of DNS servers that are presented to the container. This parameter maps to
    /// 				<code>Dns</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--dns</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    public let dnsServers: [String]?
    /// <p>A key/value map of labels to add to the container. This parameter maps to
    /// 				<code>Labels</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--label</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>. This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: <code>sudo docker version --format '{{.Server.APIVersion}}'</code>
    ///          </p>
    public let dockerLabels: [String:String]?
    /// <p>A list of strings to provide custom labels for SELinux and AppArmor multi-level
    /// 			security systems. This field is not valid for containers in tasks using the
    /// 			Fargate launch type.</p>
    /// 		       <p>With Windows containers, this parameter can be used to reference a credential spec
    /// 			file when configuring a container for Active Directory authentication. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/windows-gmsa.html">Using gMSAs for Windows
    /// 				Containers</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>This parameter maps to <code>SecurityOpt</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--security-opt</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <note>
    /// 			         <p>The Amazon ECS container agent running on a container instance must register with the
    /// 					<code>ECS_SELINUX_CAPABLE=true</code> or <code>ECS_APPARMOR_CAPABLE=true</code>
    /// 				environment variables before containers placed on that instance can use these
    /// 				security options. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-config.html">Amazon ECS Container
    /// 					Agent Configuration</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       </note>
    /// 		       <p>For more information about valid values, see <a href="https://docs.docker.com/engine/reference/run/#security-configuration">Docker
    /// 				Run Security Configuration</a>. </p>
    /// 		       <p>Valid values: "no-new-privileges" | "apparmor:PROFILE" | "label:value" |
    /// 			"credentialspec:CredentialSpecFilePath"</p>
    public let dockerSecurityOptions: [String]?
    /// <important>
    /// 			         <p>Early versions of the Amazon ECS container agent do not properly handle
    /// 					<code>entryPoint</code> parameters. If you have problems using
    /// 					<code>entryPoint</code>, update your container agent or enter your commands and
    /// 				arguments as <code>command</code> array items instead.</p>
    /// 		       </important>
    /// 		       <p>The entry point that is passed to the container. This parameter maps to
    /// 				<code>Entrypoint</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--entrypoint</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>. For more information, see <a href="https://docs.docker.com/engine/reference/builder/#entrypoint">https://docs.docker.com/engine/reference/builder/#entrypoint</a>.</p>
    public let entryPoint: [String]?
    /// <p>The environment variables to pass to a container. This parameter maps to
    /// 				<code>Env</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--env</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <important>
    /// 			         <p>We do not recommend using plaintext environment variables for sensitive
    /// 				information, such as credential data.</p>
    /// 		       </important>
    public let environment: [KeyValuePair]?
    /// <p>A list of files containing the environment variables to pass to a container. This
    /// 			parameter maps to the <code>--env-file</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <p>You can specify up to ten environment files. The file must have a <code>.env</code>
    /// 			file extension. Each line in an environment file should contain an environment variable
    /// 			in <code>VARIABLE=VALUE</code> format. Lines beginning with <code>#</code> are treated
    /// 			as comments and are ignored. For more information on the environment variable file
    /// 			syntax, see <a href="https://docs.docker.com/compose/env-file/">Declare default
    /// 				environment variables in file</a>.</p>
    /// 		       <p>If there are environment variables specified using the <code>environment</code>
    /// 			parameter in a container definition, they take precedence over the variables contained
    /// 			within an environment file. If multiple environment files are specified that contain the
    /// 			same variable, they are processed from the top down. It is recommended to use unique
    /// 			variable names. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/taskdef-envfiles.html">Specifying Environment
    /// 				Variables</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let environmentFiles: [EnvironmentFile]?
    /// <p>If the <code>essential</code> parameter of a container is marked as <code>true</code>,
    /// 			and that container fails or stops for any reason, all other containers that are part of
    /// 			the task are stopped. If the <code>essential</code> parameter of a container is marked
    /// 			as <code>false</code>, then its failure does not affect the rest of the containers in a
    /// 			task. If this parameter is omitted, a container is assumed to be essential.</p>
    /// 		       <p>All tasks must have at least one essential container. If you have an application that
    /// 			is composed of multiple containers, you should group containers that are used for a
    /// 			common purpose into components, and separate the different components into multiple task
    /// 			definitions. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/application_architecture.html">Application
    /// 				Architecture</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let essential: Bool?
    /// <p>A list of hostnames and IP address mappings to append to the <code>/etc/hosts</code>
    /// 			file on the container. This parameter maps to <code>ExtraHosts</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--add-host</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <note>
    /// 			         <p>This parameter is not supported for Windows containers or tasks that use the
    /// 					<code>awsvpc</code> network mode.</p>
    /// 		       </note>
    public let extraHosts: [HostEntry]?
    /// <p>The FireLens configuration for the container. This is used to specify and configure a
    /// 			log router for container logs. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_firelens.html">Custom Log Routing</a>
    /// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let firelensConfiguration: FirelensConfiguration?
    /// <p>The container health check command and associated configuration parameters for the
    /// 			container. This parameter maps to <code>HealthCheck</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>HEALTHCHECK</code> parameter of <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    public let healthCheck: HealthCheck?
    /// <p>The hostname to use for your container. This parameter maps to <code>Hostname</code>
    /// 			in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--hostname</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <note>
    /// 			         <p>The <code>hostname</code> parameter is not supported if you are using the
    /// 					<code>awsvpc</code> network mode.</p>
    /// 		       </note>
    public let hostname: String?
    /// <p>The image used to start a container. This string is passed directly to the Docker
    /// 			daemon. Images in the Docker Hub registry are available by default. Other repositories
    /// 			are specified with either <code>
    /// 				           <i>repository-url</i>/<i>image</i>:<i>tag</i>
    /// 			         </code> or <code>
    /// 				           <i>repository-url</i>/<i>image</i>@<i>digest</i>
    /// 			         </code>. Up to 255 letters (uppercase and lowercase), numbers, hyphens, underscores, colons, periods, forward slashes, and number signs are allowed. This parameter maps to <code>Image</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>IMAGE</code> parameter of <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>When a new task starts, the Amazon ECS container agent pulls the latest version of
    /// 					the specified image and tag for the container to use. However, subsequent
    /// 					updates to a repository image are not propagated to already running
    /// 					tasks.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>Images in Amazon ECR repositories can be specified by either using the full
    /// 						<code>registry/repository:tag</code> or
    /// 						<code>registry/repository@digest</code>. For example,
    /// 						<code>012345678910.dkr.ecr.<region-name>.amazonaws.com/<repository-name>:latest</code>
    /// 					or
    /// 						<code>012345678910.dkr.ecr.<region-name>.amazonaws.com/<repository-name>@sha256:94afd1f2e64d908bc90dbca0035a5b567EXAMPLE</code>.
    /// 				</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>Images in official repositories on Docker Hub use a single name (for example,
    /// 						<code>ubuntu</code> or <code>mongo</code>).</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>Images in other repositories on Docker Hub are qualified with an organization
    /// 					name (for example, <code>amazon/amazon-ecs-agent</code>).</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>Images in other online repositories are qualified further by a domain name
    /// 					(for example, <code>quay.io/assemblyline/ubuntu</code>).</p>
    /// 			         </li>
    ///          </ul>
    public let image: String?
    /// <p>When this parameter is <code>true</code>, this allows you to deploy containerized
    /// 			applications that require <code>stdin</code> or a <code>tty</code> to be allocated. This
    /// 			parameter maps to <code>OpenStdin</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a>
    /// 			section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--interactive</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    public let interactive: Bool?
    /// <p>The <code>links</code> parameter allows containers to communicate with each other
    /// 			without the need for port mappings. This parameter is only supported if the network mode
    /// 			of a task definition is <code>bridge</code>. The <code>name:internalName</code>
    /// 			construct is analogous to <code>name:alias</code> in Docker links.
    /// 			Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. For more information about linking Docker containers, go to
    /// 				<a href="https://docs.docker.com/network/links/">Legacy container links</a>
    /// 			in the Docker documentation. This parameter maps to <code>Links</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--link</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 			run</a>.</p>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    ///          <important>
    /// 			         <p>Containers that are collocated on a single container instance may be able to
    /// 				communicate with each other without requiring links or host port mappings. Network
    /// 				isolation is achieved on the container instance using security groups and VPC
    /// 				settings.</p>
    /// 		       </important>
    public let links: [String]?
    /// <p>Linux-specific modifications that are applied to the container, such as Linux kernel
    /// 			capabilities. For more information see <a>KernelCapabilities</a>.</p>
    /// 		       <note>
    /// 			         <p>This parameter is not supported for Windows containers.</p>
    /// 		       </note>
    public let linuxParameters: LinuxParameters?
    /// <p>The log configuration specification for the container.</p>
    /// 		       <p>This parameter maps to <code>LogConfig</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--log-driver</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>. By default, containers use the same logging driver that the Docker
    /// 			daemon uses. However the container may use a different logging driver than the Docker
    /// 			daemon by specifying a log driver with this parameter in the container definition. To
    /// 			use a different logging driver for a container, the log system must be configured
    /// 			properly on the container instance (or on a different log server for remote logging
    /// 			options). For more information on the options for different supported log drivers, see
    /// 				<a href="https://docs.docker.com/engine/admin/logging/overview/">Configure
    /// 				logging drivers</a> in the Docker documentation.</p>
    /// 		       <note>
    /// 			         <p>Amazon ECS currently supports a subset of the logging drivers available to the Docker
    /// 				daemon (shown in the <a>LogConfiguration</a> data type). Additional log
    /// 				drivers may be available in future releases of the Amazon ECS container agent.</p>
    /// 		       </note>
    /// 		       <p>This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: <code>sudo docker version --format '{{.Server.APIVersion}}'</code>
    ///          </p>
    /// 		       <note>
    /// 			         <p>The Amazon ECS container agent running on a container instance must register the
    /// 				logging drivers available on that instance with the
    /// 					<code>ECS_AVAILABLE_LOGGING_DRIVERS</code> environment variable before
    /// 				containers placed on that instance can use these log configuration options. For more
    /// 				information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-config.html">Amazon ECS Container
    /// 					Agent Configuration</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       </note>
    public let logConfiguration: LogConfiguration?
    /// <p>The amount (in MiB) of memory to present to the container. If your container attempts
    /// 			to exceed the memory specified here, the container is killed. The total amount of memory
    /// 			reserved for all containers within a task must be lower than the task
    /// 				<code>memory</code> value, if one is specified. This parameter maps to
    /// 				<code>Memory</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--memory</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <p>If using the Fargate launch type, this parameter is optional.</p>
    /// 		       <p>If using the EC2 launch type, you must specify either a task-level
    /// 			memory value or a container-level memory value. If you specify both a container-level
    /// 				<code>memory</code> and <code>memoryReservation</code> value, <code>memory</code>
    /// 			must be greater than <code>memoryReservation</code>. If you specify
    /// 				<code>memoryReservation</code>, then that value is subtracted from the available
    /// 			memory resources for the container instance on which the container is placed. Otherwise,
    /// 			the value of <code>memory</code> is used.</p>
    /// 		       <p>The Docker daemon reserves a minimum of 4 MiB of memory for a container, so you should
    /// 			not specify fewer than 4 MiB of memory for your containers.</p>
    public let memory: Int?
    /// <p>The soft limit (in MiB) of memory to reserve for the container. When system memory is
    /// 			under heavy contention, Docker attempts to keep the container memory to this soft limit.
    /// 			However, your container can consume more memory when it needs to, up to either the hard
    /// 			limit specified with the <code>memory</code> parameter (if applicable), or all of the
    /// 			available memory on the container instance, whichever comes first. This parameter maps
    /// 			to <code>MemoryReservation</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of
    /// 			the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--memory-reservation</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <p>If a task-level memory value is not specified, you must specify a non-zero integer for
    /// 			one or both of <code>memory</code> or <code>memoryReservation</code> in a container
    /// 			definition. If you specify both, <code>memory</code> must be greater than
    /// 				<code>memoryReservation</code>. If you specify <code>memoryReservation</code>, then
    /// 			that value is subtracted from the available memory resources for the container instance
    /// 			on which the container is placed. Otherwise, the value of <code>memory</code> is
    /// 			used.</p>
    /// 		       <p>For example, if your container normally uses 128 MiB of memory, but occasionally
    /// 			bursts to 256 MiB of memory for short periods of time, you can set a
    /// 				<code>memoryReservation</code> of 128 MiB, and a <code>memory</code> hard limit of
    /// 			300 MiB. This configuration would allow the container to only reserve 128 MiB of memory
    /// 			from the remaining resources on the container instance, but also allow the container to
    /// 			consume more memory resources when needed.</p>
    /// 		       <p>The Docker daemon reserves a minimum of 4 MiB of memory for a container, so you should
    /// 			not specify fewer than 4 MiB of memory for your containers. </p>
    public let memoryReservation: Int?
    /// <p>The mount points for data volumes in your container.</p>
    /// 		       <p>This parameter maps to <code>Volumes</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a>
    /// 			section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--volume</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <p>Windows containers can mount whole directories on the same drive as
    /// 				<code>$env:ProgramData</code>. Windows containers cannot mount directories on a
    /// 			different drive, and mount point cannot be across drives.</p>
    public let mountPoints: [MountPoint]?
    /// <p>The name of a container. If you are linking multiple containers together in a task
    /// 			definition, the <code>name</code> of one container can be entered in the
    /// 				<code>links</code> of another container to connect the containers.
    /// 			Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. This parameter maps to <code>name</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--name</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 			run</a>. </p>
    public let name: String?
    /// <p>The list of port mappings for the container. Port mappings allow containers to access
    /// 			ports on the host container instance to send or receive traffic.</p>
    /// 		       <p>For task definitions that use the <code>awsvpc</code> network mode, you should only
    /// 			specify the <code>containerPort</code>. The <code>hostPort</code> can be left blank or
    /// 			it must be the same value as the <code>containerPort</code>.</p>
    /// 		       <p>Port mappings on Windows use the <code>NetNAT</code> gateway address rather than
    /// 				<code>localhost</code>. There is no loopback for port mappings on Windows, so you
    /// 			cannot access a container's mapped port from the host itself. </p>
    /// 		       <p>This parameter maps to <code>PortBindings</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--publish</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>. If the network mode of a task definition is set to <code>none</code>,
    /// 			then you can't specify port mappings. If the network mode of a task definition is set to
    /// 				<code>host</code>, then host ports must either be undefined or they must match the
    /// 			container port in the port mapping.</p>
    /// 		       <note>
    /// 			         <p>After a task reaches the <code>RUNNING</code> status, manual and automatic host
    /// 				and container port assignments are visible in the <b>Network
    /// 					Bindings</b> section of a container description for a selected task in
    /// 				the Amazon ECS console. The assignments are also visible in the
    /// 					<code>networkBindings</code> section <a>DescribeTasks</a>
    /// 				responses.</p>
    /// 		       </note>
    public let portMappings: [PortMapping]?
    /// <p>When this parameter is true, the container is given elevated privileges on the host
    /// 			container instance (similar to the <code>root</code> user). This parameter maps to
    /// 				<code>Privileged</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--privileged</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    ///          <note>
    ///                                 <p>This parameter is not supported for Windows containers or tasks run on AWS Fargate.</p>
    ///                              </note>
    public let privileged: Bool?
    /// <p>When this parameter is <code>true</code>, a TTY is allocated. This parameter maps to
    /// 				<code>Tty</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--tty</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    public let pseudoTerminal: Bool?
    /// <p>When this parameter is true, the container is given read-only access to its root file
    /// 			system. This parameter maps to <code>ReadonlyRootfs</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--read-only</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    public let readonlyRootFilesystem: Bool?
    /// <p>The private repository authentication credentials to use.</p>
    public let repositoryCredentials: RepositoryCredentials?
    /// <p>The type and amount of a resource to assign to a container. The only supported
    /// 			resource is a GPU.</p>
    public let resourceRequirements: [ResourceRequirement]?
    /// <p>The secrets to pass to the container. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/specifying-sensitive-data.html">Specifying
    /// 				Sensitive Data</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let secrets: [Secret]?
    /// <p>Time duration (in seconds) to wait before giving up on resolving dependencies for a
    /// 			container. For example, you specify two containers in a task definition with containerA
    /// 			having a dependency on containerB reaching a <code>COMPLETE</code>,
    /// 			<code>SUCCESS</code>, or <code>HEALTHY</code> status. If a <code>startTimeout</code>
    /// 			value is specified for containerB and it does not reach the desired status within that
    /// 			time then containerA will give up and not start. This results in the task transitioning
    /// 			to a <code>STOPPED</code> state.</p>
    /// 		       <note>
    /// 			         <p>When the <code>ECS_CONTAINER_START_TIMEOUT</code> container agent configuration
    /// 				variable is used, it is enforced indendently from this start timeout value.</p>
    /// 		       </note>
    /// 		       <p>For tasks using the Fargate launch type, this parameter requires that
    /// 			the task or service uses platform version 1.3.0 or later.</p>
    /// 		       <p>For tasks using the EC2 launch type, your container instances require at
    /// 			least version <code>1.26.0</code> of the container agent to enable a container start
    /// 			timeout value. However, we recommend using the latest container agent version. For
    /// 			information about checking your agent version and updating to the latest version, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-update.html">Updating the Amazon ECS
    /// 				Container Agent</a> in the <i>Amazon Elastic Container Service Developer Guide</i>. If you are
    /// 			using an Amazon ECS-optimized Linux AMI, your instance needs at least version <code>1.26.0-1</code> of
    /// 			the <code>ecs-init</code> package. If your container instances are launched from version
    /// 				<code>20190301</code> or later, then they contain the required versions of the
    /// 			container agent and <code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-optimized_AMI.html">Amazon ECS-optimized Linux AMI</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let startTimeout: Int?
    /// <p>Time duration (in seconds) to wait before the container is forcefully killed if it
    /// 			doesn't exit normally on its own.</p>
    /// 		       <p>For tasks using the Fargate launch type, the task or service requires
    /// 			platform version 1.3.0 or later. The max stop timeout value is 120 seconds and if the
    /// 			parameter is not specified, the default value of 30 seconds is used.</p>
    /// 		       <p>For tasks using the EC2 launch type, if the <code>stopTimeout</code>
    /// 			parameter is not specified, the value set for the Amazon ECS container agent configuration
    /// 			variable <code>ECS_CONTAINER_STOP_TIMEOUT</code> is used by default. If neither the
    /// 				<code>stopTimeout</code> parameter or the <code>ECS_CONTAINER_STOP_TIMEOUT</code>
    /// 			agent configuration variable are set, then the default values of 30 seconds for Linux
    /// 			containers and 30 seconds on Windows containers are used. Your container instances
    /// 			require at least version 1.26.0 of the container agent to enable a container stop
    /// 			timeout value. However, we recommend using the latest container agent version. For
    /// 			information about checking your agent version and updating to the latest version, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-update.html">Updating the Amazon ECS
    /// 				Container Agent</a> in the <i>Amazon Elastic Container Service Developer Guide</i>. If you are
    /// 			using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the
    /// 				<code>ecs-init</code> package. If your container instances are launched from version
    /// 				<code>20190301</code> or later, then they contain the required versions of the
    /// 			container agent and <code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-optimized_AMI.html">Amazon ECS-optimized Linux AMI</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let stopTimeout: Int?
    /// <p>A list of namespaced kernel parameters to set in the container. This parameter maps to
    /// 				<code>Sysctls</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--sysctl</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <note>
    /// 			         <p>It is not recommended that you specify network-related <code>systemControls</code>
    /// 				parameters for multiple containers in a single task that also uses either the
    /// 					<code>awsvpc</code> or <code>host</code> network modes. For tasks that use the
    /// 					<code>awsvpc</code> network mode, the container that is started last determines
    /// 				which <code>systemControls</code> parameters take effect. For tasks that use the
    /// 					<code>host</code> network mode, it changes the container instance's namespaced
    /// 				kernel parameters as well as the containers.</p>
    /// 		       </note>
    public let systemControls: [SystemControl]?
    /// <p>A list of <code>ulimits</code> to set in the container. If a ulimit value is specified
    /// 			in a task definition, it will override the default values set by Docker. This parameter
    /// 			maps to <code>Ulimits</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--ulimit</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>. Valid naming values are displayed
    /// 			in the <a>Ulimit</a> data type.</p>
    /// 		       <p>Amazon ECS tasks hosted on Fargate use the default
    /// 							resource limit values set by the operating system with the exception of
    /// 							the <code>nofile</code> resource limit parameter which Fargate
    /// 							overrides. The <code>nofile</code> resource limit sets a restriction on
    /// 							the number of open files that a container can use. The default
    /// 								<code>nofile</code> soft limit is <code>1024</code> and hard limit
    /// 							is <code>4096</code>.</p>
    /// 		       <p>This parameter requires version 1.18 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: <code>sudo docker version --format '{{.Server.APIVersion}}'</code>
    ///          </p>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    public let ulimits: [Ulimit]?
    /// <p>The user to use inside the container. This parameter maps to <code>User</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--user</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 			run</a>.</p>
    /// 		       <important>
    /// 			         <p>When running tasks using the <code>host</code> network mode, you should not run
    /// 				containers using the root user (UID 0). It is considered best practice to use a
    /// 				non-root user.</p>
    /// 		       </important>
    /// 		       <p>You can specify the <code>user</code> using the following formats. If specifying a UID
    /// 			or GID, you must specify it as a positive integer.</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    ///                   <code>user</code>
    ///                </p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>user:group</code>
    ///                </p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>uid</code>
    ///                </p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>uid:gid</code>
    ///                </p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>user:gid</code>
    ///                </p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>uid:group</code>
    ///                </p>
    /// 			         </li>
    ///          </ul>
    ///          <note>
    ///                         <p>This parameter is not supported for Windows containers.</p>
    ///                      </note>
    public let user: String?
    /// <p>Data volumes to mount from another container. This parameter maps to
    /// 				<code>VolumesFrom</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--volumes-from</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    public let volumesFrom: [VolumeFrom]?
    /// <p>The working directory in which to run commands inside the container. This parameter
    /// 			maps to <code>WorkingDir</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--workdir</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    public let workingDirectory: String?

    public init (
        command: [String]? = nil,
        cpu: Int = 0,
        dependsOn: [ContainerDependency]? = nil,
        disableNetworking: Bool? = nil,
        dnsSearchDomains: [String]? = nil,
        dnsServers: [String]? = nil,
        dockerLabels: [String:String]? = nil,
        dockerSecurityOptions: [String]? = nil,
        entryPoint: [String]? = nil,
        environment: [KeyValuePair]? = nil,
        environmentFiles: [EnvironmentFile]? = nil,
        essential: Bool? = nil,
        extraHosts: [HostEntry]? = nil,
        firelensConfiguration: FirelensConfiguration? = nil,
        healthCheck: HealthCheck? = nil,
        hostname: String? = nil,
        image: String? = nil,
        interactive: Bool? = nil,
        links: [String]? = nil,
        linuxParameters: LinuxParameters? = nil,
        logConfiguration: LogConfiguration? = nil,
        memory: Int? = nil,
        memoryReservation: Int? = nil,
        mountPoints: [MountPoint]? = nil,
        name: String? = nil,
        portMappings: [PortMapping]? = nil,
        privileged: Bool? = nil,
        pseudoTerminal: Bool? = nil,
        readonlyRootFilesystem: Bool? = nil,
        repositoryCredentials: RepositoryCredentials? = nil,
        resourceRequirements: [ResourceRequirement]? = nil,
        secrets: [Secret]? = nil,
        startTimeout: Int? = nil,
        stopTimeout: Int? = nil,
        systemControls: [SystemControl]? = nil,
        ulimits: [Ulimit]? = nil,
        user: String? = nil,
        volumesFrom: [VolumeFrom]? = nil,
        workingDirectory: String? = nil
    )
    {
        self.command = command
        self.cpu = cpu
        self.dependsOn = dependsOn
        self.disableNetworking = disableNetworking
        self.dnsSearchDomains = dnsSearchDomains
        self.dnsServers = dnsServers
        self.dockerLabels = dockerLabels
        self.dockerSecurityOptions = dockerSecurityOptions
        self.entryPoint = entryPoint
        self.environment = environment
        self.environmentFiles = environmentFiles
        self.essential = essential
        self.extraHosts = extraHosts
        self.firelensConfiguration = firelensConfiguration
        self.healthCheck = healthCheck
        self.hostname = hostname
        self.image = image
        self.interactive = interactive
        self.links = links
        self.linuxParameters = linuxParameters
        self.logConfiguration = logConfiguration
        self.memory = memory
        self.memoryReservation = memoryReservation
        self.mountPoints = mountPoints
        self.name = name
        self.portMappings = portMappings
        self.privileged = privileged
        self.pseudoTerminal = pseudoTerminal
        self.readonlyRootFilesystem = readonlyRootFilesystem
        self.repositoryCredentials = repositoryCredentials
        self.resourceRequirements = resourceRequirements
        self.secrets = secrets
        self.startTimeout = startTimeout
        self.stopTimeout = stopTimeout
        self.systemControls = systemControls
        self.ulimits = ulimits
        self.user = user
        self.volumesFrom = volumesFrom
        self.workingDirectory = workingDirectory
    }
}

extension ContainerDependency: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case condition
        case containerName
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let condition = condition {
            try encodeContainer.encode(condition.rawValue, forKey: .condition)
        }
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let conditionDecoded = try containerValues.decodeIfPresent(ContainerCondition.self, forKey: .condition)
        condition = conditionDecoded
    }
}

extension ContainerDependency: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ContainerDependency(condition: \(String(describing: condition)), containerName: \(String(describing: containerName)))"}
}

/// <p>The dependencies defined for container startup and shutdown. A container can contain
/// 			multiple dependencies. When a dependency is defined for container startup, for container
/// 			shutdown it is reversed.</p>
/// 		       <p>Your Amazon ECS container instances require at least version 1.26.0 of the container agent
/// 			to enable container dependencies. However, we recommend using the latest container agent
/// 			version. For information about checking your agent version and updating to the latest
/// 			version, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-update.html">Updating the Amazon ECS
/// 				Container Agent</a> in the <i>Amazon Elastic Container Service Developer Guide</i>. If you are
/// 			using an Amazon ECS-optimized Linux AMI, your instance needs at least version 1.26.0-1 of the
/// 				<code>ecs-init</code> package. If your container instances are launched from version
/// 				<code>20190301</code> or later, then they contain the required versions of the
/// 			container agent and <code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-optimized_AMI.html">Amazon ECS-optimized Linux AMI</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
/// 		       <note>
/// 			         <p>For tasks using the Fargate launch type, this parameter requires
/// 				that the task or service uses platform version 1.3.0 or later.</p>
/// 		       </note>
public struct ContainerDependency: Equatable {
    /// <p>The dependency condition of the container. The following are the available conditions
    /// 			and their behavior:</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    ///                   <code>START</code> - This condition emulates the behavior of links and
    /// 					volumes today. It validates that a dependent container is started before
    /// 					permitting other containers to start.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>COMPLETE</code> - This condition validates that a dependent
    /// 					container runs to completion (exits) before permitting other containers to
    /// 					start. This can be useful for nonessential containers that run a script and then
    /// 					exit. This condition cannot be set on an essential container.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>SUCCESS</code> - This condition is the same as
    /// 						<code>COMPLETE</code>, but it also requires that the container exits with a
    /// 						<code>zero</code> status. This condition cannot be set on an essential
    /// 					container.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>HEALTHY</code> - This condition validates that the dependent
    /// 					container passes its Docker health check before permitting other containers to
    /// 					start. This requires that the dependent container has health checks configured.
    /// 					This condition is confirmed only at task startup.</p>
    /// 			         </li>
    ///          </ul>
    public let condition: ContainerCondition?
    /// <p>The name of a container.</p>
    public let containerName: String?

    public init (
        condition: ContainerCondition? = nil,
        containerName: String? = nil
    )
    {
        self.condition = condition
        self.containerName = containerName
    }
}

extension ContainerInstance: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case agentConnected
        case agentUpdateStatus
        case attachments
        case attributes
        case capacityProviderName
        case containerInstanceArn
        case ec2InstanceId
        case pendingTasksCount
        case registeredAt
        case registeredResources
        case remainingResources
        case runningTasksCount
        case status
        case statusReason
        case tags
        case version
        case versionInfo
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if agentConnected != false {
            try encodeContainer.encode(agentConnected, forKey: .agentConnected)
        }
        if let agentUpdateStatus = agentUpdateStatus {
            try encodeContainer.encode(agentUpdateStatus.rawValue, forKey: .agentUpdateStatus)
        }
        if let attachments = attachments {
            var attachmentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attachments)
            for attachments0 in attachments {
                try attachmentsContainer.encode(attachments0)
            }
        }
        if let attributes = attributes {
            var attributesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attributes)
            for attributes0 in attributes {
                try attributesContainer.encode(attributes0)
            }
        }
        if let capacityProviderName = capacityProviderName {
            try encodeContainer.encode(capacityProviderName, forKey: .capacityProviderName)
        }
        if let containerInstanceArn = containerInstanceArn {
            try encodeContainer.encode(containerInstanceArn, forKey: .containerInstanceArn)
        }
        if let ec2InstanceId = ec2InstanceId {
            try encodeContainer.encode(ec2InstanceId, forKey: .ec2InstanceId)
        }
        if pendingTasksCount != 0 {
            try encodeContainer.encode(pendingTasksCount, forKey: .pendingTasksCount)
        }
        if let registeredAt = registeredAt {
            try encodeContainer.encode(registeredAt.timeIntervalSince1970, forKey: .registeredAt)
        }
        if let registeredResources = registeredResources {
            var registeredResourcesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .registeredResources)
            for resources0 in registeredResources {
                try registeredResourcesContainer.encode(resources0)
            }
        }
        if let remainingResources = remainingResources {
            var remainingResourcesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .remainingResources)
            for resources0 in remainingResources {
                try remainingResourcesContainer.encode(resources0)
            }
        }
        if runningTasksCount != 0 {
            try encodeContainer.encode(runningTasksCount, forKey: .runningTasksCount)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let statusReason = statusReason {
            try encodeContainer.encode(statusReason, forKey: .statusReason)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if version != 0 {
            try encodeContainer.encode(version, forKey: .version)
        }
        if let versionInfo = versionInfo {
            try encodeContainer.encode(versionInfo, forKey: .versionInfo)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstanceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstanceArn)
        containerInstanceArn = containerInstanceArnDecoded
        let ec2InstanceIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ec2InstanceId)
        ec2InstanceId = ec2InstanceIdDecoded
        let capacityProviderNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .capacityProviderName)
        capacityProviderName = capacityProviderNameDecoded
        let versionDecoded = try containerValues.decode(Int.self, forKey: .version)
        version = versionDecoded
        let versionInfoDecoded = try containerValues.decodeIfPresent(VersionInfo.self, forKey: .versionInfo)
        versionInfo = versionInfoDecoded
        let remainingResourcesContainer = try containerValues.decodeIfPresent([Resource?].self, forKey: .remainingResources)
        var remainingResourcesDecoded0:[Resource]? = nil
        if let remainingResourcesContainer = remainingResourcesContainer {
            remainingResourcesDecoded0 = [Resource]()
            for structure0 in remainingResourcesContainer {
                if let structure0 = structure0 {
                    remainingResourcesDecoded0?.append(structure0)
                }
            }
        }
        remainingResources = remainingResourcesDecoded0
        let registeredResourcesContainer = try containerValues.decodeIfPresent([Resource?].self, forKey: .registeredResources)
        var registeredResourcesDecoded0:[Resource]? = nil
        if let registeredResourcesContainer = registeredResourcesContainer {
            registeredResourcesDecoded0 = [Resource]()
            for structure0 in registeredResourcesContainer {
                if let structure0 = structure0 {
                    registeredResourcesDecoded0?.append(structure0)
                }
            }
        }
        registeredResources = registeredResourcesDecoded0
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let statusReasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .statusReason)
        statusReason = statusReasonDecoded
        let agentConnectedDecoded = try containerValues.decode(Bool.self, forKey: .agentConnected)
        agentConnected = agentConnectedDecoded
        let runningTasksCountDecoded = try containerValues.decode(Int.self, forKey: .runningTasksCount)
        runningTasksCount = runningTasksCountDecoded
        let pendingTasksCountDecoded = try containerValues.decode(Int.self, forKey: .pendingTasksCount)
        pendingTasksCount = pendingTasksCountDecoded
        let agentUpdateStatusDecoded = try containerValues.decodeIfPresent(AgentUpdateStatus.self, forKey: .agentUpdateStatus)
        agentUpdateStatus = agentUpdateStatusDecoded
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
        let registeredAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .registeredAt)
        registeredAt = registeredAtDecoded
        let attachmentsContainer = try containerValues.decodeIfPresent([Attachment?].self, forKey: .attachments)
        var attachmentsDecoded0:[Attachment]? = nil
        if let attachmentsContainer = attachmentsContainer {
            attachmentsDecoded0 = [Attachment]()
            for structure0 in attachmentsContainer {
                if let structure0 = structure0 {
                    attachmentsDecoded0?.append(structure0)
                }
            }
        }
        attachments = attachmentsDecoded0
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension ContainerInstance: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ContainerInstance(agentConnected: \(String(describing: agentConnected)), agentUpdateStatus: \(String(describing: agentUpdateStatus)), attachments: \(String(describing: attachments)), attributes: \(String(describing: attributes)), capacityProviderName: \(String(describing: capacityProviderName)), containerInstanceArn: \(String(describing: containerInstanceArn)), ec2InstanceId: \(String(describing: ec2InstanceId)), pendingTasksCount: \(String(describing: pendingTasksCount)), registeredAt: \(String(describing: registeredAt)), registeredResources: \(String(describing: registeredResources)), remainingResources: \(String(describing: remainingResources)), runningTasksCount: \(String(describing: runningTasksCount)), status: \(String(describing: status)), statusReason: \(String(describing: statusReason)), tags: \(String(describing: tags)), version: \(String(describing: version)), versionInfo: \(String(describing: versionInfo)))"}
}

/// <p>An EC2 instance that is running the Amazon ECS agent and has been registered with a
/// 			cluster.</p>
public struct ContainerInstance: Equatable {
    /// <p>This parameter returns <code>true</code> if the agent is connected to Amazon ECS.
    /// 			Registered instances with an agent that may be unhealthy or stopped return
    /// 				<code>false</code>. Only instances connected to an agent can accept placement
    /// 			requests.</p>
    public let agentConnected: Bool
    /// <p>The status of the most recent agent update. If an update has never been requested,
    /// 			this value is <code>NULL</code>.</p>
    public let agentUpdateStatus: AgentUpdateStatus?
    /// <p>The resources attached to a container instance, such as elastic network
    /// 			interfaces.</p>
    public let attachments: [Attachment]?
    /// <p>The attributes set for the container instance, either by the Amazon ECS container agent at
    /// 			instance registration or manually with the <a>PutAttributes</a>
    /// 			operation.</p>
    public let attributes: [Attribute]?
    /// <p>The capacity provider associated with the container instance.</p>
    public let capacityProviderName: String?
    /// <p>The Amazon Resource Name (ARN) of the container instance. The ARN contains the <code>arn:aws:ecs</code> namespace, followed by the Region of the container instance, the AWS account ID of the container instance owner, the <code>container-instance</code> namespace, and then the container instance ID. For example, <code>arn:aws:ecs:region:aws_account_id:container-instance/container_instance_ID</code>.</p>
    public let containerInstanceArn: String?
    /// <p>The ID of the container instance. For Amazon EC2 instances, this value is the Amazon EC2
    /// 			instance ID. For external instances, this value is the AWS Systems Manager managed instance ID.</p>
    public let ec2InstanceId: String?
    /// <p>The number of tasks on the container instance that are in the <code>PENDING</code>
    /// 			status.</p>
    public let pendingTasksCount: Int
    /// <p>The Unix timestamp for when the container instance was registered.</p>
    public let registeredAt: Date?
    /// <p>For CPU and memory resource types, this parameter describes the amount of each
    /// 			resource that was available on the container instance when the container agent
    /// 			registered it with Amazon ECS. This value represents the total amount of CPU and memory that
    /// 			can be allocated on this container instance to tasks. For port resource types, this
    /// 			parameter describes the ports that were reserved by the Amazon ECS container agent when it
    /// 			registered the container instance with Amazon ECS.</p>
    public let registeredResources: [Resource]?
    /// <p>For CPU and memory resource types, this parameter describes the remaining CPU and
    /// 			memory that has not already been allocated to tasks and is therefore available for new
    /// 			tasks. For port resource types, this parameter describes the ports that were reserved by
    /// 			the Amazon ECS container agent (at instance registration time) and any task containers that
    /// 			have reserved port mappings on the host (with the <code>host</code> or
    /// 				<code>bridge</code> network mode). Any port that is not specified here is available
    /// 			for new tasks.</p>
    public let remainingResources: [Resource]?
    /// <p>The number of tasks on the container instance that are in the <code>RUNNING</code>
    /// 			status.</p>
    public let runningTasksCount: Int
    /// <p>The status of the container instance. The valid values are <code>REGISTERING</code>,
    /// 				<code>REGISTRATION_FAILED</code>, <code>ACTIVE</code>, <code>INACTIVE</code>,
    /// 				<code>DEREGISTERING</code>, or <code>DRAINING</code>.</p>
    /// 		       <p>If your account has opted in to the <code>awsvpcTrunking</code> account setting, then
    /// 			any newly registered container instance will transition to a <code>REGISTERING</code>
    /// 			status while the trunk elastic network interface is provisioned for the instance. If the
    /// 			registration fails, the instance will transition to a <code>REGISTRATION_FAILED</code>
    /// 			status. You can describe the container instance and see the reason for failure in the
    /// 				<code>statusReason</code> parameter. Once the container instance is terminated, the
    /// 			instance transitions to a <code>DEREGISTERING</code> status while the trunk elastic
    /// 			network interface is deprovisioned. The instance then transitions to an
    /// 				<code>INACTIVE</code> status.</p>
    /// 		       <p>The <code>ACTIVE</code> status indicates that the container instance can accept tasks.
    /// 			The <code>DRAINING</code> indicates that new tasks are not placed on the container
    /// 			instance and any service tasks running on the container instance are removed if
    /// 			possible. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/container-instance-draining.html">Container Instance Draining</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let status: String?
    /// <p>The reason that the container instance reached its current status.</p>
    public let statusReason: String?
    /// <p>The metadata that you apply to the container instance to help you categorize and
    /// 			organize them. Each tag consists of a key and an optional value, both of which you
    /// 			define.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The version counter for the container instance. Every time a container instance
    /// 			experiences a change that triggers a CloudWatch event, the version counter is
    /// 			incremented. If you are replicating your Amazon ECS container instance state with CloudWatch
    /// 			Events, you can compare the version of a container instance reported by the Amazon ECS APIs
    /// 			with the version reported in CloudWatch Events for the container instance (inside the
    /// 				<code>detail</code> object) to verify that the version in your event stream is
    /// 			current.</p>
    public let version: Int
    /// <p>The version information for the Amazon ECS container agent and Docker daemon running on the
    /// 			container instance.</p>
    public let versionInfo: VersionInfo?

    public init (
        agentConnected: Bool = false,
        agentUpdateStatus: AgentUpdateStatus? = nil,
        attachments: [Attachment]? = nil,
        attributes: [Attribute]? = nil,
        capacityProviderName: String? = nil,
        containerInstanceArn: String? = nil,
        ec2InstanceId: String? = nil,
        pendingTasksCount: Int = 0,
        registeredAt: Date? = nil,
        registeredResources: [Resource]? = nil,
        remainingResources: [Resource]? = nil,
        runningTasksCount: Int = 0,
        status: String? = nil,
        statusReason: String? = nil,
        tags: [Tag]? = nil,
        version: Int = 0,
        versionInfo: VersionInfo? = nil
    )
    {
        self.agentConnected = agentConnected
        self.agentUpdateStatus = agentUpdateStatus
        self.attachments = attachments
        self.attributes = attributes
        self.capacityProviderName = capacityProviderName
        self.containerInstanceArn = containerInstanceArn
        self.ec2InstanceId = ec2InstanceId
        self.pendingTasksCount = pendingTasksCount
        self.registeredAt = registeredAt
        self.registeredResources = registeredResources
        self.remainingResources = remainingResources
        self.runningTasksCount = runningTasksCount
        self.status = status
        self.statusReason = statusReason
        self.tags = tags
        self.version = version
        self.versionInfo = versionInfo
    }
}

public enum ContainerInstanceField {
    case tags
    case sdkUnknown(String)
}

extension ContainerInstanceField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ContainerInstanceField] {
        return [
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ContainerInstanceField(rawValue: rawValue) ?? ContainerInstanceField.sdkUnknown(rawValue)
    }
}

public enum ContainerInstanceStatus {
    case active
    case deregistering
    case draining
    case registering
    case registrationFailed
    case sdkUnknown(String)
}

extension ContainerInstanceStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ContainerInstanceStatus] {
        return [
            .active,
            .deregistering,
            .draining,
            .registering,
            .registrationFailed,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .active: return "ACTIVE"
        case .deregistering: return "DEREGISTERING"
        case .draining: return "DRAINING"
        case .registering: return "REGISTERING"
        case .registrationFailed: return "REGISTRATION_FAILED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ContainerInstanceStatus(rawValue: rawValue) ?? ContainerInstanceStatus.sdkUnknown(rawValue)
    }
}

extension ContainerOverride: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case command
        case cpu
        case environment
        case environmentFiles
        case memory
        case memoryReservation
        case name
        case resourceRequirements
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let command = command {
            var commandContainer = encodeContainer.nestedUnkeyedContainer(forKey: .command)
            for stringlist0 in command {
                try commandContainer.encode(stringlist0)
            }
        }
        if let cpu = cpu {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let environment = environment {
            var environmentContainer = encodeContainer.nestedUnkeyedContainer(forKey: .environment)
            for environmentvariables0 in environment {
                try environmentContainer.encode(environmentvariables0)
            }
        }
        if let environmentFiles = environmentFiles {
            var environmentFilesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .environmentFiles)
            for environmentfiles0 in environmentFiles {
                try environmentFilesContainer.encode(environmentfiles0)
            }
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let memoryReservation = memoryReservation {
            try encodeContainer.encode(memoryReservation, forKey: .memoryReservation)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let resourceRequirements = resourceRequirements {
            var resourceRequirementsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .resourceRequirements)
            for resourcerequirements0 in resourceRequirements {
                try resourceRequirementsContainer.encode(resourcerequirements0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let commandContainer = try containerValues.decodeIfPresent([String?].self, forKey: .command)
        var commandDecoded0:[String]? = nil
        if let commandContainer = commandContainer {
            commandDecoded0 = [String]()
            for string0 in commandContainer {
                if let string0 = string0 {
                    commandDecoded0?.append(string0)
                }
            }
        }
        command = commandDecoded0
        let environmentContainer = try containerValues.decodeIfPresent([KeyValuePair?].self, forKey: .environment)
        var environmentDecoded0:[KeyValuePair]? = nil
        if let environmentContainer = environmentContainer {
            environmentDecoded0 = [KeyValuePair]()
            for structure0 in environmentContainer {
                if let structure0 = structure0 {
                    environmentDecoded0?.append(structure0)
                }
            }
        }
        environment = environmentDecoded0
        let environmentFilesContainer = try containerValues.decodeIfPresent([EnvironmentFile?].self, forKey: .environmentFiles)
        var environmentFilesDecoded0:[EnvironmentFile]? = nil
        if let environmentFilesContainer = environmentFilesContainer {
            environmentFilesDecoded0 = [EnvironmentFile]()
            for structure0 in environmentFilesContainer {
                if let structure0 = structure0 {
                    environmentFilesDecoded0?.append(structure0)
                }
            }
        }
        environmentFiles = environmentFilesDecoded0
        let cpuDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .cpu)
        cpu = cpuDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .memory)
        memory = memoryDecoded
        let memoryReservationDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .memoryReservation)
        memoryReservation = memoryReservationDecoded
        let resourceRequirementsContainer = try containerValues.decodeIfPresent([ResourceRequirement?].self, forKey: .resourceRequirements)
        var resourceRequirementsDecoded0:[ResourceRequirement]? = nil
        if let resourceRequirementsContainer = resourceRequirementsContainer {
            resourceRequirementsDecoded0 = [ResourceRequirement]()
            for structure0 in resourceRequirementsContainer {
                if let structure0 = structure0 {
                    resourceRequirementsDecoded0?.append(structure0)
                }
            }
        }
        resourceRequirements = resourceRequirementsDecoded0
    }
}

extension ContainerOverride: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ContainerOverride(command: \(String(describing: command)), cpu: \(String(describing: cpu)), environment: \(String(describing: environment)), environmentFiles: \(String(describing: environmentFiles)), memory: \(String(describing: memory)), memoryReservation: \(String(describing: memoryReservation)), name: \(String(describing: name)), resourceRequirements: \(String(describing: resourceRequirements)))"}
}

/// <p>The overrides that should be sent to a container. An empty container override can be
/// 			passed in. An example of an empty container override would be
/// 				<code>{"containerOverrides": [ ] }</code>. If a non-empty container override is
/// 			specified, the <code>name</code> parameter must be included.</p>
public struct ContainerOverride: Equatable {
    /// <p>The command to send to the container that overrides the default command from the
    /// 			Docker image or the task definition. You must also specify a container name.</p>
    public let command: [String]?
    /// <p>The number of <code>cpu</code> units reserved for the container, instead of the
    /// 			default value from the task definition. You must also specify a container name.</p>
    public let cpu: Int?
    /// <p>The environment variables to send to the container. You can add new environment
    /// 			variables, which are added to the container at launch, or you can override the existing
    /// 			environment variables from the Docker image or the task definition. You must also
    /// 			specify a container name.</p>
    public let environment: [KeyValuePair]?
    /// <p>A list of files containing the environment variables to pass to a container, instead
    /// 			of the value from the container definition.</p>
    public let environmentFiles: [EnvironmentFile]?
    /// <p>The hard limit (in MiB) of memory to present to the container, instead of the default
    /// 			value from the task definition. If your container attempts to exceed the memory
    /// 			specified here, the container is killed. You must also specify a container name.</p>
    public let memory: Int?
    /// <p>The soft limit (in MiB) of memory to reserve for the container, instead of the default
    /// 			value from the task definition. You must also specify a container name.</p>
    public let memoryReservation: Int?
    /// <p>The name of the container that receives the override. This parameter is required if
    /// 			any override is specified.</p>
    public let name: String?
    /// <p>The type and amount of a resource to assign to a container, instead of the default
    /// 			value from the task definition. The only supported resource is a GPU.</p>
    public let resourceRequirements: [ResourceRequirement]?

    public init (
        command: [String]? = nil,
        cpu: Int? = nil,
        environment: [KeyValuePair]? = nil,
        environmentFiles: [EnvironmentFile]? = nil,
        memory: Int? = nil,
        memoryReservation: Int? = nil,
        name: String? = nil,
        resourceRequirements: [ResourceRequirement]? = nil
    )
    {
        self.command = command
        self.cpu = cpu
        self.environment = environment
        self.environmentFiles = environmentFiles
        self.memory = memory
        self.memoryReservation = memoryReservation
        self.name = name
        self.resourceRequirements = resourceRequirements
    }
}

extension ContainerStateChange: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerName
        case exitCode
        case imageDigest
        case networkBindings
        case reason
        case runtimeId
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
        if let exitCode = exitCode {
            try encodeContainer.encode(exitCode, forKey: .exitCode)
        }
        if let imageDigest = imageDigest {
            try encodeContainer.encode(imageDigest, forKey: .imageDigest)
        }
        if let networkBindings = networkBindings {
            var networkBindingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .networkBindings)
            for networkbindings0 in networkBindings {
                try networkBindingsContainer.encode(networkbindings0)
            }
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
        if let runtimeId = runtimeId {
            try encodeContainer.encode(runtimeId, forKey: .runtimeId)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let imageDigestDecoded = try containerValues.decodeIfPresent(String.self, forKey: .imageDigest)
        imageDigest = imageDigestDecoded
        let runtimeIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .runtimeId)
        runtimeId = runtimeIdDecoded
        let exitCodeDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .exitCode)
        exitCode = exitCodeDecoded
        let networkBindingsContainer = try containerValues.decodeIfPresent([NetworkBinding?].self, forKey: .networkBindings)
        var networkBindingsDecoded0:[NetworkBinding]? = nil
        if let networkBindingsContainer = networkBindingsContainer {
            networkBindingsDecoded0 = [NetworkBinding]()
            for structure0 in networkBindingsContainer {
                if let structure0 = structure0 {
                    networkBindingsDecoded0?.append(structure0)
                }
            }
        }
        networkBindings = networkBindingsDecoded0
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
    }
}

extension ContainerStateChange: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ContainerStateChange(containerName: \(String(describing: containerName)), exitCode: \(String(describing: exitCode)), imageDigest: \(String(describing: imageDigest)), networkBindings: \(String(describing: networkBindings)), reason: \(String(describing: reason)), runtimeId: \(String(describing: runtimeId)), status: \(String(describing: status)))"}
}

/// <p>An object representing a change in state for a container.</p>
public struct ContainerStateChange: Equatable {
    /// <p>The name of the container.</p>
    public let containerName: String?
    /// <p>The exit code for the container, if the state change is a result of the container
    /// 			exiting.</p>
    public let exitCode: Int?
    /// <p>The container image SHA 256 digest.</p>
    public let imageDigest: String?
    /// <p>Any network bindings associated with the container.</p>
    public let networkBindings: [NetworkBinding]?
    /// <p>The reason for the state change.</p>
    public let reason: String?
    /// <p>The ID of the Docker container.</p>
    public let runtimeId: String?
    /// <p>The status of the container.</p>
    public let status: String?

    public init (
        containerName: String? = nil,
        exitCode: Int? = nil,
        imageDigest: String? = nil,
        networkBindings: [NetworkBinding]? = nil,
        reason: String? = nil,
        runtimeId: String? = nil,
        status: String? = nil
    )
    {
        self.containerName = containerName
        self.exitCode = exitCode
        self.imageDigest = imageDigest
        self.networkBindings = networkBindings
        self.reason = reason
        self.runtimeId = runtimeId
        self.status = status
    }
}

public struct CreateCapacityProviderInputBodyMiddleware: Middleware {
    public let id: String = "CreateCapacityProviderInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateCapacityProviderInput>
    public typealias MOutput = OperationOutput<CreateCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateCapacityProviderOutputError>
}

extension CreateCapacityProviderInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateCapacityProviderInput(autoScalingGroupProvider: \(String(describing: autoScalingGroupProvider)), name: \(String(describing: name)), tags: \(String(describing: tags)))"}
}

extension CreateCapacityProviderInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case autoScalingGroupProvider
        case name
        case tags
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let autoScalingGroupProvider = autoScalingGroupProvider {
            try encodeContainer.encode(autoScalingGroupProvider, forKey: .autoScalingGroupProvider)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
    }
}

public struct CreateCapacityProviderInputHeadersMiddleware: Middleware {
    public let id: String = "CreateCapacityProviderInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateCapacityProviderInput>
    public typealias MOutput = OperationOutput<CreateCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateCapacityProviderOutputError>
}

public struct CreateCapacityProviderInputQueryItemMiddleware: Middleware {
    public let id: String = "CreateCapacityProviderInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateCapacityProviderInput>
    public typealias MOutput = OperationOutput<CreateCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateCapacityProviderOutputError>
}

public struct CreateCapacityProviderInput: Equatable {
    /// <p>The details of the Auto Scaling group for the capacity provider.</p>
    public let autoScalingGroupProvider: AutoScalingGroupProvider?
    /// <p>The name of the capacity provider. Up to 255 characters are allowed, including letters
    /// 			(upper and lowercase), numbers, underscores, and hyphens. The name cannot be prefixed
    /// 			with "<code>aws</code>", "<code>ecs</code>", or "<code>fargate</code>".</p>
    public let name: String?
    /// <p>The metadata that you apply to the capacity provider to help you categorize and
    /// 			organize them. Each tag consists of a key and an optional value, both of which you
    /// 			define.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?

    public init (
        autoScalingGroupProvider: AutoScalingGroupProvider? = nil,
        name: String? = nil,
        tags: [Tag]? = nil
    )
    {
        self.autoScalingGroupProvider = autoScalingGroupProvider
        self.name = name
        self.tags = tags
    }
}

struct CreateCapacityProviderInputBody: Equatable {
    public let name: String?
    public let autoScalingGroupProvider: AutoScalingGroupProvider?
    public let tags: [Tag]?
}

extension CreateCapacityProviderInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case autoScalingGroupProvider
        case name
        case tags
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let autoScalingGroupProviderDecoded = try containerValues.decodeIfPresent(AutoScalingGroupProvider.self, forKey: .autoScalingGroupProvider)
        autoScalingGroupProvider = autoScalingGroupProviderDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension CreateCapacityProviderOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension CreateCapacityProviderOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "LimitExceededException" : self = .limitExceededException(try LimitExceededException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UpdateInProgressException" : self = .updateInProgressException(try UpdateInProgressException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum CreateCapacityProviderOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case limitExceededException(LimitExceededException)
    case serverException(ServerException)
    case updateInProgressException(UpdateInProgressException)
    case unknown(UnknownAWSHttpServiceError)
}

extension CreateCapacityProviderOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateCapacityProviderOutputResponse(capacityProvider: \(String(describing: capacityProvider)))"}
}

extension CreateCapacityProviderOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: CreateCapacityProviderOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.capacityProvider = output.capacityProvider
        } else {
            self.capacityProvider = nil
        }
    }
}

public struct CreateCapacityProviderOutputResponse: Equatable {
    /// <p>The full description of the new capacity provider.</p>
    public let capacityProvider: CapacityProvider?

    public init (
        capacityProvider: CapacityProvider? = nil
    )
    {
        self.capacityProvider = capacityProvider
    }
}

struct CreateCapacityProviderOutputResponseBody: Equatable {
    public let capacityProvider: CapacityProvider?
}

extension CreateCapacityProviderOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProvider
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderDecoded = try containerValues.decodeIfPresent(CapacityProvider.self, forKey: .capacityProvider)
        capacityProvider = capacityProviderDecoded
    }
}

public struct CreateClusterInputBodyMiddleware: Middleware {
    public let id: String = "CreateClusterInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateClusterInput>
    public typealias MOutput = OperationOutput<CreateClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateClusterOutputError>
}

extension CreateClusterInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateClusterInput(capacityProviders: \(String(describing: capacityProviders)), clusterName: \(String(describing: clusterName)), configuration: \(String(describing: configuration)), defaultCapacityProviderStrategy: \(String(describing: defaultCapacityProviderStrategy)), settings: \(String(describing: settings)), tags: \(String(describing: tags)))"}
}

extension CreateClusterInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case clusterName
        case configuration
        case defaultCapacityProviderStrategy
        case settings
        case tags
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviders = capacityProviders {
            var capacityProvidersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviders)
            for stringlist0 in capacityProviders {
                try capacityProvidersContainer.encode(stringlist0)
            }
        }
        if let clusterName = clusterName {
            try encodeContainer.encode(clusterName, forKey: .clusterName)
        }
        if let configuration = configuration {
            try encodeContainer.encode(configuration, forKey: .configuration)
        }
        if let defaultCapacityProviderStrategy = defaultCapacityProviderStrategy {
            var defaultCapacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .defaultCapacityProviderStrategy)
            for capacityproviderstrategy0 in defaultCapacityProviderStrategy {
                try defaultCapacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let settings = settings {
            var settingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .settings)
            for clustersettings0 in settings {
                try settingsContainer.encode(clustersettings0)
            }
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
    }
}

public struct CreateClusterInputHeadersMiddleware: Middleware {
    public let id: String = "CreateClusterInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateClusterInput>
    public typealias MOutput = OperationOutput<CreateClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateClusterOutputError>
}

public struct CreateClusterInputQueryItemMiddleware: Middleware {
    public let id: String = "CreateClusterInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateClusterInput>
    public typealias MOutput = OperationOutput<CreateClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateClusterOutputError>
}

public struct CreateClusterInput: Equatable {
    /// <p>The short name of one or more capacity providers to associate with the cluster. A
    /// 			capacity provider must be associated with a cluster before it can be included as part of
    /// 			the default capacity provider strategy of the cluster or used in a capacity provider
    /// 			strategy when calling the <a>CreateService</a> or <a>RunTask</a>
    /// 			actions.</p>
    /// 		       <p>If specifying a capacity provider that uses an Auto Scaling group, the capacity
    /// 			provider must already be created and not already associated with another cluster. New
    /// 			Auto Scaling group capacity providers can be created with the <a>CreateCapacityProvider</a> API operation.</p>
    /// 		       <p>To use a AWS Fargate capacity provider, specify either the <code>FARGATE</code> or
    /// 				<code>FARGATE_SPOT</code> capacity providers. The AWS Fargate capacity providers are
    /// 			available to all accounts and only need to be associated with a cluster to be
    /// 			used.</p>
    /// 		       <p>The <a>PutClusterCapacityProviders</a> API operation is used to update the
    /// 			list of available capacity providers for a cluster after the cluster is created.</p>
    public let capacityProviders: [String]?
    /// <p>The name of your cluster. If you do not specify a name for your cluster, you create a
    /// 			cluster named <code>default</code>. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. </p>
    public let clusterName: String?
    /// <p>The execute command configuration for the cluster.</p>
    public let configuration: ClusterConfiguration?
    /// <p>The capacity provider strategy to set as the default for the cluster. When a default
    /// 			capacity provider strategy is set for a cluster, when calling the <a>RunTask</a> or <a>CreateService</a> APIs wtih no capacity
    /// 			provider strategy or launch type specified, the default capacity provider strategy for
    /// 			the cluster is used.</p>
    /// 		       <p>If a default capacity provider strategy is not defined for a cluster during creation,
    /// 			it can be defined later with the <a>PutClusterCapacityProviders</a> API
    /// 			operation.</p>
    public let defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The setting to use when creating a cluster. This parameter is used to enable CloudWatch
    /// 			Container Insights for a cluster. If this value is specified, it will override the
    /// 				<code>containerInsights</code> value set with <a>PutAccountSetting</a> or
    /// 				<a>PutAccountSettingDefault</a>.</p>
    public let settings: [ClusterSetting]?
    /// <p>The metadata that you apply to the cluster to help you categorize and organize them.
    /// 			Each tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?

    public init (
        capacityProviders: [String]? = nil,
        clusterName: String? = nil,
        configuration: ClusterConfiguration? = nil,
        defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        settings: [ClusterSetting]? = nil,
        tags: [Tag]? = nil
    )
    {
        self.capacityProviders = capacityProviders
        self.clusterName = clusterName
        self.configuration = configuration
        self.defaultCapacityProviderStrategy = defaultCapacityProviderStrategy
        self.settings = settings
        self.tags = tags
    }
}

struct CreateClusterInputBody: Equatable {
    public let clusterName: String?
    public let tags: [Tag]?
    public let settings: [ClusterSetting]?
    public let configuration: ClusterConfiguration?
    public let capacityProviders: [String]?
    public let defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]?
}

extension CreateClusterInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case clusterName
        case configuration
        case defaultCapacityProviderStrategy
        case settings
        case tags
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterName)
        clusterName = clusterNameDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let settingsContainer = try containerValues.decodeIfPresent([ClusterSetting?].self, forKey: .settings)
        var settingsDecoded0:[ClusterSetting]? = nil
        if let settingsContainer = settingsContainer {
            settingsDecoded0 = [ClusterSetting]()
            for structure0 in settingsContainer {
                if let structure0 = structure0 {
                    settingsDecoded0?.append(structure0)
                }
            }
        }
        settings = settingsDecoded0
        let configurationDecoded = try containerValues.decodeIfPresent(ClusterConfiguration.self, forKey: .configuration)
        configuration = configurationDecoded
        let capacityProvidersContainer = try containerValues.decodeIfPresent([String?].self, forKey: .capacityProviders)
        var capacityProvidersDecoded0:[String]? = nil
        if let capacityProvidersContainer = capacityProvidersContainer {
            capacityProvidersDecoded0 = [String]()
            for string0 in capacityProvidersContainer {
                if let string0 = string0 {
                    capacityProvidersDecoded0?.append(string0)
                }
            }
        }
        capacityProviders = capacityProvidersDecoded0
        let defaultCapacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .defaultCapacityProviderStrategy)
        var defaultCapacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let defaultCapacityProviderStrategyContainer = defaultCapacityProviderStrategyContainer {
            defaultCapacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in defaultCapacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    defaultCapacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        defaultCapacityProviderStrategy = defaultCapacityProviderStrategyDecoded0
    }
}

extension CreateClusterOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension CreateClusterOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum CreateClusterOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension CreateClusterOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateClusterOutputResponse(cluster: \(String(describing: cluster)))"}
}

extension CreateClusterOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: CreateClusterOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.cluster = output.cluster
        } else {
            self.cluster = nil
        }
    }
}

public struct CreateClusterOutputResponse: Equatable {
    /// <p>The full description of your new cluster.</p>
    public let cluster: Cluster?

    public init (
        cluster: Cluster? = nil
    )
    {
        self.cluster = cluster
    }
}

struct CreateClusterOutputResponseBody: Equatable {
    public let cluster: Cluster?
}

extension CreateClusterOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(Cluster.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

public struct CreateServiceInputBodyMiddleware: Middleware {
    public let id: String = "CreateServiceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateServiceInput>
    public typealias MOutput = OperationOutput<CreateServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateServiceOutputError>
}

extension CreateServiceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateServiceInput(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), clientToken: \(String(describing: clientToken)), cluster: \(String(describing: cluster)), deploymentConfiguration: \(String(describing: deploymentConfiguration)), deploymentController: \(String(describing: deploymentController)), desiredCount: \(String(describing: desiredCount)), enableECSManagedTags: \(String(describing: enableECSManagedTags)), enableExecuteCommand: \(String(describing: enableExecuteCommand)), healthCheckGracePeriodSeconds: \(String(describing: healthCheckGracePeriodSeconds)), launchType: \(String(describing: launchType)), loadBalancers: \(String(describing: loadBalancers)), networkConfiguration: \(String(describing: networkConfiguration)), placementConstraints: \(String(describing: placementConstraints)), placementStrategy: \(String(describing: placementStrategy)), platformVersion: \(String(describing: platformVersion)), propagateTags: \(String(describing: propagateTags)), role: \(String(describing: role)), schedulingStrategy: \(String(describing: schedulingStrategy)), serviceName: \(String(describing: serviceName)), serviceRegistries: \(String(describing: serviceRegistries)), tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension CreateServiceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case clientToken
        case cluster
        case deploymentConfiguration
        case deploymentController
        case desiredCount
        case enableECSManagedTags
        case enableExecuteCommand
        case healthCheckGracePeriodSeconds
        case launchType
        case loadBalancers
        case networkConfiguration
        case placementConstraints
        case placementStrategy
        case platformVersion
        case propagateTags
        case role
        case schedulingStrategy
        case serviceName
        case serviceRegistries
        case tags
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let clientToken = clientToken {
            try encodeContainer.encode(clientToken, forKey: .clientToken)
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let deploymentConfiguration = deploymentConfiguration {
            try encodeContainer.encode(deploymentConfiguration, forKey: .deploymentConfiguration)
        }
        if let deploymentController = deploymentController {
            try encodeContainer.encode(deploymentController, forKey: .deploymentController)
        }
        if let desiredCount = desiredCount {
            try encodeContainer.encode(desiredCount, forKey: .desiredCount)
        }
        if enableECSManagedTags != false {
            try encodeContainer.encode(enableECSManagedTags, forKey: .enableECSManagedTags)
        }
        if enableExecuteCommand != false {
            try encodeContainer.encode(enableExecuteCommand, forKey: .enableExecuteCommand)
        }
        if let healthCheckGracePeriodSeconds = healthCheckGracePeriodSeconds {
            try encodeContainer.encode(healthCheckGracePeriodSeconds, forKey: .healthCheckGracePeriodSeconds)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let loadBalancers = loadBalancers {
            var loadBalancersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .loadBalancers)
            for loadbalancers0 in loadBalancers {
                try loadBalancersContainer.encode(loadbalancers0)
            }
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if let placementConstraints = placementConstraints {
            var placementConstraintsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementConstraints)
            for placementconstraints0 in placementConstraints {
                try placementConstraintsContainer.encode(placementconstraints0)
            }
        }
        if let placementStrategy = placementStrategy {
            var placementStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementStrategy)
            for placementstrategies0 in placementStrategy {
                try placementStrategyContainer.encode(placementstrategies0)
            }
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let propagateTags = propagateTags {
            try encodeContainer.encode(propagateTags.rawValue, forKey: .propagateTags)
        }
        if let role = role {
            try encodeContainer.encode(role, forKey: .role)
        }
        if let schedulingStrategy = schedulingStrategy {
            try encodeContainer.encode(schedulingStrategy.rawValue, forKey: .schedulingStrategy)
        }
        if let serviceName = serviceName {
            try encodeContainer.encode(serviceName, forKey: .serviceName)
        }
        if let serviceRegistries = serviceRegistries {
            var serviceRegistriesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .serviceRegistries)
            for serviceregistries0 in serviceRegistries {
                try serviceRegistriesContainer.encode(serviceregistries0)
            }
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct CreateServiceInputHeadersMiddleware: Middleware {
    public let id: String = "CreateServiceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateServiceInput>
    public typealias MOutput = OperationOutput<CreateServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateServiceOutputError>
}

public struct CreateServiceInputQueryItemMiddleware: Middleware {
    public let id: String = "CreateServiceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateServiceInput>
    public typealias MOutput = OperationOutput<CreateServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateServiceOutputError>
}

public struct CreateServiceInput: Equatable {
    /// <p>The capacity provider strategy to use for the service.</p>
    /// 		       <p>If a <code>capacityProviderStrategy</code> is specified, the <code>launchType</code>
    /// 			parameter must be omitted. If no <code>capacityProviderStrategy</code> or
    /// 				<code>launchType</code> is specified, the
    /// 				<code>defaultCapacityProviderStrategy</code> for the cluster is used.</p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>Unique, case-sensitive identifier that you provide to ensure the idempotency of the
    /// 			request. Up to 32 ASCII characters are allowed.</p>
    public let clientToken: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster on which to run your service.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>Optional deployment parameters that control how many tasks run during the deployment
    /// 			and the ordering of stopping and starting tasks.</p>
    public let deploymentConfiguration: DeploymentConfiguration?
    /// <p>The deployment controller to use for the service. If no deployment controller is
    /// 			specified, the default value of <code>ECS</code> is used.</p>
    public let deploymentController: DeploymentController?
    /// <p>The number of instantiations of the specified task definition to place and keep
    /// 			running on your cluster.</p>
    /// 		       <p>This is required if <code>schedulingStrategy</code> is <code>REPLICA</code> or is not
    /// 			specified. If <code>schedulingStrategy</code> is <code>DAEMON</code> then this is not
    /// 			required.</p>
    public let desiredCount: Int?
    /// <p>Specifies whether to enable Amazon ECS managed tags for the tasks within the service. For
    /// 			more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-using-tags.html">Tagging Your Amazon ECS
    /// 				Resources</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let enableECSManagedTags: Bool
    /// <p>Whether or not the execute command functionality is enabled for the service. If
    /// 				<code>true</code>, this enables execute command functionality on all containers in
    /// 			the service tasks.</p>
    public let enableExecuteCommand: Bool
    /// <p>The period of time, in seconds, that the Amazon ECS service scheduler should ignore
    /// 			unhealthy Elastic Load Balancing target health checks after a task has first started. This is only used
    /// 			when your service is configured to use a load balancer. If your service has a load
    /// 			balancer defined and you don't specify a health check grace period value, the default
    /// 			value of <code>0</code> is used.</p>
    /// 		       <p>If your service's tasks take a while to start and respond to Elastic Load Balancing health checks, you
    /// 			can specify a health check grace period of up to 2,147,483,647 seconds. During that
    /// 			time, the Amazon ECS service scheduler ignores health check status. This grace period can
    /// 			prevent the service scheduler from marking tasks as unhealthy and stopping them before
    /// 			they have time to come up.</p>
    public let healthCheckGracePeriodSeconds: Int?
    /// <p>The infrastructure on which to run your service. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS
    /// 				launch types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>The <code>FARGATE</code> launch type runs your tasks on AWS Fargate On-Demand
    /// 			infrastructure.</p>
    /// 		       <note>
    /// 			         <p>Fargate Spot infrastructure is available for use but a capacity provider
    /// 				strategy must be used. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/userguide/fargate-capacity-providers.html">AWS Fargate capacity providers</a> in the
    /// 					<i>Amazon ECS User Guide for AWS Fargate</i>.</p>
    /// 		       </note>
    /// 		       <p>The <code>EC2</code> launch type runs your tasks on Amazon EC2 instances registered to your
    /// 			cluster.</p>
    /// 		       <p>The <code>EXTERNAL</code> launch type runs your tasks on your on-premise server or
    /// 			virtual machine (VM) capacity registered to your cluster.</p>
    /// 		       <p>A service can use either a launch type or a capacity provider strategy. If a
    /// 				<code>launchType</code> is specified, the <code>capacityProviderStrategy</code>
    /// 			parameter must be omitted.</p>
    public let launchType: LaunchType?
    /// <p>A load balancer object representing the load balancers to use with your service. For
    /// 			more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-load-balancing.html">Service Load Balancing</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>If the service is using the rolling update (<code>ECS</code>) deployment controller
    /// 			and using either an Application Load Balancer or Network Load Balancer, you must specify one or more target group ARNs to
    /// 			attach to the service. The service-linked role is required for services that make use of
    /// 			multiple target groups. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using-service-linked-roles.html">Using service-linked roles for Amazon ECS</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>If the service is using the <code>CODE_DEPLOY</code> deployment controller, the
    /// 			service is required to use either an Application Load Balancer or Network Load Balancer. When creating an AWS CodeDeploy deployment
    /// 			group, you specify two target groups (referred to as a <code>targetGroupPair</code>).
    /// 			During a deployment, AWS CodeDeploy determines which task set in your service has the status
    /// 				<code>PRIMARY</code> and associates one target group with it, and then associates
    /// 			the other target group with the replacement task set. The load balancer can also have up
    /// 			to two listeners: a required listener for production traffic and an optional listener
    /// 			that allows you perform validation tests with Lambda functions before routing production
    /// 			traffic to it.</p>
    /// 		       <p>After you create a service using the <code>ECS</code> deployment controller, the load
    /// 			balancer name or target group ARN, container name, and container port specified in the
    /// 			service definition are immutable. If you are using the <code>CODE_DEPLOY</code>
    /// 			deployment controller, these values can be changed when updating the service.</p>
    /// 		       <p>For Application Load Balancers and Network Load Balancers, this object must contain the load balancer target group ARN,
    /// 			the container name (as it appears in a container definition), and the container port to
    /// 			access from the load balancer. The load balancer name parameter must be omitted. When a
    /// 			task from this service is placed on a container instance, the container instance and
    /// 			port combination is registered as a target in the target group specified here.</p>
    /// 		       <p>For Classic Load Balancers, this object must contain the load balancer name, the container name (as it
    /// 			appears in a container definition), and the container port to access from the load
    /// 			balancer. The target group ARN parameter must be omitted. When a task from this service
    /// 			is placed on a container instance, the container instance is registered with the load
    /// 			balancer specified here.</p>
    /// 		       <p>Services with tasks that use the <code>awsvpc</code> network mode (for example, those
    /// 			with the Fargate launch type) only support Application Load Balancers and Network Load Balancers. Classic Load Balancers are
    /// 			not supported. Also, when you create any target groups for these services, you must
    /// 			choose <code>ip</code> as the target type, not <code>instance</code>, because tasks that
    /// 			use the <code>awsvpc</code> network mode are associated with an elastic network
    /// 			interface, not an Amazon EC2 instance.</p>
    public let loadBalancers: [LoadBalancer]?
    /// <p>The network configuration for the service. This parameter is required for task
    /// 			definitions that use the <code>awsvpc</code> network mode to receive their own elastic
    /// 			network interface, and it is not supported for other network modes. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-networking.html">Task networking</a>
    /// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>An array of placement constraint objects to use for tasks in your service. You can
    /// 			specify a maximum of 10 constraints per task (this limit includes constraints in the
    /// 			task definition and those specified at runtime). </p>
    public let placementConstraints: [PlacementConstraint]?
    /// <p>The placement strategy objects to use for tasks in your service. You can specify a
    /// 			maximum of five strategy rules per service.</p>
    public let placementStrategy: [PlacementStrategy]?
    /// <p>The platform version that your tasks in the service are running on. A platform version
    /// 			is specified only for tasks using the Fargate launch type. If one isn't
    /// 			specified, the <code>LATEST</code> platform version is used by default. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate platform
    /// 				versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <p>Specifies whether to propagate the tags from the task definition or the service to the
    /// 			tasks in the service. If no value is specified, the tags are not propagated. Tags can
    /// 			only be propagated to the tasks within the service during service creation. To add tags
    /// 			to a task after service creation, use the <a>TagResource</a> API
    /// 			action.</p>
    public let propagateTags: PropagateTags?
    /// <p>The name or full Amazon Resource Name (ARN) of the IAM role that allows Amazon ECS to make calls to your
    /// 			load balancer on your behalf. This parameter is only permitted if you are using a load
    /// 			balancer with your service and your task definition does not use the <code>awsvpc</code>
    /// 			network mode. If you specify the <code>role</code> parameter, you must also specify a
    /// 			load balancer object with the <code>loadBalancers</code> parameter.</p>
    /// 		       <important>
    /// 			         <p>If your account has already created the Amazon ECS service-linked role, that role is
    /// 				used by default for your service unless you specify a role here. The service-linked
    /// 				role is required if your task definition uses the <code>awsvpc</code> network mode
    /// 				or if the service is configured to use service discovery, an external deployment
    /// 				controller, multiple target groups, or Elastic Inference accelerators in which case
    /// 				you should not specify a role here. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using-service-linked-roles.html">Using
    /// 					service-linked roles for Amazon ECS</a> in the
    /// 					<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       </important>
    /// 		       <p>If your specified role has a path other than <code>/</code>, then you must either
    /// 			specify the full role ARN (this is recommended) or prefix the role name with the path.
    /// 			For example, if a role with the name <code>bar</code> has a path of <code>/foo/</code>
    /// 			then you would specify <code>/foo/bar</code> as the role name. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_identifiers.html#identifiers-friendly-names">Friendly names and paths</a> in the <i>IAM User Guide</i>.</p>
    public let role: String?
    /// <p>The scheduling strategy to use for the service. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs_services.html">Services</a>.</p>
    /// 		       <p>There are two service scheduler strategies available:</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    ///                   <code>REPLICA</code>-The replica scheduling strategy places and
    /// 					maintains the desired number of tasks across your cluster. By default, the
    /// 					service scheduler spreads tasks across Availability Zones. You can use task
    /// 					placement strategies and constraints to customize task placement decisions. This
    /// 					scheduler strategy is required if the service is using the
    /// 						<code>CODE_DEPLOY</code> or <code>EXTERNAL</code> deployment controller
    /// 					types.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>DAEMON</code>-The daemon scheduling strategy deploys exactly one
    /// 					task on each active container instance that meets all of the task placement
    /// 					constraints that you specify in your cluster. The service scheduler also
    /// 					evaluates the task placement constraints for running tasks and will stop tasks
    /// 					that do not meet the placement constraints. When you're using this strategy, you
    /// 					don't need to specify a desired number of tasks, a task placement strategy, or
    /// 					use Service Auto Scaling policies.</p>
    /// 				           <note>
    /// 					             <p>Tasks using the Fargate launch type or the
    /// 							<code>CODE_DEPLOY</code> or <code>EXTERNAL</code> deployment controller
    /// 						types don't support the <code>DAEMON</code> scheduling strategy.</p>
    /// 				           </note>
    /// 			         </li>
    ///          </ul>
    public let schedulingStrategy: SchedulingStrategy?
    /// <p>The name of your service. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. Service names must be unique within
    /// 			a cluster, but you can have similarly named services in multiple clusters within a
    /// 			Region or across multiple Regions.</p>
    public let serviceName: String?
    /// <p>The details of the service discovery registry to associate with this service. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-discovery.html">Service
    /// 				discovery</a>.</p>
    /// 		       <note>
    /// 			         <p>Each service may be associated with one service registry. Multiple service
    /// 				registries per service isn't supported.</p>
    /// 		       </note>
    public let serviceRegistries: [ServiceRegistry]?
    /// <p>The metadata that you apply to the service to help you categorize and organize them.
    /// 			Each tag consists of a key and an optional value, both of which you define. When a
    /// 			service is deleted, the tags are deleted as well.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The <code>family</code> and <code>revision</code> (<code>family:revision</code>) or
    /// 			full ARN of the task definition to run in your service. If a <code>revision</code> is
    /// 			not specified, the latest <code>ACTIVE</code> revision is used.</p>
    /// 		       <p>A task definition must be specified if the service is using either the
    /// 				<code>ECS</code> or <code>CODE_DEPLOY</code> deployment controllers.</p>
    public let taskDefinition: String?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        clientToken: String? = nil,
        cluster: String? = nil,
        deploymentConfiguration: DeploymentConfiguration? = nil,
        deploymentController: DeploymentController? = nil,
        desiredCount: Int? = nil,
        enableECSManagedTags: Bool = false,
        enableExecuteCommand: Bool = false,
        healthCheckGracePeriodSeconds: Int? = nil,
        launchType: LaunchType? = nil,
        loadBalancers: [LoadBalancer]? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        placementConstraints: [PlacementConstraint]? = nil,
        placementStrategy: [PlacementStrategy]? = nil,
        platformVersion: String? = nil,
        propagateTags: PropagateTags? = nil,
        role: String? = nil,
        schedulingStrategy: SchedulingStrategy? = nil,
        serviceName: String? = nil,
        serviceRegistries: [ServiceRegistry]? = nil,
        tags: [Tag]? = nil,
        taskDefinition: String? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.clientToken = clientToken
        self.cluster = cluster
        self.deploymentConfiguration = deploymentConfiguration
        self.deploymentController = deploymentController
        self.desiredCount = desiredCount
        self.enableECSManagedTags = enableECSManagedTags
        self.enableExecuteCommand = enableExecuteCommand
        self.healthCheckGracePeriodSeconds = healthCheckGracePeriodSeconds
        self.launchType = launchType
        self.loadBalancers = loadBalancers
        self.networkConfiguration = networkConfiguration
        self.placementConstraints = placementConstraints
        self.placementStrategy = placementStrategy
        self.platformVersion = platformVersion
        self.propagateTags = propagateTags
        self.role = role
        self.schedulingStrategy = schedulingStrategy
        self.serviceName = serviceName
        self.serviceRegistries = serviceRegistries
        self.tags = tags
        self.taskDefinition = taskDefinition
    }
}

struct CreateServiceInputBody: Equatable {
    public let cluster: String?
    public let serviceName: String?
    public let taskDefinition: String?
    public let loadBalancers: [LoadBalancer]?
    public let serviceRegistries: [ServiceRegistry]?
    public let desiredCount: Int?
    public let clientToken: String?
    public let launchType: LaunchType?
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    public let platformVersion: String?
    public let role: String?
    public let deploymentConfiguration: DeploymentConfiguration?
    public let placementConstraints: [PlacementConstraint]?
    public let placementStrategy: [PlacementStrategy]?
    public let networkConfiguration: NetworkConfiguration?
    public let healthCheckGracePeriodSeconds: Int?
    public let schedulingStrategy: SchedulingStrategy?
    public let deploymentController: DeploymentController?
    public let tags: [Tag]?
    public let enableECSManagedTags: Bool
    public let propagateTags: PropagateTags?
    public let enableExecuteCommand: Bool
}

extension CreateServiceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case clientToken
        case cluster
        case deploymentConfiguration
        case deploymentController
        case desiredCount
        case enableECSManagedTags
        case enableExecuteCommand
        case healthCheckGracePeriodSeconds
        case launchType
        case loadBalancers
        case networkConfiguration
        case placementConstraints
        case placementStrategy
        case platformVersion
        case propagateTags
        case role
        case schedulingStrategy
        case serviceName
        case serviceRegistries
        case tags
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceName)
        serviceName = serviceNameDecoded
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let loadBalancersContainer = try containerValues.decodeIfPresent([LoadBalancer?].self, forKey: .loadBalancers)
        var loadBalancersDecoded0:[LoadBalancer]? = nil
        if let loadBalancersContainer = loadBalancersContainer {
            loadBalancersDecoded0 = [LoadBalancer]()
            for structure0 in loadBalancersContainer {
                if let structure0 = structure0 {
                    loadBalancersDecoded0?.append(structure0)
                }
            }
        }
        loadBalancers = loadBalancersDecoded0
        let serviceRegistriesContainer = try containerValues.decodeIfPresent([ServiceRegistry?].self, forKey: .serviceRegistries)
        var serviceRegistriesDecoded0:[ServiceRegistry]? = nil
        if let serviceRegistriesContainer = serviceRegistriesContainer {
            serviceRegistriesDecoded0 = [ServiceRegistry]()
            for structure0 in serviceRegistriesContainer {
                if let structure0 = structure0 {
                    serviceRegistriesDecoded0?.append(structure0)
                }
            }
        }
        serviceRegistries = serviceRegistriesDecoded0
        let desiredCountDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .desiredCount)
        desiredCount = desiredCountDecoded
        let clientTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clientToken)
        clientToken = clientTokenDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let roleDecoded = try containerValues.decodeIfPresent(String.self, forKey: .role)
        role = roleDecoded
        let deploymentConfigurationDecoded = try containerValues.decodeIfPresent(DeploymentConfiguration.self, forKey: .deploymentConfiguration)
        deploymentConfiguration = deploymentConfigurationDecoded
        let placementConstraintsContainer = try containerValues.decodeIfPresent([PlacementConstraint?].self, forKey: .placementConstraints)
        var placementConstraintsDecoded0:[PlacementConstraint]? = nil
        if let placementConstraintsContainer = placementConstraintsContainer {
            placementConstraintsDecoded0 = [PlacementConstraint]()
            for structure0 in placementConstraintsContainer {
                if let structure0 = structure0 {
                    placementConstraintsDecoded0?.append(structure0)
                }
            }
        }
        placementConstraints = placementConstraintsDecoded0
        let placementStrategyContainer = try containerValues.decodeIfPresent([PlacementStrategy?].self, forKey: .placementStrategy)
        var placementStrategyDecoded0:[PlacementStrategy]? = nil
        if let placementStrategyContainer = placementStrategyContainer {
            placementStrategyDecoded0 = [PlacementStrategy]()
            for structure0 in placementStrategyContainer {
                if let structure0 = structure0 {
                    placementStrategyDecoded0?.append(structure0)
                }
            }
        }
        placementStrategy = placementStrategyDecoded0
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let healthCheckGracePeriodSecondsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .healthCheckGracePeriodSeconds)
        healthCheckGracePeriodSeconds = healthCheckGracePeriodSecondsDecoded
        let schedulingStrategyDecoded = try containerValues.decodeIfPresent(SchedulingStrategy.self, forKey: .schedulingStrategy)
        schedulingStrategy = schedulingStrategyDecoded
        let deploymentControllerDecoded = try containerValues.decodeIfPresent(DeploymentController.self, forKey: .deploymentController)
        deploymentController = deploymentControllerDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let enableECSManagedTagsDecoded = try containerValues.decode(Bool.self, forKey: .enableECSManagedTags)
        enableECSManagedTags = enableECSManagedTagsDecoded
        let propagateTagsDecoded = try containerValues.decodeIfPresent(PropagateTags.self, forKey: .propagateTags)
        propagateTags = propagateTagsDecoded
        let enableExecuteCommandDecoded = try containerValues.decode(Bool.self, forKey: .enableExecuteCommand)
        enableExecuteCommand = enableExecuteCommandDecoded
    }
}

extension CreateServiceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension CreateServiceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformTaskDefinitionIncompatibilityException" : self = .platformTaskDefinitionIncompatibilityException(try PlatformTaskDefinitionIncompatibilityException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformUnknownException" : self = .platformUnknownException(try PlatformUnknownException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum CreateServiceOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case platformTaskDefinitionIncompatibilityException(PlatformTaskDefinitionIncompatibilityException)
    case platformUnknownException(PlatformUnknownException)
    case serverException(ServerException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension CreateServiceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateServiceOutputResponse(service: \(String(describing: service)))"}
}

extension CreateServiceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: CreateServiceOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.service = output.service
        } else {
            self.service = nil
        }
    }
}

public struct CreateServiceOutputResponse: Equatable {
    /// <p>The full description of your service following the create call.</p>
    /// 		       <p>If a service is using the <code>ECS</code> deployment controller, the
    /// 				<code>deploymentController</code> and <code>taskSets</code> parameters will not be
    /// 			returned.</p>
    /// 		       <p>If the service is using the <code>CODE_DEPLOY</code> deployment controller, the
    /// 				<code>deploymentController</code>, <code>taskSets</code> and
    /// 				<code>deployments</code> parameters will be returned, however the
    /// 				<code>deployments</code> parameter will be an empty list.</p>
    public let service: Service?

    public init (
        service: Service? = nil
    )
    {
        self.service = service
    }
}

struct CreateServiceOutputResponseBody: Equatable {
    public let service: Service?
}

extension CreateServiceOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case service
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let serviceDecoded = try containerValues.decodeIfPresent(Service.self, forKey: .service)
        service = serviceDecoded
    }
}

public struct CreateTaskSetInputBodyMiddleware: Middleware {
    public let id: String = "CreateTaskSetInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateTaskSetInput>
    public typealias MOutput = OperationOutput<CreateTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateTaskSetOutputError>
}

extension CreateTaskSetInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateTaskSetInput(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), clientToken: \(String(describing: clientToken)), cluster: \(String(describing: cluster)), externalId: \(String(describing: externalId)), launchType: \(String(describing: launchType)), loadBalancers: \(String(describing: loadBalancers)), networkConfiguration: \(String(describing: networkConfiguration)), platformVersion: \(String(describing: platformVersion)), scale: \(String(describing: scale)), service: \(String(describing: service)), serviceRegistries: \(String(describing: serviceRegistries)), tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension CreateTaskSetInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case clientToken
        case cluster
        case externalId
        case launchType
        case loadBalancers
        case networkConfiguration
        case platformVersion
        case scale
        case service
        case serviceRegistries
        case tags
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let clientToken = clientToken {
            try encodeContainer.encode(clientToken, forKey: .clientToken)
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let externalId = externalId {
            try encodeContainer.encode(externalId, forKey: .externalId)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let loadBalancers = loadBalancers {
            var loadBalancersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .loadBalancers)
            for loadbalancers0 in loadBalancers {
                try loadBalancersContainer.encode(loadbalancers0)
            }
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let scale = scale {
            try encodeContainer.encode(scale, forKey: .scale)
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
        if let serviceRegistries = serviceRegistries {
            var serviceRegistriesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .serviceRegistries)
            for serviceregistries0 in serviceRegistries {
                try serviceRegistriesContainer.encode(serviceregistries0)
            }
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct CreateTaskSetInputHeadersMiddleware: Middleware {
    public let id: String = "CreateTaskSetInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateTaskSetInput>
    public typealias MOutput = OperationOutput<CreateTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateTaskSetOutputError>
}

public struct CreateTaskSetInputQueryItemMiddleware: Middleware {
    public let id: String = "CreateTaskSetInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<CreateTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<CreateTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<CreateTaskSetInput>
    public typealias MOutput = OperationOutput<CreateTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<CreateTaskSetOutputError>
}

public struct CreateTaskSetInput: Equatable {
    /// <p>The capacity provider strategy to use for the task set.</p>
    /// 		       <p>A capacity provider strategy consists of one or more capacity providers along with the
    /// 				<code>base</code> and <code>weight</code> to assign to them. A capacity provider
    /// 			must be associated with the cluster to be used in a capacity provider strategy. The
    /// 				<a>PutClusterCapacityProviders</a> API is used to associate a capacity
    /// 			provider with a cluster. Only capacity providers with an <code>ACTIVE</code> or
    /// 				<code>UPDATING</code> status can be used.</p>
    /// 		       <p>If a <code>capacityProviderStrategy</code> is specified, the <code>launchType</code>
    /// 			parameter must be omitted. If no <code>capacityProviderStrategy</code> or
    /// 				<code>launchType</code> is specified, the
    /// 				<code>defaultCapacityProviderStrategy</code> for the cluster is used.</p>
    /// 		       <p>If specifying a capacity provider that uses an Auto Scaling group, the capacity
    /// 			provider must already be created. New capacity providers can be created with the <a>CreateCapacityProvider</a> API operation.</p>
    /// 		       <p>To use a AWS Fargate capacity provider, specify either the <code>FARGATE</code> or
    /// 				<code>FARGATE_SPOT</code> capacity providers. The AWS Fargate capacity providers are
    /// 			available to all accounts and only need to be associated with a cluster to be
    /// 			used.</p>
    /// 		       <p>The <a>PutClusterCapacityProviders</a> API operation is used to update the
    /// 			list of available capacity providers for a cluster after the cluster is created.</p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>Unique, case-sensitive identifier that you provide to ensure the idempotency of the
    /// 			request. Up to 32 ASCII characters are allowed.</p>
    public let clientToken: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to create the
    /// 			task set in.</p>
    public let cluster: String?
    /// <p>An optional non-unique tag that identifies this task set in external systems. If the
    /// 			task set is associated with a service discovery registry, the tasks in this task set
    /// 			will have the <code>ECS_TASK_SET_EXTERNAL_ID</code> AWS Cloud Map attribute set to the provided
    /// 			value.</p>
    public let externalId: String?
    /// <p>The launch type that new tasks in the task set will use. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS Launch Types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>If a <code>launchType</code> is specified, the <code>capacityProviderStrategy</code>
    /// 			parameter must be omitted.</p>
    public let launchType: LaunchType?
    /// <p>A load balancer object representing the load balancer to use with the task set. The
    /// 			supported load balancer types are either an Application Load Balancer or a Network Load Balancer.</p>
    public let loadBalancers: [LoadBalancer]?
    /// <p>An object representing the network configuration for a task set.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>The platform version that the tasks in the task set should use. A platform version is
    /// 			specified only for tasks using the Fargate launch type. If one isn't
    /// 			specified, the <code>LATEST</code> platform version is used by default.</p>
    public let platformVersion: String?
    /// <p>A floating-point percentage of the desired number of tasks to place and keep running
    /// 			in the task set.</p>
    public let scale: Scale?
    /// <p>The short name or full Amazon Resource Name (ARN) of the service to create the task set in.</p>
    public let service: String?
    /// <p>The details of the service discovery registries to assign to this task set. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-discovery.html">Service
    /// 				Discovery</a>.</p>
    public let serviceRegistries: [ServiceRegistry]?
    /// <p>The metadata that you apply to the task set to help you categorize and organize them.
    /// 			Each tag consists of a key and an optional value, both of which you define. When a
    /// 			service is deleted, the tags are deleted as well.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The task definition for the tasks in the task set to use.</p>
    public let taskDefinition: String?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        clientToken: String? = nil,
        cluster: String? = nil,
        externalId: String? = nil,
        launchType: LaunchType? = nil,
        loadBalancers: [LoadBalancer]? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        platformVersion: String? = nil,
        scale: Scale? = nil,
        service: String? = nil,
        serviceRegistries: [ServiceRegistry]? = nil,
        tags: [Tag]? = nil,
        taskDefinition: String? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.clientToken = clientToken
        self.cluster = cluster
        self.externalId = externalId
        self.launchType = launchType
        self.loadBalancers = loadBalancers
        self.networkConfiguration = networkConfiguration
        self.platformVersion = platformVersion
        self.scale = scale
        self.service = service
        self.serviceRegistries = serviceRegistries
        self.tags = tags
        self.taskDefinition = taskDefinition
    }
}

struct CreateTaskSetInputBody: Equatable {
    public let service: String?
    public let cluster: String?
    public let externalId: String?
    public let taskDefinition: String?
    public let networkConfiguration: NetworkConfiguration?
    public let loadBalancers: [LoadBalancer]?
    public let serviceRegistries: [ServiceRegistry]?
    public let launchType: LaunchType?
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    public let platformVersion: String?
    public let scale: Scale?
    public let clientToken: String?
    public let tags: [Tag]?
}

extension CreateTaskSetInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case clientToken
        case cluster
        case externalId
        case launchType
        case loadBalancers
        case networkConfiguration
        case platformVersion
        case scale
        case service
        case serviceRegistries
        case tags
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let externalIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .externalId)
        externalId = externalIdDecoded
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let loadBalancersContainer = try containerValues.decodeIfPresent([LoadBalancer?].self, forKey: .loadBalancers)
        var loadBalancersDecoded0:[LoadBalancer]? = nil
        if let loadBalancersContainer = loadBalancersContainer {
            loadBalancersDecoded0 = [LoadBalancer]()
            for structure0 in loadBalancersContainer {
                if let structure0 = structure0 {
                    loadBalancersDecoded0?.append(structure0)
                }
            }
        }
        loadBalancers = loadBalancersDecoded0
        let serviceRegistriesContainer = try containerValues.decodeIfPresent([ServiceRegistry?].self, forKey: .serviceRegistries)
        var serviceRegistriesDecoded0:[ServiceRegistry]? = nil
        if let serviceRegistriesContainer = serviceRegistriesContainer {
            serviceRegistriesDecoded0 = [ServiceRegistry]()
            for structure0 in serviceRegistriesContainer {
                if let structure0 = structure0 {
                    serviceRegistriesDecoded0?.append(structure0)
                }
            }
        }
        serviceRegistries = serviceRegistriesDecoded0
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let scaleDecoded = try containerValues.decodeIfPresent(Scale.self, forKey: .scale)
        scale = scaleDecoded
        let clientTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clientToken)
        clientToken = clientTokenDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension CreateTaskSetOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension CreateTaskSetOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformTaskDefinitionIncompatibilityException" : self = .platformTaskDefinitionIncompatibilityException(try PlatformTaskDefinitionIncompatibilityException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformUnknownException" : self = .platformUnknownException(try PlatformUnknownException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotActiveException" : self = .serviceNotActiveException(try ServiceNotActiveException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum CreateTaskSetOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case platformTaskDefinitionIncompatibilityException(PlatformTaskDefinitionIncompatibilityException)
    case platformUnknownException(PlatformUnknownException)
    case serverException(ServerException)
    case serviceNotActiveException(ServiceNotActiveException)
    case serviceNotFoundException(ServiceNotFoundException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension CreateTaskSetOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "CreateTaskSetOutputResponse(taskSet: \(String(describing: taskSet)))"}
}

extension CreateTaskSetOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: CreateTaskSetOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.taskSet = output.taskSet
        } else {
            self.taskSet = nil
        }
    }
}

public struct CreateTaskSetOutputResponse: Equatable {
    /// <p>Information about a set of Amazon ECS tasks in either an AWS CodeDeploy or an
    /// 				<code>EXTERNAL</code> deployment. A task set includes details such as the desired
    /// 			number of tasks, how many tasks are running, and whether the task set serves production
    /// 			traffic.</p>
    public let taskSet: TaskSet?

    public init (
        taskSet: TaskSet? = nil
    )
    {
        self.taskSet = taskSet
    }
}

struct CreateTaskSetOutputResponseBody: Equatable {
    public let taskSet: TaskSet?
}

extension CreateTaskSetOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case taskSet
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskSetDecoded = try containerValues.decodeIfPresent(TaskSet.self, forKey: .taskSet)
        taskSet = taskSetDecoded
    }
}

public struct DeleteAccountSettingInputBodyMiddleware: Middleware {
    public let id: String = "DeleteAccountSettingInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteAccountSettingInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteAccountSettingOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteAccountSettingInput>
    public typealias MOutput = OperationOutput<DeleteAccountSettingOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteAccountSettingOutputError>
}

extension DeleteAccountSettingInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteAccountSettingInput(name: \(String(describing: name)), principalArn: \(String(describing: principalArn)))"}
}

extension DeleteAccountSettingInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case principalArn
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let principalArn = principalArn {
            try encodeContainer.encode(principalArn, forKey: .principalArn)
        }
    }
}

public struct DeleteAccountSettingInputHeadersMiddleware: Middleware {
    public let id: String = "DeleteAccountSettingInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteAccountSettingInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteAccountSettingOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteAccountSettingInput>
    public typealias MOutput = OperationOutput<DeleteAccountSettingOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteAccountSettingOutputError>
}

public struct DeleteAccountSettingInputQueryItemMiddleware: Middleware {
    public let id: String = "DeleteAccountSettingInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteAccountSettingInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteAccountSettingOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteAccountSettingInput>
    public typealias MOutput = OperationOutput<DeleteAccountSettingOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteAccountSettingOutputError>
}

public struct DeleteAccountSettingInput: Equatable {
    /// <p>The resource name for which to disable the account setting. If
    /// 				<code>serviceLongArnFormat</code> is specified, the ARN for your Amazon ECS services is
    /// 			affected. If <code>taskLongArnFormat</code> is specified, the ARN and resource ID for
    /// 			your Amazon ECS tasks is affected. If <code>containerInstanceLongArnFormat</code> is
    /// 			specified, the ARN and resource ID for your Amazon ECS container instances is affected. If
    /// 				<code>awsvpcTrunking</code> is specified, the ENI limit for your Amazon ECS container
    /// 			instances is affected.</p>
    public let name: SettingName?
    /// <p>The ARN of the principal, which can be an IAM user, IAM role, or the root user. If you
    /// 			specify the root user, it disables the account setting for all IAM users, IAM roles, and
    /// 			the root user of the account unless an IAM user or role explicitly overrides these
    /// 			settings. If this field is omitted, the setting is changed only for the authenticated
    /// 			user.</p>
    public let principalArn: String?

    public init (
        name: SettingName? = nil,
        principalArn: String? = nil
    )
    {
        self.name = name
        self.principalArn = principalArn
    }
}

struct DeleteAccountSettingInputBody: Equatable {
    public let name: SettingName?
    public let principalArn: String?
}

extension DeleteAccountSettingInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case name
        case principalArn
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(SettingName.self, forKey: .name)
        name = nameDecoded
        let principalArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .principalArn)
        principalArn = principalArnDecoded
    }
}

extension DeleteAccountSettingOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeleteAccountSettingOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeleteAccountSettingOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeleteAccountSettingOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteAccountSettingOutputResponse(setting: \(String(describing: setting)))"}
}

extension DeleteAccountSettingOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeleteAccountSettingOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.setting = output.setting
        } else {
            self.setting = nil
        }
    }
}

public struct DeleteAccountSettingOutputResponse: Equatable {
    /// <p>The account setting for the specified principal ARN.</p>
    public let setting: Setting?

    public init (
        setting: Setting? = nil
    )
    {
        self.setting = setting
    }
}

struct DeleteAccountSettingOutputResponseBody: Equatable {
    public let setting: Setting?
}

extension DeleteAccountSettingOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case setting
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let settingDecoded = try containerValues.decodeIfPresent(Setting.self, forKey: .setting)
        setting = settingDecoded
    }
}

public struct DeleteAttributesInputBodyMiddleware: Middleware {
    public let id: String = "DeleteAttributesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteAttributesInput>
    public typealias MOutput = OperationOutput<DeleteAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteAttributesOutputError>
}

extension DeleteAttributesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteAttributesInput(attributes: \(String(describing: attributes)), cluster: \(String(describing: cluster)))"}
}

extension DeleteAttributesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attributes
        case cluster
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attributes = attributes {
            var attributesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attributes)
            for attributes0 in attributes {
                try attributesContainer.encode(attributes0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
    }
}

public struct DeleteAttributesInputHeadersMiddleware: Middleware {
    public let id: String = "DeleteAttributesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteAttributesInput>
    public typealias MOutput = OperationOutput<DeleteAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteAttributesOutputError>
}

public struct DeleteAttributesInputQueryItemMiddleware: Middleware {
    public let id: String = "DeleteAttributesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteAttributesInput>
    public typealias MOutput = OperationOutput<DeleteAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteAttributesOutputError>
}

public struct DeleteAttributesInput: Equatable {
    /// <p>The attributes to delete from your resource. You can specify up to 10 attributes per
    /// 			request. For custom attributes, specify the attribute name and target ID, but do not
    /// 			specify the value. If you specify the target ID using the short form, you must also
    /// 			specify the target type.</p>
    public let attributes: [Attribute]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that contains the resource to delete
    /// 			attributes. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?

    public init (
        attributes: [Attribute]? = nil,
        cluster: String? = nil
    )
    {
        self.attributes = attributes
        self.cluster = cluster
    }
}

struct DeleteAttributesInputBody: Equatable {
    public let cluster: String?
    public let attributes: [Attribute]?
}

extension DeleteAttributesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributes
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
    }
}

extension DeleteAttributesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeleteAttributesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "TargetNotFoundException" : self = .targetNotFoundException(try TargetNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeleteAttributesOutputError: Swift.Error, Equatable {
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case targetNotFoundException(TargetNotFoundException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeleteAttributesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteAttributesOutputResponse(attributes: \(String(describing: attributes)))"}
}

extension DeleteAttributesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeleteAttributesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.attributes = output.attributes
        } else {
            self.attributes = nil
        }
    }
}

public struct DeleteAttributesOutputResponse: Equatable {
    /// <p>A list of attribute objects that were successfully deleted from your resource.</p>
    public let attributes: [Attribute]?

    public init (
        attributes: [Attribute]? = nil
    )
    {
        self.attributes = attributes
    }
}

struct DeleteAttributesOutputResponseBody: Equatable {
    public let attributes: [Attribute]?
}

extension DeleteAttributesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributes
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
    }
}

public struct DeleteCapacityProviderInputBodyMiddleware: Middleware {
    public let id: String = "DeleteCapacityProviderInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteCapacityProviderInput>
    public typealias MOutput = OperationOutput<DeleteCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteCapacityProviderOutputError>
}

extension DeleteCapacityProviderInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteCapacityProviderInput(capacityProvider: \(String(describing: capacityProvider)))"}
}

extension DeleteCapacityProviderInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProvider
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProvider = capacityProvider {
            try encodeContainer.encode(capacityProvider, forKey: .capacityProvider)
        }
    }
}

public struct DeleteCapacityProviderInputHeadersMiddleware: Middleware {
    public let id: String = "DeleteCapacityProviderInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteCapacityProviderInput>
    public typealias MOutput = OperationOutput<DeleteCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteCapacityProviderOutputError>
}

public struct DeleteCapacityProviderInputQueryItemMiddleware: Middleware {
    public let id: String = "DeleteCapacityProviderInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteCapacityProviderInput>
    public typealias MOutput = OperationOutput<DeleteCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteCapacityProviderOutputError>
}

public struct DeleteCapacityProviderInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the capacity provider to delete.</p>
    public let capacityProvider: String?

    public init (
        capacityProvider: String? = nil
    )
    {
        self.capacityProvider = capacityProvider
    }
}

struct DeleteCapacityProviderInputBody: Equatable {
    public let capacityProvider: String?
}

extension DeleteCapacityProviderInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProvider
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderDecoded = try containerValues.decodeIfPresent(String.self, forKey: .capacityProvider)
        capacityProvider = capacityProviderDecoded
    }
}

extension DeleteCapacityProviderOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeleteCapacityProviderOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeleteCapacityProviderOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeleteCapacityProviderOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteCapacityProviderOutputResponse(capacityProvider: \(String(describing: capacityProvider)))"}
}

extension DeleteCapacityProviderOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeleteCapacityProviderOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.capacityProvider = output.capacityProvider
        } else {
            self.capacityProvider = nil
        }
    }
}

public struct DeleteCapacityProviderOutputResponse: Equatable {
    /// <p>The details of the capacity provider.</p>
    public let capacityProvider: CapacityProvider?

    public init (
        capacityProvider: CapacityProvider? = nil
    )
    {
        self.capacityProvider = capacityProvider
    }
}

struct DeleteCapacityProviderOutputResponseBody: Equatable {
    public let capacityProvider: CapacityProvider?
}

extension DeleteCapacityProviderOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProvider
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderDecoded = try containerValues.decodeIfPresent(CapacityProvider.self, forKey: .capacityProvider)
        capacityProvider = capacityProviderDecoded
    }
}

public struct DeleteClusterInputBodyMiddleware: Middleware {
    public let id: String = "DeleteClusterInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteClusterInput>
    public typealias MOutput = OperationOutput<DeleteClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteClusterOutputError>
}

extension DeleteClusterInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteClusterInput(cluster: \(String(describing: cluster)))"}
}

extension DeleteClusterInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
    }
}

public struct DeleteClusterInputHeadersMiddleware: Middleware {
    public let id: String = "DeleteClusterInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteClusterInput>
    public typealias MOutput = OperationOutput<DeleteClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteClusterOutputError>
}

public struct DeleteClusterInputQueryItemMiddleware: Middleware {
    public let id: String = "DeleteClusterInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteClusterInput>
    public typealias MOutput = OperationOutput<DeleteClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteClusterOutputError>
}

public struct DeleteClusterInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster to delete.</p>
    public let cluster: String?

    public init (
        cluster: String? = nil
    )
    {
        self.cluster = cluster
    }
}

struct DeleteClusterInputBody: Equatable {
    public let cluster: String?
}

extension DeleteClusterInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

extension DeleteClusterOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeleteClusterOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterContainsContainerInstancesException" : self = .clusterContainsContainerInstancesException(try ClusterContainsContainerInstancesException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterContainsServicesException" : self = .clusterContainsServicesException(try ClusterContainsServicesException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterContainsTasksException" : self = .clusterContainsTasksException(try ClusterContainsTasksException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UpdateInProgressException" : self = .updateInProgressException(try UpdateInProgressException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeleteClusterOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterContainsContainerInstancesException(ClusterContainsContainerInstancesException)
    case clusterContainsServicesException(ClusterContainsServicesException)
    case clusterContainsTasksException(ClusterContainsTasksException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case updateInProgressException(UpdateInProgressException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeleteClusterOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteClusterOutputResponse(cluster: \(String(describing: cluster)))"}
}

extension DeleteClusterOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeleteClusterOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.cluster = output.cluster
        } else {
            self.cluster = nil
        }
    }
}

public struct DeleteClusterOutputResponse: Equatable {
    /// <p>The full description of the deleted cluster.</p>
    public let cluster: Cluster?

    public init (
        cluster: Cluster? = nil
    )
    {
        self.cluster = cluster
    }
}

struct DeleteClusterOutputResponseBody: Equatable {
    public let cluster: Cluster?
}

extension DeleteClusterOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(Cluster.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

public struct DeleteServiceInputBodyMiddleware: Middleware {
    public let id: String = "DeleteServiceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteServiceInput>
    public typealias MOutput = OperationOutput<DeleteServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteServiceOutputError>
}

extension DeleteServiceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteServiceInput(cluster: \(String(describing: cluster)), force: \(String(describing: force)), service: \(String(describing: service)))"}
}

extension DeleteServiceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case force
        case service
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let force = force {
            try encodeContainer.encode(force, forKey: .force)
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
    }
}

public struct DeleteServiceInputHeadersMiddleware: Middleware {
    public let id: String = "DeleteServiceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteServiceInput>
    public typealias MOutput = OperationOutput<DeleteServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteServiceOutputError>
}

public struct DeleteServiceInputQueryItemMiddleware: Middleware {
    public let id: String = "DeleteServiceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteServiceInput>
    public typealias MOutput = OperationOutput<DeleteServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteServiceOutputError>
}

public struct DeleteServiceInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to delete.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>If <code>true</code>, allows you to delete a service even if it has not been scaled
    /// 			down to zero tasks. It is only necessary to use this if the service is using the
    /// 				<code>REPLICA</code> scheduling strategy.</p>
    public let force: Bool?
    /// <p>The name of the service to delete.</p>
    public let service: String?

    public init (
        cluster: String? = nil,
        force: Bool? = nil,
        service: String? = nil
    )
    {
        self.cluster = cluster
        self.force = force
        self.service = service
    }
}

struct DeleteServiceInputBody: Equatable {
    public let cluster: String?
    public let service: String?
    public let force: Bool?
}

extension DeleteServiceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case force
        case service
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let forceDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .force)
        force = forceDecoded
    }
}

extension DeleteServiceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeleteServiceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeleteServiceOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case serviceNotFoundException(ServiceNotFoundException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeleteServiceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteServiceOutputResponse(service: \(String(describing: service)))"}
}

extension DeleteServiceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeleteServiceOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.service = output.service
        } else {
            self.service = nil
        }
    }
}

public struct DeleteServiceOutputResponse: Equatable {
    /// <p>The full description of the deleted service.</p>
    public let service: Service?

    public init (
        service: Service? = nil
    )
    {
        self.service = service
    }
}

struct DeleteServiceOutputResponseBody: Equatable {
    public let service: Service?
}

extension DeleteServiceOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case service
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let serviceDecoded = try containerValues.decodeIfPresent(Service.self, forKey: .service)
        service = serviceDecoded
    }
}

public struct DeleteTaskSetInputBodyMiddleware: Middleware {
    public let id: String = "DeleteTaskSetInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteTaskSetInput>
    public typealias MOutput = OperationOutput<DeleteTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteTaskSetOutputError>
}

extension DeleteTaskSetInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteTaskSetInput(cluster: \(String(describing: cluster)), force: \(String(describing: force)), service: \(String(describing: service)), taskSet: \(String(describing: taskSet)))"}
}

extension DeleteTaskSetInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case force
        case service
        case taskSet
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let force = force {
            try encodeContainer.encode(force, forKey: .force)
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
        if let taskSet = taskSet {
            try encodeContainer.encode(taskSet, forKey: .taskSet)
        }
    }
}

public struct DeleteTaskSetInputHeadersMiddleware: Middleware {
    public let id: String = "DeleteTaskSetInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteTaskSetInput>
    public typealias MOutput = OperationOutput<DeleteTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteTaskSetOutputError>
}

public struct DeleteTaskSetInputQueryItemMiddleware: Middleware {
    public let id: String = "DeleteTaskSetInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeleteTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<DeleteTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeleteTaskSetInput>
    public typealias MOutput = OperationOutput<DeleteTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeleteTaskSetOutputError>
}

public struct DeleteTaskSetInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task
    /// 			set exists in to delete.</p>
    public let cluster: String?
    /// <p>If <code>true</code>, this allows you to delete a task set even if it hasn't been
    /// 			scaled down to zero.</p>
    public let force: Bool?
    /// <p>The short name or full Amazon Resource Name (ARN) of the service that hosts the task set to
    /// 			delete.</p>
    public let service: String?
    /// <p>The task set ID or full Amazon Resource Name (ARN) of the task set to delete.</p>
    public let taskSet: String?

    public init (
        cluster: String? = nil,
        force: Bool? = nil,
        service: String? = nil,
        taskSet: String? = nil
    )
    {
        self.cluster = cluster
        self.force = force
        self.service = service
        self.taskSet = taskSet
    }
}

struct DeleteTaskSetInputBody: Equatable {
    public let cluster: String?
    public let service: String?
    public let taskSet: String?
    public let force: Bool?
}

extension DeleteTaskSetInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case force
        case service
        case taskSet
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let taskSetDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskSet)
        taskSet = taskSetDecoded
        let forceDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .force)
        force = forceDecoded
    }
}

extension DeleteTaskSetOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeleteTaskSetOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotActiveException" : self = .serviceNotActiveException(try ServiceNotActiveException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "TaskSetNotFoundException" : self = .taskSetNotFoundException(try TaskSetNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeleteTaskSetOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case serviceNotActiveException(ServiceNotActiveException)
    case serviceNotFoundException(ServiceNotFoundException)
    case taskSetNotFoundException(TaskSetNotFoundException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeleteTaskSetOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeleteTaskSetOutputResponse(taskSet: \(String(describing: taskSet)))"}
}

extension DeleteTaskSetOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeleteTaskSetOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.taskSet = output.taskSet
        } else {
            self.taskSet = nil
        }
    }
}

public struct DeleteTaskSetOutputResponse: Equatable {
    /// <p>Details about the task set.</p>
    public let taskSet: TaskSet?

    public init (
        taskSet: TaskSet? = nil
    )
    {
        self.taskSet = taskSet
    }
}

struct DeleteTaskSetOutputResponseBody: Equatable {
    public let taskSet: TaskSet?
}

extension DeleteTaskSetOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case taskSet
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskSetDecoded = try containerValues.decodeIfPresent(TaskSet.self, forKey: .taskSet)
        taskSet = taskSetDecoded
    }
}

extension Deployment: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case createdAt
        case desiredCount
        case failedTasks
        case id
        case launchType
        case networkConfiguration
        case pendingCount
        case platformVersion
        case rolloutState
        case rolloutStateReason
        case runningCount
        case status
        case taskDefinition
        case updatedAt
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let createdAt = createdAt {
            try encodeContainer.encode(createdAt.timeIntervalSince1970, forKey: .createdAt)
        }
        if desiredCount != 0 {
            try encodeContainer.encode(desiredCount, forKey: .desiredCount)
        }
        if failedTasks != 0 {
            try encodeContainer.encode(failedTasks, forKey: .failedTasks)
        }
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if pendingCount != 0 {
            try encodeContainer.encode(pendingCount, forKey: .pendingCount)
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let rolloutState = rolloutState {
            try encodeContainer.encode(rolloutState.rawValue, forKey: .rolloutState)
        }
        if let rolloutStateReason = rolloutStateReason {
            try encodeContainer.encode(rolloutStateReason, forKey: .rolloutStateReason)
        }
        if runningCount != 0 {
            try encodeContainer.encode(runningCount, forKey: .runningCount)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
        if let updatedAt = updatedAt {
            try encodeContainer.encode(updatedAt.timeIntervalSince1970, forKey: .updatedAt)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let desiredCountDecoded = try containerValues.decode(Int.self, forKey: .desiredCount)
        desiredCount = desiredCountDecoded
        let pendingCountDecoded = try containerValues.decode(Int.self, forKey: .pendingCount)
        pendingCount = pendingCountDecoded
        let runningCountDecoded = try containerValues.decode(Int.self, forKey: .runningCount)
        runningCount = runningCountDecoded
        let failedTasksDecoded = try containerValues.decode(Int.self, forKey: .failedTasks)
        failedTasks = failedTasksDecoded
        let createdAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .createdAt)
        createdAt = createdAtDecoded
        let updatedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .updatedAt)
        updatedAt = updatedAtDecoded
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let rolloutStateDecoded = try containerValues.decodeIfPresent(DeploymentRolloutState.self, forKey: .rolloutState)
        rolloutState = rolloutStateDecoded
        let rolloutStateReasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .rolloutStateReason)
        rolloutStateReason = rolloutStateReasonDecoded
    }
}

extension Deployment: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Deployment(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), createdAt: \(String(describing: createdAt)), desiredCount: \(String(describing: desiredCount)), failedTasks: \(String(describing: failedTasks)), id: \(String(describing: id)), launchType: \(String(describing: launchType)), networkConfiguration: \(String(describing: networkConfiguration)), pendingCount: \(String(describing: pendingCount)), platformVersion: \(String(describing: platformVersion)), rolloutState: \(String(describing: rolloutState)), rolloutStateReason: \(String(describing: rolloutStateReason)), runningCount: \(String(describing: runningCount)), status: \(String(describing: status)), taskDefinition: \(String(describing: taskDefinition)), updatedAt: \(String(describing: updatedAt)))"}
}

/// <p>The details of an Amazon ECS service deployment. This is used only when a service uses the
/// 				<code>ECS</code> deployment controller type.</p>
public struct Deployment: Equatable {
    /// <p>The capacity provider strategy that the deployment is using.</p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The Unix timestamp for when the service deployment was created.</p>
    public let createdAt: Date?
    /// <p>The most recent desired count of tasks that was specified for the service to deploy or
    /// 			maintain.</p>
    public let desiredCount: Int
    /// <p>The number of consecutively failed tasks in the deployment. A task is considered a
    /// 			failure if the service scheduler can't launch the task, the task doesn't transition to a
    /// 				<code>RUNNING</code> state, or if it fails any of its defined health checks and is
    /// 			stopped.</p>
    /// 		       <note>
    /// 			         <p>Once a service deployment has one or more successfully running tasks, the failed
    /// 				task count resets to zero and stops being evaluated.</p>
    /// 		       </note>
    public let failedTasks: Int
    /// <p>The ID of the deployment.</p>
    public let id: String?
    /// <p>The launch type the tasks in the service are using. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS
    /// 				Launch Types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let launchType: LaunchType?
    /// <p>The VPC subnet and security group configuration for tasks that receive their own
    ///             elastic network interface by using the <code>awsvpc</code> networking mode.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>The number of tasks in the deployment that are in the <code>PENDING</code>
    /// 			status.</p>
    public let pendingCount: Int
    /// <p>The platform version on which your tasks in the service are running. A platform
    /// 			version is only specified for tasks using the Fargate launch type. If one
    /// 			is not specified, the <code>LATEST</code> platform version is used by default. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate Platform
    /// 				Versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <note>
    /// 			         <p>The <code>rolloutState</code> of a service is only returned for services that use
    /// 				the rolling update (<code>ECS</code>) deployment type that are not behind a
    /// 				Classic Load Balancer.</p>
    /// 		       </note>
    /// 		       <p>The rollout state of the deployment. When a service deployment is started, it begins
    /// 			in an <code>IN_PROGRESS</code> state. When the service reaches a steady state, the
    /// 			deployment will transition to a <code>COMPLETED</code> state. If the service fails to
    /// 			reach a steady state and circuit breaker is enabled, the deployment will transition to a
    /// 				<code>FAILED</code> state. A deployment in <code>FAILED</code> state will launch no
    /// 			new tasks. For more information, see <a>DeploymentCircuitBreaker</a>.</p>
    public let rolloutState: DeploymentRolloutState?
    /// <p>A description of the rollout state of a deployment.</p>
    public let rolloutStateReason: String?
    /// <p>The number of tasks in the deployment that are in the <code>RUNNING</code>
    /// 			status.</p>
    public let runningCount: Int
    /// <p>The status of the deployment. The following describes each state:</p>
    /// 		       <dl>
    ///             <dt>PRIMARY</dt>
    ///             <dd>
    /// 					          <p>The most recent deployment of a service.</p>
    /// 				        </dd>
    ///             <dt>ACTIVE</dt>
    ///             <dd>
    /// 					          <p>A service deployment that still has running tasks, but are in the process
    /// 						of being replaced with a new <code>PRIMARY</code> deployment.</p>
    /// 				        </dd>
    ///             <dt>INACTIVE</dt>
    ///             <dd>
    /// 					          <p>A deployment that has been completely replaced.</p>
    /// 				        </dd>
    ///          </dl>
    public let status: String?
    /// <p>The most recent task definition that was specified for the tasks in the service to
    /// 			use.</p>
    public let taskDefinition: String?
    /// <p>The Unix timestamp for when the service deployment was last updated.</p>
    public let updatedAt: Date?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        createdAt: Date? = nil,
        desiredCount: Int = 0,
        failedTasks: Int = 0,
        id: String? = nil,
        launchType: LaunchType? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        pendingCount: Int = 0,
        platformVersion: String? = nil,
        rolloutState: DeploymentRolloutState? = nil,
        rolloutStateReason: String? = nil,
        runningCount: Int = 0,
        status: String? = nil,
        taskDefinition: String? = nil,
        updatedAt: Date? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.createdAt = createdAt
        self.desiredCount = desiredCount
        self.failedTasks = failedTasks
        self.id = id
        self.launchType = launchType
        self.networkConfiguration = networkConfiguration
        self.pendingCount = pendingCount
        self.platformVersion = platformVersion
        self.rolloutState = rolloutState
        self.rolloutStateReason = rolloutStateReason
        self.runningCount = runningCount
        self.status = status
        self.taskDefinition = taskDefinition
        self.updatedAt = updatedAt
    }
}

extension DeploymentCircuitBreaker: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case enable
        case rollback
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if enable != false {
            try encodeContainer.encode(enable, forKey: .enable)
        }
        if rollback != false {
            try encodeContainer.encode(rollback, forKey: .rollback)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let enableDecoded = try containerValues.decode(Bool.self, forKey: .enable)
        enable = enableDecoded
        let rollbackDecoded = try containerValues.decode(Bool.self, forKey: .rollback)
        rollback = rollbackDecoded
    }
}

extension DeploymentCircuitBreaker: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeploymentCircuitBreaker(enable: \(String(describing: enable)), rollback: \(String(describing: rollback)))"}
}

/// <note>
/// 			         <p>The deployment circuit breaker can only be used for services using the rolling
/// 				update (<code>ECS</code>) deployment type that are not behind a Classic Load Balancer.</p>
/// 		       </note>
/// 		       <p>The <b>deployment circuit breaker</b> determines whether a
/// 			service deployment will fail if the service can't reach a steady state. If enabled, a
/// 			service deployment will transition to a failed state and stop launching new tasks. You
/// 			can also enable Amazon ECS to roll back your service to the last completed deployment after a
/// 			failure. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-type-ecs.html">Rolling
/// 				update</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct DeploymentCircuitBreaker: Equatable {
    /// <p>Whether to enable the deployment circuit breaker logic for the service.</p>
    public let enable: Bool
    /// <p>Whether to enable Amazon ECS to roll back the service if a service deployment fails. If
    /// 			rollback is enabled, when a service deployment fails, the service is rolled back to the
    /// 			last deployment that completed successfully.</p>
    public let rollback: Bool

    public init (
        enable: Bool = false,
        rollback: Bool = false
    )
    {
        self.enable = enable
        self.rollback = rollback
    }
}

extension DeploymentConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case deploymentCircuitBreaker
        case maximumPercent
        case minimumHealthyPercent
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let deploymentCircuitBreaker = deploymentCircuitBreaker {
            try encodeContainer.encode(deploymentCircuitBreaker, forKey: .deploymentCircuitBreaker)
        }
        if let maximumPercent = maximumPercent {
            try encodeContainer.encode(maximumPercent, forKey: .maximumPercent)
        }
        if let minimumHealthyPercent = minimumHealthyPercent {
            try encodeContainer.encode(minimumHealthyPercent, forKey: .minimumHealthyPercent)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let deploymentCircuitBreakerDecoded = try containerValues.decodeIfPresent(DeploymentCircuitBreaker.self, forKey: .deploymentCircuitBreaker)
        deploymentCircuitBreaker = deploymentCircuitBreakerDecoded
        let maximumPercentDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maximumPercent)
        maximumPercent = maximumPercentDecoded
        let minimumHealthyPercentDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .minimumHealthyPercent)
        minimumHealthyPercent = minimumHealthyPercentDecoded
    }
}

extension DeploymentConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeploymentConfiguration(deploymentCircuitBreaker: \(String(describing: deploymentCircuitBreaker)), maximumPercent: \(String(describing: maximumPercent)), minimumHealthyPercent: \(String(describing: minimumHealthyPercent)))"}
}

/// <p>Optional deployment parameters that control how many tasks run during a deployment and
/// 			the ordering of stopping and starting tasks.</p>
public struct DeploymentConfiguration: Equatable {
    /// <note>
    /// 			         <p>The deployment circuit breaker can only be used for services using the rolling
    /// 				update (<code>ECS</code>) deployment type.</p>
    /// 		       </note>
    /// 		       <p>The <b>deployment circuit breaker</b> determines whether a
    /// 			service deployment will fail if the service can't reach a steady state. If deployment
    /// 			circuit breaker is enabled, a service deployment will transition to a failed state and
    /// 			stop launching new tasks. If rollback is enabled, when a service deployment fails, the
    /// 			service is rolled back to the last deployment that completed successfully.</p>
    public let deploymentCircuitBreaker: DeploymentCircuitBreaker?
    /// <p>If a service is using the rolling update (<code>ECS</code>) deployment type, the
    /// 				<b>maximum percent</b> parameter represents an upper limit
    /// 			on the number of tasks in a service that are allowed in the <code>RUNNING</code> or
    /// 				<code>PENDING</code> state during a deployment, as a percentage of the desired
    /// 			number of tasks (rounded down to the nearest integer), and while any container instances
    /// 			are in the <code>DRAINING</code> state if the service contains tasks using the
    /// 			EC2 launch type. This parameter enables you to define the deployment batch
    /// 			size. For example, if your service has a desired number of four tasks and a maximum
    /// 			percent value of 200%, the scheduler may start four new tasks before stopping the four
    /// 			older tasks (provided that the cluster resources required to do this are available). The
    /// 			default value for maximum percent is 200%.</p>
    /// 		       <p>If a service is using the blue/green (<code>CODE_DEPLOY</code>) or
    /// 				<code>EXTERNAL</code> deployment types and tasks that use the EC2
    /// 			launch type, the <b>maximum percent</b> value is set to the
    /// 			default value and is used to define the upper limit on the number of the tasks in the
    /// 			service that remain in the <code>RUNNING</code> state while the container instances are
    /// 			in the <code>DRAINING</code> state. If the tasks in the service use the
    /// 			Fargate launch type, the maximum percent value is not used, although it is
    /// 			returned when describing your service.</p>
    public let maximumPercent: Int?
    /// <p>If a service is using the rolling update (<code>ECS</code>) deployment type, the
    /// 				<b>minimum healthy percent</b> represents a lower limit on
    /// 			the number of tasks in a service that must remain in the <code>RUNNING</code> state
    /// 			during a deployment, as a percentage of the desired number of tasks (rounded up to the
    /// 			nearest integer), and while any container instances are in the <code>DRAINING</code>
    /// 			state if the service contains tasks using the EC2 launch type. This
    /// 			parameter enables you to deploy without using additional cluster capacity. For example,
    /// 			if your service has a desired number of four tasks and a minimum healthy percent of 50%,
    /// 			the scheduler may stop two existing tasks to free up cluster capacity before starting
    /// 			two new tasks. Tasks for services that <i>do not</i> use a load balancer
    /// 			are considered healthy if they are in the <code>RUNNING</code> state; tasks for services
    /// 			that <i>do</i> use a load balancer are considered healthy if they are in
    /// 			the <code>RUNNING</code> state and they are reported as healthy by the load balancer.
    /// 			The default value for minimum healthy percent is 100%.</p>
    /// 		       <p>If a service is using the blue/green (<code>CODE_DEPLOY</code>) or
    /// 				<code>EXTERNAL</code> deployment types and tasks that use the EC2
    /// 			launch type, the <b>minimum healthy percent</b> value is set
    /// 			to the default value and is used to define the lower limit on the number of the tasks in
    /// 			the service that remain in the <code>RUNNING</code> state while the container instances
    /// 			are in the <code>DRAINING</code> state. If the tasks in the service use the
    /// 			Fargate launch type, the minimum healthy percent value is not used,
    /// 			although it is returned when describing your service.</p>
    public let minimumHealthyPercent: Int?

    public init (
        deploymentCircuitBreaker: DeploymentCircuitBreaker? = nil,
        maximumPercent: Int? = nil,
        minimumHealthyPercent: Int? = nil
    )
    {
        self.deploymentCircuitBreaker = deploymentCircuitBreaker
        self.maximumPercent = maximumPercent
        self.minimumHealthyPercent = minimumHealthyPercent
    }
}

extension DeploymentController: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let typeDecoded = try containerValues.decodeIfPresent(DeploymentControllerType.self, forKey: .type)
        type = typeDecoded
    }
}

extension DeploymentController: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeploymentController(type: \(String(describing: type)))"}
}

/// <p>The deployment controller to use for the service. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-types.html">Amazon ECS Deployment Types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct DeploymentController: Equatable {
    /// <p>The deployment controller type to use.</p>
    /// 		       <p>There are three deployment controller types available:</p>
    /// 		       <dl>
    ///             <dt>ECS</dt>
    ///             <dd>
    /// 					          <p>The rolling update (<code>ECS</code>) deployment type involves replacing
    /// 						the current running version of the container with the latest version. The
    /// 						number of containers Amazon ECS adds or removes from the service during a rolling
    /// 						update is controlled by adjusting the minimum and maximum number of healthy
    /// 						tasks allowed during a service deployment, as specified in the <a>DeploymentConfiguration</a>.</p>
    /// 				        </dd>
    ///             <dt>CODE_DEPLOY</dt>
    ///             <dd>
    /// 					          <p>The blue/green (<code>CODE_DEPLOY</code>) deployment type uses the
    /// 						blue/green deployment model powered by AWS CodeDeploy, which allows you to verify a
    /// 						new deployment of a service before sending production traffic to it.</p>
    /// 				        </dd>
    ///             <dt>EXTERNAL</dt>
    ///             <dd>
    /// 					          <p>The external (<code>EXTERNAL</code>) deployment type enables you to use
    /// 						any third-party deployment controller for full control over the deployment
    /// 						process for an Amazon ECS service.</p>
    /// 				        </dd>
    ///          </dl>
    public let type: DeploymentControllerType?

    public init (
        type: DeploymentControllerType? = nil
    )
    {
        self.type = type
    }
}

public enum DeploymentControllerType {
    case codeDeploy
    case ecs
    case external
    case sdkUnknown(String)
}

extension DeploymentControllerType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [DeploymentControllerType] {
        return [
            .codeDeploy,
            .ecs,
            .external,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .codeDeploy: return "CODE_DEPLOY"
        case .ecs: return "ECS"
        case .external: return "EXTERNAL"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = DeploymentControllerType(rawValue: rawValue) ?? DeploymentControllerType.sdkUnknown(rawValue)
    }
}

public enum DeploymentRolloutState {
    case completed
    case failed
    case inProgress
    case sdkUnknown(String)
}

extension DeploymentRolloutState : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [DeploymentRolloutState] {
        return [
            .completed,
            .failed,
            .inProgress,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .completed: return "COMPLETED"
        case .failed: return "FAILED"
        case .inProgress: return "IN_PROGRESS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = DeploymentRolloutState(rawValue: rawValue) ?? DeploymentRolloutState.sdkUnknown(rawValue)
    }
}

public struct DeregisterContainerInstanceInputBodyMiddleware: Middleware {
    public let id: String = "DeregisterContainerInstanceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeregisterContainerInstanceInput>,
                  next: H) -> Swift.Result<OperationOutput<DeregisterContainerInstanceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeregisterContainerInstanceInput>
    public typealias MOutput = OperationOutput<DeregisterContainerInstanceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeregisterContainerInstanceOutputError>
}

extension DeregisterContainerInstanceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeregisterContainerInstanceInput(cluster: \(String(describing: cluster)), containerInstance: \(String(describing: containerInstance)), force: \(String(describing: force)))"}
}

extension DeregisterContainerInstanceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
        case force
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstance = containerInstance {
            try encodeContainer.encode(containerInstance, forKey: .containerInstance)
        }
        if let force = force {
            try encodeContainer.encode(force, forKey: .force)
        }
    }
}

public struct DeregisterContainerInstanceInputHeadersMiddleware: Middleware {
    public let id: String = "DeregisterContainerInstanceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeregisterContainerInstanceInput>,
                  next: H) -> Swift.Result<OperationOutput<DeregisterContainerInstanceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeregisterContainerInstanceInput>
    public typealias MOutput = OperationOutput<DeregisterContainerInstanceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeregisterContainerInstanceOutputError>
}

public struct DeregisterContainerInstanceInputQueryItemMiddleware: Middleware {
    public let id: String = "DeregisterContainerInstanceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeregisterContainerInstanceInput>,
                  next: H) -> Swift.Result<OperationOutput<DeregisterContainerInstanceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeregisterContainerInstanceInput>
    public typealias MOutput = OperationOutput<DeregisterContainerInstanceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeregisterContainerInstanceOutputError>
}

public struct DeregisterContainerInstanceInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instance to
    /// 			deregister. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The container instance ID or full ARN of the container instance to deregister.
    /// 			The ARN contains the <code>arn:aws:ecs</code> namespace, followed by the Region of the container instance, the AWS account ID of the container instance owner, the <code>container-instance</code> namespace, and then the container instance ID. For example, <code>arn:aws:ecs:region:aws_account_id:container-instance/container_instance_ID</code>.</p>
    public let containerInstance: String?
    /// <p>Forces the deregistration of the container instance. If you have tasks running on the
    /// 			container instance when you deregister it with the <code>force</code> option, these
    /// 			tasks remain running until you terminate the instance or the tasks stop through some
    /// 			other means, but they are orphaned (no longer monitored or accounted for by Amazon ECS). If
    /// 			an orphaned task on your container instance is part of an Amazon ECS service, then the
    /// 			service scheduler starts another copy of that task, on a different container instance if
    /// 			possible. </p>
    /// 		       <p>Any containers in orphaned service tasks that are registered with a Classic Load Balancer or an Application Load Balancer
    /// 			target group are deregistered. They begin connection draining according to the settings
    /// 			on the load balancer or target group.</p>
    public let force: Bool?

    public init (
        cluster: String? = nil,
        containerInstance: String? = nil,
        force: Bool? = nil
    )
    {
        self.cluster = cluster
        self.containerInstance = containerInstance
        self.force = force
    }
}

struct DeregisterContainerInstanceInputBody: Equatable {
    public let cluster: String?
    public let containerInstance: String?
    public let force: Bool?
}

extension DeregisterContainerInstanceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
        case force
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerInstanceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
        let forceDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .force)
        force = forceDecoded
    }
}

extension DeregisterContainerInstanceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeregisterContainerInstanceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeregisterContainerInstanceOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeregisterContainerInstanceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeregisterContainerInstanceOutputResponse(containerInstance: \(String(describing: containerInstance)))"}
}

extension DeregisterContainerInstanceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeregisterContainerInstanceOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.containerInstance = output.containerInstance
        } else {
            self.containerInstance = nil
        }
    }
}

public struct DeregisterContainerInstanceOutputResponse: Equatable {
    /// <p>The container instance that was deregistered.</p>
    public let containerInstance: ContainerInstance?

    public init (
        containerInstance: ContainerInstance? = nil
    )
    {
        self.containerInstance = containerInstance
    }
}

struct DeregisterContainerInstanceOutputResponseBody: Equatable {
    public let containerInstance: ContainerInstance?
}

extension DeregisterContainerInstanceOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerInstance
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstanceDecoded = try containerValues.decodeIfPresent(ContainerInstance.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
    }
}

public struct DeregisterTaskDefinitionInputBodyMiddleware: Middleware {
    public let id: String = "DeregisterTaskDefinitionInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeregisterTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<DeregisterTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeregisterTaskDefinitionInput>
    public typealias MOutput = OperationOutput<DeregisterTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeregisterTaskDefinitionOutputError>
}

extension DeregisterTaskDefinitionInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeregisterTaskDefinitionInput(taskDefinition: \(String(describing: taskDefinition)))"}
}

extension DeregisterTaskDefinitionInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct DeregisterTaskDefinitionInputHeadersMiddleware: Middleware {
    public let id: String = "DeregisterTaskDefinitionInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeregisterTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<DeregisterTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeregisterTaskDefinitionInput>
    public typealias MOutput = OperationOutput<DeregisterTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeregisterTaskDefinitionOutputError>
}

public struct DeregisterTaskDefinitionInputQueryItemMiddleware: Middleware {
    public let id: String = "DeregisterTaskDefinitionInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DeregisterTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<DeregisterTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DeregisterTaskDefinitionInput>
    public typealias MOutput = OperationOutput<DeregisterTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DeregisterTaskDefinitionOutputError>
}

public struct DeregisterTaskDefinitionInput: Equatable {
    /// <p>The <code>family</code> and <code>revision</code> (<code>family:revision</code>) or
    /// 			full Amazon Resource Name (ARN) of the task definition to deregister. You must specify a
    /// 				<code>revision</code>.</p>
    public let taskDefinition: String?

    public init (
        taskDefinition: String? = nil
    )
    {
        self.taskDefinition = taskDefinition
    }
}

struct DeregisterTaskDefinitionInputBody: Equatable {
    public let taskDefinition: String?
}

extension DeregisterTaskDefinitionInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
    }
}

extension DeregisterTaskDefinitionOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DeregisterTaskDefinitionOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DeregisterTaskDefinitionOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DeregisterTaskDefinitionOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DeregisterTaskDefinitionOutputResponse(taskDefinition: \(String(describing: taskDefinition)))"}
}

extension DeregisterTaskDefinitionOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DeregisterTaskDefinitionOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.taskDefinition = output.taskDefinition
        } else {
            self.taskDefinition = nil
        }
    }
}

public struct DeregisterTaskDefinitionOutputResponse: Equatable {
    /// <p>The full description of the deregistered task.</p>
    public let taskDefinition: TaskDefinition?

    public init (
        taskDefinition: TaskDefinition? = nil
    )
    {
        self.taskDefinition = taskDefinition
    }
}

struct DeregisterTaskDefinitionOutputResponseBody: Equatable {
    public let taskDefinition: TaskDefinition?
}

extension DeregisterTaskDefinitionOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(TaskDefinition.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
    }
}

public struct DescribeCapacityProvidersInputBodyMiddleware: Middleware {
    public let id: String = "DescribeCapacityProvidersInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeCapacityProvidersInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeCapacityProvidersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeCapacityProvidersInput>
    public typealias MOutput = OperationOutput<DescribeCapacityProvidersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeCapacityProvidersOutputError>
}

extension DescribeCapacityProvidersInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeCapacityProvidersInput(capacityProviders: \(String(describing: capacityProviders)), include: \(String(describing: include)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)))"}
}

extension DescribeCapacityProvidersInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case include
        case maxResults
        case nextToken
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviders = capacityProviders {
            var capacityProvidersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviders)
            for stringlist0 in capacityProviders {
                try capacityProvidersContainer.encode(stringlist0)
            }
        }
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for capacityproviderfieldlist0 in include {
                try includeContainer.encode(capacityproviderfieldlist0.rawValue)
            }
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
    }
}

public struct DescribeCapacityProvidersInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeCapacityProvidersInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeCapacityProvidersInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeCapacityProvidersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeCapacityProvidersInput>
    public typealias MOutput = OperationOutput<DescribeCapacityProvidersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeCapacityProvidersOutputError>
}

public struct DescribeCapacityProvidersInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeCapacityProvidersInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeCapacityProvidersInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeCapacityProvidersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeCapacityProvidersInput>
    public typealias MOutput = OperationOutput<DescribeCapacityProvidersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeCapacityProvidersOutputError>
}

public struct DescribeCapacityProvidersInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of one or more capacity providers. Up to
    /// 				<code>100</code> capacity providers can be described in an action.</p>
    public let capacityProviders: [String]?
    /// <p>Specifies whether or not you want to see the resource tags for the capacity provider.
    /// 			If <code>TAGS</code> is specified, the tags are included in the response. If this field
    /// 			is omitted, tags are not included in the response.</p>
    public let include: [CapacityProviderField]?
    /// <p>The maximum number of account setting results returned by
    /// 				<code>DescribeCapacityProviders</code> in paginated output. When this parameter is
    /// 			used, <code>DescribeCapacityProviders</code> only returns <code>maxResults</code>
    /// 			results in a single page along with a <code>nextToken</code> response element. The
    /// 			remaining results of the initial request can be seen by sending another
    /// 				<code>DescribeCapacityProviders</code> request with the returned
    /// 				<code>nextToken</code> value. This value can be between
    /// 			1 and 10. If this
    /// 			parameter is not used, then <code>DescribeCapacityProviders</code> returns up to
    /// 			10 results and a <code>nextToken</code> value
    /// 			if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a previous paginated
    /// 				<code>DescribeCapacityProviders</code> request where <code>maxResults</code> was
    /// 			used and the results exceeded the value of that parameter. Pagination continues from the
    /// 			end of the previous results that returned the <code>nextToken</code> value.</p>
    /// 		       <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?

    public init (
        capacityProviders: [String]? = nil,
        include: [CapacityProviderField]? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil
    )
    {
        self.capacityProviders = capacityProviders
        self.include = include
        self.maxResults = maxResults
        self.nextToken = nextToken
    }
}

struct DescribeCapacityProvidersInputBody: Equatable {
    public let capacityProviders: [String]?
    public let include: [CapacityProviderField]?
    public let maxResults: Int?
    public let nextToken: String?
}

extension DescribeCapacityProvidersInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case include
        case maxResults
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProvidersContainer = try containerValues.decodeIfPresent([String?].self, forKey: .capacityProviders)
        var capacityProvidersDecoded0:[String]? = nil
        if let capacityProvidersContainer = capacityProvidersContainer {
            capacityProvidersDecoded0 = [String]()
            for string0 in capacityProvidersContainer {
                if let string0 = string0 {
                    capacityProvidersDecoded0?.append(string0)
                }
            }
        }
        capacityProviders = capacityProvidersDecoded0
        let includeContainer = try containerValues.decodeIfPresent([CapacityProviderField?].self, forKey: .include)
        var includeDecoded0:[CapacityProviderField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [CapacityProviderField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

extension DescribeCapacityProvidersOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeCapacityProvidersOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeCapacityProvidersOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeCapacityProvidersOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeCapacityProvidersOutputResponse(capacityProviders: \(String(describing: capacityProviders)), failures: \(String(describing: failures)), nextToken: \(String(describing: nextToken)))"}
}

extension DescribeCapacityProvidersOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeCapacityProvidersOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.capacityProviders = output.capacityProviders
            self.failures = output.failures
            self.nextToken = output.nextToken
        } else {
            self.capacityProviders = nil
            self.failures = nil
            self.nextToken = nil
        }
    }
}

public struct DescribeCapacityProvidersOutputResponse: Equatable {
    /// <p>The list of capacity providers.</p>
    public let capacityProviders: [CapacityProvider]?
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?
    /// <p>The <code>nextToken</code> value to include in a future
    /// 				<code>DescribeCapacityProviders</code> request. When the results of a
    /// 				<code>DescribeCapacityProviders</code> request exceed <code>maxResults</code>, this
    /// 			value can be used to retrieve the next page of results. This value is <code>null</code>
    /// 			when there are no more results to return.</p>
    public let nextToken: String?

    public init (
        capacityProviders: [CapacityProvider]? = nil,
        failures: [Failure]? = nil,
        nextToken: String? = nil
    )
    {
        self.capacityProviders = capacityProviders
        self.failures = failures
        self.nextToken = nextToken
    }
}

struct DescribeCapacityProvidersOutputResponseBody: Equatable {
    public let capacityProviders: [CapacityProvider]?
    public let failures: [Failure]?
    public let nextToken: String?
}

extension DescribeCapacityProvidersOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case failures
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProvidersContainer = try containerValues.decodeIfPresent([CapacityProvider?].self, forKey: .capacityProviders)
        var capacityProvidersDecoded0:[CapacityProvider]? = nil
        if let capacityProvidersContainer = capacityProvidersContainer {
            capacityProvidersDecoded0 = [CapacityProvider]()
            for structure0 in capacityProvidersContainer {
                if let structure0 = structure0 {
                    capacityProvidersDecoded0?.append(structure0)
                }
            }
        }
        capacityProviders = capacityProvidersDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct DescribeClustersInputBodyMiddleware: Middleware {
    public let id: String = "DescribeClustersInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeClustersInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeClustersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeClustersInput>
    public typealias MOutput = OperationOutput<DescribeClustersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeClustersOutputError>
}

extension DescribeClustersInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeClustersInput(clusters: \(String(describing: clusters)), include: \(String(describing: include)))"}
}

extension DescribeClustersInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case clusters
        case include
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let clusters = clusters {
            var clustersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .clusters)
            for stringlist0 in clusters {
                try clustersContainer.encode(stringlist0)
            }
        }
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for clusterfieldlist0 in include {
                try includeContainer.encode(clusterfieldlist0.rawValue)
            }
        }
    }
}

public struct DescribeClustersInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeClustersInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeClustersInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeClustersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeClustersInput>
    public typealias MOutput = OperationOutput<DescribeClustersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeClustersOutputError>
}

public struct DescribeClustersInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeClustersInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeClustersInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeClustersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeClustersInput>
    public typealias MOutput = OperationOutput<DescribeClustersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeClustersOutputError>
}

public struct DescribeClustersInput: Equatable {
    /// <p>A list of up to 100 cluster names or full cluster Amazon Resource Name (ARN) entries.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let clusters: [String]?
    /// <p>Whether to include additional information about the clusters in the response. If this
    /// 			field is omitted, this information isn't included.</p>
    /// 		       <p>If <code>ATTACHMENTS</code> is specified, the attachments for the container instances
    /// 			or tasks within the cluster are included.</p>
    /// 		       <p>If <code>SETTINGS</code> is specified, the settings for the cluster are
    /// 			included.</p>
    /// 		       <p>If <code>STATISTICS</code> is specified, the task and service count is included,
    /// 			separated by launch type.</p>
    /// 		       <p>If <code>TAGS</code> is specified, the metadata tags associated with the cluster are
    /// 			included.</p>
    public let include: [ClusterField]?

    public init (
        clusters: [String]? = nil,
        include: [ClusterField]? = nil
    )
    {
        self.clusters = clusters
        self.include = include
    }
}

struct DescribeClustersInputBody: Equatable {
    public let clusters: [String]?
    public let include: [ClusterField]?
}

extension DescribeClustersInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case clusters
        case include
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clustersContainer = try containerValues.decodeIfPresent([String?].self, forKey: .clusters)
        var clustersDecoded0:[String]? = nil
        if let clustersContainer = clustersContainer {
            clustersDecoded0 = [String]()
            for string0 in clustersContainer {
                if let string0 = string0 {
                    clustersDecoded0?.append(string0)
                }
            }
        }
        clusters = clustersDecoded0
        let includeContainer = try containerValues.decodeIfPresent([ClusterField?].self, forKey: .include)
        var includeDecoded0:[ClusterField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [ClusterField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
    }
}

extension DescribeClustersOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeClustersOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeClustersOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeClustersOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeClustersOutputResponse(clusters: \(String(describing: clusters)), failures: \(String(describing: failures)))"}
}

extension DescribeClustersOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeClustersOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.clusters = output.clusters
            self.failures = output.failures
        } else {
            self.clusters = nil
            self.failures = nil
        }
    }
}

public struct DescribeClustersOutputResponse: Equatable {
    /// <p>The list of clusters.</p>
    public let clusters: [Cluster]?
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?

    public init (
        clusters: [Cluster]? = nil,
        failures: [Failure]? = nil
    )
    {
        self.clusters = clusters
        self.failures = failures
    }
}

struct DescribeClustersOutputResponseBody: Equatable {
    public let clusters: [Cluster]?
    public let failures: [Failure]?
}

extension DescribeClustersOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case clusters
        case failures
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clustersContainer = try containerValues.decodeIfPresent([Cluster?].self, forKey: .clusters)
        var clustersDecoded0:[Cluster]? = nil
        if let clustersContainer = clustersContainer {
            clustersDecoded0 = [Cluster]()
            for structure0 in clustersContainer {
                if let structure0 = structure0 {
                    clustersDecoded0?.append(structure0)
                }
            }
        }
        clusters = clustersDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

public struct DescribeContainerInstancesInputBodyMiddleware: Middleware {
    public let id: String = "DescribeContainerInstancesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeContainerInstancesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeContainerInstancesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeContainerInstancesInput>
    public typealias MOutput = OperationOutput<DescribeContainerInstancesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeContainerInstancesOutputError>
}

extension DescribeContainerInstancesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeContainerInstancesInput(cluster: \(String(describing: cluster)), containerInstances: \(String(describing: containerInstances)), include: \(String(describing: include)))"}
}

extension DescribeContainerInstancesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstances
        case include
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstances = containerInstances {
            var containerInstancesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containerInstances)
            for stringlist0 in containerInstances {
                try containerInstancesContainer.encode(stringlist0)
            }
        }
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for containerinstancefieldlist0 in include {
                try includeContainer.encode(containerinstancefieldlist0.rawValue)
            }
        }
    }
}

public struct DescribeContainerInstancesInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeContainerInstancesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeContainerInstancesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeContainerInstancesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeContainerInstancesInput>
    public typealias MOutput = OperationOutput<DescribeContainerInstancesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeContainerInstancesOutputError>
}

public struct DescribeContainerInstancesInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeContainerInstancesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeContainerInstancesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeContainerInstancesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeContainerInstancesInput>
    public typealias MOutput = OperationOutput<DescribeContainerInstancesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeContainerInstancesOutputError>
}

public struct DescribeContainerInstancesInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instances to
    /// 			describe. If you do not specify a cluster, the default cluster is assumed. This parameter is required if the container instance
    /// 			or container instances you are describing were launched in any cluster other than the
    /// 			default cluster.</p>
    public let cluster: String?
    /// <p>A list of up to 100 container instance IDs or full Amazon Resource Name (ARN) entries.</p>
    public let containerInstances: [String]?
    /// <p>Specifies whether you want to see the resource tags for the container instance. If
    /// 				<code>TAGS</code> is specified, the tags are included in the response. If this field
    /// 			is omitted, tags are not included in the response.</p>
    public let include: [ContainerInstanceField]?

    public init (
        cluster: String? = nil,
        containerInstances: [String]? = nil,
        include: [ContainerInstanceField]? = nil
    )
    {
        self.cluster = cluster
        self.containerInstances = containerInstances
        self.include = include
    }
}

struct DescribeContainerInstancesInputBody: Equatable {
    public let cluster: String?
    public let containerInstances: [String]?
    public let include: [ContainerInstanceField]?
}

extension DescribeContainerInstancesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstances
        case include
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerInstancesContainer = try containerValues.decodeIfPresent([String?].self, forKey: .containerInstances)
        var containerInstancesDecoded0:[String]? = nil
        if let containerInstancesContainer = containerInstancesContainer {
            containerInstancesDecoded0 = [String]()
            for string0 in containerInstancesContainer {
                if let string0 = string0 {
                    containerInstancesDecoded0?.append(string0)
                }
            }
        }
        containerInstances = containerInstancesDecoded0
        let includeContainer = try containerValues.decodeIfPresent([ContainerInstanceField?].self, forKey: .include)
        var includeDecoded0:[ContainerInstanceField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [ContainerInstanceField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
    }
}

extension DescribeContainerInstancesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeContainerInstancesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeContainerInstancesOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeContainerInstancesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeContainerInstancesOutputResponse(containerInstances: \(String(describing: containerInstances)), failures: \(String(describing: failures)))"}
}

extension DescribeContainerInstancesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeContainerInstancesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.containerInstances = output.containerInstances
            self.failures = output.failures
        } else {
            self.containerInstances = nil
            self.failures = nil
        }
    }
}

public struct DescribeContainerInstancesOutputResponse: Equatable {
    /// <p>The list of container instances.</p>
    public let containerInstances: [ContainerInstance]?
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?

    public init (
        containerInstances: [ContainerInstance]? = nil,
        failures: [Failure]? = nil
    )
    {
        self.containerInstances = containerInstances
        self.failures = failures
    }
}

struct DescribeContainerInstancesOutputResponseBody: Equatable {
    public let containerInstances: [ContainerInstance]?
    public let failures: [Failure]?
}

extension DescribeContainerInstancesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerInstances
        case failures
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstancesContainer = try containerValues.decodeIfPresent([ContainerInstance?].self, forKey: .containerInstances)
        var containerInstancesDecoded0:[ContainerInstance]? = nil
        if let containerInstancesContainer = containerInstancesContainer {
            containerInstancesDecoded0 = [ContainerInstance]()
            for structure0 in containerInstancesContainer {
                if let structure0 = structure0 {
                    containerInstancesDecoded0?.append(structure0)
                }
            }
        }
        containerInstances = containerInstancesDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

public struct DescribeServicesInputBodyMiddleware: Middleware {
    public let id: String = "DescribeServicesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeServicesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeServicesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeServicesInput>
    public typealias MOutput = OperationOutput<DescribeServicesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeServicesOutputError>
}

extension DescribeServicesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeServicesInput(cluster: \(String(describing: cluster)), include: \(String(describing: include)), services: \(String(describing: services)))"}
}

extension DescribeServicesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case include
        case services
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for servicefieldlist0 in include {
                try includeContainer.encode(servicefieldlist0.rawValue)
            }
        }
        if let services = services {
            var servicesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .services)
            for stringlist0 in services {
                try servicesContainer.encode(stringlist0)
            }
        }
    }
}

public struct DescribeServicesInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeServicesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeServicesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeServicesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeServicesInput>
    public typealias MOutput = OperationOutput<DescribeServicesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeServicesOutputError>
}

public struct DescribeServicesInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeServicesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeServicesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeServicesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeServicesInput>
    public typealias MOutput = OperationOutput<DescribeServicesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeServicesOutputError>
}

public struct DescribeServicesInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN)the cluster that hosts the service to describe.
    /// 			If you do not specify a cluster, the default cluster is assumed. This parameter is required if the service or services you are
    /// 			describing were launched in any cluster other than the default cluster.</p>
    public let cluster: String?
    /// <p>Specifies whether you want to see the resource tags for the service. If
    /// 				<code>TAGS</code> is specified, the tags are included in the response. If this field
    /// 			is omitted, tags are not included in the response.</p>
    public let include: [ServiceField]?
    /// <p>A list of services to describe. You may specify up to 10 services to describe in a
    /// 			single operation.</p>
    public let services: [String]?

    public init (
        cluster: String? = nil,
        include: [ServiceField]? = nil,
        services: [String]? = nil
    )
    {
        self.cluster = cluster
        self.include = include
        self.services = services
    }
}

struct DescribeServicesInputBody: Equatable {
    public let cluster: String?
    public let services: [String]?
    public let include: [ServiceField]?
}

extension DescribeServicesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case include
        case services
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let servicesContainer = try containerValues.decodeIfPresent([String?].self, forKey: .services)
        var servicesDecoded0:[String]? = nil
        if let servicesContainer = servicesContainer {
            servicesDecoded0 = [String]()
            for string0 in servicesContainer {
                if let string0 = string0 {
                    servicesDecoded0?.append(string0)
                }
            }
        }
        services = servicesDecoded0
        let includeContainer = try containerValues.decodeIfPresent([ServiceField?].self, forKey: .include)
        var includeDecoded0:[ServiceField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [ServiceField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
    }
}

extension DescribeServicesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeServicesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeServicesOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeServicesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeServicesOutputResponse(failures: \(String(describing: failures)), services: \(String(describing: services)))"}
}

extension DescribeServicesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeServicesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.failures = output.failures
            self.services = output.services
        } else {
            self.failures = nil
            self.services = nil
        }
    }
}

public struct DescribeServicesOutputResponse: Equatable {
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?
    /// <p>The list of services described.</p>
    public let services: [Service]?

    public init (
        failures: [Failure]? = nil,
        services: [Service]? = nil
    )
    {
        self.failures = failures
        self.services = services
    }
}

struct DescribeServicesOutputResponseBody: Equatable {
    public let services: [Service]?
    public let failures: [Failure]?
}

extension DescribeServicesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case failures
        case services
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let servicesContainer = try containerValues.decodeIfPresent([Service?].self, forKey: .services)
        var servicesDecoded0:[Service]? = nil
        if let servicesContainer = servicesContainer {
            servicesDecoded0 = [Service]()
            for structure0 in servicesContainer {
                if let structure0 = structure0 {
                    servicesDecoded0?.append(structure0)
                }
            }
        }
        services = servicesDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

public struct DescribeTaskDefinitionInputBodyMiddleware: Middleware {
    public let id: String = "DescribeTaskDefinitionInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTaskDefinitionInput>
    public typealias MOutput = OperationOutput<DescribeTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTaskDefinitionOutputError>
}

extension DescribeTaskDefinitionInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeTaskDefinitionInput(include: \(String(describing: include)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension DescribeTaskDefinitionInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case include
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for taskdefinitionfieldlist0 in include {
                try includeContainer.encode(taskdefinitionfieldlist0.rawValue)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct DescribeTaskDefinitionInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeTaskDefinitionInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTaskDefinitionInput>
    public typealias MOutput = OperationOutput<DescribeTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTaskDefinitionOutputError>
}

public struct DescribeTaskDefinitionInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeTaskDefinitionInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTaskDefinitionInput>
    public typealias MOutput = OperationOutput<DescribeTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTaskDefinitionOutputError>
}

public struct DescribeTaskDefinitionInput: Equatable {
    /// <p>Specifies whether to see the resource tags for the task definition. If
    /// 				<code>TAGS</code> is specified, the tags are included in the response. If this field
    /// 			is omitted, tags are not included in the response.</p>
    public let include: [TaskDefinitionField]?
    /// <p>The <code>family</code> for the latest <code>ACTIVE</code> revision,
    /// 				<code>family</code> and <code>revision</code> (<code>family:revision</code>) for a
    /// 			specific revision in the family, or full Amazon Resource Name (ARN) of the task definition to
    /// 			describe.</p>
    public let taskDefinition: String?

    public init (
        include: [TaskDefinitionField]? = nil,
        taskDefinition: String? = nil
    )
    {
        self.include = include
        self.taskDefinition = taskDefinition
    }
}

struct DescribeTaskDefinitionInputBody: Equatable {
    public let taskDefinition: String?
    public let include: [TaskDefinitionField]?
}

extension DescribeTaskDefinitionInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case include
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let includeContainer = try containerValues.decodeIfPresent([TaskDefinitionField?].self, forKey: .include)
        var includeDecoded0:[TaskDefinitionField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [TaskDefinitionField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
    }
}

extension DescribeTaskDefinitionOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeTaskDefinitionOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeTaskDefinitionOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeTaskDefinitionOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeTaskDefinitionOutputResponse(tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension DescribeTaskDefinitionOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeTaskDefinitionOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.tags = output.tags
            self.taskDefinition = output.taskDefinition
        } else {
            self.tags = nil
            self.taskDefinition = nil
        }
    }
}

public struct DescribeTaskDefinitionOutputResponse: Equatable {
    /// <p>The metadata that is applied to the task definition to help you categorize and
    /// 			organize them. Each tag consists of a key and an optional value, both of which you
    /// 			define.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The full task definition description.</p>
    public let taskDefinition: TaskDefinition?

    public init (
        tags: [Tag]? = nil,
        taskDefinition: TaskDefinition? = nil
    )
    {
        self.tags = tags
        self.taskDefinition = taskDefinition
    }
}

struct DescribeTaskDefinitionOutputResponseBody: Equatable {
    public let taskDefinition: TaskDefinition?
    public let tags: [Tag]?
}

extension DescribeTaskDefinitionOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case tags
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(TaskDefinition.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

public struct DescribeTaskSetsInputBodyMiddleware: Middleware {
    public let id: String = "DescribeTaskSetsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTaskSetsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTaskSetsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTaskSetsInput>
    public typealias MOutput = OperationOutput<DescribeTaskSetsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTaskSetsOutputError>
}

extension DescribeTaskSetsInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeTaskSetsInput(cluster: \(String(describing: cluster)), include: \(String(describing: include)), service: \(String(describing: service)), taskSets: \(String(describing: taskSets)))"}
}

extension DescribeTaskSetsInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case include
        case service
        case taskSets
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for tasksetfieldlist0 in include {
                try includeContainer.encode(tasksetfieldlist0.rawValue)
            }
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
        if let taskSets = taskSets {
            var taskSetsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .taskSets)
            for stringlist0 in taskSets {
                try taskSetsContainer.encode(stringlist0)
            }
        }
    }
}

public struct DescribeTaskSetsInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeTaskSetsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTaskSetsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTaskSetsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTaskSetsInput>
    public typealias MOutput = OperationOutput<DescribeTaskSetsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTaskSetsOutputError>
}

public struct DescribeTaskSetsInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeTaskSetsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTaskSetsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTaskSetsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTaskSetsInput>
    public typealias MOutput = OperationOutput<DescribeTaskSetsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTaskSetsOutputError>
}

public struct DescribeTaskSetsInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task
    /// 			sets exist in.</p>
    public let cluster: String?
    /// <p>Specifies whether to see the resource tags for the task set. If <code>TAGS</code> is
    /// 			specified, the tags are included in the response. If this field is omitted, tags are not
    /// 			included in the response.</p>
    public let include: [TaskSetField]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the service that the task sets exist in.</p>
    public let service: String?
    /// <p>The ID or full Amazon Resource Name (ARN) of task sets to
    /// 			describe.</p>
    public let taskSets: [String]?

    public init (
        cluster: String? = nil,
        include: [TaskSetField]? = nil,
        service: String? = nil,
        taskSets: [String]? = nil
    )
    {
        self.cluster = cluster
        self.include = include
        self.service = service
        self.taskSets = taskSets
    }
}

struct DescribeTaskSetsInputBody: Equatable {
    public let cluster: String?
    public let service: String?
    public let taskSets: [String]?
    public let include: [TaskSetField]?
}

extension DescribeTaskSetsInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case include
        case service
        case taskSets
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let taskSetsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .taskSets)
        var taskSetsDecoded0:[String]? = nil
        if let taskSetsContainer = taskSetsContainer {
            taskSetsDecoded0 = [String]()
            for string0 in taskSetsContainer {
                if let string0 = string0 {
                    taskSetsDecoded0?.append(string0)
                }
            }
        }
        taskSets = taskSetsDecoded0
        let includeContainer = try containerValues.decodeIfPresent([TaskSetField?].self, forKey: .include)
        var includeDecoded0:[TaskSetField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [TaskSetField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
    }
}

extension DescribeTaskSetsOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeTaskSetsOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotActiveException" : self = .serviceNotActiveException(try ServiceNotActiveException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeTaskSetsOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case serviceNotActiveException(ServiceNotActiveException)
    case serviceNotFoundException(ServiceNotFoundException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeTaskSetsOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeTaskSetsOutputResponse(failures: \(String(describing: failures)), taskSets: \(String(describing: taskSets)))"}
}

extension DescribeTaskSetsOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeTaskSetsOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.failures = output.failures
            self.taskSets = output.taskSets
        } else {
            self.failures = nil
            self.taskSets = nil
        }
    }
}

public struct DescribeTaskSetsOutputResponse: Equatable {
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?
    /// <p>The list of task sets described.</p>
    public let taskSets: [TaskSet]?

    public init (
        failures: [Failure]? = nil,
        taskSets: [TaskSet]? = nil
    )
    {
        self.failures = failures
        self.taskSets = taskSets
    }
}

struct DescribeTaskSetsOutputResponseBody: Equatable {
    public let taskSets: [TaskSet]?
    public let failures: [Failure]?
}

extension DescribeTaskSetsOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case failures
        case taskSets
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskSetsContainer = try containerValues.decodeIfPresent([TaskSet?].self, forKey: .taskSets)
        var taskSetsDecoded0:[TaskSet]? = nil
        if let taskSetsContainer = taskSetsContainer {
            taskSetsDecoded0 = [TaskSet]()
            for structure0 in taskSetsContainer {
                if let structure0 = structure0 {
                    taskSetsDecoded0?.append(structure0)
                }
            }
        }
        taskSets = taskSetsDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

public struct DescribeTasksInputBodyMiddleware: Middleware {
    public let id: String = "DescribeTasksInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTasksInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTasksOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTasksInput>
    public typealias MOutput = OperationOutput<DescribeTasksOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTasksOutputError>
}

extension DescribeTasksInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeTasksInput(cluster: \(String(describing: cluster)), include: \(String(describing: include)), tasks: \(String(describing: tasks)))"}
}

extension DescribeTasksInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case include
        case tasks
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let include = include {
            var includeContainer = encodeContainer.nestedUnkeyedContainer(forKey: .include)
            for taskfieldlist0 in include {
                try includeContainer.encode(taskfieldlist0.rawValue)
            }
        }
        if let tasks = tasks {
            var tasksContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tasks)
            for stringlist0 in tasks {
                try tasksContainer.encode(stringlist0)
            }
        }
    }
}

public struct DescribeTasksInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeTasksInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTasksInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTasksOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTasksInput>
    public typealias MOutput = OperationOutput<DescribeTasksOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTasksOutputError>
}

public struct DescribeTasksInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeTasksInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeTasksInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeTasksOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeTasksInput>
    public typealias MOutput = OperationOutput<DescribeTasksOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeTasksOutputError>
}

public struct DescribeTasksInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the task or tasks to
    /// 			describe. If you do not specify a cluster, the default cluster is assumed. This parameter is required if the task or tasks you
    /// 			are describing were launched in any cluster other than the default cluster.</p>
    public let cluster: String?
    /// <p>Specifies whether you want to see the resource tags for the task. If <code>TAGS</code>
    /// 			is specified, the tags are included in the response. If this field is omitted, tags are
    /// 			not included in the response.</p>
    public let include: [TaskField]?
    /// <p>A list of up to 100 task IDs or full ARN entries.</p>
    public let tasks: [String]?

    public init (
        cluster: String? = nil,
        include: [TaskField]? = nil,
        tasks: [String]? = nil
    )
    {
        self.cluster = cluster
        self.include = include
        self.tasks = tasks
    }
}

struct DescribeTasksInputBody: Equatable {
    public let cluster: String?
    public let tasks: [String]?
    public let include: [TaskField]?
}

extension DescribeTasksInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case include
        case tasks
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let tasksContainer = try containerValues.decodeIfPresent([String?].self, forKey: .tasks)
        var tasksDecoded0:[String]? = nil
        if let tasksContainer = tasksContainer {
            tasksDecoded0 = [String]()
            for string0 in tasksContainer {
                if let string0 = string0 {
                    tasksDecoded0?.append(string0)
                }
            }
        }
        tasks = tasksDecoded0
        let includeContainer = try containerValues.decodeIfPresent([TaskField?].self, forKey: .include)
        var includeDecoded0:[TaskField]? = nil
        if let includeContainer = includeContainer {
            includeDecoded0 = [TaskField]()
            for string0 in includeContainer {
                if let string0 = string0 {
                    includeDecoded0?.append(string0)
                }
            }
        }
        include = includeDecoded0
    }
}

extension DescribeTasksOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DescribeTasksOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DescribeTasksOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DescribeTasksOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeTasksOutputResponse(failures: \(String(describing: failures)), tasks: \(String(describing: tasks)))"}
}

extension DescribeTasksOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DescribeTasksOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.failures = output.failures
            self.tasks = output.tasks
        } else {
            self.failures = nil
            self.tasks = nil
        }
    }
}

public struct DescribeTasksOutputResponse: Equatable {
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?
    /// <p>The list of tasks.</p>
    public let tasks: [Task]?

    public init (
        failures: [Failure]? = nil,
        tasks: [Task]? = nil
    )
    {
        self.failures = failures
        self.tasks = tasks
    }
}

struct DescribeTasksOutputResponseBody: Equatable {
    public let tasks: [Task]?
    public let failures: [Failure]?
}

extension DescribeTasksOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case failures
        case tasks
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let tasksContainer = try containerValues.decodeIfPresent([Task?].self, forKey: .tasks)
        var tasksDecoded0:[Task]? = nil
        if let tasksContainer = tasksContainer {
            tasksDecoded0 = [Task]()
            for structure0 in tasksContainer {
                if let structure0 = structure0 {
                    tasksDecoded0?.append(structure0)
                }
            }
        }
        tasks = tasksDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

public enum DesiredStatus {
    case pending
    case running
    case stopped
    case sdkUnknown(String)
}

extension DesiredStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [DesiredStatus] {
        return [
            .pending,
            .running,
            .stopped,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .pending: return "PENDING"
        case .running: return "RUNNING"
        case .stopped: return "STOPPED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = DesiredStatus(rawValue: rawValue) ?? DesiredStatus.sdkUnknown(rawValue)
    }
}

extension Device: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerPath
        case hostPath
        case permissions
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerPath = containerPath {
            try encodeContainer.encode(containerPath, forKey: .containerPath)
        }
        if let hostPath = hostPath {
            try encodeContainer.encode(hostPath, forKey: .hostPath)
        }
        if let permissions = permissions {
            var permissionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .permissions)
            for devicecgrouppermissions0 in permissions {
                try permissionsContainer.encode(devicecgrouppermissions0.rawValue)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let hostPathDecoded = try containerValues.decodeIfPresent(String.self, forKey: .hostPath)
        hostPath = hostPathDecoded
        let containerPathDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerPath)
        containerPath = containerPathDecoded
        let permissionsContainer = try containerValues.decodeIfPresent([DeviceCgroupPermission?].self, forKey: .permissions)
        var permissionsDecoded0:[DeviceCgroupPermission]? = nil
        if let permissionsContainer = permissionsContainer {
            permissionsDecoded0 = [DeviceCgroupPermission]()
            for string0 in permissionsContainer {
                if let string0 = string0 {
                    permissionsDecoded0?.append(string0)
                }
            }
        }
        permissions = permissionsDecoded0
    }
}

extension Device: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Device(containerPath: \(String(describing: containerPath)), hostPath: \(String(describing: hostPath)), permissions: \(String(describing: permissions)))"}
}

/// <p>An object representing a container instance host device.</p>
public struct Device: Equatable {
    /// <p>The path inside the container at which to expose the host device.</p>
    public let containerPath: String?
    /// <p>The path for the device on the host container instance.</p>
    public let hostPath: String?
    /// <p>The explicit permissions to provide to the container for the device. By default, the
    /// 			container has permissions for <code>read</code>, <code>write</code>, and
    /// 				<code>mknod</code> for the device.</p>
    public let permissions: [DeviceCgroupPermission]?

    public init (
        containerPath: String? = nil,
        hostPath: String? = nil,
        permissions: [DeviceCgroupPermission]? = nil
    )
    {
        self.containerPath = containerPath
        self.hostPath = hostPath
        self.permissions = permissions
    }
}

public enum DeviceCgroupPermission {
    case mknod
    case read
    case write
    case sdkUnknown(String)
}

extension DeviceCgroupPermission : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [DeviceCgroupPermission] {
        return [
            .mknod,
            .read,
            .write,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .mknod: return "mknod"
        case .read: return "read"
        case .write: return "write"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = DeviceCgroupPermission(rawValue: rawValue) ?? DeviceCgroupPermission.sdkUnknown(rawValue)
    }
}

public struct DiscoverPollEndpointInputBodyMiddleware: Middleware {
    public let id: String = "DiscoverPollEndpointInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DiscoverPollEndpointInput>,
                  next: H) -> Swift.Result<OperationOutput<DiscoverPollEndpointOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DiscoverPollEndpointInput>
    public typealias MOutput = OperationOutput<DiscoverPollEndpointOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DiscoverPollEndpointOutputError>
}

extension DiscoverPollEndpointInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DiscoverPollEndpointInput(cluster: \(String(describing: cluster)), containerInstance: \(String(describing: containerInstance)))"}
}

extension DiscoverPollEndpointInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstance = containerInstance {
            try encodeContainer.encode(containerInstance, forKey: .containerInstance)
        }
    }
}

public struct DiscoverPollEndpointInputHeadersMiddleware: Middleware {
    public let id: String = "DiscoverPollEndpointInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DiscoverPollEndpointInput>,
                  next: H) -> Swift.Result<OperationOutput<DiscoverPollEndpointOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DiscoverPollEndpointInput>
    public typealias MOutput = OperationOutput<DiscoverPollEndpointOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DiscoverPollEndpointOutputError>
}

public struct DiscoverPollEndpointInputQueryItemMiddleware: Middleware {
    public let id: String = "DiscoverPollEndpointInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DiscoverPollEndpointInput>,
                  next: H) -> Swift.Result<OperationOutput<DiscoverPollEndpointOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DiscoverPollEndpointInput>
    public typealias MOutput = OperationOutput<DiscoverPollEndpointOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DiscoverPollEndpointOutputError>
}

public struct DiscoverPollEndpointInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster to which the container instance
    /// 			belongs.</p>
    public let cluster: String?
    /// <p>The container instance ID or full ARN of the container instance.
    /// 			The ARN contains the <code>arn:aws:ecs</code> namespace, followed by the Region of the container instance, the AWS account ID of the container instance owner, the <code>container-instance</code> namespace, and then the container instance ID. For example, <code>arn:aws:ecs:region:aws_account_id:container-instance/container_instance_ID</code>.</p>
    public let containerInstance: String?

    public init (
        cluster: String? = nil,
        containerInstance: String? = nil
    )
    {
        self.cluster = cluster
        self.containerInstance = containerInstance
    }
}

struct DiscoverPollEndpointInputBody: Equatable {
    public let containerInstance: String?
    public let cluster: String?
}

extension DiscoverPollEndpointInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstanceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

extension DiscoverPollEndpointOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension DiscoverPollEndpointOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum DiscoverPollEndpointOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension DiscoverPollEndpointOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DiscoverPollEndpointOutputResponse(endpoint: \(String(describing: endpoint)), telemetryEndpoint: \(String(describing: telemetryEndpoint)))"}
}

extension DiscoverPollEndpointOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: DiscoverPollEndpointOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.endpoint = output.endpoint
            self.telemetryEndpoint = output.telemetryEndpoint
        } else {
            self.endpoint = nil
            self.telemetryEndpoint = nil
        }
    }
}

public struct DiscoverPollEndpointOutputResponse: Equatable {
    /// <p>The endpoint for the Amazon ECS agent to poll.</p>
    public let endpoint: String?
    /// <p>The telemetry endpoint for the Amazon ECS agent.</p>
    public let telemetryEndpoint: String?

    public init (
        endpoint: String? = nil,
        telemetryEndpoint: String? = nil
    )
    {
        self.endpoint = endpoint
        self.telemetryEndpoint = telemetryEndpoint
    }
}

struct DiscoverPollEndpointOutputResponseBody: Equatable {
    public let endpoint: String?
    public let telemetryEndpoint: String?
}

extension DiscoverPollEndpointOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case endpoint
        case telemetryEndpoint
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let endpointDecoded = try containerValues.decodeIfPresent(String.self, forKey: .endpoint)
        endpoint = endpointDecoded
        let telemetryEndpointDecoded = try containerValues.decodeIfPresent(String.self, forKey: .telemetryEndpoint)
        telemetryEndpoint = telemetryEndpointDecoded
    }
}

extension DockerVolumeConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case autoprovision
        case driver
        case driverOpts
        case labels
        case scope
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let autoprovision = autoprovision {
            try encodeContainer.encode(autoprovision, forKey: .autoprovision)
        }
        if let driver = driver {
            try encodeContainer.encode(driver, forKey: .driver)
        }
        if let driverOpts = driverOpts {
            var driverOptsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .driverOpts)
            for (dictKey0, stringmap0) in driverOpts {
                try driverOptsContainer.encode(stringmap0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let labels = labels {
            var labelsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .labels)
            for (dictKey0, stringmap0) in labels {
                try labelsContainer.encode(stringmap0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let scope = scope {
            try encodeContainer.encode(scope.rawValue, forKey: .scope)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let scopeDecoded = try containerValues.decodeIfPresent(Scope.self, forKey: .scope)
        scope = scopeDecoded
        let autoprovisionDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .autoprovision)
        autoprovision = autoprovisionDecoded
        let driverDecoded = try containerValues.decodeIfPresent(String.self, forKey: .driver)
        driver = driverDecoded
        let driverOptsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .driverOpts)
        var driverOptsDecoded0: [String:String]? = nil
        if let driverOptsContainer = driverOptsContainer {
            driverOptsDecoded0 = [String:String]()
            for (key0, string0) in driverOptsContainer {
                if let string0 = string0 {
                    driverOptsDecoded0?[key0] = string0
                }
            }
        }
        driverOpts = driverOptsDecoded0
        let labelsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .labels)
        var labelsDecoded0: [String:String]? = nil
        if let labelsContainer = labelsContainer {
            labelsDecoded0 = [String:String]()
            for (key0, string0) in labelsContainer {
                if let string0 = string0 {
                    labelsDecoded0?[key0] = string0
                }
            }
        }
        labels = labelsDecoded0
    }
}

extension DockerVolumeConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DockerVolumeConfiguration(autoprovision: \(String(describing: autoprovision)), driver: \(String(describing: driver)), driverOpts: \(String(describing: driverOpts)), labels: \(String(describing: labels)), scope: \(String(describing: scope)))"}
}

/// <p>This parameter is specified when you are using Docker volumes. Docker volumes are only
/// 			supported when you are using the EC2 launch type. Windows containers only
/// 			support the use of the <code>local</code> driver. To use bind mounts, specify a
/// 				<code>host</code> instead.</p>
public struct DockerVolumeConfiguration: Equatable {
    /// <p>If this value is <code>true</code>, the Docker volume is created if it does not
    /// 			already exist.</p>
    /// 		       <note>
    /// 			         <p>This field is only used if the <code>scope</code> is <code>shared</code>.</p>
    /// 		       </note>
    public let autoprovision: Bool?
    /// <p>The Docker volume driver to use. The driver value must match the driver name provided
    /// 			by Docker because it is used for task placement. If the driver was installed using the
    /// 			Docker plugin CLI, use <code>docker plugin ls</code> to retrieve the driver name from
    /// 			your container instance. If the driver was installed using another method, use Docker
    /// 			plugin discovery to retrieve the driver name. For more information, see <a href="https://docs.docker.com/engine/extend/plugin_api/#plugin-discovery">Docker
    /// 				plugin discovery</a>. This parameter maps to <code>Driver</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/VolumeCreate">Create a volume</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>xxdriver</code> option to <a href="https://docs.docker.com/engine/reference/commandline/volume_create/">docker
    /// 				volume create</a>.</p>
    public let driver: String?
    /// <p>A map of Docker driver-specific options passed through. This parameter maps to
    /// 				<code>DriverOpts</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/VolumeCreate">Create a volume</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>xxopt</code> option to <a href="https://docs.docker.com/engine/reference/commandline/volume_create/">docker
    /// 				volume create</a>.</p>
    public let driverOpts: [String:String]?
    /// <p>Custom metadata to add to your Docker volume. This parameter maps to
    /// 				<code>Labels</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/VolumeCreate">Create a volume</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>xxlabel</code> option to <a href="https://docs.docker.com/engine/reference/commandline/volume_create/">docker
    /// 				volume create</a>.</p>
    public let labels: [String:String]?
    /// <p>The scope for the Docker volume that determines its lifecycle. Docker volumes that are
    /// 			scoped to a <code>task</code> are automatically provisioned when the task starts and
    /// 			destroyed when the task stops. Docker volumes that are scoped as <code>shared</code>
    /// 			persist after the task stops.</p>
    public let scope: Scope?

    public init (
        autoprovision: Bool? = nil,
        driver: String? = nil,
        driverOpts: [String:String]? = nil,
        labels: [String:String]? = nil,
        scope: Scope? = nil
    )
    {
        self.autoprovision = autoprovision
        self.driver = driver
        self.driverOpts = driverOpts
        self.labels = labels
        self.scope = scope
    }
}

extension EFSAuthorizationConfig: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case accessPointId
        case iam
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let accessPointId = accessPointId {
            try encodeContainer.encode(accessPointId, forKey: .accessPointId)
        }
        if let iam = iam {
            try encodeContainer.encode(iam.rawValue, forKey: .iam)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let accessPointIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .accessPointId)
        accessPointId = accessPointIdDecoded
        let iamDecoded = try containerValues.decodeIfPresent(EFSAuthorizationConfigIAM.self, forKey: .iam)
        iam = iamDecoded
    }
}

extension EFSAuthorizationConfig: CustomDebugStringConvertible {
    public var debugDescription: String {
        "EFSAuthorizationConfig(accessPointId: \(String(describing: accessPointId)), iam: \(String(describing: iam)))"}
}

/// <p>The authorization configuration details for the Amazon EFS file system.</p>
public struct EFSAuthorizationConfig: Equatable {
    /// <p>The Amazon EFS access point ID to use. If an access point is specified, the root directory
    /// 			value specified in the <code>EFSVolumeConfiguration</code> must either be omitted or set
    /// 			to <code>/</code> which will enforce the path set on the EFS access point. If an access
    /// 			point is used, transit encryption must be enabled in the
    /// 				<code>EFSVolumeConfiguration</code>. For more information, see <a href="https://docs.aws.amazon.com/efs/latest/ug/efs-access-points.html">Working with Amazon
    /// 				EFS Access Points</a> in the <i>Amazon Elastic File System User Guide</i>.</p>
    public let accessPointId: String?
    /// <p>Whether or not to use the Amazon ECS task IAM role defined in a task definition when
    /// 			mounting the Amazon EFS file system. If enabled, transit encryption must be enabled in the
    /// 				<code>EFSVolumeConfiguration</code>. If this parameter is omitted, the default value
    /// 			of <code>DISABLED</code> is used. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/efs-volumes.html#efs-volume-accesspoints">Using
    /// 				Amazon EFS Access Points</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let iam: EFSAuthorizationConfigIAM?

    public init (
        accessPointId: String? = nil,
        iam: EFSAuthorizationConfigIAM? = nil
    )
    {
        self.accessPointId = accessPointId
        self.iam = iam
    }
}

public enum EFSAuthorizationConfigIAM {
    case disabled
    case enabled
    case sdkUnknown(String)
}

extension EFSAuthorizationConfigIAM : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [EFSAuthorizationConfigIAM] {
        return [
            .disabled,
            .enabled,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .disabled: return "DISABLED"
        case .enabled: return "ENABLED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = EFSAuthorizationConfigIAM(rawValue: rawValue) ?? EFSAuthorizationConfigIAM.sdkUnknown(rawValue)
    }
}

public enum EFSTransitEncryption {
    case disabled
    case enabled
    case sdkUnknown(String)
}

extension EFSTransitEncryption : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [EFSTransitEncryption] {
        return [
            .disabled,
            .enabled,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .disabled: return "DISABLED"
        case .enabled: return "ENABLED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = EFSTransitEncryption(rawValue: rawValue) ?? EFSTransitEncryption.sdkUnknown(rawValue)
    }
}

extension EFSVolumeConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case authorizationConfig
        case fileSystemId
        case rootDirectory
        case transitEncryption
        case transitEncryptionPort
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let authorizationConfig = authorizationConfig {
            try encodeContainer.encode(authorizationConfig, forKey: .authorizationConfig)
        }
        if let fileSystemId = fileSystemId {
            try encodeContainer.encode(fileSystemId, forKey: .fileSystemId)
        }
        if let rootDirectory = rootDirectory {
            try encodeContainer.encode(rootDirectory, forKey: .rootDirectory)
        }
        if let transitEncryption = transitEncryption {
            try encodeContainer.encode(transitEncryption.rawValue, forKey: .transitEncryption)
        }
        if let transitEncryptionPort = transitEncryptionPort {
            try encodeContainer.encode(transitEncryptionPort, forKey: .transitEncryptionPort)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let fileSystemIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .fileSystemId)
        fileSystemId = fileSystemIdDecoded
        let rootDirectoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .rootDirectory)
        rootDirectory = rootDirectoryDecoded
        let transitEncryptionDecoded = try containerValues.decodeIfPresent(EFSTransitEncryption.self, forKey: .transitEncryption)
        transitEncryption = transitEncryptionDecoded
        let transitEncryptionPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .transitEncryptionPort)
        transitEncryptionPort = transitEncryptionPortDecoded
        let authorizationConfigDecoded = try containerValues.decodeIfPresent(EFSAuthorizationConfig.self, forKey: .authorizationConfig)
        authorizationConfig = authorizationConfigDecoded
    }
}

extension EFSVolumeConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "EFSVolumeConfiguration(authorizationConfig: \(String(describing: authorizationConfig)), fileSystemId: \(String(describing: fileSystemId)), rootDirectory: \(String(describing: rootDirectory)), transitEncryption: \(String(describing: transitEncryption)), transitEncryptionPort: \(String(describing: transitEncryptionPort)))"}
}

/// <p>This parameter is specified when you are using an Amazon Elastic File System file system for task
/// 			storage. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/efs-volumes.html">Amazon EFS Volumes</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct EFSVolumeConfiguration: Equatable {
    /// <p>The authorization configuration details for the Amazon EFS file system.</p>
    public let authorizationConfig: EFSAuthorizationConfig?
    /// <p>The Amazon EFS file system ID to use.</p>
    public let fileSystemId: String?
    /// <p>The directory within the Amazon EFS file system to mount as the root directory inside the
    /// 			host. If this parameter is omitted, the root of the Amazon EFS volume will be used.
    /// 			Specifying <code>/</code> will have the same effect as omitting this parameter.</p>
    /// 		       <important>
    /// 			         <p>If an EFS access point is specified in the <code>authorizationConfig</code>, the
    /// 				root directory parameter must either be omitted or set to <code>/</code> which will
    /// 				enforce the path set on the EFS access point.</p>
    /// 		       </important>
    public let rootDirectory: String?
    /// <p>Whether or not to enable encryption for Amazon EFS data in transit between the Amazon ECS host
    /// 			and the Amazon EFS server. Transit encryption must be enabled if Amazon EFS IAM authorization is
    /// 			used. If this parameter is omitted, the default value of <code>DISABLED</code> is used.
    /// 			For more information, see <a href="https://docs.aws.amazon.com/efs/latest/ug/encryption-in-transit.html">Encrypting Data in Transit</a> in
    /// 			the <i>Amazon Elastic File System User Guide</i>.</p>
    public let transitEncryption: EFSTransitEncryption?
    /// <p>The port to use when sending encrypted data between the Amazon ECS host and the Amazon EFS
    /// 			server. If you do not specify a transit encryption port, it will use the port selection
    /// 			strategy that the Amazon EFS mount helper uses. For more information, see <a href="https://docs.aws.amazon.com/efs/latest/ug/efs-mount-helper.html">EFS Mount
    /// 				Helper</a> in the <i>Amazon Elastic File System User Guide</i>.</p>
    public let transitEncryptionPort: Int?

    public init (
        authorizationConfig: EFSAuthorizationConfig? = nil,
        fileSystemId: String? = nil,
        rootDirectory: String? = nil,
        transitEncryption: EFSTransitEncryption? = nil,
        transitEncryptionPort: Int? = nil
    )
    {
        self.authorizationConfig = authorizationConfig
        self.fileSystemId = fileSystemId
        self.rootDirectory = rootDirectory
        self.transitEncryption = transitEncryption
        self.transitEncryptionPort = transitEncryptionPort
    }
}

extension EnvironmentFile: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case type
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let typeDecoded = try containerValues.decodeIfPresent(EnvironmentFileType.self, forKey: .type)
        type = typeDecoded
    }
}

extension EnvironmentFile: CustomDebugStringConvertible {
    public var debugDescription: String {
        "EnvironmentFile(type: \(String(describing: type)), value: \(String(describing: value)))"}
}

/// <p>A list of files containing the environment variables to pass to a container. You can
/// 			specify up to ten environment files. The file must have a <code>.env</code> file
/// 			extension. Each line in an environment file should contain an environment variable in
/// 				<code>VARIABLE=VALUE</code> format. Lines beginning with <code>#</code> are treated
/// 			as comments and are ignored. For more information on the environment variable file
/// 			syntax, see <a href="https://docs.docker.com/compose/env-file/">Declare default
/// 				environment variables in file</a>.</p>
/// 		       <p>If there are environment variables specified using the <code>environment</code>
/// 			parameter in a container definition, they take precedence over the variables contained
/// 			within an environment file. If multiple environment files are specified that contain the
/// 			same variable, they are processed from the top down. It is recommended to use unique
/// 			variable names. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/taskdef-envfiles.html">Specifying environment
/// 				variables</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
/// 		       <p>This field is only valid for containers in Fargate tasks that use
/// 			platform version <code>1.4.0</code> or later.</p>
public struct EnvironmentFile: Equatable {
    /// <p>The file type to use. The only supported value is <code>s3</code>.</p>
    public let type: EnvironmentFileType?
    /// <p>The Amazon Resource Name (ARN) of the Amazon S3 object containing the environment
    /// 			variable file.</p>
    public let value: String?

    public init (
        type: EnvironmentFileType? = nil,
        value: String? = nil
    )
    {
        self.type = type
        self.value = value
    }
}

public enum EnvironmentFileType {
    case s3
    case sdkUnknown(String)
}

extension EnvironmentFileType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [EnvironmentFileType] {
        return [
            .s3,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .s3: return "s3"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = EnvironmentFileType(rawValue: rawValue) ?? EnvironmentFileType.sdkUnknown(rawValue)
    }
}

extension EphemeralStorage: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case sizeInGiB
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if sizeInGiB != 0 {
            try encodeContainer.encode(sizeInGiB, forKey: .sizeInGiB)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let sizeInGiBDecoded = try containerValues.decode(Int.self, forKey: .sizeInGiB)
        sizeInGiB = sizeInGiBDecoded
    }
}

extension EphemeralStorage: CustomDebugStringConvertible {
    public var debugDescription: String {
        "EphemeralStorage(sizeInGiB: \(String(describing: sizeInGiB)))"}
}

/// <p>The amount of ephemeral storage to allocate for the task. This parameter is used to
/// 			expand the total amount of ephemeral storage available, beyond the default amount, for
/// 			tasks hosted on AWS Fargate. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/userguide/using_data_volumes.html">Fargate task
/// 				storage</a> in the <i>Amazon ECS User Guide for AWS Fargate</i>.</p>
/// 		       <note>
/// 			         <p>This parameter is only supported for tasks hosted on AWS Fargate using platform
/// 				version <code>1.4.0</code> or later.</p>
/// 		       </note>
public struct EphemeralStorage: Equatable {
    /// <p>The total amount, in GiB, of ephemeral storage to set for the task. The minimum
    /// 			supported value is <code>21</code> GiB and the maximum supported value is
    /// 				<code>200</code> GiB.</p>
    public let sizeInGiB: Int

    public init (
        sizeInGiB: Int = 0
    )
    {
        self.sizeInGiB = sizeInGiB
    }
}

extension ExecuteCommandConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case kmsKeyId
        case logConfiguration
        case logging
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let kmsKeyId = kmsKeyId {
            try encodeContainer.encode(kmsKeyId, forKey: .kmsKeyId)
        }
        if let logConfiguration = logConfiguration {
            try encodeContainer.encode(logConfiguration, forKey: .logConfiguration)
        }
        if let logging = logging {
            try encodeContainer.encode(logging.rawValue, forKey: .logging)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let kmsKeyIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .kmsKeyId)
        kmsKeyId = kmsKeyIdDecoded
        let loggingDecoded = try containerValues.decodeIfPresent(ExecuteCommandLogging.self, forKey: .logging)
        logging = loggingDecoded
        let logConfigurationDecoded = try containerValues.decodeIfPresent(ExecuteCommandLogConfiguration.self, forKey: .logConfiguration)
        logConfiguration = logConfigurationDecoded
    }
}

extension ExecuteCommandConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ExecuteCommandConfiguration(kmsKeyId: \(String(describing: kmsKeyId)), logConfiguration: \(String(describing: logConfiguration)), logging: \(String(describing: logging)))"}
}

/// <p>The details of the execute command configuration.</p>
public struct ExecuteCommandConfiguration: Equatable {
    /// <p>Specify an AWS Key Management Service key ID to encrypt the data between the local
    /// 			client and the container.</p>
    public let kmsKeyId: String?
    /// <p>The log configuration for the results of the execute command actions. The logs can be
    /// 			sent to CloudWatch Logs or an Amazon S3 bucket. When <code>logging=OVERRIDE</code> is
    /// 			specified, a <code>logConfiguration</code> must be provided.</p>
    public let logConfiguration: ExecuteCommandLogConfiguration?
    /// <p>The log setting to use for redirecting logs for your execute command results. The
    /// 			following log settings are available.</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    ///                   <code>NONE</code>: The execute command session is not logged.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>DEFAULT</code>: The <code>awslogs</code> configuration in the task
    /// 					definition is used. If no logging parameter is specified, it defaults to this
    /// 					value. If no <code>awslogs</code> log driver is configured in the task
    /// 					definition, the output won't be logged.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>OVERRIDE</code>: Specify the logging details as a part of
    /// 						<code>logConfiguration</code>. If the <code>OVERRIDE</code> logging option
    /// 					is specified, the <code>logConfiguration</code> is required.</p>
    /// 			         </li>
    ///          </ul>
    public let logging: ExecuteCommandLogging?

    public init (
        kmsKeyId: String? = nil,
        logConfiguration: ExecuteCommandLogConfiguration? = nil,
        logging: ExecuteCommandLogging? = nil
    )
    {
        self.kmsKeyId = kmsKeyId
        self.logConfiguration = logConfiguration
        self.logging = logging
    }
}

public struct ExecuteCommandInputBodyMiddleware: Middleware {
    public let id: String = "ExecuteCommandInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ExecuteCommandInput>,
                  next: H) -> Swift.Result<OperationOutput<ExecuteCommandOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ExecuteCommandInput>
    public typealias MOutput = OperationOutput<ExecuteCommandOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ExecuteCommandOutputError>
}

extension ExecuteCommandInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ExecuteCommandInput(cluster: \(String(describing: cluster)), command: \(String(describing: command)), container: \(String(describing: container)), interactive: \(String(describing: interactive)), task: \(String(describing: task)))"}
}

extension ExecuteCommandInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case command
        case container
        case interactive
        case task
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let command = command {
            try encodeContainer.encode(command, forKey: .command)
        }
        if let container = container {
            try encodeContainer.encode(container, forKey: .container)
        }
        if interactive != false {
            try encodeContainer.encode(interactive, forKey: .interactive)
        }
        if let task = task {
            try encodeContainer.encode(task, forKey: .task)
        }
    }
}

public struct ExecuteCommandInputHeadersMiddleware: Middleware {
    public let id: String = "ExecuteCommandInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ExecuteCommandInput>,
                  next: H) -> Swift.Result<OperationOutput<ExecuteCommandOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ExecuteCommandInput>
    public typealias MOutput = OperationOutput<ExecuteCommandOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ExecuteCommandOutputError>
}

public struct ExecuteCommandInputQueryItemMiddleware: Middleware {
    public let id: String = "ExecuteCommandInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ExecuteCommandInput>,
                  next: H) -> Swift.Result<OperationOutput<ExecuteCommandOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ExecuteCommandInput>
    public typealias MOutput = OperationOutput<ExecuteCommandOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ExecuteCommandOutputError>
}

public struct ExecuteCommandInput: Equatable {
    /// <p>The Amazon Resource Name (ARN) or short name of the cluster the task is running in.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The command to run on the container.</p>
    public let command: String?
    /// <p>The name of the container to execute the command on. A container name only needs to be
    /// 			specified for tasks containing multiple containers.</p>
    public let container: String?
    /// <p>Use this flag to run your command in interactive mode.</p>
    public let interactive: Bool
    /// <p>The Amazon Resource Name (ARN) or ID of the task the container is part of.</p>
    public let task: String?

    public init (
        cluster: String? = nil,
        command: String? = nil,
        container: String? = nil,
        interactive: Bool = false,
        task: String? = nil
    )
    {
        self.cluster = cluster
        self.command = command
        self.container = container
        self.interactive = interactive
        self.task = task
    }
}

struct ExecuteCommandInputBody: Equatable {
    public let cluster: String?
    public let container: String?
    public let command: String?
    public let interactive: Bool
    public let task: String?
}

extension ExecuteCommandInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case command
        case container
        case interactive
        case task
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerDecoded = try containerValues.decodeIfPresent(String.self, forKey: .container)
        container = containerDecoded
        let commandDecoded = try containerValues.decodeIfPresent(String.self, forKey: .command)
        command = commandDecoded
        let interactiveDecoded = try containerValues.decode(Bool.self, forKey: .interactive)
        interactive = interactiveDecoded
        let taskDecoded = try containerValues.decodeIfPresent(String.self, forKey: .task)
        task = taskDecoded
    }
}

extension ExecuteCommandLogConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cloudWatchEncryptionEnabled
        case cloudWatchLogGroupName
        case s3BucketName
        case s3EncryptionEnabled
        case s3KeyPrefix
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if cloudWatchEncryptionEnabled != false {
            try encodeContainer.encode(cloudWatchEncryptionEnabled, forKey: .cloudWatchEncryptionEnabled)
        }
        if let cloudWatchLogGroupName = cloudWatchLogGroupName {
            try encodeContainer.encode(cloudWatchLogGroupName, forKey: .cloudWatchLogGroupName)
        }
        if let s3BucketName = s3BucketName {
            try encodeContainer.encode(s3BucketName, forKey: .s3BucketName)
        }
        if s3EncryptionEnabled != false {
            try encodeContainer.encode(s3EncryptionEnabled, forKey: .s3EncryptionEnabled)
        }
        if let s3KeyPrefix = s3KeyPrefix {
            try encodeContainer.encode(s3KeyPrefix, forKey: .s3KeyPrefix)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let cloudWatchLogGroupNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cloudWatchLogGroupName)
        cloudWatchLogGroupName = cloudWatchLogGroupNameDecoded
        let cloudWatchEncryptionEnabledDecoded = try containerValues.decode(Bool.self, forKey: .cloudWatchEncryptionEnabled)
        cloudWatchEncryptionEnabled = cloudWatchEncryptionEnabledDecoded
        let s3BucketNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .s3BucketName)
        s3BucketName = s3BucketNameDecoded
        let s3EncryptionEnabledDecoded = try containerValues.decode(Bool.self, forKey: .s3EncryptionEnabled)
        s3EncryptionEnabled = s3EncryptionEnabledDecoded
        let s3KeyPrefixDecoded = try containerValues.decodeIfPresent(String.self, forKey: .s3KeyPrefix)
        s3KeyPrefix = s3KeyPrefixDecoded
    }
}

extension ExecuteCommandLogConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ExecuteCommandLogConfiguration(cloudWatchEncryptionEnabled: \(String(describing: cloudWatchEncryptionEnabled)), cloudWatchLogGroupName: \(String(describing: cloudWatchLogGroupName)), s3BucketName: \(String(describing: s3BucketName)), s3EncryptionEnabled: \(String(describing: s3EncryptionEnabled)), s3KeyPrefix: \(String(describing: s3KeyPrefix)))"}
}

/// <p>The log configuration for the results of the execute command actions. The logs can be
/// 			sent to CloudWatch Logs or an Amazon S3 bucket.</p>
public struct ExecuteCommandLogConfiguration: Equatable {
    /// <p>Whether or not to enable encryption on the CloudWatch logs. If not specified,
    /// 			encryption will be disabled.</p>
    public let cloudWatchEncryptionEnabled: Bool
    /// <p>The name of the CloudWatch log group to send logs to.</p>
    /// 		       <note>
    /// 			         <p>The CloudWatch log group must already be created.</p>
    /// 		       </note>
    public let cloudWatchLogGroupName: String?
    /// <p>The name of the S3 bucket to send logs to.</p>
    /// 		       <note>
    /// 			         <p>The S3 bucket must already be created.</p>
    /// 		       </note>
    public let s3BucketName: String?
    /// <p>Whether or not to enable encryption on the CloudWatch logs. If not specified,
    /// 			encryption will be disabled.</p>
    public let s3EncryptionEnabled: Bool
    /// <p>An optional folder in the S3 bucket to place logs in.</p>
    public let s3KeyPrefix: String?

    public init (
        cloudWatchEncryptionEnabled: Bool = false,
        cloudWatchLogGroupName: String? = nil,
        s3BucketName: String? = nil,
        s3EncryptionEnabled: Bool = false,
        s3KeyPrefix: String? = nil
    )
    {
        self.cloudWatchEncryptionEnabled = cloudWatchEncryptionEnabled
        self.cloudWatchLogGroupName = cloudWatchLogGroupName
        self.s3BucketName = s3BucketName
        self.s3EncryptionEnabled = s3EncryptionEnabled
        self.s3KeyPrefix = s3KeyPrefix
    }
}

public enum ExecuteCommandLogging {
    case `default`
    case `none`
    case `override`
    case sdkUnknown(String)
}

extension ExecuteCommandLogging : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ExecuteCommandLogging] {
        return [
            .default,
            .none,
            .override,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .default: return "DEFAULT"
        case .none: return "NONE"
        case .override: return "OVERRIDE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ExecuteCommandLogging(rawValue: rawValue) ?? ExecuteCommandLogging.sdkUnknown(rawValue)
    }
}

extension ExecuteCommandOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ExecuteCommandOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "TargetNotConnectedException" : self = .targetNotConnectedException(try TargetNotConnectedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ExecuteCommandOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case targetNotConnectedException(TargetNotConnectedException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ExecuteCommandOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ExecuteCommandOutputResponse(clusterArn: \(String(describing: clusterArn)), containerArn: \(String(describing: containerArn)), containerName: \(String(describing: containerName)), interactive: \(String(describing: interactive)), session: \(String(describing: session)), taskArn: \(String(describing: taskArn)))"}
}

extension ExecuteCommandOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ExecuteCommandOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.clusterArn = output.clusterArn
            self.containerArn = output.containerArn
            self.containerName = output.containerName
            self.interactive = output.interactive
            self.session = output.session
            self.taskArn = output.taskArn
        } else {
            self.clusterArn = nil
            self.containerArn = nil
            self.containerName = nil
            self.interactive = false
            self.session = nil
            self.taskArn = nil
        }
    }
}

public struct ExecuteCommandOutputResponse: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the cluster.</p>
    public let clusterArn: String?
    /// <p>The Amazon Resource Name (ARN) of the container.</p>
    public let containerArn: String?
    /// <p>The name of the container.</p>
    public let containerName: String?
    /// <p>Whether or not the execute command session is running in interactive mode. Amazon ECS only
    /// 			supports initiating interactive sessions, so you must specify <code>true</code> for this
    /// 			value.</p>
    public let interactive: Bool
    /// <p>The details of the SSM session that was created for this instance of
    /// 			execute-command.</p>
    public let session: Session?
    /// <p>The Amazon Resource Name (ARN) of the task.</p>
    public let taskArn: String?

    public init (
        clusterArn: String? = nil,
        containerArn: String? = nil,
        containerName: String? = nil,
        interactive: Bool = false,
        session: Session? = nil,
        taskArn: String? = nil
    )
    {
        self.clusterArn = clusterArn
        self.containerArn = containerArn
        self.containerName = containerName
        self.interactive = interactive
        self.session = session
        self.taskArn = taskArn
    }
}

struct ExecuteCommandOutputResponseBody: Equatable {
    public let clusterArn: String?
    public let containerArn: String?
    public let containerName: String?
    public let interactive: Bool
    public let session: Session?
    public let taskArn: String?
}

extension ExecuteCommandOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case clusterArn
        case containerArn
        case containerName
        case interactive
        case session
        case taskArn
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterArn)
        clusterArn = clusterArnDecoded
        let containerArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerArn)
        containerArn = containerArnDecoded
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let interactiveDecoded = try containerValues.decode(Bool.self, forKey: .interactive)
        interactive = interactiveDecoded
        let sessionDecoded = try containerValues.decodeIfPresent(Session.self, forKey: .session)
        session = sessionDecoded
        let taskArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskArn)
        taskArn = taskArnDecoded
    }
}

extension FSxWindowsFileServerAuthorizationConfig: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case credentialsParameter
        case domain
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let credentialsParameter = credentialsParameter {
            try encodeContainer.encode(credentialsParameter, forKey: .credentialsParameter)
        }
        if let domain = domain {
            try encodeContainer.encode(domain, forKey: .domain)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let credentialsParameterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .credentialsParameter)
        credentialsParameter = credentialsParameterDecoded
        let domainDecoded = try containerValues.decodeIfPresent(String.self, forKey: .domain)
        domain = domainDecoded
    }
}

extension FSxWindowsFileServerAuthorizationConfig: CustomDebugStringConvertible {
    public var debugDescription: String {
        "FSxWindowsFileServerAuthorizationConfig(credentialsParameter: \(String(describing: credentialsParameter)), domain: \(String(describing: domain)))"}
}

/// <p>The authorization configuration details for Amazon FSx for Windows File Server file system. See <a href="https://docs.aws.amazon.com/AmazonECS/latest/APIReference/API_FSxWindowsFileServerVolumeConfiguration.html">FSxWindowsFileServerVolumeConfiguration</a> in the <i>Amazon Elastic
/// 				Container Service API Reference</i>.</p>
/// 		       <p>For more information and the input format, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/wfsx-volumes.html">Amazon FSx for Windows File Server Volumes</a>
/// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct FSxWindowsFileServerAuthorizationConfig: Equatable {
    /// <p>The authorization credential option to use. The authorization credential options can
    /// 			be provided using either the Amazon Resource Name (ARN) of an AWS Secrets Manager secret or AWS Systems Manager
    /// 			Parameter Store parameter. The ARNs refer to the stored credentials.</p>
    public let credentialsParameter: String?
    /// <p>A fully qualified domain name hosted by an <a href="https://docs.aws.amazon.com/directoryservice/latest/admin-guide/directory_microsoft_ad.html">AWS Directory Service</a> Managed Microsoft AD (Active Directory) or self-hosted AD on
    /// 			Amazon EC2.</p>
    public let domain: String?

    public init (
        credentialsParameter: String? = nil,
        domain: String? = nil
    )
    {
        self.credentialsParameter = credentialsParameter
        self.domain = domain
    }
}

extension FSxWindowsFileServerVolumeConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case authorizationConfig
        case fileSystemId
        case rootDirectory
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let authorizationConfig = authorizationConfig {
            try encodeContainer.encode(authorizationConfig, forKey: .authorizationConfig)
        }
        if let fileSystemId = fileSystemId {
            try encodeContainer.encode(fileSystemId, forKey: .fileSystemId)
        }
        if let rootDirectory = rootDirectory {
            try encodeContainer.encode(rootDirectory, forKey: .rootDirectory)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let fileSystemIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .fileSystemId)
        fileSystemId = fileSystemIdDecoded
        let rootDirectoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .rootDirectory)
        rootDirectory = rootDirectoryDecoded
        let authorizationConfigDecoded = try containerValues.decodeIfPresent(FSxWindowsFileServerAuthorizationConfig.self, forKey: .authorizationConfig)
        authorizationConfig = authorizationConfigDecoded
    }
}

extension FSxWindowsFileServerVolumeConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "FSxWindowsFileServerVolumeConfiguration(authorizationConfig: \(String(describing: authorizationConfig)), fileSystemId: \(String(describing: fileSystemId)), rootDirectory: \(String(describing: rootDirectory)))"}
}

/// <p>This parameter is specified when you are using <a href="https://docs.aws.amazon.com/fsx/latest/WindowsGuide/what-is.html">Amazon FSx for Windows File Server</a> file system for task
/// 			storage.</p>
/// 		       <p>For more information and the input format, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/wfsx-volumes.html">Amazon FSx for Windows File Server Volumes</a>
/// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct FSxWindowsFileServerVolumeConfiguration: Equatable {
    /// <p>The authorization configuration details for the Amazon FSx for Windows File Server file system.</p>
    public let authorizationConfig: FSxWindowsFileServerAuthorizationConfig?
    /// <p>The Amazon FSx for Windows File Server file system ID to use.</p>
    public let fileSystemId: String?
    /// <p>The directory within the Amazon FSx for Windows File Server file system to mount as the root directory
    /// 			inside the host.</p>
    public let rootDirectory: String?

    public init (
        authorizationConfig: FSxWindowsFileServerAuthorizationConfig? = nil,
        fileSystemId: String? = nil,
        rootDirectory: String? = nil
    )
    {
        self.authorizationConfig = authorizationConfig
        self.fileSystemId = fileSystemId
        self.rootDirectory = rootDirectory
    }
}

extension Failure: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case arn
        case detail
        case reason
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let arn = arn {
            try encodeContainer.encode(arn, forKey: .arn)
        }
        if let detail = detail {
            try encodeContainer.encode(detail, forKey: .detail)
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let arnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .arn)
        arn = arnDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
        let detailDecoded = try containerValues.decodeIfPresent(String.self, forKey: .detail)
        detail = detailDecoded
    }
}

extension Failure: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Failure(arn: \(String(describing: arn)), detail: \(String(describing: detail)), reason: \(String(describing: reason)))"}
}

/// <p>A failed resource. For a list of common causes, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/api_failures_messages.html">API failure
/// 				reasons</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct Failure: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the failed resource.</p>
    public let arn: String?
    /// <p>The details of the failure.</p>
    public let detail: String?
    /// <p>The reason for the failure.</p>
    public let reason: String?

    public init (
        arn: String? = nil,
        detail: String? = nil,
        reason: String? = nil
    )
    {
        self.arn = arn
        self.detail = detail
        self.reason = reason
    }
}

extension FirelensConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case options
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let options = options {
            var optionsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .options)
            for (dictKey0, firelensconfigurationoptionsmap0) in options {
                try optionsContainer.encode(firelensconfigurationoptionsmap0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let typeDecoded = try containerValues.decodeIfPresent(FirelensConfigurationType.self, forKey: .type)
        type = typeDecoded
        let optionsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .options)
        var optionsDecoded0: [String:String]? = nil
        if let optionsContainer = optionsContainer {
            optionsDecoded0 = [String:String]()
            for (key0, string0) in optionsContainer {
                if let string0 = string0 {
                    optionsDecoded0?[key0] = string0
                }
            }
        }
        options = optionsDecoded0
    }
}

extension FirelensConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "FirelensConfiguration(options: \(String(describing: options)), type: \(String(describing: type)))"}
}

/// <p>The FireLens configuration for the container. This is used to specify and configure a
/// 			log router for container logs. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_firelens.html">Custom Log Routing</a>
/// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct FirelensConfiguration: Equatable {
    /// <p>The options to use when configuring the log router. This field is optional and can be
    /// 			used to specify a custom configuration file or to add additional metadata, such as the
    /// 			task, task definition, cluster, and container instance details to the log event. If
    /// 			specified, the syntax to use is
    /// 				<code>"options":{"enable-ecs-log-metadata":"true|false","config-file-type:"s3|file","config-file-value":"arn:aws:s3:::mybucket/fluent.conf|filepath"}</code>.
    /// 			For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_firelens.html#firelens-taskdef">Creating
    /// 				a Task Definition that Uses a FireLens Configuration</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <note>
    /// 			         <p>Tasks hosted on AWS Fargate only support the <code>file</code> configuration file
    /// 				type.</p>
    /// 		       </note>
    public let options: [String:String]?
    /// <p>The log router to use. The valid values are <code>fluentd</code> or
    /// 				<code>fluentbit</code>.</p>
    public let type: FirelensConfigurationType?

    public init (
        options: [String:String]? = nil,
        type: FirelensConfigurationType? = nil
    )
    {
        self.options = options
        self.type = type
    }
}

public enum FirelensConfigurationType {
    case fluentbit
    case fluentd
    case sdkUnknown(String)
}

extension FirelensConfigurationType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [FirelensConfigurationType] {
        return [
            .fluentbit,
            .fluentd,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .fluentbit: return "fluentbit"
        case .fluentd: return "fluentd"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = FirelensConfigurationType(rawValue: rawValue) ?? FirelensConfigurationType.sdkUnknown(rawValue)
    }
}

extension HealthCheck: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case command
        case interval
        case retries
        case startPeriod
        case timeout
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let command = command {
            var commandContainer = encodeContainer.nestedUnkeyedContainer(forKey: .command)
            for stringlist0 in command {
                try commandContainer.encode(stringlist0)
            }
        }
        if let interval = interval {
            try encodeContainer.encode(interval, forKey: .interval)
        }
        if let retries = retries {
            try encodeContainer.encode(retries, forKey: .retries)
        }
        if let startPeriod = startPeriod {
            try encodeContainer.encode(startPeriod, forKey: .startPeriod)
        }
        if let timeout = timeout {
            try encodeContainer.encode(timeout, forKey: .timeout)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let commandContainer = try containerValues.decodeIfPresent([String?].self, forKey: .command)
        var commandDecoded0:[String]? = nil
        if let commandContainer = commandContainer {
            commandDecoded0 = [String]()
            for string0 in commandContainer {
                if let string0 = string0 {
                    commandDecoded0?.append(string0)
                }
            }
        }
        command = commandDecoded0
        let intervalDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .interval)
        interval = intervalDecoded
        let timeoutDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .timeout)
        timeout = timeoutDecoded
        let retriesDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .retries)
        retries = retriesDecoded
        let startPeriodDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .startPeriod)
        startPeriod = startPeriodDecoded
    }
}

extension HealthCheck: CustomDebugStringConvertible {
    public var debugDescription: String {
        "HealthCheck(command: \(String(describing: command)), interval: \(String(describing: interval)), retries: \(String(describing: retries)), startPeriod: \(String(describing: startPeriod)), timeout: \(String(describing: timeout)))"}
}

/// <p>An object representing a container health check. Health check parameters that are
/// 			specified in a container definition override any Docker health checks that exist in the
/// 			container image (such as those specified in a parent image or from the image's
/// 			Dockerfile).</p>
/// 		       <p>You can view the health status of both individual containers and a task with the
/// 			DescribeTasks API operation or when viewing the task details in the console.</p>
/// 		       <p>The following describes the possible <code>healthStatus</code> values for a
/// 			container:</p>
/// 		       <ul>
///             <li>
/// 				           <p>
///                   <code>HEALTHY</code>-The container health check has passed
/// 					successfully.</p>
/// 			         </li>
///             <li>
/// 				           <p>
///                   <code>UNHEALTHY</code>-The container health check has failed.</p>
/// 			         </li>
///             <li>
/// 				           <p>
///                   <code>UNKNOWN</code>-The container health check is being evaluated or
/// 					there is no container health check defined.</p>
/// 			         </li>
///          </ul>
/// 		       <p>The following describes the possible <code>healthStatus</code> values for a task. The
/// 			container health check status of nonessential containers do not have an effect on the
/// 			health status of a task.</p>
/// 		       <ul>
///             <li>
/// 				           <p>
///                   <code>HEALTHY</code>-All essential containers within the task have
/// 					passed their health checks.</p>
/// 			         </li>
///             <li>
/// 				           <p>
///                   <code>UNHEALTHY</code>-One or more essential containers have failed
/// 					their health check.</p>
/// 			         </li>
///             <li>
/// 				           <p>
///                   <code>UNKNOWN</code>-The essential containers within the task are still
/// 					having their health checks evaluated or there are no container health checks
/// 					defined.</p>
/// 			         </li>
///          </ul>
/// 		       <p>If a task is run manually, and not as part of a service, the task will continue its
/// 			lifecycle regardless of its health status. For tasks that are part of a service, if the
/// 			task reports as unhealthy then the task will be stopped and the service scheduler will
/// 			replace it.</p>
/// 		       <p>The following are notes about container health check support:</p>
/// 		       <ul>
///             <li>
/// 				           <p>Container health checks require version 1.17.0 or greater of the Amazon ECS
/// 					container agent. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-update.html">Updating the
/// 						Amazon ECS Container Agent</a>.</p>
/// 			         </li>
///             <li>
/// 				           <p>Container health checks are supported for Fargate tasks if you
/// 					are using platform version 1.1.0 or greater. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate
/// 						Platform Versions</a>.</p>
/// 			         </li>
///             <li>
/// 				           <p>Container health checks are not supported for tasks that are part of a service
/// 					that is configured to use a Classic Load Balancer.</p>
/// 			         </li>
///          </ul>
public struct HealthCheck: Equatable {
    /// <p>A string array representing the command that the container runs to determine if it is
    /// 			healthy. The string array must start with <code>CMD</code> to execute the command
    /// 			arguments directly, or <code>CMD-SHELL</code> to run the command with the container's
    /// 			default shell. For example:</p>
    /// 		       <p>
    /// 			         <code>[ "CMD-SHELL", "curl -f http://localhost/ || exit 1" ]</code>
    /// 		       </p>
    /// 		       <p>An exit code of 0 indicates success, and non-zero exit code indicates failure. For
    /// 			more information, see <code>HealthCheck</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a>
    /// 			section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a>.</p>
    public let command: [String]?
    /// <p>The time period in seconds between each health check execution. You may specify
    /// 			between 5 and 300 seconds. The default value is 30 seconds.</p>
    public let interval: Int?
    /// <p>The number of times to retry a failed health check before the container is considered
    /// 			unhealthy. You may specify between 1 and 10 retries. The default value is 3.</p>
    public let retries: Int?
    /// <p>The optional grace period within which to provide containers time to bootstrap before
    /// 			failed health checks count towards the maximum number of retries. You may specify
    /// 			between 0 and 300 seconds. The <code>startPeriod</code> is disabled by default.</p>
    /// 		       <note>
    /// 			         <p>If a health check succeeds within the <code>startPeriod</code>, then the container
    /// 				is considered healthy and any subsequent failures count toward the maximum number of
    /// 				retries.</p>
    /// 		       </note>
    public let startPeriod: Int?
    /// <p>The time period in seconds to wait for a health check to succeed before it is
    /// 			considered a failure. You may specify between 2 and 60 seconds. The default value is
    /// 			5.</p>
    public let timeout: Int?

    public init (
        command: [String]? = nil,
        interval: Int? = nil,
        retries: Int? = nil,
        startPeriod: Int? = nil,
        timeout: Int? = nil
    )
    {
        self.command = command
        self.interval = interval
        self.retries = retries
        self.startPeriod = startPeriod
        self.timeout = timeout
    }
}

public enum HealthStatus {
    case healthy
    case unhealthy
    case unknown
    case sdkUnknown(String)
}

extension HealthStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [HealthStatus] {
        return [
            .healthy,
            .unhealthy,
            .unknown,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .healthy: return "HEALTHY"
        case .unhealthy: return "UNHEALTHY"
        case .unknown: return "UNKNOWN"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = HealthStatus(rawValue: rawValue) ?? HealthStatus.sdkUnknown(rawValue)
    }
}

extension HostEntry: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case hostname
        case ipAddress
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let hostname = hostname {
            try encodeContainer.encode(hostname, forKey: .hostname)
        }
        if let ipAddress = ipAddress {
            try encodeContainer.encode(ipAddress, forKey: .ipAddress)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let hostnameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .hostname)
        hostname = hostnameDecoded
        let ipAddressDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ipAddress)
        ipAddress = ipAddressDecoded
    }
}

extension HostEntry: CustomDebugStringConvertible {
    public var debugDescription: String {
        "HostEntry(hostname: \(String(describing: hostname)), ipAddress: \(String(describing: ipAddress)))"}
}

/// <p>Hostnames and IP address entries that are added to the <code>/etc/hosts</code> file of
/// 			a container via the <code>extraHosts</code> parameter of its <a>ContainerDefinition</a>. </p>
public struct HostEntry: Equatable {
    /// <p>The hostname to use in the <code>/etc/hosts</code> entry.</p>
    public let hostname: String?
    /// <p>The IP address to use in the <code>/etc/hosts</code> entry.</p>
    public let ipAddress: String?

    public init (
        hostname: String? = nil,
        ipAddress: String? = nil
    )
    {
        self.hostname = hostname
        self.ipAddress = ipAddress
    }
}

extension HostVolumeProperties: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case sourcePath
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let sourcePath = sourcePath {
            try encodeContainer.encode(sourcePath, forKey: .sourcePath)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let sourcePathDecoded = try containerValues.decodeIfPresent(String.self, forKey: .sourcePath)
        sourcePath = sourcePathDecoded
    }
}

extension HostVolumeProperties: CustomDebugStringConvertible {
    public var debugDescription: String {
        "HostVolumeProperties(sourcePath: \(String(describing: sourcePath)))"}
}

/// <p>Details on a container instance bind mount host volume.</p>
public struct HostVolumeProperties: Equatable {
    /// <p>When the <code>host</code> parameter is used, specify a <code>sourcePath</code> to
    /// 			declare the path on the host container instance that is presented to the container. If
    /// 			this parameter is empty, then the Docker daemon has assigned a host path for you. If the
    /// 				<code>host</code> parameter contains a <code>sourcePath</code> file location, then
    /// 			the data volume persists at the specified location on the host container instance until
    /// 			you delete it manually. If the <code>sourcePath</code> value does not exist on the host
    /// 			container instance, the Docker daemon creates it. If the location does exist, the
    /// 			contents of the source path folder are exported.</p>
    /// 		       <p>If you are using the Fargate launch type, the <code>sourcePath</code>
    /// 			parameter is not supported.</p>
    public let sourcePath: String?

    public init (
        sourcePath: String? = nil
    )
    {
        self.sourcePath = sourcePath
    }
}

extension InferenceAccelerator: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case deviceName
        case deviceType
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let deviceName = deviceName {
            try encodeContainer.encode(deviceName, forKey: .deviceName)
        }
        if let deviceType = deviceType {
            try encodeContainer.encode(deviceType, forKey: .deviceType)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let deviceNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .deviceName)
        deviceName = deviceNameDecoded
        let deviceTypeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .deviceType)
        deviceType = deviceTypeDecoded
    }
}

extension InferenceAccelerator: CustomDebugStringConvertible {
    public var debugDescription: String {
        "InferenceAccelerator(deviceName: \(String(describing: deviceName)), deviceType: \(String(describing: deviceType)))"}
}

/// <p>Details on a Elastic Inference accelerator. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-eia.html">Working with
/// 				Amazon Elastic Inference on Amazon ECS</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct InferenceAccelerator: Equatable {
    /// <p>The Elastic Inference accelerator device name. The <code>deviceName</code> must also
    /// 			be referenced in a container definition as a <a>ResourceRequirement</a>.</p>
    public let deviceName: String?
    /// <p>The Elastic Inference accelerator type to use.</p>
    public let deviceType: String?

    public init (
        deviceName: String? = nil,
        deviceType: String? = nil
    )
    {
        self.deviceName = deviceName
        self.deviceType = deviceType
    }
}

extension InferenceAcceleratorOverride: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case deviceName
        case deviceType
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let deviceName = deviceName {
            try encodeContainer.encode(deviceName, forKey: .deviceName)
        }
        if let deviceType = deviceType {
            try encodeContainer.encode(deviceType, forKey: .deviceType)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let deviceNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .deviceName)
        deviceName = deviceNameDecoded
        let deviceTypeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .deviceType)
        deviceType = deviceTypeDecoded
    }
}

extension InferenceAcceleratorOverride: CustomDebugStringConvertible {
    public var debugDescription: String {
        "InferenceAcceleratorOverride(deviceName: \(String(describing: deviceName)), deviceType: \(String(describing: deviceType)))"}
}

/// <p>Details on an Elastic Inference accelerator task override. This parameter is used to
/// 			override the Elastic Inference accelerator specified in the task definition. For more
/// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-eia.html">Working with Amazon Elastic Inference on Amazon ECS</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct InferenceAcceleratorOverride: Equatable {
    /// <p>The Elastic Inference accelerator device name to override for the task. This parameter
    /// 			must match a <code>deviceName</code> specified in the task definition.</p>
    public let deviceName: String?
    /// <p>The Elastic Inference accelerator type to use.</p>
    public let deviceType: String?

    public init (
        deviceName: String? = nil,
        deviceType: String? = nil
    )
    {
        self.deviceName = deviceName
        self.deviceType = deviceType
    }
}

extension InvalidParameterException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "InvalidParameterException(message: \(String(describing: message)))"}
}

extension InvalidParameterException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: InvalidParameterExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified parameter is invalid. Review the available parameters for the API
/// 			request.</p>
public struct InvalidParameterException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct InvalidParameterExceptionBody: Equatable {
    public let message: String?
}

extension InvalidParameterExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public enum IpcMode {
    case host
    case `none`
    case task
    case sdkUnknown(String)
}

extension IpcMode : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [IpcMode] {
        return [
            .host,
            .none,
            .task,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .host: return "host"
        case .none: return "none"
        case .task: return "task"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = IpcMode(rawValue: rawValue) ?? IpcMode.sdkUnknown(rawValue)
    }
}

extension KernelCapabilities: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case add
        case drop
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let add = add {
            var addContainer = encodeContainer.nestedUnkeyedContainer(forKey: .add)
            for stringlist0 in add {
                try addContainer.encode(stringlist0)
            }
        }
        if let drop = drop {
            var dropContainer = encodeContainer.nestedUnkeyedContainer(forKey: .drop)
            for stringlist0 in drop {
                try dropContainer.encode(stringlist0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let addContainer = try containerValues.decodeIfPresent([String?].self, forKey: .add)
        var addDecoded0:[String]? = nil
        if let addContainer = addContainer {
            addDecoded0 = [String]()
            for string0 in addContainer {
                if let string0 = string0 {
                    addDecoded0?.append(string0)
                }
            }
        }
        add = addDecoded0
        let dropContainer = try containerValues.decodeIfPresent([String?].self, forKey: .drop)
        var dropDecoded0:[String]? = nil
        if let dropContainer = dropContainer {
            dropDecoded0 = [String]()
            for string0 in dropContainer {
                if let string0 = string0 {
                    dropDecoded0?.append(string0)
                }
            }
        }
        drop = dropDecoded0
    }
}

extension KernelCapabilities: CustomDebugStringConvertible {
    public var debugDescription: String {
        "KernelCapabilities(add: \(String(describing: add)), drop: \(String(describing: drop)))"}
}

/// <p>The Linux capabilities for the container that are added to or dropped from the default
/// 			configuration provided by Docker. For more information on the default capabilities and
/// 			the non-default available capabilities, see <a href="https://docs.docker.com/engine/reference/run/#runtime-privilege-and-linux-capabilities">Runtime privilege and Linux capabilities</a> in the <i>Docker run
/// 				reference</i>. For more detailed information on these Linux capabilities, see
/// 			the <a href="http://man7.org/linux/man-pages/man7/capabilities.7.html">capabilities(7)</a> Linux manual page.</p>
public struct KernelCapabilities: Equatable {
    /// <p>The Linux capabilities for the container that have been added to the default
    /// 			configuration provided by Docker. This parameter maps to <code>CapAdd</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--cap-add</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <note>
    /// 			         <p>Tasks launched on AWS Fargate only support adding the <code>SYS_PTRACE</code> kernel
    /// 				capability.</p>
    /// 		       </note>
    /// 		       <p>Valid values: <code>"ALL" | "AUDIT_CONTROL" | "AUDIT_WRITE" | "BLOCK_SUSPEND" |
    /// 				"CHOWN" | "DAC_OVERRIDE" | "DAC_READ_SEARCH" | "FOWNER" | "FSETID" | "IPC_LOCK" |
    /// 				"IPC_OWNER" | "KILL" | "LEASE" | "LINUX_IMMUTABLE" | "MAC_ADMIN" | "MAC_OVERRIDE" |
    /// 				"MKNOD" | "NET_ADMIN" | "NET_BIND_SERVICE" | "NET_BROADCAST" | "NET_RAW" | "SETFCAP"
    /// 				| "SETGID" | "SETPCAP" | "SETUID" | "SYS_ADMIN" | "SYS_BOOT" | "SYS_CHROOT" |
    /// 				"SYS_MODULE" | "SYS_NICE" | "SYS_PACCT" | "SYS_PTRACE" | "SYS_RAWIO" |
    /// 				"SYS_RESOURCE" | "SYS_TIME" | "SYS_TTY_CONFIG" | "SYSLOG" |
    /// 			"WAKE_ALARM"</code>
    ///          </p>
    public let add: [String]?
    /// <p>The Linux capabilities for the container that have been removed from the default
    /// 			configuration provided by Docker. This parameter maps to <code>CapDrop</code> in the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
    /// 				<code>--cap-drop</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <p>Valid values: <code>"ALL" | "AUDIT_CONTROL" | "AUDIT_WRITE" | "BLOCK_SUSPEND" |
    /// 				"CHOWN" | "DAC_OVERRIDE" | "DAC_READ_SEARCH" | "FOWNER" | "FSETID" | "IPC_LOCK" |
    /// 				"IPC_OWNER" | "KILL" | "LEASE" | "LINUX_IMMUTABLE" | "MAC_ADMIN" | "MAC_OVERRIDE" |
    /// 				"MKNOD" | "NET_ADMIN" | "NET_BIND_SERVICE" | "NET_BROADCAST" | "NET_RAW" | "SETFCAP"
    /// 				| "SETGID" | "SETPCAP" | "SETUID" | "SYS_ADMIN" | "SYS_BOOT" | "SYS_CHROOT" |
    /// 				"SYS_MODULE" | "SYS_NICE" | "SYS_PACCT" | "SYS_PTRACE" | "SYS_RAWIO" |
    /// 				"SYS_RESOURCE" | "SYS_TIME" | "SYS_TTY_CONFIG" | "SYSLOG" |
    /// 			"WAKE_ALARM"</code>
    ///          </p>
    public let drop: [String]?

    public init (
        add: [String]? = nil,
        drop: [String]? = nil
    )
    {
        self.add = add
        self.drop = drop
    }
}

extension KeyValuePair: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
    }
}

extension KeyValuePair: CustomDebugStringConvertible {
    public var debugDescription: String {
        "KeyValuePair(name: \(String(describing: name)), value: \(String(describing: value)))"}
}

/// <p>A key-value pair object.</p>
public struct KeyValuePair: Equatable {
    /// <p>The name of the key-value pair. For environment variables, this is the name of the
    /// 			environment variable.</p>
    public let name: String?
    /// <p>The value of the key-value pair. For environment variables, this is the value of the
    /// 			environment variable.</p>
    public let value: String?

    public init (
        name: String? = nil,
        value: String? = nil
    )
    {
        self.name = name
        self.value = value
    }
}

public enum LaunchType {
    case ec2
    case external
    case fargate
    case sdkUnknown(String)
}

extension LaunchType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [LaunchType] {
        return [
            .ec2,
            .external,
            .fargate,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .ec2: return "EC2"
        case .external: return "EXTERNAL"
        case .fargate: return "FARGATE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = LaunchType(rawValue: rawValue) ?? LaunchType.sdkUnknown(rawValue)
    }
}

extension LimitExceededException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "LimitExceededException(message: \(String(describing: message)))"}
}

extension LimitExceededException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: LimitExceededExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The limit for the resource has been exceeded.</p>
public struct LimitExceededException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct LimitExceededExceptionBody: Equatable {
    public let message: String?
}

extension LimitExceededExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension LinuxParameters: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capabilities
        case devices
        case initProcessEnabled
        case maxSwap
        case sharedMemorySize
        case swappiness
        case tmpfs
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capabilities = capabilities {
            try encodeContainer.encode(capabilities, forKey: .capabilities)
        }
        if let devices = devices {
            var devicesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .devices)
            for deviceslist0 in devices {
                try devicesContainer.encode(deviceslist0)
            }
        }
        if let initProcessEnabled = initProcessEnabled {
            try encodeContainer.encode(initProcessEnabled, forKey: .initProcessEnabled)
        }
        if let maxSwap = maxSwap {
            try encodeContainer.encode(maxSwap, forKey: .maxSwap)
        }
        if let sharedMemorySize = sharedMemorySize {
            try encodeContainer.encode(sharedMemorySize, forKey: .sharedMemorySize)
        }
        if let swappiness = swappiness {
            try encodeContainer.encode(swappiness, forKey: .swappiness)
        }
        if let tmpfs = tmpfs {
            var tmpfsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tmpfs)
            for tmpfslist0 in tmpfs {
                try tmpfsContainer.encode(tmpfslist0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capabilitiesDecoded = try containerValues.decodeIfPresent(KernelCapabilities.self, forKey: .capabilities)
        capabilities = capabilitiesDecoded
        let devicesContainer = try containerValues.decodeIfPresent([Device?].self, forKey: .devices)
        var devicesDecoded0:[Device]? = nil
        if let devicesContainer = devicesContainer {
            devicesDecoded0 = [Device]()
            for structure0 in devicesContainer {
                if let structure0 = structure0 {
                    devicesDecoded0?.append(structure0)
                }
            }
        }
        devices = devicesDecoded0
        let initProcessEnabledDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .initProcessEnabled)
        initProcessEnabled = initProcessEnabledDecoded
        let sharedMemorySizeDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .sharedMemorySize)
        sharedMemorySize = sharedMemorySizeDecoded
        let tmpfsContainer = try containerValues.decodeIfPresent([Tmpfs?].self, forKey: .tmpfs)
        var tmpfsDecoded0:[Tmpfs]? = nil
        if let tmpfsContainer = tmpfsContainer {
            tmpfsDecoded0 = [Tmpfs]()
            for structure0 in tmpfsContainer {
                if let structure0 = structure0 {
                    tmpfsDecoded0?.append(structure0)
                }
            }
        }
        tmpfs = tmpfsDecoded0
        let maxSwapDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxSwap)
        maxSwap = maxSwapDecoded
        let swappinessDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .swappiness)
        swappiness = swappinessDecoded
    }
}

extension LinuxParameters: CustomDebugStringConvertible {
    public var debugDescription: String {
        "LinuxParameters(capabilities: \(String(describing: capabilities)), devices: \(String(describing: devices)), initProcessEnabled: \(String(describing: initProcessEnabled)), maxSwap: \(String(describing: maxSwap)), sharedMemorySize: \(String(describing: sharedMemorySize)), swappiness: \(String(describing: swappiness)), tmpfs: \(String(describing: tmpfs)))"}
}

/// <p>Linux-specific options that are applied to the container, such as Linux <a>KernelCapabilities</a>.</p>
public struct LinuxParameters: Equatable {
    /// <p>The Linux capabilities for the container that are added to or dropped from the default
    /// 			configuration provided by Docker.</p>
    /// 		       <note>
    /// 			         <p>For tasks that use the Fargate launch type,
    /// 					<code>capabilities</code> is supported for all platform versions but the
    /// 					<code>add</code> parameter is only supported if using platform version 1.4.0 or
    /// 				later.</p>
    /// 		       </note>
    public let capabilities: KernelCapabilities?
    /// <p>Any host devices to expose to the container. This parameter maps to
    /// 				<code>Devices</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
    /// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--device</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <note>
    /// 			         <p>If you are using tasks that use the Fargate launch type, the
    /// 					<code>devices</code> parameter is not supported.</p>
    /// 		       </note>
    public let devices: [Device]?
    /// <p>Run an <code>init</code> process inside the container that forwards signals and reaps
    /// 			processes. This parameter maps to the <code>--init</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>. This parameter requires version 1.25 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: <code>sudo docker version --format '{{.Server.APIVersion}}'</code>
    ///          </p>
    public let initProcessEnabled: Bool?
    /// <p>The total amount of swap memory (in MiB) a container can use. This parameter will be
    /// 			translated to the <code>--memory-swap</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a> where the value would be the sum of
    /// 			the container memory plus the <code>maxSwap</code> value.</p>
    /// 		       <p>If a <code>maxSwap</code> value of <code>0</code> is specified, the container will not
    /// 			use swap. Accepted values are <code>0</code> or any positive integer. If the
    /// 				<code>maxSwap</code> parameter is omitted, the container will use the swap
    /// 			configuration for the container instance it is running on. A <code>maxSwap</code> value
    /// 			must be set for the <code>swappiness</code> parameter to be used.</p>
    /// 		       <note>
    /// 			         <p>If you are using tasks that use the Fargate launch type, the
    /// 					<code>maxSwap</code> parameter is not supported.</p>
    /// 		       </note>
    public let maxSwap: Int?
    /// <p>The value for the size (in MiB) of the <code>/dev/shm</code> volume. This parameter
    /// 			maps to the <code>--shm-size</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker
    /// 				run</a>.</p>
    /// 		       <note>
    /// 			         <p>If you are using tasks that use the Fargate launch type, the
    /// 					<code>sharedMemorySize</code> parameter is not supported.</p>
    /// 		       </note>
    public let sharedMemorySize: Int?
    /// <p>This allows you to tune a container's memory swappiness behavior. A
    /// 				<code>swappiness</code> value of <code>0</code> will cause swapping to not happen
    /// 			unless absolutely necessary. A <code>swappiness</code> value of <code>100</code> will
    /// 			cause pages to be swapped very aggressively. Accepted values are whole numbers between
    /// 				<code>0</code> and <code>100</code>. If the <code>swappiness</code> parameter is not
    /// 			specified, a default value of <code>60</code> is used. If a value is not specified for
    /// 				<code>maxSwap</code> then this parameter is ignored. This parameter maps to the
    /// 				<code>--memory-swappiness</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <note>
    /// 			         <p>If you are using tasks that use the Fargate launch type, the
    /// 					<code>swappiness</code> parameter is not supported.</p>
    /// 		       </note>
    public let swappiness: Int?
    /// <p>The container path, mount options, and size (in MiB) of the tmpfs mount. This
    /// 			parameter maps to the <code>--tmpfs</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
    /// 		       <note>
    /// 			         <p>If you are using tasks that use the Fargate launch type, the
    /// 					<code>tmpfs</code> parameter is not supported.</p>
    /// 		       </note>
    public let tmpfs: [Tmpfs]?

    public init (
        capabilities: KernelCapabilities? = nil,
        devices: [Device]? = nil,
        initProcessEnabled: Bool? = nil,
        maxSwap: Int? = nil,
        sharedMemorySize: Int? = nil,
        swappiness: Int? = nil,
        tmpfs: [Tmpfs]? = nil
    )
    {
        self.capabilities = capabilities
        self.devices = devices
        self.initProcessEnabled = initProcessEnabled
        self.maxSwap = maxSwap
        self.sharedMemorySize = sharedMemorySize
        self.swappiness = swappiness
        self.tmpfs = tmpfs
    }
}

public struct ListAccountSettingsInputBodyMiddleware: Middleware {
    public let id: String = "ListAccountSettingsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListAccountSettingsInput>,
                  next: H) -> Swift.Result<OperationOutput<ListAccountSettingsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListAccountSettingsInput>
    public typealias MOutput = OperationOutput<ListAccountSettingsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListAccountSettingsOutputError>
}

extension ListAccountSettingsInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListAccountSettingsInput(effectiveSettings: \(String(describing: effectiveSettings)), maxResults: \(String(describing: maxResults)), name: \(String(describing: name)), nextToken: \(String(describing: nextToken)), principalArn: \(String(describing: principalArn)), value: \(String(describing: value)))"}
}

extension ListAccountSettingsInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case effectiveSettings
        case maxResults
        case name
        case nextToken
        case principalArn
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if effectiveSettings != false {
            try encodeContainer.encode(effectiveSettings, forKey: .effectiveSettings)
        }
        if maxResults != 0 {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let principalArn = principalArn {
            try encodeContainer.encode(principalArn, forKey: .principalArn)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }
}

public struct ListAccountSettingsInputHeadersMiddleware: Middleware {
    public let id: String = "ListAccountSettingsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListAccountSettingsInput>,
                  next: H) -> Swift.Result<OperationOutput<ListAccountSettingsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListAccountSettingsInput>
    public typealias MOutput = OperationOutput<ListAccountSettingsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListAccountSettingsOutputError>
}

public struct ListAccountSettingsInputQueryItemMiddleware: Middleware {
    public let id: String = "ListAccountSettingsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListAccountSettingsInput>,
                  next: H) -> Swift.Result<OperationOutput<ListAccountSettingsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListAccountSettingsInput>
    public typealias MOutput = OperationOutput<ListAccountSettingsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListAccountSettingsOutputError>
}

public struct ListAccountSettingsInput: Equatable {
    /// <p>Specifies whether to return the effective settings. If <code>true</code>, the account
    /// 			settings for the root user or the default setting for the <code>principalArn</code> are
    /// 			returned. If <code>false</code>, the account settings for the <code>principalArn</code>
    /// 			are returned if they are set. Otherwise, no account settings are returned.</p>
    public let effectiveSettings: Bool
    /// <p>The maximum number of account setting results returned by
    /// 				<code>ListAccountSettings</code> in paginated output. When this parameter is used,
    /// 				<code>ListAccountSettings</code> only returns <code>maxResults</code> results in a
    /// 			single page along with a <code>nextToken</code> response element. The remaining results
    /// 			of the initial request can be seen by sending another <code>ListAccountSettings</code>
    /// 			request with the returned <code>nextToken</code> value. This value can be between
    /// 			1 and 10. If this
    /// 			parameter is not used, then <code>ListAccountSettings</code> returns up to
    /// 			10 results and a <code>nextToken</code> value
    /// 			if applicable.</p>
    public let maxResults: Int
    /// <p>The name of the account setting you want to list the settings for.</p>
    public let name: SettingName?
    /// <p>The <code>nextToken</code> value returned from a <code>ListAccountSettings</code>
    /// 			request indicating that more results are available to fulfill the request and further
    /// 			calls will be needed. If <code>maxResults</code> was provided, it is possible the number
    /// 			of results to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>The ARN of the principal, which can be an IAM user, IAM role, or the root user. If
    /// 			this field is omitted, the account settings are listed only for the authenticated
    /// 			user.</p>
    /// 		       <note>
    /// 			         <p>Federated users assume the account setting of the root user and can't have
    /// 				explicit account settings set for them.</p>
    /// 		       </note>
    public let principalArn: String?
    /// <p>The value of the account settings with which to filter results. You must also specify
    /// 			an account setting name to use this parameter.</p>
    public let value: String?

    public init (
        effectiveSettings: Bool = false,
        maxResults: Int = 0,
        name: SettingName? = nil,
        nextToken: String? = nil,
        principalArn: String? = nil,
        value: String? = nil
    )
    {
        self.effectiveSettings = effectiveSettings
        self.maxResults = maxResults
        self.name = name
        self.nextToken = nextToken
        self.principalArn = principalArn
        self.value = value
    }
}

struct ListAccountSettingsInputBody: Equatable {
    public let name: SettingName?
    public let value: String?
    public let principalArn: String?
    public let effectiveSettings: Bool
    public let nextToken: String?
    public let maxResults: Int
}

extension ListAccountSettingsInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case effectiveSettings
        case maxResults
        case name
        case nextToken
        case principalArn
        case value
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(SettingName.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let principalArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .principalArn)
        principalArn = principalArnDecoded
        let effectiveSettingsDecoded = try containerValues.decode(Bool.self, forKey: .effectiveSettings)
        effectiveSettings = effectiveSettingsDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decode(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
    }
}

extension ListAccountSettingsOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListAccountSettingsOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListAccountSettingsOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListAccountSettingsOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListAccountSettingsOutputResponse(nextToken: \(String(describing: nextToken)), settings: \(String(describing: settings)))"}
}

extension ListAccountSettingsOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListAccountSettingsOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.nextToken = output.nextToken
            self.settings = output.settings
        } else {
            self.nextToken = nil
            self.settings = nil
        }
    }
}

public struct ListAccountSettingsOutputResponse: Equatable {
    /// <p>The <code>nextToken</code> value to include in a future
    /// 				<code>ListAccountSettings</code> request. When the results of a
    /// 				<code>ListAccountSettings</code> request exceed <code>maxResults</code>, this value
    /// 			can be used to retrieve the next page of results. This value is <code>null</code> when
    /// 			there are no more results to return.</p>
    public let nextToken: String?
    /// <p>The account settings for the resource.</p>
    public let settings: [Setting]?

    public init (
        nextToken: String? = nil,
        settings: [Setting]? = nil
    )
    {
        self.nextToken = nextToken
        self.settings = settings
    }
}

struct ListAccountSettingsOutputResponseBody: Equatable {
    public let settings: [Setting]?
    public let nextToken: String?
}

extension ListAccountSettingsOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case nextToken
        case settings
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let settingsContainer = try containerValues.decodeIfPresent([Setting?].self, forKey: .settings)
        var settingsDecoded0:[Setting]? = nil
        if let settingsContainer = settingsContainer {
            settingsDecoded0 = [Setting]()
            for structure0 in settingsContainer {
                if let structure0 = structure0 {
                    settingsDecoded0?.append(structure0)
                }
            }
        }
        settings = settingsDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListAttributesInputBodyMiddleware: Middleware {
    public let id: String = "ListAttributesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListAttributesInput>
    public typealias MOutput = OperationOutput<ListAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListAttributesOutputError>
}

extension ListAttributesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListAttributesInput(attributeName: \(String(describing: attributeName)), attributeValue: \(String(describing: attributeValue)), cluster: \(String(describing: cluster)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)), targetType: \(String(describing: targetType)))"}
}

extension ListAttributesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attributeName
        case attributeValue
        case cluster
        case maxResults
        case nextToken
        case targetType
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attributeName = attributeName {
            try encodeContainer.encode(attributeName, forKey: .attributeName)
        }
        if let attributeValue = attributeValue {
            try encodeContainer.encode(attributeValue, forKey: .attributeValue)
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let targetType = targetType {
            try encodeContainer.encode(targetType.rawValue, forKey: .targetType)
        }
    }
}

public struct ListAttributesInputHeadersMiddleware: Middleware {
    public let id: String = "ListAttributesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListAttributesInput>
    public typealias MOutput = OperationOutput<ListAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListAttributesOutputError>
}

public struct ListAttributesInputQueryItemMiddleware: Middleware {
    public let id: String = "ListAttributesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListAttributesInput>
    public typealias MOutput = OperationOutput<ListAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListAttributesOutputError>
}

public struct ListAttributesInput: Equatable {
    /// <p>The name of the attribute with which to filter the results. </p>
    public let attributeName: String?
    /// <p>The value of the attribute with which to filter results. You must also specify an
    /// 			attribute name to use this parameter.</p>
    public let attributeValue: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster to list attributes.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The maximum number of cluster results returned by <code>ListAttributes</code> in
    /// 			paginated output. When this parameter is used, <code>ListAttributes</code> only returns
    /// 				<code>maxResults</code> results in a single page along with a <code>nextToken</code>
    /// 			response element. The remaining results of the initial request can be seen by sending
    /// 			another <code>ListAttributes</code> request with the returned <code>nextToken</code>
    /// 			value. This value can be between 1 and 100. If this
    /// 			parameter is not used, then <code>ListAttributes</code> returns up to
    /// 			100 results and a <code>nextToken</code> value if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a <code>ListAttributes</code> request
    /// 			indicating that more results are available to fulfill the request and further calls will
    /// 			be needed. If <code>maxResults</code> was provided, it is possible the number of results
    /// 			to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>The type of the target with which to list attributes.</p>
    public let targetType: TargetType?

    public init (
        attributeName: String? = nil,
        attributeValue: String? = nil,
        cluster: String? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        targetType: TargetType? = nil
    )
    {
        self.attributeName = attributeName
        self.attributeValue = attributeValue
        self.cluster = cluster
        self.maxResults = maxResults
        self.nextToken = nextToken
        self.targetType = targetType
    }
}

struct ListAttributesInputBody: Equatable {
    public let cluster: String?
    public let targetType: TargetType?
    public let attributeName: String?
    public let attributeValue: String?
    public let nextToken: String?
    public let maxResults: Int?
}

extension ListAttributesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributeName
        case attributeValue
        case cluster
        case maxResults
        case nextToken
        case targetType
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let targetTypeDecoded = try containerValues.decodeIfPresent(TargetType.self, forKey: .targetType)
        targetType = targetTypeDecoded
        let attributeNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .attributeName)
        attributeName = attributeNameDecoded
        let attributeValueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .attributeValue)
        attributeValue = attributeValueDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
    }
}

extension ListAttributesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListAttributesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListAttributesOutputError: Swift.Error, Equatable {
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListAttributesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListAttributesOutputResponse(attributes: \(String(describing: attributes)), nextToken: \(String(describing: nextToken)))"}
}

extension ListAttributesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListAttributesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.attributes = output.attributes
            self.nextToken = output.nextToken
        } else {
            self.attributes = nil
            self.nextToken = nil
        }
    }
}

public struct ListAttributesOutputResponse: Equatable {
    /// <p>A list of attribute objects that meet the criteria of the request.</p>
    public let attributes: [Attribute]?
    /// <p>The <code>nextToken</code> value to include in a future <code>ListAttributes</code>
    /// 			request. When the results of a <code>ListAttributes</code> request exceed
    /// 				<code>maxResults</code>, this value can be used to retrieve the next page of
    /// 			results. This value is <code>null</code> when there are no more results to
    /// 			return.</p>
    public let nextToken: String?

    public init (
        attributes: [Attribute]? = nil,
        nextToken: String? = nil
    )
    {
        self.attributes = attributes
        self.nextToken = nextToken
    }
}

struct ListAttributesOutputResponseBody: Equatable {
    public let attributes: [Attribute]?
    public let nextToken: String?
}

extension ListAttributesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributes
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListClustersInputBodyMiddleware: Middleware {
    public let id: String = "ListClustersInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListClustersInput>,
                  next: H) -> Swift.Result<OperationOutput<ListClustersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListClustersInput>
    public typealias MOutput = OperationOutput<ListClustersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListClustersOutputError>
}

extension ListClustersInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListClustersInput(maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)))"}
}

extension ListClustersInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case maxResults
        case nextToken
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
    }
}

public struct ListClustersInputHeadersMiddleware: Middleware {
    public let id: String = "ListClustersInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListClustersInput>,
                  next: H) -> Swift.Result<OperationOutput<ListClustersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListClustersInput>
    public typealias MOutput = OperationOutput<ListClustersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListClustersOutputError>
}

public struct ListClustersInputQueryItemMiddleware: Middleware {
    public let id: String = "ListClustersInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListClustersInput>,
                  next: H) -> Swift.Result<OperationOutput<ListClustersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListClustersInput>
    public typealias MOutput = OperationOutput<ListClustersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListClustersOutputError>
}

public struct ListClustersInput: Equatable {
    /// <p>The maximum number of cluster results returned by <code>ListClusters</code> in
    /// 			paginated output. When this parameter is used, <code>ListClusters</code> only returns
    /// 				<code>maxResults</code> results in a single page along with a <code>nextToken</code>
    /// 			response element. The remaining results of the initial request can be seen by sending
    /// 			another <code>ListClusters</code> request with the returned <code>nextToken</code>
    /// 			value. This value can be between 1 and 100. If this
    /// 			parameter is not used, then <code>ListClusters</code> returns up to
    /// 			100 results and a <code>nextToken</code> value if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a <code>ListClusters</code> request
    /// 			indicating that more results are available to fulfill the request and further calls will
    /// 			be needed. If <code>maxResults</code> was provided, it is possible the number of results
    /// 			to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?

    public init (
        maxResults: Int? = nil,
        nextToken: String? = nil
    )
    {
        self.maxResults = maxResults
        self.nextToken = nextToken
    }
}

struct ListClustersInputBody: Equatable {
    public let nextToken: String?
    public let maxResults: Int?
}

extension ListClustersInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case maxResults
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
    }
}

extension ListClustersOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListClustersOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListClustersOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListClustersOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListClustersOutputResponse(clusterArns: \(String(describing: clusterArns)), nextToken: \(String(describing: nextToken)))"}
}

extension ListClustersOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListClustersOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.clusterArns = output.clusterArns
            self.nextToken = output.nextToken
        } else {
            self.clusterArns = nil
            self.nextToken = nil
        }
    }
}

public struct ListClustersOutputResponse: Equatable {
    /// <p>The list of full Amazon Resource Name (ARN) entries for each cluster associated with your
    /// 			account.</p>
    public let clusterArns: [String]?
    /// <p>The <code>nextToken</code> value to include in a future <code>ListClusters</code>
    /// 			request. When the results of a <code>ListClusters</code> request exceed
    /// 				<code>maxResults</code>, this value can be used to retrieve the next page of
    /// 			results. This value is <code>null</code> when there are no more results to
    /// 			return.</p>
    public let nextToken: String?

    public init (
        clusterArns: [String]? = nil,
        nextToken: String? = nil
    )
    {
        self.clusterArns = clusterArns
        self.nextToken = nextToken
    }
}

struct ListClustersOutputResponseBody: Equatable {
    public let clusterArns: [String]?
    public let nextToken: String?
}

extension ListClustersOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case clusterArns
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterArnsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .clusterArns)
        var clusterArnsDecoded0:[String]? = nil
        if let clusterArnsContainer = clusterArnsContainer {
            clusterArnsDecoded0 = [String]()
            for string0 in clusterArnsContainer {
                if let string0 = string0 {
                    clusterArnsDecoded0?.append(string0)
                }
            }
        }
        clusterArns = clusterArnsDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListContainerInstancesInputBodyMiddleware: Middleware {
    public let id: String = "ListContainerInstancesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListContainerInstancesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListContainerInstancesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListContainerInstancesInput>
    public typealias MOutput = OperationOutput<ListContainerInstancesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListContainerInstancesOutputError>
}

extension ListContainerInstancesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListContainerInstancesInput(cluster: \(String(describing: cluster)), filter: \(String(describing: filter)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)), status: \(String(describing: status)))"}
}

extension ListContainerInstancesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case filter
        case maxResults
        case nextToken
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let filter = filter {
            try encodeContainer.encode(filter, forKey: .filter)
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
    }
}

public struct ListContainerInstancesInputHeadersMiddleware: Middleware {
    public let id: String = "ListContainerInstancesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListContainerInstancesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListContainerInstancesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListContainerInstancesInput>
    public typealias MOutput = OperationOutput<ListContainerInstancesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListContainerInstancesOutputError>
}

public struct ListContainerInstancesInputQueryItemMiddleware: Middleware {
    public let id: String = "ListContainerInstancesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListContainerInstancesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListContainerInstancesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListContainerInstancesInput>
    public typealias MOutput = OperationOutput<ListContainerInstancesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListContainerInstancesOutputError>
}

public struct ListContainerInstancesInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instances to
    /// 			list. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>You can filter the results of a <code>ListContainerInstances</code> operation with
    /// 			cluster query language statements. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/cluster-query-language.html">Cluster Query Language</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let filter: String?
    /// <p>The maximum number of container instance results returned by
    /// 				<code>ListContainerInstances</code> in paginated output. When this parameter is
    /// 			used, <code>ListContainerInstances</code> only returns <code>maxResults</code> results
    /// 			in a single page along with a <code>nextToken</code> response element. The remaining
    /// 			results of the initial request can be seen by sending another
    /// 				<code>ListContainerInstances</code> request with the returned <code>nextToken</code>
    /// 			value. This value can be between 1 and 100. If this
    /// 			parameter is not used, then <code>ListContainerInstances</code> returns up to
    /// 			100 results and a <code>nextToken</code> value if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a <code>ListContainerInstances</code>
    /// 			request indicating that more results are available to fulfill the request and further
    /// 			calls will be needed. If <code>maxResults</code> was provided, it is possible the number
    /// 			of results to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>Filters the container instances by status. For example, if you specify the
    /// 				<code>DRAINING</code> status, the results include only container instances that have
    /// 			been set to <code>DRAINING</code> using <a>UpdateContainerInstancesState</a>.
    /// 			If you do not specify this parameter, the default is to include container instances set
    /// 			to all states other than <code>INACTIVE</code>.</p>
    public let status: ContainerInstanceStatus?

    public init (
        cluster: String? = nil,
        filter: String? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        status: ContainerInstanceStatus? = nil
    )
    {
        self.cluster = cluster
        self.filter = filter
        self.maxResults = maxResults
        self.nextToken = nextToken
        self.status = status
    }
}

struct ListContainerInstancesInputBody: Equatable {
    public let cluster: String?
    public let filter: String?
    public let nextToken: String?
    public let maxResults: Int?
    public let status: ContainerInstanceStatus?
}

extension ListContainerInstancesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case filter
        case maxResults
        case nextToken
        case status
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let filterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .filter)
        filter = filterDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
        let statusDecoded = try containerValues.decodeIfPresent(ContainerInstanceStatus.self, forKey: .status)
        status = statusDecoded
    }
}

extension ListContainerInstancesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListContainerInstancesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListContainerInstancesOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListContainerInstancesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListContainerInstancesOutputResponse(containerInstanceArns: \(String(describing: containerInstanceArns)), nextToken: \(String(describing: nextToken)))"}
}

extension ListContainerInstancesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListContainerInstancesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.containerInstanceArns = output.containerInstanceArns
            self.nextToken = output.nextToken
        } else {
            self.containerInstanceArns = nil
            self.nextToken = nil
        }
    }
}

public struct ListContainerInstancesOutputResponse: Equatable {
    /// <p>The list of container instances with full ARN entries for each container instance
    /// 			associated with the specified cluster.</p>
    public let containerInstanceArns: [String]?
    /// <p>The <code>nextToken</code> value to include in a future
    /// 				<code>ListContainerInstances</code> request. When the results of a
    /// 				<code>ListContainerInstances</code> request exceed <code>maxResults</code>, this
    /// 			value can be used to retrieve the next page of results. This value is <code>null</code>
    /// 			when there are no more results to return.</p>
    public let nextToken: String?

    public init (
        containerInstanceArns: [String]? = nil,
        nextToken: String? = nil
    )
    {
        self.containerInstanceArns = containerInstanceArns
        self.nextToken = nextToken
    }
}

struct ListContainerInstancesOutputResponseBody: Equatable {
    public let containerInstanceArns: [String]?
    public let nextToken: String?
}

extension ListContainerInstancesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerInstanceArns
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstanceArnsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .containerInstanceArns)
        var containerInstanceArnsDecoded0:[String]? = nil
        if let containerInstanceArnsContainer = containerInstanceArnsContainer {
            containerInstanceArnsDecoded0 = [String]()
            for string0 in containerInstanceArnsContainer {
                if let string0 = string0 {
                    containerInstanceArnsDecoded0?.append(string0)
                }
            }
        }
        containerInstanceArns = containerInstanceArnsDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListServicesInputBodyMiddleware: Middleware {
    public let id: String = "ListServicesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListServicesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListServicesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListServicesInput>
    public typealias MOutput = OperationOutput<ListServicesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListServicesOutputError>
}

extension ListServicesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListServicesInput(cluster: \(String(describing: cluster)), launchType: \(String(describing: launchType)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)), schedulingStrategy: \(String(describing: schedulingStrategy)))"}
}

extension ListServicesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case launchType
        case maxResults
        case nextToken
        case schedulingStrategy
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let schedulingStrategy = schedulingStrategy {
            try encodeContainer.encode(schedulingStrategy.rawValue, forKey: .schedulingStrategy)
        }
    }
}

public struct ListServicesInputHeadersMiddleware: Middleware {
    public let id: String = "ListServicesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListServicesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListServicesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListServicesInput>
    public typealias MOutput = OperationOutput<ListServicesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListServicesOutputError>
}

public struct ListServicesInputQueryItemMiddleware: Middleware {
    public let id: String = "ListServicesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListServicesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListServicesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListServicesInput>
    public typealias MOutput = OperationOutput<ListServicesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListServicesOutputError>
}

public struct ListServicesInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster to use when filtering the
    /// 				<code>ListServices</code> results. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The launch type to use when filtering the <code>ListServices</code> results.</p>
    public let launchType: LaunchType?
    /// <p>The maximum number of service results returned by <code>ListServices</code> in
    /// 			paginated output. When this parameter is used, <code>ListServices</code> only returns
    /// 				<code>maxResults</code> results in a single page along with a <code>nextToken</code>
    /// 			response element. The remaining results of the initial request can be seen by sending
    /// 			another <code>ListServices</code> request with the returned <code>nextToken</code>
    /// 			value. This value can be between 1 and 100. If
    /// 			this parameter is not used, then <code>ListServices</code> returns up to
    /// 			10 results and a <code>nextToken</code> value if
    /// 			applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a <code>ListServices</code> request
    /// 			indicating that more results are available to fulfill the request and further calls will
    /// 			be needed. If <code>maxResults</code> was provided, it is possible the number of results
    /// 			to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>The scheduling strategy to use when filtering the <code>ListServices</code>
    /// 			results.</p>
    public let schedulingStrategy: SchedulingStrategy?

    public init (
        cluster: String? = nil,
        launchType: LaunchType? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        schedulingStrategy: SchedulingStrategy? = nil
    )
    {
        self.cluster = cluster
        self.launchType = launchType
        self.maxResults = maxResults
        self.nextToken = nextToken
        self.schedulingStrategy = schedulingStrategy
    }
}

struct ListServicesInputBody: Equatable {
    public let cluster: String?
    public let nextToken: String?
    public let maxResults: Int?
    public let launchType: LaunchType?
    public let schedulingStrategy: SchedulingStrategy?
}

extension ListServicesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case launchType
        case maxResults
        case nextToken
        case schedulingStrategy
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let schedulingStrategyDecoded = try containerValues.decodeIfPresent(SchedulingStrategy.self, forKey: .schedulingStrategy)
        schedulingStrategy = schedulingStrategyDecoded
    }
}

extension ListServicesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListServicesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListServicesOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListServicesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListServicesOutputResponse(nextToken: \(String(describing: nextToken)), serviceArns: \(String(describing: serviceArns)))"}
}

extension ListServicesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListServicesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.nextToken = output.nextToken
            self.serviceArns = output.serviceArns
        } else {
            self.nextToken = nil
            self.serviceArns = nil
        }
    }
}

public struct ListServicesOutputResponse: Equatable {
    /// <p>The <code>nextToken</code> value to include in a future <code>ListServices</code>
    /// 			request. When the results of a <code>ListServices</code> request exceed
    /// 				<code>maxResults</code>, this value can be used to retrieve the next page of
    /// 			results. This value is <code>null</code> when there are no more results to
    /// 			return.</p>
    public let nextToken: String?
    /// <p>The list of full ARN entries for each service associated with the specified
    /// 			cluster.</p>
    public let serviceArns: [String]?

    public init (
        nextToken: String? = nil,
        serviceArns: [String]? = nil
    )
    {
        self.nextToken = nextToken
        self.serviceArns = serviceArns
    }
}

struct ListServicesOutputResponseBody: Equatable {
    public let serviceArns: [String]?
    public let nextToken: String?
}

extension ListServicesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case nextToken
        case serviceArns
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let serviceArnsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .serviceArns)
        var serviceArnsDecoded0:[String]? = nil
        if let serviceArnsContainer = serviceArnsContainer {
            serviceArnsDecoded0 = [String]()
            for string0 in serviceArnsContainer {
                if let string0 = string0 {
                    serviceArnsDecoded0?.append(string0)
                }
            }
        }
        serviceArns = serviceArnsDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListTagsForResourceInputBodyMiddleware: Middleware {
    public let id: String = "ListTagsForResourceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTagsForResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTagsForResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTagsForResourceInput>
    public typealias MOutput = OperationOutput<ListTagsForResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTagsForResourceOutputError>
}

extension ListTagsForResourceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTagsForResourceInput(resourceArn: \(String(describing: resourceArn)))"}
}

extension ListTagsForResourceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case resourceArn
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let resourceArn = resourceArn {
            try encodeContainer.encode(resourceArn, forKey: .resourceArn)
        }
    }
}

public struct ListTagsForResourceInputHeadersMiddleware: Middleware {
    public let id: String = "ListTagsForResourceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTagsForResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTagsForResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTagsForResourceInput>
    public typealias MOutput = OperationOutput<ListTagsForResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTagsForResourceOutputError>
}

public struct ListTagsForResourceInputQueryItemMiddleware: Middleware {
    public let id: String = "ListTagsForResourceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTagsForResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTagsForResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTagsForResourceInput>
    public typealias MOutput = OperationOutput<ListTagsForResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTagsForResourceOutputError>
}

public struct ListTagsForResourceInput: Equatable {
    /// <p>The Amazon Resource Name (ARN) that identifies the resource for which to list the tags. Currently, the
    /// 			supported resources are Amazon ECS tasks, services, task definitions, clusters, and container
    /// 			instances.</p>
    public let resourceArn: String?

    public init (
        resourceArn: String? = nil
    )
    {
        self.resourceArn = resourceArn
    }
}

struct ListTagsForResourceInputBody: Equatable {
    public let resourceArn: String?
}

extension ListTagsForResourceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case resourceArn
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let resourceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .resourceArn)
        resourceArn = resourceArnDecoded
    }
}

extension ListTagsForResourceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListTagsForResourceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListTagsForResourceOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListTagsForResourceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTagsForResourceOutputResponse(tags: \(String(describing: tags)))"}
}

extension ListTagsForResourceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListTagsForResourceOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.tags = output.tags
        } else {
            self.tags = nil
        }
    }
}

public struct ListTagsForResourceOutputResponse: Equatable {
    /// <p>The tags for the resource.</p>
    public let tags: [Tag]?

    public init (
        tags: [Tag]? = nil
    )
    {
        self.tags = tags
    }
}

struct ListTagsForResourceOutputResponseBody: Equatable {
    public let tags: [Tag]?
}

extension ListTagsForResourceOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case tags
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

public struct ListTaskDefinitionFamiliesInputBodyMiddleware: Middleware {
    public let id: String = "ListTaskDefinitionFamiliesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTaskDefinitionFamiliesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTaskDefinitionFamiliesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTaskDefinitionFamiliesInput>
    public typealias MOutput = OperationOutput<ListTaskDefinitionFamiliesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTaskDefinitionFamiliesOutputError>
}

extension ListTaskDefinitionFamiliesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTaskDefinitionFamiliesInput(familyPrefix: \(String(describing: familyPrefix)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)), status: \(String(describing: status)))"}
}

extension ListTaskDefinitionFamiliesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case familyPrefix
        case maxResults
        case nextToken
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let familyPrefix = familyPrefix {
            try encodeContainer.encode(familyPrefix, forKey: .familyPrefix)
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
    }
}

public struct ListTaskDefinitionFamiliesInputHeadersMiddleware: Middleware {
    public let id: String = "ListTaskDefinitionFamiliesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTaskDefinitionFamiliesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTaskDefinitionFamiliesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTaskDefinitionFamiliesInput>
    public typealias MOutput = OperationOutput<ListTaskDefinitionFamiliesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTaskDefinitionFamiliesOutputError>
}

public struct ListTaskDefinitionFamiliesInputQueryItemMiddleware: Middleware {
    public let id: String = "ListTaskDefinitionFamiliesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTaskDefinitionFamiliesInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTaskDefinitionFamiliesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTaskDefinitionFamiliesInput>
    public typealias MOutput = OperationOutput<ListTaskDefinitionFamiliesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTaskDefinitionFamiliesOutputError>
}

public struct ListTaskDefinitionFamiliesInput: Equatable {
    /// <p>The <code>familyPrefix</code> is a string that is used to filter the results of
    /// 				<code>ListTaskDefinitionFamilies</code>. If you specify a <code>familyPrefix</code>,
    /// 			only task definition family names that begin with the <code>familyPrefix</code> string
    /// 			are returned.</p>
    public let familyPrefix: String?
    /// <p>The maximum number of task definition family results returned by
    /// 				<code>ListTaskDefinitionFamilies</code> in paginated output. When this parameter is
    /// 			used, <code>ListTaskDefinitions</code> only returns <code>maxResults</code> results in a
    /// 			single page along with a <code>nextToken</code> response element. The remaining results
    /// 			of the initial request can be seen by sending another
    /// 				<code>ListTaskDefinitionFamilies</code> request with the returned
    /// 				<code>nextToken</code> value. This value can be between 1 and
    /// 			100. If this parameter is not used, then
    /// 				<code>ListTaskDefinitionFamilies</code> returns up to 100 results
    /// 			and a <code>nextToken</code> value if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a
    /// 				<code>ListTaskDefinitionFamilies</code> request indicating that more results are
    /// 			available to fulfill the request and further calls will be needed. If
    /// 				<code>maxResults</code> was provided, it is possible the number of results to be
    /// 			fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>The task definition family status with which to filter the
    /// 				<code>ListTaskDefinitionFamilies</code> results. By default, both
    /// 				<code>ACTIVE</code> and <code>INACTIVE</code> task definition families are listed.
    /// 			If this parameter is set to <code>ACTIVE</code>, only task definition families that have
    /// 			an <code>ACTIVE</code> task definition revision are returned. If this parameter is set
    /// 			to <code>INACTIVE</code>, only task definition families that do not have any
    /// 				<code>ACTIVE</code> task definition revisions are returned. If you paginate the
    /// 			resulting output, be sure to keep the <code>status</code> value constant in each
    /// 			subsequent request.</p>
    public let status: TaskDefinitionFamilyStatus?

    public init (
        familyPrefix: String? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        status: TaskDefinitionFamilyStatus? = nil
    )
    {
        self.familyPrefix = familyPrefix
        self.maxResults = maxResults
        self.nextToken = nextToken
        self.status = status
    }
}

struct ListTaskDefinitionFamiliesInputBody: Equatable {
    public let familyPrefix: String?
    public let status: TaskDefinitionFamilyStatus?
    public let nextToken: String?
    public let maxResults: Int?
}

extension ListTaskDefinitionFamiliesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case familyPrefix
        case maxResults
        case nextToken
        case status
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let familyPrefixDecoded = try containerValues.decodeIfPresent(String.self, forKey: .familyPrefix)
        familyPrefix = familyPrefixDecoded
        let statusDecoded = try containerValues.decodeIfPresent(TaskDefinitionFamilyStatus.self, forKey: .status)
        status = statusDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
    }
}

extension ListTaskDefinitionFamiliesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListTaskDefinitionFamiliesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListTaskDefinitionFamiliesOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListTaskDefinitionFamiliesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTaskDefinitionFamiliesOutputResponse(families: \(String(describing: families)), nextToken: \(String(describing: nextToken)))"}
}

extension ListTaskDefinitionFamiliesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListTaskDefinitionFamiliesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.families = output.families
            self.nextToken = output.nextToken
        } else {
            self.families = nil
            self.nextToken = nil
        }
    }
}

public struct ListTaskDefinitionFamiliesOutputResponse: Equatable {
    /// <p>The list of task definition family names that match the
    /// 				<code>ListTaskDefinitionFamilies</code> request.</p>
    public let families: [String]?
    /// <p>The <code>nextToken</code> value to include in a future
    /// 				<code>ListTaskDefinitionFamilies</code> request. When the results of a
    /// 				<code>ListTaskDefinitionFamilies</code> request exceed <code>maxResults</code>, this
    /// 			value can be used to retrieve the next page of results. This value is <code>null</code>
    /// 			when there are no more results to return.</p>
    public let nextToken: String?

    public init (
        families: [String]? = nil,
        nextToken: String? = nil
    )
    {
        self.families = families
        self.nextToken = nextToken
    }
}

struct ListTaskDefinitionFamiliesOutputResponseBody: Equatable {
    public let families: [String]?
    public let nextToken: String?
}

extension ListTaskDefinitionFamiliesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case families
        case nextToken
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let familiesContainer = try containerValues.decodeIfPresent([String?].self, forKey: .families)
        var familiesDecoded0:[String]? = nil
        if let familiesContainer = familiesContainer {
            familiesDecoded0 = [String]()
            for string0 in familiesContainer {
                if let string0 = string0 {
                    familiesDecoded0?.append(string0)
                }
            }
        }
        families = familiesDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListTaskDefinitionsInputBodyMiddleware: Middleware {
    public let id: String = "ListTaskDefinitionsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTaskDefinitionsInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTaskDefinitionsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTaskDefinitionsInput>
    public typealias MOutput = OperationOutput<ListTaskDefinitionsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTaskDefinitionsOutputError>
}

extension ListTaskDefinitionsInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTaskDefinitionsInput(familyPrefix: \(String(describing: familyPrefix)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)), sort: \(String(describing: sort)), status: \(String(describing: status)))"}
}

extension ListTaskDefinitionsInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case familyPrefix
        case maxResults
        case nextToken
        case sort
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let familyPrefix = familyPrefix {
            try encodeContainer.encode(familyPrefix, forKey: .familyPrefix)
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let sort = sort {
            try encodeContainer.encode(sort.rawValue, forKey: .sort)
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
    }
}

public struct ListTaskDefinitionsInputHeadersMiddleware: Middleware {
    public let id: String = "ListTaskDefinitionsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTaskDefinitionsInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTaskDefinitionsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTaskDefinitionsInput>
    public typealias MOutput = OperationOutput<ListTaskDefinitionsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTaskDefinitionsOutputError>
}

public struct ListTaskDefinitionsInputQueryItemMiddleware: Middleware {
    public let id: String = "ListTaskDefinitionsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTaskDefinitionsInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTaskDefinitionsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTaskDefinitionsInput>
    public typealias MOutput = OperationOutput<ListTaskDefinitionsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTaskDefinitionsOutputError>
}

public struct ListTaskDefinitionsInput: Equatable {
    /// <p>The full family name with which to filter the <code>ListTaskDefinitions</code>
    /// 			results. Specifying a <code>familyPrefix</code> limits the listed task definitions to
    /// 			task definition revisions that belong to that family.</p>
    public let familyPrefix: String?
    /// <p>The maximum number of task definition results returned by
    /// 				<code>ListTaskDefinitions</code> in paginated output. When this parameter is used,
    /// 				<code>ListTaskDefinitions</code> only returns <code>maxResults</code> results in a
    /// 			single page along with a <code>nextToken</code> response element. The remaining results
    /// 			of the initial request can be seen by sending another <code>ListTaskDefinitions</code>
    /// 			request with the returned <code>nextToken</code> value. This value can be between
    /// 			1 and 100. If this parameter is not used, then
    /// 				<code>ListTaskDefinitions</code> returns up to 100 results and a
    /// 				<code>nextToken</code> value if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a <code>ListTaskDefinitions</code>
    /// 			request indicating that more results are available to fulfill the request and further
    /// 			calls will be needed. If <code>maxResults</code> was provided, it is possible the number
    /// 			of results to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>The order in which to sort the results. Valid values are <code>ASC</code> and
    /// 				<code>DESC</code>. By default (<code>ASC</code>), task definitions are listed
    /// 			lexicographically by family name and in ascending numerical order by revision so that
    /// 			the newest task definitions in a family are listed last. Setting this parameter to
    /// 				<code>DESC</code> reverses the sort order on family name and revision so that the
    /// 			newest task definitions in a family are listed first.</p>
    public let sort: SortOrder?
    /// <p>The task definition status with which to filter the <code>ListTaskDefinitions</code>
    /// 			results. By default, only <code>ACTIVE</code> task definitions are listed. By setting
    /// 			this parameter to <code>INACTIVE</code>, you can view task definitions that are
    /// 				<code>INACTIVE</code> as long as an active task or service still references them. If
    /// 			you paginate the resulting output, be sure to keep the <code>status</code> value
    /// 			constant in each subsequent request.</p>
    public let status: TaskDefinitionStatus?

    public init (
        familyPrefix: String? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        sort: SortOrder? = nil,
        status: TaskDefinitionStatus? = nil
    )
    {
        self.familyPrefix = familyPrefix
        self.maxResults = maxResults
        self.nextToken = nextToken
        self.sort = sort
        self.status = status
    }
}

struct ListTaskDefinitionsInputBody: Equatable {
    public let familyPrefix: String?
    public let status: TaskDefinitionStatus?
    public let sort: SortOrder?
    public let nextToken: String?
    public let maxResults: Int?
}

extension ListTaskDefinitionsInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case familyPrefix
        case maxResults
        case nextToken
        case sort
        case status
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let familyPrefixDecoded = try containerValues.decodeIfPresent(String.self, forKey: .familyPrefix)
        familyPrefix = familyPrefixDecoded
        let statusDecoded = try containerValues.decodeIfPresent(TaskDefinitionStatus.self, forKey: .status)
        status = statusDecoded
        let sortDecoded = try containerValues.decodeIfPresent(SortOrder.self, forKey: .sort)
        sort = sortDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
    }
}

extension ListTaskDefinitionsOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListTaskDefinitionsOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListTaskDefinitionsOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListTaskDefinitionsOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTaskDefinitionsOutputResponse(nextToken: \(String(describing: nextToken)), taskDefinitionArns: \(String(describing: taskDefinitionArns)))"}
}

extension ListTaskDefinitionsOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListTaskDefinitionsOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.nextToken = output.nextToken
            self.taskDefinitionArns = output.taskDefinitionArns
        } else {
            self.nextToken = nil
            self.taskDefinitionArns = nil
        }
    }
}

public struct ListTaskDefinitionsOutputResponse: Equatable {
    /// <p>The <code>nextToken</code> value to include in a future
    /// 				<code>ListTaskDefinitions</code> request. When the results of a
    /// 				<code>ListTaskDefinitions</code> request exceed <code>maxResults</code>, this value
    /// 			can be used to retrieve the next page of results. This value is <code>null</code> when
    /// 			there are no more results to return.</p>
    public let nextToken: String?
    /// <p>The list of task definition Amazon Resource Name (ARN) entries for the <code>ListTaskDefinitions</code>
    /// 			request.</p>
    public let taskDefinitionArns: [String]?

    public init (
        nextToken: String? = nil,
        taskDefinitionArns: [String]? = nil
    )
    {
        self.nextToken = nextToken
        self.taskDefinitionArns = taskDefinitionArns
    }
}

struct ListTaskDefinitionsOutputResponseBody: Equatable {
    public let taskDefinitionArns: [String]?
    public let nextToken: String?
}

extension ListTaskDefinitionsOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case nextToken
        case taskDefinitionArns
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionArnsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .taskDefinitionArns)
        var taskDefinitionArnsDecoded0:[String]? = nil
        if let taskDefinitionArnsContainer = taskDefinitionArnsContainer {
            taskDefinitionArnsDecoded0 = [String]()
            for string0 in taskDefinitionArnsContainer {
                if let string0 = string0 {
                    taskDefinitionArnsDecoded0?.append(string0)
                }
            }
        }
        taskDefinitionArns = taskDefinitionArnsDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

public struct ListTasksInputBodyMiddleware: Middleware {
    public let id: String = "ListTasksInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTasksInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTasksOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTasksInput>
    public typealias MOutput = OperationOutput<ListTasksOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTasksOutputError>
}

extension ListTasksInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTasksInput(cluster: \(String(describing: cluster)), containerInstance: \(String(describing: containerInstance)), desiredStatus: \(String(describing: desiredStatus)), family: \(String(describing: family)), launchType: \(String(describing: launchType)), maxResults: \(String(describing: maxResults)), nextToken: \(String(describing: nextToken)), serviceName: \(String(describing: serviceName)), startedBy: \(String(describing: startedBy)))"}
}

extension ListTasksInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
        case desiredStatus
        case family
        case launchType
        case maxResults
        case nextToken
        case serviceName
        case startedBy
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstance = containerInstance {
            try encodeContainer.encode(containerInstance, forKey: .containerInstance)
        }
        if let desiredStatus = desiredStatus {
            try encodeContainer.encode(desiredStatus.rawValue, forKey: .desiredStatus)
        }
        if let family = family {
            try encodeContainer.encode(family, forKey: .family)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let maxResults = maxResults {
            try encodeContainer.encode(maxResults, forKey: .maxResults)
        }
        if let nextToken = nextToken {
            try encodeContainer.encode(nextToken, forKey: .nextToken)
        }
        if let serviceName = serviceName {
            try encodeContainer.encode(serviceName, forKey: .serviceName)
        }
        if let startedBy = startedBy {
            try encodeContainer.encode(startedBy, forKey: .startedBy)
        }
    }
}

public struct ListTasksInputHeadersMiddleware: Middleware {
    public let id: String = "ListTasksInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTasksInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTasksOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTasksInput>
    public typealias MOutput = OperationOutput<ListTasksOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTasksOutputError>
}

public struct ListTasksInputQueryItemMiddleware: Middleware {
    public let id: String = "ListTasksInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<ListTasksInput>,
                  next: H) -> Swift.Result<OperationOutput<ListTasksOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<ListTasksInput>
    public typealias MOutput = OperationOutput<ListTasksOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<ListTasksOutputError>
}

public struct ListTasksInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster to use when filtering the
    /// 				<code>ListTasks</code> results. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The container instance ID or full ARN of the container instance to use when
    /// 			filtering the <code>ListTasks</code> results. Specifying a
    /// 				<code>containerInstance</code> limits the results to tasks that belong to that
    /// 			container instance.</p>
    public let containerInstance: String?
    /// <p>The task desired status to use when filtering the <code>ListTasks</code> results.
    /// 			Specifying a <code>desiredStatus</code> of <code>STOPPED</code> limits the results to
    /// 			tasks that Amazon ECS has set the desired status to <code>STOPPED</code>. This can be useful
    /// 			for debugging tasks that are not starting properly or have died or finished. The default
    /// 			status filter is <code>RUNNING</code>, which shows tasks that Amazon ECS has set the desired
    /// 			status to <code>RUNNING</code>.</p>
    /// 		       <note>
    /// 			         <p>Although you can filter results based on a desired status of <code>PENDING</code>,
    /// 				this does not return any results. Amazon ECS never sets the desired status of a task to
    /// 				that value (only a task's <code>lastStatus</code> may have a value of
    /// 					<code>PENDING</code>).</p>
    /// 		       </note>
    public let desiredStatus: DesiredStatus?
    /// <p>The name of the task definition family to use when filtering the
    /// 				<code>ListTasks</code> results. Specifying a <code>family</code> limits the results
    /// 			to tasks that belong to that family.</p>
    public let family: String?
    /// <p>The launch type to use when filtering the <code>ListTasks</code> results.</p>
    public let launchType: LaunchType?
    /// <p>The maximum number of task results returned by <code>ListTasks</code> in paginated
    /// 			output. When this parameter is used, <code>ListTasks</code> only returns
    /// 				<code>maxResults</code> results in a single page along with a <code>nextToken</code>
    /// 			response element. The remaining results of the initial request can be seen by sending
    /// 			another <code>ListTasks</code> request with the returned <code>nextToken</code> value.
    /// 			This value can be between 1 and 100. If this parameter is
    /// 			not used, then <code>ListTasks</code> returns up to 100 results and a
    /// 				<code>nextToken</code> value if applicable.</p>
    public let maxResults: Int?
    /// <p>The <code>nextToken</code> value returned from a <code>ListTasks</code> request
    /// 			indicating that more results are available to fulfill the request and further calls will
    /// 			be needed. If <code>maxResults</code> was provided, it is possible the number of results
    /// 			to be fewer than <code>maxResults</code>.</p>
    ///          <note>
    ///             <p>This token should be treated as an opaque identifier that is only used to
    ///                 retrieve the next items in a list and not for other programmatic purposes.</p>
    ///         </note>
    public let nextToken: String?
    /// <p>The name of the service to use when filtering the <code>ListTasks</code> results.
    /// 			Specifying a <code>serviceName</code> limits the results to tasks that belong to that
    /// 			service.</p>
    public let serviceName: String?
    /// <p>The <code>startedBy</code> value with which to filter the task results. Specifying a
    /// 				<code>startedBy</code> value limits the results to tasks that were started with that
    /// 			value.</p>
    public let startedBy: String?

    public init (
        cluster: String? = nil,
        containerInstance: String? = nil,
        desiredStatus: DesiredStatus? = nil,
        family: String? = nil,
        launchType: LaunchType? = nil,
        maxResults: Int? = nil,
        nextToken: String? = nil,
        serviceName: String? = nil,
        startedBy: String? = nil
    )
    {
        self.cluster = cluster
        self.containerInstance = containerInstance
        self.desiredStatus = desiredStatus
        self.family = family
        self.launchType = launchType
        self.maxResults = maxResults
        self.nextToken = nextToken
        self.serviceName = serviceName
        self.startedBy = startedBy
    }
}

struct ListTasksInputBody: Equatable {
    public let cluster: String?
    public let containerInstance: String?
    public let family: String?
    public let nextToken: String?
    public let maxResults: Int?
    public let startedBy: String?
    public let serviceName: String?
    public let desiredStatus: DesiredStatus?
    public let launchType: LaunchType?
}

extension ListTasksInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
        case desiredStatus
        case family
        case launchType
        case maxResults
        case nextToken
        case serviceName
        case startedBy
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerInstanceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
        let familyDecoded = try containerValues.decodeIfPresent(String.self, forKey: .family)
        family = familyDecoded
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
        let maxResultsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maxResults)
        maxResults = maxResultsDecoded
        let startedByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .startedBy)
        startedBy = startedByDecoded
        let serviceNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceName)
        serviceName = serviceNameDecoded
        let desiredStatusDecoded = try containerValues.decodeIfPresent(DesiredStatus.self, forKey: .desiredStatus)
        desiredStatus = desiredStatusDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
    }
}

extension ListTasksOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension ListTasksOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum ListTasksOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case serviceNotFoundException(ServiceNotFoundException)
    case unknown(UnknownAWSHttpServiceError)
}

extension ListTasksOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ListTasksOutputResponse(nextToken: \(String(describing: nextToken)), taskArns: \(String(describing: taskArns)))"}
}

extension ListTasksOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ListTasksOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.nextToken = output.nextToken
            self.taskArns = output.taskArns
        } else {
            self.nextToken = nil
            self.taskArns = nil
        }
    }
}

public struct ListTasksOutputResponse: Equatable {
    /// <p>The <code>nextToken</code> value to include in a future <code>ListTasks</code>
    /// 			request. When the results of a <code>ListTasks</code> request exceed
    /// 				<code>maxResults</code>, this value can be used to retrieve the next page of
    /// 			results. This value is <code>null</code> when there are no more results to
    /// 			return.</p>
    public let nextToken: String?
    /// <p>The list of task ARN entries for the <code>ListTasks</code> request.</p>
    public let taskArns: [String]?

    public init (
        nextToken: String? = nil,
        taskArns: [String]? = nil
    )
    {
        self.nextToken = nextToken
        self.taskArns = taskArns
    }
}

struct ListTasksOutputResponseBody: Equatable {
    public let taskArns: [String]?
    public let nextToken: String?
}

extension ListTasksOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case nextToken
        case taskArns
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskArnsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .taskArns)
        var taskArnsDecoded0:[String]? = nil
        if let taskArnsContainer = taskArnsContainer {
            taskArnsDecoded0 = [String]()
            for string0 in taskArnsContainer {
                if let string0 = string0 {
                    taskArnsDecoded0?.append(string0)
                }
            }
        }
        taskArns = taskArnsDecoded0
        let nextTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextToken)
        nextToken = nextTokenDecoded
    }
}

extension LoadBalancer: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerName
        case containerPort
        case loadBalancerName
        case targetGroupArn
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
        if let containerPort = containerPort {
            try encodeContainer.encode(containerPort, forKey: .containerPort)
        }
        if let loadBalancerName = loadBalancerName {
            try encodeContainer.encode(loadBalancerName, forKey: .loadBalancerName)
        }
        if let targetGroupArn = targetGroupArn {
            try encodeContainer.encode(targetGroupArn, forKey: .targetGroupArn)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let targetGroupArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .targetGroupArn)
        targetGroupArn = targetGroupArnDecoded
        let loadBalancerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .loadBalancerName)
        loadBalancerName = loadBalancerNameDecoded
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let containerPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .containerPort)
        containerPort = containerPortDecoded
    }
}

extension LoadBalancer: CustomDebugStringConvertible {
    public var debugDescription: String {
        "LoadBalancer(containerName: \(String(describing: containerName)), containerPort: \(String(describing: containerPort)), loadBalancerName: \(String(describing: loadBalancerName)), targetGroupArn: \(String(describing: targetGroupArn)))"}
}

/// <p>The load balancer configuration to use with a service or task set.</p>
/// 		       <p>For specific notes and restrictions regarding the use of load balancers with services
/// 			and task sets, see the CreateService and CreateTaskSet actions.</p>
public struct LoadBalancer: Equatable {
    /// <p>The name of the container (as it appears in a container definition) to associate with
    /// 			the load balancer.</p>
    public let containerName: String?
    /// <p>The port on the container to associate with the load balancer. This port must
    /// 			correspond to a <code>containerPort</code> in the task definition the tasks in the
    /// 			service are using. For tasks that use the EC2 launch type, the container
    /// 			instance they are launched on must allow ingress traffic on the <code>hostPort</code> of
    /// 			the port mapping.</p>
    public let containerPort: Int?
    /// <p>The name of the load balancer to associate with the Amazon ECS service or task set.</p>
    /// 		       <p>A load balancer name is only specified when using a Classic Load Balancer. If you are using an Application Load Balancer
    /// 			or a Network Load Balancer the load balancer name parameter should be omitted.</p>
    public let loadBalancerName: String?
    /// <p>The full Amazon Resource Name (ARN) of the Elastic Load Balancing target group or groups associated with a service or
    /// 			task set.</p>
    /// 		       <p>A target group ARN is only specified when using an Application Load Balancer or Network Load Balancer. If you are using a
    /// 			Classic Load Balancer the target group ARN should be omitted.</p>
    /// 		       <p>For services using the <code>ECS</code> deployment controller, you can specify one or
    /// 			multiple target groups. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/register-multiple-targetgroups.html">Registering Multiple Target Groups with a Service</a> in
    /// 			the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>For services using the <code>CODE_DEPLOY</code> deployment controller, you are
    /// 			required to define two target groups for the load balancer. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-type-bluegreen.html">Blue/Green Deployment with CodeDeploy</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <important>
    /// 			         <p>If your service's task definition uses the <code>awsvpc</code> network mode (which
    /// 				is required for the Fargate launch type), you must choose
    /// 					<code>ip</code> as the target type, not <code>instance</code>, when creating
    /// 				your target groups because tasks that use the <code>awsvpc</code> network mode are
    /// 				associated with an elastic network interface, not an Amazon EC2 instance.</p>
    /// 		       </important>
    public let targetGroupArn: String?

    public init (
        containerName: String? = nil,
        containerPort: Int? = nil,
        loadBalancerName: String? = nil,
        targetGroupArn: String? = nil
    )
    {
        self.containerName = containerName
        self.containerPort = containerPort
        self.loadBalancerName = loadBalancerName
        self.targetGroupArn = targetGroupArn
    }
}

extension LogConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case logDriver
        case options
        case secretOptions
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let logDriver = logDriver {
            try encodeContainer.encode(logDriver.rawValue, forKey: .logDriver)
        }
        if let options = options {
            var optionsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .options)
            for (dictKey0, logconfigurationoptionsmap0) in options {
                try optionsContainer.encode(logconfigurationoptionsmap0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let secretOptions = secretOptions {
            var secretOptionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .secretOptions)
            for secretlist0 in secretOptions {
                try secretOptionsContainer.encode(secretlist0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let logDriverDecoded = try containerValues.decodeIfPresent(LogDriver.self, forKey: .logDriver)
        logDriver = logDriverDecoded
        let optionsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .options)
        var optionsDecoded0: [String:String]? = nil
        if let optionsContainer = optionsContainer {
            optionsDecoded0 = [String:String]()
            for (key0, string0) in optionsContainer {
                if let string0 = string0 {
                    optionsDecoded0?[key0] = string0
                }
            }
        }
        options = optionsDecoded0
        let secretOptionsContainer = try containerValues.decodeIfPresent([Secret?].self, forKey: .secretOptions)
        var secretOptionsDecoded0:[Secret]? = nil
        if let secretOptionsContainer = secretOptionsContainer {
            secretOptionsDecoded0 = [Secret]()
            for structure0 in secretOptionsContainer {
                if let structure0 = structure0 {
                    secretOptionsDecoded0?.append(structure0)
                }
            }
        }
        secretOptions = secretOptionsDecoded0
    }
}

extension LogConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "LogConfiguration(logDriver: \(String(describing: logDriver)), options: \(String(describing: options)), secretOptions: \(String(describing: secretOptions)))"}
}

/// <p>The log configuration for the container. This parameter maps to <code>LogConfig</code>
/// 			in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the
/// 				<code>--log-driver</code> option to <a href="https://docs.docker.com/engine/reference/commandline/run/">
///                <code>docker
/// 					run</code>
///             </a>.</p>
/// 		       <p>By default, containers use the same logging driver that the Docker daemon uses;
/// 			however the container may use a different logging driver than the Docker daemon by
/// 			specifying a log driver configuration in the container definition. For more information
/// 			on the options for different supported log drivers, see <a href="https://docs.docker.com/engine/admin/logging/overview/">Configure logging
/// 				drivers</a> in the Docker documentation.</p>
/// 		       <p>The following should be noted when specifying a log configuration for your
/// 			containers:</p>
/// 		       <ul>
///             <li>
/// 				           <p>Amazon ECS currently supports a subset of the logging drivers available to the
/// 					Docker daemon (shown in the valid values below). Additional log drivers may be
/// 					available in future releases of the Amazon ECS container agent.</p>
/// 			         </li>
///             <li>
/// 				           <p>This parameter requires version 1.18 of the Docker Remote API or greater on
/// 					your container instance.</p>
/// 			         </li>
///             <li>
/// 				           <p>For tasks hosted on Amazon EC2 instances, the Amazon ECS container agent must register
/// 					the available logging drivers with the
/// 						<code>ECS_AVAILABLE_LOGGING_DRIVERS</code> environment variable before
/// 					containers placed on that instance can use these log configuration options. For
/// 					more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-agent-config.html">Amazon ECS container agent configuration</a> in the
/// 						<i>Amazon Elastic Container Service Developer Guide</i>.</p>
/// 			         </li>
///             <li>
/// 				           <p>For tasks on AWS Fargate, because you do not have access to the underlying
/// 					infrastructure your tasks are hosted on, any additional software needed will
/// 					have to be installed outside of the task. For example, the Fluentd output
/// 					aggregators or a remote host running Logstash to send Gelf logs to.</p>
/// 			         </li>
///          </ul>
public struct LogConfiguration: Equatable {
    /// <p>The log driver to use for the container.</p>
    /// 		       <p>For tasks on AWS Fargate, the supported log drivers are <code>awslogs</code>,
    /// 				<code>splunk</code>, and <code>awsfirelens</code>.</p>
    /// 		       <p>For tasks hosted on Amazon EC2 instances, the supported log drivers are
    /// 				<code>awslogs</code>, <code>fluentd</code>, <code>gelf</code>,
    /// 				<code>json-file</code>, <code>journald</code>,
    /// 				<code>logentries</code>,<code>syslog</code>, <code>splunk</code>, and
    /// 				<code>awsfirelens</code>.</p>
    /// 		       <p>For more information about using the <code>awslogs</code> log driver, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_awslogs.html">Using
    /// 				the awslogs log driver</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>For more information about using the <code>awsfirelens</code> log driver, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_firelens.html">Custom log routing</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <note>
    /// 			         <p>If you have a custom driver that is not listed, you can fork the Amazon ECS container
    /// 				agent project that is <a href="https://github.com/aws/amazon-ecs-agent">available
    /// 					on GitHub</a> and customize it to work with that driver. We encourage you to
    /// 				submit pull requests for changes that you would like to have included. However, we
    /// 				do not currently provide support for running modified copies of this
    /// 				software.</p>
    /// 		       </note>
    public let logDriver: LogDriver?
    /// <p>The configuration options to send to the log driver. This parameter requires version 1.19 of the Docker Remote API or greater on your container instance. To check the Docker Remote API version on your container instance, log in to your container instance and run the following command: <code>sudo docker version --format '{{.Server.APIVersion}}'</code>
    ///          </p>
    public let options: [String:String]?
    /// <p>The secrets to pass to the log configuration. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/specifying-sensitive-data.html">Specifying
    /// 				Sensitive Data</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let secretOptions: [Secret]?

    public init (
        logDriver: LogDriver? = nil,
        options: [String:String]? = nil,
        secretOptions: [Secret]? = nil
    )
    {
        self.logDriver = logDriver
        self.options = options
        self.secretOptions = secretOptions
    }
}

public enum LogDriver {
    case awsfirelens
    case awslogs
    case fluentd
    case gelf
    case journald
    case jsonFile
    case splunk
    case syslog
    case sdkUnknown(String)
}

extension LogDriver : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [LogDriver] {
        return [
            .awsfirelens,
            .awslogs,
            .fluentd,
            .gelf,
            .journald,
            .jsonFile,
            .splunk,
            .syslog,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .awsfirelens: return "awsfirelens"
        case .awslogs: return "awslogs"
        case .fluentd: return "fluentd"
        case .gelf: return "gelf"
        case .journald: return "journald"
        case .jsonFile: return "json-file"
        case .splunk: return "splunk"
        case .syslog: return "syslog"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = LogDriver(rawValue: rawValue) ?? LogDriver.sdkUnknown(rawValue)
    }
}

extension ManagedAgent: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case lastStartedAt
        case lastStatus
        case name
        case reason
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let lastStartedAt = lastStartedAt {
            try encodeContainer.encode(lastStartedAt.timeIntervalSince1970, forKey: .lastStartedAt)
        }
        if let lastStatus = lastStatus {
            try encodeContainer.encode(lastStatus, forKey: .lastStatus)
        }
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let lastStartedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .lastStartedAt)
        lastStartedAt = lastStartedAtDecoded
        let nameDecoded = try containerValues.decodeIfPresent(ManagedAgentName.self, forKey: .name)
        name = nameDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
        let lastStatusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .lastStatus)
        lastStatus = lastStatusDecoded
    }
}

extension ManagedAgent: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ManagedAgent(lastStartedAt: \(String(describing: lastStartedAt)), lastStatus: \(String(describing: lastStatus)), name: \(String(describing: name)), reason: \(String(describing: reason)))"}
}

/// <p>Details about the managed agent status for the container.</p>
public struct ManagedAgent: Equatable {
    /// <p>The Unix timestamp for when the managed agent was last started.</p>
    public let lastStartedAt: Date?
    /// <p>The last known status of the managed agent.</p>
    public let lastStatus: String?
    /// <p>The name of the managed agent. When the execute command feature is enabled, the
    /// 			managed agent name is <code>ExecuteCommandAgent</code>.</p>
    public let name: ManagedAgentName?
    /// <p>The reason for why the managed agent is in the state it is in.</p>
    public let reason: String?

    public init (
        lastStartedAt: Date? = nil,
        lastStatus: String? = nil,
        name: ManagedAgentName? = nil,
        reason: String? = nil
    )
    {
        self.lastStartedAt = lastStartedAt
        self.lastStatus = lastStatus
        self.name = name
        self.reason = reason
    }
}

public enum ManagedAgentName {
    case executecommandagent
    case sdkUnknown(String)
}

extension ManagedAgentName : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ManagedAgentName] {
        return [
            .executecommandagent,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .executecommandagent: return "ExecuteCommandAgent"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ManagedAgentName(rawValue: rawValue) ?? ManagedAgentName.sdkUnknown(rawValue)
    }
}

extension ManagedAgentStateChange: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerName
        case managedAgentName
        case reason
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
        if let managedAgentName = managedAgentName {
            try encodeContainer.encode(managedAgentName.rawValue, forKey: .managedAgentName)
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let managedAgentNameDecoded = try containerValues.decodeIfPresent(ManagedAgentName.self, forKey: .managedAgentName)
        managedAgentName = managedAgentNameDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
    }
}

extension ManagedAgentStateChange: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ManagedAgentStateChange(containerName: \(String(describing: containerName)), managedAgentName: \(String(describing: managedAgentName)), reason: \(String(describing: reason)), status: \(String(describing: status)))"}
}

/// <p>An object representing a change in state for a managed agent.</p>
public struct ManagedAgentStateChange: Equatable {
    /// <p>The name of the container associated with the managed agent.</p>
    public let containerName: String?
    /// <p>The name of the managed agent.</p>
    public let managedAgentName: ManagedAgentName?
    /// <p>The reason for the status of the managed agent.</p>
    public let reason: String?
    /// <p>The status of the managed agent.</p>
    public let status: String?

    public init (
        containerName: String? = nil,
        managedAgentName: ManagedAgentName? = nil,
        reason: String? = nil,
        status: String? = nil
    )
    {
        self.containerName = containerName
        self.managedAgentName = managedAgentName
        self.reason = reason
        self.status = status
    }
}

extension ManagedScaling: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case instanceWarmupPeriod
        case maximumScalingStepSize
        case minimumScalingStepSize
        case status
        case targetCapacity
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let instanceWarmupPeriod = instanceWarmupPeriod {
            try encodeContainer.encode(instanceWarmupPeriod, forKey: .instanceWarmupPeriod)
        }
        if let maximumScalingStepSize = maximumScalingStepSize {
            try encodeContainer.encode(maximumScalingStepSize, forKey: .maximumScalingStepSize)
        }
        if let minimumScalingStepSize = minimumScalingStepSize {
            try encodeContainer.encode(minimumScalingStepSize, forKey: .minimumScalingStepSize)
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
        if let targetCapacity = targetCapacity {
            try encodeContainer.encode(targetCapacity, forKey: .targetCapacity)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let statusDecoded = try containerValues.decodeIfPresent(ManagedScalingStatus.self, forKey: .status)
        status = statusDecoded
        let targetCapacityDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .targetCapacity)
        targetCapacity = targetCapacityDecoded
        let minimumScalingStepSizeDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .minimumScalingStepSize)
        minimumScalingStepSize = minimumScalingStepSizeDecoded
        let maximumScalingStepSizeDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .maximumScalingStepSize)
        maximumScalingStepSize = maximumScalingStepSizeDecoded
        let instanceWarmupPeriodDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .instanceWarmupPeriod)
        instanceWarmupPeriod = instanceWarmupPeriodDecoded
    }
}

extension ManagedScaling: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ManagedScaling(instanceWarmupPeriod: \(String(describing: instanceWarmupPeriod)), maximumScalingStepSize: \(String(describing: maximumScalingStepSize)), minimumScalingStepSize: \(String(describing: minimumScalingStepSize)), status: \(String(describing: status)), targetCapacity: \(String(describing: targetCapacity)))"}
}

/// <p>The managed scaling settings for the Auto Scaling group capacity provider.</p>
/// 		       <p>When managed scaling is enabled, Amazon ECS manages the scale-in and scale-out actions of
/// 			the Auto Scaling group. Amazon ECS manages a target tracking scaling policy using an
/// 			Amazon ECS-managed CloudWatch metric with the specified <code>targetCapacity</code> value as the
/// 			target value for the metric. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/asg-capacity-providers.html#asg-capacity-providers-managed-scaling">Using Managed Scaling</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
/// 		       <p>If managed scaling is disabled, the user must manage the scaling of the Auto Scaling
/// 			group.</p>
public struct ManagedScaling: Equatable {
    /// <p>The period of time, in seconds, after a newly launched Amazon EC2 instance can contribute
    /// 			to CloudWatch metrics for Auto Scaling group. If this parameter is omitted, the default value
    /// 			of <code>300</code> seconds is used.</p>
    public let instanceWarmupPeriod: Int?
    /// <p>The maximum number of container instances that Amazon ECS will scale in or scale out at one
    /// 			time. If this parameter is omitted, the default value of <code>10000</code> is
    /// 			used.</p>
    public let maximumScalingStepSize: Int?
    /// <p>The minimum number of container instances that Amazon ECS will scale in or scale out at one
    /// 			time. If this parameter is omitted, the default value of <code>1</code> is used.</p>
    public let minimumScalingStepSize: Int?
    /// <p>Whether or not to enable managed scaling for the capacity provider.</p>
    public let status: ManagedScalingStatus?
    /// <p>The target capacity value for the capacity provider. The specified value must be
    /// 			greater than <code>0</code> and less than or equal to <code>100</code>. A value of
    /// 				<code>100</code> will result in the Amazon EC2 instances in your Auto Scaling group being
    /// 			completely utilized.</p>
    public let targetCapacity: Int?

    public init (
        instanceWarmupPeriod: Int? = nil,
        maximumScalingStepSize: Int? = nil,
        minimumScalingStepSize: Int? = nil,
        status: ManagedScalingStatus? = nil,
        targetCapacity: Int? = nil
    )
    {
        self.instanceWarmupPeriod = instanceWarmupPeriod
        self.maximumScalingStepSize = maximumScalingStepSize
        self.minimumScalingStepSize = minimumScalingStepSize
        self.status = status
        self.targetCapacity = targetCapacity
    }
}

public enum ManagedScalingStatus {
    case disabled
    case enabled
    case sdkUnknown(String)
}

extension ManagedScalingStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ManagedScalingStatus] {
        return [
            .disabled,
            .enabled,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .disabled: return "DISABLED"
        case .enabled: return "ENABLED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ManagedScalingStatus(rawValue: rawValue) ?? ManagedScalingStatus.sdkUnknown(rawValue)
    }
}

public enum ManagedTerminationProtection {
    case disabled
    case enabled
    case sdkUnknown(String)
}

extension ManagedTerminationProtection : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ManagedTerminationProtection] {
        return [
            .disabled,
            .enabled,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .disabled: return "DISABLED"
        case .enabled: return "ENABLED"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ManagedTerminationProtection(rawValue: rawValue) ?? ManagedTerminationProtection.sdkUnknown(rawValue)
    }
}

extension MissingVersionException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "MissingVersionException(message: \(String(describing: message)))"}
}

extension MissingVersionException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: MissingVersionExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>Amazon ECS is unable to determine the current version of the Amazon ECS container agent on the
/// 			container instance and does not have enough information to proceed with an update. This
/// 			could be because the agent running on the container instance is an older or custom
/// 			version that does not use our version information.</p>
public struct MissingVersionException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct MissingVersionExceptionBody: Equatable {
    public let message: String?
}

extension MissingVersionExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension MountPoint: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerPath
        case readOnly
        case sourceVolume
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerPath = containerPath {
            try encodeContainer.encode(containerPath, forKey: .containerPath)
        }
        if let readOnly = readOnly {
            try encodeContainer.encode(readOnly, forKey: .readOnly)
        }
        if let sourceVolume = sourceVolume {
            try encodeContainer.encode(sourceVolume, forKey: .sourceVolume)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let sourceVolumeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .sourceVolume)
        sourceVolume = sourceVolumeDecoded
        let containerPathDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerPath)
        containerPath = containerPathDecoded
        let readOnlyDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .readOnly)
        readOnly = readOnlyDecoded
    }
}

extension MountPoint: CustomDebugStringConvertible {
    public var debugDescription: String {
        "MountPoint(containerPath: \(String(describing: containerPath)), readOnly: \(String(describing: readOnly)), sourceVolume: \(String(describing: sourceVolume)))"}
}

/// <p>Details on a volume mount point that is used in a container definition.</p>
public struct MountPoint: Equatable {
    /// <p>The path on the container to mount the host volume at.</p>
    public let containerPath: String?
    /// <p>If this value is <code>true</code>, the container has read-only access to the volume.
    /// 			If this value is <code>false</code>, then the container can write to the volume. The
    /// 			default value is <code>false</code>.</p>
    public let readOnly: Bool?
    /// <p>The name of the volume to mount. Must be a volume name referenced in the
    /// 				<code>name</code> parameter of task definition <code>volume</code>.</p>
    public let sourceVolume: String?

    public init (
        containerPath: String? = nil,
        readOnly: Bool? = nil,
        sourceVolume: String? = nil
    )
    {
        self.containerPath = containerPath
        self.readOnly = readOnly
        self.sourceVolume = sourceVolume
    }
}

extension NetworkBinding: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case bindIP
        case containerPort
        case hostPort
        case `protocol` = "protocol"
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let bindIP = bindIP {
            try encodeContainer.encode(bindIP, forKey: .bindIP)
        }
        if let containerPort = containerPort {
            try encodeContainer.encode(containerPort, forKey: .containerPort)
        }
        if let hostPort = hostPort {
            try encodeContainer.encode(hostPort, forKey: .hostPort)
        }
        if let `protocol` = `protocol` {
            try encodeContainer.encode(`protocol`.rawValue, forKey: .`protocol`)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let bindIPDecoded = try containerValues.decodeIfPresent(String.self, forKey: .bindIP)
        bindIP = bindIPDecoded
        let containerPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .containerPort)
        containerPort = containerPortDecoded
        let hostPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .hostPort)
        hostPort = hostPortDecoded
        let protocolDecoded = try containerValues.decodeIfPresent(TransportProtocol.self, forKey: .protocol)
        `protocol` = protocolDecoded
    }
}

extension NetworkBinding: CustomDebugStringConvertible {
    public var debugDescription: String {
        "NetworkBinding(bindIP: \(String(describing: bindIP)), containerPort: \(String(describing: containerPort)), hostPort: \(String(describing: hostPort)), protocol: \(String(describing: `protocol`)))"}
}

/// <p>Details on the network bindings between a container and its host container instance.
/// 			After a task reaches the <code>RUNNING</code> status, manual and automatic host and
/// 			container port assignments are visible in the <code>networkBindings</code> section of
/// 				<a>DescribeTasks</a> API responses.</p>
public struct NetworkBinding: Equatable {
    /// <p>The protocol used for the network binding.</p>
    public let `protocol`: TransportProtocol?
    /// <p>The IP address that the container is bound to on the container instance.</p>
    public let bindIP: String?
    /// <p>The port number on the container that is used with the network binding.</p>
    public let containerPort: Int?
    /// <p>The port number on the host that is used with the network binding.</p>
    public let hostPort: Int?

    public init (
        `protocol`: TransportProtocol? = nil,
        bindIP: String? = nil,
        containerPort: Int? = nil,
        hostPort: Int? = nil
    )
    {
        self.`protocol` = `protocol`
        self.bindIP = bindIP
        self.containerPort = containerPort
        self.hostPort = hostPort
    }
}

extension NetworkConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case awsvpcConfiguration
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let awsvpcConfiguration = awsvpcConfiguration {
            try encodeContainer.encode(awsvpcConfiguration, forKey: .awsvpcConfiguration)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let awsvpcConfigurationDecoded = try containerValues.decodeIfPresent(AwsVpcConfiguration.self, forKey: .awsvpcConfiguration)
        awsvpcConfiguration = awsvpcConfigurationDecoded
    }
}

extension NetworkConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "NetworkConfiguration(awsvpcConfiguration: \(String(describing: awsvpcConfiguration)))"}
}

/// <p>An object representing the network configuration for a task or service.</p>
public struct NetworkConfiguration: Equatable {
    /// <p>The VPC subnets and security groups associated with a task.</p>
    /// 		       <note>
    /// 			         <p>All specified subnets and security groups must be from the same VPC.</p>
    /// 		       </note>
    public let awsvpcConfiguration: AwsVpcConfiguration?

    public init (
        awsvpcConfiguration: AwsVpcConfiguration? = nil
    )
    {
        self.awsvpcConfiguration = awsvpcConfiguration
    }
}

extension NetworkInterface: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attachmentId
        case ipv6Address
        case privateIpv4Address
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attachmentId = attachmentId {
            try encodeContainer.encode(attachmentId, forKey: .attachmentId)
        }
        if let ipv6Address = ipv6Address {
            try encodeContainer.encode(ipv6Address, forKey: .ipv6Address)
        }
        if let privateIpv4Address = privateIpv4Address {
            try encodeContainer.encode(privateIpv4Address, forKey: .privateIpv4Address)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let attachmentIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .attachmentId)
        attachmentId = attachmentIdDecoded
        let privateIpv4AddressDecoded = try containerValues.decodeIfPresent(String.self, forKey: .privateIpv4Address)
        privateIpv4Address = privateIpv4AddressDecoded
        let ipv6AddressDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ipv6Address)
        ipv6Address = ipv6AddressDecoded
    }
}

extension NetworkInterface: CustomDebugStringConvertible {
    public var debugDescription: String {
        "NetworkInterface(attachmentId: \(String(describing: attachmentId)), ipv6Address: \(String(describing: ipv6Address)), privateIpv4Address: \(String(describing: privateIpv4Address)))"}
}

/// <p>An object representing the elastic network interface for tasks that use the
/// 				<code>awsvpc</code> network mode.</p>
public struct NetworkInterface: Equatable {
    /// <p>The attachment ID for the network interface.</p>
    public let attachmentId: String?
    /// <p>The private IPv6 address for the network interface.</p>
    public let ipv6Address: String?
    /// <p>The private IPv4 address for the network interface.</p>
    public let privateIpv4Address: String?

    public init (
        attachmentId: String? = nil,
        ipv6Address: String? = nil,
        privateIpv4Address: String? = nil
    )
    {
        self.attachmentId = attachmentId
        self.ipv6Address = ipv6Address
        self.privateIpv4Address = privateIpv4Address
    }
}

public enum NetworkMode {
    case awsvpc
    case bridge
    case host
    case `none`
    case sdkUnknown(String)
}

extension NetworkMode : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [NetworkMode] {
        return [
            .awsvpc,
            .bridge,
            .host,
            .none,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .awsvpc: return "awsvpc"
        case .bridge: return "bridge"
        case .host: return "host"
        case .none: return "none"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = NetworkMode(rawValue: rawValue) ?? NetworkMode.sdkUnknown(rawValue)
    }
}

extension NoUpdateAvailableException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "NoUpdateAvailableException(message: \(String(describing: message)))"}
}

extension NoUpdateAvailableException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: NoUpdateAvailableExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>There is no update available for this Amazon ECS container agent. This could be because the
/// 			agent is already running the latest version, or it is so old that there is no update
/// 			path to the current version.</p>
public struct NoUpdateAvailableException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct NoUpdateAvailableExceptionBody: Equatable {
    public let message: String?
}

extension NoUpdateAvailableExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public enum PidMode {
    case host
    case task
    case sdkUnknown(String)
}

extension PidMode : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [PidMode] {
        return [
            .host,
            .task,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .host: return "host"
        case .task: return "task"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = PidMode(rawValue: rawValue) ?? PidMode.sdkUnknown(rawValue)
    }
}

extension PlacementConstraint: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case expression
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let expression = expression {
            try encodeContainer.encode(expression, forKey: .expression)
        }
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let typeDecoded = try containerValues.decodeIfPresent(PlacementConstraintType.self, forKey: .type)
        type = typeDecoded
        let expressionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .expression)
        expression = expressionDecoded
    }
}

extension PlacementConstraint: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PlacementConstraint(expression: \(String(describing: expression)), type: \(String(describing: type)))"}
}

/// <p>An object representing a constraint on task placement. For more information, see
/// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-placement-constraints.html">Task Placement Constraints</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
/// 		       <note>
/// 			         <p>If you are using the Fargate launch type, task placement constraints
/// 				are not supported.</p>
/// 		       </note>
public struct PlacementConstraint: Equatable {
    /// <p>A cluster query language expression to apply to the constraint. You cannot specify an
    /// 			expression if the constraint type is <code>distinctInstance</code>. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/cluster-query-language.html">Cluster Query Language</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let expression: String?
    /// <p>The type of constraint. Use <code>distinctInstance</code> to ensure that each task in
    /// 			a particular group is running on a different container instance. Use
    /// 				<code>memberOf</code> to restrict the selection to a group of valid
    /// 			candidates.</p>
    public let type: PlacementConstraintType?

    public init (
        expression: String? = nil,
        type: PlacementConstraintType? = nil
    )
    {
        self.expression = expression
        self.type = type
    }
}

public enum PlacementConstraintType {
    case distinctInstance
    case memberOf
    case sdkUnknown(String)
}

extension PlacementConstraintType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [PlacementConstraintType] {
        return [
            .distinctInstance,
            .memberOf,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .distinctInstance: return "distinctInstance"
        case .memberOf: return "memberOf"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = PlacementConstraintType(rawValue: rawValue) ?? PlacementConstraintType.sdkUnknown(rawValue)
    }
}

extension PlacementStrategy: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case field
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let field = field {
            try encodeContainer.encode(field, forKey: .field)
        }
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let typeDecoded = try containerValues.decodeIfPresent(PlacementStrategyType.self, forKey: .type)
        type = typeDecoded
        let fieldDecoded = try containerValues.decodeIfPresent(String.self, forKey: .field)
        field = fieldDecoded
    }
}

extension PlacementStrategy: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PlacementStrategy(field: \(String(describing: field)), type: \(String(describing: type)))"}
}

/// <p>The task placement strategy for a task or service. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-placement-strategies.html">Task Placement Strategies</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct PlacementStrategy: Equatable {
    /// <p>The field to apply the placement strategy against. For the <code>spread</code>
    /// 			placement strategy, valid values are <code>instanceId</code> (or <code>host</code>,
    /// 			which has the same effect), or any platform or custom attribute that is applied to a
    /// 			container instance, such as <code>attribute:ecs.availability-zone</code>. For the
    /// 				<code>binpack</code> placement strategy, valid values are <code>cpu</code> and
    /// 				<code>memory</code>. For the <code>random</code> placement strategy, this field is
    /// 			not used.</p>
    public let field: String?
    /// <p>The type of placement strategy. The <code>random</code> placement strategy randomly
    /// 			places tasks on available candidates. The <code>spread</code> placement strategy spreads
    /// 			placement across available candidates evenly based on the <code>field</code> parameter.
    /// 			The <code>binpack</code> strategy places tasks on available candidates that have the
    /// 			least available amount of the resource that is specified with the <code>field</code>
    /// 			parameter. For example, if you binpack on memory, a task is placed on the instance with
    /// 			the least amount of remaining memory (but still enough to run the task).</p>
    public let type: PlacementStrategyType?

    public init (
        field: String? = nil,
        type: PlacementStrategyType? = nil
    )
    {
        self.field = field
        self.type = type
    }
}

public enum PlacementStrategyType {
    case binpack
    case random
    case spread
    case sdkUnknown(String)
}

extension PlacementStrategyType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [PlacementStrategyType] {
        return [
            .binpack,
            .random,
            .spread,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .binpack: return "binpack"
        case .random: return "random"
        case .spread: return "spread"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = PlacementStrategyType(rawValue: rawValue) ?? PlacementStrategyType.sdkUnknown(rawValue)
    }
}

extension PlatformDevice: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case id
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
        let typeDecoded = try containerValues.decodeIfPresent(PlatformDeviceType.self, forKey: .type)
        type = typeDecoded
    }
}

extension PlatformDevice: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PlatformDevice(id: \(String(describing: id)), type: \(String(describing: type)))"}
}

/// <p>The devices that are available on the container instance. The only supported device
/// 			type is a GPU.</p>
public struct PlatformDevice: Equatable {
    /// <p>The ID for the GPU(s) on the container instance. The available GPU IDs can also be
    /// 			obtained on the container instance in the
    /// 				<code>/var/lib/ecs/gpu/nvidia_gpu_info.json</code> file.</p>
    public let id: String?
    /// <p>The type of device that is available on the container instance. The only supported
    /// 			value is <code>GPU</code>.</p>
    public let type: PlatformDeviceType?

    public init (
        id: String? = nil,
        type: PlatformDeviceType? = nil
    )
    {
        self.id = id
        self.type = type
    }
}

public enum PlatformDeviceType {
    case gpu
    case sdkUnknown(String)
}

extension PlatformDeviceType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [PlatformDeviceType] {
        return [
            .gpu,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .gpu: return "GPU"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = PlatformDeviceType(rawValue: rawValue) ?? PlatformDeviceType.sdkUnknown(rawValue)
    }
}

extension PlatformTaskDefinitionIncompatibilityException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PlatformTaskDefinitionIncompatibilityException(message: \(String(describing: message)))"}
}

extension PlatformTaskDefinitionIncompatibilityException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: PlatformTaskDefinitionIncompatibilityExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified platform version does not satisfy the task definition's required
/// 			capabilities.</p>
public struct PlatformTaskDefinitionIncompatibilityException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct PlatformTaskDefinitionIncompatibilityExceptionBody: Equatable {
    public let message: String?
}

extension PlatformTaskDefinitionIncompatibilityExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension PlatformUnknownException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PlatformUnknownException(message: \(String(describing: message)))"}
}

extension PlatformUnknownException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: PlatformUnknownExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified platform version does not exist.</p>
public struct PlatformUnknownException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct PlatformUnknownExceptionBody: Equatable {
    public let message: String?
}

extension PlatformUnknownExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension PortMapping: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerPort
        case hostPort
        case `protocol` = "protocol"
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerPort = containerPort {
            try encodeContainer.encode(containerPort, forKey: .containerPort)
        }
        if let hostPort = hostPort {
            try encodeContainer.encode(hostPort, forKey: .hostPort)
        }
        if let `protocol` = `protocol` {
            try encodeContainer.encode(`protocol`.rawValue, forKey: .`protocol`)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .containerPort)
        containerPort = containerPortDecoded
        let hostPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .hostPort)
        hostPort = hostPortDecoded
        let protocolDecoded = try containerValues.decodeIfPresent(TransportProtocol.self, forKey: .protocol)
        `protocol` = protocolDecoded
    }
}

extension PortMapping: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PortMapping(containerPort: \(String(describing: containerPort)), hostPort: \(String(describing: hostPort)), protocol: \(String(describing: `protocol`)))"}
}

/// <p>Port mappings allow containers to access ports on the host container instance to send
/// 			or receive traffic. Port mappings are specified as part of the container
/// 			definition.</p>
/// 		       <p>If you are using containers in a task with the <code>awsvpc</code> or
/// 				<code>host</code> network mode, exposed ports should be specified using
/// 				<code>containerPort</code>. The <code>hostPort</code> can be left blank or it must
/// 			be the same value as the <code>containerPort</code>.</p>
/// 		       <note>
/// 			         <p>You cannot expose the same container port for multiple protocols. An error will be
/// 				returned if this is attempted</p>
/// 		       </note>
/// 		       <p>After a task reaches the <code>RUNNING</code> status, manual and automatic host and
/// 			container port assignments are visible in the <code>networkBindings</code> section of
/// 				<a>DescribeTasks</a> API responses.</p>
public struct PortMapping: Equatable {
    /// <p>The protocol used for the port mapping. Valid values are <code>tcp</code> and
    /// 				<code>udp</code>. The default is <code>tcp</code>.</p>
    public let `protocol`: TransportProtocol?
    /// <p>The port number on the container that is bound to the user-specified or automatically
    /// 			assigned host port.</p>
    /// 		       <p>If you are using containers in a task with the <code>awsvpc</code> or
    /// 				<code>host</code> network mode, exposed ports should be specified using
    /// 				<code>containerPort</code>.</p>
    /// 		       <p>If you are using containers in a task with the <code>bridge</code> network mode and
    /// 			you specify a container port and not a host port, your container automatically receives
    /// 			a host port in the ephemeral port range. For more information, see
    /// 			<code>hostPort</code>. Port mappings that are automatically assigned in this way do not
    /// 			count toward the 100 reserved ports limit of a container instance.</p>
    public let containerPort: Int?
    /// <p>The port number on the container instance to reserve for your container.</p>
    /// 		       <p>If you are using containers in a task with the <code>awsvpc</code> or
    /// 				<code>host</code> network mode, the <code>hostPort</code> can either be left blank
    /// 			or set to the same value as the <code>containerPort</code>.</p>
    /// 		       <p>If you are using containers in a task with the <code>bridge</code> network mode, you
    /// 			can specify a non-reserved host port for your container port mapping, or you can omit
    /// 			the <code>hostPort</code> (or set it to <code>0</code>) while specifying a
    /// 				<code>containerPort</code> and your container automatically receives a port in the
    /// 			ephemeral port range for your container instance operating system and Docker
    /// 			version.</p>
    /// 		       <p>The default ephemeral port range for Docker version 1.6.0 and later is listed on the
    /// 			instance under <code>/proc/sys/net/ipv4/ip_local_port_range</code>. If this kernel
    /// 			parameter is unavailable, the default ephemeral port range from 49153 through 65535 is
    /// 			used. Do not attempt to specify a host port in the ephemeral port range as these are
    /// 			reserved for automatic assignment. In general, ports below 32768 are outside of the
    /// 			ephemeral port range.</p>
    /// 		       <note>
    /// 			         <p>The default ephemeral port range from 49153 through 65535 is always used for
    /// 				Docker versions before 1.6.0.</p>
    /// 		       </note>
    /// 		       <p>The default reserved ports are 22 for SSH, the Docker ports 2375 and 2376, and the
    /// 			Amazon ECS container agent ports 51678-51680. Any host port that was previously specified in
    /// 			a running task is also reserved while the task is running (after a task stops, the host
    /// 			port is released). The current reserved ports are displayed in the
    /// 				<code>remainingResources</code> of <a>DescribeContainerInstances</a>
    /// 			output. A container instance can have up to 100 reserved ports at a time, including the
    /// 			default reserved ports. Automatically assigned ports don't count toward the 100 reserved
    /// 			ports limit.</p>
    public let hostPort: Int?

    public init (
        `protocol`: TransportProtocol? = nil,
        containerPort: Int? = nil,
        hostPort: Int? = nil
    )
    {
        self.`protocol` = `protocol`
        self.containerPort = containerPort
        self.hostPort = hostPort
    }
}

public enum PropagateTags {
    case service
    case taskDefinition
    case sdkUnknown(String)
}

extension PropagateTags : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [PropagateTags] {
        return [
            .service,
            .taskDefinition,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .service: return "SERVICE"
        case .taskDefinition: return "TASK_DEFINITION"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = PropagateTags(rawValue: rawValue) ?? PropagateTags.sdkUnknown(rawValue)
    }
}

extension ProxyConfiguration: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerName
        case properties
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
        if let properties = properties {
            var propertiesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .properties)
            for proxyconfigurationproperties0 in properties {
                try propertiesContainer.encode(proxyconfigurationproperties0)
            }
        }
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let typeDecoded = try containerValues.decodeIfPresent(ProxyConfigurationType.self, forKey: .type)
        type = typeDecoded
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let propertiesContainer = try containerValues.decodeIfPresent([KeyValuePair?].self, forKey: .properties)
        var propertiesDecoded0:[KeyValuePair]? = nil
        if let propertiesContainer = propertiesContainer {
            propertiesDecoded0 = [KeyValuePair]()
            for structure0 in propertiesContainer {
                if let structure0 = structure0 {
                    propertiesDecoded0?.append(structure0)
                }
            }
        }
        properties = propertiesDecoded0
    }
}

extension ProxyConfiguration: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ProxyConfiguration(containerName: \(String(describing: containerName)), properties: \(String(describing: properties)), type: \(String(describing: type)))"}
}

/// <p>The configuration details for the App Mesh proxy.</p>
/// 		       <p>For tasks using the EC2 launch type, the container instances require at
/// 			least version 1.26.0 of the container agent and at least version 1.26.0-1 of the
/// 				<code>ecs-init</code> package to enable a proxy configuration. If your container
/// 			instances are launched from the Amazon ECS-optimized AMI version <code>20190301</code> or
/// 			later, then they contain the required versions of the container agent and
/// 				<code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-optimized_AMI.html">Amazon ECS-optimized Linux AMI</a>
/// 		       </p>
public struct ProxyConfiguration: Equatable {
    /// <p>The name of the container that will serve as the App Mesh proxy.</p>
    public let containerName: String?
    /// <p>The set of network configuration parameters to provide the Container Network Interface
    /// 			(CNI) plugin, specified as key-value pairs.</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    ///                   <code>IgnoredUID</code> - (Required) The user ID (UID) of the proxy
    /// 					container as defined by the <code>user</code> parameter in a container
    /// 					definition. This is used to ensure the proxy ignores its own traffic. If
    /// 						<code>IgnoredGID</code> is specified, this field can be empty.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>IgnoredGID</code> - (Required) The group ID (GID) of the proxy
    /// 					container as defined by the <code>user</code> parameter in a container
    /// 					definition. This is used to ensure the proxy ignores its own traffic. If
    /// 						<code>IgnoredUID</code> is specified, this field can be empty.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>AppPorts</code> - (Required) The list of ports that the
    /// 					application uses. Network traffic to these ports is forwarded to the
    /// 						<code>ProxyIngressPort</code> and <code>ProxyEgressPort</code>.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>ProxyIngressPort</code> - (Required) Specifies the port that
    /// 					incoming traffic to the <code>AppPorts</code> is directed to.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>ProxyEgressPort</code> - (Required) Specifies the port that
    /// 					outgoing traffic from the <code>AppPorts</code> is directed to.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>EgressIgnoredPorts</code> - (Required) The egress traffic going to
    /// 					the specified ports is ignored and not redirected to the
    /// 						<code>ProxyEgressPort</code>. It can be an empty list.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>EgressIgnoredIPs</code> - (Required) The egress traffic going to
    /// 					the specified IP addresses is ignored and not redirected to the
    /// 						<code>ProxyEgressPort</code>. It can be an empty list.</p>
    /// 			         </li>
    ///          </ul>
    public let properties: [KeyValuePair]?
    /// <p>The proxy type. The only supported value is <code>APPMESH</code>.</p>
    public let type: ProxyConfigurationType?

    public init (
        containerName: String? = nil,
        properties: [KeyValuePair]? = nil,
        type: ProxyConfigurationType? = nil
    )
    {
        self.containerName = containerName
        self.properties = properties
        self.type = type
    }
}

public enum ProxyConfigurationType {
    case appmesh
    case sdkUnknown(String)
}

extension ProxyConfigurationType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ProxyConfigurationType] {
        return [
            .appmesh,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .appmesh: return "APPMESH"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ProxyConfigurationType(rawValue: rawValue) ?? ProxyConfigurationType.sdkUnknown(rawValue)
    }
}

public struct PutAccountSettingDefaultInputBodyMiddleware: Middleware {
    public let id: String = "PutAccountSettingDefaultInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAccountSettingDefaultInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAccountSettingDefaultOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAccountSettingDefaultInput>
    public typealias MOutput = OperationOutput<PutAccountSettingDefaultOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAccountSettingDefaultOutputError>
}

extension PutAccountSettingDefaultInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutAccountSettingDefaultInput(name: \(String(describing: name)), value: \(String(describing: value)))"}
}

extension PutAccountSettingDefaultInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }
}

public struct PutAccountSettingDefaultInputHeadersMiddleware: Middleware {
    public let id: String = "PutAccountSettingDefaultInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAccountSettingDefaultInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAccountSettingDefaultOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAccountSettingDefaultInput>
    public typealias MOutput = OperationOutput<PutAccountSettingDefaultOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAccountSettingDefaultOutputError>
}

public struct PutAccountSettingDefaultInputQueryItemMiddleware: Middleware {
    public let id: String = "PutAccountSettingDefaultInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAccountSettingDefaultInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAccountSettingDefaultOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAccountSettingDefaultInput>
    public typealias MOutput = OperationOutput<PutAccountSettingDefaultOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAccountSettingDefaultOutputError>
}

public struct PutAccountSettingDefaultInput: Equatable {
    /// <p>The resource name for which to modify the account setting. If
    /// 				<code>serviceLongArnFormat</code> is specified, the ARN for your Amazon ECS services is
    /// 			affected. If <code>taskLongArnFormat</code> is specified, the ARN and resource ID for
    /// 			your Amazon ECS tasks is affected. If <code>containerInstanceLongArnFormat</code> is
    /// 			specified, the ARN and resource ID for your Amazon ECS container instances is affected. If
    /// 				<code>awsvpcTrunking</code> is specified, the ENI limit for your Amazon ECS container
    /// 			instances is affected. If <code>containerInsights</code> is specified, the default
    /// 			setting for CloudWatch Container Insights for your clusters is affected.</p>
    public let name: SettingName?
    /// <p>The account setting value for the specified principal ARN. Accepted values are
    /// 				<code>enabled</code> and <code>disabled</code>.</p>
    public let value: String?

    public init (
        name: SettingName? = nil,
        value: String? = nil
    )
    {
        self.name = name
        self.value = value
    }
}

struct PutAccountSettingDefaultInputBody: Equatable {
    public let name: SettingName?
    public let value: String?
}

extension PutAccountSettingDefaultInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case name
        case value
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(SettingName.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
    }
}

extension PutAccountSettingDefaultOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension PutAccountSettingDefaultOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum PutAccountSettingDefaultOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension PutAccountSettingDefaultOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutAccountSettingDefaultOutputResponse(setting: \(String(describing: setting)))"}
}

extension PutAccountSettingDefaultOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: PutAccountSettingDefaultOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.setting = output.setting
        } else {
            self.setting = nil
        }
    }
}

public struct PutAccountSettingDefaultOutputResponse: Equatable {
    /// <p>The current setting for a resource.</p>
    public let setting: Setting?

    public init (
        setting: Setting? = nil
    )
    {
        self.setting = setting
    }
}

struct PutAccountSettingDefaultOutputResponseBody: Equatable {
    public let setting: Setting?
}

extension PutAccountSettingDefaultOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case setting
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let settingDecoded = try containerValues.decodeIfPresent(Setting.self, forKey: .setting)
        setting = settingDecoded
    }
}

public struct PutAccountSettingInputBodyMiddleware: Middleware {
    public let id: String = "PutAccountSettingInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAccountSettingInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAccountSettingOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAccountSettingInput>
    public typealias MOutput = OperationOutput<PutAccountSettingOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAccountSettingOutputError>
}

extension PutAccountSettingInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutAccountSettingInput(name: \(String(describing: name)), principalArn: \(String(describing: principalArn)), value: \(String(describing: value)))"}
}

extension PutAccountSettingInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case principalArn
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let principalArn = principalArn {
            try encodeContainer.encode(principalArn, forKey: .principalArn)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }
}

public struct PutAccountSettingInputHeadersMiddleware: Middleware {
    public let id: String = "PutAccountSettingInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAccountSettingInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAccountSettingOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAccountSettingInput>
    public typealias MOutput = OperationOutput<PutAccountSettingOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAccountSettingOutputError>
}

public struct PutAccountSettingInputQueryItemMiddleware: Middleware {
    public let id: String = "PutAccountSettingInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAccountSettingInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAccountSettingOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAccountSettingInput>
    public typealias MOutput = OperationOutput<PutAccountSettingOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAccountSettingOutputError>
}

public struct PutAccountSettingInput: Equatable {
    /// <p>The Amazon ECS resource name for which to modify the account setting. If
    /// 				<code>serviceLongArnFormat</code> is specified, the ARN for your Amazon ECS services is
    /// 			affected. If <code>taskLongArnFormat</code> is specified, the ARN and resource ID for
    /// 			your Amazon ECS tasks is affected. If <code>containerInstanceLongArnFormat</code> is
    /// 			specified, the ARN and resource ID for your Amazon ECS container instances is affected. If
    /// 				<code>awsvpcTrunking</code> is specified, the elastic network interface (ENI) limit
    /// 			for your Amazon ECS container instances is affected. If <code>containerInsights</code> is
    /// 			specified, the default setting for CloudWatch Container Insights for your clusters is
    /// 			affected.</p>
    public let name: SettingName?
    /// <p>The ARN of the principal, which can be an IAM user, IAM role, or the root user. If you
    /// 			specify the root user, it modifies the account setting for all IAM users, IAM roles, and
    /// 			the root user of the account unless an IAM user or role explicitly overrides these
    /// 			settings. If this field is omitted, the setting is changed only for the authenticated
    /// 			user.</p>
    /// 		       <note>
    /// 			         <p>Federated users assume the account setting of the root user and can't have
    /// 				explicit account settings set for them.</p>
    /// 		       </note>
    public let principalArn: String?
    /// <p>The account setting value for the specified principal ARN. Accepted values are
    /// 				<code>enabled</code> and <code>disabled</code>.</p>
    public let value: String?

    public init (
        name: SettingName? = nil,
        principalArn: String? = nil,
        value: String? = nil
    )
    {
        self.name = name
        self.principalArn = principalArn
        self.value = value
    }
}

struct PutAccountSettingInputBody: Equatable {
    public let name: SettingName?
    public let value: String?
    public let principalArn: String?
}

extension PutAccountSettingInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case name
        case principalArn
        case value
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(SettingName.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let principalArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .principalArn)
        principalArn = principalArnDecoded
    }
}

extension PutAccountSettingOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension PutAccountSettingOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum PutAccountSettingOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension PutAccountSettingOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutAccountSettingOutputResponse(setting: \(String(describing: setting)))"}
}

extension PutAccountSettingOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: PutAccountSettingOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.setting = output.setting
        } else {
            self.setting = nil
        }
    }
}

public struct PutAccountSettingOutputResponse: Equatable {
    /// <p>The current account setting for a resource.</p>
    public let setting: Setting?

    public init (
        setting: Setting? = nil
    )
    {
        self.setting = setting
    }
}

struct PutAccountSettingOutputResponseBody: Equatable {
    public let setting: Setting?
}

extension PutAccountSettingOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case setting
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let settingDecoded = try containerValues.decodeIfPresent(Setting.self, forKey: .setting)
        setting = settingDecoded
    }
}

public struct PutAttributesInputBodyMiddleware: Middleware {
    public let id: String = "PutAttributesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAttributesInput>
    public typealias MOutput = OperationOutput<PutAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAttributesOutputError>
}

extension PutAttributesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutAttributesInput(attributes: \(String(describing: attributes)), cluster: \(String(describing: cluster)))"}
}

extension PutAttributesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attributes
        case cluster
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attributes = attributes {
            var attributesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attributes)
            for attributes0 in attributes {
                try attributesContainer.encode(attributes0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
    }
}

public struct PutAttributesInputHeadersMiddleware: Middleware {
    public let id: String = "PutAttributesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAttributesInput>
    public typealias MOutput = OperationOutput<PutAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAttributesOutputError>
}

public struct PutAttributesInputQueryItemMiddleware: Middleware {
    public let id: String = "PutAttributesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutAttributesInput>,
                  next: H) -> Swift.Result<OperationOutput<PutAttributesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutAttributesInput>
    public typealias MOutput = OperationOutput<PutAttributesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutAttributesOutputError>
}

public struct PutAttributesInput: Equatable {
    /// <p>The attributes to apply to your resource. You can specify up to 10 custom attributes
    /// 			per resource. You can specify up to 10 attributes in a single call.</p>
    public let attributes: [Attribute]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that contains the resource to apply
    /// 			attributes. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?

    public init (
        attributes: [Attribute]? = nil,
        cluster: String? = nil
    )
    {
        self.attributes = attributes
        self.cluster = cluster
    }
}

struct PutAttributesInputBody: Equatable {
    public let cluster: String?
    public let attributes: [Attribute]?
}

extension PutAttributesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributes
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
    }
}

extension PutAttributesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension PutAttributesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AttributeLimitExceededException" : self = .attributeLimitExceededException(try AttributeLimitExceededException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "TargetNotFoundException" : self = .targetNotFoundException(try TargetNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum PutAttributesOutputError: Swift.Error, Equatable {
    case attributeLimitExceededException(AttributeLimitExceededException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case targetNotFoundException(TargetNotFoundException)
    case unknown(UnknownAWSHttpServiceError)
}

extension PutAttributesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutAttributesOutputResponse(attributes: \(String(describing: attributes)))"}
}

extension PutAttributesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: PutAttributesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.attributes = output.attributes
        } else {
            self.attributes = nil
        }
    }
}

public struct PutAttributesOutputResponse: Equatable {
    /// <p>The attributes applied to your resource.</p>
    public let attributes: [Attribute]?

    public init (
        attributes: [Attribute]? = nil
    )
    {
        self.attributes = attributes
    }
}

struct PutAttributesOutputResponseBody: Equatable {
    public let attributes: [Attribute]?
}

extension PutAttributesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributes
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
    }
}

public struct PutClusterCapacityProvidersInputBodyMiddleware: Middleware {
    public let id: String = "PutClusterCapacityProvidersInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutClusterCapacityProvidersInput>,
                  next: H) -> Swift.Result<OperationOutput<PutClusterCapacityProvidersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutClusterCapacityProvidersInput>
    public typealias MOutput = OperationOutput<PutClusterCapacityProvidersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutClusterCapacityProvidersOutputError>
}

extension PutClusterCapacityProvidersInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutClusterCapacityProvidersInput(capacityProviders: \(String(describing: capacityProviders)), cluster: \(String(describing: cluster)), defaultCapacityProviderStrategy: \(String(describing: defaultCapacityProviderStrategy)))"}
}

extension PutClusterCapacityProvidersInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case cluster
        case defaultCapacityProviderStrategy
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviders = capacityProviders {
            var capacityProvidersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviders)
            for stringlist0 in capacityProviders {
                try capacityProvidersContainer.encode(stringlist0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let defaultCapacityProviderStrategy = defaultCapacityProviderStrategy {
            var defaultCapacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .defaultCapacityProviderStrategy)
            for capacityproviderstrategy0 in defaultCapacityProviderStrategy {
                try defaultCapacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
    }
}

public struct PutClusterCapacityProvidersInputHeadersMiddleware: Middleware {
    public let id: String = "PutClusterCapacityProvidersInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutClusterCapacityProvidersInput>,
                  next: H) -> Swift.Result<OperationOutput<PutClusterCapacityProvidersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutClusterCapacityProvidersInput>
    public typealias MOutput = OperationOutput<PutClusterCapacityProvidersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutClusterCapacityProvidersOutputError>
}

public struct PutClusterCapacityProvidersInputQueryItemMiddleware: Middleware {
    public let id: String = "PutClusterCapacityProvidersInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<PutClusterCapacityProvidersInput>,
                  next: H) -> Swift.Result<OperationOutput<PutClusterCapacityProvidersOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<PutClusterCapacityProvidersInput>
    public typealias MOutput = OperationOutput<PutClusterCapacityProvidersOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<PutClusterCapacityProvidersOutputError>
}

public struct PutClusterCapacityProvidersInput: Equatable {
    /// <p>The name of one or more capacity providers to associate with the cluster.</p>
    /// 		       <p>If specifying a capacity provider that uses an Auto Scaling group, the capacity
    /// 			provider must already be created. New capacity providers can be created with the <a>CreateCapacityProvider</a> API operation.</p>
    /// 		       <p>To use a AWS Fargate capacity provider, specify either the <code>FARGATE</code> or
    /// 				<code>FARGATE_SPOT</code> capacity providers. The AWS Fargate capacity providers are
    /// 			available to all accounts and only need to be associated with a cluster to be
    /// 			used.</p>
    public let capacityProviders: [String]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster to modify the capacity provider
    /// 			settings for. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The capacity provider strategy to use by default for the cluster.</p>
    /// 		       <p>When creating a service or running a task on a cluster, if no capacity provider or
    /// 			launch type is specified then the default capacity provider strategy for the cluster is
    /// 			used.</p>
    /// 		       <p>A capacity provider strategy consists of one or more capacity providers along with the
    /// 				<code>base</code> and <code>weight</code> to assign to them. A capacity provider
    /// 			must be associated with the cluster to be used in a capacity provider strategy. The
    /// 				<a>PutClusterCapacityProviders</a> API is used to associate a capacity
    /// 			provider with a cluster. Only capacity providers with an <code>ACTIVE</code> or
    /// 				<code>UPDATING</code> status can be used.</p>
    /// 		       <p>If specifying a capacity provider that uses an Auto Scaling group, the capacity
    /// 			provider must already be created. New capacity providers can be created with the <a>CreateCapacityProvider</a> API operation.</p>
    /// 		       <p>To use a AWS Fargate capacity provider, specify either the <code>FARGATE</code> or
    /// 				<code>FARGATE_SPOT</code> capacity providers. The AWS Fargate capacity providers are
    /// 			available to all accounts and only need to be associated with a cluster to be
    /// 			used.</p>
    public let defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]?

    public init (
        capacityProviders: [String]? = nil,
        cluster: String? = nil,
        defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]? = nil
    )
    {
        self.capacityProviders = capacityProviders
        self.cluster = cluster
        self.defaultCapacityProviderStrategy = defaultCapacityProviderStrategy
    }
}

struct PutClusterCapacityProvidersInputBody: Equatable {
    public let cluster: String?
    public let capacityProviders: [String]?
    public let defaultCapacityProviderStrategy: [CapacityProviderStrategyItem]?
}

extension PutClusterCapacityProvidersInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviders
        case cluster
        case defaultCapacityProviderStrategy
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let capacityProvidersContainer = try containerValues.decodeIfPresent([String?].self, forKey: .capacityProviders)
        var capacityProvidersDecoded0:[String]? = nil
        if let capacityProvidersContainer = capacityProvidersContainer {
            capacityProvidersDecoded0 = [String]()
            for string0 in capacityProvidersContainer {
                if let string0 = string0 {
                    capacityProvidersDecoded0?.append(string0)
                }
            }
        }
        capacityProviders = capacityProvidersDecoded0
        let defaultCapacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .defaultCapacityProviderStrategy)
        var defaultCapacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let defaultCapacityProviderStrategyContainer = defaultCapacityProviderStrategyContainer {
            defaultCapacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in defaultCapacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    defaultCapacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        defaultCapacityProviderStrategy = defaultCapacityProviderStrategyDecoded0
    }
}

extension PutClusterCapacityProvidersOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension PutClusterCapacityProvidersOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ResourceInUseException" : self = .resourceInUseException(try ResourceInUseException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UpdateInProgressException" : self = .updateInProgressException(try UpdateInProgressException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum PutClusterCapacityProvidersOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case resourceInUseException(ResourceInUseException)
    case serverException(ServerException)
    case updateInProgressException(UpdateInProgressException)
    case unknown(UnknownAWSHttpServiceError)
}

extension PutClusterCapacityProvidersOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "PutClusterCapacityProvidersOutputResponse(cluster: \(String(describing: cluster)))"}
}

extension PutClusterCapacityProvidersOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: PutClusterCapacityProvidersOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.cluster = output.cluster
        } else {
            self.cluster = nil
        }
    }
}

public struct PutClusterCapacityProvidersOutputResponse: Equatable {
    /// <p>Details about the cluster.</p>
    public let cluster: Cluster?

    public init (
        cluster: Cluster? = nil
    )
    {
        self.cluster = cluster
    }
}

struct PutClusterCapacityProvidersOutputResponseBody: Equatable {
    public let cluster: Cluster?
}

extension PutClusterCapacityProvidersOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(Cluster.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

public struct RegisterContainerInstanceInputBodyMiddleware: Middleware {
    public let id: String = "RegisterContainerInstanceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RegisterContainerInstanceInput>,
                  next: H) -> Swift.Result<OperationOutput<RegisterContainerInstanceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RegisterContainerInstanceInput>
    public typealias MOutput = OperationOutput<RegisterContainerInstanceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RegisterContainerInstanceOutputError>
}

extension RegisterContainerInstanceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RegisterContainerInstanceInput(attributes: \(String(describing: attributes)), cluster: \(String(describing: cluster)), containerInstanceArn: \(String(describing: containerInstanceArn)), instanceIdentityDocument: \(String(describing: instanceIdentityDocument)), instanceIdentityDocumentSignature: \(String(describing: instanceIdentityDocumentSignature)), platformDevices: \(String(describing: platformDevices)), tags: \(String(describing: tags)), totalResources: \(String(describing: totalResources)), versionInfo: \(String(describing: versionInfo)))"}
}

extension RegisterContainerInstanceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attributes
        case cluster
        case containerInstanceArn
        case instanceIdentityDocument
        case instanceIdentityDocumentSignature
        case platformDevices
        case tags
        case totalResources
        case versionInfo
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attributes = attributes {
            var attributesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attributes)
            for attributes0 in attributes {
                try attributesContainer.encode(attributes0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstanceArn = containerInstanceArn {
            try encodeContainer.encode(containerInstanceArn, forKey: .containerInstanceArn)
        }
        if let instanceIdentityDocument = instanceIdentityDocument {
            try encodeContainer.encode(instanceIdentityDocument, forKey: .instanceIdentityDocument)
        }
        if let instanceIdentityDocumentSignature = instanceIdentityDocumentSignature {
            try encodeContainer.encode(instanceIdentityDocumentSignature, forKey: .instanceIdentityDocumentSignature)
        }
        if let platformDevices = platformDevices {
            var platformDevicesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .platformDevices)
            for platformdevices0 in platformDevices {
                try platformDevicesContainer.encode(platformdevices0)
            }
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let totalResources = totalResources {
            var totalResourcesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .totalResources)
            for resources0 in totalResources {
                try totalResourcesContainer.encode(resources0)
            }
        }
        if let versionInfo = versionInfo {
            try encodeContainer.encode(versionInfo, forKey: .versionInfo)
        }
    }
}

public struct RegisterContainerInstanceInputHeadersMiddleware: Middleware {
    public let id: String = "RegisterContainerInstanceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RegisterContainerInstanceInput>,
                  next: H) -> Swift.Result<OperationOutput<RegisterContainerInstanceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RegisterContainerInstanceInput>
    public typealias MOutput = OperationOutput<RegisterContainerInstanceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RegisterContainerInstanceOutputError>
}

public struct RegisterContainerInstanceInputQueryItemMiddleware: Middleware {
    public let id: String = "RegisterContainerInstanceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RegisterContainerInstanceInput>,
                  next: H) -> Swift.Result<OperationOutput<RegisterContainerInstanceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RegisterContainerInstanceInput>
    public typealias MOutput = OperationOutput<RegisterContainerInstanceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RegisterContainerInstanceOutputError>
}

public struct RegisterContainerInstanceInput: Equatable {
    /// <p>The container instance attributes that this container instance supports.</p>
    public let attributes: [Attribute]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster with which to register your container
    /// 			instance. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The ARN of the container instance (if it was previously registered).</p>
    public let containerInstanceArn: String?
    /// <p>The instance identity document for the EC2 instance to register. This document can be
    /// 			found by running the following command from the instance: <code>curl
    /// 				http://169.254.169.254/latest/dynamic/instance-identity/document/</code>
    /// 		       </p>
    public let instanceIdentityDocument: String?
    /// <p>The instance identity document signature for the EC2 instance to register. This
    /// 			signature can be found by running the following command from the instance: <code>curl
    /// 				http://169.254.169.254/latest/dynamic/instance-identity/signature/</code>
    /// 		       </p>
    public let instanceIdentityDocumentSignature: String?
    /// <p>The devices that are available on the container instance. The only supported device
    /// 			type is a GPU.</p>
    public let platformDevices: [PlatformDevice]?
    /// <p>The metadata that you apply to the container instance to help you categorize and
    /// 			organize them. Each tag consists of a key and an optional value, both of which you
    /// 			define.</p>
    ///          <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The resources available on the instance.</p>
    public let totalResources: [Resource]?
    /// <p>The version information for the Amazon ECS container agent and Docker daemon running on the
    /// 			container instance.</p>
    public let versionInfo: VersionInfo?

    public init (
        attributes: [Attribute]? = nil,
        cluster: String? = nil,
        containerInstanceArn: String? = nil,
        instanceIdentityDocument: String? = nil,
        instanceIdentityDocumentSignature: String? = nil,
        platformDevices: [PlatformDevice]? = nil,
        tags: [Tag]? = nil,
        totalResources: [Resource]? = nil,
        versionInfo: VersionInfo? = nil
    )
    {
        self.attributes = attributes
        self.cluster = cluster
        self.containerInstanceArn = containerInstanceArn
        self.instanceIdentityDocument = instanceIdentityDocument
        self.instanceIdentityDocumentSignature = instanceIdentityDocumentSignature
        self.platformDevices = platformDevices
        self.tags = tags
        self.totalResources = totalResources
        self.versionInfo = versionInfo
    }
}

struct RegisterContainerInstanceInputBody: Equatable {
    public let cluster: String?
    public let instanceIdentityDocument: String?
    public let instanceIdentityDocumentSignature: String?
    public let totalResources: [Resource]?
    public let versionInfo: VersionInfo?
    public let containerInstanceArn: String?
    public let attributes: [Attribute]?
    public let platformDevices: [PlatformDevice]?
    public let tags: [Tag]?
}

extension RegisterContainerInstanceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attributes
        case cluster
        case containerInstanceArn
        case instanceIdentityDocument
        case instanceIdentityDocumentSignature
        case platformDevices
        case tags
        case totalResources
        case versionInfo
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let instanceIdentityDocumentDecoded = try containerValues.decodeIfPresent(String.self, forKey: .instanceIdentityDocument)
        instanceIdentityDocument = instanceIdentityDocumentDecoded
        let instanceIdentityDocumentSignatureDecoded = try containerValues.decodeIfPresent(String.self, forKey: .instanceIdentityDocumentSignature)
        instanceIdentityDocumentSignature = instanceIdentityDocumentSignatureDecoded
        let totalResourcesContainer = try containerValues.decodeIfPresent([Resource?].self, forKey: .totalResources)
        var totalResourcesDecoded0:[Resource]? = nil
        if let totalResourcesContainer = totalResourcesContainer {
            totalResourcesDecoded0 = [Resource]()
            for structure0 in totalResourcesContainer {
                if let structure0 = structure0 {
                    totalResourcesDecoded0?.append(structure0)
                }
            }
        }
        totalResources = totalResourcesDecoded0
        let versionInfoDecoded = try containerValues.decodeIfPresent(VersionInfo.self, forKey: .versionInfo)
        versionInfo = versionInfoDecoded
        let containerInstanceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstanceArn)
        containerInstanceArn = containerInstanceArnDecoded
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
        let platformDevicesContainer = try containerValues.decodeIfPresent([PlatformDevice?].self, forKey: .platformDevices)
        var platformDevicesDecoded0:[PlatformDevice]? = nil
        if let platformDevicesContainer = platformDevicesContainer {
            platformDevicesDecoded0 = [PlatformDevice]()
            for structure0 in platformDevicesContainer {
                if let structure0 = structure0 {
                    platformDevicesDecoded0?.append(structure0)
                }
            }
        }
        platformDevices = platformDevicesDecoded0
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension RegisterContainerInstanceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension RegisterContainerInstanceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum RegisterContainerInstanceOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension RegisterContainerInstanceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RegisterContainerInstanceOutputResponse(containerInstance: \(String(describing: containerInstance)))"}
}

extension RegisterContainerInstanceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: RegisterContainerInstanceOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.containerInstance = output.containerInstance
        } else {
            self.containerInstance = nil
        }
    }
}

public struct RegisterContainerInstanceOutputResponse: Equatable {
    /// <p>The container instance that was registered.</p>
    public let containerInstance: ContainerInstance?

    public init (
        containerInstance: ContainerInstance? = nil
    )
    {
        self.containerInstance = containerInstance
    }
}

struct RegisterContainerInstanceOutputResponseBody: Equatable {
    public let containerInstance: ContainerInstance?
}

extension RegisterContainerInstanceOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerInstance
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstanceDecoded = try containerValues.decodeIfPresent(ContainerInstance.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
    }
}

public struct RegisterTaskDefinitionInputBodyMiddleware: Middleware {
    public let id: String = "RegisterTaskDefinitionInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RegisterTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<RegisterTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RegisterTaskDefinitionInput>
    public typealias MOutput = OperationOutput<RegisterTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RegisterTaskDefinitionOutputError>
}

extension RegisterTaskDefinitionInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RegisterTaskDefinitionInput(containerDefinitions: \(String(describing: containerDefinitions)), cpu: \(String(describing: cpu)), ephemeralStorage: \(String(describing: ephemeralStorage)), executionRoleArn: \(String(describing: executionRoleArn)), family: \(String(describing: family)), inferenceAccelerators: \(String(describing: inferenceAccelerators)), ipcMode: \(String(describing: ipcMode)), memory: \(String(describing: memory)), networkMode: \(String(describing: networkMode)), pidMode: \(String(describing: pidMode)), placementConstraints: \(String(describing: placementConstraints)), proxyConfiguration: \(String(describing: proxyConfiguration)), requiresCompatibilities: \(String(describing: requiresCompatibilities)), tags: \(String(describing: tags)), taskRoleArn: \(String(describing: taskRoleArn)), volumes: \(String(describing: volumes)))"}
}

extension RegisterTaskDefinitionInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerDefinitions
        case cpu
        case ephemeralStorage
        case executionRoleArn
        case family
        case inferenceAccelerators
        case ipcMode
        case memory
        case networkMode
        case pidMode
        case placementConstraints
        case proxyConfiguration
        case requiresCompatibilities
        case tags
        case taskRoleArn
        case volumes
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerDefinitions = containerDefinitions {
            var containerDefinitionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containerDefinitions)
            for containerdefinitions0 in containerDefinitions {
                try containerDefinitionsContainer.encode(containerdefinitions0)
            }
        }
        if let cpu = cpu {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let ephemeralStorage = ephemeralStorage {
            try encodeContainer.encode(ephemeralStorage, forKey: .ephemeralStorage)
        }
        if let executionRoleArn = executionRoleArn {
            try encodeContainer.encode(executionRoleArn, forKey: .executionRoleArn)
        }
        if let family = family {
            try encodeContainer.encode(family, forKey: .family)
        }
        if let inferenceAccelerators = inferenceAccelerators {
            var inferenceAcceleratorsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .inferenceAccelerators)
            for inferenceaccelerators0 in inferenceAccelerators {
                try inferenceAcceleratorsContainer.encode(inferenceaccelerators0)
            }
        }
        if let ipcMode = ipcMode {
            try encodeContainer.encode(ipcMode.rawValue, forKey: .ipcMode)
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let networkMode = networkMode {
            try encodeContainer.encode(networkMode.rawValue, forKey: .networkMode)
        }
        if let pidMode = pidMode {
            try encodeContainer.encode(pidMode.rawValue, forKey: .pidMode)
        }
        if let placementConstraints = placementConstraints {
            var placementConstraintsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementConstraints)
            for taskdefinitionplacementconstraints0 in placementConstraints {
                try placementConstraintsContainer.encode(taskdefinitionplacementconstraints0)
            }
        }
        if let proxyConfiguration = proxyConfiguration {
            try encodeContainer.encode(proxyConfiguration, forKey: .proxyConfiguration)
        }
        if let requiresCompatibilities = requiresCompatibilities {
            var requiresCompatibilitiesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .requiresCompatibilities)
            for compatibilitylist0 in requiresCompatibilities {
                try requiresCompatibilitiesContainer.encode(compatibilitylist0.rawValue)
            }
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskRoleArn = taskRoleArn {
            try encodeContainer.encode(taskRoleArn, forKey: .taskRoleArn)
        }
        if let volumes = volumes {
            var volumesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .volumes)
            for volumelist0 in volumes {
                try volumesContainer.encode(volumelist0)
            }
        }
    }
}

public struct RegisterTaskDefinitionInputHeadersMiddleware: Middleware {
    public let id: String = "RegisterTaskDefinitionInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RegisterTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<RegisterTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RegisterTaskDefinitionInput>
    public typealias MOutput = OperationOutput<RegisterTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RegisterTaskDefinitionOutputError>
}

public struct RegisterTaskDefinitionInputQueryItemMiddleware: Middleware {
    public let id: String = "RegisterTaskDefinitionInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RegisterTaskDefinitionInput>,
                  next: H) -> Swift.Result<OperationOutput<RegisterTaskDefinitionOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RegisterTaskDefinitionInput>
    public typealias MOutput = OperationOutput<RegisterTaskDefinitionOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RegisterTaskDefinitionOutputError>
}

public struct RegisterTaskDefinitionInput: Equatable {
    /// <p>A list of container definitions in JSON format that describe the different containers
    /// 			that make up your task.</p>
    public let containerDefinitions: [ContainerDefinition]?
    /// <p>The number of CPU units used by the task. It can be expressed as an integer using CPU
    /// 			units, for example <code>1024</code>, or as a string using vCPUs, for example <code>1
    /// 				vCPU</code> or <code>1 vcpu</code>, in a task definition. String values are
    /// 			converted to an integer indicating the CPU units when the task definition is
    /// 			registered.</p>
    /// 		       <note>
    /// 			         <p>Task-level CPU and memory parameters are ignored for Windows containers. We
    /// 				recommend specifying container-level resources for Windows containers.</p>
    /// 		       </note>
    /// 		       <p>If you are using the EC2 launch type, this field is optional. Supported
    /// 			values are between <code>128</code> CPU units (<code>0.125</code> vCPUs) and
    /// 				<code>10240</code> CPU units (<code>10</code> vCPUs).</p>
    /// 		       <p>If you are using the Fargate launch type, this field is required and you
    /// 			must use one of the following values, which determines your range of supported values
    /// 			for the <code>memory</code> parameter:</p>
    ///          <ul>
    ///             <li>
    ///                 <p>256 (.25 vCPU) - Available <code>memory</code> values: 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>512 (.5 vCPU) - Available <code>memory</code> values: 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>1024 (1 vCPU) - Available <code>memory</code> values: 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>2048 (2 vCPU) - Available <code>memory</code> values: Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>4096 (4 vCPU) - Available <code>memory</code> values: Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB)</p>
    ///             </li>
    ///          </ul>
    public let cpu: String?
    /// <p>The amount of ephemeral storage to allocate for the task. This parameter is used to
    /// 			expand the total amount of ephemeral storage available, beyond the default amount, for
    /// 			tasks hosted on AWS Fargate. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/userguide/using_data_volumes.html">Fargate task
    /// 				storage</a> in the <i>Amazon ECS User Guide for AWS Fargate</i>.</p>
    /// 		       <note>
    /// 			         <p>This parameter is only supported for tasks hosted on AWS Fargate using platform
    /// 				version <code>1.4.0</code> or later.</p>
    /// 		       </note>
    public let ephemeralStorage: EphemeralStorage?
    /// <p>The Amazon Resource Name (ARN) of the task execution role that grants the Amazon ECS container agent
    ///             permission to make AWS API calls on your behalf. The task execution IAM role is required
    ///             depending on the requirements of your task. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_execution_IAM_role.html">Amazon ECS task
    ///                 execution IAM role</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let executionRoleArn: String?
    /// <p>You must specify a <code>family</code> for a task definition, which allows you to
    /// 			track multiple versions of the same task definition. The <code>family</code> is used as
    /// 			a name for your task definition. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed.</p>
    public let family: String?
    /// <p>The Elastic Inference accelerators to use for the containers in the task.</p>
    public let inferenceAccelerators: [InferenceAccelerator]?
    /// <p>The IPC resource namespace to use for the containers in the task. The valid values are
    ///                 <code>host</code>, <code>task</code>, or <code>none</code>. If <code>host</code> is
    ///             specified, then all containers within the tasks that specified the <code>host</code> IPC
    ///             mode on the same container instance share the same IPC resources with the host Amazon EC2
    ///             instance. If <code>task</code> is specified, all containers within the specified task
    ///             share the same IPC resources. If <code>none</code> is specified, then IPC resources
    ///             within the containers of a task are private and not shared with other containers in a
    ///             task or on the container instance. If no value is specified, then the IPC resource
    ///             namespace sharing depends on the Docker daemon setting on the container instance. For
    ///             more information, see <a href="https://docs.docker.com/engine/reference/run/#ipc-settings---ipc">IPC
    ///                 settings</a> in the <i>Docker run reference</i>.</p>
    ///         <p>If the <code>host</code> IPC mode is used, be aware that there is a heightened risk of
    ///             undesired IPC namespace expose. For more information, see <a href="https://docs.docker.com/engine/security/security/">Docker
    ///             security</a>.</p>
    ///         <p>If you are setting namespaced kernel parameters using <code>systemControls</code> for
    ///             the containers in the task, the following will apply to your IPC resource namespace. For
    ///             more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html">System
    ///                 Controls</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    ///         <ul>
    ///             <li>
    ///                 <p>For tasks that use the <code>host</code> IPC mode, IPC namespace related
    ///                         <code>systemControls</code> are not supported.</p>
    ///             </li>
    ///             <li>
    ///                 <p>For tasks that use the <code>task</code> IPC mode, IPC namespace related
    ///                         <code>systemControls</code> will apply to all containers within a
    ///                     task.</p>
    ///             </li>
    ///          </ul>
    ///          <note>
    ///                                 <p>This parameter is not supported for Windows containers or tasks run on AWS Fargate.</p>
    ///                              </note>
    public let ipcMode: IpcMode?
    /// <p>The amount of memory (in MiB) used by the task. It can be expressed as an integer
    /// 			using MiB, for example <code>1024</code>, or as a string using GB, for example
    /// 				<code>1GB</code> or <code>1 GB</code>, in a task definition. String values are
    /// 			converted to an integer indicating the MiB when the task definition is
    /// 			registered.</p>
    /// 		       <note>
    /// 			         <p>Task-level CPU and memory parameters are ignored for Windows containers. We
    /// 				recommend specifying container-level resources for Windows containers.</p>
    /// 		       </note>
    /// 		       <p>If using the EC2 launch type, this field is optional.</p>
    /// 		       <p>If using the Fargate launch type, this field is required and you must
    /// 			use one of the following values, which determines your range of supported values for the
    /// 				<code>cpu</code> parameter:</p>
    ///          <ul>
    ///             <li>
    ///                 <p>512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) - Available <code>cpu</code> values: 256 (.25 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) - Available <code>cpu</code> values: 512 (.5 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) - Available <code>cpu</code> values: 1024 (1 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) - Available <code>cpu</code> values: 2048 (2 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB) - Available <code>cpu</code> values: 4096 (4 vCPU)</p>
    ///             </li>
    ///          </ul>
    public let memory: String?
    /// <p>The Docker networking mode to use for the containers in the task. The valid values are
    ///                 <code>none</code>, <code>bridge</code>, <code>awsvpc</code>, and <code>host</code>.
    ///             If no network mode is specified, the default is <code>bridge</code>.</p>
    ///             <p>For Amazon ECS tasks on Fargate, the <code>awsvpc</code> network mode is required.
    ///             For Amazon ECS tasks on Amazon EC2 instances, any network mode can be used. If the network
    ///             mode is set to <code>none</code>, you cannot specify port mappings in your container
    ///             definitions, and the tasks containers do not have external connectivity. The
    ///                 <code>host</code> and <code>awsvpc</code> network modes offer the highest networking
    ///             performance for containers because they use the EC2 network stack instead of the
    ///             virtualized network stack provided by the <code>bridge</code> mode.</p>
    ///         <p>With the <code>host</code> and <code>awsvpc</code> network modes, exposed container
    ///             ports are mapped directly to the corresponding host port (for the <code>host</code>
    ///             network mode) or the attached elastic network interface port (for the
    ///                 <code>awsvpc</code> network mode), so you cannot take advantage of dynamic host port
    ///             mappings. </p>
    ///             <important>
    ///                         <p>When using the <code>host</code> network mode, you should not run
    ///                             containers using the root user (UID 0). It is considered best practice
    ///                             to use a non-root user.</p>
    ///                     </important>
    ///         <p>If the network mode is <code>awsvpc</code>, the task is allocated an elastic network
    ///             interface, and you must specify a <a>NetworkConfiguration</a> value when you create
    ///             a service or run a task with the task definition. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-networking.html">Task Networking</a> in the
    ///                 <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    ///         <note>
    ///             <p>Currently, only Amazon ECS-optimized AMIs, other Amazon Linux variants with the
    ///                     <code>ecs-init</code> package, or AWS Fargate infrastructure support the
    ///                     <code>awsvpc</code> network mode. </p>
    ///         </note>
    ///         <p>If the network mode is <code>host</code>, you cannot run multiple instantiations of the
    ///             same task on a single container instance when port mappings are used.</p>
    ///         <p>Docker for Windows uses different network modes than Docker for Linux. When you
    ///             register a task definition with Windows containers, you must not specify a network mode.
    ///             If you use the console to register a task definition with Windows containers, you must
    ///             choose the <code><default></code> network mode object. </p>
    ///         <p>For more information, see <a href="https://docs.docker.com/engine/reference/run/#network-settings">Network
    ///                 settings</a> in the <i>Docker run reference</i>.</p>
    public let networkMode: NetworkMode?
    /// <p>The process namespace to use for the containers in the task. The valid
    ///                             values are <code>host</code> or <code>task</code>. If <code>host</code>
    ///                             is specified, then all containers within the tasks that specified the
    ///                                 <code>host</code> PID mode on the same container instance share the
    ///                             same process namespace with the host Amazon EC2 instance. If <code>task</code> is
    ///                             specified, all containers within the specified task share the same
    ///                             process namespace. If no value is specified, the default is a private
    ///                             namespace. For more information, see <a href="https://docs.docker.com/engine/reference/run/#pid-settings---pid">PID settings</a> in the <i>Docker run
    ///                                 reference</i>.</p>
    ///                         <p>If the <code>host</code> PID mode is used, be aware that there is a
    ///                             heightened risk of undesired process namespace expose. For more
    ///                             information, see <a href="https://docs.docker.com/engine/security/security/">Docker
    ///                                 security</a>.</p>
    ///          <note>
    ///                                 <p>This parameter is not supported for Windows containers or tasks run on AWS Fargate.</p>
    ///                              </note>
    public let pidMode: PidMode?
    /// <p>An array of placement constraint objects to use for the task. You can specify a
    /// 			maximum of 10 constraints per task (this limit includes constraints in the task
    /// 			definition and those specified at runtime).</p>
    public let placementConstraints: [TaskDefinitionPlacementConstraint]?
    /// <p>The configuration details for the App Mesh proxy.</p>
    /// 		       <p>For tasks hosted on Amazon EC2 instances, the container instances require at least version
    /// 				<code>1.26.0</code> of the container agent and at least version
    /// 				<code>1.26.0-1</code> of the <code>ecs-init</code> package to enable a proxy
    /// 			configuration. If your container instances are launched from the Amazon ECS-optimized
    /// 			AMI version <code>20190301</code> or later, then they contain the required versions of
    /// 			the container agent and <code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-ami-versions.html">Amazon ECS-optimized AMI versions</a> in the
    /// 			<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let proxyConfiguration: ProxyConfiguration?
    /// <p>The task launch type that Amazon ECS should validate the task definition against. A client
    /// 			exception is returned if the task definition doesn't validate against the
    /// 			compatibilities specified. If no value is specified, the parameter is omitted from the
    /// 			response.</p>
    public let requiresCompatibilities: [Compatibility]?
    /// <p>The metadata that you apply to the task definition to help you categorize and organize
    /// 			them. Each tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the IAM role that containers in this task can
    /// 			assume. All containers in this task are granted the permissions that are specified in
    /// 			this role. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html">IAM Roles for
    /// 				Tasks</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let taskRoleArn: String?
    /// <p>A list of volume definitions in JSON format that containers in your task may
    /// 			use.</p>
    public let volumes: [Volume]?

    public init (
        containerDefinitions: [ContainerDefinition]? = nil,
        cpu: String? = nil,
        ephemeralStorage: EphemeralStorage? = nil,
        executionRoleArn: String? = nil,
        family: String? = nil,
        inferenceAccelerators: [InferenceAccelerator]? = nil,
        ipcMode: IpcMode? = nil,
        memory: String? = nil,
        networkMode: NetworkMode? = nil,
        pidMode: PidMode? = nil,
        placementConstraints: [TaskDefinitionPlacementConstraint]? = nil,
        proxyConfiguration: ProxyConfiguration? = nil,
        requiresCompatibilities: [Compatibility]? = nil,
        tags: [Tag]? = nil,
        taskRoleArn: String? = nil,
        volumes: [Volume]? = nil
    )
    {
        self.containerDefinitions = containerDefinitions
        self.cpu = cpu
        self.ephemeralStorage = ephemeralStorage
        self.executionRoleArn = executionRoleArn
        self.family = family
        self.inferenceAccelerators = inferenceAccelerators
        self.ipcMode = ipcMode
        self.memory = memory
        self.networkMode = networkMode
        self.pidMode = pidMode
        self.placementConstraints = placementConstraints
        self.proxyConfiguration = proxyConfiguration
        self.requiresCompatibilities = requiresCompatibilities
        self.tags = tags
        self.taskRoleArn = taskRoleArn
        self.volumes = volumes
    }
}

struct RegisterTaskDefinitionInputBody: Equatable {
    public let family: String?
    public let taskRoleArn: String?
    public let executionRoleArn: String?
    public let networkMode: NetworkMode?
    public let containerDefinitions: [ContainerDefinition]?
    public let volumes: [Volume]?
    public let placementConstraints: [TaskDefinitionPlacementConstraint]?
    public let requiresCompatibilities: [Compatibility]?
    public let cpu: String?
    public let memory: String?
    public let tags: [Tag]?
    public let pidMode: PidMode?
    public let ipcMode: IpcMode?
    public let proxyConfiguration: ProxyConfiguration?
    public let inferenceAccelerators: [InferenceAccelerator]?
    public let ephemeralStorage: EphemeralStorage?
}

extension RegisterTaskDefinitionInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerDefinitions
        case cpu
        case ephemeralStorage
        case executionRoleArn
        case family
        case inferenceAccelerators
        case ipcMode
        case memory
        case networkMode
        case pidMode
        case placementConstraints
        case proxyConfiguration
        case requiresCompatibilities
        case tags
        case taskRoleArn
        case volumes
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let familyDecoded = try containerValues.decodeIfPresent(String.self, forKey: .family)
        family = familyDecoded
        let taskRoleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskRoleArn)
        taskRoleArn = taskRoleArnDecoded
        let executionRoleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .executionRoleArn)
        executionRoleArn = executionRoleArnDecoded
        let networkModeDecoded = try containerValues.decodeIfPresent(NetworkMode.self, forKey: .networkMode)
        networkMode = networkModeDecoded
        let containerDefinitionsContainer = try containerValues.decodeIfPresent([ContainerDefinition?].self, forKey: .containerDefinitions)
        var containerDefinitionsDecoded0:[ContainerDefinition]? = nil
        if let containerDefinitionsContainer = containerDefinitionsContainer {
            containerDefinitionsDecoded0 = [ContainerDefinition]()
            for structure0 in containerDefinitionsContainer {
                if let structure0 = structure0 {
                    containerDefinitionsDecoded0?.append(structure0)
                }
            }
        }
        containerDefinitions = containerDefinitionsDecoded0
        let volumesContainer = try containerValues.decodeIfPresent([Volume?].self, forKey: .volumes)
        var volumesDecoded0:[Volume]? = nil
        if let volumesContainer = volumesContainer {
            volumesDecoded0 = [Volume]()
            for structure0 in volumesContainer {
                if let structure0 = structure0 {
                    volumesDecoded0?.append(structure0)
                }
            }
        }
        volumes = volumesDecoded0
        let placementConstraintsContainer = try containerValues.decodeIfPresent([TaskDefinitionPlacementConstraint?].self, forKey: .placementConstraints)
        var placementConstraintsDecoded0:[TaskDefinitionPlacementConstraint]? = nil
        if let placementConstraintsContainer = placementConstraintsContainer {
            placementConstraintsDecoded0 = [TaskDefinitionPlacementConstraint]()
            for structure0 in placementConstraintsContainer {
                if let structure0 = structure0 {
                    placementConstraintsDecoded0?.append(structure0)
                }
            }
        }
        placementConstraints = placementConstraintsDecoded0
        let requiresCompatibilitiesContainer = try containerValues.decodeIfPresent([Compatibility?].self, forKey: .requiresCompatibilities)
        var requiresCompatibilitiesDecoded0:[Compatibility]? = nil
        if let requiresCompatibilitiesContainer = requiresCompatibilitiesContainer {
            requiresCompatibilitiesDecoded0 = [Compatibility]()
            for string0 in requiresCompatibilitiesContainer {
                if let string0 = string0 {
                    requiresCompatibilitiesDecoded0?.append(string0)
                }
            }
        }
        requiresCompatibilities = requiresCompatibilitiesDecoded0
        let cpuDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cpu)
        cpu = cpuDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .memory)
        memory = memoryDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let pidModeDecoded = try containerValues.decodeIfPresent(PidMode.self, forKey: .pidMode)
        pidMode = pidModeDecoded
        let ipcModeDecoded = try containerValues.decodeIfPresent(IpcMode.self, forKey: .ipcMode)
        ipcMode = ipcModeDecoded
        let proxyConfigurationDecoded = try containerValues.decodeIfPresent(ProxyConfiguration.self, forKey: .proxyConfiguration)
        proxyConfiguration = proxyConfigurationDecoded
        let inferenceAcceleratorsContainer = try containerValues.decodeIfPresent([InferenceAccelerator?].self, forKey: .inferenceAccelerators)
        var inferenceAcceleratorsDecoded0:[InferenceAccelerator]? = nil
        if let inferenceAcceleratorsContainer = inferenceAcceleratorsContainer {
            inferenceAcceleratorsDecoded0 = [InferenceAccelerator]()
            for structure0 in inferenceAcceleratorsContainer {
                if let structure0 = structure0 {
                    inferenceAcceleratorsDecoded0?.append(structure0)
                }
            }
        }
        inferenceAccelerators = inferenceAcceleratorsDecoded0
        let ephemeralStorageDecoded = try containerValues.decodeIfPresent(EphemeralStorage.self, forKey: .ephemeralStorage)
        ephemeralStorage = ephemeralStorageDecoded
    }
}

extension RegisterTaskDefinitionOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension RegisterTaskDefinitionOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum RegisterTaskDefinitionOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension RegisterTaskDefinitionOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RegisterTaskDefinitionOutputResponse(tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension RegisterTaskDefinitionOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: RegisterTaskDefinitionOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.tags = output.tags
            self.taskDefinition = output.taskDefinition
        } else {
            self.tags = nil
            self.taskDefinition = nil
        }
    }
}

public struct RegisterTaskDefinitionOutputResponse: Equatable {
    /// <p>The list of tags associated with the task definition.</p>
    public let tags: [Tag]?
    /// <p>The full description of the registered task definition.</p>
    public let taskDefinition: TaskDefinition?

    public init (
        tags: [Tag]? = nil,
        taskDefinition: TaskDefinition? = nil
    )
    {
        self.tags = tags
        self.taskDefinition = taskDefinition
    }
}

struct RegisterTaskDefinitionOutputResponseBody: Equatable {
    public let taskDefinition: TaskDefinition?
    public let tags: [Tag]?
}

extension RegisterTaskDefinitionOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case tags
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(TaskDefinition.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension RepositoryCredentials: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case credentialsParameter
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let credentialsParameter = credentialsParameter {
            try encodeContainer.encode(credentialsParameter, forKey: .credentialsParameter)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let credentialsParameterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .credentialsParameter)
        credentialsParameter = credentialsParameterDecoded
    }
}

extension RepositoryCredentials: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RepositoryCredentials(credentialsParameter: \(String(describing: credentialsParameter)))"}
}

/// <p>The repository credentials for private registry authentication.</p>
public struct RepositoryCredentials: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the secret containing the private repository
    /// 			credentials.</p>
    /// 		       <note>
    /// 			         <p>When you are using the Amazon ECS API, AWS CLI, or AWS SDK, if the secret exists in the
    /// 				same Region as the task that you are launching then you can use either the full ARN
    /// 				or the name of the secret. When you are using the AWS Management Console, you must specify the
    /// 				full ARN of the secret.</p>
    /// 		       </note>
    public let credentialsParameter: String?

    public init (
        credentialsParameter: String? = nil
    )
    {
        self.credentialsParameter = credentialsParameter
    }
}

extension Resource: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case doubleValue
        case integerValue
        case longValue
        case name
        case stringSetValue
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if doubleValue != 0.0 {
            try encodeContainer.encode(doubleValue, forKey: .doubleValue)
        }
        if integerValue != 0 {
            try encodeContainer.encode(integerValue, forKey: .integerValue)
        }
        if longValue != 0 {
            try encodeContainer.encode(longValue, forKey: .longValue)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let stringSetValue = stringSetValue {
            var stringSetValueContainer = encodeContainer.nestedUnkeyedContainer(forKey: .stringSetValue)
            for stringlist0 in stringSetValue {
                try stringSetValueContainer.encode(stringlist0)
            }
        }
        if let type = type {
            try encodeContainer.encode(type, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let typeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .type)
        type = typeDecoded
        let doubleValueDecoded = try containerValues.decode(Double.self, forKey: .doubleValue)
        doubleValue = doubleValueDecoded
        let longValueDecoded = try containerValues.decode(Int.self, forKey: .longValue)
        longValue = longValueDecoded
        let integerValueDecoded = try containerValues.decode(Int.self, forKey: .integerValue)
        integerValue = integerValueDecoded
        let stringSetValueContainer = try containerValues.decodeIfPresent([String?].self, forKey: .stringSetValue)
        var stringSetValueDecoded0:[String]? = nil
        if let stringSetValueContainer = stringSetValueContainer {
            stringSetValueDecoded0 = [String]()
            for string0 in stringSetValueContainer {
                if let string0 = string0 {
                    stringSetValueDecoded0?.append(string0)
                }
            }
        }
        stringSetValue = stringSetValueDecoded0
    }
}

extension Resource: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Resource(doubleValue: \(String(describing: doubleValue)), integerValue: \(String(describing: integerValue)), longValue: \(String(describing: longValue)), name: \(String(describing: name)), stringSetValue: \(String(describing: stringSetValue)), type: \(String(describing: type)))"}
}

/// <p>Describes the resources available for a container instance.</p>
public struct Resource: Equatable {
    /// <p>When the <code>doubleValue</code> type is set, the value of the resource must be a
    /// 			double precision floating-point type.</p>
    public let doubleValue: Double
    /// <p>When the <code>integerValue</code> type is set, the value of the resource must be an
    /// 			integer.</p>
    public let integerValue: Int
    /// <p>When the <code>longValue</code> type is set, the value of the resource must be an
    /// 			extended precision floating-point type.</p>
    public let longValue: Int
    /// <p>The name of the resource, such as <code>CPU</code>, <code>MEMORY</code>,
    /// 				<code>PORTS</code>, <code>PORTS_UDP</code>, or a user-defined resource.</p>
    public let name: String?
    /// <p>When the <code>stringSetValue</code> type is set, the value of the resource must be a
    /// 			string type.</p>
    public let stringSetValue: [String]?
    /// <p>The type of the resource, such as <code>INTEGER</code>, <code>DOUBLE</code>,
    /// 				<code>LONG</code>, or <code>STRINGSET</code>.</p>
    public let type: String?

    public init (
        doubleValue: Double = 0.0,
        integerValue: Int = 0,
        longValue: Int = 0,
        name: String? = nil,
        stringSetValue: [String]? = nil,
        type: String? = nil
    )
    {
        self.doubleValue = doubleValue
        self.integerValue = integerValue
        self.longValue = longValue
        self.name = name
        self.stringSetValue = stringSetValue
        self.type = type
    }
}

extension ResourceInUseException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ResourceInUseException(message: \(String(describing: message)))"}
}

extension ResourceInUseException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ResourceInUseExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified resource is in-use and cannot be removed.</p>
public struct ResourceInUseException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ResourceInUseExceptionBody: Equatable {
    public let message: String?
}

extension ResourceInUseExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ResourceNotFoundException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ResourceNotFoundException(message: \(String(describing: message)))"}
}

extension ResourceNotFoundException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ResourceNotFoundExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified resource could not be found.</p>
public struct ResourceNotFoundException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ResourceNotFoundExceptionBody: Equatable {
    public let message: String?
}

extension ResourceNotFoundExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ResourceRequirement: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case type
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let typeDecoded = try containerValues.decodeIfPresent(ResourceType.self, forKey: .type)
        type = typeDecoded
    }
}

extension ResourceRequirement: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ResourceRequirement(type: \(String(describing: type)), value: \(String(describing: value)))"}
}

/// <p>The type and amount of a resource to assign to a container. The supported resource
/// 			types are GPUs and Elastic Inference accelerators. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-gpu.html">Working with
/// 				GPUs on Amazon ECS</a> or <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-eia.html">Working with Amazon Elastic Inference on Amazon ECS</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>
///          </p>
public struct ResourceRequirement: Equatable {
    /// <p>The type of resource to assign to a container. The supported values are
    /// 				<code>GPU</code> or <code>InferenceAccelerator</code>.</p>
    public let type: ResourceType?
    /// <p>The value for the specified resource type.</p>
    /// 		       <p>If the <code>GPU</code> type is used, the value is the number of physical
    /// 				<code>GPUs</code> the Amazon ECS container agent will reserve for the container. The
    /// 			number of GPUs reserved for all containers in a task should not exceed the number of
    /// 			available GPUs on the container instance the task is launched on.</p>
    /// 		       <p>If the <code>InferenceAccelerator</code> type is used, the <code>value</code> should
    /// 			match the <code>deviceName</code> for an <a>InferenceAccelerator</a>
    /// 			specified in a task definition.</p>
    public let value: String?

    public init (
        type: ResourceType? = nil,
        value: String? = nil
    )
    {
        self.type = type
        self.value = value
    }
}

public enum ResourceType {
    case gpu
    case inferenceAccelerator
    case sdkUnknown(String)
}

extension ResourceType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ResourceType] {
        return [
            .gpu,
            .inferenceAccelerator,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .gpu: return "GPU"
        case .inferenceAccelerator: return "InferenceAccelerator"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ResourceType(rawValue: rawValue) ?? ResourceType.sdkUnknown(rawValue)
    }
}

public struct RunTaskInputBodyMiddleware: Middleware {
    public let id: String = "RunTaskInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RunTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<RunTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RunTaskInput>
    public typealias MOutput = OperationOutput<RunTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RunTaskOutputError>
}

extension RunTaskInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RunTaskInput(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), cluster: \(String(describing: cluster)), count: \(String(describing: count)), enableECSManagedTags: \(String(describing: enableECSManagedTags)), enableExecuteCommand: \(String(describing: enableExecuteCommand)), group: \(String(describing: group)), launchType: \(String(describing: launchType)), networkConfiguration: \(String(describing: networkConfiguration)), overrides: \(String(describing: overrides)), placementConstraints: \(String(describing: placementConstraints)), placementStrategy: \(String(describing: placementStrategy)), platformVersion: \(String(describing: platformVersion)), propagateTags: \(String(describing: propagateTags)), referenceId: \(String(describing: referenceId)), startedBy: \(String(describing: startedBy)), tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension RunTaskInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case cluster
        case count
        case enableECSManagedTags
        case enableExecuteCommand
        case group
        case launchType
        case networkConfiguration
        case overrides
        case placementConstraints
        case placementStrategy
        case platformVersion
        case propagateTags
        case referenceId
        case startedBy
        case tags
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let count = count {
            try encodeContainer.encode(count, forKey: .count)
        }
        if enableECSManagedTags != false {
            try encodeContainer.encode(enableECSManagedTags, forKey: .enableECSManagedTags)
        }
        if enableExecuteCommand != false {
            try encodeContainer.encode(enableExecuteCommand, forKey: .enableExecuteCommand)
        }
        if let group = group {
            try encodeContainer.encode(group, forKey: .group)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if let overrides = overrides {
            try encodeContainer.encode(overrides, forKey: .overrides)
        }
        if let placementConstraints = placementConstraints {
            var placementConstraintsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementConstraints)
            for placementconstraints0 in placementConstraints {
                try placementConstraintsContainer.encode(placementconstraints0)
            }
        }
        if let placementStrategy = placementStrategy {
            var placementStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementStrategy)
            for placementstrategies0 in placementStrategy {
                try placementStrategyContainer.encode(placementstrategies0)
            }
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let propagateTags = propagateTags {
            try encodeContainer.encode(propagateTags.rawValue, forKey: .propagateTags)
        }
        if let referenceId = referenceId {
            try encodeContainer.encode(referenceId, forKey: .referenceId)
        }
        if let startedBy = startedBy {
            try encodeContainer.encode(startedBy, forKey: .startedBy)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct RunTaskInputHeadersMiddleware: Middleware {
    public let id: String = "RunTaskInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RunTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<RunTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RunTaskInput>
    public typealias MOutput = OperationOutput<RunTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RunTaskOutputError>
}

public struct RunTaskInputQueryItemMiddleware: Middleware {
    public let id: String = "RunTaskInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<RunTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<RunTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<RunTaskInput>
    public typealias MOutput = OperationOutput<RunTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<RunTaskOutputError>
}

public struct RunTaskInput: Equatable {
    /// <p>The capacity provider strategy to use for the task.</p>
    /// 		       <p>If a <code>capacityProviderStrategy</code> is specified, the <code>launchType</code>
    /// 			parameter must be omitted. If no <code>capacityProviderStrategy</code> or
    /// 				<code>launchType</code> is specified, the
    /// 				<code>defaultCapacityProviderStrategy</code> for the cluster is used.</p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster on which to run your task.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The number of instantiations of the specified task to place on your cluster. You can
    /// 			specify up to 10 tasks per call.</p>
    public let count: Int?
    /// <p>Specifies whether to enable Amazon ECS managed tags for the task. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-using-tags.html">Tagging Your Amazon ECS
    /// 				Resources</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let enableECSManagedTags: Bool
    /// <p>Whether or not to enable the execute command functionality for the containers in this
    /// 			task. If <code>true</code>, this enables execute command functionality on all containers
    /// 			in the task.</p>
    public let enableExecuteCommand: Bool
    /// <p>The name of the task group to associate with the task. The default value is the family
    /// 			name of the task definition (for example, family:my-family-name).</p>
    public let group: String?
    /// <p>The infrastructure on which to run your standalone task. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS launch types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>The <code>FARGATE</code> launch type runs your tasks on AWS Fargate On-Demand
    /// 			infrastructure.</p>
    /// 		       <note>
    /// 			         <p>Fargate Spot infrastructure is available for use but a capacity provider
    /// 				strategy must be used. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/userguide/fargate-capacity-providers.html">AWS Fargate capacity providers</a> in the
    /// 					<i>Amazon ECS User Guide for AWS Fargate</i>.</p>
    /// 		       </note>
    /// 		       <p>The <code>EC2</code> launch type runs your tasks on Amazon EC2 instances registered to your
    /// 			cluster.</p>
    /// 		       <p>The <code>EXTERNAL</code> launch type runs your tasks on your on-premise server or
    /// 			virtual machine (VM) capacity registered to your cluster.</p>
    /// 		       <p>A task can use either a launch type or a capacity provider strategy. If a
    /// 				<code>launchType</code> is specified, the <code>capacityProviderStrategy</code>
    /// 			parameter must be omitted.</p>
    public let launchType: LaunchType?
    /// <p>The network configuration for the task. This parameter is required for task
    /// 			definitions that use the <code>awsvpc</code> network mode to receive their own elastic
    /// 			network interface, and it is not supported for other network modes. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-networking.html">Task Networking</a>
    /// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>A list of container overrides in JSON format that specify the name of a container in
    /// 			the specified task definition and the overrides it should receive. You can override the
    /// 			default command for a container (that is specified in the task definition or Docker
    /// 			image) with a <code>command</code> override. You can also override existing environment
    /// 			variables (that are specified in the task definition or Docker image) on a container or
    /// 			add new environment variables to it with an <code>environment</code> override.</p>
    /// 		       <note>
    /// 			         <p>A total of 8192 characters are allowed for overrides. This limit includes the JSON
    /// 				formatting characters of the override structure.</p>
    /// 		       </note>
    public let overrides: TaskOverride?
    /// <p>An array of placement constraint objects to use for the task. You can specify up to 10
    /// 			constraints per task (including constraints in the task definition and those specified
    /// 			at runtime).</p>
    public let placementConstraints: [PlacementConstraint]?
    /// <p>The placement strategy objects to use for the task. You can specify a maximum of five
    /// 			strategy rules per task.</p>
    public let placementStrategy: [PlacementStrategy]?
    /// <p>The platform version the task should run. A platform version is only specified for
    /// 			tasks using the Fargate launch type. If one is not specified, the
    /// 				<code>LATEST</code> platform version is used by default. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate Platform
    /// 				Versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <p>Specifies whether to propagate the tags from the task definition to the task. If no
    /// 			value is specified, the tags are not propagated. Tags can only be propagated to the task
    /// 			during task creation. To add tags to a task after task creation, use the <a>TagResource</a> API action.</p>
    /// 		       <note>
    /// 			         <p>An error will be received if you specify the <code>SERVICE</code> option when
    /// 				running a task.</p>
    /// 		       </note>
    public let propagateTags: PropagateTags?
    /// <p>The reference ID to use for the task.</p>
    public let referenceId: String?
    /// <p>An optional tag specified when a task is started. For example, if you automatically
    /// 			trigger a task to run a batch process job, you could apply a unique identifier for that
    /// 			job to your task with the <code>startedBy</code> parameter. You can then identify which
    /// 			tasks belong to that job by filtering the results of a <a>ListTasks</a> call
    /// 			with the <code>startedBy</code> value. Up to 36 letters (uppercase and lowercase),
    /// 			numbers, hyphens, and underscores are allowed.</p>
    /// 		       <p>If a task is started by an Amazon ECS service, then the <code>startedBy</code> parameter
    /// 			contains the deployment ID of the service that starts it.</p>
    public let startedBy: String?
    /// <p>The metadata that you apply to the task to help you categorize and organize them. Each
    /// 			tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The <code>family</code> and <code>revision</code> (<code>family:revision</code>) or
    /// 			full ARN of the task definition to run. If a <code>revision</code> is not specified,
    /// 			the latest <code>ACTIVE</code> revision is used.</p>
    public let taskDefinition: String?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        cluster: String? = nil,
        count: Int? = nil,
        enableECSManagedTags: Bool = false,
        enableExecuteCommand: Bool = false,
        group: String? = nil,
        launchType: LaunchType? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        overrides: TaskOverride? = nil,
        placementConstraints: [PlacementConstraint]? = nil,
        placementStrategy: [PlacementStrategy]? = nil,
        platformVersion: String? = nil,
        propagateTags: PropagateTags? = nil,
        referenceId: String? = nil,
        startedBy: String? = nil,
        tags: [Tag]? = nil,
        taskDefinition: String? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.cluster = cluster
        self.count = count
        self.enableECSManagedTags = enableECSManagedTags
        self.enableExecuteCommand = enableExecuteCommand
        self.group = group
        self.launchType = launchType
        self.networkConfiguration = networkConfiguration
        self.overrides = overrides
        self.placementConstraints = placementConstraints
        self.placementStrategy = placementStrategy
        self.platformVersion = platformVersion
        self.propagateTags = propagateTags
        self.referenceId = referenceId
        self.startedBy = startedBy
        self.tags = tags
        self.taskDefinition = taskDefinition
    }
}

struct RunTaskInputBody: Equatable {
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    public let cluster: String?
    public let count: Int?
    public let enableECSManagedTags: Bool
    public let enableExecuteCommand: Bool
    public let group: String?
    public let launchType: LaunchType?
    public let networkConfiguration: NetworkConfiguration?
    public let overrides: TaskOverride?
    public let placementConstraints: [PlacementConstraint]?
    public let placementStrategy: [PlacementStrategy]?
    public let platformVersion: String?
    public let propagateTags: PropagateTags?
    public let referenceId: String?
    public let startedBy: String?
    public let tags: [Tag]?
    public let taskDefinition: String?
}

extension RunTaskInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case cluster
        case count
        case enableECSManagedTags
        case enableExecuteCommand
        case group
        case launchType
        case networkConfiguration
        case overrides
        case placementConstraints
        case placementStrategy
        case platformVersion
        case propagateTags
        case referenceId
        case startedBy
        case tags
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let countDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .count)
        count = countDecoded
        let enableECSManagedTagsDecoded = try containerValues.decode(Bool.self, forKey: .enableECSManagedTags)
        enableECSManagedTags = enableECSManagedTagsDecoded
        let enableExecuteCommandDecoded = try containerValues.decode(Bool.self, forKey: .enableExecuteCommand)
        enableExecuteCommand = enableExecuteCommandDecoded
        let groupDecoded = try containerValues.decodeIfPresent(String.self, forKey: .group)
        group = groupDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let overridesDecoded = try containerValues.decodeIfPresent(TaskOverride.self, forKey: .overrides)
        overrides = overridesDecoded
        let placementConstraintsContainer = try containerValues.decodeIfPresent([PlacementConstraint?].self, forKey: .placementConstraints)
        var placementConstraintsDecoded0:[PlacementConstraint]? = nil
        if let placementConstraintsContainer = placementConstraintsContainer {
            placementConstraintsDecoded0 = [PlacementConstraint]()
            for structure0 in placementConstraintsContainer {
                if let structure0 = structure0 {
                    placementConstraintsDecoded0?.append(structure0)
                }
            }
        }
        placementConstraints = placementConstraintsDecoded0
        let placementStrategyContainer = try containerValues.decodeIfPresent([PlacementStrategy?].self, forKey: .placementStrategy)
        var placementStrategyDecoded0:[PlacementStrategy]? = nil
        if let placementStrategyContainer = placementStrategyContainer {
            placementStrategyDecoded0 = [PlacementStrategy]()
            for structure0 in placementStrategyContainer {
                if let structure0 = structure0 {
                    placementStrategyDecoded0?.append(structure0)
                }
            }
        }
        placementStrategy = placementStrategyDecoded0
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let propagateTagsDecoded = try containerValues.decodeIfPresent(PropagateTags.self, forKey: .propagateTags)
        propagateTags = propagateTagsDecoded
        let referenceIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .referenceId)
        referenceId = referenceIdDecoded
        let startedByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .startedBy)
        startedBy = startedByDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
    }
}

extension RunTaskOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension RunTaskOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "BlockedException" : self = .blockedException(try BlockedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformTaskDefinitionIncompatibilityException" : self = .platformTaskDefinitionIncompatibilityException(try PlatformTaskDefinitionIncompatibilityException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformUnknownException" : self = .platformUnknownException(try PlatformUnknownException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum RunTaskOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case blockedException(BlockedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case platformTaskDefinitionIncompatibilityException(PlatformTaskDefinitionIncompatibilityException)
    case platformUnknownException(PlatformUnknownException)
    case serverException(ServerException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension RunTaskOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "RunTaskOutputResponse(failures: \(String(describing: failures)), tasks: \(String(describing: tasks)))"}
}

extension RunTaskOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: RunTaskOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.failures = output.failures
            self.tasks = output.tasks
        } else {
            self.failures = nil
            self.tasks = nil
        }
    }
}

public struct RunTaskOutputResponse: Equatable {
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?
    /// <p>A full description of the tasks that were run. The tasks that were successfully placed
    /// 			on your cluster are described here.</p>
    public let tasks: [Task]?

    public init (
        failures: [Failure]? = nil,
        tasks: [Task]? = nil
    )
    {
        self.failures = failures
        self.tasks = tasks
    }
}

struct RunTaskOutputResponseBody: Equatable {
    public let tasks: [Task]?
    public let failures: [Failure]?
}

extension RunTaskOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case failures
        case tasks
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let tasksContainer = try containerValues.decodeIfPresent([Task?].self, forKey: .tasks)
        var tasksDecoded0:[Task]? = nil
        if let tasksContainer = tasksContainer {
            tasksDecoded0 = [Task]()
            for structure0 in tasksContainer {
                if let structure0 = structure0 {
                    tasksDecoded0?.append(structure0)
                }
            }
        }
        tasks = tasksDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

extension Scale: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case unit
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let unit = unit {
            try encodeContainer.encode(unit.rawValue, forKey: .unit)
        }
        if value != 0.0 {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let valueDecoded = try containerValues.decode(Double.self, forKey: .value)
        value = valueDecoded
        let unitDecoded = try containerValues.decodeIfPresent(ScaleUnit.self, forKey: .unit)
        unit = unitDecoded
    }
}

extension Scale: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Scale(unit: \(String(describing: unit)), value: \(String(describing: value)))"}
}

/// <p>A floating-point percentage of the desired number of tasks to place and keep running
/// 			in the task set.</p>
public struct Scale: Equatable {
    /// <p>The unit of measure for the scale value.</p>
    public let unit: ScaleUnit?
    /// <p>The value, specified as a percent total of a service's <code>desiredCount</code>, to
    /// 			scale the task set. Accepted values are numbers between 0 and 100.</p>
    public let value: Double

    public init (
        unit: ScaleUnit? = nil,
        value: Double = 0.0
    )
    {
        self.unit = unit
        self.value = value
    }
}

public enum ScaleUnit {
    case percent
    case sdkUnknown(String)
}

extension ScaleUnit : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ScaleUnit] {
        return [
            .percent,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .percent: return "PERCENT"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ScaleUnit(rawValue: rawValue) ?? ScaleUnit.sdkUnknown(rawValue)
    }
}

public enum SchedulingStrategy {
    case daemon
    case replica
    case sdkUnknown(String)
}

extension SchedulingStrategy : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [SchedulingStrategy] {
        return [
            .daemon,
            .replica,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .daemon: return "DAEMON"
        case .replica: return "REPLICA"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = SchedulingStrategy(rawValue: rawValue) ?? SchedulingStrategy.sdkUnknown(rawValue)
    }
}

public enum Scope {
    case shared
    case task
    case sdkUnknown(String)
}

extension Scope : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [Scope] {
        return [
            .shared,
            .task,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .shared: return "shared"
        case .task: return "task"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = Scope(rawValue: rawValue) ?? Scope.sdkUnknown(rawValue)
    }
}

extension Secret: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case valueFrom
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
        if let valueFrom = valueFrom {
            try encodeContainer.encode(valueFrom, forKey: .valueFrom)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let valueFromDecoded = try containerValues.decodeIfPresent(String.self, forKey: .valueFrom)
        valueFrom = valueFromDecoded
    }
}

extension Secret: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Secret(name: \(String(describing: name)), valueFrom: \(String(describing: valueFrom)))"}
}

/// <p>An object representing the secret to expose to your container. Secrets can be exposed
/// 			to a container in the following ways:</p>
/// 		       <ul>
///             <li>
/// 				           <p>To inject sensitive data into your containers as environment variables, use
/// 					the <code>secrets</code> container definition parameter.</p>
/// 			         </li>
///             <li>
/// 				           <p>To reference sensitive information in the log configuration of a container,
/// 					use the <code>secretOptions</code> container definition parameter.</p>
/// 			         </li>
///          </ul>
/// 		       <p>For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/specifying-sensitive-data.html">Specifying
/// 				Sensitive Data</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
public struct Secret: Equatable {
    /// <p>The name of the secret.</p>
    public let name: String?
    /// <p>The secret to expose to the container. The supported values are either the full ARN of
    /// 			the AWS Secrets Manager secret or the full ARN of the parameter in the AWS Systems Manager Parameter Store.</p>
    /// 		       <note>
    /// 			         <p>If the AWS Systems Manager Parameter Store parameter exists in the same Region as the task you
    /// 				are launching, then you can use either the full ARN or name of the parameter. If the
    /// 				parameter exists in a different Region, then the full ARN must be specified.</p>
    /// 		       </note>
    public let valueFrom: String?

    public init (
        name: String? = nil,
        valueFrom: String? = nil
    )
    {
        self.name = name
        self.valueFrom = valueFrom
    }
}

extension ServerException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ServerException(message: \(String(describing: message)))"}
}

extension ServerException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ServerExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>These errors are usually caused by a server issue.</p>
public struct ServerException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .server
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ServerExceptionBody: Equatable {
    public let message: String?
}

extension ServerExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension Service: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case clusterArn
        case createdAt
        case createdBy
        case deploymentConfiguration
        case deploymentController
        case deployments
        case desiredCount
        case enableECSManagedTags
        case enableExecuteCommand
        case events
        case healthCheckGracePeriodSeconds
        case launchType
        case loadBalancers
        case networkConfiguration
        case pendingCount
        case placementConstraints
        case placementStrategy
        case platformVersion
        case propagateTags
        case roleArn
        case runningCount
        case schedulingStrategy
        case serviceArn
        case serviceName
        case serviceRegistries
        case status
        case tags
        case taskDefinition
        case taskSets
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let clusterArn = clusterArn {
            try encodeContainer.encode(clusterArn, forKey: .clusterArn)
        }
        if let createdAt = createdAt {
            try encodeContainer.encode(createdAt.timeIntervalSince1970, forKey: .createdAt)
        }
        if let createdBy = createdBy {
            try encodeContainer.encode(createdBy, forKey: .createdBy)
        }
        if let deploymentConfiguration = deploymentConfiguration {
            try encodeContainer.encode(deploymentConfiguration, forKey: .deploymentConfiguration)
        }
        if let deploymentController = deploymentController {
            try encodeContainer.encode(deploymentController, forKey: .deploymentController)
        }
        if let deployments = deployments {
            var deploymentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .deployments)
            for deployments0 in deployments {
                try deploymentsContainer.encode(deployments0)
            }
        }
        if desiredCount != 0 {
            try encodeContainer.encode(desiredCount, forKey: .desiredCount)
        }
        if enableECSManagedTags != false {
            try encodeContainer.encode(enableECSManagedTags, forKey: .enableECSManagedTags)
        }
        if enableExecuteCommand != false {
            try encodeContainer.encode(enableExecuteCommand, forKey: .enableExecuteCommand)
        }
        if let events = events {
            var eventsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .events)
            for serviceevents0 in events {
                try eventsContainer.encode(serviceevents0)
            }
        }
        if let healthCheckGracePeriodSeconds = healthCheckGracePeriodSeconds {
            try encodeContainer.encode(healthCheckGracePeriodSeconds, forKey: .healthCheckGracePeriodSeconds)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let loadBalancers = loadBalancers {
            var loadBalancersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .loadBalancers)
            for loadbalancers0 in loadBalancers {
                try loadBalancersContainer.encode(loadbalancers0)
            }
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if pendingCount != 0 {
            try encodeContainer.encode(pendingCount, forKey: .pendingCount)
        }
        if let placementConstraints = placementConstraints {
            var placementConstraintsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementConstraints)
            for placementconstraints0 in placementConstraints {
                try placementConstraintsContainer.encode(placementconstraints0)
            }
        }
        if let placementStrategy = placementStrategy {
            var placementStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementStrategy)
            for placementstrategies0 in placementStrategy {
                try placementStrategyContainer.encode(placementstrategies0)
            }
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let propagateTags = propagateTags {
            try encodeContainer.encode(propagateTags.rawValue, forKey: .propagateTags)
        }
        if let roleArn = roleArn {
            try encodeContainer.encode(roleArn, forKey: .roleArn)
        }
        if runningCount != 0 {
            try encodeContainer.encode(runningCount, forKey: .runningCount)
        }
        if let schedulingStrategy = schedulingStrategy {
            try encodeContainer.encode(schedulingStrategy.rawValue, forKey: .schedulingStrategy)
        }
        if let serviceArn = serviceArn {
            try encodeContainer.encode(serviceArn, forKey: .serviceArn)
        }
        if let serviceName = serviceName {
            try encodeContainer.encode(serviceName, forKey: .serviceName)
        }
        if let serviceRegistries = serviceRegistries {
            var serviceRegistriesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .serviceRegistries)
            for serviceregistries0 in serviceRegistries {
                try serviceRegistriesContainer.encode(serviceregistries0)
            }
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
        if let taskSets = taskSets {
            var taskSetsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .taskSets)
            for tasksets0 in taskSets {
                try taskSetsContainer.encode(tasksets0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let serviceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceArn)
        serviceArn = serviceArnDecoded
        let serviceNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceName)
        serviceName = serviceNameDecoded
        let clusterArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterArn)
        clusterArn = clusterArnDecoded
        let loadBalancersContainer = try containerValues.decodeIfPresent([LoadBalancer?].self, forKey: .loadBalancers)
        var loadBalancersDecoded0:[LoadBalancer]? = nil
        if let loadBalancersContainer = loadBalancersContainer {
            loadBalancersDecoded0 = [LoadBalancer]()
            for structure0 in loadBalancersContainer {
                if let structure0 = structure0 {
                    loadBalancersDecoded0?.append(structure0)
                }
            }
        }
        loadBalancers = loadBalancersDecoded0
        let serviceRegistriesContainer = try containerValues.decodeIfPresent([ServiceRegistry?].self, forKey: .serviceRegistries)
        var serviceRegistriesDecoded0:[ServiceRegistry]? = nil
        if let serviceRegistriesContainer = serviceRegistriesContainer {
            serviceRegistriesDecoded0 = [ServiceRegistry]()
            for structure0 in serviceRegistriesContainer {
                if let structure0 = structure0 {
                    serviceRegistriesDecoded0?.append(structure0)
                }
            }
        }
        serviceRegistries = serviceRegistriesDecoded0
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let desiredCountDecoded = try containerValues.decode(Int.self, forKey: .desiredCount)
        desiredCount = desiredCountDecoded
        let runningCountDecoded = try containerValues.decode(Int.self, forKey: .runningCount)
        runningCount = runningCountDecoded
        let pendingCountDecoded = try containerValues.decode(Int.self, forKey: .pendingCount)
        pendingCount = pendingCountDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let deploymentConfigurationDecoded = try containerValues.decodeIfPresent(DeploymentConfiguration.self, forKey: .deploymentConfiguration)
        deploymentConfiguration = deploymentConfigurationDecoded
        let taskSetsContainer = try containerValues.decodeIfPresent([TaskSet?].self, forKey: .taskSets)
        var taskSetsDecoded0:[TaskSet]? = nil
        if let taskSetsContainer = taskSetsContainer {
            taskSetsDecoded0 = [TaskSet]()
            for structure0 in taskSetsContainer {
                if let structure0 = structure0 {
                    taskSetsDecoded0?.append(structure0)
                }
            }
        }
        taskSets = taskSetsDecoded0
        let deploymentsContainer = try containerValues.decodeIfPresent([Deployment?].self, forKey: .deployments)
        var deploymentsDecoded0:[Deployment]? = nil
        if let deploymentsContainer = deploymentsContainer {
            deploymentsDecoded0 = [Deployment]()
            for structure0 in deploymentsContainer {
                if let structure0 = structure0 {
                    deploymentsDecoded0?.append(structure0)
                }
            }
        }
        deployments = deploymentsDecoded0
        let roleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .roleArn)
        roleArn = roleArnDecoded
        let eventsContainer = try containerValues.decodeIfPresent([ServiceEvent?].self, forKey: .events)
        var eventsDecoded0:[ServiceEvent]? = nil
        if let eventsContainer = eventsContainer {
            eventsDecoded0 = [ServiceEvent]()
            for structure0 in eventsContainer {
                if let structure0 = structure0 {
                    eventsDecoded0?.append(structure0)
                }
            }
        }
        events = eventsDecoded0
        let createdAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .createdAt)
        createdAt = createdAtDecoded
        let placementConstraintsContainer = try containerValues.decodeIfPresent([PlacementConstraint?].self, forKey: .placementConstraints)
        var placementConstraintsDecoded0:[PlacementConstraint]? = nil
        if let placementConstraintsContainer = placementConstraintsContainer {
            placementConstraintsDecoded0 = [PlacementConstraint]()
            for structure0 in placementConstraintsContainer {
                if let structure0 = structure0 {
                    placementConstraintsDecoded0?.append(structure0)
                }
            }
        }
        placementConstraints = placementConstraintsDecoded0
        let placementStrategyContainer = try containerValues.decodeIfPresent([PlacementStrategy?].self, forKey: .placementStrategy)
        var placementStrategyDecoded0:[PlacementStrategy]? = nil
        if let placementStrategyContainer = placementStrategyContainer {
            placementStrategyDecoded0 = [PlacementStrategy]()
            for structure0 in placementStrategyContainer {
                if let structure0 = structure0 {
                    placementStrategyDecoded0?.append(structure0)
                }
            }
        }
        placementStrategy = placementStrategyDecoded0
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let healthCheckGracePeriodSecondsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .healthCheckGracePeriodSeconds)
        healthCheckGracePeriodSeconds = healthCheckGracePeriodSecondsDecoded
        let schedulingStrategyDecoded = try containerValues.decodeIfPresent(SchedulingStrategy.self, forKey: .schedulingStrategy)
        schedulingStrategy = schedulingStrategyDecoded
        let deploymentControllerDecoded = try containerValues.decodeIfPresent(DeploymentController.self, forKey: .deploymentController)
        deploymentController = deploymentControllerDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let createdByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .createdBy)
        createdBy = createdByDecoded
        let enableECSManagedTagsDecoded = try containerValues.decode(Bool.self, forKey: .enableECSManagedTags)
        enableECSManagedTags = enableECSManagedTagsDecoded
        let propagateTagsDecoded = try containerValues.decodeIfPresent(PropagateTags.self, forKey: .propagateTags)
        propagateTags = propagateTagsDecoded
        let enableExecuteCommandDecoded = try containerValues.decode(Bool.self, forKey: .enableExecuteCommand)
        enableExecuteCommand = enableExecuteCommandDecoded
    }
}

extension Service: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Service(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), clusterArn: \(String(describing: clusterArn)), createdAt: \(String(describing: createdAt)), createdBy: \(String(describing: createdBy)), deploymentConfiguration: \(String(describing: deploymentConfiguration)), deploymentController: \(String(describing: deploymentController)), deployments: \(String(describing: deployments)), desiredCount: \(String(describing: desiredCount)), enableECSManagedTags: \(String(describing: enableECSManagedTags)), enableExecuteCommand: \(String(describing: enableExecuteCommand)), events: \(String(describing: events)), healthCheckGracePeriodSeconds: \(String(describing: healthCheckGracePeriodSeconds)), launchType: \(String(describing: launchType)), loadBalancers: \(String(describing: loadBalancers)), networkConfiguration: \(String(describing: networkConfiguration)), pendingCount: \(String(describing: pendingCount)), placementConstraints: \(String(describing: placementConstraints)), placementStrategy: \(String(describing: placementStrategy)), platformVersion: \(String(describing: platformVersion)), propagateTags: \(String(describing: propagateTags)), roleArn: \(String(describing: roleArn)), runningCount: \(String(describing: runningCount)), schedulingStrategy: \(String(describing: schedulingStrategy)), serviceArn: \(String(describing: serviceArn)), serviceName: \(String(describing: serviceName)), serviceRegistries: \(String(describing: serviceRegistries)), status: \(String(describing: status)), tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)), taskSets: \(String(describing: taskSets)))"}
}

/// <p>Details on a service within a cluster</p>
public struct Service: Equatable {
    /// <p>The capacity provider strategy associated with the service.</p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The Amazon Resource Name (ARN) of the cluster that hosts the service.</p>
    public let clusterArn: String?
    /// <p>The Unix timestamp for when the service was created.</p>
    public let createdAt: Date?
    /// <p>The principal that created the service.</p>
    public let createdBy: String?
    /// <p>Optional deployment parameters that control how many tasks run during the deployment
    /// 			and the ordering of stopping and starting tasks.</p>
    public let deploymentConfiguration: DeploymentConfiguration?
    /// <p>The deployment controller type the service is using. When using the DescribeServices
    /// 			API, this field is omitted if the service is using the <code>ECS</code> deployment
    /// 			controller type.</p>
    public let deploymentController: DeploymentController?
    /// <p>The current state of deployments for the service.</p>
    public let deployments: [Deployment]?
    /// <p>The desired number of instantiations of the task definition to keep running on the
    /// 			service. This value is specified when the service is created with <a>CreateService</a>, and it can be modified with <a>UpdateService</a>.</p>
    public let desiredCount: Int
    /// <p>Specifies whether to enable Amazon ECS managed tags for the tasks in the service. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-using-tags.html">Tagging Your Amazon ECS
    /// 				Resources</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let enableECSManagedTags: Bool
    /// <p>Whether or not the execute command functionality is enabled for the service. If
    /// 				<code>true</code>, the execute command functionality is enabled for all containers
    /// 			in tasks as part of the service.</p>
    public let enableExecuteCommand: Bool
    /// <p>The event stream for your service. A maximum of 100 of the latest events are
    /// 			displayed.</p>
    public let events: [ServiceEvent]?
    /// <p>The period of time, in seconds, that the Amazon ECS service scheduler ignores unhealthy
    /// 			Elastic Load Balancing target health checks after a task has first started.</p>
    public let healthCheckGracePeriodSeconds: Int?
    /// <p>The infrastructure on which your service is running. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS
    /// 				launch types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let launchType: LaunchType?
    /// <p>A list of Elastic Load Balancing load balancer objects, containing the load balancer name, the
    /// 			container name (as it appears in a container definition), and the container port to
    /// 			access from the load balancer.</p>
    public let loadBalancers: [LoadBalancer]?
    /// <p>The VPC subnet and security group configuration for tasks that receive their own
    ///             elastic network interface by using the <code>awsvpc</code> networking mode.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>The number of tasks in the cluster that are in the <code>PENDING</code> state.</p>
    public let pendingCount: Int
    /// <p>The placement constraints for the tasks in the service.</p>
    public let placementConstraints: [PlacementConstraint]?
    /// <p>The placement strategy that determines how tasks for the service are placed.</p>
    public let placementStrategy: [PlacementStrategy]?
    /// <p>The platform version on which to run your service. A platform version is only
    /// 			specified for tasks using the Fargate launch type. If one is not
    /// 			specified, the <code>LATEST</code> platform version is used by default. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate Platform
    /// 				Versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <p>Specifies whether to propagate the tags from the task definition or the service to the
    /// 			task. If no value is specified, the tags are not propagated.</p>
    public let propagateTags: PropagateTags?
    /// <p>The ARN of the IAM role associated with the service that allows the Amazon ECS container
    /// 			agent to register container instances with an Elastic Load Balancing load balancer.</p>
    public let roleArn: String?
    /// <p>The number of tasks in the cluster that are in the <code>RUNNING</code> state.</p>
    public let runningCount: Int
    /// <p>The scheduling strategy to use for the service. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs_services.html">Services</a>.</p>
    /// 		       <p>There are two service scheduler strategies available:</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>
    ///                   <code>REPLICA</code>-The replica scheduling strategy places and
    /// 					maintains the desired number of tasks across your cluster. By default, the
    /// 					service scheduler spreads tasks across Availability Zones. You can use task
    /// 					placement strategies and constraints to customize task placement
    /// 					decisions.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>
    ///                   <code>DAEMON</code>-The daemon scheduling strategy deploys exactly one
    /// 					task on each active container instance that meets all of the task placement
    /// 					constraints that you specify in your cluster. The service scheduler also
    /// 					evaluates the task placement constraints for running tasks and will stop tasks
    /// 					that do not meet the placement constraints.</p>
    /// 				           <note>
    /// 					             <p>Fargate tasks do not support the <code>DAEMON</code>
    /// 						scheduling strategy.</p>
    /// 				           </note>
    /// 			         </li>
    ///          </ul>
    public let schedulingStrategy: SchedulingStrategy?
    /// <p>The ARN that identifies the service. The ARN contains the <code>arn:aws:ecs</code> namespace, followed by the Region of the service, the AWS account ID of the service owner, the <code>service</code> namespace, and then the service name. For example, <code>arn:aws:ecs:region:012345678910:service/my-service</code>.</p>
    public let serviceArn: String?
    /// <p>The name of your service. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. Service names must be unique within
    /// 			a cluster, but you can have similarly named services in multiple clusters within a
    /// 			Region or across multiple Regions.</p>
    public let serviceName: String?
    /// <p>The details of the service discovery registries to assign to this service. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-discovery.html">Service
    /// 				Discovery</a>.</p>
    public let serviceRegistries: [ServiceRegistry]?
    /// <p>The status of the service. The valid values are <code>ACTIVE</code>,
    /// 				<code>DRAINING</code>, or <code>INACTIVE</code>.</p>
    public let status: String?
    /// <p>The metadata that you apply to the service to help you categorize and organize them.
    /// 			Each tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The task definition to use for tasks in the service. This value is specified when the
    /// 			service is created with <a>CreateService</a>, and it can be modified with
    /// 				<a>UpdateService</a>.</p>
    public let taskDefinition: String?
    /// <p>Information about a set of Amazon ECS tasks in either an AWS CodeDeploy or an <code>EXTERNAL</code>
    /// 			deployment. An Amazon ECS task set includes details such as the desired number of tasks, how
    /// 			many tasks are running, and whether the task set serves production traffic.</p>
    public let taskSets: [TaskSet]?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        clusterArn: String? = nil,
        createdAt: Date? = nil,
        createdBy: String? = nil,
        deploymentConfiguration: DeploymentConfiguration? = nil,
        deploymentController: DeploymentController? = nil,
        deployments: [Deployment]? = nil,
        desiredCount: Int = 0,
        enableECSManagedTags: Bool = false,
        enableExecuteCommand: Bool = false,
        events: [ServiceEvent]? = nil,
        healthCheckGracePeriodSeconds: Int? = nil,
        launchType: LaunchType? = nil,
        loadBalancers: [LoadBalancer]? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        pendingCount: Int = 0,
        placementConstraints: [PlacementConstraint]? = nil,
        placementStrategy: [PlacementStrategy]? = nil,
        platformVersion: String? = nil,
        propagateTags: PropagateTags? = nil,
        roleArn: String? = nil,
        runningCount: Int = 0,
        schedulingStrategy: SchedulingStrategy? = nil,
        serviceArn: String? = nil,
        serviceName: String? = nil,
        serviceRegistries: [ServiceRegistry]? = nil,
        status: String? = nil,
        tags: [Tag]? = nil,
        taskDefinition: String? = nil,
        taskSets: [TaskSet]? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.clusterArn = clusterArn
        self.createdAt = createdAt
        self.createdBy = createdBy
        self.deploymentConfiguration = deploymentConfiguration
        self.deploymentController = deploymentController
        self.deployments = deployments
        self.desiredCount = desiredCount
        self.enableECSManagedTags = enableECSManagedTags
        self.enableExecuteCommand = enableExecuteCommand
        self.events = events
        self.healthCheckGracePeriodSeconds = healthCheckGracePeriodSeconds
        self.launchType = launchType
        self.loadBalancers = loadBalancers
        self.networkConfiguration = networkConfiguration
        self.pendingCount = pendingCount
        self.placementConstraints = placementConstraints
        self.placementStrategy = placementStrategy
        self.platformVersion = platformVersion
        self.propagateTags = propagateTags
        self.roleArn = roleArn
        self.runningCount = runningCount
        self.schedulingStrategy = schedulingStrategy
        self.serviceArn = serviceArn
        self.serviceName = serviceName
        self.serviceRegistries = serviceRegistries
        self.status = status
        self.tags = tags
        self.taskDefinition = taskDefinition
        self.taskSets = taskSets
    }
}

extension ServiceEvent: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case createdAt
        case id
        case message
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let createdAt = createdAt {
            try encodeContainer.encode(createdAt.timeIntervalSince1970, forKey: .createdAt)
        }
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
        if let message = message {
            try encodeContainer.encode(message, forKey: .message)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
        let createdAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .createdAt)
        createdAt = createdAtDecoded
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ServiceEvent: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ServiceEvent(createdAt: \(String(describing: createdAt)), id: \(String(describing: id)), message: \(String(describing: message)))"}
}

/// <p>Details on an event associated with a service.</p>
public struct ServiceEvent: Equatable {
    /// <p>The Unix timestamp for when the event was triggered.</p>
    public let createdAt: Date?
    /// <p>The ID string of the event.</p>
    public let id: String?
    /// <p>The event message.</p>
    public let message: String?

    public init (
        createdAt: Date? = nil,
        id: String? = nil,
        message: String? = nil
    )
    {
        self.createdAt = createdAt
        self.id = id
        self.message = message
    }
}

public enum ServiceField {
    case tags
    case sdkUnknown(String)
}

extension ServiceField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ServiceField] {
        return [
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ServiceField(rawValue: rawValue) ?? ServiceField.sdkUnknown(rawValue)
    }
}

extension ServiceNotActiveException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ServiceNotActiveException(message: \(String(describing: message)))"}
}

extension ServiceNotActiveException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ServiceNotActiveExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified service is not active. You can't update a service that is inactive. If
/// 			you have previously deleted a service, you can re-create it with <a>CreateService</a>.</p>
public struct ServiceNotActiveException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ServiceNotActiveExceptionBody: Equatable {
    public let message: String?
}

extension ServiceNotActiveExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ServiceNotFoundException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ServiceNotFoundException(message: \(String(describing: message)))"}
}

extension ServiceNotFoundException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: ServiceNotFoundExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified service could not be found. You can view your available services with
/// 				<a>ListServices</a>. Amazon ECS services are cluster-specific and
/// 			Region-specific.</p>
public struct ServiceNotFoundException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct ServiceNotFoundExceptionBody: Equatable {
    public let message: String?
}

extension ServiceNotFoundExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension ServiceRegistry: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerName
        case containerPort
        case port
        case registryArn
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
        if let containerPort = containerPort {
            try encodeContainer.encode(containerPort, forKey: .containerPort)
        }
        if let port = port {
            try encodeContainer.encode(port, forKey: .port)
        }
        if let registryArn = registryArn {
            try encodeContainer.encode(registryArn, forKey: .registryArn)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let registryArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .registryArn)
        registryArn = registryArnDecoded
        let portDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .port)
        port = portDecoded
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let containerPortDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .containerPort)
        containerPort = containerPortDecoded
    }
}

extension ServiceRegistry: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ServiceRegistry(containerName: \(String(describing: containerName)), containerPort: \(String(describing: containerPort)), port: \(String(describing: port)), registryArn: \(String(describing: registryArn)))"}
}

/// <p>Details of the service registry.</p>
public struct ServiceRegistry: Equatable {
    /// <p>The container name value, already specified in the task definition, to be used for
    /// 			your service discovery service. If the task definition that your service task specifies
    /// 			uses the <code>bridge</code> or <code>host</code> network mode, you must specify a
    /// 				<code>containerName</code> and <code>containerPort</code> combination from the task
    /// 			definition. If the task definition that your service task specifies uses the
    /// 				<code>awsvpc</code> network mode and a type SRV DNS record is used, you must specify
    /// 			either a <code>containerName</code> and <code>containerPort</code> combination or a
    /// 				<code>port</code> value, but not both.</p>
    public let containerName: String?
    /// <p>The port value, already specified in the task definition, to be used for your service
    /// 			discovery service. If the task definition your service task specifies uses the
    /// 				<code>bridge</code> or <code>host</code> network mode, you must specify a
    /// 				<code>containerName</code> and <code>containerPort</code> combination from the task
    /// 			definition. If the task definition your service task specifies uses the
    /// 				<code>awsvpc</code> network mode and a type SRV DNS record is used, you must specify
    /// 			either a <code>containerName</code> and <code>containerPort</code> combination or a
    /// 				<code>port</code> value, but not both.</p>
    public let containerPort: Int?
    /// <p>The port value used if your service discovery service specified an SRV record. This
    /// 			field may be used if both the <code>awsvpc</code> network mode and SRV records are
    /// 			used.</p>
    public let port: Int?
    /// <p>The Amazon Resource Name (ARN) of the service registry. The currently supported service registry is
    /// 			AWS Cloud Map. For more information, see <a href="https://docs.aws.amazon.com/cloud-map/latest/api/API_CreateService.html">CreateService</a>.</p>
    public let registryArn: String?

    public init (
        containerName: String? = nil,
        containerPort: Int? = nil,
        port: Int? = nil,
        registryArn: String? = nil
    )
    {
        self.containerName = containerName
        self.containerPort = containerPort
        self.port = port
        self.registryArn = registryArn
    }
}

extension Session: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case sessionId
        case streamUrl
        case tokenValue
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let sessionId = sessionId {
            try encodeContainer.encode(sessionId, forKey: .sessionId)
        }
        if let streamUrl = streamUrl {
            try encodeContainer.encode(streamUrl, forKey: .streamUrl)
        }
        if let tokenValue = tokenValue {
            try encodeContainer.encode(tokenValue, forKey: .tokenValue)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let sessionIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .sessionId)
        sessionId = sessionIdDecoded
        let streamUrlDecoded = try containerValues.decodeIfPresent(String.self, forKey: .streamUrl)
        streamUrl = streamUrlDecoded
        let tokenValueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .tokenValue)
        tokenValue = tokenValueDecoded
    }
}

extension Session: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Session(sessionId: \(String(describing: sessionId)), streamUrl: \(String(describing: streamUrl)), tokenValue: \(String(describing: tokenValue)))"}
}

/// <p>The details of the execute command session.</p>
public struct Session: Equatable {
    /// <p>The ID of the execute command session.</p>
    public let sessionId: String?
    /// <p>A URL back to managed agent on the container that the SSM Session Manager client uses
    /// 			to send commands and receive output from the container.</p>
    public let streamUrl: String?
    /// <p>An encrypted token value containing session and caller information. Used to
    /// 			authenticate the connection to the container.</p>
    public let tokenValue: String?

    public init (
        sessionId: String? = nil,
        streamUrl: String? = nil,
        tokenValue: String? = nil
    )
    {
        self.sessionId = sessionId
        self.streamUrl = streamUrl
        self.tokenValue = tokenValue
    }
}

extension Setting: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case name
        case principalArn
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if let principalArn = principalArn {
            try encodeContainer.encode(principalArn, forKey: .principalArn)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(SettingName.self, forKey: .name)
        name = nameDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let principalArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .principalArn)
        principalArn = principalArnDecoded
    }
}

extension Setting: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Setting(name: \(String(describing: name)), principalArn: \(String(describing: principalArn)), value: \(String(describing: value)))"}
}

/// <p>The current account setting for a resource.</p>
public struct Setting: Equatable {
    /// <p>The Amazon ECS resource name.</p>
    public let name: SettingName?
    /// <p>The ARN of the principal, which can be an IAM user, IAM role, or the root user. If
    /// 			this field is omitted, the authenticated user is assumed.</p>
    public let principalArn: String?
    /// <p>Whether the account setting is enabled or disabled for the specified resource.</p>
    public let value: String?

    public init (
        name: SettingName? = nil,
        principalArn: String? = nil,
        value: String? = nil
    )
    {
        self.name = name
        self.principalArn = principalArn
        self.value = value
    }
}

public enum SettingName {
    case awsvpcTrunking
    case containerInsights
    case containerInstanceLongArnFormat
    case serviceLongArnFormat
    case taskLongArnFormat
    case sdkUnknown(String)
}

extension SettingName : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [SettingName] {
        return [
            .awsvpcTrunking,
            .containerInsights,
            .containerInstanceLongArnFormat,
            .serviceLongArnFormat,
            .taskLongArnFormat,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .awsvpcTrunking: return "awsvpcTrunking"
        case .containerInsights: return "containerInsights"
        case .containerInstanceLongArnFormat: return "containerInstanceLongArnFormat"
        case .serviceLongArnFormat: return "serviceLongArnFormat"
        case .taskLongArnFormat: return "taskLongArnFormat"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = SettingName(rawValue: rawValue) ?? SettingName.sdkUnknown(rawValue)
    }
}

public enum SortOrder {
    case asc
    case desc
    case sdkUnknown(String)
}

extension SortOrder : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [SortOrder] {
        return [
            .asc,
            .desc,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .asc: return "ASC"
        case .desc: return "DESC"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = SortOrder(rawValue: rawValue) ?? SortOrder.sdkUnknown(rawValue)
    }
}

public enum StabilityStatus {
    case stabilizing
    case steadyState
    case sdkUnknown(String)
}

extension StabilityStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [StabilityStatus] {
        return [
            .stabilizing,
            .steadyState,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .stabilizing: return "STABILIZING"
        case .steadyState: return "STEADY_STATE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = StabilityStatus(rawValue: rawValue) ?? StabilityStatus.sdkUnknown(rawValue)
    }
}

public struct StartTaskInputBodyMiddleware: Middleware {
    public let id: String = "StartTaskInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<StartTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<StartTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<StartTaskInput>
    public typealias MOutput = OperationOutput<StartTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<StartTaskOutputError>
}

extension StartTaskInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "StartTaskInput(cluster: \(String(describing: cluster)), containerInstances: \(String(describing: containerInstances)), enableECSManagedTags: \(String(describing: enableECSManagedTags)), enableExecuteCommand: \(String(describing: enableExecuteCommand)), group: \(String(describing: group)), networkConfiguration: \(String(describing: networkConfiguration)), overrides: \(String(describing: overrides)), propagateTags: \(String(describing: propagateTags)), referenceId: \(String(describing: referenceId)), startedBy: \(String(describing: startedBy)), tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension StartTaskInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstances
        case enableECSManagedTags
        case enableExecuteCommand
        case group
        case networkConfiguration
        case overrides
        case propagateTags
        case referenceId
        case startedBy
        case tags
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstances = containerInstances {
            var containerInstancesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containerInstances)
            for stringlist0 in containerInstances {
                try containerInstancesContainer.encode(stringlist0)
            }
        }
        if enableECSManagedTags != false {
            try encodeContainer.encode(enableECSManagedTags, forKey: .enableECSManagedTags)
        }
        if enableExecuteCommand != false {
            try encodeContainer.encode(enableExecuteCommand, forKey: .enableExecuteCommand)
        }
        if let group = group {
            try encodeContainer.encode(group, forKey: .group)
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if let overrides = overrides {
            try encodeContainer.encode(overrides, forKey: .overrides)
        }
        if let propagateTags = propagateTags {
            try encodeContainer.encode(propagateTags.rawValue, forKey: .propagateTags)
        }
        if let referenceId = referenceId {
            try encodeContainer.encode(referenceId, forKey: .referenceId)
        }
        if let startedBy = startedBy {
            try encodeContainer.encode(startedBy, forKey: .startedBy)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct StartTaskInputHeadersMiddleware: Middleware {
    public let id: String = "StartTaskInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<StartTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<StartTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<StartTaskInput>
    public typealias MOutput = OperationOutput<StartTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<StartTaskOutputError>
}

public struct StartTaskInputQueryItemMiddleware: Middleware {
    public let id: String = "StartTaskInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<StartTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<StartTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<StartTaskInput>
    public typealias MOutput = OperationOutput<StartTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<StartTaskOutputError>
}

public struct StartTaskInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster on which to start your task.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The container instance IDs or full ARN entries for the container instances on which
    /// 			you would like to place your task. You can specify up to 10 container instances.</p>
    public let containerInstances: [String]?
    /// <p>Specifies whether to enable Amazon ECS managed tags for the task. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-using-tags.html">Tagging Your Amazon ECS
    /// 				Resources</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let enableECSManagedTags: Bool
    /// <p>Whether or not the execute command functionality is enabled for the task. If
    /// 				<code>true</code>, this enables execute command functionality on all containers in
    /// 			the task.</p>
    public let enableExecuteCommand: Bool
    /// <p>The name of the task group to associate with the task. The default value is the family
    /// 			name of the task definition (for example, family:my-family-name).</p>
    public let group: String?
    /// <p>The VPC subnet and security group configuration for tasks that receive their own
    ///             elastic network interface by using the <code>awsvpc</code> networking mode.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>A list of container overrides in JSON format that specify the name of a container in
    /// 			the specified task definition and the overrides it should receive. You can override the
    /// 			default command for a container (that is specified in the task definition or Docker
    /// 			image) with a <code>command</code> override. You can also override existing environment
    /// 			variables (that are specified in the task definition or Docker image) on a container or
    /// 			add new environment variables to it with an <code>environment</code> override.</p>
    /// 		       <note>
    /// 			         <p>A total of 8192 characters are allowed for overrides. This limit includes the JSON
    /// 				formatting characters of the override structure.</p>
    /// 		       </note>
    public let overrides: TaskOverride?
    /// <p>Specifies whether to propagate the tags from the task definition or the service to the
    /// 			task. If no value is specified, the tags are not propagated.</p>
    public let propagateTags: PropagateTags?
    /// <p>The reference ID to use for the task.</p>
    public let referenceId: String?
    /// <p>An optional tag specified when a task is started. For example, if you automatically
    /// 			trigger a task to run a batch process job, you could apply a unique identifier for that
    /// 			job to your task with the <code>startedBy</code> parameter. You can then identify which
    /// 			tasks belong to that job by filtering the results of a <a>ListTasks</a> call
    /// 			with the <code>startedBy</code> value. Up to 36 letters (uppercase and lowercase),
    /// 			numbers, hyphens, and underscores are allowed.</p>
    /// 		       <p>If a task is started by an Amazon ECS service, then the <code>startedBy</code> parameter
    /// 			contains the deployment ID of the service that starts it.</p>
    public let startedBy: String?
    /// <p>The metadata that you apply to the task to help you categorize and organize them. Each
    /// 			tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The <code>family</code> and <code>revision</code> (<code>family:revision</code>) or
    /// 			full ARN of the task definition to start. If a <code>revision</code> is not specified,
    /// 			the latest <code>ACTIVE</code> revision is used.</p>
    public let taskDefinition: String?

    public init (
        cluster: String? = nil,
        containerInstances: [String]? = nil,
        enableECSManagedTags: Bool = false,
        enableExecuteCommand: Bool = false,
        group: String? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        overrides: TaskOverride? = nil,
        propagateTags: PropagateTags? = nil,
        referenceId: String? = nil,
        startedBy: String? = nil,
        tags: [Tag]? = nil,
        taskDefinition: String? = nil
    )
    {
        self.cluster = cluster
        self.containerInstances = containerInstances
        self.enableECSManagedTags = enableECSManagedTags
        self.enableExecuteCommand = enableExecuteCommand
        self.group = group
        self.networkConfiguration = networkConfiguration
        self.overrides = overrides
        self.propagateTags = propagateTags
        self.referenceId = referenceId
        self.startedBy = startedBy
        self.tags = tags
        self.taskDefinition = taskDefinition
    }
}

struct StartTaskInputBody: Equatable {
    public let cluster: String?
    public let containerInstances: [String]?
    public let enableECSManagedTags: Bool
    public let enableExecuteCommand: Bool
    public let group: String?
    public let networkConfiguration: NetworkConfiguration?
    public let overrides: TaskOverride?
    public let propagateTags: PropagateTags?
    public let referenceId: String?
    public let startedBy: String?
    public let tags: [Tag]?
    public let taskDefinition: String?
}

extension StartTaskInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstances
        case enableECSManagedTags
        case enableExecuteCommand
        case group
        case networkConfiguration
        case overrides
        case propagateTags
        case referenceId
        case startedBy
        case tags
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerInstancesContainer = try containerValues.decodeIfPresent([String?].self, forKey: .containerInstances)
        var containerInstancesDecoded0:[String]? = nil
        if let containerInstancesContainer = containerInstancesContainer {
            containerInstancesDecoded0 = [String]()
            for string0 in containerInstancesContainer {
                if let string0 = string0 {
                    containerInstancesDecoded0?.append(string0)
                }
            }
        }
        containerInstances = containerInstancesDecoded0
        let enableECSManagedTagsDecoded = try containerValues.decode(Bool.self, forKey: .enableECSManagedTags)
        enableECSManagedTags = enableECSManagedTagsDecoded
        let enableExecuteCommandDecoded = try containerValues.decode(Bool.self, forKey: .enableExecuteCommand)
        enableExecuteCommand = enableExecuteCommandDecoded
        let groupDecoded = try containerValues.decodeIfPresent(String.self, forKey: .group)
        group = groupDecoded
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let overridesDecoded = try containerValues.decodeIfPresent(TaskOverride.self, forKey: .overrides)
        overrides = overridesDecoded
        let propagateTagsDecoded = try containerValues.decodeIfPresent(PropagateTags.self, forKey: .propagateTags)
        propagateTags = propagateTagsDecoded
        let referenceIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .referenceId)
        referenceId = referenceIdDecoded
        let startedByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .startedBy)
        startedBy = startedByDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
    }
}

extension StartTaskOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension StartTaskOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum StartTaskOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension StartTaskOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "StartTaskOutputResponse(failures: \(String(describing: failures)), tasks: \(String(describing: tasks)))"}
}

extension StartTaskOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: StartTaskOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.failures = output.failures
            self.tasks = output.tasks
        } else {
            self.failures = nil
            self.tasks = nil
        }
    }
}

public struct StartTaskOutputResponse: Equatable {
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?
    /// <p>A full description of the tasks that were started. Each task that was successfully
    /// 			placed on your container instances is described.</p>
    public let tasks: [Task]?

    public init (
        failures: [Failure]? = nil,
        tasks: [Task]? = nil
    )
    {
        self.failures = failures
        self.tasks = tasks
    }
}

struct StartTaskOutputResponseBody: Equatable {
    public let tasks: [Task]?
    public let failures: [Failure]?
}

extension StartTaskOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case failures
        case tasks
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let tasksContainer = try containerValues.decodeIfPresent([Task?].self, forKey: .tasks)
        var tasksDecoded0:[Task]? = nil
        if let tasksContainer = tasksContainer {
            tasksDecoded0 = [Task]()
            for structure0 in tasksContainer {
                if let structure0 = structure0 {
                    tasksDecoded0?.append(structure0)
                }
            }
        }
        tasks = tasksDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

public struct StopTaskInputBodyMiddleware: Middleware {
    public let id: String = "StopTaskInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<StopTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<StopTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<StopTaskInput>
    public typealias MOutput = OperationOutput<StopTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<StopTaskOutputError>
}

extension StopTaskInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "StopTaskInput(cluster: \(String(describing: cluster)), reason: \(String(describing: reason)), task: \(String(describing: task)))"}
}

extension StopTaskInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case reason
        case task
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
        if let task = task {
            try encodeContainer.encode(task, forKey: .task)
        }
    }
}

public struct StopTaskInputHeadersMiddleware: Middleware {
    public let id: String = "StopTaskInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<StopTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<StopTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<StopTaskInput>
    public typealias MOutput = OperationOutput<StopTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<StopTaskOutputError>
}

public struct StopTaskInputQueryItemMiddleware: Middleware {
    public let id: String = "StopTaskInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<StopTaskInput>,
                  next: H) -> Swift.Result<OperationOutput<StopTaskOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<StopTaskInput>
    public typealias MOutput = OperationOutput<StopTaskOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<StopTaskOutputError>
}

public struct StopTaskInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the task to stop.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>An optional message specified when a task is stopped. For example, if you are using a
    /// 			custom scheduler, you can use this parameter to specify the reason for stopping the task
    /// 			here, and the message appears in subsequent <a>DescribeTasks</a> API
    /// 			operations on this task. Up to 255 characters are allowed in this message.</p>
    public let reason: String?
    /// <p>The task ID or full Amazon Resource Name (ARN) of the task to stop.</p>
    public let task: String?

    public init (
        cluster: String? = nil,
        reason: String? = nil,
        task: String? = nil
    )
    {
        self.cluster = cluster
        self.reason = reason
        self.task = task
    }
}

struct StopTaskInputBody: Equatable {
    public let cluster: String?
    public let task: String?
    public let reason: String?
}

extension StopTaskInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case reason
        case task
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let taskDecoded = try containerValues.decodeIfPresent(String.self, forKey: .task)
        task = taskDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
    }
}

extension StopTaskOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension StopTaskOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum StopTaskOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension StopTaskOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "StopTaskOutputResponse(task: \(String(describing: task)))"}
}

extension StopTaskOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: StopTaskOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.task = output.task
        } else {
            self.task = nil
        }
    }
}

public struct StopTaskOutputResponse: Equatable {
    /// <p>The task that was stopped.</p>
    public let task: Task?

    public init (
        task: Task? = nil
    )
    {
        self.task = task
    }
}

struct StopTaskOutputResponseBody: Equatable {
    public let task: Task?
}

extension StopTaskOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case task
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDecoded = try containerValues.decodeIfPresent(Task.self, forKey: .task)
        task = taskDecoded
    }
}

public struct SubmitAttachmentStateChangesInputBodyMiddleware: Middleware {
    public let id: String = "SubmitAttachmentStateChangesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitAttachmentStateChangesInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitAttachmentStateChangesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitAttachmentStateChangesInput>
    public typealias MOutput = OperationOutput<SubmitAttachmentStateChangesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitAttachmentStateChangesOutputError>
}

extension SubmitAttachmentStateChangesInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SubmitAttachmentStateChangesInput(attachments: \(String(describing: attachments)), cluster: \(String(describing: cluster)))"}
}

extension SubmitAttachmentStateChangesInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attachments
        case cluster
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attachments = attachments {
            var attachmentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attachments)
            for attachmentstatechanges0 in attachments {
                try attachmentsContainer.encode(attachmentstatechanges0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
    }
}

public struct SubmitAttachmentStateChangesInputHeadersMiddleware: Middleware {
    public let id: String = "SubmitAttachmentStateChangesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitAttachmentStateChangesInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitAttachmentStateChangesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitAttachmentStateChangesInput>
    public typealias MOutput = OperationOutput<SubmitAttachmentStateChangesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitAttachmentStateChangesOutputError>
}

public struct SubmitAttachmentStateChangesInputQueryItemMiddleware: Middleware {
    public let id: String = "SubmitAttachmentStateChangesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitAttachmentStateChangesInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitAttachmentStateChangesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitAttachmentStateChangesInput>
    public typealias MOutput = OperationOutput<SubmitAttachmentStateChangesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitAttachmentStateChangesOutputError>
}

public struct SubmitAttachmentStateChangesInput: Equatable {
    /// <p>Any attachments associated with the state change request.</p>
    public let attachments: [AttachmentStateChange]?
    /// <p>The short name or full ARN of the cluster that hosts the container instance the
    /// 			attachment belongs to.</p>
    public let cluster: String?

    public init (
        attachments: [AttachmentStateChange]? = nil,
        cluster: String? = nil
    )
    {
        self.attachments = attachments
        self.cluster = cluster
    }
}

struct SubmitAttachmentStateChangesInputBody: Equatable {
    public let cluster: String?
    public let attachments: [AttachmentStateChange]?
}

extension SubmitAttachmentStateChangesInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attachments
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let attachmentsContainer = try containerValues.decodeIfPresent([AttachmentStateChange?].self, forKey: .attachments)
        var attachmentsDecoded0:[AttachmentStateChange]? = nil
        if let attachmentsContainer = attachmentsContainer {
            attachmentsDecoded0 = [AttachmentStateChange]()
            for structure0 in attachmentsContainer {
                if let structure0 = structure0 {
                    attachmentsDecoded0?.append(structure0)
                }
            }
        }
        attachments = attachmentsDecoded0
    }
}

extension SubmitAttachmentStateChangesOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension SubmitAttachmentStateChangesOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum SubmitAttachmentStateChangesOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension SubmitAttachmentStateChangesOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SubmitAttachmentStateChangesOutputResponse(acknowledgment: \(String(describing: acknowledgment)))"}
}

extension SubmitAttachmentStateChangesOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: SubmitAttachmentStateChangesOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.acknowledgment = output.acknowledgment
        } else {
            self.acknowledgment = nil
        }
    }
}

public struct SubmitAttachmentStateChangesOutputResponse: Equatable {
    /// <p>Acknowledgement of the state change.</p>
    public let acknowledgment: String?

    public init (
        acknowledgment: String? = nil
    )
    {
        self.acknowledgment = acknowledgment
    }
}

struct SubmitAttachmentStateChangesOutputResponseBody: Equatable {
    public let acknowledgment: String?
}

extension SubmitAttachmentStateChangesOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case acknowledgment
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let acknowledgmentDecoded = try containerValues.decodeIfPresent(String.self, forKey: .acknowledgment)
        acknowledgment = acknowledgmentDecoded
    }
}

public struct SubmitContainerStateChangeInputBodyMiddleware: Middleware {
    public let id: String = "SubmitContainerStateChangeInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitContainerStateChangeInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitContainerStateChangeOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitContainerStateChangeInput>
    public typealias MOutput = OperationOutput<SubmitContainerStateChangeOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitContainerStateChangeOutputError>
}

extension SubmitContainerStateChangeInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SubmitContainerStateChangeInput(cluster: \(String(describing: cluster)), containerName: \(String(describing: containerName)), exitCode: \(String(describing: exitCode)), networkBindings: \(String(describing: networkBindings)), reason: \(String(describing: reason)), runtimeId: \(String(describing: runtimeId)), status: \(String(describing: status)), task: \(String(describing: task)))"}
}

extension SubmitContainerStateChangeInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerName
        case exitCode
        case networkBindings
        case reason
        case runtimeId
        case status
        case task
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerName = containerName {
            try encodeContainer.encode(containerName, forKey: .containerName)
        }
        if let exitCode = exitCode {
            try encodeContainer.encode(exitCode, forKey: .exitCode)
        }
        if let networkBindings = networkBindings {
            var networkBindingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .networkBindings)
            for networkbindings0 in networkBindings {
                try networkBindingsContainer.encode(networkbindings0)
            }
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
        if let runtimeId = runtimeId {
            try encodeContainer.encode(runtimeId, forKey: .runtimeId)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let task = task {
            try encodeContainer.encode(task, forKey: .task)
        }
    }
}

public struct SubmitContainerStateChangeInputHeadersMiddleware: Middleware {
    public let id: String = "SubmitContainerStateChangeInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitContainerStateChangeInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitContainerStateChangeOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitContainerStateChangeInput>
    public typealias MOutput = OperationOutput<SubmitContainerStateChangeOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitContainerStateChangeOutputError>
}

public struct SubmitContainerStateChangeInputQueryItemMiddleware: Middleware {
    public let id: String = "SubmitContainerStateChangeInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitContainerStateChangeInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitContainerStateChangeOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitContainerStateChangeInput>
    public typealias MOutput = OperationOutput<SubmitContainerStateChangeOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitContainerStateChangeOutputError>
}

public struct SubmitContainerStateChangeInput: Equatable {
    /// <p>The short name or full ARN of the cluster that hosts the container.</p>
    public let cluster: String?
    /// <p>The name of the container.</p>
    public let containerName: String?
    /// <p>The exit code returned for the state change request.</p>
    public let exitCode: Int?
    /// <p>The network bindings of the container.</p>
    public let networkBindings: [NetworkBinding]?
    /// <p>The reason for the state change request.</p>
    public let reason: String?
    /// <p>The ID of the Docker container.</p>
    public let runtimeId: String?
    /// <p>The status of the state change request.</p>
    public let status: String?
    /// <p>The task ID or full Amazon Resource Name (ARN) of the task that hosts the container.</p>
    public let task: String?

    public init (
        cluster: String? = nil,
        containerName: String? = nil,
        exitCode: Int? = nil,
        networkBindings: [NetworkBinding]? = nil,
        reason: String? = nil,
        runtimeId: String? = nil,
        status: String? = nil,
        task: String? = nil
    )
    {
        self.cluster = cluster
        self.containerName = containerName
        self.exitCode = exitCode
        self.networkBindings = networkBindings
        self.reason = reason
        self.runtimeId = runtimeId
        self.status = status
        self.task = task
    }
}

struct SubmitContainerStateChangeInputBody: Equatable {
    public let cluster: String?
    public let task: String?
    public let containerName: String?
    public let runtimeId: String?
    public let status: String?
    public let exitCode: Int?
    public let reason: String?
    public let networkBindings: [NetworkBinding]?
}

extension SubmitContainerStateChangeInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerName
        case exitCode
        case networkBindings
        case reason
        case runtimeId
        case status
        case task
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let taskDecoded = try containerValues.decodeIfPresent(String.self, forKey: .task)
        task = taskDecoded
        let containerNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerName)
        containerName = containerNameDecoded
        let runtimeIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .runtimeId)
        runtimeId = runtimeIdDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let exitCodeDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .exitCode)
        exitCode = exitCodeDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
        let networkBindingsContainer = try containerValues.decodeIfPresent([NetworkBinding?].self, forKey: .networkBindings)
        var networkBindingsDecoded0:[NetworkBinding]? = nil
        if let networkBindingsContainer = networkBindingsContainer {
            networkBindingsDecoded0 = [NetworkBinding]()
            for structure0 in networkBindingsContainer {
                if let structure0 = structure0 {
                    networkBindingsDecoded0?.append(structure0)
                }
            }
        }
        networkBindings = networkBindingsDecoded0
    }
}

extension SubmitContainerStateChangeOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension SubmitContainerStateChangeOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum SubmitContainerStateChangeOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension SubmitContainerStateChangeOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SubmitContainerStateChangeOutputResponse(acknowledgment: \(String(describing: acknowledgment)))"}
}

extension SubmitContainerStateChangeOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: SubmitContainerStateChangeOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.acknowledgment = output.acknowledgment
        } else {
            self.acknowledgment = nil
        }
    }
}

public struct SubmitContainerStateChangeOutputResponse: Equatable {
    /// <p>Acknowledgement of the state change.</p>
    public let acknowledgment: String?

    public init (
        acknowledgment: String? = nil
    )
    {
        self.acknowledgment = acknowledgment
    }
}

struct SubmitContainerStateChangeOutputResponseBody: Equatable {
    public let acknowledgment: String?
}

extension SubmitContainerStateChangeOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case acknowledgment
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let acknowledgmentDecoded = try containerValues.decodeIfPresent(String.self, forKey: .acknowledgment)
        acknowledgment = acknowledgmentDecoded
    }
}

public struct SubmitTaskStateChangeInputBodyMiddleware: Middleware {
    public let id: String = "SubmitTaskStateChangeInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitTaskStateChangeInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitTaskStateChangeOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitTaskStateChangeInput>
    public typealias MOutput = OperationOutput<SubmitTaskStateChangeOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitTaskStateChangeOutputError>
}

extension SubmitTaskStateChangeInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SubmitTaskStateChangeInput(attachments: \(String(describing: attachments)), cluster: \(String(describing: cluster)), containers: \(String(describing: containers)), executionStoppedAt: \(String(describing: executionStoppedAt)), managedAgents: \(String(describing: managedAgents)), pullStartedAt: \(String(describing: pullStartedAt)), pullStoppedAt: \(String(describing: pullStoppedAt)), reason: \(String(describing: reason)), status: \(String(describing: status)), task: \(String(describing: task)))"}
}

extension SubmitTaskStateChangeInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attachments
        case cluster
        case containers
        case executionStoppedAt
        case managedAgents
        case pullStartedAt
        case pullStoppedAt
        case reason
        case status
        case task
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attachments = attachments {
            var attachmentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attachments)
            for attachmentstatechanges0 in attachments {
                try attachmentsContainer.encode(attachmentstatechanges0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containers = containers {
            var containersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containers)
            for containerstatechanges0 in containers {
                try containersContainer.encode(containerstatechanges0)
            }
        }
        if let executionStoppedAt = executionStoppedAt {
            try encodeContainer.encode(executionStoppedAt.timeIntervalSince1970, forKey: .executionStoppedAt)
        }
        if let managedAgents = managedAgents {
            var managedAgentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .managedAgents)
            for managedagentstatechanges0 in managedAgents {
                try managedAgentsContainer.encode(managedagentstatechanges0)
            }
        }
        if let pullStartedAt = pullStartedAt {
            try encodeContainer.encode(pullStartedAt.timeIntervalSince1970, forKey: .pullStartedAt)
        }
        if let pullStoppedAt = pullStoppedAt {
            try encodeContainer.encode(pullStoppedAt.timeIntervalSince1970, forKey: .pullStoppedAt)
        }
        if let reason = reason {
            try encodeContainer.encode(reason, forKey: .reason)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let task = task {
            try encodeContainer.encode(task, forKey: .task)
        }
    }
}

public struct SubmitTaskStateChangeInputHeadersMiddleware: Middleware {
    public let id: String = "SubmitTaskStateChangeInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitTaskStateChangeInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitTaskStateChangeOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitTaskStateChangeInput>
    public typealias MOutput = OperationOutput<SubmitTaskStateChangeOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitTaskStateChangeOutputError>
}

public struct SubmitTaskStateChangeInputQueryItemMiddleware: Middleware {
    public let id: String = "SubmitTaskStateChangeInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SubmitTaskStateChangeInput>,
                  next: H) -> Swift.Result<OperationOutput<SubmitTaskStateChangeOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SubmitTaskStateChangeInput>
    public typealias MOutput = OperationOutput<SubmitTaskStateChangeOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SubmitTaskStateChangeOutputError>
}

public struct SubmitTaskStateChangeInput: Equatable {
    /// <p>Any attachments associated with the state change request.</p>
    public let attachments: [AttachmentStateChange]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the task.</p>
    public let cluster: String?
    /// <p>Any containers associated with the state change request.</p>
    public let containers: [ContainerStateChange]?
    /// <p>The Unix timestamp for when the task execution stopped.</p>
    public let executionStoppedAt: Date?
    /// <p>The details for the managed agent associated with the task.</p>
    public let managedAgents: [ManagedAgentStateChange]?
    /// <p>The Unix timestamp for when the container image pull began.</p>
    public let pullStartedAt: Date?
    /// <p>The Unix timestamp for when the container image pull completed.</p>
    public let pullStoppedAt: Date?
    /// <p>The reason for the state change request.</p>
    public let reason: String?
    /// <p>The status of the state change request.</p>
    public let status: String?
    /// <p>The task ID or full ARN of the task in the state change request.</p>
    public let task: String?

    public init (
        attachments: [AttachmentStateChange]? = nil,
        cluster: String? = nil,
        containers: [ContainerStateChange]? = nil,
        executionStoppedAt: Date? = nil,
        managedAgents: [ManagedAgentStateChange]? = nil,
        pullStartedAt: Date? = nil,
        pullStoppedAt: Date? = nil,
        reason: String? = nil,
        status: String? = nil,
        task: String? = nil
    )
    {
        self.attachments = attachments
        self.cluster = cluster
        self.containers = containers
        self.executionStoppedAt = executionStoppedAt
        self.managedAgents = managedAgents
        self.pullStartedAt = pullStartedAt
        self.pullStoppedAt = pullStoppedAt
        self.reason = reason
        self.status = status
        self.task = task
    }
}

struct SubmitTaskStateChangeInputBody: Equatable {
    public let cluster: String?
    public let task: String?
    public let status: String?
    public let reason: String?
    public let containers: [ContainerStateChange]?
    public let attachments: [AttachmentStateChange]?
    public let managedAgents: [ManagedAgentStateChange]?
    public let pullStartedAt: Date?
    public let pullStoppedAt: Date?
    public let executionStoppedAt: Date?
}

extension SubmitTaskStateChangeInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case attachments
        case cluster
        case containers
        case executionStoppedAt
        case managedAgents
        case pullStartedAt
        case pullStoppedAt
        case reason
        case status
        case task
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let taskDecoded = try containerValues.decodeIfPresent(String.self, forKey: .task)
        task = taskDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let reasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reason)
        reason = reasonDecoded
        let containersContainer = try containerValues.decodeIfPresent([ContainerStateChange?].self, forKey: .containers)
        var containersDecoded0:[ContainerStateChange]? = nil
        if let containersContainer = containersContainer {
            containersDecoded0 = [ContainerStateChange]()
            for structure0 in containersContainer {
                if let structure0 = structure0 {
                    containersDecoded0?.append(structure0)
                }
            }
        }
        containers = containersDecoded0
        let attachmentsContainer = try containerValues.decodeIfPresent([AttachmentStateChange?].self, forKey: .attachments)
        var attachmentsDecoded0:[AttachmentStateChange]? = nil
        if let attachmentsContainer = attachmentsContainer {
            attachmentsDecoded0 = [AttachmentStateChange]()
            for structure0 in attachmentsContainer {
                if let structure0 = structure0 {
                    attachmentsDecoded0?.append(structure0)
                }
            }
        }
        attachments = attachmentsDecoded0
        let managedAgentsContainer = try containerValues.decodeIfPresent([ManagedAgentStateChange?].self, forKey: .managedAgents)
        var managedAgentsDecoded0:[ManagedAgentStateChange]? = nil
        if let managedAgentsContainer = managedAgentsContainer {
            managedAgentsDecoded0 = [ManagedAgentStateChange]()
            for structure0 in managedAgentsContainer {
                if let structure0 = structure0 {
                    managedAgentsDecoded0?.append(structure0)
                }
            }
        }
        managedAgents = managedAgentsDecoded0
        let pullStartedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .pullStartedAt)
        pullStartedAt = pullStartedAtDecoded
        let pullStoppedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .pullStoppedAt)
        pullStoppedAt = pullStoppedAtDecoded
        let executionStoppedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .executionStoppedAt)
        executionStoppedAt = executionStoppedAtDecoded
    }
}

extension SubmitTaskStateChangeOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension SubmitTaskStateChangeOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum SubmitTaskStateChangeOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension SubmitTaskStateChangeOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SubmitTaskStateChangeOutputResponse(acknowledgment: \(String(describing: acknowledgment)))"}
}

extension SubmitTaskStateChangeOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: SubmitTaskStateChangeOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.acknowledgment = output.acknowledgment
        } else {
            self.acknowledgment = nil
        }
    }
}

public struct SubmitTaskStateChangeOutputResponse: Equatable {
    /// <p>Acknowledgement of the state change.</p>
    public let acknowledgment: String?

    public init (
        acknowledgment: String? = nil
    )
    {
        self.acknowledgment = acknowledgment
    }
}

struct SubmitTaskStateChangeOutputResponseBody: Equatable {
    public let acknowledgment: String?
}

extension SubmitTaskStateChangeOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case acknowledgment
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let acknowledgmentDecoded = try containerValues.decodeIfPresent(String.self, forKey: .acknowledgment)
        acknowledgment = acknowledgmentDecoded
    }
}

extension SystemControl: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case namespace
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let namespace = namespace {
            try encodeContainer.encode(namespace, forKey: .namespace)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let namespaceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .namespace)
        namespace = namespaceDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
    }
}

extension SystemControl: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SystemControl(namespace: \(String(describing: namespace)), value: \(String(describing: value)))"}
}

/// <p>A list of namespaced kernel parameters to set in the container. This parameter maps to
/// 				<code>Sysctls</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the
/// 			<a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--sysctl</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>.</p>
/// 		       <p>It is not recommended that you specify network-related <code>systemControls</code>
/// 			parameters for multiple containers in a single task that also uses either the
/// 				<code>awsvpc</code> or <code>host</code> network mode for the following
/// 			reasons:</p>
/// 		       <ul>
///             <li>
/// 				           <p>For tasks that use the <code>awsvpc</code> network mode, if you set
/// 						<code>systemControls</code> for any container, it applies to all containers
/// 					in the task. If you set different <code>systemControls</code> for multiple
/// 					containers in a single task, the container that is started last determines which
/// 						<code>systemControls</code> take effect.</p>
/// 			         </li>
///             <li>
/// 				           <p>For tasks that use the <code>host</code> network mode, the
/// 						<code>systemControls</code> parameter applies to the container instance's
/// 					kernel parameter as well as that of all containers of any tasks running on that
/// 					container instance.</p>
/// 			         </li>
///          </ul>
public struct SystemControl: Equatable {
    /// <p>The namespaced kernel parameter for which to set a <code>value</code>.</p>
    public let namespace: String?
    /// <p>The value for the namespaced kernel parameter specified in
    /// 			<code>namespace</code>.</p>
    public let value: String?

    public init (
        namespace: String? = nil,
        value: String? = nil
    )
    {
        self.namespace = namespace
        self.value = value
    }
}

extension Tag: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case key
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let key = key {
            try encodeContainer.encode(key, forKey: .key)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let keyDecoded = try containerValues.decodeIfPresent(String.self, forKey: .key)
        key = keyDecoded
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
    }
}

extension Tag: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Tag(key: \(String(describing: key)), value: \(String(describing: value)))"}
}

/// <p>The metadata that you apply to a resource to help you categorize and organize them.
/// 			Each tag consists of a key and an optional value, both of which you define.</p>
/// 		       <p>The following basic restrictions apply to tags:</p>
///         <ul>
///             <li>
///                 <p>Maximum number of tags per resource - 50</p>
///             </li>
///             <li>
///                 <p>For each resource, each tag key must be unique, and each tag key can have only
///                     one value.</p>
///             </li>
///             <li>
///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
///             </li>
///             <li>
///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
///             </li>
///             <li>
///                 <p>If your tagging schema is used across multiple services and resources,
///                     remember that other services may have restrictions on allowed characters.
///                     Generally allowed characters are: letters, numbers, and spaces representable in
///                     UTF-8, and the following characters: + - = . _ : / @.</p>
///             </li>
///             <li>
///                 <p>Tag keys and values are case-sensitive.</p>
///             </li>
///             <li>
///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
///                     combination of such as a prefix for either keys or values as it is reserved for
///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
///                     this prefix do not count against your tags per resource limit.</p>
///             </li>
///          </ul>
public struct Tag: Equatable {
    /// <p>One part of a key-value pair that make up a tag. A <code>key</code> is a general label
    /// 			that acts like a category for more specific tag values.</p>
    public let key: String?
    /// <p>The optional part of a key-value pair that make up a tag. A <code>value</code> acts as
    /// 			a descriptor within a tag category (key).</p>
    public let value: String?

    public init (
        key: String? = nil,
        value: String? = nil
    )
    {
        self.key = key
        self.value = value
    }
}

public struct TagResourceInputBodyMiddleware: Middleware {
    public let id: String = "TagResourceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<TagResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<TagResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<TagResourceInput>
    public typealias MOutput = OperationOutput<TagResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<TagResourceOutputError>
}

extension TagResourceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TagResourceInput(resourceArn: \(String(describing: resourceArn)), tags: \(String(describing: tags)))"}
}

extension TagResourceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case resourceArn
        case tags
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let resourceArn = resourceArn {
            try encodeContainer.encode(resourceArn, forKey: .resourceArn)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
    }
}

public struct TagResourceInputHeadersMiddleware: Middleware {
    public let id: String = "TagResourceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<TagResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<TagResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<TagResourceInput>
    public typealias MOutput = OperationOutput<TagResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<TagResourceOutputError>
}

public struct TagResourceInputQueryItemMiddleware: Middleware {
    public let id: String = "TagResourceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<TagResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<TagResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<TagResourceInput>
    public typealias MOutput = OperationOutput<TagResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<TagResourceOutputError>
}

public struct TagResourceInput: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the resource to which to add tags. Currently, the supported resources
    /// 			are Amazon ECS capacity providers, tasks, services, task definitions, clusters, and container
    /// 			instances.</p>
    public let resourceArn: String?
    /// <p>The tags to add to the resource. A tag is an array of key-value pairs.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?

    public init (
        resourceArn: String? = nil,
        tags: [Tag]? = nil
    )
    {
        self.resourceArn = resourceArn
        self.tags = tags
    }
}

struct TagResourceInputBody: Equatable {
    public let resourceArn: String?
    public let tags: [Tag]?
}

extension TagResourceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case resourceArn
        case tags
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let resourceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .resourceArn)
        resourceArn = resourceArnDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension TagResourceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension TagResourceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ResourceNotFoundException" : self = .resourceNotFoundException(try ResourceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum TagResourceOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case resourceNotFoundException(ResourceNotFoundException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension TagResourceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TagResourceOutputResponse()"}
}

extension TagResourceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
    }
}

public struct TagResourceOutputResponse: Equatable {

    public init() {}
}

struct TagResourceOutputResponseBody: Equatable {
}

extension TagResourceOutputResponseBody: Decodable {

    public init (from decoder: Decoder) throws {
    }
}

extension TargetNotConnectedException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TargetNotConnectedException(message: \(String(describing: message)))"}
}

extension TargetNotConnectedException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: TargetNotConnectedExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The target container is not properly configured with the execute command agent or the
/// 			container is no longer active or running.</p>
public struct TargetNotConnectedException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct TargetNotConnectedExceptionBody: Equatable {
    public let message: String?
}

extension TargetNotConnectedExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension TargetNotFoundException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TargetNotFoundException(message: \(String(describing: message)))"}
}

extension TargetNotFoundException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: TargetNotFoundExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified target could not be found. You can view your available container
/// 			instances with <a>ListContainerInstances</a>. Amazon ECS container instances are
/// 			cluster-specific and Region-specific.</p>
public struct TargetNotFoundException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct TargetNotFoundExceptionBody: Equatable {
    public let message: String?
}

extension TargetNotFoundExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public enum TargetType {
    case containerInstance
    case sdkUnknown(String)
}

extension TargetType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TargetType] {
        return [
            .containerInstance,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .containerInstance: return "container-instance"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TargetType(rawValue: rawValue) ?? TargetType.sdkUnknown(rawValue)
    }
}

extension Task: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attachments
        case attributes
        case availabilityZone
        case capacityProviderName
        case clusterArn
        case connectivity
        case connectivityAt
        case containerInstanceArn
        case containers
        case cpu
        case createdAt
        case desiredStatus
        case enableExecuteCommand
        case ephemeralStorage
        case executionStoppedAt
        case group
        case healthStatus
        case inferenceAccelerators
        case lastStatus
        case launchType
        case memory
        case overrides
        case platformVersion
        case pullStartedAt
        case pullStoppedAt
        case startedAt
        case startedBy
        case stopCode
        case stoppedAt
        case stoppedReason
        case stoppingAt
        case tags
        case taskArn
        case taskDefinitionArn
        case version
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let attachments = attachments {
            var attachmentsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attachments)
            for attachments0 in attachments {
                try attachmentsContainer.encode(attachments0)
            }
        }
        if let attributes = attributes {
            var attributesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .attributes)
            for attributes0 in attributes {
                try attributesContainer.encode(attributes0)
            }
        }
        if let availabilityZone = availabilityZone {
            try encodeContainer.encode(availabilityZone, forKey: .availabilityZone)
        }
        if let capacityProviderName = capacityProviderName {
            try encodeContainer.encode(capacityProviderName, forKey: .capacityProviderName)
        }
        if let clusterArn = clusterArn {
            try encodeContainer.encode(clusterArn, forKey: .clusterArn)
        }
        if let connectivity = connectivity {
            try encodeContainer.encode(connectivity.rawValue, forKey: .connectivity)
        }
        if let connectivityAt = connectivityAt {
            try encodeContainer.encode(connectivityAt.timeIntervalSince1970, forKey: .connectivityAt)
        }
        if let containerInstanceArn = containerInstanceArn {
            try encodeContainer.encode(containerInstanceArn, forKey: .containerInstanceArn)
        }
        if let containers = containers {
            var containersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containers)
            for containers0 in containers {
                try containersContainer.encode(containers0)
            }
        }
        if let cpu = cpu {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let createdAt = createdAt {
            try encodeContainer.encode(createdAt.timeIntervalSince1970, forKey: .createdAt)
        }
        if let desiredStatus = desiredStatus {
            try encodeContainer.encode(desiredStatus, forKey: .desiredStatus)
        }
        if enableExecuteCommand != false {
            try encodeContainer.encode(enableExecuteCommand, forKey: .enableExecuteCommand)
        }
        if let ephemeralStorage = ephemeralStorage {
            try encodeContainer.encode(ephemeralStorage, forKey: .ephemeralStorage)
        }
        if let executionStoppedAt = executionStoppedAt {
            try encodeContainer.encode(executionStoppedAt.timeIntervalSince1970, forKey: .executionStoppedAt)
        }
        if let group = group {
            try encodeContainer.encode(group, forKey: .group)
        }
        if let healthStatus = healthStatus {
            try encodeContainer.encode(healthStatus.rawValue, forKey: .healthStatus)
        }
        if let inferenceAccelerators = inferenceAccelerators {
            var inferenceAcceleratorsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .inferenceAccelerators)
            for inferenceaccelerators0 in inferenceAccelerators {
                try inferenceAcceleratorsContainer.encode(inferenceaccelerators0)
            }
        }
        if let lastStatus = lastStatus {
            try encodeContainer.encode(lastStatus, forKey: .lastStatus)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let overrides = overrides {
            try encodeContainer.encode(overrides, forKey: .overrides)
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let pullStartedAt = pullStartedAt {
            try encodeContainer.encode(pullStartedAt.timeIntervalSince1970, forKey: .pullStartedAt)
        }
        if let pullStoppedAt = pullStoppedAt {
            try encodeContainer.encode(pullStoppedAt.timeIntervalSince1970, forKey: .pullStoppedAt)
        }
        if let startedAt = startedAt {
            try encodeContainer.encode(startedAt.timeIntervalSince1970, forKey: .startedAt)
        }
        if let startedBy = startedBy {
            try encodeContainer.encode(startedBy, forKey: .startedBy)
        }
        if let stopCode = stopCode {
            try encodeContainer.encode(stopCode.rawValue, forKey: .stopCode)
        }
        if let stoppedAt = stoppedAt {
            try encodeContainer.encode(stoppedAt.timeIntervalSince1970, forKey: .stoppedAt)
        }
        if let stoppedReason = stoppedReason {
            try encodeContainer.encode(stoppedReason, forKey: .stoppedReason)
        }
        if let stoppingAt = stoppingAt {
            try encodeContainer.encode(stoppingAt.timeIntervalSince1970, forKey: .stoppingAt)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskArn = taskArn {
            try encodeContainer.encode(taskArn, forKey: .taskArn)
        }
        if let taskDefinitionArn = taskDefinitionArn {
            try encodeContainer.encode(taskDefinitionArn, forKey: .taskDefinitionArn)
        }
        if version != 0 {
            try encodeContainer.encode(version, forKey: .version)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let attachmentsContainer = try containerValues.decodeIfPresent([Attachment?].self, forKey: .attachments)
        var attachmentsDecoded0:[Attachment]? = nil
        if let attachmentsContainer = attachmentsContainer {
            attachmentsDecoded0 = [Attachment]()
            for structure0 in attachmentsContainer {
                if let structure0 = structure0 {
                    attachmentsDecoded0?.append(structure0)
                }
            }
        }
        attachments = attachmentsDecoded0
        let attributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .attributes)
        var attributesDecoded0:[Attribute]? = nil
        if let attributesContainer = attributesContainer {
            attributesDecoded0 = [Attribute]()
            for structure0 in attributesContainer {
                if let structure0 = structure0 {
                    attributesDecoded0?.append(structure0)
                }
            }
        }
        attributes = attributesDecoded0
        let availabilityZoneDecoded = try containerValues.decodeIfPresent(String.self, forKey: .availabilityZone)
        availabilityZone = availabilityZoneDecoded
        let capacityProviderNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .capacityProviderName)
        capacityProviderName = capacityProviderNameDecoded
        let clusterArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterArn)
        clusterArn = clusterArnDecoded
        let connectivityDecoded = try containerValues.decodeIfPresent(Connectivity.self, forKey: .connectivity)
        connectivity = connectivityDecoded
        let connectivityAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .connectivityAt)
        connectivityAt = connectivityAtDecoded
        let containerInstanceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstanceArn)
        containerInstanceArn = containerInstanceArnDecoded
        let containersContainer = try containerValues.decodeIfPresent([Container?].self, forKey: .containers)
        var containersDecoded0:[Container]? = nil
        if let containersContainer = containersContainer {
            containersDecoded0 = [Container]()
            for structure0 in containersContainer {
                if let structure0 = structure0 {
                    containersDecoded0?.append(structure0)
                }
            }
        }
        containers = containersDecoded0
        let cpuDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cpu)
        cpu = cpuDecoded
        let createdAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .createdAt)
        createdAt = createdAtDecoded
        let desiredStatusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .desiredStatus)
        desiredStatus = desiredStatusDecoded
        let enableExecuteCommandDecoded = try containerValues.decode(Bool.self, forKey: .enableExecuteCommand)
        enableExecuteCommand = enableExecuteCommandDecoded
        let executionStoppedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .executionStoppedAt)
        executionStoppedAt = executionStoppedAtDecoded
        let groupDecoded = try containerValues.decodeIfPresent(String.self, forKey: .group)
        group = groupDecoded
        let healthStatusDecoded = try containerValues.decodeIfPresent(HealthStatus.self, forKey: .healthStatus)
        healthStatus = healthStatusDecoded
        let inferenceAcceleratorsContainer = try containerValues.decodeIfPresent([InferenceAccelerator?].self, forKey: .inferenceAccelerators)
        var inferenceAcceleratorsDecoded0:[InferenceAccelerator]? = nil
        if let inferenceAcceleratorsContainer = inferenceAcceleratorsContainer {
            inferenceAcceleratorsDecoded0 = [InferenceAccelerator]()
            for structure0 in inferenceAcceleratorsContainer {
                if let structure0 = structure0 {
                    inferenceAcceleratorsDecoded0?.append(structure0)
                }
            }
        }
        inferenceAccelerators = inferenceAcceleratorsDecoded0
        let lastStatusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .lastStatus)
        lastStatus = lastStatusDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .memory)
        memory = memoryDecoded
        let overridesDecoded = try containerValues.decodeIfPresent(TaskOverride.self, forKey: .overrides)
        overrides = overridesDecoded
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let pullStartedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .pullStartedAt)
        pullStartedAt = pullStartedAtDecoded
        let pullStoppedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .pullStoppedAt)
        pullStoppedAt = pullStoppedAtDecoded
        let startedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .startedAt)
        startedAt = startedAtDecoded
        let startedByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .startedBy)
        startedBy = startedByDecoded
        let stopCodeDecoded = try containerValues.decodeIfPresent(TaskStopCode.self, forKey: .stopCode)
        stopCode = stopCodeDecoded
        let stoppedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .stoppedAt)
        stoppedAt = stoppedAtDecoded
        let stoppedReasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .stoppedReason)
        stoppedReason = stoppedReasonDecoded
        let stoppingAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .stoppingAt)
        stoppingAt = stoppingAtDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
        let taskArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskArn)
        taskArn = taskArnDecoded
        let taskDefinitionArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinitionArn)
        taskDefinitionArn = taskDefinitionArnDecoded
        let versionDecoded = try containerValues.decode(Int.self, forKey: .version)
        version = versionDecoded
        let ephemeralStorageDecoded = try containerValues.decodeIfPresent(EphemeralStorage.self, forKey: .ephemeralStorage)
        ephemeralStorage = ephemeralStorageDecoded
    }
}

extension Task: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Task(attachments: \(String(describing: attachments)), attributes: \(String(describing: attributes)), availabilityZone: \(String(describing: availabilityZone)), capacityProviderName: \(String(describing: capacityProviderName)), clusterArn: \(String(describing: clusterArn)), connectivity: \(String(describing: connectivity)), connectivityAt: \(String(describing: connectivityAt)), containerInstanceArn: \(String(describing: containerInstanceArn)), containers: \(String(describing: containers)), cpu: \(String(describing: cpu)), createdAt: \(String(describing: createdAt)), desiredStatus: \(String(describing: desiredStatus)), enableExecuteCommand: \(String(describing: enableExecuteCommand)), ephemeralStorage: \(String(describing: ephemeralStorage)), executionStoppedAt: \(String(describing: executionStoppedAt)), group: \(String(describing: group)), healthStatus: \(String(describing: healthStatus)), inferenceAccelerators: \(String(describing: inferenceAccelerators)), lastStatus: \(String(describing: lastStatus)), launchType: \(String(describing: launchType)), memory: \(String(describing: memory)), overrides: \(String(describing: overrides)), platformVersion: \(String(describing: platformVersion)), pullStartedAt: \(String(describing: pullStartedAt)), pullStoppedAt: \(String(describing: pullStoppedAt)), startedAt: \(String(describing: startedAt)), startedBy: \(String(describing: startedBy)), stopCode: \(String(describing: stopCode)), stoppedAt: \(String(describing: stoppedAt)), stoppedReason: \(String(describing: stoppedReason)), stoppingAt: \(String(describing: stoppingAt)), tags: \(String(describing: tags)), taskArn: \(String(describing: taskArn)), taskDefinitionArn: \(String(describing: taskDefinitionArn)), version: \(String(describing: version)))"}
}

/// <p>Details on a task in a cluster.</p>
public struct Task: Equatable {
    /// <p>The Elastic Network Adapter associated with the task if the task uses the
    /// 				<code>awsvpc</code> network mode.</p>
    public let attachments: [Attachment]?
    /// <p>The attributes of the task</p>
    public let attributes: [Attribute]?
    /// <p>The availability zone of the task.</p>
    public let availabilityZone: String?
    /// <p>The capacity provider associated with the task.</p>
    public let capacityProviderName: String?
    /// <p>The ARN of the cluster that hosts the task.</p>
    public let clusterArn: String?
    /// <p>The connectivity status of a task.</p>
    public let connectivity: Connectivity?
    /// <p>The Unix timestamp for when the task last went into <code>CONNECTED</code>
    /// 			status.</p>
    public let connectivityAt: Date?
    /// <p>The ARN of the container instances that host the task.</p>
    public let containerInstanceArn: String?
    /// <p>The containers associated with the task.</p>
    public let containers: [Container]?
    /// <p>The number of CPU units used by the task as expressed in a task definition. It can be
    /// 			expressed as an integer using CPU units, for example <code>1024</code>. It can also be
    /// 			expressed as a string using vCPUs, for example <code>1 vCPU</code> or <code>1
    /// 				vcpu</code>. String values are converted to an integer indicating the CPU units when
    /// 			the task definition is registered.</p>
    /// 		       <p>If you are using the EC2 launch type, this field is optional. Supported
    /// 			values are between <code>128</code> CPU units (<code>0.125</code> vCPUs) and
    /// 				<code>10240</code> CPU units (<code>10</code> vCPUs).</p>
    /// 		       <p>If you are using the Fargate launch type, this field is required and you
    /// 			must use one of the following values, which determines your range of supported values
    /// 			for the <code>memory</code> parameter:</p>
    ///          <ul>
    ///             <li>
    ///                 <p>256 (.25 vCPU) - Available <code>memory</code> values: 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>512 (.5 vCPU) - Available <code>memory</code> values: 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>1024 (1 vCPU) - Available <code>memory</code> values: 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>2048 (2 vCPU) - Available <code>memory</code> values: Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>4096 (4 vCPU) - Available <code>memory</code> values: Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB)</p>
    ///             </li>
    ///          </ul>
    public let cpu: String?
    /// <p>The Unix timestamp for when the task was created (the task entered the
    /// 				<code>PENDING</code> state).</p>
    public let createdAt: Date?
    /// <p>The desired status of the task. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-lifecycle.html">Task
    /// 			Lifecycle</a>.</p>
    public let desiredStatus: String?
    /// <p>Whether or not execute command functionality is enabled for this task. If
    /// 				<code>true</code>, this enables execute command functionality on all containers in
    /// 			the task.</p>
    public let enableExecuteCommand: Bool
    /// <p>The ephemeral storage settings for the task.</p>
    public let ephemeralStorage: EphemeralStorage?
    /// <p>The Unix timestamp for when the task execution stopped.</p>
    public let executionStoppedAt: Date?
    /// <p>The name of the task group associated with the task.</p>
    public let group: String?
    /// <p>The health status for the task, which is determined by the health of the essential
    /// 			containers in the task. If all essential containers in the task are reporting as
    /// 				<code>HEALTHY</code>, then the task status also reports as <code>HEALTHY</code>. If
    /// 			any essential containers in the task are reporting as <code>UNHEALTHY</code> or
    /// 				<code>UNKNOWN</code>, then the task status also reports as <code>UNHEALTHY</code> or
    /// 				<code>UNKNOWN</code>, accordingly.</p>
    /// 		       <note>
    /// 			         <p>The Amazon ECS container agent does not monitor or report on Docker health checks that
    /// 				are embedded in a container image (such as those specified in a parent image or from
    /// 				the image's Dockerfile) and not specified in the container definition. Health check
    /// 				parameters that are specified in a container definition override any Docker health
    /// 				checks that exist in the container image.</p>
    /// 		       </note>
    public let healthStatus: HealthStatus?
    /// <p>The Elastic Inference accelerator associated with the task.</p>
    public let inferenceAccelerators: [InferenceAccelerator]?
    /// <p>The last known status of the task. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-lifecycle.html">Task
    /// 				Lifecycle</a>.</p>
    public let lastStatus: String?
    /// <p>The infrastructure on which your task is running. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS
    /// 				launch types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let launchType: LaunchType?
    /// <p>The amount of memory (in MiB) used by the task as expressed in a task definition. It
    /// 			can be expressed as an integer using MiB, for example <code>1024</code>. It can also be
    /// 			expressed as a string using GB, for example <code>1GB</code> or <code>1 GB</code>.
    /// 			String values are converted to an integer indicating the MiB when the task definition is
    /// 			registered.</p>
    /// 		       <p>If you are using the EC2 launch type, this field is optional.</p>
    /// 		       <p>If you are using the Fargate launch type, this field is required and you
    /// 			must use one of the following values, which determines your range of supported values
    /// 			for the <code>cpu</code> parameter:</p>
    ///          <ul>
    ///             <li>
    ///                 <p>512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) - Available <code>cpu</code> values: 256 (.25 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) - Available <code>cpu</code> values: 512 (.5 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) - Available <code>cpu</code> values: 1024 (1 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) - Available <code>cpu</code> values: 2048 (2 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB) - Available <code>cpu</code> values: 4096 (4 vCPU)</p>
    ///             </li>
    ///          </ul>
    public let memory: String?
    /// <p>One or more container overrides.</p>
    public let overrides: TaskOverride?
    /// <p>The platform version on which your task is running. A platform version is only
    /// 			specified for tasks using the Fargate launch type. If one is not
    /// 			specified, the <code>LATEST</code> platform version is used by default. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate Platform
    /// 				Versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <p>The Unix timestamp for when the container image pull began.</p>
    public let pullStartedAt: Date?
    /// <p>The Unix timestamp for when the container image pull completed.</p>
    public let pullStoppedAt: Date?
    /// <p>The Unix timestamp for when the task started (the task transitioned from the
    /// 				<code>PENDING</code> state to the <code>RUNNING</code> state).</p>
    public let startedAt: Date?
    /// <p>The tag specified when a task is started. If the task is started by an Amazon ECS service,
    /// 			then the <code>startedBy</code> parameter contains the deployment ID of the service that
    /// 			starts it.</p>
    public let startedBy: String?
    /// <p>The stop code indicating why a task was stopped. The <code>stoppedReason</code> may
    /// 			contain additional details.</p>
    public let stopCode: TaskStopCode?
    /// <p>The Unix timestamp for when the task was stopped (the task transitioned from the
    /// 				<code>RUNNING</code> state to the <code>STOPPED</code> state).</p>
    public let stoppedAt: Date?
    /// <p>The reason that the task was stopped.</p>
    public let stoppedReason: String?
    /// <p>The Unix timestamp for when the task stops (transitions from the <code>RUNNING</code>
    /// 			state to <code>STOPPED</code>).</p>
    public let stoppingAt: Date?
    /// <p>The metadata that you apply to the task to help you categorize and organize them. Each
    /// 			tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The Amazon Resource Name (ARN) of the task.</p>
    public let taskArn: String?
    /// <p>The ARN of the task definition that creates the task.</p>
    public let taskDefinitionArn: String?
    /// <p>The version counter for the task. Every time a task experiences a change that triggers
    /// 			a CloudWatch event, the version counter is incremented. If you are replicating your Amazon ECS task
    /// 			state with CloudWatch Events, you can compare the version of a task reported by the Amazon ECS API
    /// 			actions with the version reported in CloudWatch Events for the task (inside the
    /// 				<code>detail</code> object) to verify that the version in your event stream is
    /// 			current.</p>
    public let version: Int

    public init (
        attachments: [Attachment]? = nil,
        attributes: [Attribute]? = nil,
        availabilityZone: String? = nil,
        capacityProviderName: String? = nil,
        clusterArn: String? = nil,
        connectivity: Connectivity? = nil,
        connectivityAt: Date? = nil,
        containerInstanceArn: String? = nil,
        containers: [Container]? = nil,
        cpu: String? = nil,
        createdAt: Date? = nil,
        desiredStatus: String? = nil,
        enableExecuteCommand: Bool = false,
        ephemeralStorage: EphemeralStorage? = nil,
        executionStoppedAt: Date? = nil,
        group: String? = nil,
        healthStatus: HealthStatus? = nil,
        inferenceAccelerators: [InferenceAccelerator]? = nil,
        lastStatus: String? = nil,
        launchType: LaunchType? = nil,
        memory: String? = nil,
        overrides: TaskOverride? = nil,
        platformVersion: String? = nil,
        pullStartedAt: Date? = nil,
        pullStoppedAt: Date? = nil,
        startedAt: Date? = nil,
        startedBy: String? = nil,
        stopCode: TaskStopCode? = nil,
        stoppedAt: Date? = nil,
        stoppedReason: String? = nil,
        stoppingAt: Date? = nil,
        tags: [Tag]? = nil,
        taskArn: String? = nil,
        taskDefinitionArn: String? = nil,
        version: Int = 0
    )
    {
        self.attachments = attachments
        self.attributes = attributes
        self.availabilityZone = availabilityZone
        self.capacityProviderName = capacityProviderName
        self.clusterArn = clusterArn
        self.connectivity = connectivity
        self.connectivityAt = connectivityAt
        self.containerInstanceArn = containerInstanceArn
        self.containers = containers
        self.cpu = cpu
        self.createdAt = createdAt
        self.desiredStatus = desiredStatus
        self.enableExecuteCommand = enableExecuteCommand
        self.ephemeralStorage = ephemeralStorage
        self.executionStoppedAt = executionStoppedAt
        self.group = group
        self.healthStatus = healthStatus
        self.inferenceAccelerators = inferenceAccelerators
        self.lastStatus = lastStatus
        self.launchType = launchType
        self.memory = memory
        self.overrides = overrides
        self.platformVersion = platformVersion
        self.pullStartedAt = pullStartedAt
        self.pullStoppedAt = pullStoppedAt
        self.startedAt = startedAt
        self.startedBy = startedBy
        self.stopCode = stopCode
        self.stoppedAt = stoppedAt
        self.stoppedReason = stoppedReason
        self.stoppingAt = stoppingAt
        self.tags = tags
        self.taskArn = taskArn
        self.taskDefinitionArn = taskDefinitionArn
        self.version = version
    }
}

extension TaskDefinition: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case compatibilities
        case containerDefinitions
        case cpu
        case deregisteredAt
        case ephemeralStorage
        case executionRoleArn
        case family
        case inferenceAccelerators
        case ipcMode
        case memory
        case networkMode
        case pidMode
        case placementConstraints
        case proxyConfiguration
        case registeredAt
        case registeredBy
        case requiresAttributes
        case requiresCompatibilities
        case revision
        case status
        case taskDefinitionArn
        case taskRoleArn
        case volumes
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let compatibilities = compatibilities {
            var compatibilitiesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .compatibilities)
            for compatibilitylist0 in compatibilities {
                try compatibilitiesContainer.encode(compatibilitylist0.rawValue)
            }
        }
        if let containerDefinitions = containerDefinitions {
            var containerDefinitionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containerDefinitions)
            for containerdefinitions0 in containerDefinitions {
                try containerDefinitionsContainer.encode(containerdefinitions0)
            }
        }
        if let cpu = cpu {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let deregisteredAt = deregisteredAt {
            try encodeContainer.encode(deregisteredAt.timeIntervalSince1970, forKey: .deregisteredAt)
        }
        if let ephemeralStorage = ephemeralStorage {
            try encodeContainer.encode(ephemeralStorage, forKey: .ephemeralStorage)
        }
        if let executionRoleArn = executionRoleArn {
            try encodeContainer.encode(executionRoleArn, forKey: .executionRoleArn)
        }
        if let family = family {
            try encodeContainer.encode(family, forKey: .family)
        }
        if let inferenceAccelerators = inferenceAccelerators {
            var inferenceAcceleratorsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .inferenceAccelerators)
            for inferenceaccelerators0 in inferenceAccelerators {
                try inferenceAcceleratorsContainer.encode(inferenceaccelerators0)
            }
        }
        if let ipcMode = ipcMode {
            try encodeContainer.encode(ipcMode.rawValue, forKey: .ipcMode)
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let networkMode = networkMode {
            try encodeContainer.encode(networkMode.rawValue, forKey: .networkMode)
        }
        if let pidMode = pidMode {
            try encodeContainer.encode(pidMode.rawValue, forKey: .pidMode)
        }
        if let placementConstraints = placementConstraints {
            var placementConstraintsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementConstraints)
            for taskdefinitionplacementconstraints0 in placementConstraints {
                try placementConstraintsContainer.encode(taskdefinitionplacementconstraints0)
            }
        }
        if let proxyConfiguration = proxyConfiguration {
            try encodeContainer.encode(proxyConfiguration, forKey: .proxyConfiguration)
        }
        if let registeredAt = registeredAt {
            try encodeContainer.encode(registeredAt.timeIntervalSince1970, forKey: .registeredAt)
        }
        if let registeredBy = registeredBy {
            try encodeContainer.encode(registeredBy, forKey: .registeredBy)
        }
        if let requiresAttributes = requiresAttributes {
            var requiresAttributesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .requiresAttributes)
            for requiresattributes0 in requiresAttributes {
                try requiresAttributesContainer.encode(requiresattributes0)
            }
        }
        if let requiresCompatibilities = requiresCompatibilities {
            var requiresCompatibilitiesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .requiresCompatibilities)
            for compatibilitylist0 in requiresCompatibilities {
                try requiresCompatibilitiesContainer.encode(compatibilitylist0.rawValue)
            }
        }
        if revision != 0 {
            try encodeContainer.encode(revision, forKey: .revision)
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
        if let taskDefinitionArn = taskDefinitionArn {
            try encodeContainer.encode(taskDefinitionArn, forKey: .taskDefinitionArn)
        }
        if let taskRoleArn = taskRoleArn {
            try encodeContainer.encode(taskRoleArn, forKey: .taskRoleArn)
        }
        if let volumes = volumes {
            var volumesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .volumes)
            for volumelist0 in volumes {
                try volumesContainer.encode(volumelist0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskDefinitionArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinitionArn)
        taskDefinitionArn = taskDefinitionArnDecoded
        let containerDefinitionsContainer = try containerValues.decodeIfPresent([ContainerDefinition?].self, forKey: .containerDefinitions)
        var containerDefinitionsDecoded0:[ContainerDefinition]? = nil
        if let containerDefinitionsContainer = containerDefinitionsContainer {
            containerDefinitionsDecoded0 = [ContainerDefinition]()
            for structure0 in containerDefinitionsContainer {
                if let structure0 = structure0 {
                    containerDefinitionsDecoded0?.append(structure0)
                }
            }
        }
        containerDefinitions = containerDefinitionsDecoded0
        let familyDecoded = try containerValues.decodeIfPresent(String.self, forKey: .family)
        family = familyDecoded
        let taskRoleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskRoleArn)
        taskRoleArn = taskRoleArnDecoded
        let executionRoleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .executionRoleArn)
        executionRoleArn = executionRoleArnDecoded
        let networkModeDecoded = try containerValues.decodeIfPresent(NetworkMode.self, forKey: .networkMode)
        networkMode = networkModeDecoded
        let revisionDecoded = try containerValues.decode(Int.self, forKey: .revision)
        revision = revisionDecoded
        let volumesContainer = try containerValues.decodeIfPresent([Volume?].self, forKey: .volumes)
        var volumesDecoded0:[Volume]? = nil
        if let volumesContainer = volumesContainer {
            volumesDecoded0 = [Volume]()
            for structure0 in volumesContainer {
                if let structure0 = structure0 {
                    volumesDecoded0?.append(structure0)
                }
            }
        }
        volumes = volumesDecoded0
        let statusDecoded = try containerValues.decodeIfPresent(TaskDefinitionStatus.self, forKey: .status)
        status = statusDecoded
        let requiresAttributesContainer = try containerValues.decodeIfPresent([Attribute?].self, forKey: .requiresAttributes)
        var requiresAttributesDecoded0:[Attribute]? = nil
        if let requiresAttributesContainer = requiresAttributesContainer {
            requiresAttributesDecoded0 = [Attribute]()
            for structure0 in requiresAttributesContainer {
                if let structure0 = structure0 {
                    requiresAttributesDecoded0?.append(structure0)
                }
            }
        }
        requiresAttributes = requiresAttributesDecoded0
        let placementConstraintsContainer = try containerValues.decodeIfPresent([TaskDefinitionPlacementConstraint?].self, forKey: .placementConstraints)
        var placementConstraintsDecoded0:[TaskDefinitionPlacementConstraint]? = nil
        if let placementConstraintsContainer = placementConstraintsContainer {
            placementConstraintsDecoded0 = [TaskDefinitionPlacementConstraint]()
            for structure0 in placementConstraintsContainer {
                if let structure0 = structure0 {
                    placementConstraintsDecoded0?.append(structure0)
                }
            }
        }
        placementConstraints = placementConstraintsDecoded0
        let compatibilitiesContainer = try containerValues.decodeIfPresent([Compatibility?].self, forKey: .compatibilities)
        var compatibilitiesDecoded0:[Compatibility]? = nil
        if let compatibilitiesContainer = compatibilitiesContainer {
            compatibilitiesDecoded0 = [Compatibility]()
            for string0 in compatibilitiesContainer {
                if let string0 = string0 {
                    compatibilitiesDecoded0?.append(string0)
                }
            }
        }
        compatibilities = compatibilitiesDecoded0
        let requiresCompatibilitiesContainer = try containerValues.decodeIfPresent([Compatibility?].self, forKey: .requiresCompatibilities)
        var requiresCompatibilitiesDecoded0:[Compatibility]? = nil
        if let requiresCompatibilitiesContainer = requiresCompatibilitiesContainer {
            requiresCompatibilitiesDecoded0 = [Compatibility]()
            for string0 in requiresCompatibilitiesContainer {
                if let string0 = string0 {
                    requiresCompatibilitiesDecoded0?.append(string0)
                }
            }
        }
        requiresCompatibilities = requiresCompatibilitiesDecoded0
        let cpuDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cpu)
        cpu = cpuDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .memory)
        memory = memoryDecoded
        let inferenceAcceleratorsContainer = try containerValues.decodeIfPresent([InferenceAccelerator?].self, forKey: .inferenceAccelerators)
        var inferenceAcceleratorsDecoded0:[InferenceAccelerator]? = nil
        if let inferenceAcceleratorsContainer = inferenceAcceleratorsContainer {
            inferenceAcceleratorsDecoded0 = [InferenceAccelerator]()
            for structure0 in inferenceAcceleratorsContainer {
                if let structure0 = structure0 {
                    inferenceAcceleratorsDecoded0?.append(structure0)
                }
            }
        }
        inferenceAccelerators = inferenceAcceleratorsDecoded0
        let pidModeDecoded = try containerValues.decodeIfPresent(PidMode.self, forKey: .pidMode)
        pidMode = pidModeDecoded
        let ipcModeDecoded = try containerValues.decodeIfPresent(IpcMode.self, forKey: .ipcMode)
        ipcMode = ipcModeDecoded
        let proxyConfigurationDecoded = try containerValues.decodeIfPresent(ProxyConfiguration.self, forKey: .proxyConfiguration)
        proxyConfiguration = proxyConfigurationDecoded
        let registeredAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .registeredAt)
        registeredAt = registeredAtDecoded
        let deregisteredAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .deregisteredAt)
        deregisteredAt = deregisteredAtDecoded
        let registeredByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .registeredBy)
        registeredBy = registeredByDecoded
        let ephemeralStorageDecoded = try containerValues.decodeIfPresent(EphemeralStorage.self, forKey: .ephemeralStorage)
        ephemeralStorage = ephemeralStorageDecoded
    }
}

extension TaskDefinition: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TaskDefinition(compatibilities: \(String(describing: compatibilities)), containerDefinitions: \(String(describing: containerDefinitions)), cpu: \(String(describing: cpu)), deregisteredAt: \(String(describing: deregisteredAt)), ephemeralStorage: \(String(describing: ephemeralStorage)), executionRoleArn: \(String(describing: executionRoleArn)), family: \(String(describing: family)), inferenceAccelerators: \(String(describing: inferenceAccelerators)), ipcMode: \(String(describing: ipcMode)), memory: \(String(describing: memory)), networkMode: \(String(describing: networkMode)), pidMode: \(String(describing: pidMode)), placementConstraints: \(String(describing: placementConstraints)), proxyConfiguration: \(String(describing: proxyConfiguration)), registeredAt: \(String(describing: registeredAt)), registeredBy: \(String(describing: registeredBy)), requiresAttributes: \(String(describing: requiresAttributes)), requiresCompatibilities: \(String(describing: requiresCompatibilities)), revision: \(String(describing: revision)), status: \(String(describing: status)), taskDefinitionArn: \(String(describing: taskDefinitionArn)), taskRoleArn: \(String(describing: taskRoleArn)), volumes: \(String(describing: volumes)))"}
}

/// <p>The details of a task definition which describes the container and volume definitions
/// 			of an Amazon Elastic Container Service task. You can specify which Docker images to use, the required
/// 			resources, and other configurations related to launching the task definition through an
/// 			Amazon ECS service or task.</p>
public struct TaskDefinition: Equatable {
    /// <p>The task launch types the task definition validated against during task definition
    /// 			registration. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS launch types</a>
    /// 			in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let compatibilities: [Compatibility]?
    /// <p>A list of container definitions in JSON format that describe the different containers
    /// 			that make up your task. For more information about container definition parameters and
    /// 			defaults, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_defintions.html">Amazon ECS Task
    /// 				Definitions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let containerDefinitions: [ContainerDefinition]?
    /// <p>The number of <code>cpu</code> units used by the task. If you are using the EC2 launch
    /// 			type, this field is optional and any value can be used. If you are using the Fargate
    /// 			launch type, this field is required and you must use one of the following values, which
    /// 			determines your range of valid values for the <code>memory</code> parameter:</p>
    /// 		       <ul>
    ///             <li>
    ///                 <p>256 (.25 vCPU) - Available <code>memory</code> values: 512 (0.5 GB), 1024 (1 GB), 2048 (2 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>512 (.5 vCPU) - Available <code>memory</code> values: 1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>1024 (1 vCPU) - Available <code>memory</code> values: 2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>2048 (2 vCPU) - Available <code>memory</code> values: Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB)</p>
    ///             </li>
    ///             <li>
    ///                 <p>4096 (4 vCPU) - Available <code>memory</code> values: Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB)</p>
    ///             </li>
    ///          </ul>
    public let cpu: String?
    /// <p>The Unix timestamp for when the task definition was deregistered.</p>
    public let deregisteredAt: Date?
    /// <p>The ephemeral storage settings to use for tasks run with the task definition.</p>
    public let ephemeralStorage: EphemeralStorage?
    /// <p>The Amazon Resource Name (ARN) of the task execution role that grants the Amazon ECS container agent
    ///             permission to make AWS API calls on your behalf. The task execution IAM role is required
    ///             depending on the requirements of your task. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_execution_IAM_role.html">Amazon ECS task
    ///                 execution IAM role</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let executionRoleArn: String?
    /// <p>The name of a family that this task definition is registered to. Up to 255 letters
    /// 			(uppercase and lowercase), numbers, hyphens, and underscores are allowed.</p>
    /// 		       <p>A family groups multiple versions of a task definition. Amazon ECS gives the first task
    /// 			definition that you registered to a family a revision number of 1. Amazon ECS gives
    /// 			sequential revision numbers to each task definition that you add.</p>
    public let family: String?
    /// <p>The Elastic Inference accelerator associated with the task.</p>
    public let inferenceAccelerators: [InferenceAccelerator]?
    /// <p>The IPC resource namespace to use for the containers in the task. The valid values are
    ///                 <code>host</code>, <code>task</code>, or <code>none</code>. If <code>host</code> is
    ///             specified, then all containers within the tasks that specified the <code>host</code> IPC
    ///             mode on the same container instance share the same IPC resources with the host Amazon EC2
    ///             instance. If <code>task</code> is specified, all containers within the specified task
    ///             share the same IPC resources. If <code>none</code> is specified, then IPC resources
    ///             within the containers of a task are private and not shared with other containers in a
    ///             task or on the container instance. If no value is specified, then the IPC resource
    ///             namespace sharing depends on the Docker daemon setting on the container instance. For
    ///             more information, see <a href="https://docs.docker.com/engine/reference/run/#ipc-settings---ipc">IPC
    ///                 settings</a> in the <i>Docker run reference</i>.</p>
    ///         <p>If the <code>host</code> IPC mode is used, be aware that there is a heightened risk of
    ///             undesired IPC namespace expose. For more information, see <a href="https://docs.docker.com/engine/security/security/">Docker
    ///             security</a>.</p>
    ///         <p>If you are setting namespaced kernel parameters using <code>systemControls</code> for
    ///             the containers in the task, the following will apply to your IPC resource namespace. For
    ///             more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html">System
    ///                 Controls</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    ///         <ul>
    ///             <li>
    ///                 <p>For tasks that use the <code>host</code> IPC mode, IPC namespace related
    ///                         <code>systemControls</code> are not supported.</p>
    ///             </li>
    ///             <li>
    ///                 <p>For tasks that use the <code>task</code> IPC mode, IPC namespace related
    ///                         <code>systemControls</code> will apply to all containers within a
    ///                     task.</p>
    ///             </li>
    ///          </ul>
    ///          <note>
    ///                                 <p>This parameter is not supported for Windows containers or tasks run on AWS Fargate.</p>
    ///                              </note>
    public let ipcMode: IpcMode?
    /// <p>The amount (in MiB) of memory used by the task.</p>
    /// 		       <p>If your tasks will be run on Amazon EC2 instances, you must specify either a task-level
    /// 			memory value or a container-level memory value. This field is optional and any value can
    /// 			be used. If a task-level memory value is specified then the container-level memory value
    /// 			is optional. For more information regarding container-level memory and memory
    /// 			reservation, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/APIReference/API_ContainerDefinition.html">ContainerDefinition</a>.</p>
    /// 		       <p>If your tasks will be run on AWS Fargate, this field is required and you must use one of
    /// 			the following values, which determines your range of valid values for the
    /// 				<code>cpu</code> parameter:</p>
    ///          <ul>
    ///             <li>
    ///                 <p>512 (0.5 GB), 1024 (1 GB), 2048 (2 GB) - Available <code>cpu</code> values: 256 (.25 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>1024 (1 GB), 2048 (2 GB), 3072 (3 GB), 4096 (4 GB) - Available <code>cpu</code> values: 512 (.5 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>2048 (2 GB), 3072 (3 GB), 4096 (4 GB), 5120 (5 GB), 6144 (6 GB), 7168 (7 GB), 8192 (8 GB) - Available <code>cpu</code> values: 1024 (1 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>Between 4096 (4 GB) and 16384 (16 GB) in increments of 1024 (1 GB) - Available <code>cpu</code> values: 2048 (2 vCPU)</p>
    ///             </li>
    ///             <li>
    ///                 <p>Between 8192 (8 GB) and 30720 (30 GB) in increments of 1024 (1 GB) - Available <code>cpu</code> values: 4096 (4 vCPU)</p>
    ///             </li>
    ///          </ul>
    public let memory: String?
    /// <p>The Docker networking mode to use for the containers in the task. The valid values are
    ///                 <code>none</code>, <code>bridge</code>, <code>awsvpc</code>, and <code>host</code>.
    ///             If no network mode is specified, the default is <code>bridge</code>.</p>
    ///             <p>For Amazon ECS tasks on Fargate, the <code>awsvpc</code> network mode is required.
    ///             For Amazon ECS tasks on Amazon EC2 instances, any network mode can be used. If the network
    ///             mode is set to <code>none</code>, you cannot specify port mappings in your container
    ///             definitions, and the tasks containers do not have external connectivity. The
    ///                 <code>host</code> and <code>awsvpc</code> network modes offer the highest networking
    ///             performance for containers because they use the EC2 network stack instead of the
    ///             virtualized network stack provided by the <code>bridge</code> mode.</p>
    ///         <p>With the <code>host</code> and <code>awsvpc</code> network modes, exposed container
    ///             ports are mapped directly to the corresponding host port (for the <code>host</code>
    ///             network mode) or the attached elastic network interface port (for the
    ///                 <code>awsvpc</code> network mode), so you cannot take advantage of dynamic host port
    ///             mappings. </p>
    ///             <important>
    ///                         <p>When using the <code>host</code> network mode, you should not run
    ///                             containers using the root user (UID 0). It is considered best practice
    ///                             to use a non-root user.</p>
    ///                     </important>
    ///         <p>If the network mode is <code>awsvpc</code>, the task is allocated an elastic network
    ///             interface, and you must specify a <a>NetworkConfiguration</a> value when you create
    ///             a service or run a task with the task definition. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-networking.html">Task Networking</a> in the
    ///                 <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    ///         <note>
    ///             <p>Currently, only Amazon ECS-optimized AMIs, other Amazon Linux variants with the
    ///                     <code>ecs-init</code> package, or AWS Fargate infrastructure support the
    ///                     <code>awsvpc</code> network mode. </p>
    ///         </note>
    ///         <p>If the network mode is <code>host</code>, you cannot run multiple instantiations of the
    ///             same task on a single container instance when port mappings are used.</p>
    ///         <p>Docker for Windows uses different network modes than Docker for Linux. When you
    ///             register a task definition with Windows containers, you must not specify a network mode.
    ///             If you use the console to register a task definition with Windows containers, you must
    ///             choose the <code><default></code> network mode object. </p>
    ///         <p>For more information, see <a href="https://docs.docker.com/engine/reference/run/#network-settings">Network
    ///                 settings</a> in the <i>Docker run reference</i>.</p>
    public let networkMode: NetworkMode?
    /// <p>The process namespace to use for the containers in the task. The valid
    ///                             values are <code>host</code> or <code>task</code>. If <code>host</code>
    ///                             is specified, then all containers within the tasks that specified the
    ///                                 <code>host</code> PID mode on the same container instance share the
    ///                             same process namespace with the host Amazon EC2 instance. If <code>task</code> is
    ///                             specified, all containers within the specified task share the same
    ///                             process namespace. If no value is specified, the default is a private
    ///                             namespace. For more information, see <a href="https://docs.docker.com/engine/reference/run/#pid-settings---pid">PID settings</a> in the <i>Docker run
    ///                                 reference</i>.</p>
    ///                         <p>If the <code>host</code> PID mode is used, be aware that there is a
    ///                             heightened risk of undesired process namespace expose. For more
    ///                             information, see <a href="https://docs.docker.com/engine/security/security/">Docker
    ///                                 security</a>.</p>
    ///          <note>
    ///                                 <p>This parameter is not supported for Windows containers or tasks run on AWS Fargate.</p>
    ///                              </note>
    public let pidMode: PidMode?
    /// <p>An array of placement constraint objects to use for tasks.</p>
    /// 		       <note>
    /// 			         <p>This parameter is not supported for tasks run on AWS Fargate.</p>
    /// 		       </note>
    public let placementConstraints: [TaskDefinitionPlacementConstraint]?
    /// <p>The configuration details for the App Mesh proxy.</p>
    /// 		       <p>Your Amazon ECS container instances require at least version 1.26.0 of the container agent
    /// 			and at least version 1.26.0-1 of the <code>ecs-init</code> package to enable a proxy
    /// 			configuration. If your container instances are launched from the Amazon ECS-optimized AMI
    /// 			version <code>20190301</code> or later, then they contain the required versions of the
    /// 			container agent and <code>ecs-init</code>. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs-optimized_AMI.html">Amazon ECS-optimized Linux AMI</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let proxyConfiguration: ProxyConfiguration?
    /// <p>The Unix timestamp for when the task definition was registered.</p>
    public let registeredAt: Date?
    /// <p>The principal that registered the task definition.</p>
    public let registeredBy: String?
    /// <p>The container instance attributes required by your task. When an Amazon EC2 instance is
    /// 			registered to your cluster, the Amazon ECS container agent assigns some standard attributes
    /// 			to the instance. You can apply custom attributes, specified as key-value pairs using the
    /// 			Amazon ECS console or the <a>PutAttributes</a> API. These attributes are used when
    /// 			considering task placement for tasks hosted on Amazon EC2 instances. For more information,
    /// 			see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-placement-constraints.html#attributes">Attributes</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <note>
    /// 			         <p>This parameter is not supported for tasks run on AWS Fargate.</p>
    /// 		       </note>
    public let requiresAttributes: [Attribute]?
    /// <p>The task launch types the task definition was validated against. To determine which
    /// 			task launch types the task definition is validated for, see the <a>TaskDefinition$compatibilities</a> parameter.</p>
    public let requiresCompatibilities: [Compatibility]?
    /// <p>The revision of the task in a particular family. The revision is a version number of a
    /// 			task definition in a family. When you register a task definition for the first time, the
    /// 			revision is <code>1</code>. Each time that you register a new revision of a task
    /// 			definition in the same family, the revision value always increases by one, even if you
    /// 			have deregistered previous revisions in this family.</p>
    public let revision: Int
    /// <p>The status of the task definition.</p>
    public let status: TaskDefinitionStatus?
    /// <p>The full Amazon Resource Name (ARN) of the task definition.</p>
    public let taskDefinitionArn: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the AWS Identity and Access Management (IAM) role that grants containers in the
    /// 			task permission to call AWS APIs on your behalf. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html">Amazon ECS
    /// 				Task Role</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <p>IAM roles for tasks on Windows require that the <code>-EnableTaskIAMRole</code> option
    /// 			is set when you launch the Amazon ECS-optimized Windows AMI. Your containers must also run some
    /// 			configuration code in order to take advantage of the feature. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/windows_task_IAM_roles.html">Windows IAM roles
    /// 				for tasks</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let taskRoleArn: String?
    /// <p>The list of data volume definitions for the task. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_data_volumes.html">Using data volumes in tasks</a> in the
    /// 			<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    /// 		       <note>
    /// 			         <p>The <code>host</code> and <code>sourcePath</code> parameters are not supported for
    /// 				tasks run on AWS Fargate.</p>
    /// 		       </note>
    public let volumes: [Volume]?

    public init (
        compatibilities: [Compatibility]? = nil,
        containerDefinitions: [ContainerDefinition]? = nil,
        cpu: String? = nil,
        deregisteredAt: Date? = nil,
        ephemeralStorage: EphemeralStorage? = nil,
        executionRoleArn: String? = nil,
        family: String? = nil,
        inferenceAccelerators: [InferenceAccelerator]? = nil,
        ipcMode: IpcMode? = nil,
        memory: String? = nil,
        networkMode: NetworkMode? = nil,
        pidMode: PidMode? = nil,
        placementConstraints: [TaskDefinitionPlacementConstraint]? = nil,
        proxyConfiguration: ProxyConfiguration? = nil,
        registeredAt: Date? = nil,
        registeredBy: String? = nil,
        requiresAttributes: [Attribute]? = nil,
        requiresCompatibilities: [Compatibility]? = nil,
        revision: Int = 0,
        status: TaskDefinitionStatus? = nil,
        taskDefinitionArn: String? = nil,
        taskRoleArn: String? = nil,
        volumes: [Volume]? = nil
    )
    {
        self.compatibilities = compatibilities
        self.containerDefinitions = containerDefinitions
        self.cpu = cpu
        self.deregisteredAt = deregisteredAt
        self.ephemeralStorage = ephemeralStorage
        self.executionRoleArn = executionRoleArn
        self.family = family
        self.inferenceAccelerators = inferenceAccelerators
        self.ipcMode = ipcMode
        self.memory = memory
        self.networkMode = networkMode
        self.pidMode = pidMode
        self.placementConstraints = placementConstraints
        self.proxyConfiguration = proxyConfiguration
        self.registeredAt = registeredAt
        self.registeredBy = registeredBy
        self.requiresAttributes = requiresAttributes
        self.requiresCompatibilities = requiresCompatibilities
        self.revision = revision
        self.status = status
        self.taskDefinitionArn = taskDefinitionArn
        self.taskRoleArn = taskRoleArn
        self.volumes = volumes
    }
}

public enum TaskDefinitionFamilyStatus {
    case active
    case all
    case inactive
    case sdkUnknown(String)
}

extension TaskDefinitionFamilyStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskDefinitionFamilyStatus] {
        return [
            .active,
            .all,
            .inactive,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .active: return "ACTIVE"
        case .all: return "ALL"
        case .inactive: return "INACTIVE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskDefinitionFamilyStatus(rawValue: rawValue) ?? TaskDefinitionFamilyStatus.sdkUnknown(rawValue)
    }
}

public enum TaskDefinitionField {
    case tags
    case sdkUnknown(String)
}

extension TaskDefinitionField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskDefinitionField] {
        return [
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskDefinitionField(rawValue: rawValue) ?? TaskDefinitionField.sdkUnknown(rawValue)
    }
}

extension TaskDefinitionPlacementConstraint: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case expression
        case type
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let expression = expression {
            try encodeContainer.encode(expression, forKey: .expression)
        }
        if let type = type {
            try encodeContainer.encode(type.rawValue, forKey: .type)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let typeDecoded = try containerValues.decodeIfPresent(TaskDefinitionPlacementConstraintType.self, forKey: .type)
        type = typeDecoded
        let expressionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .expression)
        expression = expressionDecoded
    }
}

extension TaskDefinitionPlacementConstraint: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TaskDefinitionPlacementConstraint(expression: \(String(describing: expression)), type: \(String(describing: type)))"}
}

/// <p>An object representing a constraint on task placement in the task definition. For more
/// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-placement-constraints.html">Task placement constraints</a> in the
/// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
/// 		       <note>
/// 			         <p>Task placement constraints are not supported for tasks run on AWS Fargate.</p>
/// 		       </note>
public struct TaskDefinitionPlacementConstraint: Equatable {
    /// <p>A cluster query language expression to apply to the constraint. For more information,
    /// 			see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/cluster-query-language.html">Cluster query language</a> in the
    /// 				<i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let expression: String?
    /// <p>The type of constraint. The <code>MemberOf</code> constraint restricts selection to be
    /// 			from a group of valid candidates.</p>
    public let type: TaskDefinitionPlacementConstraintType?

    public init (
        expression: String? = nil,
        type: TaskDefinitionPlacementConstraintType? = nil
    )
    {
        self.expression = expression
        self.type = type
    }
}

public enum TaskDefinitionPlacementConstraintType {
    case memberOf
    case sdkUnknown(String)
}

extension TaskDefinitionPlacementConstraintType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskDefinitionPlacementConstraintType] {
        return [
            .memberOf,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .memberOf: return "memberOf"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskDefinitionPlacementConstraintType(rawValue: rawValue) ?? TaskDefinitionPlacementConstraintType.sdkUnknown(rawValue)
    }
}

public enum TaskDefinitionStatus {
    case active
    case inactive
    case sdkUnknown(String)
}

extension TaskDefinitionStatus : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskDefinitionStatus] {
        return [
            .active,
            .inactive,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .active: return "ACTIVE"
        case .inactive: return "INACTIVE"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskDefinitionStatus(rawValue: rawValue) ?? TaskDefinitionStatus.sdkUnknown(rawValue)
    }
}

public enum TaskField {
    case tags
    case sdkUnknown(String)
}

extension TaskField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskField] {
        return [
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskField(rawValue: rawValue) ?? TaskField.sdkUnknown(rawValue)
    }
}

extension TaskOverride: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerOverrides
        case cpu
        case ephemeralStorage
        case executionRoleArn
        case inferenceAcceleratorOverrides
        case memory
        case taskRoleArn
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerOverrides = containerOverrides {
            var containerOverridesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containerOverrides)
            for containeroverrides0 in containerOverrides {
                try containerOverridesContainer.encode(containeroverrides0)
            }
        }
        if let cpu = cpu {
            try encodeContainer.encode(cpu, forKey: .cpu)
        }
        if let ephemeralStorage = ephemeralStorage {
            try encodeContainer.encode(ephemeralStorage, forKey: .ephemeralStorage)
        }
        if let executionRoleArn = executionRoleArn {
            try encodeContainer.encode(executionRoleArn, forKey: .executionRoleArn)
        }
        if let inferenceAcceleratorOverrides = inferenceAcceleratorOverrides {
            var inferenceAcceleratorOverridesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .inferenceAcceleratorOverrides)
            for inferenceacceleratoroverrides0 in inferenceAcceleratorOverrides {
                try inferenceAcceleratorOverridesContainer.encode(inferenceacceleratoroverrides0)
            }
        }
        if let memory = memory {
            try encodeContainer.encode(memory, forKey: .memory)
        }
        if let taskRoleArn = taskRoleArn {
            try encodeContainer.encode(taskRoleArn, forKey: .taskRoleArn)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerOverridesContainer = try containerValues.decodeIfPresent([ContainerOverride?].self, forKey: .containerOverrides)
        var containerOverridesDecoded0:[ContainerOverride]? = nil
        if let containerOverridesContainer = containerOverridesContainer {
            containerOverridesDecoded0 = [ContainerOverride]()
            for structure0 in containerOverridesContainer {
                if let structure0 = structure0 {
                    containerOverridesDecoded0?.append(structure0)
                }
            }
        }
        containerOverrides = containerOverridesDecoded0
        let cpuDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cpu)
        cpu = cpuDecoded
        let inferenceAcceleratorOverridesContainer = try containerValues.decodeIfPresent([InferenceAcceleratorOverride?].self, forKey: .inferenceAcceleratorOverrides)
        var inferenceAcceleratorOverridesDecoded0:[InferenceAcceleratorOverride]? = nil
        if let inferenceAcceleratorOverridesContainer = inferenceAcceleratorOverridesContainer {
            inferenceAcceleratorOverridesDecoded0 = [InferenceAcceleratorOverride]()
            for structure0 in inferenceAcceleratorOverridesContainer {
                if let structure0 = structure0 {
                    inferenceAcceleratorOverridesDecoded0?.append(structure0)
                }
            }
        }
        inferenceAcceleratorOverrides = inferenceAcceleratorOverridesDecoded0
        let executionRoleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .executionRoleArn)
        executionRoleArn = executionRoleArnDecoded
        let memoryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .memory)
        memory = memoryDecoded
        let taskRoleArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskRoleArn)
        taskRoleArn = taskRoleArnDecoded
        let ephemeralStorageDecoded = try containerValues.decodeIfPresent(EphemeralStorage.self, forKey: .ephemeralStorage)
        ephemeralStorage = ephemeralStorageDecoded
    }
}

extension TaskOverride: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TaskOverride(containerOverrides: \(String(describing: containerOverrides)), cpu: \(String(describing: cpu)), ephemeralStorage: \(String(describing: ephemeralStorage)), executionRoleArn: \(String(describing: executionRoleArn)), inferenceAcceleratorOverrides: \(String(describing: inferenceAcceleratorOverrides)), memory: \(String(describing: memory)), taskRoleArn: \(String(describing: taskRoleArn)))"}
}

/// <p>The overrides associated with a task.</p>
public struct TaskOverride: Equatable {
    /// <p>One or more container overrides sent to a task.</p>
    public let containerOverrides: [ContainerOverride]?
    /// <p>The cpu override for the task.</p>
    public let cpu: String?
    /// <p>The ephemeral storage setting override for the task.</p>
    /// 		       <note>
    /// 			         <p>This parameter is only supported for tasks hosted on AWS Fargate using platform
    /// 				version <code>1.4.0</code> or later.</p>
    /// 		       </note>
    public let ephemeralStorage: EphemeralStorage?
    /// <p>The Amazon Resource Name (ARN) of the task execution IAM role override for the task.</p>
    public let executionRoleArn: String?
    /// <p>The Elastic Inference accelerator override for the task.</p>
    public let inferenceAcceleratorOverrides: [InferenceAcceleratorOverride]?
    /// <p>The memory override for the task.</p>
    public let memory: String?
    /// <p>The Amazon Resource Name (ARN) of the IAM role that containers in this task can assume. All containers
    /// 			in this task are granted the permissions that are specified in this role.</p>
    public let taskRoleArn: String?

    public init (
        containerOverrides: [ContainerOverride]? = nil,
        cpu: String? = nil,
        ephemeralStorage: EphemeralStorage? = nil,
        executionRoleArn: String? = nil,
        inferenceAcceleratorOverrides: [InferenceAcceleratorOverride]? = nil,
        memory: String? = nil,
        taskRoleArn: String? = nil
    )
    {
        self.containerOverrides = containerOverrides
        self.cpu = cpu
        self.ephemeralStorage = ephemeralStorage
        self.executionRoleArn = executionRoleArn
        self.inferenceAcceleratorOverrides = inferenceAcceleratorOverrides
        self.memory = memory
        self.taskRoleArn = taskRoleArn
    }
}

extension TaskSet: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case clusterArn
        case computedDesiredCount
        case createdAt
        case externalId
        case id
        case launchType
        case loadBalancers
        case networkConfiguration
        case pendingCount
        case platformVersion
        case runningCount
        case scale
        case serviceArn
        case serviceRegistries
        case stabilityStatus
        case stabilityStatusAt
        case startedBy
        case status
        case tags
        case taskDefinition
        case taskSetArn
        case updatedAt
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let clusterArn = clusterArn {
            try encodeContainer.encode(clusterArn, forKey: .clusterArn)
        }
        if computedDesiredCount != 0 {
            try encodeContainer.encode(computedDesiredCount, forKey: .computedDesiredCount)
        }
        if let createdAt = createdAt {
            try encodeContainer.encode(createdAt.timeIntervalSince1970, forKey: .createdAt)
        }
        if let externalId = externalId {
            try encodeContainer.encode(externalId, forKey: .externalId)
        }
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
        if let launchType = launchType {
            try encodeContainer.encode(launchType.rawValue, forKey: .launchType)
        }
        if let loadBalancers = loadBalancers {
            var loadBalancersContainer = encodeContainer.nestedUnkeyedContainer(forKey: .loadBalancers)
            for loadbalancers0 in loadBalancers {
                try loadBalancersContainer.encode(loadbalancers0)
            }
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if pendingCount != 0 {
            try encodeContainer.encode(pendingCount, forKey: .pendingCount)
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if runningCount != 0 {
            try encodeContainer.encode(runningCount, forKey: .runningCount)
        }
        if let scale = scale {
            try encodeContainer.encode(scale, forKey: .scale)
        }
        if let serviceArn = serviceArn {
            try encodeContainer.encode(serviceArn, forKey: .serviceArn)
        }
        if let serviceRegistries = serviceRegistries {
            var serviceRegistriesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .serviceRegistries)
            for serviceregistries0 in serviceRegistries {
                try serviceRegistriesContainer.encode(serviceregistries0)
            }
        }
        if let stabilityStatus = stabilityStatus {
            try encodeContainer.encode(stabilityStatus.rawValue, forKey: .stabilityStatus)
        }
        if let stabilityStatusAt = stabilityStatusAt {
            try encodeContainer.encode(stabilityStatusAt.timeIntervalSince1970, forKey: .stabilityStatusAt)
        }
        if let startedBy = startedBy {
            try encodeContainer.encode(startedBy, forKey: .startedBy)
        }
        if let status = status {
            try encodeContainer.encode(status, forKey: .status)
        }
        if let tags = tags {
            var tagsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tags)
            for tags0 in tags {
                try tagsContainer.encode(tags0)
            }
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
        if let taskSetArn = taskSetArn {
            try encodeContainer.encode(taskSetArn, forKey: .taskSetArn)
        }
        if let updatedAt = updatedAt {
            try encodeContainer.encode(updatedAt.timeIntervalSince1970, forKey: .updatedAt)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
        let taskSetArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskSetArn)
        taskSetArn = taskSetArnDecoded
        let serviceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceArn)
        serviceArn = serviceArnDecoded
        let clusterArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clusterArn)
        clusterArn = clusterArnDecoded
        let startedByDecoded = try containerValues.decodeIfPresent(String.self, forKey: .startedBy)
        startedBy = startedByDecoded
        let externalIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .externalId)
        externalId = externalIdDecoded
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let computedDesiredCountDecoded = try containerValues.decode(Int.self, forKey: .computedDesiredCount)
        computedDesiredCount = computedDesiredCountDecoded
        let pendingCountDecoded = try containerValues.decode(Int.self, forKey: .pendingCount)
        pendingCount = pendingCountDecoded
        let runningCountDecoded = try containerValues.decode(Int.self, forKey: .runningCount)
        runningCount = runningCountDecoded
        let createdAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .createdAt)
        createdAt = createdAtDecoded
        let updatedAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .updatedAt)
        updatedAt = updatedAtDecoded
        let launchTypeDecoded = try containerValues.decodeIfPresent(LaunchType.self, forKey: .launchType)
        launchType = launchTypeDecoded
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let loadBalancersContainer = try containerValues.decodeIfPresent([LoadBalancer?].self, forKey: .loadBalancers)
        var loadBalancersDecoded0:[LoadBalancer]? = nil
        if let loadBalancersContainer = loadBalancersContainer {
            loadBalancersDecoded0 = [LoadBalancer]()
            for structure0 in loadBalancersContainer {
                if let structure0 = structure0 {
                    loadBalancersDecoded0?.append(structure0)
                }
            }
        }
        loadBalancers = loadBalancersDecoded0
        let serviceRegistriesContainer = try containerValues.decodeIfPresent([ServiceRegistry?].self, forKey: .serviceRegistries)
        var serviceRegistriesDecoded0:[ServiceRegistry]? = nil
        if let serviceRegistriesContainer = serviceRegistriesContainer {
            serviceRegistriesDecoded0 = [ServiceRegistry]()
            for structure0 in serviceRegistriesContainer {
                if let structure0 = structure0 {
                    serviceRegistriesDecoded0?.append(structure0)
                }
            }
        }
        serviceRegistries = serviceRegistriesDecoded0
        let scaleDecoded = try containerValues.decodeIfPresent(Scale.self, forKey: .scale)
        scale = scaleDecoded
        let stabilityStatusDecoded = try containerValues.decodeIfPresent(StabilityStatus.self, forKey: .stabilityStatus)
        stabilityStatus = stabilityStatusDecoded
        let stabilityStatusAtDecoded = try containerValues.decodeIfPresent(Date.self, forKey: .stabilityStatusAt)
        stabilityStatusAt = stabilityStatusAtDecoded
        let tagsContainer = try containerValues.decodeIfPresent([Tag?].self, forKey: .tags)
        var tagsDecoded0:[Tag]? = nil
        if let tagsContainer = tagsContainer {
            tagsDecoded0 = [Tag]()
            for structure0 in tagsContainer {
                if let structure0 = structure0 {
                    tagsDecoded0?.append(structure0)
                }
            }
        }
        tags = tagsDecoded0
    }
}

extension TaskSet: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TaskSet(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), clusterArn: \(String(describing: clusterArn)), computedDesiredCount: \(String(describing: computedDesiredCount)), createdAt: \(String(describing: createdAt)), externalId: \(String(describing: externalId)), id: \(String(describing: id)), launchType: \(String(describing: launchType)), loadBalancers: \(String(describing: loadBalancers)), networkConfiguration: \(String(describing: networkConfiguration)), pendingCount: \(String(describing: pendingCount)), platformVersion: \(String(describing: platformVersion)), runningCount: \(String(describing: runningCount)), scale: \(String(describing: scale)), serviceArn: \(String(describing: serviceArn)), serviceRegistries: \(String(describing: serviceRegistries)), stabilityStatus: \(String(describing: stabilityStatus)), stabilityStatusAt: \(String(describing: stabilityStatusAt)), startedBy: \(String(describing: startedBy)), status: \(String(describing: status)), tags: \(String(describing: tags)), taskDefinition: \(String(describing: taskDefinition)), taskSetArn: \(String(describing: taskSetArn)), updatedAt: \(String(describing: updatedAt)))"}
}

/// <p>Information about a set of Amazon ECS tasks in either an AWS CodeDeploy or an <code>EXTERNAL</code>
/// 			deployment. An Amazon ECS task set includes details such as the desired number of tasks, how
/// 			many tasks are running, and whether the task set serves production traffic.</p>
public struct TaskSet: Equatable {
    /// <p>The capacity provider strategy associated with the task set.</p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The Amazon Resource Name (ARN) of the cluster that the service that hosts the task set exists
    /// 			in.</p>
    public let clusterArn: String?
    /// <p>The computed desired count for the task set. This is calculated by multiplying the
    /// 			service's <code>desiredCount</code> by the task set's <code>scale</code> percentage. The
    /// 			result is always rounded up. For example, if the computed desired count is 1.2, it
    /// 			rounds up to 2 tasks.</p>
    public let computedDesiredCount: Int
    /// <p>The Unix timestamp for when the task set was created.</p>
    public let createdAt: Date?
    /// <p>The external ID associated with the task set.</p>
    /// 		       <p>If a task set is created by an AWS CodeDeploy deployment, the <code>externalId</code> parameter
    /// 			contains the AWS CodeDeploy deployment ID.</p>
    /// 		       <p>If a task set is created for an external deployment and is associated with a service
    /// 			discovery registry, the <code>externalId</code> parameter contains the
    /// 				<code>ECS_TASK_SET_EXTERNAL_ID</code> AWS Cloud Map attribute.</p>
    public let externalId: String?
    /// <p>The ID of the task set.</p>
    public let id: String?
    /// <p>The launch type the tasks in the task set are using. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html">Amazon ECS
    /// 				launch types</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let launchType: LaunchType?
    /// <p>Details on a load balancer that is used with a task set.</p>
    public let loadBalancers: [LoadBalancer]?
    /// <p>The network configuration for the task set.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>The number of tasks in the task set that are in the <code>PENDING</code> status during
    /// 			a deployment. A task in the <code>PENDING</code> state is preparing to enter the
    /// 				<code>RUNNING</code> state. A task set enters the <code>PENDING</code> status when
    /// 			it launches for the first time or when it is restarted after being in the
    /// 				<code>STOPPED</code> state.</p>
    public let pendingCount: Int
    /// <p>The AWS Fargate platform version on which the tasks in the task set are running. A
    /// 			platform version is only specified for tasks run on AWS Fargate. For more information, see
    /// 				<a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate platform
    /// 				versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <p>The number of tasks in the task set that are in the <code>RUNNING</code> status during
    /// 			a deployment. A task in the <code>RUNNING</code> state is running and ready for
    /// 			use.</p>
    public let runningCount: Int
    /// <p>A floating-point percentage of the desired number of tasks to place and keep running
    /// 			in the task set.</p>
    public let scale: Scale?
    /// <p>The Amazon Resource Name (ARN) of the service the task set exists in.</p>
    public let serviceArn: String?
    /// <p>The details of the service discovery registries to assign to this task set. For more
    /// 			information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-discovery.html">Service
    /// 				discovery</a>.</p>
    public let serviceRegistries: [ServiceRegistry]?
    /// <p>The stability status, which indicates whether the task set has reached a steady state.
    /// 			If the following conditions are met, the task set will be in
    /// 			<code>STEADY_STATE</code>:</p>
    /// 		       <ul>
    ///             <li>
    /// 				           <p>The task <code>runningCount</code> is equal to the
    /// 						<code>computedDesiredCount</code>.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>The <code>pendingCount</code> is <code>0</code>.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>There are no tasks running on container instances in the <code>DRAINING</code>
    /// 					status.</p>
    /// 			         </li>
    ///             <li>
    /// 				           <p>All tasks are reporting a healthy status from the load balancers, service
    /// 					discovery, and container health checks.</p>
    /// 			         </li>
    ///          </ul>
    /// 		       <p>If any of those conditions are not met, the stability status returns
    /// 				<code>STABILIZING</code>.</p>
    public let stabilityStatus: StabilityStatus?
    /// <p>The Unix timestamp for when the task set stability status was retrieved.</p>
    public let stabilityStatusAt: Date?
    /// <p>The tag specified when a task set is started. If the task set is created by an AWS CodeDeploy
    /// 			deployment, the <code>startedBy</code> parameter is <code>CODE_DEPLOY</code>. For a task
    /// 			set created for an external deployment, the startedBy field isn't used.</p>
    public let startedBy: String?
    /// <p>The status of the task set. The following describes each state:</p>
    /// 		       <dl>
    ///             <dt>PRIMARY</dt>
    ///             <dd>
    /// 					          <p>The task set is serving production traffic.</p>
    /// 				        </dd>
    ///             <dt>ACTIVE</dt>
    ///             <dd>
    /// 					          <p>The task set is not serving production traffic.</p>
    /// 				        </dd>
    ///             <dt>DRAINING</dt>
    ///             <dd>
    /// 					          <p>The tasks in the task set are being stopped and their corresponding
    /// 						targets are being deregistered from their target group.</p>
    /// 				        </dd>
    ///          </dl>
    public let status: String?
    /// <p>The metadata that you apply to the task set to help you categorize and organize them.
    /// 			Each tag consists of a key and an optional value, both of which you define.</p>
    /// 		       <p>The following basic restrictions apply to tags:</p>
    ///         <ul>
    ///             <li>
    ///                 <p>Maximum number of tags per resource - 50</p>
    ///             </li>
    ///             <li>
    ///                 <p>For each resource, each tag key must be unique, and each tag key can have only
    ///                     one value.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum key length - 128 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>Maximum value length - 256 Unicode characters in UTF-8</p>
    ///             </li>
    ///             <li>
    ///                 <p>If your tagging schema is used across multiple services and resources,
    ///                     remember that other services may have restrictions on allowed characters.
    ///                     Generally allowed characters are: letters, numbers, and spaces representable in
    ///                     UTF-8, and the following characters: + - = . _ : / @.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Tag keys and values are case-sensitive.</p>
    ///             </li>
    ///             <li>
    ///                 <p>Do not use <code>aws:</code>, <code>AWS:</code>, or any upper or lowercase
    ///                     combination of such as a prefix for either keys or values as it is reserved for
    ///                     AWS use. You cannot edit or delete tag keys or values with this prefix. Tags with
    ///                     this prefix do not count against your tags per resource limit.</p>
    ///             </li>
    ///          </ul>
    public let tags: [Tag]?
    /// <p>The task definition the task set is using.</p>
    public let taskDefinition: String?
    /// <p>The Amazon Resource Name (ARN) of the task set.</p>
    public let taskSetArn: String?
    /// <p>The Unix timestamp for when the task set was last updated.</p>
    public let updatedAt: Date?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        clusterArn: String? = nil,
        computedDesiredCount: Int = 0,
        createdAt: Date? = nil,
        externalId: String? = nil,
        id: String? = nil,
        launchType: LaunchType? = nil,
        loadBalancers: [LoadBalancer]? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        pendingCount: Int = 0,
        platformVersion: String? = nil,
        runningCount: Int = 0,
        scale: Scale? = nil,
        serviceArn: String? = nil,
        serviceRegistries: [ServiceRegistry]? = nil,
        stabilityStatus: StabilityStatus? = nil,
        stabilityStatusAt: Date? = nil,
        startedBy: String? = nil,
        status: String? = nil,
        tags: [Tag]? = nil,
        taskDefinition: String? = nil,
        taskSetArn: String? = nil,
        updatedAt: Date? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.clusterArn = clusterArn
        self.computedDesiredCount = computedDesiredCount
        self.createdAt = createdAt
        self.externalId = externalId
        self.id = id
        self.launchType = launchType
        self.loadBalancers = loadBalancers
        self.networkConfiguration = networkConfiguration
        self.pendingCount = pendingCount
        self.platformVersion = platformVersion
        self.runningCount = runningCount
        self.scale = scale
        self.serviceArn = serviceArn
        self.serviceRegistries = serviceRegistries
        self.stabilityStatus = stabilityStatus
        self.stabilityStatusAt = stabilityStatusAt
        self.startedBy = startedBy
        self.status = status
        self.tags = tags
        self.taskDefinition = taskDefinition
        self.taskSetArn = taskSetArn
        self.updatedAt = updatedAt
    }
}

public enum TaskSetField {
    case tags
    case sdkUnknown(String)
}

extension TaskSetField : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskSetField] {
        return [
            .tags,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tags: return "TAGS"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskSetField(rawValue: rawValue) ?? TaskSetField.sdkUnknown(rawValue)
    }
}

extension TaskSetNotFoundException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "TaskSetNotFoundException(message: \(String(describing: message)))"}
}

extension TaskSetNotFoundException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: TaskSetNotFoundExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified task set could not be found. You can view your available task sets with
/// 				<a>DescribeTaskSets</a>. Task sets are specific to each cluster, service
/// 			and Region.</p>
public struct TaskSetNotFoundException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct TaskSetNotFoundExceptionBody: Equatable {
    public let message: String?
}

extension TaskSetNotFoundExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public enum TaskStopCode {
    case essentialContainerExited
    case taskFailedToStart
    case userInitiated
    case sdkUnknown(String)
}

extension TaskStopCode : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TaskStopCode] {
        return [
            .essentialContainerExited,
            .taskFailedToStart,
            .userInitiated,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .essentialContainerExited: return "EssentialContainerExited"
        case .taskFailedToStart: return "TaskFailedToStart"
        case .userInitiated: return "UserInitiated"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TaskStopCode(rawValue: rawValue) ?? TaskStopCode.sdkUnknown(rawValue)
    }
}

extension Tmpfs: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case containerPath
        case mountOptions
        case size
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let containerPath = containerPath {
            try encodeContainer.encode(containerPath, forKey: .containerPath)
        }
        if let mountOptions = mountOptions {
            var mountOptionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .mountOptions)
            for stringlist0 in mountOptions {
                try mountOptionsContainer.encode(stringlist0)
            }
        }
        if size != 0 {
            try encodeContainer.encode(size, forKey: .size)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerPathDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerPath)
        containerPath = containerPathDecoded
        let sizeDecoded = try containerValues.decode(Int.self, forKey: .size)
        size = sizeDecoded
        let mountOptionsContainer = try containerValues.decodeIfPresent([String?].self, forKey: .mountOptions)
        var mountOptionsDecoded0:[String]? = nil
        if let mountOptionsContainer = mountOptionsContainer {
            mountOptionsDecoded0 = [String]()
            for string0 in mountOptionsContainer {
                if let string0 = string0 {
                    mountOptionsDecoded0?.append(string0)
                }
            }
        }
        mountOptions = mountOptionsDecoded0
    }
}

extension Tmpfs: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Tmpfs(containerPath: \(String(describing: containerPath)), mountOptions: \(String(describing: mountOptions)), size: \(String(describing: size)))"}
}

/// <p>The container path, mount options, and size of the tmpfs mount.</p>
public struct Tmpfs: Equatable {
    /// <p>The absolute file path where the tmpfs volume is to be mounted.</p>
    public let containerPath: String?
    /// <p>The list of tmpfs volume mount options.</p>
    /// 		       <p>Valid values: <code>"defaults" | "ro" | "rw" | "suid" | "nosuid" | "dev" | "nodev" |
    /// 				"exec" | "noexec" | "sync" | "async" | "dirsync" | "remount" | "mand" | "nomand" |
    /// 				"atime" | "noatime" | "diratime" | "nodiratime" | "bind" | "rbind" | "unbindable" |
    /// 				"runbindable" | "private" | "rprivate" | "shared" | "rshared" | "slave" | "rslave" |
    /// 				"relatime" | "norelatime" | "strictatime" | "nostrictatime" | "mode" | "uid" | "gid"
    /// 				| "nr_inodes" | "nr_blocks" | "mpol"</code>
    ///          </p>
    public let mountOptions: [String]?
    /// <p>The maximum size (in MiB) of the tmpfs volume.</p>
    public let size: Int

    public init (
        containerPath: String? = nil,
        mountOptions: [String]? = nil,
        size: Int = 0
    )
    {
        self.containerPath = containerPath
        self.mountOptions = mountOptions
        self.size = size
    }
}

public enum TransportProtocol {
    case tcp
    case udp
    case sdkUnknown(String)
}

extension TransportProtocol : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [TransportProtocol] {
        return [
            .tcp,
            .udp,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .tcp: return "tcp"
        case .udp: return "udp"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = TransportProtocol(rawValue: rawValue) ?? TransportProtocol.sdkUnknown(rawValue)
    }
}

extension Ulimit: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case hardLimit
        case name
        case softLimit
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if hardLimit != 0 {
            try encodeContainer.encode(hardLimit, forKey: .hardLimit)
        }
        if let name = name {
            try encodeContainer.encode(name.rawValue, forKey: .name)
        }
        if softLimit != 0 {
            try encodeContainer.encode(softLimit, forKey: .softLimit)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(UlimitName.self, forKey: .name)
        name = nameDecoded
        let softLimitDecoded = try containerValues.decode(Int.self, forKey: .softLimit)
        softLimit = softLimitDecoded
        let hardLimitDecoded = try containerValues.decode(Int.self, forKey: .hardLimit)
        hardLimit = hardLimitDecoded
    }
}

extension Ulimit: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Ulimit(hardLimit: \(String(describing: hardLimit)), name: \(String(describing: name)), softLimit: \(String(describing: softLimit)))"}
}

/// <p>The <code>ulimit</code> settings to pass to the container.</p>
/// 		       <p>Amazon ECS tasks hosted on Fargate use the default
/// 							resource limit values set by the operating system with the exception of
/// 							the <code>nofile</code> resource limit parameter which Fargate
/// 							overrides. The <code>nofile</code> resource limit sets a restriction on
/// 							the number of open files that a container can use. The default
/// 								<code>nofile</code> soft limit is <code>1024</code> and hard limit
/// 							is <code>4096</code>.</p>
public struct Ulimit: Equatable {
    /// <p>The hard limit for the ulimit type.</p>
    public let hardLimit: Int
    /// <p>The <code>type</code> of the <code>ulimit</code>.</p>
    public let name: UlimitName?
    /// <p>The soft limit for the ulimit type.</p>
    public let softLimit: Int

    public init (
        hardLimit: Int = 0,
        name: UlimitName? = nil,
        softLimit: Int = 0
    )
    {
        self.hardLimit = hardLimit
        self.name = name
        self.softLimit = softLimit
    }
}

public enum UlimitName {
    case core
    case cpu
    case data
    case fsize
    case locks
    case memlock
    case msgqueue
    case nice
    case nofile
    case nproc
    case rss
    case rtprio
    case rttime
    case sigpending
    case stack
    case sdkUnknown(String)
}

extension UlimitName : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [UlimitName] {
        return [
            .core,
            .cpu,
            .data,
            .fsize,
            .locks,
            .memlock,
            .msgqueue,
            .nice,
            .nofile,
            .nproc,
            .rss,
            .rtprio,
            .rttime,
            .sigpending,
            .stack,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .core: return "core"
        case .cpu: return "cpu"
        case .data: return "data"
        case .fsize: return "fsize"
        case .locks: return "locks"
        case .memlock: return "memlock"
        case .msgqueue: return "msgqueue"
        case .nice: return "nice"
        case .nofile: return "nofile"
        case .nproc: return "nproc"
        case .rss: return "rss"
        case .rtprio: return "rtprio"
        case .rttime: return "rttime"
        case .sigpending: return "sigpending"
        case .stack: return "stack"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = UlimitName(rawValue: rawValue) ?? UlimitName.sdkUnknown(rawValue)
    }
}

extension UnsupportedFeatureException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UnsupportedFeatureException(message: \(String(describing: message)))"}
}

extension UnsupportedFeatureException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UnsupportedFeatureExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>The specified task is not supported in this Region.</p>
public struct UnsupportedFeatureException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct UnsupportedFeatureExceptionBody: Equatable {
    public let message: String?
}

extension UnsupportedFeatureExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public struct UntagResourceInputBodyMiddleware: Middleware {
    public let id: String = "UntagResourceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UntagResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<UntagResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UntagResourceInput>
    public typealias MOutput = OperationOutput<UntagResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UntagResourceOutputError>
}

extension UntagResourceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UntagResourceInput(resourceArn: \(String(describing: resourceArn)), tagKeys: \(String(describing: tagKeys)))"}
}

extension UntagResourceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case resourceArn
        case tagKeys
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let resourceArn = resourceArn {
            try encodeContainer.encode(resourceArn, forKey: .resourceArn)
        }
        if let tagKeys = tagKeys {
            var tagKeysContainer = encodeContainer.nestedUnkeyedContainer(forKey: .tagKeys)
            for tagkeys0 in tagKeys {
                try tagKeysContainer.encode(tagkeys0)
            }
        }
    }
}

public struct UntagResourceInputHeadersMiddleware: Middleware {
    public let id: String = "UntagResourceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UntagResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<UntagResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UntagResourceInput>
    public typealias MOutput = OperationOutput<UntagResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UntagResourceOutputError>
}

public struct UntagResourceInputQueryItemMiddleware: Middleware {
    public let id: String = "UntagResourceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UntagResourceInput>,
                  next: H) -> Swift.Result<OperationOutput<UntagResourceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UntagResourceInput>
    public typealias MOutput = OperationOutput<UntagResourceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UntagResourceOutputError>
}

public struct UntagResourceInput: Equatable {
    /// <p>The Amazon Resource Name (ARN) of the resource from which to delete tags. Currently, the supported
    /// 			resources are Amazon ECS capacity providers, tasks, services, task definitions, clusters, and
    /// 			container instances.</p>
    public let resourceArn: String?
    /// <p>The keys of the tags to be removed.</p>
    public let tagKeys: [String]?

    public init (
        resourceArn: String? = nil,
        tagKeys: [String]? = nil
    )
    {
        self.resourceArn = resourceArn
        self.tagKeys = tagKeys
    }
}

struct UntagResourceInputBody: Equatable {
    public let resourceArn: String?
    public let tagKeys: [String]?
}

extension UntagResourceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case resourceArn
        case tagKeys
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let resourceArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .resourceArn)
        resourceArn = resourceArnDecoded
        let tagKeysContainer = try containerValues.decodeIfPresent([String?].self, forKey: .tagKeys)
        var tagKeysDecoded0:[String]? = nil
        if let tagKeysContainer = tagKeysContainer {
            tagKeysDecoded0 = [String]()
            for string0 in tagKeysContainer {
                if let string0 = string0 {
                    tagKeysDecoded0?.append(string0)
                }
            }
        }
        tagKeys = tagKeysDecoded0
    }
}

extension UntagResourceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UntagResourceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ResourceNotFoundException" : self = .resourceNotFoundException(try ResourceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UntagResourceOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case resourceNotFoundException(ResourceNotFoundException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UntagResourceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UntagResourceOutputResponse()"}
}

extension UntagResourceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
    }
}

public struct UntagResourceOutputResponse: Equatable {

    public init() {}
}

struct UntagResourceOutputResponseBody: Equatable {
}

extension UntagResourceOutputResponseBody: Decodable {

    public init (from decoder: Decoder) throws {
    }
}

public struct UpdateCapacityProviderInputBodyMiddleware: Middleware {
    public let id: String = "UpdateCapacityProviderInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateCapacityProviderInput>
    public typealias MOutput = OperationOutput<UpdateCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateCapacityProviderOutputError>
}

extension UpdateCapacityProviderInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateCapacityProviderInput(autoScalingGroupProvider: \(String(describing: autoScalingGroupProvider)), name: \(String(describing: name)))"}
}

extension UpdateCapacityProviderInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case autoScalingGroupProvider
        case name
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let autoScalingGroupProvider = autoScalingGroupProvider {
            try encodeContainer.encode(autoScalingGroupProvider, forKey: .autoScalingGroupProvider)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
    }
}

public struct UpdateCapacityProviderInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateCapacityProviderInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateCapacityProviderInput>
    public typealias MOutput = OperationOutput<UpdateCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateCapacityProviderOutputError>
}

public struct UpdateCapacityProviderInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateCapacityProviderInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateCapacityProviderInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateCapacityProviderOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateCapacityProviderInput>
    public typealias MOutput = OperationOutput<UpdateCapacityProviderOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateCapacityProviderOutputError>
}

public struct UpdateCapacityProviderInput: Equatable {
    /// <p>An object representing the parameters to update for the Auto Scaling group capacity
    /// 			provider.</p>
    public let autoScalingGroupProvider: AutoScalingGroupProviderUpdate?
    /// <p>The name of the capacity provider to update.</p>
    public let name: String?

    public init (
        autoScalingGroupProvider: AutoScalingGroupProviderUpdate? = nil,
        name: String? = nil
    )
    {
        self.autoScalingGroupProvider = autoScalingGroupProvider
        self.name = name
    }
}

struct UpdateCapacityProviderInputBody: Equatable {
    public let name: String?
    public let autoScalingGroupProvider: AutoScalingGroupProviderUpdate?
}

extension UpdateCapacityProviderInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case autoScalingGroupProvider
        case name
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let autoScalingGroupProviderDecoded = try containerValues.decodeIfPresent(AutoScalingGroupProviderUpdate.self, forKey: .autoScalingGroupProvider)
        autoScalingGroupProvider = autoScalingGroupProviderDecoded
    }
}

extension UpdateCapacityProviderOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateCapacityProviderOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateCapacityProviderOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateCapacityProviderOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateCapacityProviderOutputResponse(capacityProvider: \(String(describing: capacityProvider)))"}
}

extension UpdateCapacityProviderOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateCapacityProviderOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.capacityProvider = output.capacityProvider
        } else {
            self.capacityProvider = nil
        }
    }
}

public struct UpdateCapacityProviderOutputResponse: Equatable {
    /// <p>Details about the capacity provider.</p>
    public let capacityProvider: CapacityProvider?

    public init (
        capacityProvider: CapacityProvider? = nil
    )
    {
        self.capacityProvider = capacityProvider
    }
}

struct UpdateCapacityProviderOutputResponseBody: Equatable {
    public let capacityProvider: CapacityProvider?
}

extension UpdateCapacityProviderOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProvider
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let capacityProviderDecoded = try containerValues.decodeIfPresent(CapacityProvider.self, forKey: .capacityProvider)
        capacityProvider = capacityProviderDecoded
    }
}

public struct UpdateClusterInputBodyMiddleware: Middleware {
    public let id: String = "UpdateClusterInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateClusterInput>
    public typealias MOutput = OperationOutput<UpdateClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateClusterOutputError>
}

extension UpdateClusterInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateClusterInput(cluster: \(String(describing: cluster)), configuration: \(String(describing: configuration)), settings: \(String(describing: settings)))"}
}

extension UpdateClusterInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case configuration
        case settings
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let configuration = configuration {
            try encodeContainer.encode(configuration, forKey: .configuration)
        }
        if let settings = settings {
            var settingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .settings)
            for clustersettings0 in settings {
                try settingsContainer.encode(clustersettings0)
            }
        }
    }
}

public struct UpdateClusterInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateClusterInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateClusterInput>
    public typealias MOutput = OperationOutput<UpdateClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateClusterOutputError>
}

public struct UpdateClusterInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateClusterInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateClusterInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateClusterOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateClusterInput>
    public typealias MOutput = OperationOutput<UpdateClusterOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateClusterOutputError>
}

public struct UpdateClusterInput: Equatable {
    /// <p>The name of the cluster to modify the settings for.</p>
    public let cluster: String?
    /// <p>The execute command configuration for the cluster.</p>
    public let configuration: ClusterConfiguration?
    /// <p>The cluster settings for your cluster.</p>
    public let settings: [ClusterSetting]?

    public init (
        cluster: String? = nil,
        configuration: ClusterConfiguration? = nil,
        settings: [ClusterSetting]? = nil
    )
    {
        self.cluster = cluster
        self.configuration = configuration
        self.settings = settings
    }
}

struct UpdateClusterInputBody: Equatable {
    public let cluster: String?
    public let settings: [ClusterSetting]?
    public let configuration: ClusterConfiguration?
}

extension UpdateClusterInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case configuration
        case settings
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let settingsContainer = try containerValues.decodeIfPresent([ClusterSetting?].self, forKey: .settings)
        var settingsDecoded0:[ClusterSetting]? = nil
        if let settingsContainer = settingsContainer {
            settingsDecoded0 = [ClusterSetting]()
            for structure0 in settingsContainer {
                if let structure0 = structure0 {
                    settingsDecoded0?.append(structure0)
                }
            }
        }
        settings = settingsDecoded0
        let configurationDecoded = try containerValues.decodeIfPresent(ClusterConfiguration.self, forKey: .configuration)
        configuration = configurationDecoded
    }
}

extension UpdateClusterOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateClusterOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateClusterOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateClusterOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateClusterOutputResponse(cluster: \(String(describing: cluster)))"}
}

extension UpdateClusterOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateClusterOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.cluster = output.cluster
        } else {
            self.cluster = nil
        }
    }
}

public struct UpdateClusterOutputResponse: Equatable {
    /// <p>Details about the cluster.</p>
    public let cluster: Cluster?

    public init (
        cluster: Cluster? = nil
    )
    {
        self.cluster = cluster
    }
}

struct UpdateClusterOutputResponseBody: Equatable {
    public let cluster: Cluster?
}

extension UpdateClusterOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(Cluster.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

public struct UpdateClusterSettingsInputBodyMiddleware: Middleware {
    public let id: String = "UpdateClusterSettingsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateClusterSettingsInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateClusterSettingsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateClusterSettingsInput>
    public typealias MOutput = OperationOutput<UpdateClusterSettingsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateClusterSettingsOutputError>
}

extension UpdateClusterSettingsInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateClusterSettingsInput(cluster: \(String(describing: cluster)), settings: \(String(describing: settings)))"}
}

extension UpdateClusterSettingsInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case settings
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let settings = settings {
            var settingsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .settings)
            for clustersettings0 in settings {
                try settingsContainer.encode(clustersettings0)
            }
        }
    }
}

public struct UpdateClusterSettingsInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateClusterSettingsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateClusterSettingsInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateClusterSettingsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateClusterSettingsInput>
    public typealias MOutput = OperationOutput<UpdateClusterSettingsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateClusterSettingsOutputError>
}

public struct UpdateClusterSettingsInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateClusterSettingsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateClusterSettingsInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateClusterSettingsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateClusterSettingsInput>
    public typealias MOutput = OperationOutput<UpdateClusterSettingsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateClusterSettingsOutputError>
}

public struct UpdateClusterSettingsInput: Equatable {
    /// <p>The name of the cluster to modify the settings for.</p>
    public let cluster: String?
    /// <p>The setting to use by default for a cluster. This parameter is used to enable CloudWatch
    /// 			Container Insights for a cluster. If this value is specified, it will override the
    /// 				<code>containerInsights</code> value set with <a>PutAccountSetting</a> or
    /// 				<a>PutAccountSettingDefault</a>.</p>
    public let settings: [ClusterSetting]?

    public init (
        cluster: String? = nil,
        settings: [ClusterSetting]? = nil
    )
    {
        self.cluster = cluster
        self.settings = settings
    }
}

struct UpdateClusterSettingsInputBody: Equatable {
    public let cluster: String?
    public let settings: [ClusterSetting]?
}

extension UpdateClusterSettingsInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case settings
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let settingsContainer = try containerValues.decodeIfPresent([ClusterSetting?].self, forKey: .settings)
        var settingsDecoded0:[ClusterSetting]? = nil
        if let settingsContainer = settingsContainer {
            settingsDecoded0 = [ClusterSetting]()
            for structure0 in settingsContainer {
                if let structure0 = structure0 {
                    settingsDecoded0?.append(structure0)
                }
            }
        }
        settings = settingsDecoded0
    }
}

extension UpdateClusterSettingsOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateClusterSettingsOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateClusterSettingsOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateClusterSettingsOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateClusterSettingsOutputResponse(cluster: \(String(describing: cluster)))"}
}

extension UpdateClusterSettingsOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateClusterSettingsOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.cluster = output.cluster
        } else {
            self.cluster = nil
        }
    }
}

public struct UpdateClusterSettingsOutputResponse: Equatable {
    /// <p>Details about the cluster</p>
    public let cluster: Cluster?

    public init (
        cluster: Cluster? = nil
    )
    {
        self.cluster = cluster
    }
}

struct UpdateClusterSettingsOutputResponseBody: Equatable {
    public let cluster: Cluster?
}

extension UpdateClusterSettingsOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(Cluster.self, forKey: .cluster)
        cluster = clusterDecoded
    }
}

public struct UpdateContainerAgentInputBodyMiddleware: Middleware {
    public let id: String = "UpdateContainerAgentInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateContainerAgentInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateContainerAgentOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateContainerAgentInput>
    public typealias MOutput = OperationOutput<UpdateContainerAgentOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateContainerAgentOutputError>
}

extension UpdateContainerAgentInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateContainerAgentInput(cluster: \(String(describing: cluster)), containerInstance: \(String(describing: containerInstance)))"}
}

extension UpdateContainerAgentInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstance = containerInstance {
            try encodeContainer.encode(containerInstance, forKey: .containerInstance)
        }
    }
}

public struct UpdateContainerAgentInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateContainerAgentInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateContainerAgentInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateContainerAgentOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateContainerAgentInput>
    public typealias MOutput = OperationOutput<UpdateContainerAgentOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateContainerAgentOutputError>
}

public struct UpdateContainerAgentInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateContainerAgentInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateContainerAgentInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateContainerAgentOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateContainerAgentInput>
    public typealias MOutput = OperationOutput<UpdateContainerAgentOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateContainerAgentOutputError>
}

public struct UpdateContainerAgentInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that your container instance is
    /// 			running on. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>The container instance ID or full ARN entries for the container instance on which
    /// 			you would like to update the Amazon ECS container agent.</p>
    public let containerInstance: String?

    public init (
        cluster: String? = nil,
        containerInstance: String? = nil
    )
    {
        self.cluster = cluster
        self.containerInstance = containerInstance
    }
}

struct UpdateContainerAgentInputBody: Equatable {
    public let cluster: String?
    public let containerInstance: String?
}

extension UpdateContainerAgentInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstance
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerInstanceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
    }
}

extension UpdateContainerAgentOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateContainerAgentOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "MissingVersionException" : self = .missingVersionException(try MissingVersionException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "NoUpdateAvailableException" : self = .noUpdateAvailableException(try NoUpdateAvailableException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UpdateInProgressException" : self = .updateInProgressException(try UpdateInProgressException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateContainerAgentOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case missingVersionException(MissingVersionException)
    case noUpdateAvailableException(NoUpdateAvailableException)
    case serverException(ServerException)
    case updateInProgressException(UpdateInProgressException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateContainerAgentOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateContainerAgentOutputResponse(containerInstance: \(String(describing: containerInstance)))"}
}

extension UpdateContainerAgentOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateContainerAgentOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.containerInstance = output.containerInstance
        } else {
            self.containerInstance = nil
        }
    }
}

public struct UpdateContainerAgentOutputResponse: Equatable {
    /// <p>The container instance for which the container agent was updated.</p>
    public let containerInstance: ContainerInstance?

    public init (
        containerInstance: ContainerInstance? = nil
    )
    {
        self.containerInstance = containerInstance
    }
}

struct UpdateContainerAgentOutputResponseBody: Equatable {
    public let containerInstance: ContainerInstance?
}

extension UpdateContainerAgentOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerInstance
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstanceDecoded = try containerValues.decodeIfPresent(ContainerInstance.self, forKey: .containerInstance)
        containerInstance = containerInstanceDecoded
    }
}

public struct UpdateContainerInstancesStateInputBodyMiddleware: Middleware {
    public let id: String = "UpdateContainerInstancesStateInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateContainerInstancesStateInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateContainerInstancesStateOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateContainerInstancesStateInput>
    public typealias MOutput = OperationOutput<UpdateContainerInstancesStateOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateContainerInstancesStateOutputError>
}

extension UpdateContainerInstancesStateInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateContainerInstancesStateInput(cluster: \(String(describing: cluster)), containerInstances: \(String(describing: containerInstances)), status: \(String(describing: status)))"}
}

extension UpdateContainerInstancesStateInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstances
        case status
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let containerInstances = containerInstances {
            var containerInstancesContainer = encodeContainer.nestedUnkeyedContainer(forKey: .containerInstances)
            for stringlist0 in containerInstances {
                try containerInstancesContainer.encode(stringlist0)
            }
        }
        if let status = status {
            try encodeContainer.encode(status.rawValue, forKey: .status)
        }
    }
}

public struct UpdateContainerInstancesStateInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateContainerInstancesStateInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateContainerInstancesStateInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateContainerInstancesStateOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateContainerInstancesStateInput>
    public typealias MOutput = OperationOutput<UpdateContainerInstancesStateOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateContainerInstancesStateOutputError>
}

public struct UpdateContainerInstancesStateInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateContainerInstancesStateInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateContainerInstancesStateInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateContainerInstancesStateOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateContainerInstancesStateInput>
    public typealias MOutput = OperationOutput<UpdateContainerInstancesStateOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateContainerInstancesStateOutputError>
}

public struct UpdateContainerInstancesStateInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the container instance to
    /// 			update. If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>A list of container instance IDs or full ARN entries.</p>
    public let containerInstances: [String]?
    /// <p>The container instance state with which to update the container instance. The only
    /// 			valid values for this action are <code>ACTIVE</code> and <code>DRAINING</code>. A
    /// 			container instance can only be updated to <code>DRAINING</code> status once it has
    /// 			reached an <code>ACTIVE</code> state. If a container instance is in
    /// 				<code>REGISTERING</code>, <code>DEREGISTERING</code>, or
    /// 				<code>REGISTRATION_FAILED</code> state you can describe the container instance but
    /// 			will be unable to update the container instance state.</p>
    public let status: ContainerInstanceStatus?

    public init (
        cluster: String? = nil,
        containerInstances: [String]? = nil,
        status: ContainerInstanceStatus? = nil
    )
    {
        self.cluster = cluster
        self.containerInstances = containerInstances
        self.status = status
    }
}

struct UpdateContainerInstancesStateInputBody: Equatable {
    public let cluster: String?
    public let containerInstances: [String]?
    public let status: ContainerInstanceStatus?
}

extension UpdateContainerInstancesStateInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case containerInstances
        case status
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let containerInstancesContainer = try containerValues.decodeIfPresent([String?].self, forKey: .containerInstances)
        var containerInstancesDecoded0:[String]? = nil
        if let containerInstancesContainer = containerInstancesContainer {
            containerInstancesDecoded0 = [String]()
            for string0 in containerInstancesContainer {
                if let string0 = string0 {
                    containerInstancesDecoded0?.append(string0)
                }
            }
        }
        containerInstances = containerInstancesDecoded0
        let statusDecoded = try containerValues.decodeIfPresent(ContainerInstanceStatus.self, forKey: .status)
        status = statusDecoded
    }
}

extension UpdateContainerInstancesStateOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateContainerInstancesStateOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateContainerInstancesStateOutputError: Swift.Error, Equatable {
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateContainerInstancesStateOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateContainerInstancesStateOutputResponse(containerInstances: \(String(describing: containerInstances)), failures: \(String(describing: failures)))"}
}

extension UpdateContainerInstancesStateOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateContainerInstancesStateOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.containerInstances = output.containerInstances
            self.failures = output.failures
        } else {
            self.containerInstances = nil
            self.failures = nil
        }
    }
}

public struct UpdateContainerInstancesStateOutputResponse: Equatable {
    /// <p>The list of container instances.</p>
    public let containerInstances: [ContainerInstance]?
    /// <p>Any failures associated with the call.</p>
    public let failures: [Failure]?

    public init (
        containerInstances: [ContainerInstance]? = nil,
        failures: [Failure]? = nil
    )
    {
        self.containerInstances = containerInstances
        self.failures = failures
    }
}

struct UpdateContainerInstancesStateOutputResponseBody: Equatable {
    public let containerInstances: [ContainerInstance]?
    public let failures: [Failure]?
}

extension UpdateContainerInstancesStateOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case containerInstances
        case failures
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let containerInstancesContainer = try containerValues.decodeIfPresent([ContainerInstance?].self, forKey: .containerInstances)
        var containerInstancesDecoded0:[ContainerInstance]? = nil
        if let containerInstancesContainer = containerInstancesContainer {
            containerInstancesDecoded0 = [ContainerInstance]()
            for structure0 in containerInstancesContainer {
                if let structure0 = structure0 {
                    containerInstancesDecoded0?.append(structure0)
                }
            }
        }
        containerInstances = containerInstancesDecoded0
        let failuresContainer = try containerValues.decodeIfPresent([Failure?].self, forKey: .failures)
        var failuresDecoded0:[Failure]? = nil
        if let failuresContainer = failuresContainer {
            failuresDecoded0 = [Failure]()
            for structure0 in failuresContainer {
                if let structure0 = structure0 {
                    failuresDecoded0?.append(structure0)
                }
            }
        }
        failures = failuresDecoded0
    }
}

extension UpdateInProgressException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateInProgressException(message: \(String(describing: message)))"}
}

extension UpdateInProgressException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateInProgressExceptionBody = try responseDecoder.decode(responseBody: data)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>There is already a current Amazon ECS container agent update in progress on the specified
/// 			container instance. If the container agent becomes disconnected while it is in a
/// 			transitional stage, such as <code>PENDING</code> or <code>STAGING</code>, the update
/// 			process can get stuck in that state. However, when the agent reconnects, it resumes
/// 			where it stopped previously.</p>
public struct UpdateInProgressException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct UpdateInProgressExceptionBody: Equatable {
    public let message: String?
}

extension UpdateInProgressExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

public struct UpdateServiceInputBodyMiddleware: Middleware {
    public let id: String = "UpdateServiceInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateServiceInput>
    public typealias MOutput = OperationOutput<UpdateServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateServiceOutputError>
}

extension UpdateServiceInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateServiceInput(capacityProviderStrategy: \(String(describing: capacityProviderStrategy)), cluster: \(String(describing: cluster)), deploymentConfiguration: \(String(describing: deploymentConfiguration)), desiredCount: \(String(describing: desiredCount)), enableExecuteCommand: \(String(describing: enableExecuteCommand)), forceNewDeployment: \(String(describing: forceNewDeployment)), healthCheckGracePeriodSeconds: \(String(describing: healthCheckGracePeriodSeconds)), networkConfiguration: \(String(describing: networkConfiguration)), placementConstraints: \(String(describing: placementConstraints)), placementStrategy: \(String(describing: placementStrategy)), platformVersion: \(String(describing: platformVersion)), service: \(String(describing: service)), taskDefinition: \(String(describing: taskDefinition)))"}
}

extension UpdateServiceInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case cluster
        case deploymentConfiguration
        case desiredCount
        case enableExecuteCommand
        case forceNewDeployment
        case healthCheckGracePeriodSeconds
        case networkConfiguration
        case placementConstraints
        case placementStrategy
        case platformVersion
        case service
        case taskDefinition
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let capacityProviderStrategy = capacityProviderStrategy {
            var capacityProviderStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .capacityProviderStrategy)
            for capacityproviderstrategy0 in capacityProviderStrategy {
                try capacityProviderStrategyContainer.encode(capacityproviderstrategy0)
            }
        }
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let deploymentConfiguration = deploymentConfiguration {
            try encodeContainer.encode(deploymentConfiguration, forKey: .deploymentConfiguration)
        }
        if let desiredCount = desiredCount {
            try encodeContainer.encode(desiredCount, forKey: .desiredCount)
        }
        if let enableExecuteCommand = enableExecuteCommand {
            try encodeContainer.encode(enableExecuteCommand, forKey: .enableExecuteCommand)
        }
        if forceNewDeployment != false {
            try encodeContainer.encode(forceNewDeployment, forKey: .forceNewDeployment)
        }
        if let healthCheckGracePeriodSeconds = healthCheckGracePeriodSeconds {
            try encodeContainer.encode(healthCheckGracePeriodSeconds, forKey: .healthCheckGracePeriodSeconds)
        }
        if let networkConfiguration = networkConfiguration {
            try encodeContainer.encode(networkConfiguration, forKey: .networkConfiguration)
        }
        if let placementConstraints = placementConstraints {
            var placementConstraintsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementConstraints)
            for placementconstraints0 in placementConstraints {
                try placementConstraintsContainer.encode(placementconstraints0)
            }
        }
        if let placementStrategy = placementStrategy {
            var placementStrategyContainer = encodeContainer.nestedUnkeyedContainer(forKey: .placementStrategy)
            for placementstrategies0 in placementStrategy {
                try placementStrategyContainer.encode(placementstrategies0)
            }
        }
        if let platformVersion = platformVersion {
            try encodeContainer.encode(platformVersion, forKey: .platformVersion)
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
        if let taskDefinition = taskDefinition {
            try encodeContainer.encode(taskDefinition, forKey: .taskDefinition)
        }
    }
}

public struct UpdateServiceInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateServiceInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateServiceInput>
    public typealias MOutput = OperationOutput<UpdateServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateServiceOutputError>
}

public struct UpdateServiceInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateServiceInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateServiceInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateServiceOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateServiceInput>
    public typealias MOutput = OperationOutput<UpdateServiceOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateServiceOutputError>
}

public struct UpdateServiceInput: Equatable {
    /// <p>The capacity provider strategy to update the service to use.</p>
    /// 		       <p>If the service is using the default capacity provider strategy for the cluster, the
    /// 			service can be updated to use one or more capacity providers as opposed to the default
    /// 			capacity provider strategy. However, when a service is using a capacity provider
    /// 			strategy that is not the default capacity provider strategy, the service cannot be
    /// 			updated to use the cluster's default capacity provider strategy.</p>
    /// 		       <p>A capacity provider strategy consists of one or more capacity providers along with the
    /// 				<code>base</code> and <code>weight</code> to assign to them. A capacity provider
    /// 			must be associated with the cluster to be used in a capacity provider strategy. The
    /// 				<a>PutClusterCapacityProviders</a> API is used to associate a capacity
    /// 			provider with a cluster. Only capacity providers with an <code>ACTIVE</code> or
    /// 				<code>UPDATING</code> status can be used.</p>
    /// 		       <p>If specifying a capacity provider that uses an Auto Scaling group, the capacity
    /// 			provider must already be created. New capacity providers can be created with the <a>CreateCapacityProvider</a> API operation.</p>
    /// 		       <p>To use a AWS Fargate capacity provider, specify either the <code>FARGATE</code> or
    /// 				<code>FARGATE_SPOT</code> capacity providers. The AWS Fargate capacity providers are
    /// 			available to all accounts and only need to be associated with a cluster to be
    /// 			used.</p>
    /// 		       <p>The <a>PutClusterCapacityProviders</a> API operation is used to update the
    /// 			list of available capacity providers for a cluster after the cluster is created.</p>
    /// 		       <p></p>
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that your service is running on.
    /// 			If you do not specify a cluster, the default cluster is assumed.</p>
    public let cluster: String?
    /// <p>Optional deployment parameters that control how many tasks run during the deployment
    /// 			and the ordering of stopping and starting tasks.</p>
    public let deploymentConfiguration: DeploymentConfiguration?
    /// <p>The number of instantiations of the task to place and keep running in your
    /// 			service.</p>
    public let desiredCount: Int?
    /// <p>If <code>true</code>, this enables execute command functionality on all task
    /// 			containers.</p>
    /// 		       <p>If you do not want to override the value that was set when the service was created,
    /// 			you can set this to <code>null</code> when performing this action.</p>
    public let enableExecuteCommand: Bool?
    /// <p>Whether to force a new deployment of the service. Deployments are not forced by
    /// 			default. You can use this option to trigger a new deployment with no service definition
    /// 			changes. For example, you can update a service's tasks to use a newer Docker image with
    /// 			the same image/tag combination (<code>my_image:latest</code>) or to roll Fargate tasks
    /// 			onto a newer platform version.</p>
    public let forceNewDeployment: Bool
    /// <p>The period of time, in seconds, that the Amazon ECS service scheduler should ignore
    /// 			unhealthy Elastic Load Balancing target health checks after a task has first started. This is only valid
    /// 			if your service is configured to use a load balancer. If your service's tasks take a
    /// 			while to start and respond to Elastic Load Balancing health checks, you can specify a health check grace
    /// 			period of up to 2,147,483,647 seconds. During that time, the Amazon ECS service
    /// 			scheduler ignores the Elastic Load Balancing health check status. This grace period can prevent the ECS
    /// 			service scheduler from marking tasks as unhealthy and stopping them before they have
    /// 			time to come up.</p>
    public let healthCheckGracePeriodSeconds: Int?
    /// <p>An object representing the network configuration for the service.</p>
    public let networkConfiguration: NetworkConfiguration?
    /// <p>An array of task placement constraint objects to update the service to use. If no
    /// 			value is specified, the existing placement constraints for the service will remain
    /// 			unchanged. If this value is specified, it will override any existing placement
    /// 			constraints defined for the service. To remove all existing placement constraints,
    /// 			specify an empty array.</p>
    /// 		       <p>You can specify a maximum of 10 constraints per task (this limit includes constraints
    /// 			in the task definition and those specified at runtime).</p>
    public let placementConstraints: [PlacementConstraint]?
    /// <p>The task placement strategy objects to update the service to use. If no value is
    /// 			specified, the existing placement strategy for the service will remain unchanged. If
    /// 			this value is specified, it will override the existing placement strategy defined for
    /// 			the service. To remove an existing placement strategy, specify an empty object.</p>
    /// 		       <p>You can specify a maximum of five strategy rules per service.</p>
    public let placementStrategy: [PlacementStrategy]?
    /// <p>The platform version on which your tasks in the service are running. A platform
    /// 			version is only specified for tasks using the Fargate launch type. If a
    /// 			platform version is not specified, the <code>LATEST</code> platform version is used by
    /// 			default. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/platform_versions.html">AWS Fargate Platform
    /// 				Versions</a> in the <i>Amazon Elastic Container Service Developer Guide</i>.</p>
    public let platformVersion: String?
    /// <p>The name of the service to update.</p>
    public let service: String?
    /// <p>The <code>family</code> and <code>revision</code> (<code>family:revision</code>) or
    /// 			full ARN of the task definition to run in your service. If a <code>revision</code> is
    /// 			not specified, the latest <code>ACTIVE</code> revision is used. If you modify the task
    /// 			definition with <code>UpdateService</code>, Amazon ECS spawns a task with the new version of
    /// 			the task definition and then stops an old task after the new version is running.</p>
    public let taskDefinition: String?

    public init (
        capacityProviderStrategy: [CapacityProviderStrategyItem]? = nil,
        cluster: String? = nil,
        deploymentConfiguration: DeploymentConfiguration? = nil,
        desiredCount: Int? = nil,
        enableExecuteCommand: Bool? = nil,
        forceNewDeployment: Bool = false,
        healthCheckGracePeriodSeconds: Int? = nil,
        networkConfiguration: NetworkConfiguration? = nil,
        placementConstraints: [PlacementConstraint]? = nil,
        placementStrategy: [PlacementStrategy]? = nil,
        platformVersion: String? = nil,
        service: String? = nil,
        taskDefinition: String? = nil
    )
    {
        self.capacityProviderStrategy = capacityProviderStrategy
        self.cluster = cluster
        self.deploymentConfiguration = deploymentConfiguration
        self.desiredCount = desiredCount
        self.enableExecuteCommand = enableExecuteCommand
        self.forceNewDeployment = forceNewDeployment
        self.healthCheckGracePeriodSeconds = healthCheckGracePeriodSeconds
        self.networkConfiguration = networkConfiguration
        self.placementConstraints = placementConstraints
        self.placementStrategy = placementStrategy
        self.platformVersion = platformVersion
        self.service = service
        self.taskDefinition = taskDefinition
    }
}

struct UpdateServiceInputBody: Equatable {
    public let cluster: String?
    public let service: String?
    public let desiredCount: Int?
    public let taskDefinition: String?
    public let capacityProviderStrategy: [CapacityProviderStrategyItem]?
    public let deploymentConfiguration: DeploymentConfiguration?
    public let networkConfiguration: NetworkConfiguration?
    public let placementConstraints: [PlacementConstraint]?
    public let placementStrategy: [PlacementStrategy]?
    public let platformVersion: String?
    public let forceNewDeployment: Bool
    public let healthCheckGracePeriodSeconds: Int?
    public let enableExecuteCommand: Bool?
}

extension UpdateServiceInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case capacityProviderStrategy
        case cluster
        case deploymentConfiguration
        case desiredCount
        case enableExecuteCommand
        case forceNewDeployment
        case healthCheckGracePeriodSeconds
        case networkConfiguration
        case placementConstraints
        case placementStrategy
        case platformVersion
        case service
        case taskDefinition
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let desiredCountDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .desiredCount)
        desiredCount = desiredCountDecoded
        let taskDefinitionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskDefinition)
        taskDefinition = taskDefinitionDecoded
        let capacityProviderStrategyContainer = try containerValues.decodeIfPresent([CapacityProviderStrategyItem?].self, forKey: .capacityProviderStrategy)
        var capacityProviderStrategyDecoded0:[CapacityProviderStrategyItem]? = nil
        if let capacityProviderStrategyContainer = capacityProviderStrategyContainer {
            capacityProviderStrategyDecoded0 = [CapacityProviderStrategyItem]()
            for structure0 in capacityProviderStrategyContainer {
                if let structure0 = structure0 {
                    capacityProviderStrategyDecoded0?.append(structure0)
                }
            }
        }
        capacityProviderStrategy = capacityProviderStrategyDecoded0
        let deploymentConfigurationDecoded = try containerValues.decodeIfPresent(DeploymentConfiguration.self, forKey: .deploymentConfiguration)
        deploymentConfiguration = deploymentConfigurationDecoded
        let networkConfigurationDecoded = try containerValues.decodeIfPresent(NetworkConfiguration.self, forKey: .networkConfiguration)
        networkConfiguration = networkConfigurationDecoded
        let placementConstraintsContainer = try containerValues.decodeIfPresent([PlacementConstraint?].self, forKey: .placementConstraints)
        var placementConstraintsDecoded0:[PlacementConstraint]? = nil
        if let placementConstraintsContainer = placementConstraintsContainer {
            placementConstraintsDecoded0 = [PlacementConstraint]()
            for structure0 in placementConstraintsContainer {
                if let structure0 = structure0 {
                    placementConstraintsDecoded0?.append(structure0)
                }
            }
        }
        placementConstraints = placementConstraintsDecoded0
        let placementStrategyContainer = try containerValues.decodeIfPresent([PlacementStrategy?].self, forKey: .placementStrategy)
        var placementStrategyDecoded0:[PlacementStrategy]? = nil
        if let placementStrategyContainer = placementStrategyContainer {
            placementStrategyDecoded0 = [PlacementStrategy]()
            for structure0 in placementStrategyContainer {
                if let structure0 = structure0 {
                    placementStrategyDecoded0?.append(structure0)
                }
            }
        }
        placementStrategy = placementStrategyDecoded0
        let platformVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platformVersion)
        platformVersion = platformVersionDecoded
        let forceNewDeploymentDecoded = try containerValues.decode(Bool.self, forKey: .forceNewDeployment)
        forceNewDeployment = forceNewDeploymentDecoded
        let healthCheckGracePeriodSecondsDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .healthCheckGracePeriodSeconds)
        healthCheckGracePeriodSeconds = healthCheckGracePeriodSecondsDecoded
        let enableExecuteCommandDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .enableExecuteCommand)
        enableExecuteCommand = enableExecuteCommandDecoded
    }
}

extension UpdateServiceOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateServiceOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformTaskDefinitionIncompatibilityException" : self = .platformTaskDefinitionIncompatibilityException(try PlatformTaskDefinitionIncompatibilityException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "PlatformUnknownException" : self = .platformUnknownException(try PlatformUnknownException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotActiveException" : self = .serviceNotActiveException(try ServiceNotActiveException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateServiceOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case platformTaskDefinitionIncompatibilityException(PlatformTaskDefinitionIncompatibilityException)
    case platformUnknownException(PlatformUnknownException)
    case serverException(ServerException)
    case serviceNotActiveException(ServiceNotActiveException)
    case serviceNotFoundException(ServiceNotFoundException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateServiceOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateServiceOutputResponse(service: \(String(describing: service)))"}
}

extension UpdateServiceOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateServiceOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.service = output.service
        } else {
            self.service = nil
        }
    }
}

public struct UpdateServiceOutputResponse: Equatable {
    /// <p>The full description of your service following the update call.</p>
    public let service: Service?

    public init (
        service: Service? = nil
    )
    {
        self.service = service
    }
}

struct UpdateServiceOutputResponseBody: Equatable {
    public let service: Service?
}

extension UpdateServiceOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case service
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let serviceDecoded = try containerValues.decodeIfPresent(Service.self, forKey: .service)
        service = serviceDecoded
    }
}

public struct UpdateServicePrimaryTaskSetInputBodyMiddleware: Middleware {
    public let id: String = "UpdateServicePrimaryTaskSetInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateServicePrimaryTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateServicePrimaryTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateServicePrimaryTaskSetInput>
    public typealias MOutput = OperationOutput<UpdateServicePrimaryTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateServicePrimaryTaskSetOutputError>
}

extension UpdateServicePrimaryTaskSetInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateServicePrimaryTaskSetInput(cluster: \(String(describing: cluster)), primaryTaskSet: \(String(describing: primaryTaskSet)), service: \(String(describing: service)))"}
}

extension UpdateServicePrimaryTaskSetInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case primaryTaskSet
        case service
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let primaryTaskSet = primaryTaskSet {
            try encodeContainer.encode(primaryTaskSet, forKey: .primaryTaskSet)
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
    }
}

public struct UpdateServicePrimaryTaskSetInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateServicePrimaryTaskSetInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateServicePrimaryTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateServicePrimaryTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateServicePrimaryTaskSetInput>
    public typealias MOutput = OperationOutput<UpdateServicePrimaryTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateServicePrimaryTaskSetOutputError>
}

public struct UpdateServicePrimaryTaskSetInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateServicePrimaryTaskSetInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateServicePrimaryTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateServicePrimaryTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateServicePrimaryTaskSetInput>
    public typealias MOutput = OperationOutput<UpdateServicePrimaryTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateServicePrimaryTaskSetOutputError>
}

public struct UpdateServicePrimaryTaskSetInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task
    /// 			set exists in.</p>
    public let cluster: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the task set to set as the primary task set in the
    /// 			deployment.</p>
    public let primaryTaskSet: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the service that the task set exists in.</p>
    public let service: String?

    public init (
        cluster: String? = nil,
        primaryTaskSet: String? = nil,
        service: String? = nil
    )
    {
        self.cluster = cluster
        self.primaryTaskSet = primaryTaskSet
        self.service = service
    }
}

struct UpdateServicePrimaryTaskSetInputBody: Equatable {
    public let cluster: String?
    public let service: String?
    public let primaryTaskSet: String?
}

extension UpdateServicePrimaryTaskSetInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case primaryTaskSet
        case service
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let primaryTaskSetDecoded = try containerValues.decodeIfPresent(String.self, forKey: .primaryTaskSet)
        primaryTaskSet = primaryTaskSetDecoded
    }
}

extension UpdateServicePrimaryTaskSetOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateServicePrimaryTaskSetOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotActiveException" : self = .serviceNotActiveException(try ServiceNotActiveException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "TaskSetNotFoundException" : self = .taskSetNotFoundException(try TaskSetNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateServicePrimaryTaskSetOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case serviceNotActiveException(ServiceNotActiveException)
    case serviceNotFoundException(ServiceNotFoundException)
    case taskSetNotFoundException(TaskSetNotFoundException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateServicePrimaryTaskSetOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateServicePrimaryTaskSetOutputResponse(taskSet: \(String(describing: taskSet)))"}
}

extension UpdateServicePrimaryTaskSetOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateServicePrimaryTaskSetOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.taskSet = output.taskSet
        } else {
            self.taskSet = nil
        }
    }
}

public struct UpdateServicePrimaryTaskSetOutputResponse: Equatable {
    /// <p>Details about the task set.</p>
    public let taskSet: TaskSet?

    public init (
        taskSet: TaskSet? = nil
    )
    {
        self.taskSet = taskSet
    }
}

struct UpdateServicePrimaryTaskSetOutputResponseBody: Equatable {
    public let taskSet: TaskSet?
}

extension UpdateServicePrimaryTaskSetOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case taskSet
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskSetDecoded = try containerValues.decodeIfPresent(TaskSet.self, forKey: .taskSet)
        taskSet = taskSetDecoded
    }
}

public struct UpdateTaskSetInputBodyMiddleware: Middleware {
    public let id: String = "UpdateTaskSetInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            if try !input.operationInput.allPropertiesAreNull() {
                let encoder = context.getEncoder()
                let data = try encoder.encode(input.operationInput)
                let body = HttpBody.data(data)
                input.builder.withBody(body)
            }
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateTaskSetInput>
    public typealias MOutput = OperationOutput<UpdateTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateTaskSetOutputError>
}

extension UpdateTaskSetInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateTaskSetInput(cluster: \(String(describing: cluster)), scale: \(String(describing: scale)), service: \(String(describing: service)), taskSet: \(String(describing: taskSet)))"}
}

extension UpdateTaskSetInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cluster
        case scale
        case service
        case taskSet
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cluster = cluster {
            try encodeContainer.encode(cluster, forKey: .cluster)
        }
        if let scale = scale {
            try encodeContainer.encode(scale, forKey: .scale)
        }
        if let service = service {
            try encodeContainer.encode(service, forKey: .service)
        }
        if let taskSet = taskSet {
            try encodeContainer.encode(taskSet, forKey: .taskSet)
        }
    }
}

public struct UpdateTaskSetInputHeadersMiddleware: Middleware {
    public let id: String = "UpdateTaskSetInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateTaskSetInput>
    public typealias MOutput = OperationOutput<UpdateTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateTaskSetOutputError>
}

public struct UpdateTaskSetInputQueryItemMiddleware: Middleware {
    public let id: String = "UpdateTaskSetInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UpdateTaskSetInput>,
                  next: H) -> Swift.Result<OperationOutput<UpdateTaskSetOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UpdateTaskSetInput>
    public typealias MOutput = OperationOutput<UpdateTaskSetOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UpdateTaskSetOutputError>
}

public struct UpdateTaskSetInput: Equatable {
    /// <p>The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service that the task
    /// 			set exists in.</p>
    public let cluster: String?
    /// <p>A floating-point percentage of the desired number of tasks to place and keep running
    /// 			in the task set.</p>
    public let scale: Scale?
    /// <p>The short name or full Amazon Resource Name (ARN) of the service that the task set exists in.</p>
    public let service: String?
    /// <p>The short name or full Amazon Resource Name (ARN) of the task set to update.</p>
    public let taskSet: String?

    public init (
        cluster: String? = nil,
        scale: Scale? = nil,
        service: String? = nil,
        taskSet: String? = nil
    )
    {
        self.cluster = cluster
        self.scale = scale
        self.service = service
        self.taskSet = taskSet
    }
}

struct UpdateTaskSetInputBody: Equatable {
    public let cluster: String?
    public let service: String?
    public let taskSet: String?
    public let scale: Scale?
}

extension UpdateTaskSetInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case cluster
        case scale
        case service
        case taskSet
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clusterDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cluster)
        cluster = clusterDecoded
        let serviceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .service)
        service = serviceDecoded
        let taskSetDecoded = try containerValues.decodeIfPresent(String.self, forKey: .taskSet)
        taskSet = taskSetDecoded
        let scaleDecoded = try containerValues.decodeIfPresent(Scale.self, forKey: .scale)
        scale = scaleDecoded
    }
}

extension UpdateTaskSetOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UpdateTaskSetOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "AccessDeniedException" : self = .accessDeniedException(try AccessDeniedException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClientException" : self = .clientException(try ClientException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ClusterNotFoundException" : self = .clusterNotFoundException(try ClusterNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "InvalidParameterException" : self = .invalidParameterException(try InvalidParameterException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServerException" : self = .serverException(try ServerException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotActiveException" : self = .serviceNotActiveException(try ServiceNotActiveException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "ServiceNotFoundException" : self = .serviceNotFoundException(try ServiceNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "TaskSetNotFoundException" : self = .taskSetNotFoundException(try TaskSetNotFoundException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        case "UnsupportedFeatureException" : self = .unsupportedFeatureException(try UnsupportedFeatureException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UpdateTaskSetOutputError: Swift.Error, Equatable {
    case accessDeniedException(AccessDeniedException)
    case clientException(ClientException)
    case clusterNotFoundException(ClusterNotFoundException)
    case invalidParameterException(InvalidParameterException)
    case serverException(ServerException)
    case serviceNotActiveException(ServiceNotActiveException)
    case serviceNotFoundException(ServiceNotFoundException)
    case taskSetNotFoundException(TaskSetNotFoundException)
    case unsupportedFeatureException(UnsupportedFeatureException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UpdateTaskSetOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UpdateTaskSetOutputResponse(taskSet: \(String(describing: taskSet)))"}
}

extension UpdateTaskSetOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .stream(let reader) = httpResponse.body,
            let responseDecoder = decoder {
            let data = reader.toBytes().toData()
            let output: UpdateTaskSetOutputResponseBody = try responseDecoder.decode(responseBody: data)
            self.taskSet = output.taskSet
        } else {
            self.taskSet = nil
        }
    }
}

public struct UpdateTaskSetOutputResponse: Equatable {
    /// <p>Details about the task set.</p>
    public let taskSet: TaskSet?

    public init (
        taskSet: TaskSet? = nil
    )
    {
        self.taskSet = taskSet
    }
}

struct UpdateTaskSetOutputResponseBody: Equatable {
    public let taskSet: TaskSet?
}

extension UpdateTaskSetOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case taskSet
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let taskSetDecoded = try containerValues.decodeIfPresent(TaskSet.self, forKey: .taskSet)
        taskSet = taskSetDecoded
    }
}

extension VersionInfo: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case agentHash
        case agentVersion
        case dockerVersion
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let agentHash = agentHash {
            try encodeContainer.encode(agentHash, forKey: .agentHash)
        }
        if let agentVersion = agentVersion {
            try encodeContainer.encode(agentVersion, forKey: .agentVersion)
        }
        if let dockerVersion = dockerVersion {
            try encodeContainer.encode(dockerVersion, forKey: .dockerVersion)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let agentVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .agentVersion)
        agentVersion = agentVersionDecoded
        let agentHashDecoded = try containerValues.decodeIfPresent(String.self, forKey: .agentHash)
        agentHash = agentHashDecoded
        let dockerVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .dockerVersion)
        dockerVersion = dockerVersionDecoded
    }
}

extension VersionInfo: CustomDebugStringConvertible {
    public var debugDescription: String {
        "VersionInfo(agentHash: \(String(describing: agentHash)), agentVersion: \(String(describing: agentVersion)), dockerVersion: \(String(describing: dockerVersion)))"}
}

/// <p>The Docker and Amazon ECS container agent version information about a container
/// 			instance.</p>
public struct VersionInfo: Equatable {
    /// <p>The Git commit hash for the Amazon ECS container agent build on the <a href="https://github.com/aws/amazon-ecs-agent/commits/master">amazon-ecs-agent
    /// 			</a> GitHub repository.</p>
    public let agentHash: String?
    /// <p>The version number of the Amazon ECS container agent.</p>
    public let agentVersion: String?
    /// <p>The Docker version running on the container instance.</p>
    public let dockerVersion: String?

    public init (
        agentHash: String? = nil,
        agentVersion: String? = nil,
        dockerVersion: String? = nil
    )
    {
        self.agentHash = agentHash
        self.agentVersion = agentVersion
        self.dockerVersion = dockerVersion
    }
}

extension Volume: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case dockerVolumeConfiguration
        case efsVolumeConfiguration
        case fsxWindowsFileServerVolumeConfiguration
        case host
        case name
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let dockerVolumeConfiguration = dockerVolumeConfiguration {
            try encodeContainer.encode(dockerVolumeConfiguration, forKey: .dockerVolumeConfiguration)
        }
        if let efsVolumeConfiguration = efsVolumeConfiguration {
            try encodeContainer.encode(efsVolumeConfiguration, forKey: .efsVolumeConfiguration)
        }
        if let fsxWindowsFileServerVolumeConfiguration = fsxWindowsFileServerVolumeConfiguration {
            try encodeContainer.encode(fsxWindowsFileServerVolumeConfiguration, forKey: .fsxWindowsFileServerVolumeConfiguration)
        }
        if let host = host {
            try encodeContainer.encode(host, forKey: .host)
        }
        if let name = name {
            try encodeContainer.encode(name, forKey: .name)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let hostDecoded = try containerValues.decodeIfPresent(HostVolumeProperties.self, forKey: .host)
        host = hostDecoded
        let dockerVolumeConfigurationDecoded = try containerValues.decodeIfPresent(DockerVolumeConfiguration.self, forKey: .dockerVolumeConfiguration)
        dockerVolumeConfiguration = dockerVolumeConfigurationDecoded
        let efsVolumeConfigurationDecoded = try containerValues.decodeIfPresent(EFSVolumeConfiguration.self, forKey: .efsVolumeConfiguration)
        efsVolumeConfiguration = efsVolumeConfigurationDecoded
        let fsxWindowsFileServerVolumeConfigurationDecoded = try containerValues.decodeIfPresent(FSxWindowsFileServerVolumeConfiguration.self, forKey: .fsxWindowsFileServerVolumeConfiguration)
        fsxWindowsFileServerVolumeConfiguration = fsxWindowsFileServerVolumeConfigurationDecoded
    }
}

extension Volume: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Volume(dockerVolumeConfiguration: \(String(describing: dockerVolumeConfiguration)), efsVolumeConfiguration: \(String(describing: efsVolumeConfiguration)), fsxWindowsFileServerVolumeConfiguration: \(String(describing: fsxWindowsFileServerVolumeConfiguration)), host: \(String(describing: host)), name: \(String(describing: name)))"}
}

/// <p>A data volume used in a task definition. For tasks that use the Amazon Elastic File
/// 			System (Amazon EFS), specify an <code>efsVolumeConfiguration</code>. For Windows tasks
/// 			that use Amazon FSx for Windows File Server file system, specify a
/// 				<code>fsxWindowsFileServerVolumeConfiguration</code>. For tasks that use a Docker
/// 			volume, specify a <code>DockerVolumeConfiguration</code>. For tasks that use a bind
/// 			mount host volume, specify a <code>host</code> and optional <code>sourcePath</code>. For
/// 			more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/using_data_volumes.html">Using Data Volumes in
/// 				Tasks</a>.</p>
public struct Volume: Equatable {
    /// <p>This parameter is specified when you are using Docker volumes.</p>
    /// 		       <p>Windows containers only support the use of the <code>local</code> driver. To use bind
    /// 			mounts, specify the <code>host</code> parameter instead.</p>
    /// 		       <note>
    /// 			         <p>Docker volumes are not supported by tasks run on AWS Fargate.</p>
    /// 		       </note>
    public let dockerVolumeConfiguration: DockerVolumeConfiguration?
    /// <p>This parameter is specified when you are using an Amazon Elastic File System file system for task
    /// 			storage.</p>
    public let efsVolumeConfiguration: EFSVolumeConfiguration?
    /// <p>This parameter is specified when you are using Amazon FSx for Windows File Server file system for task
    /// 			storage.</p>
    public let fsxWindowsFileServerVolumeConfiguration: FSxWindowsFileServerVolumeConfiguration?
    /// <p>This parameter is specified when you are using bind mount host volumes. The contents
    /// 			of the <code>host</code> parameter determine whether your bind mount host volume
    /// 			persists on the host container instance and where it is stored. If the <code>host</code>
    /// 			parameter is empty, then the Docker daemon assigns a host path for your data volume.
    /// 			However, the data is not guaranteed to persist after the containers associated with it
    /// 			stop running.</p>
    /// 		       <p>Windows containers can mount whole directories on the same drive as
    /// 				<code>$env:ProgramData</code>. Windows containers cannot mount directories on a
    /// 			different drive, and mount point cannot be across drives. For example, you can mount
    /// 				<code>C:\my\path:C:\my\path</code> and <code>D:\:D:\</code>, but not
    /// 				<code>D:\my\path:C:\my\path</code> or <code>D:\:C:\my\path</code>.</p>
    public let host: HostVolumeProperties?
    /// <p>The name of the volume. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are allowed. This name is referenced in the
    /// 				<code>sourceVolume</code> parameter of container definition
    /// 			<code>mountPoints</code>.</p>
    public let name: String?

    public init (
        dockerVolumeConfiguration: DockerVolumeConfiguration? = nil,
        efsVolumeConfiguration: EFSVolumeConfiguration? = nil,
        fsxWindowsFileServerVolumeConfiguration: FSxWindowsFileServerVolumeConfiguration? = nil,
        host: HostVolumeProperties? = nil,
        name: String? = nil
    )
    {
        self.dockerVolumeConfiguration = dockerVolumeConfiguration
        self.efsVolumeConfiguration = efsVolumeConfiguration
        self.fsxWindowsFileServerVolumeConfiguration = fsxWindowsFileServerVolumeConfiguration
        self.host = host
        self.name = name
    }
}

extension VolumeFrom: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case readOnly
        case sourceContainer
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let readOnly = readOnly {
            try encodeContainer.encode(readOnly, forKey: .readOnly)
        }
        if let sourceContainer = sourceContainer {
            try encodeContainer.encode(sourceContainer, forKey: .sourceContainer)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let sourceContainerDecoded = try containerValues.decodeIfPresent(String.self, forKey: .sourceContainer)
        sourceContainer = sourceContainerDecoded
        let readOnlyDecoded = try containerValues.decodeIfPresent(Bool.self, forKey: .readOnly)
        readOnly = readOnlyDecoded
    }
}

extension VolumeFrom: CustomDebugStringConvertible {
    public var debugDescription: String {
        "VolumeFrom(readOnly: \(String(describing: readOnly)), sourceContainer: \(String(describing: sourceContainer)))"}
}

/// <p>Details on a data volume from another container in the same task definition.</p>
public struct VolumeFrom: Equatable {
    /// <p>If this value is <code>true</code>, the container has read-only access to the volume.
    /// 			If this value is <code>false</code>, then the container can write to the volume. The
    /// 			default value is <code>false</code>.</p>
    public let readOnly: Bool?
    /// <p>The name of another container within the same task definition from which to mount
    /// 			volumes.</p>
    public let sourceContainer: String?

    public init (
        readOnly: Bool? = nil,
        sourceContainer: String? = nil
    )
    {
        self.readOnly = readOnly
        self.sourceContainer = sourceContainer
    }
}
