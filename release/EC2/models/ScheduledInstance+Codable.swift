// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension ScheduledInstance: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case availabilityZone = "availabilityZone"
        case createDate = "createDate"
        case hourlyPrice = "hourlyPrice"
        case instanceCount = "instanceCount"
        case instanceType = "instanceType"
        case networkPlatform = "networkPlatform"
        case nextSlotStartTime = "nextSlotStartTime"
        case platform = "platform"
        case previousSlotEndTime = "previousSlotEndTime"
        case recurrence = "recurrence"
        case scheduledInstanceId = "scheduledInstanceId"
        case slotDurationInHours = "slotDurationInHours"
        case termEndDate = "termEndDate"
        case termStartDate = "termStartDate"
        case totalScheduledInstanceHours = "totalScheduledInstanceHours"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let availabilityZone = availabilityZone {
            try container.encode(availabilityZone, forKey: Key("AvailabilityZone"))
        }
        if let createDate = createDate {
            try container.encode(TimestampWrapper(createDate, format: .dateTime), forKey: Key("CreateDate"))
        }
        if let hourlyPrice = hourlyPrice {
            try container.encode(hourlyPrice, forKey: Key("HourlyPrice"))
        }
        if instanceCount != 0 {
            try container.encode(instanceCount, forKey: Key("InstanceCount"))
        }
        if let instanceType = instanceType {
            try container.encode(instanceType, forKey: Key("InstanceType"))
        }
        if let networkPlatform = networkPlatform {
            try container.encode(networkPlatform, forKey: Key("NetworkPlatform"))
        }
        if let nextSlotStartTime = nextSlotStartTime {
            try container.encode(TimestampWrapper(nextSlotStartTime, format: .dateTime), forKey: Key("NextSlotStartTime"))
        }
        if let platform = platform {
            try container.encode(platform, forKey: Key("Platform"))
        }
        if let previousSlotEndTime = previousSlotEndTime {
            try container.encode(TimestampWrapper(previousSlotEndTime, format: .dateTime), forKey: Key("PreviousSlotEndTime"))
        }
        if let recurrence = recurrence {
            try container.encode(recurrence, forKey: Key("Recurrence"))
        }
        if let scheduledInstanceId = scheduledInstanceId {
            try container.encode(scheduledInstanceId, forKey: Key("ScheduledInstanceId"))
        }
        if slotDurationInHours != 0 {
            try container.encode(slotDurationInHours, forKey: Key("SlotDurationInHours"))
        }
        if let termEndDate = termEndDate {
            try container.encode(TimestampWrapper(termEndDate, format: .dateTime), forKey: Key("TermEndDate"))
        }
        if let termStartDate = termStartDate {
            try container.encode(TimestampWrapper(termStartDate, format: .dateTime), forKey: Key("TermStartDate"))
        }
        if totalScheduledInstanceHours != 0 {
            try container.encode(totalScheduledInstanceHours, forKey: Key("TotalScheduledInstanceHours"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let availabilityZoneDecoded = try containerValues.decodeIfPresent(String.self, forKey: .availabilityZone)
        availabilityZone = availabilityZoneDecoded
        let createDateDecoded = try containerValues.decodeIfPresent(String.self, forKey: .createDate)
        var createDateBuffer:Date? = nil
        if let createDateDecoded = createDateDecoded {
            createDateBuffer = try TimestampWrapperDecoder.parseDateStringValue(createDateDecoded, format: .dateTime)
        }
        createDate = createDateBuffer
        let hourlyPriceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .hourlyPrice)
        hourlyPrice = hourlyPriceDecoded
        let instanceCountDecoded = try containerValues.decode(Int.self, forKey: .instanceCount)
        instanceCount = instanceCountDecoded
        let instanceTypeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .instanceType)
        instanceType = instanceTypeDecoded
        let networkPlatformDecoded = try containerValues.decodeIfPresent(String.self, forKey: .networkPlatform)
        networkPlatform = networkPlatformDecoded
        let nextSlotStartTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .nextSlotStartTime)
        var nextSlotStartTimeBuffer:Date? = nil
        if let nextSlotStartTimeDecoded = nextSlotStartTimeDecoded {
            nextSlotStartTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(nextSlotStartTimeDecoded, format: .dateTime)
        }
        nextSlotStartTime = nextSlotStartTimeBuffer
        let platformDecoded = try containerValues.decodeIfPresent(String.self, forKey: .platform)
        platform = platformDecoded
        let previousSlotEndTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .previousSlotEndTime)
        var previousSlotEndTimeBuffer:Date? = nil
        if let previousSlotEndTimeDecoded = previousSlotEndTimeDecoded {
            previousSlotEndTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(previousSlotEndTimeDecoded, format: .dateTime)
        }
        previousSlotEndTime = previousSlotEndTimeBuffer
        let recurrenceDecoded = try containerValues.decodeIfPresent(ScheduledInstanceRecurrence.self, forKey: .recurrence)
        recurrence = recurrenceDecoded
        let scheduledInstanceIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .scheduledInstanceId)
        scheduledInstanceId = scheduledInstanceIdDecoded
        let slotDurationInHoursDecoded = try containerValues.decode(Int.self, forKey: .slotDurationInHours)
        slotDurationInHours = slotDurationInHoursDecoded
        let termEndDateDecoded = try containerValues.decodeIfPresent(String.self, forKey: .termEndDate)
        var termEndDateBuffer:Date? = nil
        if let termEndDateDecoded = termEndDateDecoded {
            termEndDateBuffer = try TimestampWrapperDecoder.parseDateStringValue(termEndDateDecoded, format: .dateTime)
        }
        termEndDate = termEndDateBuffer
        let termStartDateDecoded = try containerValues.decodeIfPresent(String.self, forKey: .termStartDate)
        var termStartDateBuffer:Date? = nil
        if let termStartDateDecoded = termStartDateDecoded {
            termStartDateBuffer = try TimestampWrapperDecoder.parseDateStringValue(termStartDateDecoded, format: .dateTime)
        }
        termStartDate = termStartDateBuffer
        let totalScheduledInstanceHoursDecoded = try containerValues.decode(Int.self, forKey: .totalScheduledInstanceHours)
        totalScheduledInstanceHours = totalScheduledInstanceHoursDecoded
    }
}
