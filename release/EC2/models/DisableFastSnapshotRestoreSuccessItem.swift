// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

/// <p>Describes fast snapshot restores that were successfully disabled.</p>
public struct DisableFastSnapshotRestoreSuccessItem: Equatable {
    /// <p>The Availability Zone.</p>
    public let availabilityZone: String?
    /// <p>The time at which fast snapshot restores entered the <code>disabled</code> state.</p>
    public let disabledTime: Date?
    /// <p>The time at which fast snapshot restores entered the <code>disabling</code> state.</p>
    public let disablingTime: Date?
    /// <p>The time at which fast snapshot restores entered the <code>enabled</code> state.</p>
    public let enabledTime: Date?
    /// <p>The time at which fast snapshot restores entered the <code>enabling</code> state.</p>
    public let enablingTime: Date?
    /// <p>The time at which fast snapshot restores entered the <code>optimizing</code> state.</p>
    public let optimizingTime: Date?
    /// <p>The AWS owner alias that enabled fast snapshot restores on the snapshot. This is intended for future use.</p>
    public let ownerAlias: String?
    /// <p>The ID of the AWS account that enabled fast snapshot restores on the snapshot.</p>
    public let ownerId: String?
    /// <p>The ID of the snapshot.</p>
    public let snapshotId: String?
    /// <p>The state of fast snapshot restores for the snapshot.</p>
    public let state: FastSnapshotRestoreStateCode?
    /// <p>The reason for the state transition. The possible values are as follows:</p>
    ///          <ul>
    ///             <li>
    ///                <p>
    ///                   <code>Client.UserInitiated</code> - The state successfully transitioned to <code>enabling</code> or
    ///           <code>disabling</code>.</p>
    ///             </li>
    ///             <li>
    ///                <p>
    ///                   <code>Client.UserInitiated - Lifecycle state transition</code> - The state successfully transitioned
    ///           to <code>optimizing</code>, <code>enabled</code>, or <code>disabled</code>.</p>
    ///             </li>
    ///          </ul>
    public let stateTransitionReason: String?

    public init (
        availabilityZone: String? = nil,
        disabledTime: Date? = nil,
        disablingTime: Date? = nil,
        enabledTime: Date? = nil,
        enablingTime: Date? = nil,
        optimizingTime: Date? = nil,
        ownerAlias: String? = nil,
        ownerId: String? = nil,
        snapshotId: String? = nil,
        state: FastSnapshotRestoreStateCode? = nil,
        stateTransitionReason: String? = nil
    )
    {
        self.availabilityZone = availabilityZone
        self.disabledTime = disabledTime
        self.disablingTime = disablingTime
        self.enabledTime = enabledTime
        self.enablingTime = enablingTime
        self.optimizingTime = optimizingTime
        self.ownerAlias = ownerAlias
        self.ownerId = ownerId
        self.snapshotId = snapshotId
        self.state = state
        self.stateTransitionReason = stateTransitionReason
    }
}

extension DisableFastSnapshotRestoreSuccessItem: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DisableFastSnapshotRestoreSuccessItem(availabilityZone: \(String(describing: availabilityZone)), disabledTime: \(String(describing: disabledTime)), disablingTime: \(String(describing: disablingTime)), enabledTime: \(String(describing: enabledTime)), enablingTime: \(String(describing: enablingTime)), optimizingTime: \(String(describing: optimizingTime)), ownerAlias: \(String(describing: ownerAlias)), ownerId: \(String(describing: ownerId)), snapshotId: \(String(describing: snapshotId)), state: \(String(describing: state)), stateTransitionReason: \(String(describing: stateTransitionReason)))"}
}
