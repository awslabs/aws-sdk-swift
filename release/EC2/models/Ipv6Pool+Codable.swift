// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension Ipv6Pool: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case description = "description"
        case poolCidrBlocks = "poolCidrBlockSet"
        case poolId = "poolId"
        case tags = "tagSet"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let description = description {
            try container.encode(description, forKey: Key("Description"))
        }
        if let poolCidrBlocks = poolCidrBlocks {
            if !poolCidrBlocks.isEmpty {
                for (index0, poolcidrblock0) in poolCidrBlocks.enumerated() {
                    var poolCidrBlocksContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("PoolCidrBlockSet.\(index0.advanced(by: 1))"))
                    try poolCidrBlocksContainer0.encode(poolcidrblock0, forKey: Key(""))
                }
            }
        }
        if let poolId = poolId {
            try container.encode(poolId, forKey: Key("PoolId"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let poolIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .poolId)
        poolId = poolIdDecoded
        let descriptionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .description)
        description = descriptionDecoded
        if containerValues.contains(.poolCidrBlocks) {
            struct KeyVal0{struct item{}}
            let poolCidrBlocksWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .poolCidrBlocks)
            if let poolCidrBlocksWrappedContainer = poolCidrBlocksWrappedContainer {
                let poolCidrBlocksContainer = try poolCidrBlocksWrappedContainer.decodeIfPresent([PoolCidrBlock].self, forKey: .member)
                var poolCidrBlocksBuffer:[PoolCidrBlock]? = nil
                if let poolCidrBlocksContainer = poolCidrBlocksContainer {
                    poolCidrBlocksBuffer = [PoolCidrBlock]()
                    for structureContainer0 in poolCidrBlocksContainer {
                        poolCidrBlocksBuffer?.append(structureContainer0)
                    }
                }
                poolCidrBlocks = poolCidrBlocksBuffer
            } else {
                poolCidrBlocks = []
            }
        } else {
            poolCidrBlocks = nil
        }
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
    }
}
