// Code generated by smithy-swift-codegen. DO NOT EDIT!



public enum ExportTaskState {
    case active
    case cancelled
    case cancelling
    case completed
    case sdkUnknown(String)
}

extension ExportTaskState : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ExportTaskState] {
        return [
            .active,
            .cancelled,
            .cancelling,
            .completed,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .active: return "active"
        case .cancelled: return "cancelled"
        case .cancelling: return "cancelling"
        case .completed: return "completed"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ExportTaskState(rawValue: rawValue) ?? ExportTaskState.sdkUnknown(rawValue)
    }
}
