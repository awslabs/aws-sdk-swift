// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

public struct DescribeHostsInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeHostsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeHostsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeHostsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeHostsInput>
    public typealias MOutput = OperationOutput<DescribeHostsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeHostsOutputError>
}

public struct DescribeHostsInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeHostsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeHostsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeHostsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeHostsInput>
    public typealias MOutput = OperationOutput<DescribeHostsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeHostsOutputError>
}

public struct DescribeHostsInputBodyMiddleware: Middleware {
    public let id: String = "DescribeHostsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeHostsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeHostsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            let encoder = context.getEncoder()
            let data = try encoder.encode(input.operationInput)
            let body = HttpBody.data(data)
            input.builder.withBody(body)
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeHostsInput>
    public typealias MOutput = OperationOutput<DescribeHostsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeHostsOutputError>
}

extension DescribeHostsInput: Encodable, Reflection {
    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let filter = filter {
            if !filter.isEmpty {
                for (index0, filter0) in filter.enumerated() {
                    var filterContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Filter.\(index0.advanced(by: 1))"))
                    try filterContainer0.encode(filter0, forKey: Key(""))
                }
            }
        }
        if let hostIds = hostIds {
            if !hostIds.isEmpty {
                for (index0, dedicatedhostid0) in hostIds.enumerated() {
                    var hostIdsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("HostId.\(index0.advanced(by: 1))"))
                    try hostIdsContainer0.encode(dedicatedhostid0, forKey: Key(""))
                }
            }
        }
        if maxResults != 0 {
            try container.encode(maxResults, forKey: Key("MaxResults"))
        }
        if let nextToken = nextToken {
            try container.encode(nextToken, forKey: Key("NextToken"))
        }
        try container.encode("DescribeHosts", forKey:Key("Action"))
        try container.encode("2016-11-15", forKey:Key("Version"))
    }
}
