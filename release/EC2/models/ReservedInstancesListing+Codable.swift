// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension ReservedInstancesListing: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case clientToken = "clientToken"
        case createDate = "createDate"
        case instanceCounts = "instanceCounts"
        case priceSchedules = "priceSchedules"
        case reservedInstancesId = "reservedInstancesId"
        case reservedInstancesListingId = "reservedInstancesListingId"
        case status = "status"
        case statusMessage = "statusMessage"
        case tags = "tagSet"
        case updateDate = "updateDate"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let clientToken = clientToken {
            try container.encode(clientToken, forKey: Key("ClientToken"))
        }
        if let createDate = createDate {
            try container.encode(TimestampWrapper(createDate, format: .dateTime), forKey: Key("CreateDate"))
        }
        if let instanceCounts = instanceCounts {
            if !instanceCounts.isEmpty {
                for (index0, instancecount0) in instanceCounts.enumerated() {
                    var instanceCountsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("InstanceCounts.\(index0.advanced(by: 1))"))
                    try instanceCountsContainer0.encode(instancecount0, forKey: Key(""))
                }
            }
        }
        if let priceSchedules = priceSchedules {
            if !priceSchedules.isEmpty {
                for (index0, priceschedule0) in priceSchedules.enumerated() {
                    var priceSchedulesContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("PriceSchedules.\(index0.advanced(by: 1))"))
                    try priceSchedulesContainer0.encode(priceschedule0, forKey: Key(""))
                }
            }
        }
        if let reservedInstancesId = reservedInstancesId {
            try container.encode(reservedInstancesId, forKey: Key("ReservedInstancesId"))
        }
        if let reservedInstancesListingId = reservedInstancesListingId {
            try container.encode(reservedInstancesListingId, forKey: Key("ReservedInstancesListingId"))
        }
        if let status = status {
            try container.encode(status, forKey: Key("Status"))
        }
        if let statusMessage = statusMessage {
            try container.encode(statusMessage, forKey: Key("StatusMessage"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let updateDate = updateDate {
            try container.encode(TimestampWrapper(updateDate, format: .dateTime), forKey: Key("UpdateDate"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clientTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clientToken)
        clientToken = clientTokenDecoded
        let createDateDecoded = try containerValues.decodeIfPresent(String.self, forKey: .createDate)
        var createDateBuffer:Date? = nil
        if let createDateDecoded = createDateDecoded {
            createDateBuffer = try TimestampWrapperDecoder.parseDateStringValue(createDateDecoded, format: .dateTime)
        }
        createDate = createDateBuffer
        if containerValues.contains(.instanceCounts) {
            struct KeyVal0{struct item{}}
            let instanceCountsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .instanceCounts)
            if let instanceCountsWrappedContainer = instanceCountsWrappedContainer {
                let instanceCountsContainer = try instanceCountsWrappedContainer.decodeIfPresent([InstanceCount].self, forKey: .member)
                var instanceCountsBuffer:[InstanceCount]? = nil
                if let instanceCountsContainer = instanceCountsContainer {
                    instanceCountsBuffer = [InstanceCount]()
                    for structureContainer0 in instanceCountsContainer {
                        instanceCountsBuffer?.append(structureContainer0)
                    }
                }
                instanceCounts = instanceCountsBuffer
            } else {
                instanceCounts = []
            }
        } else {
            instanceCounts = nil
        }
        if containerValues.contains(.priceSchedules) {
            struct KeyVal0{struct item{}}
            let priceSchedulesWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .priceSchedules)
            if let priceSchedulesWrappedContainer = priceSchedulesWrappedContainer {
                let priceSchedulesContainer = try priceSchedulesWrappedContainer.decodeIfPresent([PriceSchedule].self, forKey: .member)
                var priceSchedulesBuffer:[PriceSchedule]? = nil
                if let priceSchedulesContainer = priceSchedulesContainer {
                    priceSchedulesBuffer = [PriceSchedule]()
                    for structureContainer0 in priceSchedulesContainer {
                        priceSchedulesBuffer?.append(structureContainer0)
                    }
                }
                priceSchedules = priceSchedulesBuffer
            } else {
                priceSchedules = []
            }
        } else {
            priceSchedules = nil
        }
        let reservedInstancesIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reservedInstancesId)
        reservedInstancesId = reservedInstancesIdDecoded
        let reservedInstancesListingIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .reservedInstancesListingId)
        reservedInstancesListingId = reservedInstancesListingIdDecoded
        let statusDecoded = try containerValues.decodeIfPresent(ListingStatus.self, forKey: .status)
        status = statusDecoded
        let statusMessageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .statusMessage)
        statusMessage = statusMessageDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
        let updateDateDecoded = try containerValues.decodeIfPresent(String.self, forKey: .updateDate)
        var updateDateBuffer:Date? = nil
        if let updateDateDecoded = updateDateDecoded {
            updateDateBuffer = try TimestampWrapperDecoder.parseDateStringValue(updateDateDecoded, format: .dateTime)
        }
        updateDate = updateDateBuffer
    }
}
