// Code generated by smithy-swift-codegen. DO NOT EDIT!



/// <p>Describes overrides for a launch template.</p>
public struct FleetLaunchTemplateOverrides: Equatable {
    /// <p>The Availability Zone in which to launch the instances.</p>
    public let availabilityZone: String?
    /// <p>The instance type.</p>
    public let instanceType: InstanceType?
    /// <p>The maximum price per unit hour that you are willing to pay for a Spot Instance.</p>
    public let maxPrice: String?
    /// <p>The location where the instance launched, if applicable.</p>
    public let placement: PlacementResponse?
    /// <p>The priority for the launch template override. The highest priority is launched
    ///          first.</p>
    ///          <p>If the On-Demand <code>AllocationStrategy</code> is set to <code>prioritized</code>,
    ///          EC2 Fleet uses priority to determine which launch template override to use first in fulfilling
    ///          On-Demand capacity.</p>
    ///          <p>If the Spot <code>AllocationStrategy</code> is set to
    ///             <code>capacity-optimized-prioritized</code>, EC2 Fleet uses priority on a best-effort basis
    ///          to determine which launch template override to use in fulfilling Spot capacity, but
    ///          optimizes for capacity first.</p>
    ///          <p>Valid values are whole numbers starting at <code>0</code>. The lower the number, the
    ///          higher the priority. If no number is set, the override has the lowest priority. You can set
    ///          the same priority for different launch template overrides.</p>
    public let priority: Double
    /// <p>The ID of the subnet in which to launch the instances.</p>
    public let subnetId: String?
    /// <p>The number of units provided by the specified instance type.</p>
    public let weightedCapacity: Double

    public init (
        availabilityZone: String? = nil,
        instanceType: InstanceType? = nil,
        maxPrice: String? = nil,
        placement: PlacementResponse? = nil,
        priority: Double = 0.0,
        subnetId: String? = nil,
        weightedCapacity: Double = 0.0
    )
    {
        self.availabilityZone = availabilityZone
        self.instanceType = instanceType
        self.maxPrice = maxPrice
        self.placement = placement
        self.priority = priority
        self.subnetId = subnetId
        self.weightedCapacity = weightedCapacity
    }
}

extension FleetLaunchTemplateOverrides: CustomDebugStringConvertible {
    public var debugDescription: String {
        "FleetLaunchTemplateOverrides(availabilityZone: \(String(describing: availabilityZone)), instanceType: \(String(describing: instanceType)), maxPrice: \(String(describing: maxPrice)), placement: \(String(describing: placement)), priority: \(String(describing: priority)), subnetId: \(String(describing: subnetId)), weightedCapacity: \(String(describing: weightedCapacity)))"}
}
