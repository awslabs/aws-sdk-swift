// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

public struct DescribeLaunchTemplateVersionsInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeLaunchTemplateVersionsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeLaunchTemplateVersionsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeLaunchTemplateVersionsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeLaunchTemplateVersionsInput>
    public typealias MOutput = OperationOutput<DescribeLaunchTemplateVersionsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeLaunchTemplateVersionsOutputError>
}

public struct DescribeLaunchTemplateVersionsInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeLaunchTemplateVersionsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeLaunchTemplateVersionsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeLaunchTemplateVersionsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeLaunchTemplateVersionsInput>
    public typealias MOutput = OperationOutput<DescribeLaunchTemplateVersionsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeLaunchTemplateVersionsOutputError>
}

public struct DescribeLaunchTemplateVersionsInputBodyMiddleware: Middleware {
    public let id: String = "DescribeLaunchTemplateVersionsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeLaunchTemplateVersionsInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeLaunchTemplateVersionsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            let encoder = context.getEncoder()
            let data = try encoder.encode(input.operationInput)
            let body = HttpBody.data(data)
            input.builder.withBody(body)
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeLaunchTemplateVersionsInput>
    public typealias MOutput = OperationOutput<DescribeLaunchTemplateVersionsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeLaunchTemplateVersionsOutputError>
}

extension DescribeLaunchTemplateVersionsInput: Encodable, Reflection {
    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if dryRun != false {
            try container.encode(dryRun, forKey: Key("DryRun"))
        }
        if let filters = filters {
            if !filters.isEmpty {
                for (index0, filter0) in filters.enumerated() {
                    var filtersContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Filter.\(index0.advanced(by: 1))"))
                    try filtersContainer0.encode(filter0, forKey: Key(""))
                }
            }
        }
        if let launchTemplateId = launchTemplateId {
            try container.encode(launchTemplateId, forKey: Key("LaunchTemplateId"))
        }
        if let launchTemplateName = launchTemplateName {
            try container.encode(launchTemplateName, forKey: Key("LaunchTemplateName"))
        }
        if maxResults != 0 {
            try container.encode(maxResults, forKey: Key("MaxResults"))
        }
        if let maxVersion = maxVersion {
            try container.encode(maxVersion, forKey: Key("MaxVersion"))
        }
        if let minVersion = minVersion {
            try container.encode(minVersion, forKey: Key("MinVersion"))
        }
        if let nextToken = nextToken {
            try container.encode(nextToken, forKey: Key("NextToken"))
        }
        if let versions = versions {
            if !versions.isEmpty {
                for (index0, string0) in versions.enumerated() {
                    var versionsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("LaunchTemplateVersion.\(index0.advanced(by: 1))"))
                    try versionsContainer0.encode(string0, forKey: Key(""))
                }
            }
        }
        try container.encode("DescribeLaunchTemplateVersions", forKey:Key("Action"))
        try container.encode("2016-11-15", forKey:Key("Version"))
    }
}
