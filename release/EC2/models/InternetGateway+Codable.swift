// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension InternetGateway: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case attachments = "attachmentSet"
        case internetGatewayId = "internetGatewayId"
        case ownerId = "ownerId"
        case tags = "tagSet"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let attachments = attachments {
            if !attachments.isEmpty {
                for (index0, internetgatewayattachment0) in attachments.enumerated() {
                    var attachmentsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("AttachmentSet.\(index0.advanced(by: 1))"))
                    try attachmentsContainer0.encode(internetgatewayattachment0, forKey: Key(""))
                }
            }
        }
        if let internetGatewayId = internetGatewayId {
            try container.encode(internetGatewayId, forKey: Key("InternetGatewayId"))
        }
        if let ownerId = ownerId {
            try container.encode(ownerId, forKey: Key("OwnerId"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        if containerValues.contains(.attachments) {
            struct KeyVal0{struct item{}}
            let attachmentsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .attachments)
            if let attachmentsWrappedContainer = attachmentsWrappedContainer {
                let attachmentsContainer = try attachmentsWrappedContainer.decodeIfPresent([InternetGatewayAttachment].self, forKey: .member)
                var attachmentsBuffer:[InternetGatewayAttachment]? = nil
                if let attachmentsContainer = attachmentsContainer {
                    attachmentsBuffer = [InternetGatewayAttachment]()
                    for structureContainer0 in attachmentsContainer {
                        attachmentsBuffer?.append(structureContainer0)
                    }
                }
                attachments = attachmentsBuffer
            } else {
                attachments = []
            }
        } else {
            attachments = nil
        }
        let internetGatewayIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .internetGatewayId)
        internetGatewayId = internetGatewayIdDecoded
        let ownerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerId)
        ownerId = ownerIdDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
    }
}
