// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension VpnGateway: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case amazonSideAsn = "amazonSideAsn"
        case availabilityZone = "availabilityZone"
        case state = "state"
        case tags = "tagSet"
        case type = "type"
        case vpcAttachments = "attachments"
        case vpnGatewayId = "vpnGatewayId"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if amazonSideAsn != 0 {
            try container.encode(amazonSideAsn, forKey: Key("AmazonSideAsn"))
        }
        if let availabilityZone = availabilityZone {
            try container.encode(availabilityZone, forKey: Key("AvailabilityZone"))
        }
        if let state = state {
            try container.encode(state, forKey: Key("State"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let type = type {
            try container.encode(type, forKey: Key("Type"))
        }
        if let vpcAttachments = vpcAttachments {
            if !vpcAttachments.isEmpty {
                for (index0, vpcattachment0) in vpcAttachments.enumerated() {
                    var vpcAttachmentsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Attachments.\(index0.advanced(by: 1))"))
                    try vpcAttachmentsContainer0.encode(vpcattachment0, forKey: Key(""))
                }
            }
        }
        if let vpnGatewayId = vpnGatewayId {
            try container.encode(vpnGatewayId, forKey: Key("VpnGatewayId"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let availabilityZoneDecoded = try containerValues.decodeIfPresent(String.self, forKey: .availabilityZone)
        availabilityZone = availabilityZoneDecoded
        let stateDecoded = try containerValues.decodeIfPresent(VpnState.self, forKey: .state)
        state = stateDecoded
        let typeDecoded = try containerValues.decodeIfPresent(GatewayType.self, forKey: .type)
        type = typeDecoded
        if containerValues.contains(.vpcAttachments) {
            struct KeyVal0{struct item{}}
            let vpcAttachmentsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .vpcAttachments)
            if let vpcAttachmentsWrappedContainer = vpcAttachmentsWrappedContainer {
                let vpcAttachmentsContainer = try vpcAttachmentsWrappedContainer.decodeIfPresent([VpcAttachment].self, forKey: .member)
                var vpcAttachmentsBuffer:[VpcAttachment]? = nil
                if let vpcAttachmentsContainer = vpcAttachmentsContainer {
                    vpcAttachmentsBuffer = [VpcAttachment]()
                    for structureContainer0 in vpcAttachmentsContainer {
                        vpcAttachmentsBuffer?.append(structureContainer0)
                    }
                }
                vpcAttachments = vpcAttachmentsBuffer
            } else {
                vpcAttachments = []
            }
        } else {
            vpcAttachments = nil
        }
        let vpnGatewayIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .vpnGatewayId)
        vpnGatewayId = vpnGatewayIdDecoded
        let amazonSideAsnDecoded = try containerValues.decode(Int.self, forKey: .amazonSideAsn)
        amazonSideAsn = amazonSideAsnDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
    }
}
