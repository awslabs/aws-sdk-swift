// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

public struct ModifyCapacityReservationInput: Equatable {
    /// <p>Reserved. Capacity Reservations you have created are accepted by default.</p>
    public let accept: Bool
    /// <p>The ID of the Capacity Reservation.</p>
    public let capacityReservationId: String?
    /// <p>Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is <code>DryRunOperation</code>. Otherwise, it is <code>UnauthorizedOperation</code>.</p>
    public let dryRun: Bool
    /// <p>The date and time at which the Capacity Reservation expires. When a Capacity Reservation expires, the reserved capacity
    /// 			is released and you can no longer launch instances into it. The Capacity Reservation's state changes to
    /// 				<code>expired</code> when it reaches its end date and time.</p>
    /// 		       <p>The Capacity Reservation is cancelled within an hour from the specified time. For example, if you specify
    /// 			5/31/2019, 13:30:55, the Capacity Reservation is guaranteed to end between 13:30:55 and 14:30:55 on 5/31/2019.</p>
    /// 		       <p>You must provide an <code>EndDate</code> value if <code>EndDateType</code> is
    /// 				<code>limited</code>. Omit <code>EndDate</code> if <code>EndDateType</code> is
    /// 				<code>unlimited</code>.</p>
    public let endDate: Date?
    /// <p>Indicates the way in which the Capacity Reservation ends. A Capacity Reservation can have one of the following end
    /// 			types:</p>
    /// 		       <ul>
    ///             <li>
    ///                <p>
    ///                   <code>unlimited</code> - The Capacity Reservation remains active until you explicitly cancel it. Do not
    /// 					provide an <code>EndDate</code> value if <code>EndDateType</code> is
    /// 						<code>unlimited</code>.</p>
    ///             </li>
    ///             <li>
    ///                <p>
    ///                   <code>limited</code> - The Capacity Reservation expires automatically at a specified date and time. You must
    /// 					provide an <code>EndDate</code> value if <code>EndDateType</code> is
    /// 						<code>limited</code>.</p>
    ///             </li>
    ///          </ul>
    public let endDateType: EndDateType?
    /// <p>The number of instances for which to reserve capacity.</p>
    public let instanceCount: Int

    public init (
        accept: Bool = false,
        capacityReservationId: String? = nil,
        dryRun: Bool = false,
        endDate: Date? = nil,
        endDateType: EndDateType? = nil,
        instanceCount: Int = 0
    )
    {
        self.accept = accept
        self.capacityReservationId = capacityReservationId
        self.dryRun = dryRun
        self.endDate = endDate
        self.endDateType = endDateType
        self.instanceCount = instanceCount
    }
}

extension ModifyCapacityReservationInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "ModifyCapacityReservationInput(accept: \(String(describing: accept)), capacityReservationId: \(String(describing: capacityReservationId)), dryRun: \(String(describing: dryRun)), endDate: \(String(describing: endDate)), endDateType: \(String(describing: endDateType)), instanceCount: \(String(describing: instanceCount)))"}
}
