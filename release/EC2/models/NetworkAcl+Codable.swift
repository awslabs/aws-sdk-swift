// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension NetworkAcl: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case associations = "associationSet"
        case entries = "entrySet"
        case isDefault = "default"
        case networkAclId = "networkAclId"
        case ownerId = "ownerId"
        case tags = "tagSet"
        case vpcId = "vpcId"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let associations = associations {
            if !associations.isEmpty {
                for (index0, networkaclassociation0) in associations.enumerated() {
                    var associationsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("AssociationSet.\(index0.advanced(by: 1))"))
                    try associationsContainer0.encode(networkaclassociation0, forKey: Key(""))
                }
            }
        }
        if let entries = entries {
            if !entries.isEmpty {
                for (index0, networkaclentry0) in entries.enumerated() {
                    var entriesContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("EntrySet.\(index0.advanced(by: 1))"))
                    try entriesContainer0.encode(networkaclentry0, forKey: Key(""))
                }
            }
        }
        if isDefault != false {
            try container.encode(isDefault, forKey: Key("Default"))
        }
        if let networkAclId = networkAclId {
            try container.encode(networkAclId, forKey: Key("NetworkAclId"))
        }
        if let ownerId = ownerId {
            try container.encode(ownerId, forKey: Key("OwnerId"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let vpcId = vpcId {
            try container.encode(vpcId, forKey: Key("VpcId"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        if containerValues.contains(.associations) {
            struct KeyVal0{struct item{}}
            let associationsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .associations)
            if let associationsWrappedContainer = associationsWrappedContainer {
                let associationsContainer = try associationsWrappedContainer.decodeIfPresent([NetworkAclAssociation].self, forKey: .member)
                var associationsBuffer:[NetworkAclAssociation]? = nil
                if let associationsContainer = associationsContainer {
                    associationsBuffer = [NetworkAclAssociation]()
                    for structureContainer0 in associationsContainer {
                        associationsBuffer?.append(structureContainer0)
                    }
                }
                associations = associationsBuffer
            } else {
                associations = []
            }
        } else {
            associations = nil
        }
        if containerValues.contains(.entries) {
            struct KeyVal0{struct item{}}
            let entriesWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .entries)
            if let entriesWrappedContainer = entriesWrappedContainer {
                let entriesContainer = try entriesWrappedContainer.decodeIfPresent([NetworkAclEntry].self, forKey: .member)
                var entriesBuffer:[NetworkAclEntry]? = nil
                if let entriesContainer = entriesContainer {
                    entriesBuffer = [NetworkAclEntry]()
                    for structureContainer0 in entriesContainer {
                        entriesBuffer?.append(structureContainer0)
                    }
                }
                entries = entriesBuffer
            } else {
                entries = []
            }
        } else {
            entries = nil
        }
        let isDefaultDecoded = try containerValues.decode(Bool.self, forKey: .isDefault)
        isDefault = isDefaultDecoded
        let networkAclIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .networkAclId)
        networkAclId = networkAclIdDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
        let vpcIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .vpcId)
        vpcId = vpcIdDecoded
        let ownerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerId)
        ownerId = ownerIdDecoded
    }
}
