// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

public struct DescribeLaunchTemplatesInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeLaunchTemplatesInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeLaunchTemplatesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeLaunchTemplatesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeLaunchTemplatesInput>
    public typealias MOutput = OperationOutput<DescribeLaunchTemplatesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeLaunchTemplatesOutputError>
}

public struct DescribeLaunchTemplatesInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeLaunchTemplatesInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeLaunchTemplatesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeLaunchTemplatesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeLaunchTemplatesInput>
    public typealias MOutput = OperationOutput<DescribeLaunchTemplatesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeLaunchTemplatesOutputError>
}

public struct DescribeLaunchTemplatesInputBodyMiddleware: Middleware {
    public let id: String = "DescribeLaunchTemplatesInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeLaunchTemplatesInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeLaunchTemplatesOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            let encoder = context.getEncoder()
            let data = try encoder.encode(input.operationInput)
            let body = HttpBody.data(data)
            input.builder.withBody(body)
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeLaunchTemplatesInput>
    public typealias MOutput = OperationOutput<DescribeLaunchTemplatesOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeLaunchTemplatesOutputError>
}

extension DescribeLaunchTemplatesInput: Encodable, Reflection {
    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if dryRun != false {
            try container.encode(dryRun, forKey: Key("DryRun"))
        }
        if let filters = filters {
            if !filters.isEmpty {
                for (index0, filter0) in filters.enumerated() {
                    var filtersContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Filter.\(index0.advanced(by: 1))"))
                    try filtersContainer0.encode(filter0, forKey: Key(""))
                }
            }
        }
        if let launchTemplateIds = launchTemplateIds {
            if !launchTemplateIds.isEmpty {
                for (index0, launchtemplateid0) in launchTemplateIds.enumerated() {
                    var launchTemplateIdsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("LaunchTemplateId.\(index0.advanced(by: 1))"))
                    try launchTemplateIdsContainer0.encode(launchtemplateid0, forKey: Key(""))
                }
            }
        }
        if let launchTemplateNames = launchTemplateNames {
            if !launchTemplateNames.isEmpty {
                for (index0, launchtemplatename0) in launchTemplateNames.enumerated() {
                    var launchTemplateNamesContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("LaunchTemplateName.\(index0.advanced(by: 1))"))
                    try launchTemplateNamesContainer0.encode(launchtemplatename0, forKey: Key(""))
                }
            }
        }
        if maxResults != 0 {
            try container.encode(maxResults, forKey: Key("MaxResults"))
        }
        if let nextToken = nextToken {
            try container.encode(nextToken, forKey: Key("NextToken"))
        }
        try container.encode("DescribeLaunchTemplates", forKey:Key("Action"))
        try container.encode("2016-11-15", forKey:Key("Version"))
    }
}
