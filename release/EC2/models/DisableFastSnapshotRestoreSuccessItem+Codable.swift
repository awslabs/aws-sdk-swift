// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension DisableFastSnapshotRestoreSuccessItem: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case availabilityZone = "availabilityZone"
        case disabledTime = "disabledTime"
        case disablingTime = "disablingTime"
        case enabledTime = "enabledTime"
        case enablingTime = "enablingTime"
        case optimizingTime = "optimizingTime"
        case ownerAlias = "ownerAlias"
        case ownerId = "ownerId"
        case snapshotId = "snapshotId"
        case state = "state"
        case stateTransitionReason = "stateTransitionReason"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let availabilityZone = availabilityZone {
            try container.encode(availabilityZone, forKey: Key("AvailabilityZone"))
        }
        if let disabledTime = disabledTime {
            try container.encode(TimestampWrapper(disabledTime, format: .dateTime), forKey: Key("DisabledTime"))
        }
        if let disablingTime = disablingTime {
            try container.encode(TimestampWrapper(disablingTime, format: .dateTime), forKey: Key("DisablingTime"))
        }
        if let enabledTime = enabledTime {
            try container.encode(TimestampWrapper(enabledTime, format: .dateTime), forKey: Key("EnabledTime"))
        }
        if let enablingTime = enablingTime {
            try container.encode(TimestampWrapper(enablingTime, format: .dateTime), forKey: Key("EnablingTime"))
        }
        if let optimizingTime = optimizingTime {
            try container.encode(TimestampWrapper(optimizingTime, format: .dateTime), forKey: Key("OptimizingTime"))
        }
        if let ownerAlias = ownerAlias {
            try container.encode(ownerAlias, forKey: Key("OwnerAlias"))
        }
        if let ownerId = ownerId {
            try container.encode(ownerId, forKey: Key("OwnerId"))
        }
        if let snapshotId = snapshotId {
            try container.encode(snapshotId, forKey: Key("SnapshotId"))
        }
        if let state = state {
            try container.encode(state, forKey: Key("State"))
        }
        if let stateTransitionReason = stateTransitionReason {
            try container.encode(stateTransitionReason, forKey: Key("StateTransitionReason"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let snapshotIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .snapshotId)
        snapshotId = snapshotIdDecoded
        let availabilityZoneDecoded = try containerValues.decodeIfPresent(String.self, forKey: .availabilityZone)
        availabilityZone = availabilityZoneDecoded
        let stateDecoded = try containerValues.decodeIfPresent(FastSnapshotRestoreStateCode.self, forKey: .state)
        state = stateDecoded
        let stateTransitionReasonDecoded = try containerValues.decodeIfPresent(String.self, forKey: .stateTransitionReason)
        stateTransitionReason = stateTransitionReasonDecoded
        let ownerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerId)
        ownerId = ownerIdDecoded
        let ownerAliasDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerAlias)
        ownerAlias = ownerAliasDecoded
        let enablingTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .enablingTime)
        var enablingTimeBuffer:Date? = nil
        if let enablingTimeDecoded = enablingTimeDecoded {
            enablingTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(enablingTimeDecoded, format: .dateTime)
        }
        enablingTime = enablingTimeBuffer
        let optimizingTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .optimizingTime)
        var optimizingTimeBuffer:Date? = nil
        if let optimizingTimeDecoded = optimizingTimeDecoded {
            optimizingTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(optimizingTimeDecoded, format: .dateTime)
        }
        optimizingTime = optimizingTimeBuffer
        let enabledTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .enabledTime)
        var enabledTimeBuffer:Date? = nil
        if let enabledTimeDecoded = enabledTimeDecoded {
            enabledTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(enabledTimeDecoded, format: .dateTime)
        }
        enabledTime = enabledTimeBuffer
        let disablingTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .disablingTime)
        var disablingTimeBuffer:Date? = nil
        if let disablingTimeDecoded = disablingTimeDecoded {
            disablingTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(disablingTimeDecoded, format: .dateTime)
        }
        disablingTime = disablingTimeBuffer
        let disabledTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .disabledTime)
        var disabledTimeBuffer:Date? = nil
        if let disabledTimeDecoded = disabledTimeDecoded {
            disabledTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(disabledTimeDecoded, format: .dateTime)
        }
        disabledTime = disabledTimeBuffer
    }
}
