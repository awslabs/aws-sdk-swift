// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension FpgaDeviceInfo: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case count = "count"
        case manufacturer = "manufacturer"
        case memoryInfo = "memoryInfo"
        case name = "name"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let count = count {
            try container.encode(count, forKey: Key("Count"))
        }
        if let manufacturer = manufacturer {
            try container.encode(manufacturer, forKey: Key("Manufacturer"))
        }
        if let memoryInfo = memoryInfo {
            try container.encode(memoryInfo, forKey: Key("MemoryInfo"))
        }
        if let name = name {
            try container.encode(name, forKey: Key("Name"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let manufacturerDecoded = try containerValues.decodeIfPresent(String.self, forKey: .manufacturer)
        manufacturer = manufacturerDecoded
        let countDecoded = try containerValues.decodeIfPresent(Int.self, forKey: .count)
        count = countDecoded
        let memoryInfoDecoded = try containerValues.decodeIfPresent(FpgaDeviceMemoryInfo.self, forKey: .memoryInfo)
        memoryInfo = memoryInfoDecoded
    }
}
