// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

public struct DescribeScheduledInstanceAvailabilityInputHeadersMiddleware: Middleware {
    public let id: String = "DescribeScheduledInstanceAvailabilityInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeScheduledInstanceAvailabilityInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeScheduledInstanceAvailabilityOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeScheduledInstanceAvailabilityInput>
    public typealias MOutput = OperationOutput<DescribeScheduledInstanceAvailabilityOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeScheduledInstanceAvailabilityOutputError>
}

public struct DescribeScheduledInstanceAvailabilityInputQueryItemMiddleware: Middleware {
    public let id: String = "DescribeScheduledInstanceAvailabilityInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeScheduledInstanceAvailabilityInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeScheduledInstanceAvailabilityOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeScheduledInstanceAvailabilityInput>
    public typealias MOutput = OperationOutput<DescribeScheduledInstanceAvailabilityOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeScheduledInstanceAvailabilityOutputError>
}

public struct DescribeScheduledInstanceAvailabilityInputBodyMiddleware: Middleware {
    public let id: String = "DescribeScheduledInstanceAvailabilityInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<DescribeScheduledInstanceAvailabilityInput>,
                  next: H) -> Swift.Result<OperationOutput<DescribeScheduledInstanceAvailabilityOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        do {
            let encoder = context.getEncoder()
            let data = try encoder.encode(input.operationInput)
            let body = HttpBody.data(data)
            input.builder.withBody(body)
        } catch let err {
            return .failure(.client(ClientError.serializationFailed(err.localizedDescription)))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<DescribeScheduledInstanceAvailabilityInput>
    public typealias MOutput = OperationOutput<DescribeScheduledInstanceAvailabilityOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<DescribeScheduledInstanceAvailabilityOutputError>
}

extension DescribeScheduledInstanceAvailabilityInput: Encodable, Reflection {
    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if dryRun != false {
            try container.encode(dryRun, forKey: Key("DryRun"))
        }
        if let filters = filters {
            if !filters.isEmpty {
                for (index0, filter0) in filters.enumerated() {
                    var filtersContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Filter.\(index0.advanced(by: 1))"))
                    try filtersContainer0.encode(filter0, forKey: Key(""))
                }
            }
        }
        if let firstSlotStartTimeRange = firstSlotStartTimeRange {
            try container.encode(firstSlotStartTimeRange, forKey: Key("FirstSlotStartTimeRange"))
        }
        if maxResults != 0 {
            try container.encode(maxResults, forKey: Key("MaxResults"))
        }
        if maxSlotDurationInHours != 0 {
            try container.encode(maxSlotDurationInHours, forKey: Key("MaxSlotDurationInHours"))
        }
        if minSlotDurationInHours != 0 {
            try container.encode(minSlotDurationInHours, forKey: Key("MinSlotDurationInHours"))
        }
        if let nextToken = nextToken {
            try container.encode(nextToken, forKey: Key("NextToken"))
        }
        if let recurrence = recurrence {
            try container.encode(recurrence, forKey: Key("Recurrence"))
        }
        try container.encode("DescribeScheduledInstanceAvailability", forKey:Key("Action"))
        try container.encode("2016-11-15", forKey:Key("Version"))
    }
}
