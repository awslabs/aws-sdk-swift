// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension PlacementGroup: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case groupId = "groupId"
        case groupName = "groupName"
        case partitionCount = "partitionCount"
        case state = "state"
        case strategy = "strategy"
        case tags = "tagSet"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let groupId = groupId {
            try container.encode(groupId, forKey: Key("GroupId"))
        }
        if let groupName = groupName {
            try container.encode(groupName, forKey: Key("GroupName"))
        }
        if partitionCount != 0 {
            try container.encode(partitionCount, forKey: Key("PartitionCount"))
        }
        if let state = state {
            try container.encode(state, forKey: Key("State"))
        }
        if let strategy = strategy {
            try container.encode(strategy, forKey: Key("Strategy"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let groupNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .groupName)
        groupName = groupNameDecoded
        let stateDecoded = try containerValues.decodeIfPresent(PlacementGroupState.self, forKey: .state)
        state = stateDecoded
        let strategyDecoded = try containerValues.decodeIfPresent(PlacementStrategy.self, forKey: .strategy)
        strategy = strategyDecoded
        let partitionCountDecoded = try containerValues.decode(Int.self, forKey: .partitionCount)
        partitionCount = partitionCountDecoded
        let groupIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .groupId)
        groupId = groupIdDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
    }
}
