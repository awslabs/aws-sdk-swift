// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension VpcEndpoint: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case creationTimestamp = "creationTimestamp"
        case dnsEntries = "dnsEntrySet"
        case groups = "groupSet"
        case lastError = "lastError"
        case networkInterfaceIds = "networkInterfaceIdSet"
        case ownerId = "ownerId"
        case policyDocument = "policyDocument"
        case privateDnsEnabled = "privateDnsEnabled"
        case requesterManaged = "requesterManaged"
        case routeTableIds = "routeTableIdSet"
        case serviceName = "serviceName"
        case state = "state"
        case subnetIds = "subnetIdSet"
        case tags = "tagSet"
        case vpcEndpointId = "vpcEndpointId"
        case vpcEndpointType = "vpcEndpointType"
        case vpcId = "vpcId"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let creationTimestamp = creationTimestamp {
            try container.encode(TimestampWrapper(creationTimestamp, format: .dateTime), forKey: Key("CreationTimestamp"))
        }
        if let dnsEntries = dnsEntries {
            if !dnsEntries.isEmpty {
                for (index0, dnsentry0) in dnsEntries.enumerated() {
                    var dnsEntriesContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("DnsEntrySet.\(index0.advanced(by: 1))"))
                    try dnsEntriesContainer0.encode(dnsentry0, forKey: Key(""))
                }
            }
        }
        if let groups = groups {
            if !groups.isEmpty {
                for (index0, securitygroupidentifier0) in groups.enumerated() {
                    var groupsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("GroupSet.\(index0.advanced(by: 1))"))
                    try groupsContainer0.encode(securitygroupidentifier0, forKey: Key(""))
                }
            }
        }
        if let lastError = lastError {
            try container.encode(lastError, forKey: Key("LastError"))
        }
        if let networkInterfaceIds = networkInterfaceIds {
            if !networkInterfaceIds.isEmpty {
                for (index0, string0) in networkInterfaceIds.enumerated() {
                    var networkInterfaceIdsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("NetworkInterfaceIdSet.\(index0.advanced(by: 1))"))
                    try networkInterfaceIdsContainer0.encode(string0, forKey: Key(""))
                }
            }
        }
        if let ownerId = ownerId {
            try container.encode(ownerId, forKey: Key("OwnerId"))
        }
        if let policyDocument = policyDocument {
            try container.encode(policyDocument, forKey: Key("PolicyDocument"))
        }
        if privateDnsEnabled != false {
            try container.encode(privateDnsEnabled, forKey: Key("PrivateDnsEnabled"))
        }
        if requesterManaged != false {
            try container.encode(requesterManaged, forKey: Key("RequesterManaged"))
        }
        if let routeTableIds = routeTableIds {
            if !routeTableIds.isEmpty {
                for (index0, string0) in routeTableIds.enumerated() {
                    var routeTableIdsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("RouteTableIdSet.\(index0.advanced(by: 1))"))
                    try routeTableIdsContainer0.encode(string0, forKey: Key(""))
                }
            }
        }
        if let serviceName = serviceName {
            try container.encode(serviceName, forKey: Key("ServiceName"))
        }
        if let state = state {
            try container.encode(state, forKey: Key("State"))
        }
        if let subnetIds = subnetIds {
            if !subnetIds.isEmpty {
                for (index0, string0) in subnetIds.enumerated() {
                    var subnetIdsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("SubnetIdSet.\(index0.advanced(by: 1))"))
                    try subnetIdsContainer0.encode(string0, forKey: Key(""))
                }
            }
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let vpcEndpointId = vpcEndpointId {
            try container.encode(vpcEndpointId, forKey: Key("VpcEndpointId"))
        }
        if let vpcEndpointType = vpcEndpointType {
            try container.encode(vpcEndpointType, forKey: Key("VpcEndpointType"))
        }
        if let vpcId = vpcId {
            try container.encode(vpcId, forKey: Key("VpcId"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let vpcEndpointIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .vpcEndpointId)
        vpcEndpointId = vpcEndpointIdDecoded
        let vpcEndpointTypeDecoded = try containerValues.decodeIfPresent(VpcEndpointType.self, forKey: .vpcEndpointType)
        vpcEndpointType = vpcEndpointTypeDecoded
        let vpcIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .vpcId)
        vpcId = vpcIdDecoded
        let serviceNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceName)
        serviceName = serviceNameDecoded
        let stateDecoded = try containerValues.decodeIfPresent(State.self, forKey: .state)
        state = stateDecoded
        let policyDocumentDecoded = try containerValues.decodeIfPresent(String.self, forKey: .policyDocument)
        policyDocument = policyDocumentDecoded
        if containerValues.contains(.routeTableIds) {
            struct KeyVal0{struct item{}}
            let routeTableIdsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .routeTableIds)
            if let routeTableIdsWrappedContainer = routeTableIdsWrappedContainer {
                let routeTableIdsContainer = try routeTableIdsWrappedContainer.decodeIfPresent([String].self, forKey: .member)
                var routeTableIdsBuffer:[String]? = nil
                if let routeTableIdsContainer = routeTableIdsContainer {
                    routeTableIdsBuffer = [String]()
                    for stringContainer0 in routeTableIdsContainer {
                        routeTableIdsBuffer?.append(stringContainer0)
                    }
                }
                routeTableIds = routeTableIdsBuffer
            } else {
                routeTableIds = []
            }
        } else {
            routeTableIds = nil
        }
        if containerValues.contains(.subnetIds) {
            struct KeyVal0{struct item{}}
            let subnetIdsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .subnetIds)
            if let subnetIdsWrappedContainer = subnetIdsWrappedContainer {
                let subnetIdsContainer = try subnetIdsWrappedContainer.decodeIfPresent([String].self, forKey: .member)
                var subnetIdsBuffer:[String]? = nil
                if let subnetIdsContainer = subnetIdsContainer {
                    subnetIdsBuffer = [String]()
                    for stringContainer0 in subnetIdsContainer {
                        subnetIdsBuffer?.append(stringContainer0)
                    }
                }
                subnetIds = subnetIdsBuffer
            } else {
                subnetIds = []
            }
        } else {
            subnetIds = nil
        }
        if containerValues.contains(.groups) {
            struct KeyVal0{struct item{}}
            let groupsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .groups)
            if let groupsWrappedContainer = groupsWrappedContainer {
                let groupsContainer = try groupsWrappedContainer.decodeIfPresent([SecurityGroupIdentifier].self, forKey: .member)
                var groupsBuffer:[SecurityGroupIdentifier]? = nil
                if let groupsContainer = groupsContainer {
                    groupsBuffer = [SecurityGroupIdentifier]()
                    for structureContainer0 in groupsContainer {
                        groupsBuffer?.append(structureContainer0)
                    }
                }
                groups = groupsBuffer
            } else {
                groups = []
            }
        } else {
            groups = nil
        }
        let privateDnsEnabledDecoded = try containerValues.decode(Bool.self, forKey: .privateDnsEnabled)
        privateDnsEnabled = privateDnsEnabledDecoded
        let requesterManagedDecoded = try containerValues.decode(Bool.self, forKey: .requesterManaged)
        requesterManaged = requesterManagedDecoded
        if containerValues.contains(.networkInterfaceIds) {
            struct KeyVal0{struct item{}}
            let networkInterfaceIdsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .networkInterfaceIds)
            if let networkInterfaceIdsWrappedContainer = networkInterfaceIdsWrappedContainer {
                let networkInterfaceIdsContainer = try networkInterfaceIdsWrappedContainer.decodeIfPresent([String].self, forKey: .member)
                var networkInterfaceIdsBuffer:[String]? = nil
                if let networkInterfaceIdsContainer = networkInterfaceIdsContainer {
                    networkInterfaceIdsBuffer = [String]()
                    for stringContainer0 in networkInterfaceIdsContainer {
                        networkInterfaceIdsBuffer?.append(stringContainer0)
                    }
                }
                networkInterfaceIds = networkInterfaceIdsBuffer
            } else {
                networkInterfaceIds = []
            }
        } else {
            networkInterfaceIds = nil
        }
        if containerValues.contains(.dnsEntries) {
            struct KeyVal0{struct item{}}
            let dnsEntriesWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .dnsEntries)
            if let dnsEntriesWrappedContainer = dnsEntriesWrappedContainer {
                let dnsEntriesContainer = try dnsEntriesWrappedContainer.decodeIfPresent([DnsEntry].self, forKey: .member)
                var dnsEntriesBuffer:[DnsEntry]? = nil
                if let dnsEntriesContainer = dnsEntriesContainer {
                    dnsEntriesBuffer = [DnsEntry]()
                    for structureContainer0 in dnsEntriesContainer {
                        dnsEntriesBuffer?.append(structureContainer0)
                    }
                }
                dnsEntries = dnsEntriesBuffer
            } else {
                dnsEntries = []
            }
        } else {
            dnsEntries = nil
        }
        let creationTimestampDecoded = try containerValues.decodeIfPresent(String.self, forKey: .creationTimestamp)
        var creationTimestampBuffer:Date? = nil
        if let creationTimestampDecoded = creationTimestampDecoded {
            creationTimestampBuffer = try TimestampWrapperDecoder.parseDateStringValue(creationTimestampDecoded, format: .dateTime)
        }
        creationTimestamp = creationTimestampBuffer
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
        let ownerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerId)
        ownerId = ownerIdDecoded
        let lastErrorDecoded = try containerValues.decodeIfPresent(LastError.self, forKey: .lastError)
        lastError = lastErrorDecoded
    }
}
