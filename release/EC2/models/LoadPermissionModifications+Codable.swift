// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension LoadPermissionModifications: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case add = "Add"
        case remove = "Remove"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let add = add {
            if !add.isEmpty {
                for (index0, loadpermissionrequest0) in add.enumerated() {
                    var addContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Add.\(index0.advanced(by: 1))"))
                    try addContainer0.encode(loadpermissionrequest0, forKey: Key(""))
                }
            }
        }
        if let remove = remove {
            if !remove.isEmpty {
                for (index0, loadpermissionrequest0) in remove.enumerated() {
                    var removeContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Remove.\(index0.advanced(by: 1))"))
                    try removeContainer0.encode(loadpermissionrequest0, forKey: Key(""))
                }
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        if containerValues.contains(.add) {
            struct KeyVal0{struct item{}}
            let addWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .add)
            if let addWrappedContainer = addWrappedContainer {
                let addContainer = try addWrappedContainer.decodeIfPresent([LoadPermissionRequest].self, forKey: .member)
                var addBuffer:[LoadPermissionRequest]? = nil
                if let addContainer = addContainer {
                    addBuffer = [LoadPermissionRequest]()
                    for structureContainer0 in addContainer {
                        addBuffer?.append(structureContainer0)
                    }
                }
                add = addBuffer
            } else {
                add = []
            }
        } else {
            add = nil
        }
        if containerValues.contains(.remove) {
            struct KeyVal0{struct item{}}
            let removeWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .remove)
            if let removeWrappedContainer = removeWrappedContainer {
                let removeContainer = try removeWrappedContainer.decodeIfPresent([LoadPermissionRequest].self, forKey: .member)
                var removeBuffer:[LoadPermissionRequest]? = nil
                if let removeContainer = removeContainer {
                    removeBuffer = [LoadPermissionRequest]()
                    for structureContainer0 in removeContainer {
                        removeBuffer?.append(structureContainer0)
                    }
                }
                remove = removeBuffer
            } else {
                remove = []
            }
        } else {
            remove = nil
        }
    }
}
