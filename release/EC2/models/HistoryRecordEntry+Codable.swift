// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension HistoryRecordEntry: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case eventInformation = "eventInformation"
        case eventType = "eventType"
        case timestamp = "timestamp"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let eventInformation = eventInformation {
            try container.encode(eventInformation, forKey: Key("EventInformation"))
        }
        if let eventType = eventType {
            try container.encode(eventType, forKey: Key("EventType"))
        }
        if let timestamp = timestamp {
            try container.encode(TimestampWrapper(timestamp, format: .dateTime), forKey: Key("Timestamp"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let eventInformationDecoded = try containerValues.decodeIfPresent(EventInformation.self, forKey: .eventInformation)
        eventInformation = eventInformationDecoded
        let eventTypeDecoded = try containerValues.decodeIfPresent(FleetEventType.self, forKey: .eventType)
        eventType = eventTypeDecoded
        let timestampDecoded = try containerValues.decodeIfPresent(String.self, forKey: .timestamp)
        var timestampBuffer:Date? = nil
        if let timestampDecoded = timestampDecoded {
            timestampBuffer = try TimestampWrapperDecoder.parseDateStringValue(timestampDecoded, format: .dateTime)
        }
        timestamp = timestampBuffer
    }
}
