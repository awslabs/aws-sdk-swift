// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension ClientVpnEndpoint: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case associatedTargetNetworks = "associatedTargetNetwork"
        case authenticationOptions = "authenticationOptions"
        case clientCidrBlock = "clientCidrBlock"
        case clientConnectOptions = "clientConnectOptions"
        case clientVpnEndpointId = "clientVpnEndpointId"
        case connectionLogOptions = "connectionLogOptions"
        case creationTime = "creationTime"
        case deletionTime = "deletionTime"
        case description = "description"
        case dnsName = "dnsName"
        case dnsServers = "dnsServer"
        case securityGroupIds = "securityGroupIdSet"
        case selfServicePortalUrl = "selfServicePortalUrl"
        case serverCertificateArn = "serverCertificateArn"
        case splitTunnel = "splitTunnel"
        case status = "status"
        case tags = "tagSet"
        case transportProtocol = "transportProtocol"
        case vpcId = "vpcId"
        case vpnPort = "vpnPort"
        case vpnProtocol = "vpnProtocol"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let associatedTargetNetworks = associatedTargetNetworks {
            if !associatedTargetNetworks.isEmpty {
                for (index0, associatedtargetnetwork0) in associatedTargetNetworks.enumerated() {
                    var associatedTargetNetworksContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("AssociatedTargetNetwork.\(index0.advanced(by: 1))"))
                    try associatedTargetNetworksContainer0.encode(associatedtargetnetwork0, forKey: Key(""))
                }
            }
        }
        if let authenticationOptions = authenticationOptions {
            if !authenticationOptions.isEmpty {
                for (index0, clientvpnauthentication0) in authenticationOptions.enumerated() {
                    var authenticationOptionsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("AuthenticationOptions.\(index0.advanced(by: 1))"))
                    try authenticationOptionsContainer0.encode(clientvpnauthentication0, forKey: Key(""))
                }
            }
        }
        if let clientCidrBlock = clientCidrBlock {
            try container.encode(clientCidrBlock, forKey: Key("ClientCidrBlock"))
        }
        if let clientConnectOptions = clientConnectOptions {
            try container.encode(clientConnectOptions, forKey: Key("ClientConnectOptions"))
        }
        if let clientVpnEndpointId = clientVpnEndpointId {
            try container.encode(clientVpnEndpointId, forKey: Key("ClientVpnEndpointId"))
        }
        if let connectionLogOptions = connectionLogOptions {
            try container.encode(connectionLogOptions, forKey: Key("ConnectionLogOptions"))
        }
        if let creationTime = creationTime {
            try container.encode(creationTime, forKey: Key("CreationTime"))
        }
        if let deletionTime = deletionTime {
            try container.encode(deletionTime, forKey: Key("DeletionTime"))
        }
        if let description = description {
            try container.encode(description, forKey: Key("Description"))
        }
        if let dnsName = dnsName {
            try container.encode(dnsName, forKey: Key("DnsName"))
        }
        if let dnsServers = dnsServers {
            if !dnsServers.isEmpty {
                for (index0, string0) in dnsServers.enumerated() {
                    var dnsServersContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("DnsServer.\(index0.advanced(by: 1))"))
                    try dnsServersContainer0.encode(string0, forKey: Key(""))
                }
            }
        }
        if let securityGroupIds = securityGroupIds {
            if !securityGroupIds.isEmpty {
                for (index0, securitygroupid0) in securityGroupIds.enumerated() {
                    var securityGroupIdsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("SecurityGroupIdSet.\(index0.advanced(by: 1))"))
                    try securityGroupIdsContainer0.encode(securitygroupid0, forKey: Key(""))
                }
            }
        }
        if let selfServicePortalUrl = selfServicePortalUrl {
            try container.encode(selfServicePortalUrl, forKey: Key("SelfServicePortalUrl"))
        }
        if let serverCertificateArn = serverCertificateArn {
            try container.encode(serverCertificateArn, forKey: Key("ServerCertificateArn"))
        }
        if splitTunnel != false {
            try container.encode(splitTunnel, forKey: Key("SplitTunnel"))
        }
        if let status = status {
            try container.encode(status, forKey: Key("Status"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let transportProtocol = transportProtocol {
            try container.encode(transportProtocol, forKey: Key("TransportProtocol"))
        }
        if let vpcId = vpcId {
            try container.encode(vpcId, forKey: Key("VpcId"))
        }
        if vpnPort != 0 {
            try container.encode(vpnPort, forKey: Key("VpnPort"))
        }
        if let vpnProtocol = vpnProtocol {
            try container.encode(vpnProtocol, forKey: Key("VpnProtocol"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let clientVpnEndpointIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clientVpnEndpointId)
        clientVpnEndpointId = clientVpnEndpointIdDecoded
        let descriptionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .description)
        description = descriptionDecoded
        let statusDecoded = try containerValues.decodeIfPresent(ClientVpnEndpointStatus.self, forKey: .status)
        status = statusDecoded
        let creationTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .creationTime)
        creationTime = creationTimeDecoded
        let deletionTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .deletionTime)
        deletionTime = deletionTimeDecoded
        let dnsNameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .dnsName)
        dnsName = dnsNameDecoded
        let clientCidrBlockDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clientCidrBlock)
        clientCidrBlock = clientCidrBlockDecoded
        if containerValues.contains(.dnsServers) {
            struct KeyVal0{struct item{}}
            let dnsServersWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .dnsServers)
            if let dnsServersWrappedContainer = dnsServersWrappedContainer {
                let dnsServersContainer = try dnsServersWrappedContainer.decodeIfPresent([String].self, forKey: .member)
                var dnsServersBuffer:[String]? = nil
                if let dnsServersContainer = dnsServersContainer {
                    dnsServersBuffer = [String]()
                    for stringContainer0 in dnsServersContainer {
                        dnsServersBuffer?.append(stringContainer0)
                    }
                }
                dnsServers = dnsServersBuffer
            } else {
                dnsServers = []
            }
        } else {
            dnsServers = nil
        }
        let splitTunnelDecoded = try containerValues.decode(Bool.self, forKey: .splitTunnel)
        splitTunnel = splitTunnelDecoded
        let vpnProtocolDecoded = try containerValues.decodeIfPresent(VpnProtocol.self, forKey: .vpnProtocol)
        vpnProtocol = vpnProtocolDecoded
        let transportProtocolDecoded = try containerValues.decodeIfPresent(TransportProtocol.self, forKey: .transportProtocol)
        transportProtocol = transportProtocolDecoded
        let vpnPortDecoded = try containerValues.decode(Int.self, forKey: .vpnPort)
        vpnPort = vpnPortDecoded
        if containerValues.contains(.associatedTargetNetworks) {
            struct KeyVal0{struct item{}}
            let associatedTargetNetworksWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .associatedTargetNetworks)
            if let associatedTargetNetworksWrappedContainer = associatedTargetNetworksWrappedContainer {
                let associatedTargetNetworksContainer = try associatedTargetNetworksWrappedContainer.decodeIfPresent([AssociatedTargetNetwork].self, forKey: .member)
                var associatedTargetNetworksBuffer:[AssociatedTargetNetwork]? = nil
                if let associatedTargetNetworksContainer = associatedTargetNetworksContainer {
                    associatedTargetNetworksBuffer = [AssociatedTargetNetwork]()
                    for structureContainer0 in associatedTargetNetworksContainer {
                        associatedTargetNetworksBuffer?.append(structureContainer0)
                    }
                }
                associatedTargetNetworks = associatedTargetNetworksBuffer
            } else {
                associatedTargetNetworks = []
            }
        } else {
            associatedTargetNetworks = nil
        }
        let serverCertificateArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serverCertificateArn)
        serverCertificateArn = serverCertificateArnDecoded
        if containerValues.contains(.authenticationOptions) {
            struct KeyVal0{struct item{}}
            let authenticationOptionsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .authenticationOptions)
            if let authenticationOptionsWrappedContainer = authenticationOptionsWrappedContainer {
                let authenticationOptionsContainer = try authenticationOptionsWrappedContainer.decodeIfPresent([ClientVpnAuthentication].self, forKey: .member)
                var authenticationOptionsBuffer:[ClientVpnAuthentication]? = nil
                if let authenticationOptionsContainer = authenticationOptionsContainer {
                    authenticationOptionsBuffer = [ClientVpnAuthentication]()
                    for structureContainer0 in authenticationOptionsContainer {
                        authenticationOptionsBuffer?.append(structureContainer0)
                    }
                }
                authenticationOptions = authenticationOptionsBuffer
            } else {
                authenticationOptions = []
            }
        } else {
            authenticationOptions = nil
        }
        let connectionLogOptionsDecoded = try containerValues.decodeIfPresent(ConnectionLogResponseOptions.self, forKey: .connectionLogOptions)
        connectionLogOptions = connectionLogOptionsDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
        if containerValues.contains(.securityGroupIds) {
            struct KeyVal0{struct item{}}
            let securityGroupIdsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .securityGroupIds)
            if let securityGroupIdsWrappedContainer = securityGroupIdsWrappedContainer {
                let securityGroupIdsContainer = try securityGroupIdsWrappedContainer.decodeIfPresent([String].self, forKey: .member)
                var securityGroupIdsBuffer:[String]? = nil
                if let securityGroupIdsContainer = securityGroupIdsContainer {
                    securityGroupIdsBuffer = [String]()
                    for stringContainer0 in securityGroupIdsContainer {
                        securityGroupIdsBuffer?.append(stringContainer0)
                    }
                }
                securityGroupIds = securityGroupIdsBuffer
            } else {
                securityGroupIds = []
            }
        } else {
            securityGroupIds = nil
        }
        let vpcIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .vpcId)
        vpcId = vpcIdDecoded
        let selfServicePortalUrlDecoded = try containerValues.decodeIfPresent(String.self, forKey: .selfServicePortalUrl)
        selfServicePortalUrl = selfServicePortalUrlDecoded
        let clientConnectOptionsDecoded = try containerValues.decodeIfPresent(ClientConnectResponseOptions.self, forKey: .clientConnectOptions)
        clientConnectOptions = clientConnectOptionsDecoded
    }
}
