// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension FleetData: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case activityStatus = "activityStatus"
        case clientToken = "clientToken"
        case createTime = "createTime"
        case errors = "errorSet"
        case excessCapacityTerminationPolicy = "excessCapacityTerminationPolicy"
        case fleetId = "fleetId"
        case fleetState = "fleetState"
        case fulfilledCapacity = "fulfilledCapacity"
        case fulfilledOnDemandCapacity = "fulfilledOnDemandCapacity"
        case instances = "fleetInstanceSet"
        case launchTemplateConfigs = "launchTemplateConfigs"
        case onDemandOptions = "onDemandOptions"
        case replaceUnhealthyInstances = "replaceUnhealthyInstances"
        case spotOptions = "spotOptions"
        case tags = "tagSet"
        case targetCapacitySpecification = "targetCapacitySpecification"
        case terminateInstancesWithExpiration = "terminateInstancesWithExpiration"
        case type = "type"
        case validFrom = "validFrom"
        case validUntil = "validUntil"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let activityStatus = activityStatus {
            try container.encode(activityStatus, forKey: Key("ActivityStatus"))
        }
        if let clientToken = clientToken {
            try container.encode(clientToken, forKey: Key("ClientToken"))
        }
        if let createTime = createTime {
            try container.encode(TimestampWrapper(createTime, format: .dateTime), forKey: Key("CreateTime"))
        }
        if let errors = errors {
            if !errors.isEmpty {
                for (index0, describefleeterror0) in errors.enumerated() {
                    var errorsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("ErrorSet.\(index0.advanced(by: 1))"))
                    try errorsContainer0.encode(describefleeterror0, forKey: Key(""))
                }
            }
        }
        if let excessCapacityTerminationPolicy = excessCapacityTerminationPolicy {
            try container.encode(excessCapacityTerminationPolicy, forKey: Key("ExcessCapacityTerminationPolicy"))
        }
        if let fleetId = fleetId {
            try container.encode(fleetId, forKey: Key("FleetId"))
        }
        if let fleetState = fleetState {
            try container.encode(fleetState, forKey: Key("FleetState"))
        }
        if fulfilledCapacity != 0.0 {
            try container.encode(String(fulfilledCapacity), forKey: Key("FulfilledCapacity"))
        }
        if fulfilledOnDemandCapacity != 0.0 {
            try container.encode(String(fulfilledOnDemandCapacity), forKey: Key("FulfilledOnDemandCapacity"))
        }
        if let instances = instances {
            if !instances.isEmpty {
                for (index0, describefleetsinstances0) in instances.enumerated() {
                    var instancesContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("FleetInstanceSet.\(index0.advanced(by: 1))"))
                    try instancesContainer0.encode(describefleetsinstances0, forKey: Key(""))
                }
            }
        }
        if let launchTemplateConfigs = launchTemplateConfigs {
            if !launchTemplateConfigs.isEmpty {
                for (index0, fleetlaunchtemplateconfig0) in launchTemplateConfigs.enumerated() {
                    var launchTemplateConfigsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("LaunchTemplateConfigs.\(index0.advanced(by: 1))"))
                    try launchTemplateConfigsContainer0.encode(fleetlaunchtemplateconfig0, forKey: Key(""))
                }
            }
        }
        if let onDemandOptions = onDemandOptions {
            try container.encode(onDemandOptions, forKey: Key("OnDemandOptions"))
        }
        if replaceUnhealthyInstances != false {
            try container.encode(replaceUnhealthyInstances, forKey: Key("ReplaceUnhealthyInstances"))
        }
        if let spotOptions = spotOptions {
            try container.encode(spotOptions, forKey: Key("SpotOptions"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let targetCapacitySpecification = targetCapacitySpecification {
            try container.encode(targetCapacitySpecification, forKey: Key("TargetCapacitySpecification"))
        }
        if terminateInstancesWithExpiration != false {
            try container.encode(terminateInstancesWithExpiration, forKey: Key("TerminateInstancesWithExpiration"))
        }
        if let type = type {
            try container.encode(type, forKey: Key("Type"))
        }
        if let validFrom = validFrom {
            try container.encode(TimestampWrapper(validFrom, format: .dateTime), forKey: Key("ValidFrom"))
        }
        if let validUntil = validUntil {
            try container.encode(TimestampWrapper(validUntil, format: .dateTime), forKey: Key("ValidUntil"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let activityStatusDecoded = try containerValues.decodeIfPresent(FleetActivityStatus.self, forKey: .activityStatus)
        activityStatus = activityStatusDecoded
        let createTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .createTime)
        var createTimeBuffer:Date? = nil
        if let createTimeDecoded = createTimeDecoded {
            createTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(createTimeDecoded, format: .dateTime)
        }
        createTime = createTimeBuffer
        let fleetIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .fleetId)
        fleetId = fleetIdDecoded
        let fleetStateDecoded = try containerValues.decodeIfPresent(FleetStateCode.self, forKey: .fleetState)
        fleetState = fleetStateDecoded
        let clientTokenDecoded = try containerValues.decodeIfPresent(String.self, forKey: .clientToken)
        clientToken = clientTokenDecoded
        let excessCapacityTerminationPolicyDecoded = try containerValues.decodeIfPresent(FleetExcessCapacityTerminationPolicy.self, forKey: .excessCapacityTerminationPolicy)
        excessCapacityTerminationPolicy = excessCapacityTerminationPolicyDecoded
        let fulfilledCapacityDecoded = try containerValues.decode(Double.self, forKey: .fulfilledCapacity)
        fulfilledCapacity = fulfilledCapacityDecoded
        let fulfilledOnDemandCapacityDecoded = try containerValues.decode(Double.self, forKey: .fulfilledOnDemandCapacity)
        fulfilledOnDemandCapacity = fulfilledOnDemandCapacityDecoded
        if containerValues.contains(.launchTemplateConfigs) {
            struct KeyVal0{struct item{}}
            let launchTemplateConfigsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .launchTemplateConfigs)
            if let launchTemplateConfigsWrappedContainer = launchTemplateConfigsWrappedContainer {
                let launchTemplateConfigsContainer = try launchTemplateConfigsWrappedContainer.decodeIfPresent([FleetLaunchTemplateConfig].self, forKey: .member)
                var launchTemplateConfigsBuffer:[FleetLaunchTemplateConfig]? = nil
                if let launchTemplateConfigsContainer = launchTemplateConfigsContainer {
                    launchTemplateConfigsBuffer = [FleetLaunchTemplateConfig]()
                    for structureContainer0 in launchTemplateConfigsContainer {
                        launchTemplateConfigsBuffer?.append(structureContainer0)
                    }
                }
                launchTemplateConfigs = launchTemplateConfigsBuffer
            } else {
                launchTemplateConfigs = []
            }
        } else {
            launchTemplateConfigs = nil
        }
        let targetCapacitySpecificationDecoded = try containerValues.decodeIfPresent(TargetCapacitySpecification.self, forKey: .targetCapacitySpecification)
        targetCapacitySpecification = targetCapacitySpecificationDecoded
        let terminateInstancesWithExpirationDecoded = try containerValues.decode(Bool.self, forKey: .terminateInstancesWithExpiration)
        terminateInstancesWithExpiration = terminateInstancesWithExpirationDecoded
        let typeDecoded = try containerValues.decodeIfPresent(FleetType.self, forKey: .type)
        type = typeDecoded
        let validFromDecoded = try containerValues.decodeIfPresent(String.self, forKey: .validFrom)
        var validFromBuffer:Date? = nil
        if let validFromDecoded = validFromDecoded {
            validFromBuffer = try TimestampWrapperDecoder.parseDateStringValue(validFromDecoded, format: .dateTime)
        }
        validFrom = validFromBuffer
        let validUntilDecoded = try containerValues.decodeIfPresent(String.self, forKey: .validUntil)
        var validUntilBuffer:Date? = nil
        if let validUntilDecoded = validUntilDecoded {
            validUntilBuffer = try TimestampWrapperDecoder.parseDateStringValue(validUntilDecoded, format: .dateTime)
        }
        validUntil = validUntilBuffer
        let replaceUnhealthyInstancesDecoded = try containerValues.decode(Bool.self, forKey: .replaceUnhealthyInstances)
        replaceUnhealthyInstances = replaceUnhealthyInstancesDecoded
        let spotOptionsDecoded = try containerValues.decodeIfPresent(SpotOptions.self, forKey: .spotOptions)
        spotOptions = spotOptionsDecoded
        let onDemandOptionsDecoded = try containerValues.decodeIfPresent(OnDemandOptions.self, forKey: .onDemandOptions)
        onDemandOptions = onDemandOptionsDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
        if containerValues.contains(.errors) {
            struct KeyVal0{struct item{}}
            let errorsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .errors)
            if let errorsWrappedContainer = errorsWrappedContainer {
                let errorsContainer = try errorsWrappedContainer.decodeIfPresent([DescribeFleetError].self, forKey: .member)
                var errorsBuffer:[DescribeFleetError]? = nil
                if let errorsContainer = errorsContainer {
                    errorsBuffer = [DescribeFleetError]()
                    for structureContainer0 in errorsContainer {
                        errorsBuffer?.append(structureContainer0)
                    }
                }
                errors = errorsBuffer
            } else {
                errors = []
            }
        } else {
            errors = nil
        }
        if containerValues.contains(.instances) {
            struct KeyVal0{struct item{}}
            let instancesWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .instances)
            if let instancesWrappedContainer = instancesWrappedContainer {
                let instancesContainer = try instancesWrappedContainer.decodeIfPresent([DescribeFleetsInstances].self, forKey: .member)
                var instancesBuffer:[DescribeFleetsInstances]? = nil
                if let instancesContainer = instancesContainer {
                    instancesBuffer = [DescribeFleetsInstances]()
                    for structureContainer0 in instancesContainer {
                        instancesBuffer?.append(structureContainer0)
                    }
                }
                instances = instancesBuffer
            } else {
                instances = []
            }
        } else {
            instances = nil
        }
    }
}
