// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension FpgaImage: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case createTime = "createTime"
        case dataRetentionSupport = "dataRetentionSupport"
        case description = "description"
        case fpgaImageGlobalId = "fpgaImageGlobalId"
        case fpgaImageId = "fpgaImageId"
        case name = "name"
        case ownerAlias = "ownerAlias"
        case ownerId = "ownerId"
        case pciId = "pciId"
        case productCodes = "productCodes"
        case `public` = "public"
        case shellVersion = "shellVersion"
        case state = "state"
        case tags = "tags"
        case updateTime = "updateTime"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let createTime = createTime {
            try container.encode(TimestampWrapper(createTime, format: .dateTime), forKey: Key("CreateTime"))
        }
        if dataRetentionSupport != false {
            try container.encode(dataRetentionSupport, forKey: Key("DataRetentionSupport"))
        }
        if let description = description {
            try container.encode(description, forKey: Key("Description"))
        }
        if let fpgaImageGlobalId = fpgaImageGlobalId {
            try container.encode(fpgaImageGlobalId, forKey: Key("FpgaImageGlobalId"))
        }
        if let fpgaImageId = fpgaImageId {
            try container.encode(fpgaImageId, forKey: Key("FpgaImageId"))
        }
        if let name = name {
            try container.encode(name, forKey: Key("Name"))
        }
        if let ownerAlias = ownerAlias {
            try container.encode(ownerAlias, forKey: Key("OwnerAlias"))
        }
        if let ownerId = ownerId {
            try container.encode(ownerId, forKey: Key("OwnerId"))
        }
        if let pciId = pciId {
            try container.encode(pciId, forKey: Key("PciId"))
        }
        if let productCodes = productCodes {
            if !productCodes.isEmpty {
                for (index0, productcode0) in productCodes.enumerated() {
                    var productCodesContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("ProductCodes.\(index0.advanced(by: 1))"))
                    try productCodesContainer0.encode(productcode0, forKey: Key(""))
                }
            }
        }
        if `public` != false {
            try container.encode(`public`, forKey: Key("Public"))
        }
        if let shellVersion = shellVersion {
            try container.encode(shellVersion, forKey: Key("ShellVersion"))
        }
        if let state = state {
            try container.encode(state, forKey: Key("State"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("Tags.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let updateTime = updateTime {
            try container.encode(TimestampWrapper(updateTime, format: .dateTime), forKey: Key("UpdateTime"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let fpgaImageIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .fpgaImageId)
        fpgaImageId = fpgaImageIdDecoded
        let fpgaImageGlobalIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .fpgaImageGlobalId)
        fpgaImageGlobalId = fpgaImageGlobalIdDecoded
        let nameDecoded = try containerValues.decodeIfPresent(String.self, forKey: .name)
        name = nameDecoded
        let descriptionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .description)
        description = descriptionDecoded
        let shellVersionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .shellVersion)
        shellVersion = shellVersionDecoded
        let pciIdDecoded = try containerValues.decodeIfPresent(PciId.self, forKey: .pciId)
        pciId = pciIdDecoded
        let stateDecoded = try containerValues.decodeIfPresent(FpgaImageState.self, forKey: .state)
        state = stateDecoded
        let createTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .createTime)
        var createTimeBuffer:Date? = nil
        if let createTimeDecoded = createTimeDecoded {
            createTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(createTimeDecoded, format: .dateTime)
        }
        createTime = createTimeBuffer
        let updateTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .updateTime)
        var updateTimeBuffer:Date? = nil
        if let updateTimeDecoded = updateTimeDecoded {
            updateTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(updateTimeDecoded, format: .dateTime)
        }
        updateTime = updateTimeBuffer
        let ownerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerId)
        ownerId = ownerIdDecoded
        let ownerAliasDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerAlias)
        ownerAlias = ownerAliasDecoded
        if containerValues.contains(.productCodes) {
            struct KeyVal0{struct item{}}
            let productCodesWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .productCodes)
            if let productCodesWrappedContainer = productCodesWrappedContainer {
                let productCodesContainer = try productCodesWrappedContainer.decodeIfPresent([ProductCode].self, forKey: .member)
                var productCodesBuffer:[ProductCode]? = nil
                if let productCodesContainer = productCodesContainer {
                    productCodesBuffer = [ProductCode]()
                    for structureContainer0 in productCodesContainer {
                        productCodesBuffer?.append(structureContainer0)
                    }
                }
                productCodes = productCodesBuffer
            } else {
                productCodes = []
            }
        } else {
            productCodes = nil
        }
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
        let publicDecoded = try containerValues.decode(Bool.self, forKey: .public)
        `public` = publicDecoded
        let dataRetentionSupportDecoded = try containerValues.decode(Bool.self, forKey: .dataRetentionSupport)
        dataRetentionSupport = dataRetentionSupportDecoded
    }
}
