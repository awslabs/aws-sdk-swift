// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension ConnectionNotification: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case connectionEvents = "connectionEvents"
        case connectionNotificationArn = "connectionNotificationArn"
        case connectionNotificationId = "connectionNotificationId"
        case connectionNotificationState = "connectionNotificationState"
        case connectionNotificationType = "connectionNotificationType"
        case serviceId = "serviceId"
        case vpcEndpointId = "vpcEndpointId"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let connectionEvents = connectionEvents {
            if !connectionEvents.isEmpty {
                for (index0, string0) in connectionEvents.enumerated() {
                    var connectionEventsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("ConnectionEvents.\(index0.advanced(by: 1))"))
                    try connectionEventsContainer0.encode(string0, forKey: Key(""))
                }
            }
        }
        if let connectionNotificationArn = connectionNotificationArn {
            try container.encode(connectionNotificationArn, forKey: Key("ConnectionNotificationArn"))
        }
        if let connectionNotificationId = connectionNotificationId {
            try container.encode(connectionNotificationId, forKey: Key("ConnectionNotificationId"))
        }
        if let connectionNotificationState = connectionNotificationState {
            try container.encode(connectionNotificationState, forKey: Key("ConnectionNotificationState"))
        }
        if let connectionNotificationType = connectionNotificationType {
            try container.encode(connectionNotificationType, forKey: Key("ConnectionNotificationType"))
        }
        if let serviceId = serviceId {
            try container.encode(serviceId, forKey: Key("ServiceId"))
        }
        if let vpcEndpointId = vpcEndpointId {
            try container.encode(vpcEndpointId, forKey: Key("VpcEndpointId"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let connectionNotificationIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .connectionNotificationId)
        connectionNotificationId = connectionNotificationIdDecoded
        let serviceIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .serviceId)
        serviceId = serviceIdDecoded
        let vpcEndpointIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .vpcEndpointId)
        vpcEndpointId = vpcEndpointIdDecoded
        let connectionNotificationTypeDecoded = try containerValues.decodeIfPresent(ConnectionNotificationType.self, forKey: .connectionNotificationType)
        connectionNotificationType = connectionNotificationTypeDecoded
        let connectionNotificationArnDecoded = try containerValues.decodeIfPresent(String.self, forKey: .connectionNotificationArn)
        connectionNotificationArn = connectionNotificationArnDecoded
        if containerValues.contains(.connectionEvents) {
            struct KeyVal0{struct item{}}
            let connectionEventsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .connectionEvents)
            if let connectionEventsWrappedContainer = connectionEventsWrappedContainer {
                let connectionEventsContainer = try connectionEventsWrappedContainer.decodeIfPresent([String].self, forKey: .member)
                var connectionEventsBuffer:[String]? = nil
                if let connectionEventsContainer = connectionEventsContainer {
                    connectionEventsBuffer = [String]()
                    for stringContainer0 in connectionEventsContainer {
                        connectionEventsBuffer?.append(stringContainer0)
                    }
                }
                connectionEvents = connectionEventsBuffer
            } else {
                connectionEvents = []
            }
        } else {
            connectionEvents = nil
        }
        let connectionNotificationStateDecoded = try containerValues.decodeIfPresent(ConnectionNotificationState.self, forKey: .connectionNotificationState)
        connectionNotificationState = connectionNotificationStateDecoded
    }
}
