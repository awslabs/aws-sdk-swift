// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension TransitGatewayConnectPeer: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case connectPeerConfiguration = "connectPeerConfiguration"
        case creationTime = "creationTime"
        case state = "state"
        case tags = "tagSet"
        case transitGatewayAttachmentId = "transitGatewayAttachmentId"
        case transitGatewayConnectPeerId = "transitGatewayConnectPeerId"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let connectPeerConfiguration = connectPeerConfiguration {
            try container.encode(connectPeerConfiguration, forKey: Key("ConnectPeerConfiguration"))
        }
        if let creationTime = creationTime {
            try container.encode(TimestampWrapper(creationTime, format: .dateTime), forKey: Key("CreationTime"))
        }
        if let state = state {
            try container.encode(state, forKey: Key("State"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let transitGatewayAttachmentId = transitGatewayAttachmentId {
            try container.encode(transitGatewayAttachmentId, forKey: Key("TransitGatewayAttachmentId"))
        }
        if let transitGatewayConnectPeerId = transitGatewayConnectPeerId {
            try container.encode(transitGatewayConnectPeerId, forKey: Key("TransitGatewayConnectPeerId"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let transitGatewayAttachmentIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .transitGatewayAttachmentId)
        transitGatewayAttachmentId = transitGatewayAttachmentIdDecoded
        let transitGatewayConnectPeerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .transitGatewayConnectPeerId)
        transitGatewayConnectPeerId = transitGatewayConnectPeerIdDecoded
        let stateDecoded = try containerValues.decodeIfPresent(TransitGatewayConnectPeerState.self, forKey: .state)
        state = stateDecoded
        let creationTimeDecoded = try containerValues.decodeIfPresent(String.self, forKey: .creationTime)
        var creationTimeBuffer:Date? = nil
        if let creationTimeDecoded = creationTimeDecoded {
            creationTimeBuffer = try TimestampWrapperDecoder.parseDateStringValue(creationTimeDecoded, format: .dateTime)
        }
        creationTime = creationTimeBuffer
        let connectPeerConfigurationDecoded = try containerValues.decodeIfPresent(TransitGatewayConnectPeerConfiguration.self, forKey: .connectPeerConfiguration)
        connectPeerConfiguration = connectPeerConfigurationDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
    }
}
