// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension IdFormat: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case deadline = "deadline"
        case resource = "resource"
        case useLongIds = "useLongIds"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let deadline = deadline {
            try container.encode(TimestampWrapper(deadline, format: .dateTime), forKey: Key("Deadline"))
        }
        if let resource = resource {
            try container.encode(resource, forKey: Key("Resource"))
        }
        if useLongIds != false {
            try container.encode(useLongIds, forKey: Key("UseLongIds"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let deadlineDecoded = try containerValues.decodeIfPresent(String.self, forKey: .deadline)
        var deadlineBuffer:Date? = nil
        if let deadlineDecoded = deadlineDecoded {
            deadlineBuffer = try TimestampWrapperDecoder.parseDateStringValue(deadlineDecoded, format: .dateTime)
        }
        deadline = deadlineBuffer
        let resourceDecoded = try containerValues.decodeIfPresent(String.self, forKey: .resource)
        resource = resourceDecoded
        let useLongIdsDecoded = try containerValues.decode(Bool.self, forKey: .useLongIds)
        useLongIds = useLongIdsDecoded
    }
}
