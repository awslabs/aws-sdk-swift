// Code generated by smithy-swift-codegen. DO NOT EDIT!



/// <p>Contains the parameters for DescribeScheduledInstanceAvailability.</p>
public struct DescribeScheduledInstanceAvailabilityInput: Equatable {
    /// <p>Checks whether you have the required permissions for the action, without actually making the request,
    ///    and provides an error response. If you have the required permissions, the error response is <code>DryRunOperation</code>.
    ///    Otherwise, it is <code>UnauthorizedOperation</code>.</p>
    public let dryRun: Bool
    /// <p>The filters.</p>
    ///          <ul>
    ///             <li>
    ///                <p>
    ///                   <code>availability-zone</code> - The Availability Zone (for example, <code>us-west-2a</code>).</p>
    ///             </li>
    ///             <li>
    ///                <p>
    ///                   <code>instance-type</code> - The instance type (for example, <code>c4.large</code>).</p>
    ///             </li>
    ///             <li>
    ///                <p>
    ///                   <code>network-platform</code> - The network platform (<code>EC2-Classic</code> or <code>EC2-VPC</code>).</p>
    ///             </li>
    ///             <li>
    ///                <p>
    ///                   <code>platform</code> - The platform (<code>Linux/UNIX</code> or <code>Windows</code>).</p>
    ///             </li>
    ///          </ul>
    public let filters: [Filter]?
    /// <p>The time period for the first schedule to start.</p>
    public let firstSlotStartTimeRange: SlotDateTimeRangeRequest?
    /// <p>The maximum number of results to return in a single call.
    ///          This value can be between 5 and 300. The default value is 300.
    ///          To retrieve the remaining results, make another call with the returned
    ///          <code>NextToken</code> value.</p>
    public let maxResults: Int
    /// <p>The maximum available duration, in hours. This value must be greater than <code>MinSlotDurationInHours</code>
    ///          and less than 1,720.</p>
    public let maxSlotDurationInHours: Int
    /// <p>The minimum available duration, in hours. The minimum required duration is 1,200 hours per year. For example, the minimum daily schedule is 4 hours, the minimum weekly schedule is 24 hours, and the minimum monthly schedule is 100 hours.</p>
    public let minSlotDurationInHours: Int
    /// <p>The token for the next set of results.</p>
    public let nextToken: String?
    /// <p>The schedule recurrence.</p>
    public let recurrence: ScheduledInstanceRecurrenceRequest?

    public init (
        dryRun: Bool = false,
        filters: [Filter]? = nil,
        firstSlotStartTimeRange: SlotDateTimeRangeRequest? = nil,
        maxResults: Int = 0,
        maxSlotDurationInHours: Int = 0,
        minSlotDurationInHours: Int = 0,
        nextToken: String? = nil,
        recurrence: ScheduledInstanceRecurrenceRequest? = nil
    )
    {
        self.dryRun = dryRun
        self.filters = filters
        self.firstSlotStartTimeRange = firstSlotStartTimeRange
        self.maxResults = maxResults
        self.maxSlotDurationInHours = maxSlotDurationInHours
        self.minSlotDurationInHours = minSlotDurationInHours
        self.nextToken = nextToken
        self.recurrence = recurrence
    }
}

extension DescribeScheduledInstanceAvailabilityInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DescribeScheduledInstanceAvailabilityInput(dryRun: \(String(describing: dryRun)), filters: \(String(describing: filters)), firstSlotStartTimeRange: \(String(describing: firstSlotStartTimeRange)), maxResults: \(String(describing: maxResults)), maxSlotDurationInHours: \(String(describing: maxSlotDurationInHours)), minSlotDurationInHours: \(String(describing: minSlotDurationInHours)), nextToken: \(String(describing: nextToken)), recurrence: \(String(describing: recurrence)))"}
}
