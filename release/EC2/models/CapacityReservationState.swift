// Code generated by smithy-swift-codegen. DO NOT EDIT!



public enum CapacityReservationState {
    case active
    case cancelled
    case expired
    case failed
    case pending
    case sdkUnknown(String)
}

extension CapacityReservationState : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [CapacityReservationState] {
        return [
            .active,
            .cancelled,
            .expired,
            .failed,
            .pending,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .active: return "active"
        case .cancelled: return "cancelled"
        case .expired: return "expired"
        case .failed: return "failed"
        case .pending: return "pending"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = CapacityReservationState(rawValue: rawValue) ?? CapacityReservationState.sdkUnknown(rawValue)
    }
}
