// Code generated by smithy-swift-codegen. DO NOT EDIT!



public enum FpgaImageStateCode {
    case available
    case failed
    case pending
    case unavailable
    case sdkUnknown(String)
}

extension FpgaImageStateCode : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [FpgaImageStateCode] {
        return [
            .available,
            .failed,
            .pending,
            .unavailable,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .available: return "available"
        case .failed: return "failed"
        case .pending: return "pending"
        case .unavailable: return "unavailable"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = FpgaImageStateCode(rawValue: rawValue) ?? FpgaImageStateCode.sdkUnknown(rawValue)
    }
}
