// Code generated by smithy-swift-codegen. DO NOT EDIT!

import ClientRuntime

extension TrafficMirrorSession: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case description = "description"
        case networkInterfaceId = "networkInterfaceId"
        case ownerId = "ownerId"
        case packetLength = "packetLength"
        case sessionNumber = "sessionNumber"
        case tags = "tagSet"
        case trafficMirrorFilterId = "trafficMirrorFilterId"
        case trafficMirrorSessionId = "trafficMirrorSessionId"
        case trafficMirrorTargetId = "trafficMirrorTargetId"
        case virtualNetworkId = "virtualNetworkId"
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: Key.self)
        if let description = description {
            try container.encode(description, forKey: Key("Description"))
        }
        if let networkInterfaceId = networkInterfaceId {
            try container.encode(networkInterfaceId, forKey: Key("NetworkInterfaceId"))
        }
        if let ownerId = ownerId {
            try container.encode(ownerId, forKey: Key("OwnerId"))
        }
        if packetLength != 0 {
            try container.encode(packetLength, forKey: Key("PacketLength"))
        }
        if sessionNumber != 0 {
            try container.encode(sessionNumber, forKey: Key("SessionNumber"))
        }
        if let tags = tags {
            if !tags.isEmpty {
                for (index0, tag0) in tags.enumerated() {
                    var tagsContainer0 = container.nestedContainer(keyedBy: Key.self, forKey: Key("TagSet.\(index0.advanced(by: 1))"))
                    try tagsContainer0.encode(tag0, forKey: Key(""))
                }
            }
        }
        if let trafficMirrorFilterId = trafficMirrorFilterId {
            try container.encode(trafficMirrorFilterId, forKey: Key("TrafficMirrorFilterId"))
        }
        if let trafficMirrorSessionId = trafficMirrorSessionId {
            try container.encode(trafficMirrorSessionId, forKey: Key("TrafficMirrorSessionId"))
        }
        if let trafficMirrorTargetId = trafficMirrorTargetId {
            try container.encode(trafficMirrorTargetId, forKey: Key("TrafficMirrorTargetId"))
        }
        if virtualNetworkId != 0 {
            try container.encode(virtualNetworkId, forKey: Key("VirtualNetworkId"))
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let trafficMirrorSessionIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .trafficMirrorSessionId)
        trafficMirrorSessionId = trafficMirrorSessionIdDecoded
        let trafficMirrorTargetIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .trafficMirrorTargetId)
        trafficMirrorTargetId = trafficMirrorTargetIdDecoded
        let trafficMirrorFilterIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .trafficMirrorFilterId)
        trafficMirrorFilterId = trafficMirrorFilterIdDecoded
        let networkInterfaceIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .networkInterfaceId)
        networkInterfaceId = networkInterfaceIdDecoded
        let ownerIdDecoded = try containerValues.decodeIfPresent(String.self, forKey: .ownerId)
        ownerId = ownerIdDecoded
        let packetLengthDecoded = try containerValues.decode(Int.self, forKey: .packetLength)
        packetLength = packetLengthDecoded
        let sessionNumberDecoded = try containerValues.decode(Int.self, forKey: .sessionNumber)
        sessionNumber = sessionNumberDecoded
        let virtualNetworkIdDecoded = try containerValues.decode(Int.self, forKey: .virtualNetworkId)
        virtualNetworkId = virtualNetworkIdDecoded
        let descriptionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .description)
        description = descriptionDecoded
        if containerValues.contains(.tags) {
            struct KeyVal0{struct item{}}
            let tagsWrappedContainer = containerValues.nestedContainerNonThrowable(keyedBy: CollectionMemberCodingKey<KeyVal0.item>.CodingKeys.self, forKey: .tags)
            if let tagsWrappedContainer = tagsWrappedContainer {
                let tagsContainer = try tagsWrappedContainer.decodeIfPresent([Tag].self, forKey: .member)
                var tagsBuffer:[Tag]? = nil
                if let tagsContainer = tagsContainer {
                    tagsBuffer = [Tag]()
                    for structureContainer0 in tagsContainer {
                        tagsBuffer?.append(structureContainer0)
                    }
                }
                tags = tagsBuffer
            } else {
                tags = []
            }
        } else {
            tags = nil
        }
    }
}
