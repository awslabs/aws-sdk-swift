// Code generated by smithy-swift-codegen. DO NOT EDIT!



/// <p>The compression used for Athena query results.</p>
public enum AthenaResultCompressionType {
    case gzip
    case snappy
    case zlib
    case sdkUnknown(String)
}

extension AthenaResultCompressionType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [AthenaResultCompressionType] {
        return [
            .gzip,
            .snappy,
            .zlib,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .gzip: return "GZIP"
        case .snappy: return "SNAPPY"
        case .zlib: return "ZLIB"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = AthenaResultCompressionType(rawValue: rawValue) ?? AthenaResultCompressionType.sdkUnknown(rawValue)
    }
}
