// Code generated by smithy-swift-codegen. DO NOT EDIT!



public enum ModelPackageType {
    case both
    case unversioned
    case versioned
    case sdkUnknown(String)
}

extension ModelPackageType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ModelPackageType] {
        return [
            .both,
            .unversioned,
            .versioned,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .both: return "Both"
        case .unversioned: return "Unversioned"
        case .versioned: return "Versioned"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ModelPackageType(rawValue: rawValue) ?? ModelPackageType.sdkUnknown(rawValue)
    }
}
