// Code generated by smithy-swift-codegen. DO NOT EDIT!
import AWSClientRuntime
import ClientRuntime

extension Bucket: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case count
        case value
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if count != 0 {
            try encodeContainer.encode(count, forKey: .count)
        }
        if let value = value {
            try encodeContainer.encode(value, forKey: .value)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let valueDecoded = try containerValues.decodeIfPresent(String.self, forKey: .value)
        value = valueDecoded
        let countDecoded = try containerValues.decode(Int.self, forKey: .count)
        count = countDecoded
    }
}

extension Bucket: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Bucket(count: \(String(describing: count)), value: \(String(describing: value)))"}
}

/// <p>A container for facet information. </p>
public struct Bucket: Equatable {
    /// <p>The number of hits that contain the facet value in the specified facet field.</p>
    public let count: Int
    /// <p>The  facet value being counted.</p>
    public let value: String?

    public init (
        count: Int = 0,
        value: String? = nil
    )
    {
        self.count = count
        self.value = value
    }
}

extension BucketInfo: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case buckets
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let buckets = buckets {
            var bucketsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .buckets)
            for bucketlist0 in buckets {
                try bucketsContainer.encode(bucketlist0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let bucketsContainer = try containerValues.decodeIfPresent([Bucket?].self, forKey: .buckets)
        var bucketsDecoded0:[Bucket]? = nil
        if let bucketsContainer = bucketsContainer {
            bucketsDecoded0 = [Bucket]()
            for structure0 in bucketsContainer {
                if let structure0 = structure0 {
                    bucketsDecoded0?.append(structure0)
                }
            }
        }
        buckets = bucketsDecoded0
    }
}

extension BucketInfo: CustomDebugStringConvertible {
    public var debugDescription: String {
        "BucketInfo(buckets: \(String(describing: buckets)))"}
}

/// <p>A container for the calculated facet values and counts.</p>
public struct BucketInfo: Equatable {
    /// <p>A list of the calculated facet values and counts.</p>
    public let buckets: [Bucket]?

    public init (
        buckets: [Bucket]? = nil
    )
    {
        self.buckets = buckets
    }
}

public enum ContentType {
    case applicationJson
    case applicationXml
    case sdkUnknown(String)
}

extension ContentType : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [ContentType] {
        return [
            .applicationJson,
            .applicationXml,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .applicationJson: return "application/json"
        case .applicationXml: return "application/xml"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = ContentType(rawValue: rawValue) ?? ContentType.sdkUnknown(rawValue)
    }
}

extension DocumentServiceException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DocumentServiceException(message: \(String(describing: message)), status: \(String(describing: status)))"}
}

extension DocumentServiceException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .data(let data) = httpResponse.body,
            let unwrappedData = data,
            let responseDecoder = decoder {
            let output: DocumentServiceExceptionBody = try responseDecoder.decode(responseBody: unwrappedData)
            self.message = output.message
            self.status = output.status
        } else {
            self.message = nil
            self.status = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>Information about any problems encountered while processing an upload request.</p>
public struct DocumentServiceException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    /// <p>The description of the errors returned by the document service.</p>
    public var message: String?
    /// <p>The return status of a document upload request, <code>error</code> or <code>success</code>.</p>
    public var status: String?

    public init (
        message: String? = nil,
        status: String? = nil
    )
    {
        self.message = message
        self.status = status
    }
}

struct DocumentServiceExceptionBody: Equatable {
    public let status: String?
    public let message: String?
}

extension DocumentServiceExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
        case status
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension DocumentServiceWarning: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let message = message {
            try encodeContainer.encode(message, forKey: .message)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension DocumentServiceWarning: CustomDebugStringConvertible {
    public var debugDescription: String {
        "DocumentServiceWarning(message: \(String(describing: message)))"}
}

/// <p>A warning returned by the document service when an issue is discovered while processing an upload request.</p>
public struct DocumentServiceWarning: Equatable {
    /// <p>The description for a warning returned by the document service.</p>
    public let message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

extension FieldStats: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case count
        case max
        case mean
        case min
        case missing
        case stddev
        case sum
        case sumOfSquares
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if count != 0 {
            try encodeContainer.encode(count, forKey: .count)
        }
        if let max = max {
            try encodeContainer.encode(max, forKey: .max)
        }
        if let mean = mean {
            try encodeContainer.encode(mean, forKey: .mean)
        }
        if let min = min {
            try encodeContainer.encode(min, forKey: .min)
        }
        if missing != 0 {
            try encodeContainer.encode(missing, forKey: .missing)
        }
        if stddev != 0.0 {
            try encodeContainer.encode(stddev, forKey: .stddev)
        }
        if sum != 0.0 {
            try encodeContainer.encode(sum, forKey: .sum)
        }
        if sumOfSquares != 0.0 {
            try encodeContainer.encode(sumOfSquares, forKey: .sumOfSquares)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let minDecoded = try containerValues.decodeIfPresent(String.self, forKey: .min)
        min = minDecoded
        let maxDecoded = try containerValues.decodeIfPresent(String.self, forKey: .max)
        max = maxDecoded
        let countDecoded = try containerValues.decode(Int.self, forKey: .count)
        count = countDecoded
        let missingDecoded = try containerValues.decode(Int.self, forKey: .missing)
        missing = missingDecoded
        let sumDecoded = try containerValues.decode(Double.self, forKey: .sum)
        sum = sumDecoded
        let sumOfSquaresDecoded = try containerValues.decode(Double.self, forKey: .sumOfSquares)
        sumOfSquares = sumOfSquaresDecoded
        let meanDecoded = try containerValues.decodeIfPresent(String.self, forKey: .mean)
        mean = meanDecoded
        let stddevDecoded = try containerValues.decode(Double.self, forKey: .stddev)
        stddev = stddevDecoded
    }
}

extension FieldStats: CustomDebugStringConvertible {
    public var debugDescription: String {
        "FieldStats(count: \(String(describing: count)), max: \(String(describing: max)), mean: \(String(describing: mean)), min: \(String(describing: min)), missing: \(String(describing: missing)), stddev: \(String(describing: stddev)), sum: \(String(describing: sum)), sumOfSquares: \(String(describing: sumOfSquares)))"}
}

/// <p>The statistics for a field calculated in the request.</p>
public struct FieldStats: Equatable {
    /// <p>The number of documents that contain a value in the specified field in the result set.</p>
    public let count: Int
    /// <p>The maximum value found in the specified field in the result set.</p>
    ///        <p>If the field is numeric (<code>int</code>, <code>int-array</code>, <code>double</code>, or <code>double-array</code>), <code>max</code> is the string representation of a double-precision 64-bit floating point value. If the field is <code>date</code> or <code>date-array</code>, <code>max</code> is the string representation of a date with the format specified in <a href="http://tools.ietf.org/html/rfc3339">IETF RFC3339</a>: yyyy-mm-ddTHH:mm:ss.SSSZ.</p>
    public let max: String?
    /// <p>The average of the values found in the specified field in the result set.</p>
    ///        <p>If the field is numeric (<code>int</code>, <code>int-array</code>, <code>double</code>, or <code>double-array</code>), <code>mean</code> is the string representation of a double-precision 64-bit floating point value. If the field is <code>date</code> or <code>date-array</code>, <code>mean</code> is the string representation of a date with the format specified in <a href="http://tools.ietf.org/html/rfc3339">IETF RFC3339</a>: yyyy-mm-ddTHH:mm:ss.SSSZ.</p>
    public let mean: String?
    /// <p>The minimum value found in the specified field in the result set.</p>
    ///        <p>If the field is numeric (<code>int</code>, <code>int-array</code>, <code>double</code>, or <code>double-array</code>), <code>min</code> is the string representation of a double-precision 64-bit floating point value. If the field is <code>date</code> or <code>date-array</code>, <code>min</code> is the string representation of a date with the format specified in <a href="http://tools.ietf.org/html/rfc3339">IETF RFC3339</a>: yyyy-mm-ddTHH:mm:ss.SSSZ.</p>
    public let min: String?
    /// <p>The number of documents that do not contain a value in the specified field in the result set.</p>
    public let missing: Int
    /// <p>The standard deviation of the values in the specified field in the result set.</p>
    public let stddev: Double
    /// <p>The sum of the field values across the documents in the result set. <code>null</code> for date fields.</p>
    public let sum: Double
    /// <p>The sum of all field values in the result set squared.</p>
    public let sumOfSquares: Double

    public init (
        count: Int = 0,
        max: String? = nil,
        mean: String? = nil,
        min: String? = nil,
        missing: Int = 0,
        stddev: Double = 0.0,
        sum: Double = 0.0,
        sumOfSquares: Double = 0.0
    )
    {
        self.count = count
        self.max = max
        self.mean = mean
        self.min = min
        self.missing = missing
        self.stddev = stddev
        self.sum = sum
        self.sumOfSquares = sumOfSquares
    }
}

extension Hit: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case exprs
        case fields
        case highlights
        case id
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let exprs = exprs {
            var exprsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .exprs)
            for (dictKey0, exprs0) in exprs {
                try exprsContainer.encode(exprs0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let fields = fields {
            var fieldsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .fields)
            for (dictKey0, fields0) in fields {
                try fieldsContainer.encode(fields0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let highlights = highlights {
            var highlightsContainer = encodeContainer.nestedContainer(keyedBy: Key.self, forKey: .highlights)
            for (dictKey0, highlights0) in highlights {
                try highlightsContainer.encode(highlights0, forKey: Key(stringValue: dictKey0))
            }
        }
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
        let fieldsContainer = try containerValues.decodeIfPresent([String: [String?]?].self, forKey: .fields)
        var fieldsDecoded0: [String:[String]]? = nil
        if let fieldsContainer = fieldsContainer {
            fieldsDecoded0 = [String:[String]]()
            for (key0, fieldvalue0) in fieldsContainer {
                var fieldvalue0Decoded0: [String]? = nil
                if let fieldvalue0 = fieldvalue0 {
                    fieldvalue0Decoded0 = [String]()
                    for string1 in fieldvalue0 {
                        if let string1 = string1 {
                            fieldvalue0Decoded0?.append(string1)
                        }
                    }
                }
                fieldsDecoded0?[key0] = fieldvalue0Decoded0
            }
        }
        fields = fieldsDecoded0
        let exprsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .exprs)
        var exprsDecoded0: [String:String]? = nil
        if let exprsContainer = exprsContainer {
            exprsDecoded0 = [String:String]()
            for (key0, string0) in exprsContainer {
                if let string0 = string0 {
                    exprsDecoded0?[key0] = string0
                }
            }
        }
        exprs = exprsDecoded0
        let highlightsContainer = try containerValues.decodeIfPresent([String: String?].self, forKey: .highlights)
        var highlightsDecoded0: [String:String]? = nil
        if let highlightsContainer = highlightsContainer {
            highlightsDecoded0 = [String:String]()
            for (key0, string0) in highlightsContainer {
                if let string0 = string0 {
                    highlightsDecoded0?[key0] = string0
                }
            }
        }
        highlights = highlightsDecoded0
    }
}

extension Hit: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Hit(exprs: \(String(describing: exprs)), fields: \(String(describing: fields)), highlights: \(String(describing: highlights)), id: \(String(describing: id)))"}
}

/// <p>Information about a document that matches the search request.</p>
public struct Hit: Equatable {
    /// <p>The expressions returned from a document that matches the search request.</p>
    public let exprs: [String:String]?
    /// <p>The fields returned from a document that matches the search request.</p>
    public let fields: [String:[String]]?
    /// <p>The highlights returned from a document that matches the search request.</p>
    public let highlights: [String:String]?
    /// <p>The document ID of a document that matches the search request.</p>
    public let id: String?

    public init (
        exprs: [String:String]? = nil,
        fields: [String:[String]]? = nil,
        highlights: [String:String]? = nil,
        id: String? = nil
    )
    {
        self.exprs = exprs
        self.fields = fields
        self.highlights = highlights
        self.id = id
    }
}

extension Hits: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case cursor
        case found
        case hit
        case start
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let cursor = cursor {
            try encodeContainer.encode(cursor, forKey: .cursor)
        }
        if found != 0 {
            try encodeContainer.encode(found, forKey: .found)
        }
        if let hit = hit {
            var hitContainer = encodeContainer.nestedUnkeyedContainer(forKey: .hit)
            for hitlist0 in hit {
                try hitContainer.encode(hitlist0)
            }
        }
        if start != 0 {
            try encodeContainer.encode(start, forKey: .start)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let foundDecoded = try containerValues.decode(Int.self, forKey: .found)
        found = foundDecoded
        let startDecoded = try containerValues.decode(Int.self, forKey: .start)
        start = startDecoded
        let cursorDecoded = try containerValues.decodeIfPresent(String.self, forKey: .cursor)
        cursor = cursorDecoded
        let hitContainer = try containerValues.decodeIfPresent([Hit?].self, forKey: .hit)
        var hitDecoded0:[Hit]? = nil
        if let hitContainer = hitContainer {
            hitDecoded0 = [Hit]()
            for structure0 in hitContainer {
                if let structure0 = structure0 {
                    hitDecoded0?.append(structure0)
                }
            }
        }
        hit = hitDecoded0
    }
}

extension Hits: CustomDebugStringConvertible {
    public var debugDescription: String {
        "Hits(cursor: \(String(describing: cursor)), found: \(String(describing: found)), hit: \(String(describing: hit)), start: \(String(describing: start)))"}
}

/// <p>The collection of documents that match the search request.</p>
public struct Hits: Equatable {
    /// <p>A cursor that can be used to retrieve the next set of matching documents when you want to page through a large result set.</p>
    public let cursor: String?
    /// <p>The total number of documents that match the search request.</p>
    public let found: Int
    /// <p>A document that matches the search request.</p>
    public let hit: [Hit]?
    /// <p>The index of the first matching document.</p>
    public let start: Int

    public init (
        cursor: String? = nil,
        found: Int = 0,
        hit: [Hit]? = nil,
        start: Int = 0
    )
    {
        self.cursor = cursor
        self.found = found
        self.hit = hit
        self.start = start
    }
}

public enum QueryParser {
    case dismax
    case lucene
    case simple
    case structured
    case sdkUnknown(String)
}

extension QueryParser : Equatable, RawRepresentable, Codable, CaseIterable, Hashable {
    public static var allCases: [QueryParser] {
        return [
            .dismax,
            .lucene,
            .simple,
            .structured,
            .sdkUnknown("")
        ]
    }
    public init?(rawValue: String) {
        let value = Self.allCases.first(where: { $0.rawValue == rawValue })
        self = value ?? Self.sdkUnknown(rawValue)
    }
    public var rawValue: String {
        switch self {
        case .dismax: return "dismax"
        case .lucene: return "lucene"
        case .simple: return "simple"
        case .structured: return "structured"
        case let .sdkUnknown(s): return s
        }
    }
    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        let rawValue = try container.decode(RawValue.self)
        self = QueryParser(rawValue: rawValue) ?? QueryParser.sdkUnknown(rawValue)
    }
}

extension SearchException: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SearchException(message: \(String(describing: message)))"}
}

extension SearchException: AWSHttpServiceError {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        if case .data(let data) = httpResponse.body,
            let unwrappedData = data,
            let responseDecoder = decoder {
            let output: SearchExceptionBody = try responseDecoder.decode(responseBody: unwrappedData)
            self.message = output.message
        } else {
            self.message = nil
        }
        self._headers = httpResponse.headers
        self._statusCode = httpResponse.statusCode
        self._requestID = requestID
        self._message = message
    }
}

/// <p>Information about any problems encountered while processing a search request.</p>
public struct SearchException: ClientRuntime.ServiceError, Equatable {
    public var _headers: ClientRuntime.Headers?
    public var _statusCode: HttpStatusCode?
    public var _message: String?
    public var _requestID: String?
    public var _retryable: Bool = false
    public var _isThrottling: Bool = false
    public var _type: ErrorType = .client
    /// <p>A description of the error returned by the search service.</p>
    public var message: String?

    public init (
        message: String? = nil
    )
    {
        self.message = message
    }
}

struct SearchExceptionBody: Equatable {
    public let message: String?
}

extension SearchExceptionBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case message
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let messageDecoded = try containerValues.decodeIfPresent(String.self, forKey: .message)
        message = messageDecoded
    }
}

extension SearchInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SearchInput(cursor: \(String(describing: cursor)), expr: \(String(describing: expr)), facet: \(String(describing: facet)), filterQuery: \(String(describing: filterQuery)), highlight: \(String(describing: highlight)), partial: \(String(describing: partial)), query: \(String(describing: query)), queryOptions: \(String(describing: queryOptions)), queryParser: \(String(describing: queryParser)), return: \(String(describing: `return`)), size: \(String(describing: size)), sort: \(String(describing: sort)), start: \(String(describing: start)), stats: \(String(describing: stats)))"}
}

extension SearchInput: Encodable, Reflection {

    public func encode(to encoder: Encoder) throws {
    }
}

public struct SearchInputHeadersMiddleware: Middleware {
    public let id: String = "SearchInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SearchInput>,
                  next: H) -> Swift.Result<OperationOutput<SearchOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SearchInput>
    public typealias MOutput = OperationOutput<SearchOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SearchOutputError>
}

public struct SearchInputQueryItemMiddleware: Middleware {
    public let id: String = "SearchInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SearchInput>,
                  next: H) -> Swift.Result<OperationOutput<SearchOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        input.builder.withQueryItem(URLQueryItem(name: "format", value: "sdk"))
        input.builder.withQueryItem(URLQueryItem(name: "pretty", value: "true"))
        if let cursor = input.operationInput.cursor {
            let cursorQueryItem = URLQueryItem(name: "cursor".urlPercentEncoding(), value: String(cursor).urlPercentEncoding())
            input.builder.withQueryItem(cursorQueryItem)
        }
        if let queryOptions = input.operationInput.queryOptions {
            let queryOptionsQueryItem = URLQueryItem(name: "q.options".urlPercentEncoding(), value: String(queryOptions).urlPercentEncoding())
            input.builder.withQueryItem(queryOptionsQueryItem)
        }
        if let query = input.operationInput.query {
            let queryQueryItem = URLQueryItem(name: "q".urlPercentEncoding(), value: String(query).urlPercentEncoding())
            input.builder.withQueryItem(queryQueryItem)
        }
        let startQueryItem = URLQueryItem(name: "start".urlPercentEncoding(), value: String(input.operationInput.start).urlPercentEncoding())
        input.builder.withQueryItem(startQueryItem)
        if let sort = input.operationInput.sort {
            let sortQueryItem = URLQueryItem(name: "sort".urlPercentEncoding(), value: String(sort).urlPercentEncoding())
            input.builder.withQueryItem(sortQueryItem)
        }
        if let queryParser = input.operationInput.queryParser {
            let queryParserQueryItem = URLQueryItem(name: "q.parser".urlPercentEncoding(), value: String(queryParser.rawValue).urlPercentEncoding())
            input.builder.withQueryItem(queryParserQueryItem)
        }
        if let highlight = input.operationInput.highlight {
            let highlightQueryItem = URLQueryItem(name: "highlight".urlPercentEncoding(), value: String(highlight).urlPercentEncoding())
            input.builder.withQueryItem(highlightQueryItem)
        }
        let sizeQueryItem = URLQueryItem(name: "size".urlPercentEncoding(), value: String(input.operationInput.size).urlPercentEncoding())
        input.builder.withQueryItem(sizeQueryItem)
        if let stats = input.operationInput.stats {
            let statsQueryItem = URLQueryItem(name: "stats".urlPercentEncoding(), value: String(stats).urlPercentEncoding())
            input.builder.withQueryItem(statsQueryItem)
        }
        if let expr = input.operationInput.expr {
            let exprQueryItem = URLQueryItem(name: "expr".urlPercentEncoding(), value: String(expr).urlPercentEncoding())
            input.builder.withQueryItem(exprQueryItem)
        }
        if let facet = input.operationInput.facet {
            let facetQueryItem = URLQueryItem(name: "facet".urlPercentEncoding(), value: String(facet).urlPercentEncoding())
            input.builder.withQueryItem(facetQueryItem)
        }
        let partialQueryItem = URLQueryItem(name: "partial".urlPercentEncoding(), value: String(input.operationInput.partial).urlPercentEncoding())
        input.builder.withQueryItem(partialQueryItem)
        if let `return` = input.operationInput.`return` {
            let returnQueryItem = URLQueryItem(name: "return".urlPercentEncoding(), value: String(`return`).urlPercentEncoding())
            input.builder.withQueryItem(returnQueryItem)
        }
        if let filterQuery = input.operationInput.filterQuery {
            let filterQueryQueryItem = URLQueryItem(name: "fq".urlPercentEncoding(), value: String(filterQuery).urlPercentEncoding())
            input.builder.withQueryItem(filterQueryQueryItem)
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SearchInput>
    public typealias MOutput = OperationOutput<SearchOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SearchOutputError>
}

/// <p>Container for the parameters to the <code>Search</code> request.</p>
public struct SearchInput: Equatable {
    /// <p>Specifies the field and expression values to include in the response. Multiple fields or expressions are specified as a comma-separated list. By default, a search response includes all
    ///          return enabled fields (<code>_all_fields</code>).
    ///          To  return only the document IDs for the matching documents,
    ///          specify <code>_no_fields</code>.
    ///          To retrieve the relevance score calculated for each document,
    ///          specify <code>_score</code>.  </p>
    public let `return`: String?
    /// <p>Retrieves a cursor value you can use to page through large result sets.
    ///          Use the <code>size</code> parameter to control the number of hits to include in each response. You can specify either the <code>cursor</code> or
    ///          <code>start</code> parameter in a request; they are mutually exclusive. To get the first cursor, set the cursor value to <code>initial</code>. In subsequent requests, specify the cursor value returned in the hits section of the response. </p>
    ///       <p>For more
    ///          information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/paginating-results.html">Paginating Results</a> in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let cursor: String?
    /// <p>Defines one or more numeric expressions that can be used to sort results or specify search or filter
    ///          criteria. You can also specify expressions as return fields. </p>
    ///       <p>You specify the expressions in JSON using the form <code>{"EXPRESSIONNAME":"EXPRESSION"}</code>. You can define and use multiple expressions in a search request. For example:</p>
    ///       <p><code>
    ///          {"expression1":"_score*rating", "expression2":"(1/rank)*year"}
    ///       </code> </p>
    ///       <p>For information about the variables, operators, and functions you can use in expressions, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/configuring-expressions.html#writing-expressions">Writing Expressions</a>
    ///          in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let expr: String?
    /// <p>Specifies one or more fields for which to get facet information, and options that control how the facet information is returned. Each specified field must be facet-enabled in the domain configuration. The fields and options are specified in JSON using the form <code>{"FIELD":{"OPTION":VALUE,"OPTION:"STRING"},"FIELD":{"OPTION":VALUE,"OPTION":"STRING"}}</code>.</p>
    ///          <p>You can specify the following faceting options:</p>
    ///       <ul>
    ///          <li>
    ///             <p><code>buckets</code> specifies an array of the facet values or
    ///                ranges to count. Ranges are specified using the same syntax that you use to search for a range of values.
    ///                For more information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/searching-ranges.html">
    ///                   Searching for a Range of Values</a> in the <i>Amazon CloudSearch Developer Guide</i>. Buckets are returned in the order they
    ///                are specified in the request. The <code>sort</code> and <code>size</code> options are
    ///                not valid if you specify <code>buckets</code>.</p>
    ///          </li>
    ///          <li>
    ///             <p><code>size</code> specifies the maximum number of facets to include in the results. By
    ///                default, Amazon CloudSearch returns counts for the top 10. The
    ///                <code>size</code> parameter is only valid when you specify
    ///                the <code>sort</code> option; it cannot be used in conjunction
    ///                with <code>buckets</code>.</p>
    ///          </li>
    ///          <li>
    ///             <p><code>sort</code> specifies how you want to sort the facets in
    ///                the results: <code>bucket</code> or <code>count</code>. Specify
    ///                <code>bucket</code> to sort alphabetically or numerically by
    ///                facet value (in ascending order). Specify <code>count</code> to
    ///                sort by the facet counts computed for each facet value (in
    ///                descending order). To retrieve facet counts for particular
    ///                values or ranges of values, use the <code>buckets</code> option
    ///                instead of <code>sort</code>. </p>
    ///          </li>
    ///       </ul>
    ///       <p>If no facet options are specified, facet counts are computed for all field values, the facets are sorted by facet count, and the top 10 facets are returned in the results.</p>
    ///
    ///       <p>To count particular buckets of values, use the <code>buckets</code> option. For example, the following request uses the <code>buckets</code> option to calculate and return facet counts by decade.</p>
    ///       <p><code>
    ///          {"year":{"buckets":["[1970,1979]","[1980,1989]","[1990,1999]","[2000,2009]","[2010,}"]}}
    ///       </code></p>
    ///
    ///          <p>To sort facets by facet count, use the <code>count</code> option. For example, the following request sets the <code>sort</code> option to <code>count</code> to sort the facet values by facet count, with the facet
    ///             values that have the most matching documents listed first. Setting the <code>size</code> option to 3 returns only the top three facet values.</p>
    ///          <p><code>
    ///             {"year":{"sort":"count","size":3}}
    ///          </code></p>
    ///
    ///          <p>To sort the facets by value, use the <code>bucket</code> option. For example, the following  request sets the <code>sort</code> option to <code>bucket</code> to sort the facet values numerically by year, with earliest year listed first. </p>
    ///          <p><code>
    ///             {"year":{"sort":"bucket"}}
    ///          </code></p>
    ///       <p>For more
    ///          information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/faceting.html">Getting and Using Facet Information</a>
    ///          in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let facet: String?
    /// <p>Specifies a structured query that filters the results of a search without affecting how the results are scored and sorted. You use <code>filterQuery</code> in conjunction with the <code>query</code> parameter to filter the documents that match the constraints specified in the <code>query</code> parameter. Specifying a filter controls only which matching documents are included in the results, it has no effect on how they are scored and sorted. The <code>filterQuery</code> parameter supports the full structured query syntax. </p>
    ///       <p>For more information about using filters, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/filtering-results.html">Filtering Matching Documents</a>
    ///          in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let filterQuery: String?
    /// <p>Retrieves highlights for matches in the specified <code>text</code> or
    ///          <code>text-array</code> fields. Each specified field must be highlight enabled in the domain configuration. The fields and options are specified in JSON using the form <code>{"FIELD":{"OPTION":VALUE,"OPTION:"STRING"},"FIELD":{"OPTION":VALUE,"OPTION":"STRING"}}</code>.</p>
    ///       <p>You can specify the following highlight options:</p>
    ///          <ul>
    ///             <li>
    ///                <code>format</code>: specifies the format of the data in the
    ///                   text field: <code>text</code> or <code>html</code>. When data is
    ///                   returned as HTML, all non-alphanumeric characters are encoded.
    ///                   The default is <code>html</code>.
    ///             </li>
    ///             <li>
    ///                <code>max_phrases</code>: specifies the maximum number of
    ///                   occurrences of the search term(s) you want to highlight. By
    ///                   default, the first occurrence is highlighted.
    ///             </li>
    ///             <li>
    ///                <code>pre_tag</code>: specifies the string to prepend to an
    ///                   occurrence of a search term. The default for HTML highlights is
    ///                <code>&lt;em&gt;</code>. The default for text highlights is
    ///                   <code>*</code>.
    ///             </li>
    ///             <li>
    ///                <code>post_tag</code>: specifies the string to append to an
    ///                   occurrence of a search term. The default for HTML highlights is
    ///                   <code>&lt;/em&gt;</code>. The default for text highlights is
    ///                   <code>*</code>.
    ///             </li>
    ///          </ul>
    ///          <p>If no highlight options are specified for a field, the returned field text is treated as HTML and the first match is
    ///             highlighted with emphasis tags:  <code>&lt;em>search-term&lt;/em&gt;</code>.</p>
    ///             <p>For example, the following request retrieves highlights for the <code>actors</code> and <code>title</code> fields.</p>
    ///                  <p> <code>{
    ///     "actors": {},
    ///     "title": {"format": "text","max_phrases": 2,"pre_tag": "<b>","post_tag": "</b>"}
    ///                     }</code></p>
    public let highlight: String?
    /// <p>Enables partial results to be returned if one or more index partitions are unavailable. When your search index is partitioned across multiple search instances, by default Amazon CloudSearch only returns results if every partition can be queried. This means that the failure of a single search instance can result in 5xx (internal server) errors. When you enable partial results, Amazon CloudSearch returns whatever results are available and includes the percentage of documents searched in the search results (percent-searched). This enables you to more gracefully degrade your users' search experience. For example, rather than displaying no results, you could display the partial results and a message indicating that the results might be incomplete due to a temporary system outage.</p>
    public let partial: Bool
    /// <p>Specifies the search criteria for the request. How you specify the search
    ///          criteria depends on the query parser used for the request and the parser options
    ///          specified in the <code>queryOptions</code> parameter. By default,
    ///          the <code>simple</code> query parser is used to process requests. To use
    ///          the <code>structured</code>, <code>lucene</code>, or <code>dismax</code> query parser,
    ///          you must also specify the <code>queryParser</code> parameter. </p>
    ///             <p>For more information about specifying search criteria, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/searching.html">Searching Your Data</a> in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let query: String?
    /// <p>Configures options for the query parser specified in the <code>queryParser</code> parameter. You specify the options in JSON using the following form <code>{"OPTION1":"VALUE1","OPTION2":VALUE2"..."OPTIONN":"VALUEN"}.</code></p>
    ///
    ///       <p>The options you can configure vary according to which parser you use:</p>
    ///       <ul>
    ///          <li><code>defaultOperator</code>: The default operator used to combine individual terms in the search string. For example: <code>defaultOperator: 'or'</code>. For the <code>dismax</code> parser, you specify a percentage that represents the percentage of terms in the search string (rounded down) that must match, rather than a default operator. A value of <code>0%</code> is the equivalent to OR, and a value of <code>100%</code> is equivalent to AND. The percentage must be specified as a value in the range 0-100 followed by the percent (%) symbol. For example, <code>defaultOperator: 50%</code>. Valid values: <code>and</code>, <code>or</code>, a percentage in the range 0%-100% (<code>dismax</code>). Default: <code>and</code> (<code>simple</code>, <code>structured</code>, <code>lucene</code>) or <code>100</code> (<code>dismax</code>). Valid for: <code>simple</code>, <code>structured</code>, <code>lucene</code>, and <code>dismax</code>.</li>
    ///          <li><code>fields</code>: An array of the fields to search when no fields are specified in a search. If no fields are specified in a search and this option is not specified, all text and text-array fields are searched. You can specify a weight for each field to control the relative importance of each field when Amazon CloudSearch calculates relevance scores. To specify a field weight, append a caret (<code>^</code>) symbol and the weight to the field name. For example, to boost the importance of the <code>title</code> field over the <code>description</code> field you could specify: <code>"fields":["title^5","description"]</code>.  Valid values: The name of any configured field and an optional numeric value greater than zero. Default: All <code>text</code> and <code>text-array</code> fields. Valid for: <code>simple</code>, <code>structured</code>, <code>lucene</code>, and <code>dismax</code>.</li>
    ///          <li><code>operators</code>: An array of the operators or special characters you want to
    ///             disable for the simple query parser. If you disable the
    ///             <code>and</code>, <code>or</code>, or <code>not</code>
    ///             operators, the corresponding operators (<code>+</code>,
    ///             <code>|</code>, <code>-</code>) have no special meaning and
    ///             are dropped from the search string. Similarly, disabling
    ///             <code>prefix</code> disables the wildcard operator
    ///             (<code>*</code>) and disabling <code>phrase</code> disables
    ///             the ability to search for phrases by enclosing phrases in double
    ///             quotes. Disabling precedence disables the ability to control
    ///             order of precedence using parentheses. Disabling <code>near</code> disables
    ///             the ability to use the ~ operator to perform a sloppy phrase search.
    ///             Disabling the <code>fuzzy</code> operator disables the ability to use the ~ operator to perform a fuzzy search.
    ///             <code>escape</code> disables the ability to use a backslash
    ///             (<code>\</code>) to escape special characters within the
    ///             search string. Disabling whitespace is an advanced option that
    ///             prevents the parser from tokenizing on whitespace, which can be
    ///             useful for Vietnamese. (It prevents Vietnamese words from being
    ///             split incorrectly.) For example, you could disable all operators
    ///             other than the phrase operator to support just simple term and
    ///             phrase queries: <code>"operators":["and","not","or",
    ///                "prefix"]</code>. Valid values: <code>and</code>, <code>escape</code>,
    ///             <code>fuzzy</code>, <code>near</code>, <code>not</code>, <code>or</code>,
    ///             <code>phrase</code>, <code>precedence</code>, <code>prefix</code>,
    ///             <code>whitespace</code>. Default: All
    ///             operators and special characters are enabled. Valid for:
    ///             <code>simple</code>.</li>
    ///          <li><code>phraseFields</code>: An array of the <code>text</code> or <code>text-array</code> fields you want to use for
    ///             phrase searches. When the terms in the search string appear in
    ///             close proximity within a field, the field scores higher. You can
    ///             specify a weight for each field to boost that score. The
    ///             <code>phraseSlop</code> option controls how much the matches
    ///             can deviate from the search string and still be boosted. To
    ///             specify a field weight, append a caret (<code>^</code>) symbol
    ///             and the weight to the field name. For example, to boost phrase
    ///             matches in the <code>title</code> field over the
    ///             <code>abstract</code> field, you could specify:
    ///             <code>"phraseFields":["title^3",
    ///                "plot"]</code>
    ///             Valid values: The name of any <code>text</code> or
    ///             <code>text-array</code> field and an optional numeric value greater than zero. Default: No fields. If you don't specify any fields with <code>phraseFields</code>, proximity scoring is disabled even if <code>phraseSlop</code> is specified. Valid
    ///             for: <code>dismax</code>.</li>
    ///          <li><code>phraseSlop</code>: An integer value that specifies how much matches can deviate from the search phrase and still be boosted according to the weights specified in the <code>phraseFields</code> option; for example, <code>phraseSlop: 2</code>. You must also specify <code>phraseFields</code> to enable proximity scoring. Valid values: positive integers. Default: 0. Valid for: <code>dismax</code>.</li>
    ///          <li><code>explicitPhraseSlop</code>: An integer value that specifies how much a match can deviate from the search phrase when the phrase is enclosed in double quotes in the search string. (Phrases that exceed this proximity distance are not considered a match.) For example, to specify a slop of three for dismax phrase queries, you would specify <code>"explicitPhraseSlop":3</code>. Valid values: positive integers. Default: 0. Valid for: <code>dismax</code>.</li>
    ///          <li><code>tieBreaker</code>: When a term in the search string is found in a document's field, a score is calculated for that field based on how common the word is in that field compared to other documents. If the term occurs in multiple fields within a document, by default only the highest scoring field contributes to the document's overall score. You can specify a <code>tieBreaker</code> value to enable the matches in lower-scoring fields to contribute to the document's score. That way, if two documents have the same max field score for a particular term, the score for the document that has matches in more fields will be higher. The formula for calculating the score with a tieBreaker is <code>(max field score) + (tieBreaker) * (sum of the scores for the rest of the matching fields)</code>.
    ///
    ///             Set <code>tieBreaker</code> to 0 to disregard all but the highest scoring field (pure max): <code>"tieBreaker":0</code>. Set to 1 to sum the scores from all fields (pure sum): <code>"tieBreaker":1</code>. Valid values: 0.0 to 1.0. Default: 0.0. Valid for: <code>dismax</code>.
    ///          </li>
    ///       </ul>
    public let queryOptions: String?
    /// <p>Specifies which
    ///          query parser to use to process the request. If <code>queryParser</code> is not specified, Amazon CloudSearch
    ///          uses the <code>simple</code> query parser. </p>
    ///       <p>Amazon CloudSearch supports four query parsers:</p>
    ///       <ul>
    ///          <li>
    ///             <code>simple</code>: perform simple searches of <code>text</code> and
    ///                <code>text-array</code> fields. By default, the
    ///                <code>simple</code> query parser searches all
    ///                <code>text</code> and <code>text-array</code> fields. You
    ///                can specify which fields to search by with the
    ///                <code>queryOptions</code> parameter. If you prefix a search
    ///                term with a plus sign (+) documents must contain the term to be considered a match.
    ///                (This is the default, unless you configure the default operator with the <code>queryOptions</code> parameter.)
    ///                You can use the <code>-</code> (NOT), <code>|</code>
    ///                (OR), and <code>*</code> (wildcard) operators to exclude
    ///                particular terms, find results that match any of the specified
    ///                terms, or search for a prefix. To search for a phrase rather
    ///                than individual terms, enclose the phrase in double quotes. For
    ///                more information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/searching-text.html">Searching for Text</a> in the <i>Amazon CloudSearch Developer Guide</i>.
    ///          </li>
    ///          <li>
    ///             <code>structured</code>: perform advanced searches by combining
    ///                multiple expressions to define the search criteria. You can also search
    ///                within particular fields, search for values and ranges of values, and use
    ///                advanced options such as term boosting, <code>matchall</code>, and <code>near</code>.
    ///                For more information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/searching-compound-queries.html">Constructing Compound Queries</a> in the <i>Amazon CloudSearch Developer Guide</i>.
    ///          </li>
    ///          <li>
    ///             <code>lucene</code>: search using the Apache Lucene query parser syntax.
    ///                For more information, see <a href="http://lucene.apache.org/core/4_6_0/queryparser/org/apache/lucene/queryparser/classic/package-summary.html#package_description">Apache Lucene Query Parser Syntax</a>.
    ///          </li>
    ///          <li>
    ///             <code>dismax</code>: search using the simplified subset of the Apache Lucene query parser syntax
    ///                defined by the DisMax query parser.
    ///                For more information, see <a href="http://wiki.apache.org/solr/DisMaxQParserPlugin#Query_Syntax">DisMax Query Parser Syntax</a>.
    ///          </li>
    ///
    ///       </ul>
    public let queryParser: QueryParser?
    /// <p>Specifies the maximum number of search hits to include in the response. </p>
    public let size: Int
    /// <p>Specifies the fields or custom expressions to use to sort the search
    ///          results. Multiple fields or expressions are specified as a comma-separated list.
    ///          You must specify the sort direction (<code>asc</code> or
    ///          <code>desc</code>) for each field; for example, <code>year
    ///             desc,title asc</code>. To use a field to sort results, the field must be sort-enabled in
    ///          the domain configuration. Array type fields cannot be used for sorting.
    ///          If no <code>sort</code> parameter is specified, results are sorted by
    ///          their default relevance scores in descending order: <code>_score
    ///             desc</code>. You can also sort by document ID
    ///          (<code>_id asc</code>) and version (<code>_version desc</code>).</p>
    ///       <p>For more information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/sorting-results.html">Sorting Results</a> in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let sort: String?
    /// <p>Specifies the offset of the first search hit you want to return. Note that the result set is zero-based; the first result is at index 0. You can specify either the <code>start</code> or <code>cursor</code> parameter in a request, they are mutually exclusive.  </p>
    ///       <p>For more information, see <a href="http://docs.aws.amazon.com/cloudsearch/latest/developerguide/paginating-results.html">Paginating Results</a> in the <i>Amazon CloudSearch Developer Guide</i>.</p>
    public let start: Int
    /// <p>Specifies one or more fields for which to get statistics information. Each specified field must be facet-enabled in the domain configuration. The fields are specified in JSON using the form:</p>
    ///       <code>{"FIELD-A":{},"FIELD-B":{}}</code>
    ///       <p>There are currently no options supported for statistics.</p>
    public let stats: String?

    public init (
        `return`: String? = nil,
        cursor: String? = nil,
        expr: String? = nil,
        facet: String? = nil,
        filterQuery: String? = nil,
        highlight: String? = nil,
        partial: Bool = false,
        query: String? = nil,
        queryOptions: String? = nil,
        queryParser: QueryParser? = nil,
        size: Int = 0,
        sort: String? = nil,
        start: Int = 0,
        stats: String? = nil
    )
    {
        self.`return` = `return`
        self.cursor = cursor
        self.expr = expr
        self.facet = facet
        self.filterQuery = filterQuery
        self.highlight = highlight
        self.partial = partial
        self.query = query
        self.queryOptions = queryOptions
        self.queryParser = queryParser
        self.size = size
        self.sort = sort
        self.start = start
        self.stats = stats
    }
}

struct SearchInputBody: Equatable {
}

extension SearchInputBody: Decodable {

    public init (from decoder: Decoder) throws {
    }
}

extension SearchOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension SearchOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "SearchException" : self = .searchException(try SearchException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum SearchOutputError: Swift.Error, Equatable {
    case searchException(SearchException)
    case unknown(UnknownAWSHttpServiceError)
}

extension SearchOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SearchOutputResponse(facets: \(String(describing: facets)), hits: \(String(describing: hits)), stats: \(String(describing: stats)), status: \(String(describing: status)))"}
}

extension SearchOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .data(let data) = httpResponse.body,
            let unwrappedData = data,
            let responseDecoder = decoder {
            let output: SearchOutputResponseBody = try responseDecoder.decode(responseBody: unwrappedData)
            self.facets = output.facets
            self.hits = output.hits
            self.stats = output.stats
            self.status = output.status
        } else {
            self.facets = nil
            self.hits = nil
            self.stats = nil
            self.status = nil
        }
    }
}

/// <p>The result of a <code>Search</code> request. Contains the documents that match the specified search criteria and any requested fields, highlights, and facet information.</p>
public struct SearchOutputResponse: Equatable {
    /// <p>The requested facet information.</p>
    public let facets: [String:BucketInfo]?
    /// <p>The documents that match the search criteria.</p>
    public let hits: Hits?
    /// <p>The requested field statistics information.</p>
    public let stats: [String:FieldStats]?
    /// <p>The status information returned for the search request.</p>
    public let status: SearchStatus?

    public init (
        facets: [String:BucketInfo]? = nil,
        hits: Hits? = nil,
        stats: [String:FieldStats]? = nil,
        status: SearchStatus? = nil
    )
    {
        self.facets = facets
        self.hits = hits
        self.stats = stats
        self.status = status
    }
}

struct SearchOutputResponseBody: Equatable {
    public let status: SearchStatus?
    public let hits: Hits?
    public let facets: [String:BucketInfo]?
    public let stats: [String:FieldStats]?
}

extension SearchOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case facets
        case hits
        case stats
        case status
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let statusDecoded = try containerValues.decodeIfPresent(SearchStatus.self, forKey: .status)
        status = statusDecoded
        let hitsDecoded = try containerValues.decodeIfPresent(Hits.self, forKey: .hits)
        hits = hitsDecoded
        let facetsContainer = try containerValues.decodeIfPresent([String: BucketInfo?].self, forKey: .facets)
        var facetsDecoded0: [String:BucketInfo]? = nil
        if let facetsContainer = facetsContainer {
            facetsDecoded0 = [String:BucketInfo]()
            for (key0, bucketinfo0) in facetsContainer {
                if let bucketinfo0 = bucketinfo0 {
                    facetsDecoded0?[key0] = bucketinfo0
                }
            }
        }
        facets = facetsDecoded0
        let statsContainer = try containerValues.decodeIfPresent([String: FieldStats?].self, forKey: .stats)
        var statsDecoded0: [String:FieldStats]? = nil
        if let statsContainer = statsContainer {
            statsDecoded0 = [String:FieldStats]()
            for (key0, fieldstats0) in statsContainer {
                if let fieldstats0 = fieldstats0 {
                    statsDecoded0?[key0] = fieldstats0
                }
            }
        }
        stats = statsDecoded0
    }
}

extension SearchStatus: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case rid
        case timems
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let rid = rid {
            try encodeContainer.encode(rid, forKey: .rid)
        }
        if timems != 0 {
            try encodeContainer.encode(timems, forKey: .timems)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let timemsDecoded = try containerValues.decode(Int.self, forKey: .timems)
        timems = timemsDecoded
        let ridDecoded = try containerValues.decodeIfPresent(String.self, forKey: .rid)
        rid = ridDecoded
    }
}

extension SearchStatus: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SearchStatus(rid: \(String(describing: rid)), timems: \(String(describing: timems)))"}
}

/// <p>Contains the resource id (<code>rid</code>) and the time it took to process the request (<code>timems</code>).</p>
public struct SearchStatus: Equatable {
    /// <p>The encrypted resource ID for the request.</p>
    public let rid: String?
    /// <p>How long it took to process the request, in milliseconds.</p>
    public let timems: Int

    public init (
        rid: String? = nil,
        timems: Int = 0
    )
    {
        self.rid = rid
        self.timems = timems
    }
}

extension SuggestInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SuggestInput(query: \(String(describing: query)), size: \(String(describing: size)), suggester: \(String(describing: suggester)))"}
}

extension SuggestInput: Encodable, Reflection {

    public func encode(to encoder: Encoder) throws {
    }
}

public struct SuggestInputHeadersMiddleware: Middleware {
    public let id: String = "SuggestInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SuggestInput>,
                  next: H) -> Swift.Result<OperationOutput<SuggestOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SuggestInput>
    public typealias MOutput = OperationOutput<SuggestOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SuggestOutputError>
}

public struct SuggestInputQueryItemMiddleware: Middleware {
    public let id: String = "SuggestInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<SuggestInput>,
                  next: H) -> Swift.Result<OperationOutput<SuggestOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        input.builder.withQueryItem(URLQueryItem(name: "format", value: "sdk"))
        input.builder.withQueryItem(URLQueryItem(name: "pretty", value: "true"))
        if let suggester = input.operationInput.suggester {
            let suggesterQueryItem = URLQueryItem(name: "suggester".urlPercentEncoding(), value: String(suggester).urlPercentEncoding())
            input.builder.withQueryItem(suggesterQueryItem)
        }
        let sizeQueryItem = URLQueryItem(name: "size".urlPercentEncoding(), value: String(input.operationInput.size).urlPercentEncoding())
        input.builder.withQueryItem(sizeQueryItem)
        if let query = input.operationInput.query {
            let queryQueryItem = URLQueryItem(name: "q".urlPercentEncoding(), value: String(query).urlPercentEncoding())
            input.builder.withQueryItem(queryQueryItem)
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<SuggestInput>
    public typealias MOutput = OperationOutput<SuggestOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<SuggestOutputError>
}

/// <p>Container for the parameters to the <code>Suggest</code> request.</p>
public struct SuggestInput: Equatable {
    /// <p>Specifies the string for which you want to get suggestions.</p>
    public let query: String?
    /// <p>Specifies the maximum number of suggestions to return. </p>
    public let size: Int
    /// <p>Specifies the name of the suggester to use to find suggested matches.</p>
    public let suggester: String?

    public init (
        query: String? = nil,
        size: Int = 0,
        suggester: String? = nil
    )
    {
        self.query = query
        self.size = size
        self.suggester = suggester
    }
}

struct SuggestInputBody: Equatable {
}

extension SuggestInputBody: Decodable {

    public init (from decoder: Decoder) throws {
    }
}

extension SuggestModel: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case found
        case query
        case suggestions
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if found != 0 {
            try encodeContainer.encode(found, forKey: .found)
        }
        if let query = query {
            try encodeContainer.encode(query, forKey: .query)
        }
        if let suggestions = suggestions {
            var suggestionsContainer = encodeContainer.nestedUnkeyedContainer(forKey: .suggestions)
            for suggestions0 in suggestions {
                try suggestionsContainer.encode(suggestions0)
            }
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let queryDecoded = try containerValues.decodeIfPresent(String.self, forKey: .query)
        query = queryDecoded
        let foundDecoded = try containerValues.decode(Int.self, forKey: .found)
        found = foundDecoded
        let suggestionsContainer = try containerValues.decodeIfPresent([SuggestionMatch?].self, forKey: .suggestions)
        var suggestionsDecoded0:[SuggestionMatch]? = nil
        if let suggestionsContainer = suggestionsContainer {
            suggestionsDecoded0 = [SuggestionMatch]()
            for structure0 in suggestionsContainer {
                if let structure0 = structure0 {
                    suggestionsDecoded0?.append(structure0)
                }
            }
        }
        suggestions = suggestionsDecoded0
    }
}

extension SuggestModel: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SuggestModel(found: \(String(describing: found)), query: \(String(describing: query)), suggestions: \(String(describing: suggestions)))"}
}

/// <p>Container for the suggestion information returned in a <code>SuggestResponse</code>.</p>
public struct SuggestModel: Equatable {
    /// <p>The number of documents that were found to match the query string.</p>
    public let found: Int
    /// <p>The query string specified in the suggest request.</p>
    public let query: String?
    /// <p>The documents that match the query string.</p>
    public let suggestions: [SuggestionMatch]?

    public init (
        found: Int = 0,
        query: String? = nil,
        suggestions: [SuggestionMatch]? = nil
    )
    {
        self.found = found
        self.query = query
        self.suggestions = suggestions
    }
}

extension SuggestOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension SuggestOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "SearchException" : self = .searchException(try SearchException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum SuggestOutputError: Swift.Error, Equatable {
    case searchException(SearchException)
    case unknown(UnknownAWSHttpServiceError)
}

extension SuggestOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SuggestOutputResponse(status: \(String(describing: status)), suggest: \(String(describing: suggest)))"}
}

extension SuggestOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .data(let data) = httpResponse.body,
            let unwrappedData = data,
            let responseDecoder = decoder {
            let output: SuggestOutputResponseBody = try responseDecoder.decode(responseBody: unwrappedData)
            self.status = output.status
            self.suggest = output.suggest
        } else {
            self.status = nil
            self.suggest = nil
        }
    }
}

/// <p>Contains the response to a <code>Suggest</code> request.</p>
public struct SuggestOutputResponse: Equatable {
    /// <p>The status of a <code>SuggestRequest</code>. Contains the resource ID (<code>rid</code>) and how long it took to process the request (<code>timems</code>).</p>
    public let status: SuggestStatus?
    /// <p>Container for the matching search suggestion information.</p>
    public let suggest: SuggestModel?

    public init (
        status: SuggestStatus? = nil,
        suggest: SuggestModel? = nil
    )
    {
        self.status = status
        self.suggest = suggest
    }
}

struct SuggestOutputResponseBody: Equatable {
    public let status: SuggestStatus?
    public let suggest: SuggestModel?
}

extension SuggestOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case status
        case suggest
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let statusDecoded = try containerValues.decodeIfPresent(SuggestStatus.self, forKey: .status)
        status = statusDecoded
        let suggestDecoded = try containerValues.decodeIfPresent(SuggestModel.self, forKey: .suggest)
        suggest = suggestDecoded
    }
}

extension SuggestStatus: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case rid
        case timems
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let rid = rid {
            try encodeContainer.encode(rid, forKey: .rid)
        }
        if timems != 0 {
            try encodeContainer.encode(timems, forKey: .timems)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let timemsDecoded = try containerValues.decode(Int.self, forKey: .timems)
        timems = timemsDecoded
        let ridDecoded = try containerValues.decodeIfPresent(String.self, forKey: .rid)
        rid = ridDecoded
    }
}

extension SuggestStatus: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SuggestStatus(rid: \(String(describing: rid)), timems: \(String(describing: timems)))"}
}

/// <p>Contains the resource id (<code>rid</code>) and the time it took to process the request (<code>timems</code>).</p>
public struct SuggestStatus: Equatable {
    /// <p>The encrypted resource ID for the request.</p>
    public let rid: String?
    /// <p>How long it took to process the request, in milliseconds.</p>
    public let timems: Int

    public init (
        rid: String? = nil,
        timems: Int = 0
    )
    {
        self.rid = rid
        self.timems = timems
    }
}

extension SuggestionMatch: Codable, Reflection {
    enum CodingKeys: String, CodingKey {
        case id
        case score
        case suggestion
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let id = id {
            try encodeContainer.encode(id, forKey: .id)
        }
        if score != 0 {
            try encodeContainer.encode(score, forKey: .score)
        }
        if let suggestion = suggestion {
            try encodeContainer.encode(suggestion, forKey: .suggestion)
        }
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let suggestionDecoded = try containerValues.decodeIfPresent(String.self, forKey: .suggestion)
        suggestion = suggestionDecoded
        let scoreDecoded = try containerValues.decode(Int.self, forKey: .score)
        score = scoreDecoded
        let idDecoded = try containerValues.decodeIfPresent(String.self, forKey: .id)
        id = idDecoded
    }
}

extension SuggestionMatch: CustomDebugStringConvertible {
    public var debugDescription: String {
        "SuggestionMatch(id: \(String(describing: id)), score: \(String(describing: score)), suggestion: \(String(describing: suggestion)))"}
}

/// <p>An autocomplete suggestion that matches the query string specified in a <code>SuggestRequest</code>. </p>
public struct SuggestionMatch: Equatable {
    /// <p>The document ID of the suggested document.</p>
    public let id: String?
    /// <p>The relevance score of a suggested match.</p>
    public let score: Int
    /// <p>The string that matches the query string specified in the <code>SuggestRequest</code>. </p>
    public let suggestion: String?

    public init (
        id: String? = nil,
        score: Int = 0,
        suggestion: String? = nil
    )
    {
        self.id = id
        self.score = score
        self.suggestion = suggestion
    }
}

public struct UploadDocumentsInputBodyMiddleware: Middleware {
    public let id: String = "UploadDocumentsInputBodyMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UploadDocumentsInput>,
                  next: H) -> Swift.Result<OperationOutput<UploadDocumentsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        if let documents = input.operationInput.documents {
            let documentsdata = documents
            let documentsbody = HttpBody.stream(documentsdata)
            input.builder.withBody(documentsbody)
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UploadDocumentsInput>
    public typealias MOutput = OperationOutput<UploadDocumentsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UploadDocumentsOutputError>
}

extension UploadDocumentsInput: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UploadDocumentsInput(contentType: \(String(describing: contentType)), documents: \(String(describing: documents)))"}
}

extension UploadDocumentsInput: Encodable, Reflection {
    enum CodingKeys: String, CodingKey {
        case documents
    }

    public func encode(to encoder: Encoder) throws {
        var encodeContainer = encoder.container(keyedBy: CodingKeys.self)
        if let documents = documents {
            try encodeContainer.encode(documents.toBytes().toData(), forKey: .documents)
        }
    }
}

public struct UploadDocumentsInputHeadersMiddleware: Middleware {
    public let id: String = "UploadDocumentsInputHeadersMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UploadDocumentsInput>,
                  next: H) -> Swift.Result<OperationOutput<UploadDocumentsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        if let contentType = input.operationInput.contentType {
            input.builder.withHeader(name: "Content-Type", value: String(contentType.rawValue))
        }
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UploadDocumentsInput>
    public typealias MOutput = OperationOutput<UploadDocumentsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UploadDocumentsOutputError>
}

public struct UploadDocumentsInputQueryItemMiddleware: Middleware {
    public let id: String = "UploadDocumentsInputQueryItemMiddleware"

    public init() {}

    public func handle<H>(context: Context,
                  input: SerializeStepInput<UploadDocumentsInput>,
                  next: H) -> Swift.Result<OperationOutput<UploadDocumentsOutputResponse>, MError>
    where H: Handler,
    Self.MInput == H.Input,
    Self.MOutput == H.Output,
    Self.Context == H.Context,
    Self.MError == H.MiddlewareError
    {
        input.builder.withQueryItem(URLQueryItem(name: "format", value: "sdk"))
        return next.handle(context: context, input: input)
    }

    public typealias MInput = SerializeStepInput<UploadDocumentsInput>
    public typealias MOutput = OperationOutput<UploadDocumentsOutputResponse>
    public typealias Context = ClientRuntime.HttpContext
    public typealias MError = SdkError<UploadDocumentsOutputError>
}

/// <p>Container for the parameters to the <code>UploadDocuments</code> request.</p>
public struct UploadDocumentsInput: Equatable {
    /// <p>The format of the batch you are uploading. Amazon CloudSearch supports two document batch formats:</p>
    ///       <ul>
    ///          <li>application/json</li>
    ///          <li>application/xml</li>
    ///       </ul>
    public let contentType: ContentType?
    /// <p>A batch of documents formatted in JSON or HTML.</p>
    public let documents: ByteStream?

    public init (
        contentType: ContentType? = nil,
        documents: ByteStream? = nil
    )
    {
        self.contentType = contentType
        self.documents = documents
    }
}

struct UploadDocumentsInputBody: Equatable {
    public let documents: ByteStream?
}

extension UploadDocumentsInputBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case documents
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let documentsDecoded = try containerValues.decodeIfPresent(ByteStream.self, forKey: .documents)
        documents = documentsDecoded
    }
}

extension UploadDocumentsOutputError: HttpResponseBinding {
    public init(httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        let errorDetails = try RestJSONError(httpResponse: httpResponse)
        let requestID = httpResponse.headers.value(for: X_AMZN_REQUEST_ID_HEADER)
        try self.init(errorType: errorDetails.errorType, httpResponse: httpResponse, decoder: decoder, message: errorDetails.errorMessage, requestID: requestID)
    }
}

extension UploadDocumentsOutputError {
    public init(errorType: String?, httpResponse: HttpResponse, decoder: ResponseDecoder? = nil, message: String? = nil, requestID: String? = nil) throws {
        switch errorType {
        case "DocumentServiceException" : self = .documentServiceException(try DocumentServiceException(httpResponse: httpResponse, decoder: decoder, message: message, requestID: requestID))
        default : self = .unknown(UnknownAWSHttpServiceError(httpResponse: httpResponse, message: message, requestID: requestID))
        }
    }
}

public enum UploadDocumentsOutputError: Swift.Error, Equatable {
    case documentServiceException(DocumentServiceException)
    case unknown(UnknownAWSHttpServiceError)
}

extension UploadDocumentsOutputResponse: CustomDebugStringConvertible {
    public var debugDescription: String {
        "UploadDocumentsOutputResponse(adds: \(String(describing: adds)), deletes: \(String(describing: deletes)), status: \(String(describing: status)), warnings: \(String(describing: warnings)))"}
}

extension UploadDocumentsOutputResponse: HttpResponseBinding {
    public init (httpResponse: HttpResponse, decoder: ResponseDecoder? = nil) throws {
        if case .data(let data) = httpResponse.body,
            let unwrappedData = data,
            let responseDecoder = decoder {
            let output: UploadDocumentsOutputResponseBody = try responseDecoder.decode(responseBody: unwrappedData)
            self.adds = output.adds
            self.deletes = output.deletes
            self.status = output.status
            self.warnings = output.warnings
        } else {
            self.adds = 0
            self.deletes = 0
            self.status = nil
            self.warnings = nil
        }
    }
}

/// <p>Contains the response to an <code>UploadDocuments</code> request.</p>
public struct UploadDocumentsOutputResponse: Equatable {
    /// <p>The number of documents that were added to the search domain.</p>
    public let adds: Int
    /// <p>The number of documents that were deleted from the search domain.</p>
    public let deletes: Int
    /// <p>The status of an <code>UploadDocumentsRequest</code>.</p>
    public let status: String?
    /// <p>Any warnings returned by the document service about the documents being uploaded.</p>
    public let warnings: [DocumentServiceWarning]?

    public init (
        adds: Int = 0,
        deletes: Int = 0,
        status: String? = nil,
        warnings: [DocumentServiceWarning]? = nil
    )
    {
        self.adds = adds
        self.deletes = deletes
        self.status = status
        self.warnings = warnings
    }
}

struct UploadDocumentsOutputResponseBody: Equatable {
    public let status: String?
    public let adds: Int
    public let deletes: Int
    public let warnings: [DocumentServiceWarning]?
}

extension UploadDocumentsOutputResponseBody: Decodable {
    enum CodingKeys: String, CodingKey {
        case adds
        case deletes
        case status
        case warnings
    }

    public init (from decoder: Decoder) throws {
        let containerValues = try decoder.container(keyedBy: CodingKeys.self)
        let statusDecoded = try containerValues.decodeIfPresent(String.self, forKey: .status)
        status = statusDecoded
        let addsDecoded = try containerValues.decode(Int.self, forKey: .adds)
        adds = addsDecoded
        let deletesDecoded = try containerValues.decode(Int.self, forKey: .deletes)
        deletes = deletesDecoded
        let warningsContainer = try containerValues.decodeIfPresent([DocumentServiceWarning?].self, forKey: .warnings)
        var warningsDecoded0:[DocumentServiceWarning]? = nil
        if let warningsContainer = warningsContainer {
            warningsDecoded0 = [DocumentServiceWarning]()
            for structure0 in warningsContainer {
                if let structure0 = structure0 {
                    warningsDecoded0?.append(structure0)
                }
            }
        }
        warnings = warningsDecoded0
    }
}
