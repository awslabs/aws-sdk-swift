//
// Copyright Amazon.com Inc. or its affiliates.
// All Rights Reserved.
//
// SPDX-License-Identifier: Apache-2.0
//

// Code generated by smithy-swift-codegen. DO NOT EDIT!

import Foundation
import protocol ClientRuntime.PaginateToken
import struct ClientRuntime.PaginatorSequence

extension CloudWatchOmniClient {
    /// Paginate over `[GetContextGraphOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[GetContextGraphInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `GetContextGraphOutput`
    public func getContextGraphPaginated(input: GetContextGraphInput) -> ClientRuntime.PaginatorSequence<GetContextGraphInput, GetContextGraphOutput> {
        return ClientRuntime.PaginatorSequence<GetContextGraphInput, GetContextGraphOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.getContextGraph(input:))
    }
}

extension GetContextGraphInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> GetContextGraphInput {
        return GetContextGraphInput(
            depth: self.depth,
            edgeFilters: self.edgeFilters,
            endTime: self.endTime,
            includeMetadata: self.includeMetadata,
            maxEdgesPerNode: self.maxEdgesPerNode,
            maxResults: self.maxResults,
            nextToken: token,
            nodeFilters: self.nodeFilters,
            startTime: self.startTime
        )}
}

extension PaginatorSequence where OperationStackInput == GetContextGraphInput, OperationStackOutput == GetContextGraphOutput {
    /// This paginator transforms the `AsyncSequence` returned by `getContextGraphPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.Node]`
    /// - Returns: `[CloudWatchOmniClientTypes.Node]`
    public func nodes() async throws -> [CloudWatchOmniClientTypes.Node] {
        return try await self.asyncCompactMap { item in item.nodes }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[GetTelemetryQueryResultsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[GetTelemetryQueryResultsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `GetTelemetryQueryResultsOutput`
    public func getTelemetryQueryResultsPaginated(input: GetTelemetryQueryResultsInput) -> ClientRuntime.PaginatorSequence<GetTelemetryQueryResultsInput, GetTelemetryQueryResultsOutput> {
        return ClientRuntime.PaginatorSequence<GetTelemetryQueryResultsInput, GetTelemetryQueryResultsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.getTelemetryQueryResults(input:))
    }
}

extension GetTelemetryQueryResultsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> GetTelemetryQueryResultsInput {
        return GetTelemetryQueryResultsInput(
            maxResults: self.maxResults,
            nextToken: token,
            queryId: self.queryId
        )}
}

extension PaginatorSequence where OperationStackInput == GetTelemetryQueryResultsInput, OperationStackOutput == GetTelemetryQueryResultsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `getTelemetryQueryResultsPaginated`
    /// to access the nested member `[[Swift.String: Swift.String]]`
    /// - Returns: `[[Swift.String: Swift.String]]`
    public func rows() async throws -> [[Swift.String: Swift.String]] {
        return try await self.asyncCompactMap { item in item.rows }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListAccessGrantsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListAccessGrantsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListAccessGrantsOutput`
    public func listAccessGrantsPaginated(input: ListAccessGrantsInput) -> ClientRuntime.PaginatorSequence<ListAccessGrantsInput, ListAccessGrantsOutput> {
        return ClientRuntime.PaginatorSequence<ListAccessGrantsInput, ListAccessGrantsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listAccessGrants(input:))
    }
}

extension ListAccessGrantsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListAccessGrantsInput {
        return ListAccessGrantsInput(
            domainId: self.domainId,
            maxResults: self.maxResults,
            nextToken: token,
            permission: self.permission,
            principalId: self.principalId,
            principalType: self.principalType,
            spaceId: self.spaceId
        )}
}

extension PaginatorSequence where OperationStackInput == ListAccessGrantsInput, OperationStackOutput == ListAccessGrantsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listAccessGrantsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.AccessGrantSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.AccessGrantSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.AccessGrantSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListAccessProfilesOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListAccessProfilesInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListAccessProfilesOutput`
    public func listAccessProfilesPaginated(input: ListAccessProfilesInput) -> ClientRuntime.PaginatorSequence<ListAccessProfilesInput, ListAccessProfilesOutput> {
        return ClientRuntime.PaginatorSequence<ListAccessProfilesInput, ListAccessProfilesOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listAccessProfiles(input:))
    }
}

extension ListAccessProfilesInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListAccessProfilesInput {
        return ListAccessProfilesInput(
            maxResults: self.maxResults,
            nextToken: token,
            spaceId: self.spaceId
        )}
}

extension PaginatorSequence where OperationStackInput == ListAccessProfilesInput, OperationStackOutput == ListAccessProfilesOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listAccessProfilesPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.AccessProfileSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.AccessProfileSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.AccessProfileSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListAlertsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListAlertsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListAlertsOutput`
    public func listAlertsPaginated(input: ListAlertsInput) -> ClientRuntime.PaginatorSequence<ListAlertsInput, ListAlertsOutput> {
        return ClientRuntime.PaginatorSequence<ListAlertsInput, ListAlertsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listAlerts(input:))
    }
}

extension ListAlertsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListAlertsInput {
        return ListAlertsInput(
            filterCriteria: self.filterCriteria,
            maxResults: self.maxResults,
            nextToken: token,
            sortBy: self.sortBy,
            sortOrder: self.sortOrder,
            spaceId: self.spaceId
        )}
}

extension PaginatorSequence where OperationStackInput == ListAlertsInput, OperationStackOutput == ListAlertsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listAlertsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.AlertSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.AlertSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.AlertSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListDomainAccessGrantsForOrganizationOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListDomainAccessGrantsForOrganizationInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListDomainAccessGrantsForOrganizationOutput`
    public func listDomainAccessGrantsForOrganizationPaginated(input: ListDomainAccessGrantsForOrganizationInput) -> ClientRuntime.PaginatorSequence<ListDomainAccessGrantsForOrganizationInput, ListDomainAccessGrantsForOrganizationOutput> {
        return ClientRuntime.PaginatorSequence<ListDomainAccessGrantsForOrganizationInput, ListDomainAccessGrantsForOrganizationOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listDomainAccessGrantsForOrganization(input:))
    }
}

extension ListDomainAccessGrantsForOrganizationInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListDomainAccessGrantsForOrganizationInput {
        return ListDomainAccessGrantsForOrganizationInput(
            domainId: self.domainId,
            maxResults: self.maxResults,
            nextToken: token,
            permission: self.permission,
            principalId: self.principalId,
            principalType: self.principalType
        )}
}

extension PaginatorSequence where OperationStackInput == ListDomainAccessGrantsForOrganizationInput, OperationStackOutput == ListDomainAccessGrantsForOrganizationOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listDomainAccessGrantsForOrganizationPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.OrganizationAccessGrantSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.OrganizationAccessGrantSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.OrganizationAccessGrantSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListDomainsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListDomainsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListDomainsOutput`
    public func listDomainsPaginated(input: ListDomainsInput) -> ClientRuntime.PaginatorSequence<ListDomainsInput, ListDomainsOutput> {
        return ClientRuntime.PaginatorSequence<ListDomainsInput, ListDomainsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listDomains(input:))
    }
}

extension ListDomainsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListDomainsInput {
        return ListDomainsInput(
            maxResults: self.maxResults,
            nextToken: token
        )}
}

extension PaginatorSequence where OperationStackInput == ListDomainsInput, OperationStackOutput == ListDomainsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listDomainsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.DomainSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.DomainSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.DomainSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListIntegrationsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListIntegrationsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListIntegrationsOutput`
    public func listIntegrationsPaginated(input: ListIntegrationsInput) -> ClientRuntime.PaginatorSequence<ListIntegrationsInput, ListIntegrationsOutput> {
        return ClientRuntime.PaginatorSequence<ListIntegrationsInput, ListIntegrationsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listIntegrations(input:))
    }
}

extension ListIntegrationsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListIntegrationsInput {
        return ListIntegrationsInput(
            integrationType: self.integrationType,
            maxResults: self.maxResults,
            name: self.name,
            nextToken: token,
            status: self.status
        )}
}

extension PaginatorSequence where OperationStackInput == ListIntegrationsInput, OperationStackOutput == ListIntegrationsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listIntegrationsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.Integration]`
    /// - Returns: `[CloudWatchOmniClientTypes.Integration]`
    public func items() async throws -> [CloudWatchOmniClientTypes.Integration] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListOmniDashboardsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListOmniDashboardsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListOmniDashboardsOutput`
    public func listOmniDashboardsPaginated(input: ListOmniDashboardsInput) -> ClientRuntime.PaginatorSequence<ListOmniDashboardsInput, ListOmniDashboardsOutput> {
        return ClientRuntime.PaginatorSequence<ListOmniDashboardsInput, ListOmniDashboardsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listOmniDashboards(input:))
    }
}

extension ListOmniDashboardsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListOmniDashboardsInput {
        return ListOmniDashboardsInput(
            maxResults: self.maxResults,
            namePrefix: self.namePrefix,
            nextToken: token,
            spaceId: self.spaceId
        )}
}

extension PaginatorSequence where OperationStackInput == ListOmniDashboardsInput, OperationStackOutput == ListOmniDashboardsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listOmniDashboardsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.OmniDashboardSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.OmniDashboardSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.OmniDashboardSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListSpacesOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListSpacesInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListSpacesOutput`
    public func listSpacesPaginated(input: ListSpacesInput) -> ClientRuntime.PaginatorSequence<ListSpacesInput, ListSpacesOutput> {
        return ClientRuntime.PaginatorSequence<ListSpacesInput, ListSpacesOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listSpaces(input:))
    }
}

extension ListSpacesInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListSpacesInput {
        return ListSpacesInput(
            domainId: self.domainId,
            maxResults: self.maxResults,
            nextToken: token
        )}
}

extension PaginatorSequence where OperationStackInput == ListSpacesInput, OperationStackOutput == ListSpacesOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listSpacesPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.SpaceSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.SpaceSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.SpaceSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListSpacesForOrganizationOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListSpacesForOrganizationInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListSpacesForOrganizationOutput`
    public func listSpacesForOrganizationPaginated(input: ListSpacesForOrganizationInput) -> ClientRuntime.PaginatorSequence<ListSpacesForOrganizationInput, ListSpacesForOrganizationOutput> {
        return ClientRuntime.PaginatorSequence<ListSpacesForOrganizationInput, ListSpacesForOrganizationOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listSpacesForOrganization(input:))
    }
}

extension ListSpacesForOrganizationInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListSpacesForOrganizationInput {
        return ListSpacesForOrganizationInput(
            maxResults: self.maxResults,
            nextToken: token
        )}
}

extension PaginatorSequence where OperationStackInput == ListSpacesForOrganizationInput, OperationStackOutput == ListSpacesForOrganizationOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listSpacesForOrganizationPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.SpaceSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.SpaceSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.SpaceSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListTelemetryFieldsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListTelemetryFieldsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListTelemetryFieldsOutput`
    public func listTelemetryFieldsPaginated(input: ListTelemetryFieldsInput) -> ClientRuntime.PaginatorSequence<ListTelemetryFieldsInput, ListTelemetryFieldsOutput> {
        return ClientRuntime.PaginatorSequence<ListTelemetryFieldsInput, ListTelemetryFieldsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listTelemetryFields(input:))
    }
}

extension ListTelemetryFieldsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListTelemetryFieldsInput {
        return ListTelemetryFieldsInput(
            dataSetName: self.dataSetName,
            endTime: self.endTime,
            nextToken: token,
            startTime: self.startTime,
            telemetryType: self.telemetryType
        )}
}

extension PaginatorSequence where OperationStackInput == ListTelemetryFieldsInput, OperationStackOutput == ListTelemetryFieldsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listTelemetryFieldsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.Field]`
    /// - Returns: `[CloudWatchOmniClientTypes.Field]`
    public func fields() async throws -> [CloudWatchOmniClientTypes.Field] {
        return try await self.asyncCompactMap { item in item.fields }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListTelemetryQuerySessionsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListTelemetryQuerySessionsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListTelemetryQuerySessionsOutput`
    public func listTelemetryQuerySessionsPaginated(input: ListTelemetryQuerySessionsInput) -> ClientRuntime.PaginatorSequence<ListTelemetryQuerySessionsInput, ListTelemetryQuerySessionsOutput> {
        return ClientRuntime.PaginatorSequence<ListTelemetryQuerySessionsInput, ListTelemetryQuerySessionsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listTelemetryQuerySessions(input:))
    }
}

extension ListTelemetryQuerySessionsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListTelemetryQuerySessionsInput {
        return ListTelemetryQuerySessionsInput(
            maxResults: self.maxResults,
            nextToken: token
        )}
}

extension PaginatorSequence where OperationStackInput == ListTelemetryQuerySessionsInput, OperationStackOutput == ListTelemetryQuerySessionsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listTelemetryQuerySessionsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.SessionSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.SessionSummary]`
    public func sessions() async throws -> [CloudWatchOmniClientTypes.SessionSummary] {
        return try await self.asyncCompactMap { item in item.sessions }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[ListViewsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[ListViewsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `ListViewsOutput`
    public func listViewsPaginated(input: ListViewsInput) -> ClientRuntime.PaginatorSequence<ListViewsInput, ListViewsOutput> {
        return ClientRuntime.PaginatorSequence<ListViewsInput, ListViewsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.listViews(input:))
    }
}

extension ListViewsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> ListViewsInput {
        return ListViewsInput(
            maxResults: self.maxResults,
            nextToken: token,
            type: self.type
        )}
}

extension PaginatorSequence where OperationStackInput == ListViewsInput, OperationStackOutput == ListViewsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `listViewsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.ViewSummary]`
    /// - Returns: `[CloudWatchOmniClientTypes.ViewSummary]`
    public func items() async throws -> [CloudWatchOmniClientTypes.ViewSummary] {
        return try await self.asyncCompactMap { item in item.items }
    }
}
extension CloudWatchOmniClient {
    /// Paginate over `[SearchPrincipalsOutput]` results.
    ///
    /// When this operation is called, an `AsyncSequence` is created. AsyncSequences are lazy so no service
    /// calls are made until the sequence is iterated over. This also means there is no guarantee that the request is valid
    /// until then. If there are errors in your request, you will see the failures only after you start iterating.
    /// - Parameters:
    ///     - input: A `[SearchPrincipalsInput]` to start pagination
    /// - Returns: An `AsyncSequence` that can iterate over `SearchPrincipalsOutput`
    public func searchPrincipalsPaginated(input: SearchPrincipalsInput) -> ClientRuntime.PaginatorSequence<SearchPrincipalsInput, SearchPrincipalsOutput> {
        return ClientRuntime.PaginatorSequence<SearchPrincipalsInput, SearchPrincipalsOutput>(input: input, inputKey: \.nextToken, outputKey: \.nextToken, paginationFunction: self.searchPrincipals(input:))
    }
}

extension SearchPrincipalsInput: ClientRuntime.PaginateToken {
    public func usingPaginationToken(_ token: Swift.String) -> SearchPrincipalsInput {
        return SearchPrincipalsInput(
            domainId: self.domainId,
            maxResults: self.maxResults,
            nextToken: token,
            searchQuery: self.searchQuery
        )}
}

extension PaginatorSequence where OperationStackInput == SearchPrincipalsInput, OperationStackOutput == SearchPrincipalsOutput {
    /// This paginator transforms the `AsyncSequence` returned by `searchPrincipalsPaginated`
    /// to access the nested member `[CloudWatchOmniClientTypes.PrincipalSearchResult]`
    /// - Returns: `[CloudWatchOmniClientTypes.PrincipalSearchResult]`
    public func results() async throws -> [CloudWatchOmniClientTypes.PrincipalSearchResult] {
        return try await self.asyncCompactMap { item in item.results }
    }
}
